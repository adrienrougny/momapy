{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"momapy","text":"<p>momapy is a library for working with molecular maps. It currently supports SBGN and CellDesigner maps. Its key feature is its definition of a map, that is formed of two entities: a model, that describes what concepts are represented, and a layout, that describes how these concepts are represented. This definition is borrowed from SBML and its extensions layout+render, that allow users to add a layout to an SBML model. momapy aims at extending this definition to SBGN and CellDesigner maps.</p> <p>Features of momapy include the following:</p> <ul> <li>support for SBGN PD and AF maps (read/write SBGN-ML with annotations, rendering information, and notes) and CellDesigner (read/write, with annotations and notes)</li> <li>read-only support for SBML (model only, no layout)</li> <li>decomposition of a map object into:<ul> <li>a model object;</li> <li>a layout object;</li> <li>a mapping from layout element objects to model element objects.</li> </ul> </li> <li>map, model, layout and mapping objects comparison; fast object in set checking</li> <li>rendering of maps to images (SVG, PDF, JPEG, PNG, WebP), or onto a Skia canvas for live rendering (via the <code>skia</code> renderer)</li> <li>support for styling and CSS-like stylesheets (including effects such as shadows)</li> <li>automatic geometry and anchors (for arcs, shape borders)</li> <li>local positioning (e.g. right of shape, fit set of shapes)</li> <li>easy extension with new model and layout element types</li> </ul>"},{"location":"#installation","title":"Installation","text":"<p>momapy is available as a Python package and can be installed with pip as follows (Python &gt;=3.10,&lt;=3.14):</p> <p><code>pip install momapy</code></p>"},{"location":"#optional-dependencies","title":"Optional dependencies","text":"<p>momapy includes several optional dependencies for rendering maps with the skia or cairo backends:</p> <p>skia</p> <p><code>pip install momapy[skia]</code></p> <p>This extra depends on skia-python (https://github.com/kyamagu/skia-python), which itself depends on the following system packages:</p> <ul> <li>opengl</li> <li>libegl</li> <li>fontconfig</li> </ul> <p>cairo</p> <p><code>pip install momapy[cairo]</code></p> <p>This extra depends on pygobject (https://pygobject.gnome.org/guide/sysdeps.html), which itself depends on the following system packages:</p> <ul> <li>glib</li> <li>libffi</li> </ul> <p>all</p> <p>All optional dependencies can also be installed together:</p> <p><code>pip install momapy[all]</code></p>"},{"location":"#usage","title":"Usage","text":"<p>Typical usage of momapy includes reading a map and exploring its model:</p> <pre><code>from momapy.io import read\n\nmap_ = read(\"my_map.sbgn\").obj\nfor process in map_.model.processes:\n    print(process)\n</code></pre> <p>Or rendering its layout:</p> <pre><code>from momapy.io import read\nfrom momapy.rendering import render_map\n\nmap_ = read(\"my_map.sbgn\").obj\nrender_map(map_, \"my_file.svg\")\n</code></pre> <p>momapy also provides a command-line interface:</p> <pre><code>momapy render my_map.sbgn -o output.pdf\nmomapy export my_map.sbgn -o output.sbgn\n</code></pre> <p>See CLI reference for details.</p> <p>You can also customize the appearance using CSS-like stylesheets:</p> <pre><code>from momapy.styling import StyleSheet, apply_style_sheet\n\nstyle_sheet = StyleSheet.from_file(\"my_style.css\")\nstyled_map = apply_style_sheet(map_, style_sheet)\n</code></pre>"},{"location":"#user-manual","title":"User manual","text":"<p>A user manual showcasing the main feature of momapy is available here: User manual.</p>"},{"location":"#documentation","title":"Documentation","text":"<p>A complete documentaton for momapy's API is available here: API reference.</p>"},{"location":"CHANGELOG/","title":"Changelog","text":"<p>All notable changes to momapy will be documented in this file.</p> <p>The format is based on Keep a Changelog, and this project adheres to Conventional Commits.</p>"},{"location":"CHANGELOG/#0130-2026-08-06","title":"[0.13.0] - 2026-08-06","text":""},{"location":"CHANGELOG/#added","title":"Added","text":"<ul> <li>Support Bbox in the directional _of functions (positioning)*</li> <li>Support pretty_print(max_depth=None) for unlimited depth (utils)</li> <li>Support info on SBML maps and fail cleanly on unsupported types (cli)</li> <li>Add -T/--text to emit XML to stdout instead of pickle (cli)</li> <li>Accept style presets in render/export/visualize (cli)</li> <li>Add get_color, fast has_color, name-only list_colors (coloring)</li> <li>Add rgb_range/alpha_range to from_rgb/from_rgba (coloring)</li> <li>Make rendering bases and IOResult officially public (api)</li> <li>Auto-detect writer from map type; collapse SBGN-ML writer to one class (io)</li> <li>Give a clear read-only error when exporting SBML (cli)</li> <li>Add with_annotations/with_notes and type hints to write (io.pickle)</li> <li>Thread source_id_to_annotations/notes through writers (io)</li> <li>Expose map-building default font sizes as public API (sbgn,celldesigner)</li> <li>Add SBMLMap and uniform read() return contract (sbml)</li> <li>Expose momapy.version</li> </ul>"},{"location":"CHANGELOG/#changed","title":"Changed","text":"<ul> <li>Extract build_subunit_to_complex to _writing (celldesigner.io)</li> <li>Drop dead code and an unused parameter, add sbml.utils all</li> <li>Fit delegates bbox merging to Bbox.union (positioning)</li> <li>Drop the never-read sbml_id_to_sbml_element context state (sbml.io)</li> <li>Drop redundant all from io/core.py (io)</li> <li>Rename data-transform make_ helpers to get_ (celldesigner.io)</li> <li>Rename builtin-shadowing filter field to filter_</li> <li>Drop vestigial Compartment.state_variables field (sbgn.pd)</li> <li>Compose TagLayout from SBGNNode instead of PD TagLayout (sbgn.af)</li> <li>Make UnitOfInformation extend SBGNAuxiliaryUnit (sbgn.af)</li> <li>Drop dead tuple handling in _build_layout_to_model_id_mapping (cli)</li> <li>Drop dead style_sheet param from visualize helpers (cli)</li> <li>Remove unused get_model_and_layout_classes wrapper (sbgn.io.sbgnml)</li> <li>Remove dead _make_style_sheet and its unused imports (sbgn.io.sbgnml)</li> <li>Make renderer output-config fully private (rendering)</li> <li>Remove unused _FONT_WEIGHT_TO_VALUE constant (drawing)</li> <li>Use module-qualified dataclasses in the three outlier files</li> <li>Use full-word locals in Segment.get_distance_to_point (geometry)</li> <li>Make to_rgba/to_rgb return floats with opt-in round_to_int (coloring)</li> <li>Drop duplicated MapInfo TypedDicts from get_info (sbgn,celldesigner)</li> <li>Make Terminal extend SBGNAuxiliaryUnit like PD (sbgn.af)</li> <li>Move list-attributes presentation flag to -P (cli)</li> <li>Extract KEY_TO_CLASS to _reading_classification (celldesigner.io)</li> <li>Make make_empty_ reader classmethods (celldesigner.io)*</li> <li>Rename build_make_sbml_element to make_sbml_document (celldesigner.io)</li> <li>Align XML-helper arg names with sbgnml (celldesigner.io)</li> <li>Move file-existence check to utils.check_file_exists (io)</li> <li>Share format constants via io _constants module (celldesigner)</li> <li>Tidy public surface via all conventions</li> <li>Harmonize from_file signature and default_format (rendering)</li> <li>Remove unused SBML root document class (sbml)</li> <li>Drop find_font from top-level core re-exports (core)</li> <li>Rename diagonal distance args to distance_y/distance_x (positioning)</li> <li>Make builder registry private (_builders) (builder)</li> <li>Make I/O result dataclasses kw_only (io)</li> <li>Default map model and layout to None (sbgn)</li> <li>Share SimpleSpeciesReference base across all role classes (celldesigner)</li> <li>Unify the referenced-element field to referred_element</li> <li>Split Direction into Direction (cardinals) + Orientation (axes) (core)</li> <li>Use public momapy.io read/write imports (cli)</li> <li>Unify apply_style_sheet call target (cli)</li> <li>Add TypedDict return type for get_info (sbgn,celldesigner)</li> <li>Raise RuntimeError on save/restore underflow (rendering)</li> <li>Drop redundant reversible redeclaration (celldesigner)</li> <li>Parameterize reading-context collection fields (io)</li> <li>Add type hints to pretty_print, display, _prepare_layout_elements</li> <li>Add TypeVar and signatures to collection generics (utils)</li> <li>Populate all ReaderResult fields in read (sbml.io)</li> <li>Annotate options on abstract Writer.write (io)</li> <li>Annotate is_submodel other param (sbgn.pd)</li> <li>Mark register_model_element internal as _register_model_element (io)</li> <li>Declare public all for nodes, arcs, shapes (meta)</li> <li>Export Reader/Writer and define module all (io)</li> <li>Make FilterEffect base kw_only (drawing)</li> <li>Drop divergent id_to_element write param (io.pickle)</li> <li>Give filter enums string values (drawing)</li> <li>Relax SBGNPDMap model/layout to optional (sbgn.pd)</li> <li>Unify Tag/Terminal field to referred_element (sbgn)</li> <li>Rename reader modules to convention (sbml.io)</li> <li>Typed universal reader option set + contract docs (io)</li> <li>Remove the unused omit_keys parameter (builder)</li> <li>Drop keyword-only marker from tidy helpers (sbgn,celldesigner)</li> <li>Make Renderer.render_map a concrete convenience method (rendering)</li> <li>Add all to flat modules, re-export plugins, document meta</li> <li>Drop dead with_render_information, accept-and-ignore options uniformly (io)</li> <li>Remove dead monitoring module, fix Builder eq/hash (builder)</li> <li>Make Model abstract via abc.ABC (core)</li> <li>Add SupportsFileOutput mixin for file-output capability; unify from_file (rendering)</li> </ul>"},{"location":"CHANGELOG/#documentation","title":"Documentation","text":"<ul> <li>Fix three stale CellDesigner/SBGN-AF inventory entries (api-reference)</li> <li>Fix inaccurate and copy-paste field/module docstrings</li> <li>Mark conceptually-abstract model/layout base classes as abstract (model)</li> <li>Fix CellDesigner modulator hierarchy in API_REFERENCE</li> <li>Fix sbgnml _writing.py cross-reference and merge duplicate section</li> <li>Fix PD Process hierarchy in API_REFERENCE</li> <li>Fix DEGRADED classification None slot in API_REFERENCE</li> <li>Document NoneValue singleton in API_REFERENCE drawing section</li> <li>Document public geometry functions in API_REFERENCE</li> <li>Add Cairo backend to api_reference overview</li> <li>Capture styled map from apply_style_sheet in examples (styling)</li> <li>Fix descendant-selector example to use GroupLayout (styling)</li> <li>Correct unknown-property behavior note (styling)</li> <li>Remove nonexistent opacity property from properties table (styling)</li> <li>Remove nonexistent Degraded model class from API_REFERENCE</li> <li>List all supported formats in package docstring (rendering)</li> <li>Drop nonexistent Vector class from geometry.py structure comment</li> <li>Bracket optional input/-o in export synopsis (cli)</li> <li>Document stdin fallback for info and visualize (cli)</li> <li>Remove unsupported --xsep/--ysep from snap-arcs example (cli)</li> <li>Drop non-running Examples block from _run (cli)</li> <li>Use frozenset() in model module docstring example (sbml)</li> <li>Soften Layout-suffix rule in layout.py docstring (celldesigner)</li> <li>Fix modifications field descriptions on species (celldesigner)</li> <li>Fix elements.py module docstring to mark bases public (celldesigner)</li> <li>Correct xsep/ysep read() docstring (sbgn.io.sbgnml)</li> <li>Give is_submodel/is_ovav full Google-style docstrings (sbgn.pd)</li> <li>Drop non-running Examples blocks from base layout/map classes (sbgn)</li> <li>Fix bare Point() in cairo/skia renderer examples (rendering)</li> <li>Fix bare Point() in shapes/arcs/nodes examples per import style (meta)</li> <li>Mark Text.transformed transformation param as unused (drawing)</li> <li>Fix module docstring example imports (geometry)</li> <li>Document both lookup directions in Map.get_mapping (core.map)</li> <li>Fix Node.east_south_east docstring typo (core.layout)</li> <li>Correct SBGN-ML KEY_TO_CLASS value type to tuple[type | None, type] (API_REFERENCE)</li> <li>Document Renderer.get_bolder/get_lighter_font_weight (API_REFERENCE)</li> <li>Remove invented language-concept definitions and homogenize model/layout docstrings</li> <li>Qualify Point in Cairo and Skia renderer docstring examples (rendering)</li> <li>Add required rx/ry to module docstring Rectangle example (drawing)</li> <li>Replace GLFW-window claim with skia live-rendering note</li> <li>Sync cli.md with the current command-line interface (cli)</li> <li>Fix copy-paste errors in layout/model/tidy docstrings</li> <li>Drop nonexistent PD EmptySet, fix LogicalOperatorInput type (api-reference)</li> <li>Import from public paths in docstring/guide examples</li> <li>Fix stylesheet examples so they actually parse (styling)</li> <li>Note CompositeEffect in_/in2 map to SVG feComposite (drawing)</li> <li>Enrich top-level CLI description (cli)</li> <li>Fix tidy example to use a real op (cli)</li> <li>Document private I/O module layout and privacy convention (io)</li> <li>Note read-only SBML support and self-contain render example</li> <li>Add Cairo renderer API reference page</li> <li>Fix lowercase true/false to True/False in predicate docstrings</li> <li>Document dataclass fields via field metadata + griffe backfill extension</li> <li>Upgrade node, arc, and shape class docstrings to Google-style (meta)</li> <li>Upgrade model and layout class docstrings to Google-style (sbgn.pd)</li> <li>Upgrade reader/writer docstrings to Google-style (io)</li> <li>Note universal read options may be inert per format (io)</li> <li>Note element base classes/mixins are internal (sbgn,celldesigner)</li> <li>Mark context dataclasses internal in public-surface notes (io)</li> <li>Sbml read docs in API_REFERENCE (sbml)</li> <li>Document top-level momapy package as deliberately re-exporting only version</li> <li>Annotate get_filter_region as returning Bbox | None (drawing)</li> <li>Add a loose versioning and deprecation policy</li> <li>Note CD writer recomputes link geometry on round-trip (celldesigner.io)</li> <li>Document the Layout-suffix naming rule for layout classes</li> <li>Document the two descendants() semantics (containment vs reachability) (core)</li> <li>Add type hints and docstrings to public core and Renderer methods</li> <li>Refresh project structure, CLI subcommands, mapping wording</li> <li>Fix source_id_to_layout_element docstring (io)</li> <li>Drop stale result.exceptions reference</li> <li>Reframe I/O make_* as internal helpers, not public contract</li> </ul>"},{"location":"CHANGELOG/#fixed","title":"Fixed","text":"<ul> <li>Detect file format independently of the locale encoding (io)</li> <li>Pin ruff to the 0.14 line for the lint gate (ci)</li> <li>Pick the negative-modulation class from the target (celldesigner.io)</li> <li>Pick the negative-modulation class from the target (celldesigner.io)</li> <li>Emit map-level annotations and notes on write (sbgn.io.sbgnml)</li> <li>Guard the info model block against a None model (cli)</li> <li>Draw Rectangle top-left cut corner explicitly (meta.shapes)</li> <li>Make StyleSheet.ior merge in place (styling)</li> <li>StyleSheet.from_files returns an empty StyleSheet on empty input (styling)</li> <li>Derive get_info map_type from the map class, not the model (sbgn.utils)</li> <li>Round-trip self-modulations without None endpoints (celldesigner.io)</li> <li>Widen PickleWriter.write obj type to Any (io.pickle)</li> <li>Honor the ReaderResult id-table contract on round-trip (io.pickle)</li> <li>Make SBGN layout bases keyword-only to hide positional style params (sbgn)</li> <li>Default Point.round() (geometry)</li> <li>Reject empty input with a clear error, consistently (rendering)</li> <li>Guard Renderer.render_map against a layout-less map (rendering)</li> <li>Keep a label-less stoichiometry glyph in the layout (sbgn.io)</li> <li>Raise a clear error on a segment-less arc (core)</li> <li>Degrade a zero-radius EllipticalArc to a straight line (geometry)</li> <li>Round-trip bare Model/Layout pickles by object shape (io.pickle)</li> <li>Guard is_submap against an incomplete other map (core)</li> <li>Exit cleanly on read and render errors instead of dumping a traceback (cli)</li> <li>Guard get_info against a None model like the SBGN/CD siblings (sbml)</li> <li>Give DoubleHeadedArc its own group a distinct _own id (core)</li> <li>Key skia blur edge_mode on NoneValue; document NoneValue vs None (rendering)</li> <li>Parse an empty stylesheet to an empty StyleSheet (styling)</li> <li>Raise a clear error when styling a map without a layout (styling)</li> <li>Place complex label at label_center when fitting content (celldesigner)</li> <li>Port-resolve modulation arc target like the source (sbgn.io)</li> <li>Serialize ModificationState.SULFATED instead of dropping it (celldesigner)</li> <li>Handle None model/layout uniformly in get_info (sbgn,celldesigner)</li> <li>Default root layout background to white like SBGN (celldesigner)</li> <li>Make apply_style_sheet ancestors param explicitly Optional (styling)</li> <li>Drop redundant Any | None on ReaderResult/WriterResult.obj (io.core)</li> <li>Use modern union syntax for SVGElement.value (rendering)</li> <li>Annotate Point.iter return type (geometry)</li> <li>Narrow Bezier get_intersection_with_line to list[Point] (geometry)</li> <li>Widen get_mapping return to include frozenset keys (core.mapping)</li> <li>Annotate super_or_builder return as super, not typing.Type (builder)</li> <li>Restore NoneValueType on DoubleHeadedArc arrowhead stroke fields (core.layout)</li> <li>Add IOResult to io.core all (io)</li> <li>Nest Catalysis under PhysicalStimulation to match the modifier side (celldesigner)</li> <li>Correct PALMYTOYLATED to PALMITOYLATED so palmitoylation round-trips (celldesigner)</li> <li>Reject rendering/visualizing a layout-less map with a clean error (cli)</li> <li>Return a frozen map from set_nodes_to_fit_labels when both omit flags set (celldesigner)</li> <li>Make Selector a real abc.ABC to match its docstring and abstractmethod (styling)</li> <li>Fall back to composite id for species references without metaid (sbml.io)</li> <li>Implement SBMLModel.is_submodel subset check instead of no-op stub (sbml)</li> <li>Reject writing a map without both a model and a layout (io)</li> <li>Give _ReactionNodeMixin a default _font_size_func (celldesigner)</li> <li>Grey Dissociation/Truncation arrowheads in highlight_layout_elements (celldesigner)</li> <li>Remove dead branch and add explicit fallback in is_process_left_to_right (sbgn.io.sbgnml)</li> <li>Rename dead border_stroke_width to stroke_width so AF borders render thick (sbgn.af)</li> <li>Include the top-level Model in build_id_mappings id_to_element (io)</li> <li>Raise clear ValueError when rendering a layout-less map (rendering)</li> <li>Exclude id_ from get_stylable_attributes so listed names round-trip (styling)</li> <li>Resolve percentage filter regions bbox-relative under USER_SPACE_ON_USE (drawing)</li> <li>Clamp Segment.shortened so over-shortening yields a zero-length segment (geometry)</li> <li>Clamp EllipticalArc.shortened and make degenerate arcs well-defined (geometry)</li> <li>Correct EllipticalArc.transformed minor-axis probe and radians output (geometry,drawing)</li> <li>Return empty list from Arc.points() for a segment-less arc (core)</li> <li>Make GroupLayout.own_bbox/own_to_geometry self-only (core)</li> <li>Return an all-None map when reading with_model=False and with_layout=False (io)</li> <li>Import Point so pretty_print works without a preloaded momapy.geometry (utils)</li> <li>Read sweep_flag not seep_flag when building elliptical-arc path action (core)</li> <li>Default Reaction.reversible to False (sbml)</li> <li>Surface SBML read-only error cleanly in export/style (cli)</li> <li>Gate fit-complexes to SBGN-PD and report tidy errors cleanly (cli)</li> <li>Honor --no-snap-arcs in fit-complexes/compartments/submaps (cli)</li> <li>List supported formats in unsupported-format error (rendering)</li> <li>Default multi_pages to False to match the CLI (rendering)</li> <li>Populate ReaderResult id_to_element via build_id_mappings (sbml.io)</li> <li>Raise FileNotFoundError up front for missing input files (io)</li> <li>Raise clear error on invalid return_type (io)</li> <li>Fill in element types for bare dict/list annotations (types)</li> <li>Parameterize plugin registries with their element type (types)</li> <li>Type get_drawing_elements_angle unit as Literal (types)</li> <li>Type sequence-indexing args as Sequence not Collection (types)</li> <li>Add | None to nullable return annotations (types)</li> <li>Annotate variable-length tuple fields as tuple[X, ...] (types)</li> <li>Annotate Color.or return type as Self (coloring)</li> <li>Round alpha in to_hexa for exact hex round-trip (coloring)</li> <li>Make the | alpha operator use the 0.0-1.0 scale (coloring)</li> <li>Raise clear error for unknown SBGN-ML 0.3 map version (sbgn.io)</li> <li>Center Bar shape on its position (meta)</li> <li>Only fit complex to its own subunit layouts when tidying (celldesigner)</li> <li>Make NoneValue pickle-stable so identity survives round-trips (drawing)</li> <li>Error-contract cleanups for @import, registry getters, get_initial_value, CLI usage</li> <li>Auto-detect readers in registration order so CellDesigner precedes SBML (io)</li> <li>Export to file preserves annotations and notes (cli)</li> <li>To_rgb no longer truncates fractional ranges (coloring)</li> <li>Validate with_alpha range, document or percent scale (coloring)</li> <li>Writer.write returns WriterResult instead of None (sbgn.io.sbgnml)</li> </ul>"},{"location":"CHANGELOG/#miscellaneous","title":"Miscellaneous","text":"<ul> <li>Drop leftover momapy.transform references</li> <li>Adopt ruff D+ANN quality gate and bring the tree into compliance</li> </ul>"},{"location":"CHANGELOG/#styling","title":"Styling","text":"<ul> <li>Correct inaccurate type annotations and numeric literals</li> <li>Normalize int literals to float for float-typed fields/params</li> <li>Use from-style absolute imports across the codebase</li> <li>Compare NoneValue by identity (is / is not) (rendering)</li> <li>Del leaked registration loop variables (io,rendering)</li> </ul>"},{"location":"CHANGELOG/#0120-2026-06-15","title":"[0.12.0] - 2026-06-15","text":""},{"location":"CHANGELOG/#added_1","title":"Added","text":"<ul> <li>Expose display() and print_source() with optional notebook deps (utils)</li> </ul>"},{"location":"CHANGELOG/#changed_1","title":"Changed","text":"<ul> <li>Unify the six mapping classes on one frozendict .inverse contract (utils)</li> <li>Drop vestigial no-op replace parameter from add_mapping (core.mapping)</li> </ul>"},{"location":"CHANGELOG/#documentation_1","title":"Documentation","text":"<ul> <li>Regenerate API_REFERENCE.md and fix stale meta docstrings</li> </ul>"},{"location":"CHANGELOG/#fixed_1","title":"Fixed","text":"<ul> <li>Correct auxiliary unit sizing and rendering defaults (sbgn)</li> <li>Serialize AF units of information with entity child (sbgn.io.sbgnml)</li> <li>Enlarge default unit-of-information layout sizes (sbgn.af)</li> </ul>"},{"location":"CHANGELOG/#styling_1","title":"Styling","text":"<ul> <li>Remove unused imports, fix ambiguous names, drop duplicate test</li> </ul>"},{"location":"CHANGELOG/#0112-2026-06-05","title":"[0.11.2] - 2026-06-05","text":""},{"location":"CHANGELOG/#changed_2","title":"Changed","text":"<ul> <li>Resolve residue angle via mapping, not layout-id parsing (celldesigner.writer)</li> </ul>"},{"location":"CHANGELOG/#fixed_2","title":"Fixed","text":"<ul> <li>Pass module name string to LogicalOperator ForwardRef (sbgn.model)</li> <li>Key modification/structural-state layout ids on alias, not species (celldesigner.reader)</li> <li>Project writer ids onto valid, unique, consistent XML ids (io)</li> <li>Key subunit_to_complex by id() to distinguish content-equal species (celldesigner.writer)</li> </ul>"},{"location":"CHANGELOG/#testing","title":"Testing","text":"<ul> <li>Added FOXO3_activity map to celldesigner testing</li> </ul>"},{"location":"CHANGELOG/#0111-2026-05-05","title":"[0.11.1] - 2026-05-05","text":""},{"location":"CHANGELOG/#changed_3","title":"Changed","text":"<ul> <li>Identity-keyed multidict for xml_id/source_id model element mappings (io)</li> <li>Identity-keyed inverse for LayoutModelMapping (core.mapping)</li> </ul>"},{"location":"CHANGELOG/#fixed_3","title":"Fixed","text":"<ul> <li>Source subunit alias ids from model walk, not mapping lookup (celldesigner.writer)</li> </ul>"},{"location":"CHANGELOG/#0110-2026-05-04","title":"[0.11.0] - 2026-05-04","text":""},{"location":"CHANGELOG/#added_2","title":"Added","text":"<ul> <li>Replace Degraded model class with layout-driven emission for external flux (celldesigner)</li> <li>Add snap_arcs option to size-changing tidy helpers (sbgn,celldesigner,cli)</li> <li>Clean up visualize page title and add favicon (cli)</li> <li>Implement arithmetic feComposite operator and fix ARTIHMETIC typo (drawing,rendering)</li> <li>Re-export ReaderResult and WriterResult from momapy.io (io)</li> </ul>"},{"location":"CHANGELOG/#changed_4","title":"Changed","text":"<ul> <li>Replace EmptySet/Degraded model elements with process flags (sbgn,celldesigner)</li> <li>Expose font and text attributes as instance fields (sbgn,celldesigner)</li> </ul>"},{"location":"CHANGELOG/#documentation_2","title":"Documentation","text":"<ul> <li>Document external source/sink invariant on Reaction and StoichiometricProcess (model)</li> <li>Add ID assignment sections to SBGN-ML and CellDesigner I/O pages (api)</li> <li>Use favicon as header logo</li> <li>Add favicon and switch yellow brand color to amber</li> <li>Add API reference landing page (api)</li> <li>Align API reference with public re-export facades (api)</li> </ul>"},{"location":"CHANGELOG/#fixed_4","title":"Fixed","text":"<ul> <li>Resolve modulation alias layouts from arc, not model identity (celldesigner.writer)</li> <li>Exclude compartments from tidy's label-fit step (celldesigner.utils)</li> <li>Skip modulation referencing degraded species (celldesigner)</li> <li>Add pickling of _singleton_to_key (layout model mapping)</li> <li>Mark _MultiMixin._make_subunit_shape as abstractmethod (sbgn)</li> <li>Resolve relative @import against stylesheet directory; remove os.chdir hack (styling)</li> </ul>"},{"location":"CHANGELOG/#miscellaneous_1","title":"Miscellaneous","text":"<ul> <li>Remove speculative # TO DO markers (builder)</li> <li>Remove resolved TODO comments on Reactant.base and Product.base (celldesigner)</li> </ul>"},{"location":"CHANGELOG/#other","title":"Other","text":"<ul> <li>Merge branch 'develop'</li> </ul>"},{"location":"CHANGELOG/#styling_2","title":"Styling","text":"<ul> <li>Align species default sizes and colors with CellDesigner conventions (celldesigner.layout)</li> </ul>"},{"location":"CHANGELOG/#0104-2026-04-23","title":"[0.10.4] - 2026-04-23","text":""},{"location":"CHANGELOG/#added_3","title":"Added","text":"<ul> <li>Expose per-source-id annotations and notes on ReaderResult (io)</li> </ul>"},{"location":"CHANGELOG/#documentation_3","title":"Documentation","text":"<ul> <li>Document ReaderResult fields and surjection dicts in SKILL.md (skills)</li> </ul>"},{"location":"CHANGELOG/#0103-2026-04-23","title":"[0.10.3] - 2026-04-23","text":""},{"location":"CHANGELOG/#cicd","title":"CI/CD","text":"<ul> <li>Use mike for versioned documentation deployment (docs)</li> <li>Install momapy and its dependencies before building docs (docs)</li> </ul>"},{"location":"CHANGELOG/#documentation_4","title":"Documentation","text":"<ul> <li>Fix cross-reference syntax in layout-model mapping docstrings</li> <li>Fix table rendering in sbgn pd/af and celldesigner module docstrings</li> <li>Fix and expand CLI reference, index, and about pages</li> </ul>"},{"location":"CHANGELOG/#fixed_5","title":"Fixed","text":"<ul> <li>Add missing type annotations to resolve griffe warnings (typing)</li> </ul>"},{"location":"CHANGELOG/#other_1","title":"Other","text":"<ul> <li>Merge branch 'develop'</li> </ul>"},{"location":"CHANGELOG/#0102-2026-04-22","title":"[0.10.2] - 2026-04-22","text":""},{"location":"CHANGELOG/#changed_5","title":"Changed","text":"<ul> <li>Updated with new public facing api (demos)</li> </ul>"},{"location":"CHANGELOG/#documentation_5","title":"Documentation","text":"<ul> <li>Fix SKILL.md bugs (rendering fence, backend name, visualize, info) (skill)</li> <li>Clarify import conventions in SKILL.md (skill)</li> <li>Cross-link layout-model mapping catalogues</li> <li>Regenerate after package-split refactor (API_REFERENCE)</li> <li>Prefer public re-exports in usage examples</li> <li>Rename CODEBASE_SPEC.md to API_REFERENCE.md and move under skills/momapy/</li> <li>Add installable Claude Code skill for momapy</li> </ul>"},{"location":"CHANGELOG/#0101-2026-04-22","title":"[0.10.1] - 2026-04-22","text":""},{"location":"CHANGELOG/#changed_6","title":"Changed","text":"<ul> <li>Rename format-specific Reading/WritingContext subclasses (io)</li> </ul>"},{"location":"CHANGELOG/#0100-2026-04-22","title":"[0.10.0] - 2026-04-22","text":""},{"location":"CHANGELOG/#added_4","title":"Added","text":"<ul> <li>Write RDF annotations in CellDesigner writer (celldesigner.io)</li> <li>Add package facade with re-exports (celldesigner)</li> <li>Read/write SQUARE_CLOSEUP_ compartments (celldesigner.io)*</li> <li>Add closeup compartment layouts (celldesigner)</li> <li>Check parent dir exists before writing / rendering (utils)</li> </ul>"},{"location":"CHANGELOG/#changed_7","title":"Changed","text":"<ul> <li>Migrate utils callers to get_child_layout_elements (sbgn, celldesigner)</li> <li>Drop tuple values from layout_model_mapping (celldesigner.io.celldesigner)</li> <li>Drop tuple values from layout_model_mapping (sbgn.io.sbgnml)</li> <li>Add get_child_layout_elements and make _singleton_to_key a SurjectionDict (core.mapping)</li> <li>Update type annotations and isinstance checks (rendering,styling,positioning)</li> <li>Replace new_element with new_builder_object in readers (io)</li> <li>Drop pre-made map/model/layout builders (sbgn,celldesigner,sbml)</li> <li>Remove PointBuilder and BboxBuilder module-level names (geometry)</li> <li>Remove pre-made builder classes and convenience methods (core)</li> <li>Copy user-defined dunders to generated builders (builder)</li> <li>Expose readers and writers via format subpackage init facades (io)</li> <li>Move registry and helpers to core, expose via init facade (rendering)</li> <li>Move registry and helpers to core, expose via init facade (io)</li> <li>Move registry logic to core, keep init as facade (rendering)</li> <li>Move registry logic to core, keep init as facade (io)</li> <li>Promote styling module to subpackage with facade (styling)</li> <li>Convert pd/af module bodies to from-imports (sbgn)</li> <li>Add subpackage facade and update callers after restructur (sbgn)</li> <li>Split af.py into af/ subpackage (model/layout/map + facade) (sbgn)</li> <li>Split pd.py into pd/ subpackage (model/layout/map + facade) (sbgn)</li> <li>Split core.py into elements/model/layout/map (sbgn)</li> <li>Update callers after sbml/core.py removal (sbml)</li> <li>Split core.py into elements/model, mirror momapy.core layout (sbml)</li> <li>Update callers after core.py removal (celldesigner)</li> <li>Split core.py into elements/map, mirror momapy.core layout (celldesigner)</li> <li>Split core.py into core/model/layout (celldesigner)</li> <li>Migrate core submodules and init facade to absolute from-imports (core)</li> <li>Drop unused IOResult.exceptions field (io.core)</li> <li>Add type hints to public methods (rendering.svg_native)</li> <li>Add type hints to public functions (coloring)</li> <li>Hoist WritingContext base to momapy.io.utils (io)</li> <li>Normalize type hints to PEP 604 union syntax (sbgn.af)</li> <li>Annotate file_path on SBGN-ML and CellDesigner writers (io)</li> <li>Drop dead None checks after get_reader/get_writer (io.core)</li> <li>Include unsupported type in TypeError messages (positioning)</li> <li>Remove no-op pass branch in modulation reader (celldesigner.io)</li> <li>Use isinstance_or_builder helper (sbgn.utils)</li> <li>Align stroke/fill NoneValue checks with skia (rendering.cairo)</li> <li>Consolidate format-specific pickle readers into one pickle handler (io pickle)</li> </ul>"},{"location":"CHANGELOG/#documentation_6","title":"Documentation","text":"<ul> <li>Describe uniform layout_model_mapping scheme</li> <li>Update CODEBASE_SPEC and demos for builder refactor</li> <li>Fix misleading init docstring (meta)</li> <li>Flag sbml subpackage as partial skeleton in package docstring (sbml)</li> <li>Document subpackage layout and exposed stylesheets (sbgn.styling)</li> <li>Codify absolute-from-imports policy</li> <li>Codify absolute-imports rule with init.py re-export carve-out (claude)</li> <li>Require ruff format after edits (claude)</li> <li>Expand terse model subclass docstrings (sbgn.af)</li> <li>Add docstrings to reader helper functions (celldesigner.io)</li> <li>Add docstrings to anchor methods (celldesigner.core)</li> <li>Note filter effects are unsupported (rendering.cairo)</li> <li>Replace commented-out example with runnable one (plugins.core)</li> <li>Mark CellDesigner as read/write (readme)</li> </ul>"},{"location":"CHANGELOG/#fixed_6","title":"Fixed","text":"<ul> <li>Read source files as UTF-8 in super discipline test (tests)</li> <li>Harden coordinate frames against degenerate geometry (celldesigner.io)</li> <li>Remap pre-4.0 modifier types to KnownOrUnknownModulator subclasses (celldesigner.io)</li> <li>Restore _writing module import in writer (celldesigner.io)</li> <li>Read gene/RNA/AntisenseRNA templates without type attribute (celldesigner.io)</li> <li>Narrow unsupported-map-type error reporting (sbgn.io.sbgnml)</li> <li>Return map when set_nodes_to_fit_labels is a no-op (sbgn.utils)</li> <li>Set font_weight initial value to FontWeight.NORMAL (rendering)</li> <li>Correct RBG/RBGA typos in error messages (coloring)</li> <li>Stop swallowing exceptions in super_or_builder (builder)</li> <li>Drop duplicate LineTo in _TruncatedProteinShape (celldesigner)</li> </ul>"},{"location":"CHANGELOG/#miscellaneous_2","title":"Miscellaneous","text":"<ul> <li>Remove unused imports in pd/af layouts and models (sbgn)</li> <li>Extend default style to new compartment layouts (celldesigner.utils)</li> <li>Remove Claude sandbox artifact gitignore entries</li> <li>Drop unused with_styles reader parameter (sbgn.io.sbgnml)</li> <li>Drop unused lxml import (celldesigner.io)</li> </ul>"},{"location":"CHANGELOG/#styling_3","title":"Styling","text":"<ul> <li>Ruff-format unrelated test/conftest files</li> <li>Ruffed demos</li> <li>Apply ruff format across src/</li> </ul>"},{"location":"CHANGELOG/#testing_1","title":"Testing","text":"<ul> <li>Assert singleton participant mappings (sbgn.io.sbgnml)</li> <li>Forbid raw super(...) in the frozen hierarchy</li> </ul>"},{"location":"CHANGELOG/#091-2026-04-20","title":"[0.9.1] - 2026-04-20","text":""},{"location":"CHANGELOG/#changed_8","title":"Changed","text":"<ul> <li>Class RDFAnnotation plain dataclass, sbo_term as str (sbml core)</li> </ul>"},{"location":"CHANGELOG/#090-2026-04-20","title":"[0.9.0] - 2026-04-20","text":""},{"location":"CHANGELOG/#added_5","title":"Added","text":"<ul> <li>Add descendants() to Model and ModelElement (core)</li> </ul>"},{"location":"CHANGELOG/#changed_9","title":"Changed","text":"<ul> <li>Collapse build_id_mappings signature to a single obj (io.utils)</li> <li>Rename SBase to SBMLModelElement; detach SBMLModel from it (sbml)</li> <li>Split real_source_ids by model/layout side (celldesigner.io)</li> </ul>"},{"location":"CHANGELOG/#documentation_7","title":"Documentation","text":"<ul> <li>Added codebase specification</li> </ul>"},{"location":"CHANGELOG/#fixed_7","title":"Fixed","text":"<ul> <li>Populate source_id_to_model_element for models that aren't ModelElements (io)</li> <li>Repair is_submodel in SBGNAFModel and CellDesignerModel (model)</li> <li>Iterate children in make_annotations/make_notes and deduplicate (io)</li> <li>Harden make_notes and replace deprecated .getchildren() (io)</li> <li>Use float | None for stoichiometry across all formats</li> <li>Fix broken CrossPoint drawing_elements (meta shapes)</li> <li>Fix broken matrix() transform output (rendering svg_native)</li> <li>Return None for source_id_to_model/layout_element when not requested (io)</li> </ul>"},{"location":"CHANGELOG/#miscellaneous_3","title":"Miscellaneous","text":"<ul> <li>Remove animating module</li> </ul>"},{"location":"CHANGELOG/#081-2026-04-16","title":"[0.8.1] - 2026-04-16","text":""},{"location":"CHANGELOG/#added_6","title":"Added","text":"<ul> <li>Change --tidy to -t, add --to-top-left to export and visualize (cli)</li> <li>Snap arc endpoints to closest anchor point when tidying (celldesigner utils)</li> <li>Snap arc endpoints to closest anchor point when tidying (celldesigner)</li> </ul>"},{"location":"CHANGELOG/#changed_10","title":"Changed","text":"<ul> <li>Replace set_arcs_to_orthogonal with straighten_arcs (celldesigner utils)</li> </ul>"},{"location":"CHANGELOG/#fixed_8","title":"Fixed","text":"<ul> <li>Initialize logic_arc before layout block in logic gate reader (celldesigner io)</li> </ul>"},{"location":"CHANGELOG/#080-2026-04-16","title":"[0.8.0] - 2026-04-16","text":""},{"location":"CHANGELOG/#added_7","title":"Added","text":"<ul> <li>Add colors subcommand to list named colors (cli)</li> <li>Add style subcommand and list styles (cli)</li> <li>Add implicit stdin/stdout piping support (cli)</li> <li>Add tidy subcommand with granular layout operations (cli)</li> <li>Add  subcommand for inspecting stylable properties (cli)</li> <li>Set model and layout ids from SBML model id (celldesigner io)</li> <li>Add BooleanLogicGateInput model element (celldesigner)</li> <li>Set map, model and layout ids from sbgnml map id (sbgn io)</li> <li>Add visualize subcommand for interactive map viewing (cli)</li> <li>Added stdout output and -o option for export and info (cli)</li> <li>Add info subcommand for map file inspection (cli)</li> <li>Add get_info utility function (celldesigner)</li> <li>Add get_info utility function (sbgn)</li> <li>Add list subcommand for readers, writers, and renderers (cli)</li> </ul>"},{"location":"CHANGELOG/#changed_11","title":"Changed","text":"<ul> <li>Align tidy default separations across formats (utils)</li> <li>Replace ids with id_to_element, source_id_to_model/layout_element (io)</li> <li>Rename annotations/notes to element_to_annotations/element_to_notes (io)</li> <li>Require metaid for modifier elements (celldesigner io)</li> <li>Use distinct ids for model and layout elements (sbgn io)</li> </ul>"},{"location":"CHANGELOG/#documentation_8","title":"Documentation","text":"<ul> <li>Note space replacement in structural state ID documentation</li> <li>Make ID assignment docstrings consistent between SBGN and CellDesigner readers</li> <li>Update CellDesigner ID assignment documentation</li> <li>Add BooleanGateInput to CellDesigner ID table and docstring</li> <li>Update CellDesigner ID assignment documentation</li> <li>Add ID assignment documentation to reader module docstrings</li> <li>Update boolean gate, logic arc and modulator in CellDesigner ID table</li> <li>Update boolean gate and logic arc in CellDesigner ID table</li> <li>Update modification and structural state in CellDesigner ID table</li> <li>Mark modulator as registered in CellDesigner ID table</li> <li>Update ID assignment patterns for CellDesigner</li> <li>Add ID assignment patterns to CLAUDE.md</li> </ul>"},{"location":"CHANGELOG/#fixed_9","title":"Fixed","text":"<ul> <li>Handle missing notes on complex subunit species (celldesigner io)</li> <li>Catch all exceptions in pickle check_file methods (io)</li> <li>Resolve correct alias for boolean gate inputs in writer (celldesigner io)</li> <li>Fix roundtrip bugs in writer for model and MINERVA validation (celldesigner io)</li> <li>Fix roundtrip bugs in writer for model and MINERVA validation (celldesigner io)</li> <li>Fix roundtrip ordering bugs in writer (celldesigner io)</li> <li>Update active layout when resizing nodes and complexes (celldesigner tidy)</li> <li>Apply styling before tidying (cli)</li> <li>Fit complexes using subunit model mappings (celldesigner tidy)</li> <li>Increase default compartment padding to 25 (celldesigner tidy)</li> <li>Include child compartments when fitting compartment size (celldesigner)</li> <li>Detect prefixed presentation attributes by suffix matching (styling)</li> <li>Add modification and structural state to layout_model_mapping (celldesigner io)</li> <li>Use deterministic ids for reactant and product layout elements (celldesigner io)</li> <li>Use deterministic ids for boolean gate and logic arc elements (celldesigner io)</li> <li>Use deterministic ids for species modification and structural state elements (celldesigner io)</li> <li>Register modifier model and layout elements in xml_id lookup dicts (celldesigner io)</li> <li>Use distinct ids for reaction, modulation and modifier layout elements (celldesigner io)</li> <li>Register frozen child elements in xml_id lookup dicts (sbgn io)</li> <li>Use anchor elements only when building layout-to-model id mapping for visualize subcommand (cli)</li> <li>Set missing id_ on arc layout elements (sbgn io)</li> <li>Close temp file before writing to fix Windows PermissionError (cli)</li> <li>Render hypothetical species with dashed stroke (celldesigner)</li> <li>Fix modification residue angle computation (celldesigner)</li> <li>Fix species and modification residue writing (celldesigner)</li> <li>Add AF map classes to writer classification (sbgn io)</li> <li>Replace deprecated pyparsing.delimited_list with DelimitedList (styling)</li> <li>Harmonize intersection boundary checks to parametric t with PARAMETER_TOLERANCE (geometry)</li> </ul>"},{"location":"CHANGELOG/#testing_2","title":"Testing","text":"<ul> <li>Added roundtrip tests (celldesigner io)</li> </ul>"},{"location":"CHANGELOG/#070-2026-04-13","title":"[0.7.0] - 2026-04-13","text":""},{"location":"CHANGELOG/#added_8","title":"Added","text":"<ul> <li>Add get_stylable_attributes utility function (styling)</li> <li>Read line width and color from CellDesigner XML (celldesigner io)</li> <li>Write line width, color, and font size from layout (celldesigner io)</li> <li>Read line width and color from CellDesigner XML (celldesigner io)</li> <li>Add export subcommand for map export (cli)</li> <li>Support tidy flag for CellDesigner maps (cli)</li> <li>Add set_arcs_to_orthogonal utility function (celldesigner)</li> <li>Added tyding functions (celldesigner)</li> <li>Added tidying functions (celldesigner)</li> <li>Added experimental writer (celldesigner io)</li> <li>Added indentation markers (pretty print)</li> <li>Added utils module (celldesigner)</li> </ul>"},{"location":"CHANGELOG/#changed_12","title":"Changed","text":"<ul> <li>Removed defaults for sbgn tidy (cli)</li> <li>Set sensible defaults for tidy function parameters (sbgn)</li> <li>Clean CellDesigner writer conventions (celldesigner io)</li> <li>Clean CellDesigner reader with reading_context API (celldesigner io)</li> <li>Clean SBGN reader with reading_context API (sbgn io)</li> <li>Now uses reaction node anchor functions (celldesigner io)</li> <li>Renamed functions and variables more appropriately for writer (celldesigner io)</li> <li>Added _reading prefix for reading modules (celldesigner io)</li> <li>Added _reading prefix for reading modules (celldesigner io)</li> <li>Added _reading prefix for reading modules (celldesigner io)</li> </ul>"},{"location":"CHANGELOG/#documentation_9","title":"Documentation","text":"<ul> <li>Add documentation for export subcommand (cli)</li> <li>Improved for map vs map builder (sbgn)</li> <li>Simplified the top example (momapy)</li> </ul>"},{"location":"CHANGELOG/#fixed_10","title":"Fixed","text":"<ul> <li>Use correct coordinate frame bases in writer inverse transforms (celldesigner io)</li> <li>Derive reaction link participants from layout arcs (celldesigner io)</li> <li>Derive reaction link participants from layout arcs (celldesigner.io)</li> <li>Use correct coordinate frame bases for arc edit point transforms (celldesigner io)</li> <li>Correct connector tip fractions to 0.375/0.625 (celldesigner)</li> <li>Use target anchor as coordinate frame basis for modifier arcs (celldesigner io)</li> <li>Fixed writing gates when input is a subunit (celldesigner io)</li> <li>Now has correct metaids for reactants/products/modifiers</li> <li>Fixed dedup issues and refactor of deduplication (celldesigner io)</li> <li>Fixed dedup issues and refactor of deduplication (sbgn io)</li> <li>Added fallback for undocumented anchor points (e.g., 'INACTIVE') (celldesigner io)</li> <li>Added missing source and target to modulation layouts (celldesigner io)</li> <li>Fix layout-model mapping consistency after dedup (celldesigner io)</li> <li>Fix mapping for Boolean gates (celldesigner io)</li> <li>Added subunit remap when identical complexes (celldesigner io)</li> <li>Fix bug in adding subunits where no replacement was done (celldesigner io)</li> <li>Fix setting .base to true for base reactants/products (celldesigner io)</li> <li>Fix flattening of reactions and gates (celldesigner io)</li> <li>Fixed expansion of reactions and modulations in layout model mapping (celldesigner io)</li> <li>Fixed bug when reading with no return type (celldesigner io)</li> </ul>"},{"location":"CHANGELOG/#miscellaneous_4","title":"Miscellaneous","text":"<ul> <li>Exclude .venv-sandbox from build (gitignore)</li> <li>Add full-word variable naming convention to CLAUDE.md</li> <li>Added new layout model mapping philosophy (AGENTS)</li> <li>Added poubelle (gitignore)</li> <li>Cleaned file (gitignore)</li> <li>Formatting with ruff</li> <li>Updated reactome analysis (demos)</li> </ul>"},{"location":"CHANGELOG/#testing_3","title":"Testing","text":"<ul> <li>Add layout_model_mapping integrity tests for SBGN and CellDesigner (io)</li> </ul>"},{"location":"CHANGELOG/#061-2026-03-18","title":"[0.6.1] - 2026-03-18","text":""},{"location":"CHANGELOG/#added_9","title":"Added","text":"<ul> <li>Added a demo on map comparison</li> <li>Added colinear case when reading reactions (celldesigner)</li> </ul>"},{"location":"CHANGELOG/#cicd_1","title":"CI/CD","text":"<ul> <li>Bumped action versions</li> </ul>"},{"location":"CHANGELOG/#changed_13","title":"Changed","text":"<ul> <li>Hid wip celldesigner writer</li> <li>Moved demos from demo to demos</li> <li>Moved utils to root (demo)</li> <li>Rearranged demo files</li> <li>Removed unnecessary re-export of reader (celldesigner io)</li> </ul>"},{"location":"CHANGELOG/#documentation_10","title":"Documentation","text":"<ul> <li>Moved demo to new paths and name</li> </ul>"},{"location":"CHANGELOG/#fixed_11","title":"Fixed","text":"<ul> <li>Replaced with correct apt deps (binder)</li> <li>Removed escaping brackets (binder)</li> <li>Model elements such as modulations are now mapped to the full set of layout elements (sbgn io)</li> <li>Removed &gt;&gt;&gt; from examples</li> <li>User manual files after refactor</li> <li>Added skia extra dep to binder</li> <li>Updated the demo with latest refactors (demo)</li> <li>Fixed bug in coordinates of logic gate arcs (celldesigner)</li> </ul>"},{"location":"CHANGELOG/#miscellaneous_5","title":"Miscellaneous","text":"<ul> <li>Updated reactome analysis with maps display (demos)</li> <li>Updated reactome analysis (demos)</li> <li>Added apt packages required for skia-python</li> <li>Simplified entries</li> <li>Updated deps</li> <li>Added jupyterlab dev dep</li> <li>Wip implement celldesigner writer</li> <li>Ruff formatting</li> </ul>"},{"location":"CHANGELOG/#testing_4","title":"Testing","text":"<ul> <li>Fixed new paths (demo)</li> </ul>"},{"location":"CHANGELOG/#060-2026-03-10","title":"[0.6.0] - 2026-03-10","text":""},{"location":"CHANGELOG/#added_10","title":"Added","text":"<ul> <li>Added cache for font queries (fonts)</li> <li>Added a helper function for creating a bbox around points and refactored to use it</li> <li>Added group araound textlayout; made better svg element ids and classes</li> <li>Replaced shapely and bezier geometry by custom analytical geometry</li> </ul>"},{"location":"CHANGELOG/#changed_14","title":"Changed","text":"<ul> <li>Replaced named tuple by dataclass (fonts)</li> <li>Replaced matplotlib fonts finding by custom one</li> <li>Renamed self_ methods to own_ methods</li> <li>Replaced Bezier approximations with direct elliptical arc calculations (geometry)</li> <li>Removed unnecessary helper functions (geometry)</li> </ul>"},{"location":"CHANGELOG/#fixed_12","title":"Fixed","text":"<ul> <li>Set modifications to own_angle rather than angle (celldesigner)</li> </ul>"},{"location":"CHANGELOG/#miscellaneous_6","title":"Miscellaneous","text":"<ul> <li>Added support for python 3.13 and 3.14</li> <li>Added TODO.txt to .gitignore</li> </ul>"},{"location":"CHANGELOG/#testing_5","title":"Testing","text":"<ul> <li>Updated test after refactor (geometry)</li> <li>Added tests for union of bbox and making bbox around points</li> </ul>"},{"location":"CHANGELOG/#050-2026-03-04","title":"[0.5.0] - 2026-03-04","text":""},{"location":"CHANGELOG/#added_11","title":"Added","text":"<ul> <li>Added optional dict cache to replace</li> <li>Added singleton mapping for anchor layout element (layout model mapping)</li> <li>Removed collection builders by mutable collections (builder)</li> </ul>"},{"location":"CHANGELOG/#cicd_2","title":"CI/CD","text":"<ul> <li>Removed commit and made CHANGELOG.md directly from docs.yml (changelog)</li> </ul>"},{"location":"CHANGELOG/#changed_15","title":"Changed","text":"<ul> <li>Moved building builders outside of registering function</li> <li>Complete split of model functions, with reader as an orchestrator</li> <li>Complete split of layout and model functions, with reader as an orchestrator</li> <li>Moved annotations and notes parsing to sbml</li> <li>Replace model name from Model to SBMLModel</li> <li>Removed duplicate modification and changed order</li> <li>Complete refactor of celldesigner io</li> <li>Arc flattening and associated refactor + writer cleaning</li> <li>Complete refactor with code cleaning (sbgnml)</li> <li>Complete refactor, separated parsing/model/layout (sbgnml)</li> <li>Made subpackage and divided into modules (core)</li> </ul>"},{"location":"CHANGELOG/#fixed_13","title":"Fixed","text":"<ul> <li>Active borders are now layout elements (fix #3)</li> <li>Absence of modification is now treated as a modification with state='empty' (celldesigner)</li> <li>Now hashable (builder)</li> </ul>"},{"location":"CHANGELOG/#miscellaneous_7","title":"Miscellaneous","text":"<ul> <li>Added instructions for debates/ and plans/ directories</li> <li>Added debates/</li> <li>Added CLAUDE.md and disallowed use of dangerouslyDisableSandbox</li> <li>Added claude sandbox artifacts</li> </ul>"},{"location":"CHANGELOG/#testing_6","title":"Testing","text":"<ul> <li>Added tests for new active layout elements</li> <li>Added tests for notes and annotations</li> <li>Added tests for sbml</li> <li>Added tests for sbgn.utils</li> </ul>"},{"location":"CHANGELOG/#044-2026-02-24","title":"[0.4.4] - 2026-02-24","text":""},{"location":"CHANGELOG/#added_12","title":"Added","text":"<ul> <li>Added tidy option (cli)</li> <li>Changed default font to Arial for macos and win and deja vu sans for linux</li> <li>Added support for af compartment uoi</li> <li>Added support for uoi for compartments and submap terminals in af</li> <li>Added support for cardinality (a la newt)</li> </ul>"},{"location":"CHANGELOG/#cicd_3","title":"CI/CD","text":"<ul> <li>Changed regex for version; made release conditional to testing; made docs independant of release</li> </ul>"},{"location":"CHANGELOG/#fixed_14","title":"Fixed","text":"<ul> <li>Fixed label position for compartments; fixed rerouting equivalence arcs (tidy)</li> <li>Activity is now a dataclass</li> <li>Added uoi to ad compartment model element</li> <li>Default font sizes are now closer to newt</li> <li>Or nodes in af were stored and rendererd as and</li> <li>Bug in shapely for bezier and elliptical curves</li> <li>Bug in fraction for Arc</li> <li>Accepts tag with no orientation</li> </ul>"},{"location":"CHANGELOG/#miscellaneous_8","title":"Miscellaneous","text":"<ul> <li>Added AGENTS.md for code agents</li> <li>Cleaned useless function (sbgn.io.sbgnml)</li> </ul>"},{"location":"CHANGELOG/#testing_7","title":"Testing","text":"<ul> <li>Added tests for Arc fraction and</li> </ul>"},{"location":"CHANGELOG/#043-2026-02-13","title":"[0.4.3] - 2026-02-13","text":""},{"location":"CHANGELOG/#documentation_11","title":"Documentation","text":"<ul> <li>Removed linenums for code snippets</li> <li>Update example with new io and render_map api</li> </ul>"},{"location":"CHANGELOG/#fixed_15","title":"Fixed","text":"<ul> <li>Changed stoichiometry type from int to float</li> </ul>"},{"location":"CHANGELOG/#miscellaneous_9","title":"Miscellaneous","text":"<ul> <li>Uv sync</li> <li>Removed tracking of site/</li> </ul>"},{"location":"CHANGELOG/#042-2026-02-12","title":"[0.4.2] - 2026-02-12","text":""},{"location":"CHANGELOG/#documentation_12","title":"Documentation","text":"<ul> <li>Changed order of sections</li> <li>Added symlink to CHANGELOG.md from root to docs/</li> </ul>"},{"location":"CHANGELOG/#fixed_16","title":"Fixed","text":"<ul> <li>Bug in point to fortranarray and in handling shapely polygons</li> <li>Bug in invert of scaling</li> </ul>"},{"location":"CHANGELOG/#miscellaneous_10","title":"Miscellaneous","text":"<ul> <li>Added coverage files</li> </ul>"},{"location":"CHANGELOG/#testing_8","title":"Testing","text":"<ul> <li>Added new tests</li> <li>Added coverage</li> </ul>"},{"location":"CHANGELOG/#041-2026-02-10","title":"[0.4.1] - 2026-02-10","text":""},{"location":"CHANGELOG/#documentation_13","title":"Documentation","text":"<ul> <li>Added docs for cli and styling</li> </ul>"},{"location":"CHANGELOG/#040-2026-02-10","title":"[0.4.0] - 2026-02-10","text":""},{"location":"CHANGELOG/#added_13","title":"Added","text":"<ul> <li>Added precommit linting (@conventional)</li> <li>Added renderer and format autodetection (rendering)</li> </ul>"},{"location":"CHANGELOG/#cicd_4","title":"CI/CD","text":"<ul> <li>Added automated changelog with git-cliff</li> </ul>"},{"location":"CHANGELOG/#fixed_17","title":"Fixed","text":"<ul> <li>Distinguish scenario min and all for rendering images</li> </ul>"},{"location":"CHANGELOG/#miscellaneous_11","title":"Miscellaneous","text":"<ul> <li>Removed old testing file</li> <li>Docs/plans-&gt;plans</li> </ul>"},{"location":"CHANGELOG/#other_2","title":"Other","text":"<ul> <li>Added test for demo</li> <li>Plugins.registry-&gt;plugins.core</li> <li>Updated docs</li> <li>Updated docs</li> <li>Removed version; updated docs</li> <li>_renderer_registry-&gt;renderer_registry</li> <li>Added docs/plans and .tox</li> <li>Rendering., io.: changed registering renderers/readers/writers to plugin system; plugins: new</li> <li>Updated; rendering._cairo: rm</li> <li>Update with renderer changes</li> </ul>"},{"location":"CHANGELOG/#styling_4","title":"Styling","text":"<ul> <li>Changed default fonts to DejaVu Sans</li> </ul>"},{"location":"CHANGELOG/#testing_9","title":"Testing","text":"<ul> <li>Update with new plugin api</li> </ul>"},{"location":"CHANGELOG/#030-2026-02-03","title":"[0.3.0] - 2026-02-03","text":""},{"location":"CHANGELOG/#other_3","title":"Other","text":"<ul> <li>Fixed bug in argument names</li> <li>Update</li> <li>Init</li> <li>Added entry point momapy</li> <li>Changed 'formats' attributes to 'supported_formats'; added ValueError for unsupported formats</li> </ul>"},{"location":"CHANGELOG/#028-2026-01-27","title":"[0.2.8] - 2026-01-27","text":""},{"location":"CHANGELOG/#fixed_18","title":"Fixed","text":"<ul> <li>Fixed python version to 3.13 for packaging</li> </ul>"},{"location":"CHANGELOG/#other_4","title":"Other","text":"<ul> <li>Io.core; celldesigner.io.celldesigner: replaced ReaderResult's ids by FrozenSurjectionDict</li> <li>Modified so it gets the latest tag on the main branch when triggering manually (Claude)</li> <li>Add workflow_dispatch option to trigger manually</li> </ul>"},{"location":"CHANGELOG/#027-2026-01-26","title":"[0.2.7] - 2026-01-26","text":""},{"location":"CHANGELOG/#other_5","title":"Other","text":"<ul> <li>Fixed id of layout element taking id of the cd element it comes from</li> </ul>"},{"location":"CHANGELOG/#026-2026-01-21","title":"[0.2.6] - 2026-01-21","text":""},{"location":"CHANGELOG/#other_6","title":"Other","text":"<ul> <li>Fixed layout model mapping for subunits</li> </ul>"},{"location":"CHANGELOG/#025-2026-01-14","title":"[0.2.5] - 2026-01-14","text":""},{"location":"CHANGELOG/#other_7","title":"Other","text":"<ul> <li>Merge branch 'develop' for release</li> <li>Added layout model mapping for compartments</li> </ul>"},{"location":"CHANGELOG/#024-2026-01-13","title":"[0.2.4] - 2026-01-13","text":""},{"location":"CHANGELOG/#other_8","title":"Other","text":"<ul> <li>Merge branch 'develop' for release</li> <li>Fixed order of compartment aliases so they respect z-order</li> </ul>"},{"location":"CHANGELOG/#023-2026-01-08","title":"[0.2.3] - 2026-01-08","text":""},{"location":"CHANGELOG/#other_9","title":"Other","text":"<ul> <li>Merge branch 'develop' for release</li> <li>Added specific supported python 3 versions</li> <li>Fixed bug in COPYING badge url</li> <li>Fixed bug in COPYING badge url</li> <li>Added badges</li> <li>Merge branch 'develop'</li> <li>Added binder link and how to run the demo locally</li> <li>Removed useless demo extra; binder/requirements.txt: replaced demo extra by jupyter dep</li> <li>Added demo extra and moved jupyter dep from dev to demo; binder/requirements.txt: added file for binder</li> </ul>"},{"location":"CHANGELOG/#022-2026-01-07","title":"[0.2.2] - 2026-01-07","text":""},{"location":"CHANGELOG/#documentation_14","title":"Documentation","text":"<ul> <li>Update with new default rendering and reading modules</li> <li>Removed copyright</li> </ul>"},{"location":"CHANGELOG/#other_10","title":"Other","text":"<ul> <li>Merge branch 'develop'</li> <li>Merge branch 'develop'</li> </ul>"},{"location":"CHANGELOG/#021-2026-01-07","title":"[0.2.1] - 2026-01-07","text":""},{"location":"CHANGELOG/#other_11","title":"Other","text":"<ul> <li>Merge branch 'develop'</li> <li>Fixed bug where rendering.skia was imported</li> <li>Celldesigner.core, celldesigner.io.celldesigner: changed default font to Arial</li> <li>Replaced celldesigner_pickle by celldesigner-pickle</li> <li>Fixed bug momapy.io.WriterResult-&gt;momapy.io.core.WriterResult</li> <li>Fixed type hint for .selectors attribute</li> <li>Added tests (Claude)</li> <li>Replaced pp by pyparsing</li> <li>Added Not and Compound selectors</li> <li>Update with new core for io and svg for rendering</li> <li>Update explaining optional deps</li> <li>Made true release conditional to TestPyPi release's success</li> </ul>"},{"location":"CHANGELOG/#020-2026-01-06","title":"[0.2.0] - 2026-01-06","text":""},{"location":"CHANGELOG/#fixed_19","title":"Fixed","text":"<ul> <li>Fixed bug in checking files: added try in case the file is binary</li> <li>Fixed bug where super was called in FrozenSurjectionDict init</li> <li>Fixed test</li> <li>Fixed import and extents bug</li> <li>Fixed bug in class names for logical operators</li> <li>Fixed bug: replaced ink_bbox by bbox</li> <li>Fixed bug in value=='' for state variables</li> <li>Fixed bugs in writing sbgnml</li> <li>Fixed bug</li> <li>Fixed bug where only macos would be tested</li> <li>Fixed bug with group_transform in rendering; added transform if no group_transform</li> <li>Fixed bug</li> <li>Fixed bug: LogicArc-&gt;LogicArcLayout</li> <li>Fixed bug in making LayoutModelMappingBuilder with new keys as frozendict</li> <li>Fixed bug in filter vs filter_ and filter_.id vs filter_.id_</li> <li>Fixed absent NoneValueType type hint for dahsarrays and others</li> <li>Fixed bug in make_boolean_logic_gate</li> </ul>"},{"location":"CHANGELOG/#other_12","title":"Other","text":"<ul> <li>Merge branch 'develop'</li> <li>Made automatic github release</li> <li>Updated tests to have frozendict notes and annotations</li> <li>Changed type of notes and annotations to frozendict</li> <li>One tox job per os</li> <li>Adapted workflow to test all python versions on all os with tox</li> <li>Added explicit utf-8 encoding when writing files to handle windows</li> <li>Added tox.ini and python version</li> <li>Typing.Self-&gt;typing_extensions.Self for 3.10 compat</li> <li>Switched from pdm to uv; pdm-bachend to hatchling; made test dep group</li> <li>Changed typing.Self to typing_extensions.Self for compat with 3.10</li> <li>Added ModuleNotFoundError as potential source for not available renderer</li> <li>Update</li> <li>Added pytest tests to push on testing branch</li> <li>Updated with new rendering tests</li> <li>Merge branch 'develop'</li> <li>Cleaned github actions</li> <li>Removed comments and unnecessary asserts</li> <li>Udpated tests</li> <li>Removed all extras</li> <li>Added insallation github action</li> <li>Added pytest and tests generated by Claude</li> <li>Update</li> <li>Solved circular imports when registering; fixed bug in sbgn/io/sbgmml.py</li> <li>Moved readers and writers registration to init of subpackage for sbml and celldesigner</li> <li>Removed buged sbgnml-0.2 writer</li> <li>Moved io to own subpackage; moved registering readers and writers to subpackage init</li> <li>Moved registering svg-native renderer to base of subpackage</li> <li>Changed default renderer to svg-native</li> <li>Moved cairo and skia to extras</li> <li>Added warning about installing extras</li> <li>Added uharfbuzz and matplotlib</li> <li>Replaced ink_bbox by bbox</li> <li>Change default font to DejaVu Sans; replaced ink_bbox by bbox</li> <li>Changed pango layout to harfbuzz and matplotlib (for fonts)</li> <li>Merge branch 'develop' into freetype</li> <li>Added freetype-py dep</li> <li>Added test layout with freetype</li> <li>Updated installation instructions</li> <li>Updated dependencies</li> <li>Added dependencies</li> <li>Added frozen annotations</li> <li>Added reading id</li> <li>Added more layout model mappings</li> <li>Added sbgn/io/pickle.py and rendering/cairo.py that were ai generated based on template files</li> <li>Updated demo</li> <li>Update</li> <li>Update</li> <li>Added print_source function</li> <li>Update</li> <li>Added participants of model elements to layout model mapping for: modulations, processes, logical operators</li> <li>Added support for HAlignment in TextLayout; added HAligment.CENTER to all sbgn labels</li> <li>Updated demo</li> <li>Updated demo</li> <li>Added NoneValue to edge_mode translation</li> <li>Updated demo</li> <li>Id-&gt;id_</li> <li>Mod demo: added example; fixed bug in display where markers were transformed</li> <li>_ReactionLayout-&gt;ReactionLayout</li> <li>In Filter: id-&gt;id_</li> <li>Made left/right_connector_fraction public attributes</li> <li>Added function to set label font size</li> <li>Updated packages</li> </ul>"},{"location":"CHANGELOG/#013-2025-08-28","title":"[0.1.3] - 2025-08-28","text":""},{"location":"CHANGELOG/#fixed_20","title":"Fixed","text":"<ul> <li>Fixed link to doc</li> </ul>"},{"location":"CHANGELOG/#012-2025-08-28","title":"[0.1.2] - 2025-08-28","text":""},{"location":"CHANGELOG/#other_13","title":"Other","text":"<ul> <li>Updated docs/index.md and README.md</li> <li>Cleaned</li> <li>Made version regex start with number after v</li> <li>Publish to testpipy first</li> </ul>"},{"location":"CHANGELOG/#011-2025-08-28","title":"[0.1.1] - 2025-08-28","text":""},{"location":"CHANGELOG/#other_14","title":"Other","text":"<ul> <li>Added module level doc</li> <li>Build only if version</li> <li>Only on version tags</li> <li>Back to version as v.*</li> <li>Added dynamic versioning</li> <li>Added cd/ci for automatic release on tags</li> <li>Moved to pdm build backend</li> <li>Updated doc page</li> </ul>"},{"location":"CHANGELOG/#010-2025-08-12","title":"[0.1.0] - 2025-08-12","text":""},{"location":"CHANGELOG/#changed_16","title":"Changed","text":"<ul> <li>Refactored; fixed bugs; added truncation layout; id-&gt;id_</li> <li>Refactored</li> <li>Refactored reactions and taken into account transport</li> <li>Refactored cairo renderer</li> <li>Refactoring and bugs</li> <li>Refactored code and fixed in set_arcs_to_borders; added tidy function</li> <li>Refactored rendering; render multiple maps</li> </ul>"},{"location":"CHANGELOG/#fixed_21","title":"Fixed","text":"<ul> <li>Fixed typo</li> <li>Fixed merge</li> <li>Fixed bugs and cleaned up</li> <li>Fixed bug in to_top_left</li> <li>Fixed return type of fit function</li> <li>Fixed bug in biology-qualifiers: isValue-&gt;is</li> <li>Fix #8</li> <li>Fixed bug outputing wrong map element types depending on mode</li> <li>Fixed typo</li> <li>Fixed bug in intersection of line and shapely object; added rounding</li> <li>Fixed bug type selector</li> <li>Fixed bug no species in map</li> <li>Fixed bug no reaction in map</li> <li>Fixed bug in handling undefined state variables</li> <li>Fixed bug in product from product link: was using Reactant</li> <li>Fixed bug in processing annotations</li> <li>Fixed bug in make_notes...: return [] rather than None</li> <li>Fixed typo in _make_model_no_subelements...: layout-&gt;model</li> <li>Fixed bug in finding correct module</li> <li>Fixed bug: compartments and species templates were not added to the model</li> <li>Fixed bug not taking into account subunits</li> <li>Fixed bugs</li> <li>Fixed bug: species_aliases includes subunits; removed subunits from those</li> <li>Fixed bug: species_aliases includes subunits; removed subunits from those</li> <li>Fixed bug in reading structural states</li> <li>Fixed bug in start_arrowhead_base</li> <li>Fixed bug biology-model-&gt;model-qualifiers</li> <li>Fixed bug when no tagetLink in reaction modification</li> <li>Fixed imports</li> <li>Fixed module name after moving annotations to sbml</li> <li>Fixed imports</li> <li>Fixed bug: need to escape text values</li> <li>Fixed import bug</li> <li>Fixed childless functions: layout_elements=None-&gt;layout_elements=tuple([])</li> <li>Fixed bug in arrowhead_border: is None-&gt;isnan</li> <li>Fixed TransportLayout._end_arrowhead_border_drawing_elements: RIGTH-&gt;LEFT</li> <li>Fixe bug intersection of line and segment</li> <li>Fixed dimensions of layout</li> <li>Fixed calls to new signature of momapy.positioning.fraction_of function</li> <li>Fixed calls to new signature of momapy.positioning.fit function</li> <li>Fixed bug self_children not implemented for arcs</li> <li>Fixed bug in writer</li> <li>Fixed bugs; took into account AF</li> <li>Fixed variable names</li> <li>Fixed border_-&gt;</li> <li>Fixed bug with layout.fill being NoneValue</li> <li>Fixed id-&gt;id_</li> <li>Fixed bug in arrowhead_drawing_elements</li> <li>Fixed id-&gt;id_</li> <li>Fixed bug with file names</li> <li>Fixed type hints</li> <li>Fixed ) bug</li> <li>Fixed type hint: self-&gt;Self</li> <li>Fixed bugs with names</li> <li>Fixed bug in Point: div to truediv</li> <li>Fixed label_center</li> <li>Fixed bug in product and reactant links</li> <li>Fixed bugs in arrowhead_*</li> <li>Fixed bug in call with wrong signature</li> <li>Fixed bug in end and start arrowhead length</li> <li>Fixed bug in grouplayout bbox</li> <li>Fixed typo group drawing elements</li> <li>Fixed bug in start_arrowhead for double_headed_arcs</li> <li>Fixed bug shorten_bezier_curve, shorten_elliptical_arc: segment-&gt;bezier_curve, elliptical_arc</li> <li>Fixed bug transform_elliptical_arc: self-&gt;elliptical_arc</li> <li>Fixed bug: north_north_easr-&gt;north_north_east</li> <li>Fixed bug setting dasharray to dashoffset</li> <li>Fixed bug with doubleheadedarc</li> <li>Fixed bugs</li> <li>Fixed bugd in DoubleTriangle: momapy.nodes-&gt;momapy.meta.shapes, border_drawing_elements-&gt;drawing_elements</li> <li>Fixed xsdata version to 23.8</li> <li>Fixed bug position of layouts</li> <li>Fixed bug RectangleCompartment</li> <li>Fixed not right model element when already exists in set</li> <li>Fixed bugs in shapes and receptor_layout</li> <li>Fixed bug in multi_mixin</li> <li>Fixed bug outside_model_element in make_and_add_compartment</li> <li>Fixed bug in build function for frozendict: self-&gt;obj</li> <li>Fixed bug with names of celldesignerspeciesreference and gene, rna, antisenserna</li> <li>Fixed bug with name of protein that was a property: species already have a name (from SBML)</li> <li>Fixed bug: did not take into account compartments</li> <li>Fixed few bugs</li> <li>Fixed bug: not taking into account compartment</li> <li>Fixed names</li> <li>Fixed bug</li> <li>Fixed bug</li> <li>Fixed type hint bug in association layout</li> <li>Fixed bug type hint BQ</li> <li>Fixed union bug</li> <li>Fixed bug: color alpha int-&gt;float</li> <li>Fixed bug: colors float-&gt;int</li> <li>Fixed bug: product was made with Reactant cls</li> <li>Fixed bug: uois added to svs</li> <li>Fixed buggy inheritance of stimulation layout and equivalence arc layout</li> <li>Fixed buggy inheritance of logic arc layout</li> <li>Fixed builder for forward refs; added type | new_type in builder to allow non builders in builders</li> <li>Fixed bug with colors: if colors not in list of color definitions, assumes its hex def</li> <li>Fixed bug when no list_of_color_definitions or no list_of_styles</li> <li>Fixed bug in split_line_coords</li> <li>Fixed with new presentation attributes</li> <li>Fixed type of transform from transformation to tuple[transformation]</li> <li>Fixed bug: subunit attr can be None</li> <li>Fixed typo</li> <li>Fixed bug: no unset in simple value grammar</li> <li>Fixed typo</li> <li>Fixed font_color to new stroke/fill</li> <li>Fixed typo</li> <li>Fixed typing bug</li> <li>Fixed imports</li> <li>Fixed cairo import</li> <li>Fixed bug render_curve_to</li> <li>Fixed bug when saving state</li> <li>Fixed bugs</li> <li>Fixed import errors</li> <li>Fixed kw_only in making skia renderer</li> <li>Fixed fill and stroke for textlayouts</li> <li>Fixed Close-&gt;ClosePath</li> <li>Fixed bugs</li> <li>Fixed bug font weight</li> <li>Fixed transformed for text</li> <li>Fixed new meta imports</li> <li>Fixed bug on subunit presentation attributes</li> <li>Fixed typo</li> <li>Fixed typo</li> <li>Fixed typos</li> <li>Fixed typo</li> <li>Fixed bug in bbox for shapely obj</li> <li>Fixed bugs</li> <li>Fixed typos</li> <li>Fixed bug: two times slope1</li> <li>Fixed Ellipse.to_path: now 4 elliptical arcs</li> <li>Fixed bug: use arrohead_base for computing arrohead angle</li> <li>Fixed bug: render_curve_to-&gt;add_curve_to</li> <li>Fixed bug: to_cubic-&gt;to_curve_to</li> <li>Fixed bug: fx-&gt;fX,fy-&gt;fY</li> <li>Fixed bug</li> <li>Fixed bugs</li> <li>Fixed bugs</li> <li>Fixed bug: n_segs not passed through for bezier curve to shapely</li> <li>Fixed bug: no compartment</li> <li>Fixed bug comparing points</li> <li>Fixed multipages</li> <li>Fixed bug: inverted is_sublayout relation</li> <li>Fixed bug in compartment_ref</li> <li>Fixed typo</li> <li>Fixed bbox for whole map</li> <li>Fixed bug when no state in sv</li> <li>Fixed bug in hex output</li> <li>Fixed bug: hex repr to 'x'</li> <li>Fixed bug: hex on int not float</li> <li>Fixed bug</li> <li>Fixed bug in get_mapping</li> <li>Fixed bug when no extension</li> <li>Fixed reverse order for points of consumption</li> <li>Fixed bugs</li> <li>Fixed bug when color_str started with '#'</li> <li>Fixed bugs with new layout_model_mapping</li> <li>Fixed bug for ior: now like or and not dict.ior</li> <li>Fixed bug</li> <li>Fixed bug with or selector</li> <li>Fixed bug in is_ovav: fields-&gt;dataclasses.fields</li> <li>Fixed bug in shortened when self.length() == 0</li> <li>Fixed bug in typing</li> <li>Fixed typo in model: switched SpeciesReference and SimpleSpeciesReference</li> <li>Fixed bug in coinsident lines</li> <li>Fixed bug points to segments</li> <li>Fixed bug: arc.x and arc.y</li> <li>Fixed typo in angle</li> <li>Fixed bug in crosspoint border drawing element</li> <li>Fixed bug: TextLayoutElement -&gt; TextLayout</li> <li>Fixed label textlayout width and height</li> <li>Fixed bug</li> <li>Fixed bug in stylesheet list</li> <li>Fixed bug in reading list of stylesheets</li> <li>Fixed bug with returning results</li> <li>Fixed bug when builders were used instead of built objects</li> <li>Fixed unused imports</li> <li>Fixed namespace error</li> <li>Fixed tidy; set_arcs_to_borders does not do anything if no intersection is found</li> <li>Fixed imports</li> <li>Fixed imports</li> <li>Fixed import</li> <li>Fixed bug</li> <li>Fix + vanted.css</li> <li>Fixed bug in frozendict ini</li> <li>Fixed frozendict bug: initialization from dict rather than list</li> <li>Fix bug</li> <li>Fixed bugs</li> <li>Fixed bug in set_arcs_to_borders</li> <li>Fixed bug when last_segment.length() == 0 in arc anchors</li> <li>Fixed bug: set_matrix -&gt; transform in cairo renderer</li> <li>Fixed bug in intersection of line and circle</li> <li>Fixed bug in render_maps</li> <li>Fixed bug in textlayoutelement</li> <li>Fixed bugs</li> <li>Fixed import bug</li> <li>Fixed bugs and replaced nodelabel by textlayout</li> <li>Fixed bugs in render_maps: calculation of min_x and min_y</li> <li>Fixed bug with labels of compartments in set_compartments...</li> <li>Fixed bug with PhantomLayoutElement</li> <li>Fixed bugs with arrowhead bboxes</li> <li>Fixed bug in copying in render_map with to_top_left==True</li> <li>Fixed bugs</li> <li>Fixed typo</li> <li>Fixed make_builder: Builder is now always a base class, and connectable_attributes are well set</li> <li>Fixed imports</li> <li>Fixed rendering of text with new coord system</li> <li>Fixed import</li> <li>Fixed imports</li> </ul>"},{"location":"CHANGELOG/#other_15","title":"Other","text":"<ul> <li>Updated docs</li> <li>Added workflow for building docs</li> <li>Updated doc</li> <li>Updated demo</li> <li>Rm unused import</li> <li>Updated with proper display for layout elements with transformations</li> <li>Bbox now takes into account ink_bbox of text</li> <li>Ruff</li> <li>Merge branch 'develop' into shapely_transform</li> <li>Merge branch 'demo' into develop</li> <li>Updated demo; fixed bug in utils</li> <li>Added transform</li> <li>Made transform a tuple of transformation</li> <li>Udpated docs</li> <li>Added ruff, pygments</li> <li>Rm red layout stroke</li> <li>Removed config from param for creating a skia renderer</li> <li>Updated doc</li> <li>Updated packages and removed sphinx related and black deps</li> <li>Updated doc</li> <li>Updated layout_model_mapping</li> <li>Update</li> <li>Merged</li> <li>Updated doc</li> <li>Updated documentation</li> <li>Updated documentation</li> <li>Merge branch 'develop' into docs_mkdocs</li> <li>Updated documentation</li> <li>Update</li> <li>Added reading sbml skeleton</li> <li>Updated demo</li> <li>Added EMPTY_SET key; fixed bug in writing compartmentRef attribute</li> <li>Updated functions to new layout_model_mapping</li> <li>Added replace=True to add_mappings</li> <li>Added replace option in add_mapping</li> <li>Support for annotations, compartment_refs</li> <li>Merge branch 'develop' into write_sbgnml</li> <li>Replaced call to Pango.extents_to_pixels by pos / Pango.SCALE to avoid new bug with function</li> <li>Merge branch 'develop' into write_sbgnml</li> <li>Added pdm-readiness</li> <li>Added cross_hv_of; cross_vh_of; set_mid_of; set_cross_hv_of; set_cross_vh_of functions</li> <li>Added support in CellDesignerModel for Regions</li> <li>Added mid_of function</li> <li>Closes #19</li> <li>Closes #18</li> <li>Closes #14: replace is not NoneValue by != NoneValue</li> <li>Closes #13: fixed bug in Color.to_rgba; fixed range to float in skia fill and stroke</li> <li>Closes #10</li> <li>Closes #9</li> <li>Updated dependencies</li> <li>Cleaned dependencies</li> <li>Added id and class in rendering</li> <li>Added structural state layout</li> <li>Added post_init to pointbuilder</li> <li>Replaced is momapy.drawing.NoneValue by ==</li> <li>For notes, annotations, ids: frozendict to dict</li> <li>Added hash and eq methods for NoneValue singleton</li> <li>Added handling non text file exception in read</li> <li>Added celldesigner pickle read and write</li> <li>Removed reduduntant exceptions from WriterResult</li> <li>Added _active to ids of active species</li> <li>Added support for dasharray in SingleNode</li> <li>Added support for stroke_dasharray</li> <li>Mappings are now frozendicts</li> <li>Merge branch 'develop' into cd_layout_model_mapping</li> <li>Rounded for shapely</li> <li>Rm unused import</li> <li>Replaced typing for source and target of arc to LayoutElement</li> <li>Added layout model mapping for reactions and modulations</li> <li>Added layout_model_mapping for species</li> <li>Changed require-python to &gt;=3.10</li> <li>Added SurjectionDict</li> <li>Merge branch 'develop' into write_sbgnml</li> <li>Modification residues are now sorted following their angle (ascending)</li> <li>Taken into account undefined names for modification residues with an order attribute</li> <li>Added order to modification residue</li> <li>Update with new sbgnml export</li> <li>Updated writing sbgnml</li> <li>Changed function to make notes; added support for map/model notes and annotations</li> <li>Added support for notes and annotations on the map itself</li> <li>Rm annotations to frozendict</li> <li>Added notes support for elements</li> <li>Added notes for compartments</li> <li>Added export of notes</li> <li>Updated to python 3.12</li> <li>Rm transformation of annotations and notes into frozen sets</li> <li>Changed python</li> <li>Start of new writer</li> <li>Added support for af unit of informations</li> <li>Added specific function for making activities</li> <li>Removed unnecessary _sbgnml_parser*</li> <li>Added support for labels in submaps</li> <li>Added new support for reading af without xsdata</li> <li>Added ids to map_element_to_ids</li> <li>Added ids to ReaderResult</li> <li>Removed print</li> <li>Merge branch 'develop' into cd_ids</li> <li>Merged sbgn_revamp</li> <li>Added model layout mapping</li> <li>Continued revamping</li> <li>Removed annotations and notes</li> <li>Added biomodel qualifiers</li> <li>Rvamped</li> <li>Removed old unused parse file</li> <li>Added map_element_to_ids mapping</li> <li>Removed useless dict transform of model_element_to_layout_element</li> <li>Removed unused parser file</li> <li>Annotations is now frozen</li> <li>Sbml.Annotation-&gt;sbml.RDFAnnotation</li> <li>List-&gt;set</li> <li>Added ids for reactants and products</li> <li>Stoichiometry can be None</li> <li>Finished model maker</li> <li>Added Palmytoylated</li> <li>Added PALMITOYLATED</li> <li>DON'T CARE-&gt;DON_T_CARE</li> <li>Removed unused re import</li> <li>AntisenseRNA-&gt;AntisenseRNA</li> <li>Optional listOfModification in reaction</li> <li>Added annotations</li> <li>Removed map_element_to_annotations from CellDesignerMap</li> <li>Added boolean logic gates for modulations</li> <li>Added logic gates</li> <li>Added class for logic arc layouts</li> <li>Changed size and font of logic gates</li> <li>Added gate layouts</li> <li>Continued revamping</li> <li>Removed useless newline</li> <li>Removed useless print</li> <li>Continued revamping</li> <li>Udpated</li> <li>Updated with_model/with_layout options</li> <li>Read/write_file-&gt;read/write; added os.PathLike type annotation</li> <li>More new methods</li> <li>In Writer: write()-&gt;write_file()</li> <li>Beginning of refactoring celldesigner reading</li> <li>Update</li> <li>Added annotations for included species</li> <li>First fix read_file</li> <li>New way of parsing: model, layout, map; annotations are outside of objects</li> <li>Udpate with doc</li> <li>Added undoc-members</li> <li>Changed conf.py location for new org</li> <li>Update docs/ organization</li> <li>Mv .readthedocs.yaml to root</li> <li>Added files for readthedocs</li> <li>Mv sphinx_docs-&gt;docs</li> <li>Removed ipykernel dependency</li> <li>Udpated demo</li> <li>Added dist/</li> <li>Udpate with classifiers and urls</li> <li>Updated package description</li> <li>Update</li> <li>Update</li> <li>Remove intersection of line with drawing and layout elements</li> <li>Added docs</li> <li>Updated demo</li> <li>Updated demo</li> <li>Moved demo to /; cleaned up</li> <li>Added documentation</li> <li>Udpated docs</li> <li>Udpated doc</li> <li>Updated doc</li> <li>Update doc</li> <li>New_builder-&gt;new_builder_object; added type hints</li> <li>New_builder-&gt;new_builder_object</li> <li>Added full input of SBGN AF maps</li> <li>Update</li> <li>Updated demo</li> <li>Updated type hints and methods</li> <li>Added from_top_left option</li> <li>Removed unused import</li> <li>Update demo</li> <li>Update with new attr names</li> <li>Added center to border and angle functions</li> <li>Update with new attr names</li> <li>Fill of layout set to NoneValue</li> <li>Changed qualified attrs to unqualified attrs (default)</li> <li>Added notebook</li> <li>Added docs\"</li> <li>Added init.py</li> <li>Added init.py</li> <li>To python 3.12</li> <li>Added documentation</li> <li>Updated</li> <li>Added particular anchors for celldesigner shapes</li> <li>Moved labels of modifications</li> <li>Taken into account active; fixed bug in position of compartments</li> <li>Added some anchor points</li> <li>Added support for modulations</li> <li>Taken into account catalysis</li> <li>Put labels in right place</li> <li>Added replacement of text by character</li> <li>Added functions to find angle and border of shapely_object and collection of drawing elements</li> <li>Update with dissociation</li> <li>Replaced fit layout by the size of the layout</li> <li>Added super_or_builder method</li> <li>Added todos</li> <li>Remade taking into account edit points with center or link anchor</li> <li>Added anchor_point method to layout element</li> <li>Added comment</li> <li>Add get_transformation_for_frame_function</li> <li>Partially fixed bug when no link anchor</li> <li>Taken into account omitted transition, unknown transition, transcription, translation</li> <li>Taken into account start node in base reactant layout</li> <li>Refacrtored state_transition_layout and add heterodimer_association_layout</li> <li>Took into account base reactants and edit points for association</li> <li>Added layout for state transition, with reactants and products</li> <li>Begined adding layout for reactions</li> <li>Added with_layout option to prevent from making the layout</li> <li>Added intermediate cardinal points</li> <li>Added state transition layout prototype</li> <li>Added compartment layouts; refactored code</li> <li>Add_or_replace_element_in_set now returns the element that is in the set at the end</li> <li>Code layout</li> <li>In _Modifier: species-&gt;referred_species</li> <li>Replaced .species by .referred_species in SpeciesReferences; replaced CellDesignerSpeciesReference by SpeciesTemplate</li> <li>Added reading of empty state variable</li> <li>Readded Annotation</li> <li>Removed notes and annotations from sbase</li> <li>Support for parsing sbml annotations inside notes</li> <li>Added support for reduced boolean logic gates</li> <li>Added listOfGateMember to reaction</li> <li>Rm make layout and model_layout_mapping for celldesigner</li> <li>Added None to model, layout and layout_model_mapping</li> <li>Added annotations</li> <li>Map_element_to_annotation-&gt;map_element_to_annotations</li> <li>Added rdf annotations for species</li> <li>Added function to make annotations</li> <li>Qdded attributeFormDefault='qualified' for rdf</li> <li>Added map_element_to_annotation to Map</li> <li>Added attributes of annotations</li> <li>Added empty state which arises sometimes</li> <li>Changed to using aliases instead of species</li> <li>Added acetylated modification</li> <li>Updated schemas and changed location</li> <li>Now transforming aliases rather than species</li> <li>Added hypothetical for species</li> <li>Taken into account: outside for complexed; modifiers with gates</li> <li>Modification-&gt;modificationType</li> <li>Homodimer-&gt;homomultimer</li> <li>Adapted to minerva bug on type for rna, gene and anitsenserna references</li> <li>Added # abstract</li> <li>Removed momapy-model from ids for model elements</li> <li>Removed metaid, notes and annotations of comparison for species</li> <li>Udpated the celldesigner schema with reduced notation</li> <li>Update</li> <li>Cleaned model</li> <li>Cleaned the model</li> <li>Update</li> <li>Cleaned modulations in CellDesignerModel</li> <li>Cleaned modulations and modifiers</li> <li>Cleaned modifier and added comment</li> <li>Modulators-&gt;modifiers</li> <li>No printing of base types or objects evaluating to False (e.g., empty collections)</li> <li>Removed structuralstatevalue: now always str</li> <li>Checks for subclass rather than exact type matching in exclude_cls</li> <li>Cleaned make parser for celldesigner</li> <li>Merge branch 'develop' into celldesigner</li> <li>Updated python version to &gt;3.10 and &lt;3.11</li> <li>Added AuxiliaryUnit concept</li> <li>Redid tpying.union for forwardref</li> <li>Replaced referes_to attribute by reference attribute</li> <li>Got rid of typing.Union</li> <li>Made the ids of model elements deterministic</li> <li>Cleaned</li> <li>Added add_or_replace_elemement_in_set function</li> <li>Merge branch 'fix_builder' into celldesigner</li> <li>Removed UndefinedVariable class; use order instead</li> <li>Added annotations reading for macromolecules</li> <li>Added superclass BiomodelQualifier for BQModel and BQBiol</li> <li>Added module for forwardref of logicaloperator</li> <li>Moved make_pango to new _pango module; forced ids to str</li> <li>Cleaned up</li> <li>Applying stylesheet to objects; to builder becomes _ function</li> <li>Update</li> <li>Shortening a segment of length 0 now returns a copy of the segment</li> <li>Update</li> <li>Updated code</li> <li>Update with new way: build objects</li> <li>Exluded Point from collections in pretty_print function</li> <li>Added class exclusion feature in pretty print; changed color:black-&gt;green</li> <li>Simplified names of mappings</li> <li>Added  make_union option to the transform_type</li> <li>Removed add_element method</li> <li>Builder transform type: now returns type_ | new_type</li> <li>Bug fix: stopped transforming default factory functions</li> <li>Changed variable names: field_-&gt;field, mappings to more conventional names</li> <li>Antisens-&gt;Antisense</li> <li>Merge branch 'develop' into celldesigner</li> <li>Can perform functions on map</li> <li>Changed check_file methods: no use of parser anymore</li> <li>Render extension: made attributes qualified</li> <li>Cleaned up; moved schemas and make_parser outside of src</li> <li>Added parser for sbgnml_0_2 and _0_3</li> <li>Updated the README with link to demo</li> <li>Udpated the demo</li> <li>Update</li> <li>Added License</li> <li>Added text to README</li> <li>Merge branch 'develop' into celldesigner</li> <li>Removed tests from git</li> <li>Readded celldesigner/</li> <li>Readded sbml/core.py</li> <li>Deleted sbml/ and celldesigner/ to move them to branches</li> <li>Pipenv-&gt;pdm; cleaned up</li> <li>Changed rectangle compartment</li> <li>Merge branch 'develop' into celldesigner</li> <li>Added script for making a css template file from a module</li> <li>Updated and added styles with @import</li> <li>Updated demo</li> <li>Changed defaults for subunits attributes</li> <li>Updated with new presentation attributes</li> <li>Changed defaults for presentation attributes</li> <li>LogicArc-&gt;LogicArcLayout;EquivalenceArc-&gt;EquivalenceArcLayout</li> <li>Added | None to types for stroke_width</li> <li>Path_shorten-&gt;shorten; added start_shorten to Arc</li> <li>Now built with @import cs_black_and_white and newt_no_cs</li> <li>Added chdir for relative css @import</li> <li>Added import statement feature inside css</li> <li>StyleSheet read_file-&gt;StyleSheet.from_file; join_style_sheets-&gt;combine_style_sheets;read_string-&gt;from_string; added from_files classmethod</li> <li>Update</li> <li>Pseudo-fixed bug with unset</li> <li>Update</li> <li>Added script to make a css template</li> <li>Harmonize initial values</li> <li>Layout is now a Node</li> <li>Added unset css value, that is transformed to Python's None</li> <li>Added default values for presentation attrs of SBGNNode</li> <li>Changed list_colors to print_colors; list_colors now list all pairs (color_name, color) of the module</li> <li>Cleaned up code: new SBGNSingle/DoubleHeadedArc; arrowhead_drawing elements coded for arcs instead of subclassing; cleaned attributes order</li> <li>Updated layouts with new shapes</li> <li>Cleaned typing</li> <li>File creation</li> <li>Added make_node and make_arc functions</li> <li>Changed default values for presentation attritubutes in TextLayout; use of make_pango_layout from utils in textLayout</li> <li>Added get-initial_value method; fixed bug font weight mapping</li> <li>Added get_initial_value method</li> <li>Added FONT_WEIGHT_VALUE_MAPPING</li> <li>Moved testing functions to testing; added make_pango_layout to utils; removed print_classes from utils</li> <li>Added transformation of elliptical arc to bezier curves; cleaning</li> <li>Cleaning</li> <li>Cleaned up</li> <li>Added sbmlutils</li> <li>Continued coding drawing_elements methods</li> <li>Update</li> <li>Font family and size become mandatory on text layouts</li> <li>Filter on all drawing elements</li> <li>Added new StatefulRenderer</li> <li>Added fill to textlayout</li> <li>To python 3.11</li> <li>Normalized presentation attributes; support in svg-native renderer</li> <li>Update with new drawing elements</li> <li>Added new classes following the spec</li> <li>Added import for svg_native renderer</li> <li>Added styling: fill, stroke, font</li> <li>Added support for font style and weight; added support for text-anchor (not skia)</li> <li>Added fill rule</li> <li>Merge branch 'develop' into sbml_layout</li> <li>Removed childless method from GroupLayout</li> <li>NodeLayout-&gt;Node; ArcLayout-&gt;Arc</li> <li>Update</li> <li>Shorten-&gt;path_shorten</li> <li>Added defaults for nodes and arcs; cleaned up</li> <li>Removed nodes width and height in render function</li> <li>Changed defaults of metas</li> <li>Taken into account new shape buisness</li> <li>Simple and Multi mixins return shapes instead of nodes</li> <li>Cleaned up</li> <li>Moved Shape to core</li> <li>Shapes, nodes and arcs to meta dir</li> <li>Revamped to shapely for elliptical arc function</li> <li>Revamped _split_line_string function</li> <li>Removed translated method from layout_element since not used anywhere</li> <li>Updated nodes</li> <li>Finished nodes and added all arcs</li> <li>Removed arrowhead_fill from defaults</li> <li>Reverted width for Bar: should only be arrowhead_stroke_width</li> <li>Added width for Bar</li> <li>Added rendering_testing for arcs; changed rendering nodes on grid to rendering_layout_elements</li> <li>Finished rendering function</li> <li>Cleaned up</li> <li>Renamed geometry functions; added some new functions</li> <li>Added rendering utility functions</li> <li>Revamped intersection function</li> <li>Changed defaults and attributes' order</li> <li>Revamped nodes</li> <li>New styling attributes for connectors</li> <li>Revamped sbgn nodes</li> <li>Cleaned defaults</li> <li>Increased error tolerance for elliptical_arc to shapely</li> <li>Added new arcs</li> <li>Cleaned code and fixed bug with arrowhead_length==0</li> <li>Shapes-&gt;nodes</li> <li>Now SingleHeaded and DoubleHeaded arcs</li> <li>Shapes-&gt;nodes</li> <li>Started to change to new border style</li> <li>New start and end arrowheads in arc; new border in node</li> <li>New and updated functions for segments, elliptical arcs and bezier curves</li> <li>Cleaned up</li> <li>Added beginning of layout and render</li> <li>First commit</li> <li>Added x and y attribute to Layout</li> <li>MapLayout-&gt;Layout</li> <li>Added anchors for MapLayout</li> <li>Width cannot be &lt; to height</li> <li>Removed arrowhead_bbox from arcs</li> <li>Removed addition in path; Close-&gt;ClosePath action</li> <li>Update</li> <li>Vanted-&gt;sbgned</li> <li>Cleaned up</li> <li>Updated demo</li> <li>Updated demo</li> <li>Added pretty print function</li> <li>Added apply_style_sheet to map</li> <li>Removed return map from write</li> <li>Added compartments to entities</li> <li>Model and layout are now non optional</li> <li>Added support for notes</li> <li>Update</li> <li>Changed notes parsing to strict</li> <li>Added listOfBackgroundImages</li> <li>Added annotations to map, notes to map, model, model_element</li> <li>Cleaned up</li> <li>Added option for reading and writing annotations</li> <li>Support for writing annotations</li> <li>Added annotations to sbgnmodel</li> <li>Specified annotation class</li> <li>Added support for sbml miriam annotation</li> <li>Package name from momapy.about</li> <li>Added package name</li> <li>Added option for render_information</li> <li>Added support for more glyphs</li> <li>Added init.py</li> <li>Remade parser</li> <li>Added render information attributes</li> <li>Added writing render information</li> <li>Added about with version and author</li> <li>Added leading # to to_hex and to_hexa return string</li> <li>Added to_hex and to_hexa methods</li> <li>Update sbgnml writer</li> <li>Update</li> <li>Udpate</li> <li>Updated tidy function and nodes fit labels function</li> <li>Udpated set_arcs_to_borders</li> <li>Update</li> <li>Updated newt.css</li> <li>Added reversibility of processes; left-to-right of processes and operators</li> <li>Added get_mapping to mapbuilder</li> <li>Added upack option to get_mapping: unpacks the frozenset(frozenset) result; added get_mapping to map</li> <li>Added left_to_right attribute to layout with connectors</li> <li>Removed source and target from children in ArcLayout</li> <li>Added reversibility for processes</li> <li>New set_arcs_to_borders function</li> <li>Added monitoring</li> <li>Changed default renderer to skia</li> <li>Update</li> <li>Cleaned up</li> <li>New subpackage</li> <li>Cleaned up</li> <li>Changed to reader class</li> <li>Staticmethod-&gt;classmethod</li> <li>Staticmethod-&gt;classmethod</li> <li>Added new reader class</li> <li>Changed parser file name</li> <li>Updated read function; added perturbation unit of informaion to schema</li> <li>Take into account map bbox</li> <li>New read write functions</li> <li>Added additional error: class where add_element is called</li> <li>Base_left/right_connector-&gt;left/right_connector_base/g</li> <li>Added two new anchors: left_connector_tip and right_connector_tip</li> <li>Update</li> <li>Update</li> <li>Added utils module with a new str func</li> <li>Added new layout_model_mapping</li> <li>To python 3.11</li> <li>Added pyparsing to setup</li> <li>Cleaned</li> <li>ObjectBoundingBox-&gt;ObjectBoundingBox</li> <li>Writes to file only if output_file is defined in config</li> <li>Added reactant and product edit points</li> <li>Changed default width and height of reaction node</li> <li>New nodes taken into account</li> <li>Update</li> <li>Fixed bug in copying dict for joining two stylesheets</li> <li>Removed geometry import</li> <li>Update</li> <li>State variable set to '' is now considered to be None</li> <li>Child can now be None and</li> <li>Added default width and height to Arrow</li> <li>More shapes taken into account</li> <li>Added CellDesigner shapes and layout elements</li> <li>Update</li> <li>Added SBGNPD/AFLayout</li> <li>Update</li> <li>Cleaned up</li> <li>Update</li> <li>Build methods and from_object methods now take a mapping as parameter which avoids rebuilding identical object</li> <li>Remove colors object: momapy.coloring.colors. -&gt; momapy.coloring.; rgb, rgba, hex, hexa functions-&gt;from_rgb, from_rgba,... methods of Color</li> <li>Update</li> <li>Model_layout_mapping -&gt; layout_model_mapping</li> <li>Updated dependencies</li> <li>Kw_only and new defaults</li> <li>Updated to python 3.10; added Pipfile*</li> <li> <ul> <li>in css is now possible; replace _ by - in css of sbgn</li> </ul> </li> <li>Updated notebook</li> <li>Revert \"updated notebook and cleaned up\"</li> </ul> <p>This reverts commit c115e10923928ebf55f116931bfbf39504451429. - Updated notebook and cleaned up - Changed defaults for af units of information - Made style for logic arc - Redeveloped svg native renderer - Changed arguments order in SkiaRenderer - Bug in renderer ini and added antialiasing to imagefilter painting - Added new reaction types - Added reactions and reactionmofication - Added PHYSICAL_STIMULATION to reaction types - Preserves init from being overwritten by xsdata - Trigger-&gt; triggering + typo - Updated CellDesigner schema - Added Compartment - Other gates; complexes; compartments - Typo - Added Modulator and changed modifiers ontology - Added rest of modifier types - Updated read_file - Modulators -&gt; modifiers - Modulator to Modifier - Added boolean logic gates - Added return value for read_file - Update - Cleaned - Added white colorscheme - Added classselector; simplified typeselector code - Update - Added rendering methods for all filter effects - Fixed bug;added enums for filter effects - Added with_alpha method - Added notebook - Added is_submapping for ModelLayoutMapping; cleaned is_sublayout - Position of text in TextMixin is now label_center() rather than position - Updated - Cleaned up - Added dasharray property - Svg implementation of get_region_filter - Updated newt style - Updated demo - Added get_filter_region method to drawing element - Bug in shapely method of elleptical arc - Implemented drop shadow filter - Replaced path in stadium by rectangle - Updated demo - Cleaned up - Cleaned up - Updated to_shapely method for elliptical arc - Changed sweep flag of stadium shape - Udpated demo - Added to_polygons option to to_shapely method - Fit nodes to labels: to logical bbox - Reverted Direction back to momapy.core - Anchors no longer abstractmethods, but implemented in parent nodelayout class - Moved Direction to sbgn.core - Added file - Updated gitignore - Updated dependencies - Fraction moved to core - New intersection method with shapely - Intersection function: new name - Updated demo - Added demo as subpackage - Cleaned; added general intersection function - Added shapely geometry - Added shapely dependency - Cleaned up and added div for point(builder) - Take into account bezier curves - Rearranged fraction function for bezier curve - Added bezier curve; core/arc: changed points to segments - Changed to python 3.9; added bezier module - Reworked transformed method for elliptical arc - Arrowhead now used transformed method instead of svg-style transform attribute - Typo - Removed arc drawing element - Removed arc drawing element - Added transformed methods - Updated rendereres - Bug skia renderer no dataclass decorator - Updated demo - Added fraction method to arclayout - Cleaned up - Updated demo - Added default width and height to SBGN glyphs - Cleaned up - Updated demo - Update - Added anchors to textlayout; fixed bugs in angles - Added anchors for crosspoint - Merged - Updated demo - Ignore demo/*.svg - Added demo - Moved is_sublayout method from LayoutElement to MapLayout - Updated vanted and newt styles with filter: none - Ink_bbox() -&gt; bbox() - Updated vanted and newt styles with filter: none - Updated newt style sheet - Updated stylesheet - Descendants-&gt;ancestors in apply_style_sheet function - Added newt stylesheet - Cleaned up - Added submap, submodel and sublayout methods; added translated method - Added crosspoint - Updated demo - Anchors are computed automatically; shapes are computed using joints - Added demo and cleaned up - Updated celldesigner model and io - Merge branch 'cd' into developpment - Cd model elements now inherit from sbml core elements - Merge skia - Updated with skia - Added skia renderer - X/ysep for nodes set to 5 - Updated with required package skia-python - New filter effects and new svg-native-compat renderer - Implemented af model, layout and rendering - Merge remote-tracking branch 'origin/cd_dev' into cd - New model elements - Merge branch 'main_dev' into cd_dev - New celldesigner model - Added connectors stroke width - Added CardinalityLayout - Added support for cardinality and stoichiometry - Added package data - Import sbgn stylesheets - Upadted setup.py - New terminal layout - Shadows for subunits - Added filter for subunits in sbgn shape - Added none possibility for filters - Clean up - Color for subunits of macromolecule multimer layout - Added list value type - New fit submaps to content function - New styles for sumap and tag layout - Added new directions - Added submap and tag layouts - Set position of layout to position of label with anchor label_center - Added inverted hexagon shape and perturbing agent layout - Updated to new SBGNShape style - Updated with beginning of metaclass - Cleaned up - Is_ovav method - Clean up - Clean up code - Updated style sheets - Changed default filter units to object bounding box - Translation-&gt;translate in svg-native renderer - +import momapy.drawing - Impl copy and deepcopy for NoneValue singleton - Update - Added set_complexes_to_fit_content utils function - Fill of consumption to none - Removed shadows for arcs - Emptyset must have a stroke color - Set stroke to none - Bug _render_filters - Typo - Typo - Typo - Typo - Added shadows.css stylsheet - Added drop-shadow in styling css - Added filter with drop shadow effect - Typo - Layout fill to white - Changed font sizes in shapes with text - Aux units font - Font to Cantarell - Font to cantarell - Text drawing element: font_color replaced by fill and stroke - Rgba to rgb and opacity - Layout fill: none; textlayout: font_color - Default sbgn style: fill of layout set to none - Styling none returns NoneValue - Default fill value to black - Set_size-&gt;set_absolute_size in pango font description - String to output file - Set_size-&gt;set_absolute_size for pango font description - Merge branch 'arc_to_elliptical' into native_svg - Changed arc to elliptical arc in shapes - More methods for native svg renderer - First class and methods for nativesvgrenderer - Int to float - Multiple style sheets; join style sheets - Update - Added default_colorscheme.css - +width and height - Added default stylesheet for sbgn - Added strict option to applying collection of styles - Removed _ from color names to be consistent with css - Added none value that does not work - Added has_color method to colors - Added none value in style_sheets - Added set_auilliary_units_to_borders function - Added self_border and self_angle functions - Splited fontdescription into fontsize and fontfamily - +pyparsing - Added parser for stylesheets - Clean up - In node layout: removed foreground_path; renamed background_path to border_drawing_element - Render_maps(): changed bbox to self_bbox to compute surface width and height - Changed drawing path of Layout: bbox() -&gt; self_bbox() - Cleanup - Added ancestor and parent selectors;type selector-&gt;class selector; now ancestors in select method - Added children mehtod to layouts - Added tidy and stylesheet options to read_file - Can now supply a mapbuilder to apply_style_sheet - Added set_arcs_to_borders function - Cleaning - Addition of point to point builder - Added automatic border functions and related changes - New function for intersection of line and ellipse - Removed y_from_x methods (not much sense) - New geometry shapes: Arc, Circle; new intersection functions - Recodes parallel lines; added slope and intercept to line; recoded intersection of lines - Added set_layout_to_fit_content function - Added set_nodes_to_fit_labels - Label_center taken into account and bold text in shapes - Added label_center anchor - Added vertical alignment for textlayoutelement - Bugs and replaces text by new text and textlayout - Changed nodelabel to textlayoutelement - New text drawing element and textlayoutelement - Added style_sheets from styling to render_map(s) - Added position to sbgn maps when reading sbgn-ml file - Added position to layout - Removed optional from transform attribute of drawing element (default is tuple) - Dfault for transform attribute is set to tuple - Merge branch 'main_dev' into sbgn_dev - Fit now return position, width, height and not bbox: fixed related bugs in self.bbox() functions - Bug in set_compartments_to_fit_content - Merge branch 'main_dev' into sbgn_dev - Bug in set_fit function - New set_compartments_to_fit_content function - Merge branch 'main_dev' into sbgn_dev - Fit returns position, width and height rather than a bbox; implemented set_fit function - Added new sbgn shapes - Added EmptySetLayout to emptyset and source and sink - New styling feature - Completed fraction function - Recoded arcs with anchors - Cleaned code - Maj - Bug restore after stroke and fill; factored _stroke_and_fill - Added height and width in _make_renderer_for_render_function - Typo in parameter name - Added a function to render a collection of drawing elements and factored make_renderer_for_render_function - Added ellipse to drawing and rendering - Added arcs to drawing and rendering - Typo - Added origin point for rotation - Experimental update with updated_object - Revamped relative_point to updated_object - Removed prints - New animating module - Bug fix - Bug fix in Color.or - Bug fix in Color.or - Added png rendering - Updated Pipfile - Cleanup - Cleanup - Cleanup - Cleaned imports - Added alpha modification to Color - Mod essai.py - Cleanup - Reference-&gt;phantomlayoutelement, fixed bugs, fixed positioning with new coord sys - Added self_bbox to grouplayout and to nodelayout - Mod bbox to follow new coord system - Mod essai.py - Inverted y axis to mimick svg and sbgnml - Cleaned rendering and drawing code - Updated gitignore - Updated grouplayoutelement - Cleanup - Renderer to rendering - Renderer.py to rendering.py - Cleanup - Removed position.py.save - Added folder momapy/ - Removed submodule momapy - Updated gitignore - Updated pipfile - Initial commit</p>"},{"location":"CHANGELOG/#styling_5","title":"Styling","text":"<ul> <li>Styles to/from render_information: border&gt;group attrs for nodes, arrowhead&gt;path&gt;group attrs for arcs</li> <li>Stylesheet is now unfrozen so it can be easily modified</li> <li>Styles in grouplayoutelement applied to the top group element; arcs have styles for arrowhead</li> <li>Style_sheets from files and multiple style_sheets</li> <li>Stylesheet applied to layout instead of map</li> </ul>"},{"location":"about/","title":"About","text":""},{"location":"about/#project","title":"Project","text":"<p>momapy is a Python library for working with molecular maps. It supports SBGN PD and AF maps and CellDesigner maps, providing a unified API for reading, writing, inspecting, styling, and rendering them.</p> <p>The project is developed at the University of Luxembourg.</p>"},{"location":"about/#authors","title":"Authors","text":"<ul> <li>Adrien Rougny \u2014 lead developer (adrien.rougny@uni.lu)</li> </ul>"},{"location":"about/#contributing","title":"Contributing","text":"<p>Bug reports and feature requests are welcome via the GitHub issue tracker.</p> <p>To contribute code:</p> <ol> <li>Fork the repository on GitHub.</li> <li>Create a feature branch.</li> <li>Make your changes, following the coding conventions in <code>CLAUDE.md</code>.</li> <li>Run the test suite: <code>pytest tests/</code></li> <li>Open a pull request against <code>main</code>.</li> </ol> <p>All commits must follow Conventional Commits (enforced via commitlint). See <code>CLAUDE.md</code> for the list of valid types and examples.</p>"},{"location":"about/#versioning-and-deprecation-policy","title":"Versioning and deprecation policy","text":"<p>momapy loosely follows semantic versioning as a guideline, not a strict contract.</p> <p>Below <code>1.0</code> the public API is not stable and may change in a breaking way in any release.</p> <p>From <code>1.0</code> onward, breaking changes are introduced gradually where practical: a deprecated API keeps working and emits a <code>DeprecationWarning</code> for at least one release before removal, naming a replacement when one exists.</p> <p>The public API is what the documentation and each subpackage's <code>__all__</code> describe. Underscore-prefixed names are not public and may change at any time.</p>"},{"location":"about/#license","title":"License","text":"<p>This code is distributed under the GPLv3 license.</p>"},{"location":"cli/","title":"CLI Reference","text":""},{"location":"cli/#overview","title":"Overview","text":"<p>The <code>momapy</code> command-line interface (CLI) allows you to work with molecular maps directly from the command line without writing Python code. It supports SBGN-ML and CellDesigner map formats.</p> <p>Available subcommands:</p> <ul> <li><code>render</code> \u2014 Render maps to image formats (SVG, PDF, PNG, PS, JPEG, WebP)</li> <li><code>export</code> \u2014 Export maps back to their original format (useful for roundtrip testing)</li> <li><code>info</code> \u2014 Print a summary of a map file's contents</li> <li><code>list</code> \u2014 List available readers, writers, renderers, colors, styles, or stylable attributes</li> <li><code>tidy</code> \u2014 Apply layout tidying operations (fit nodes, snap arcs, etc.)</li> <li><code>style</code> \u2014 Bake CSS stylesheets into map data and output the styled map</li> <li><code>visualize</code> \u2014 Open an interactive viewer in the default web browser</li> </ul>"},{"location":"cli/#synopsis","title":"Synopsis","text":"<pre><code>momapy render &lt;input_file_path&gt;... -o &lt;output_file_path&gt; [options]\nmomapy export [&lt;input_file_path&gt;] [-o &lt;output_file_path&gt;] [options]\nmomapy info [&lt;input_file_path&gt;] [options]\nmomapy list {readers,writers,renderers,colors,styles,attributes}\nmomapy tidy &lt;operation&gt; [&lt;input_file_path&gt;] [options]\nmomapy style [&lt;input_file_path&gt;] [options]\nmomapy visualize [&lt;input_file_path&gt;] [options]\n</code></pre>"},{"location":"cli/#subcommand-render","title":"Subcommand: <code>render</code>","text":"<p>Renders one or more molecular maps to an image file.</p>"},{"location":"cli/#arguments","title":"Arguments","text":"Argument Description <code>input_file_path</code> One or more input file paths (SBGN-ML or CellDesigner format; reads from stdin if omitted)"},{"location":"cli/#options","title":"Options","text":"Option Short Description <code>--output-file-path</code> <code>-o</code> Output file path (required) <code>--renderer</code> <code>-r</code> Renderer to use: <code>svg-native</code>, <code>svg-native-compat</code>, <code>skia</code>, <code>cairo</code> (auto-detected if omitted) <code>--format</code> <code>-f</code> Output format: <code>svg</code>, <code>pdf</code>, <code>png</code>, <code>ps</code>, <code>jpeg</code>, <code>webp</code> (inferred from extension if omitted) <code>--multi-pages</code> <code>-m</code> Render one map per page (for multi-input PDF) <code>--to-top-left</code> <code>-l</code> Move elements to the top-left of the page <code>--tidy</code> <code>-t</code> Tidy the map (reroute arcs, fit labels, etc.) <code>--style-sheet-file-path</code> <code>-s</code> Custom CSS style sheet file path (repeatable) <code>--preset</code> <code>-p</code> Built-in preset name (repeatable); see <code>momapy list styles</code>"},{"location":"cli/#rendererformat-compatibility","title":"Renderer/Format Compatibility","text":"Format svg-native svg-native-compat skia cairo SVG \u2713 \u2713 \u2713 \u2713 PDF \u2717 \u2717 \u2713 \u2713 PNG \u2717 \u2717 \u2713 \u2713 PS \u2717 \u2717 \u2717 \u2713 JPEG \u2717 \u2717 \u2713 \u2717 WebP \u2717 \u2717 \u2713 \u2717 <p>Note: The <code>svg-native</code> and <code>svg-native-compat</code> renderers are always available and have no external dependencies; <code>svg-native-compat</code> converts filters (e.g. drop shadows) into a form that renders in older SVG viewers. The <code>skia</code> and <code>cairo</code> renderers require optional dependencies to be installed.</p>"},{"location":"cli/#examples","title":"Examples","text":""},{"location":"cli/#basic-rendering","title":"Basic rendering","text":"<p>Render an SBGN-ML file to SVG:</p> <pre><code>momapy render my_map.sbgn -o output.svg\n</code></pre>"},{"location":"cli/#render-to-pdf","title":"Render to PDF","text":"<p>Render to PDF (requires skia or cairo renderer):</p> <pre><code>momapy render my_map.sbgn -o output.pdf\n</code></pre>"},{"location":"cli/#specify-renderer-explicitly","title":"Specify renderer explicitly","text":"<p>Force the use of a specific renderer:</p> <pre><code>momapy render my_map.sbgn -o output.pdf -r cairo\n</code></pre>"},{"location":"cli/#multiple-input-files","title":"Multiple input files","text":"<p>Render multiple maps into a single multi-page PDF:</p> <pre><code>momapy render map1.sbgn map2.sbgn map3.sbgn -o combined.pdf -m\n</code></pre>"},{"location":"cli/#apply-style-sheets","title":"Apply style sheets","text":"<p>Apply one or more CSS style sheets to customize the appearance:</p> <pre><code>momapy render my_map.sbgn -o output.svg -s custom_styles.css\nmomapy render my_map.sbgn -o output.svg -s base.css -s overrides.css\n</code></pre>"},{"location":"cli/#adjust-layout-position","title":"Adjust layout position","text":"<p>Move all elements to the top-left corner of the output:</p> <pre><code>momapy render my_map.sbgn -o output.svg -l\n</code></pre>"},{"location":"cli/#installation","title":"Installation","text":"<p>The CLI is installed automatically when you install momapy:</p> <pre><code>pip install momapy\n</code></pre> <p>To use PDF, PNG, PS, JPEG, or WebP output formats, install the optional rendering dependencies:</p> <pre><code>pip install momapy[skia]  # For skia renderer\npip install momapy[cairo]  # For cairo renderer\npip install momapy[all]    # For all renderers\n</code></pre>"},{"location":"cli/#subcommand-export","title":"Subcommand: <code>export</code>","text":"<p>Reads a map and writes it back in the same format. This is useful for roundtrip testing (read with the reader, write with the writer) and for applying transformations like tidying or styling before re-exporting.</p>"},{"location":"cli/#arguments_1","title":"Arguments","text":"Argument Description <code>input_file_path</code> Input file path (SBGN-ML or CellDesigner format; reads from stdin if omitted)"},{"location":"cli/#options_1","title":"Options","text":"Option Short Description <code>--output-file-path</code> <code>-o</code> Output file path (default: stdout) <code>--text</code> <code>-T</code> Write human-readable XML to stdout instead of a pickled <code>ReaderResult</code> (the default when piping); no effect with <code>-o</code> <code>--tidy</code> <code>-t</code> Tidy the map (reroute arcs, fit labels, etc.) <code>--to-top-left</code> <code>-l</code> Move elements to the top-left of the page <code>--style-sheet-file-path</code> <code>-s</code> Custom CSS style sheet file path (repeatable) <code>--preset</code> <code>-p</code> Built-in preset name (repeatable); see <code>momapy list styles</code> <p>The writer is inferred automatically from the map type: SBGN maps are exported using the <code>sbgnml</code> writer, CellDesigner maps using the <code>celldesigner</code> writer. SBML is read-only and cannot be exported.</p> <p>If no output file is specified, the result is written to standard output. When the output is a pipe, a pickled <code>ReaderResult</code> is written by default (so <code>momapy</code> commands can be chained); pass <code>-T</code> to emit XML text instead.</p>"},{"location":"cli/#examples_1","title":"Examples","text":""},{"location":"cli/#export-to-stdout","title":"Export to stdout","text":"<pre><code>momapy export my_map.sbgn\n</code></pre>"},{"location":"cli/#basic-roundtrip","title":"Basic roundtrip","text":"<p>Export an SBGN-ML file:</p> <pre><code>momapy export my_map.sbgn -o output.sbgn\n</code></pre> <p>Export a CellDesigner file:</p> <pre><code>momapy export my_map.xml -o output.xml\n</code></pre>"},{"location":"cli/#export-with-tidy","title":"Export with tidy","text":"<p>Tidy the map before exporting (reroute arcs, fit labels, etc.):</p> <pre><code>momapy export my_map.sbgn -o output.sbgn -t\n</code></pre>"},{"location":"cli/#apply-style-sheets_1","title":"Apply style sheets","text":"<p>Apply a stylesheet before exporting:</p> <pre><code>momapy export my_map.sbgn -o output.sbgn -s custom_styles.css\nmomapy export my_map.sbgn -o output.sbgn -s base.css -s overrides.css\n</code></pre>"},{"location":"cli/#subcommand-info","title":"Subcommand: <code>info</code>","text":"<p>Prints a summary of a map file's contents, including the map type, model element counts, and layout dimensions.</p>"},{"location":"cli/#arguments_2","title":"Arguments","text":"Argument Description <code>input_file_path</code> Input file path (SBGN-ML or CellDesigner format; reads from stdin if omitted)"},{"location":"cli/#options_2","title":"Options","text":"Option Short Description <code>--output-file-path</code> <code>-o</code> Output file path (default: stdout) <code>--format</code> <code>-f</code> Output format: <code>text</code> (default) or <code>json</code>"},{"location":"cli/#examples_2","title":"Examples","text":""},{"location":"cli/#inspect-an-sbgn-ml-file","title":"Inspect an SBGN-ML file","text":"<pre><code>momapy info my_map.sbgn\n</code></pre> <p>Output:</p> <pre><code>File:      my_map.sbgn\nMap type:  SBGN PD\n\nModel:\n  compartments:             0\n  entity pools:             28\n  processes:                10\n  modulations:              10\n  logical operators:        0\n  equivalence operators:    0\n  submaps:                  0\n  tags:                     0\n\nLayout:\n  dimensions:               1170.0 x 560.0\n  elements:                 122\n</code></pre>"},{"location":"cli/#json-output","title":"JSON output","text":"<pre><code>momapy info my_map.sbgn --format json\n</code></pre> <p>Output:</p> <pre><code>{\n  \"map_type\": \"SBGN PD\",\n  \"model\": {\n    \"compartments\": 0,\n    \"entity_pools\": 28,\n    \"processes\": 10,\n    \"modulations\": 10,\n    \"logical_operators\": 0,\n    \"equivalence_operators\": 0,\n    \"submaps\": 0,\n    \"tags\": 0\n  },\n  \"layout\": {\n    \"width\": 1170.0,\n    \"height\": 560.0,\n    \"elements\": 122\n  },\n  \"file\": \"my_map.sbgn\"\n}\n</code></pre>"},{"location":"cli/#inspect-a-celldesigner-file","title":"Inspect a CellDesigner file","text":"<pre><code>momapy info my_map.xml\n</code></pre>"},{"location":"cli/#subcommand-list","title":"Subcommand: <code>list</code>","text":"<p>Lists plugins, named colors, built-in style presets, or stylable attributes of a layout element class.</p>"},{"location":"cli/#arguments_3","title":"Arguments","text":"Argument Description <code>readers</code> List available map readers <code>writers</code> List available map writers <code>renderers</code> List available renderers (with supported output formats) <code>colors</code> List all named colors (displayed in their own color) <code>styles</code> List built-in style presets <code>attributes &lt;class_path&gt;</code> List stylable attributes of a layout element class"},{"location":"cli/#examples_3","title":"Examples","text":""},{"location":"cli/#list-available-readers","title":"List available readers","text":"<pre><code>momapy list readers\n</code></pre> <p>Output:</p> <pre><code>celldesigner\npickle\nsbgnml\nsbgnml-0.2\nsbgnml-0.3\nsbml\n</code></pre>"},{"location":"cli/#list-available-writers","title":"List available writers","text":"<pre><code>momapy list writers\n</code></pre> <p>Output:</p> <pre><code>celldesigner\npickle\nsbgnml\nsbgnml-0.3\n</code></pre>"},{"location":"cli/#list-available-renderers","title":"List available renderers","text":"<pre><code>momapy list renderers\n</code></pre> <p>Output:</p> <pre><code>cairo (formats: pdf, svg, png, ps)\nskia (formats: pdf, svg, png, jpeg, webp)\nsvg-native (formats: svg)\nsvg-native-compat (formats: svg)\n</code></pre> <p>If a renderer's dependencies are not installed, it shows <code>(not installed)</code> instead of formats.</p>"},{"location":"cli/#list-named-colors","title":"List named colors","text":"<pre><code>momapy list colors\n</code></pre> <p>Each color name is printed in its own color. Useful for picking values for <code>fill</code>, <code>stroke</code>, and similar properties in stylesheets.</p>"},{"location":"cli/#list-built-in-style-presets","title":"List built-in style presets","text":"<pre><code>momapy list styles\n</code></pre> <p>Output:</p> <pre><code>cs_black_and_white       Black and white colorscheme\ncs_default               Default colorscheme\nfs_shadows               Drop shadows\nnewt                     Newt style\nsbgned                   SBGN-ED style\n</code></pre> <p>Preset names can be passed to <code>momapy style -p</code> or to <code>momapy render</code>/<code>momapy export</code>/<code>momapy visualize</code> via <code>--preset</code>.</p>"},{"location":"cli/#list-stylable-attributes-of-a-class","title":"List stylable attributes of a class","text":"<pre><code>momapy list attributes momapy.sbgn.pd:MacromoleculeLayout\n</code></pre> <p>Lists every attribute that can be targeted in a stylesheet for the given class. Pass <code>-P</code> / <code>--presentation-only</code> to restrict the output to visual presentation attributes (fill, stroke, font, etc.).</p> <pre><code>momapy list attributes momapy.sbgn.pd:MacromoleculeLayout --presentation-only\n</code></pre>"},{"location":"cli/#subcommand-tidy","title":"Subcommand: <code>tidy</code>","text":"<p>Applies layout tidying operations to a map and outputs the result. Use a specific operation sub-command or <code>all</code> to run the full pipeline.</p>"},{"location":"cli/#synopsis_1","title":"Synopsis","text":"<pre><code>momapy tidy &lt;operation&gt; [&lt;input_file_path&gt;] [options]\n</code></pre>"},{"location":"cli/#operations","title":"Operations","text":"Operation Description <code>all</code> Apply all tidying operations in sequence <code>fit-species</code> Resize species nodes to fit their labels (CellDesigner only) <code>fit-epns</code> Resize entity pool nodes to fit their labels (SBGN only) <code>fit-auxiliary</code> Position and resize auxiliary elements (modifications/state variables/units of information) <code>fit-complexes</code> Resize complexes to fit their subunits <code>fit-compartments</code> Resize compartments to fit their content <code>fit-submaps</code> Resize submaps to fit their terminals (SBGN only) <code>fit-layout</code> Resize the overall layout to fit all elements <code>snap-arcs</code> Snap arc endpoints to node borders <code>straighten-arcs</code> Straighten near-horizontal and near-vertical arc segments (CellDesigner only)"},{"location":"cli/#options_3","title":"Options","text":"<p>All operations accept:</p> Option Short Description <code>--output-file-path</code> <code>-o</code> Output file path (default: stdout) <code>--text</code> <code>-T</code> Write human-readable XML to stdout instead of a pickled <code>ReaderResult</code> (the default when piping); no effect with <code>-o</code> <p>The <code>all</code>, <code>fit-species</code>, <code>fit-epns</code>, <code>fit-auxiliary</code>, <code>fit-complexes</code>, <code>fit-compartments</code>, <code>fit-submaps</code>, and <code>fit-layout</code> operations also accept:</p> Option Description <code>--xsep &lt;float&gt;</code> Horizontal padding (default: depends on operation and map type) <code>--ysep &lt;float&gt;</code> Vertical padding (default: depends on operation and map type) <p>The <code>fit-species</code>, <code>fit-epns</code>, <code>fit-auxiliary</code>, <code>fit-complexes</code>, <code>fit-compartments</code>, and <code>fit-submaps</code> operations also accept:</p> Option Description <code>--no-snap-arcs</code> Do not snap arc endpoints to node borders after the operation (useful when chaining tidy operations; run <code>tidy snap-arcs</code> once at the end) <p>The <code>all</code> and <code>straighten-arcs</code> operations also accept:</p> Option Description <code>--angle-tolerance &lt;float&gt;</code> Angle tolerance in degrees for straightening (default: 5.0)"},{"location":"cli/#examples_4","title":"Examples","text":""},{"location":"cli/#apply-all-tidying-operations","title":"Apply all tidying operations","text":"<pre><code>momapy tidy all my_map.sbgn -o tidy_map.sbgn\n</code></pre>"},{"location":"cli/#fit-entity-pool-nodes-to-their-labels","title":"Fit entity pool nodes to their labels","text":"<pre><code>momapy tidy fit-epns my_map.sbgn -o tidy_map.sbgn\n</code></pre>"},{"location":"cli/#snap-arc-endpoints-to-node-borders","title":"Snap arc endpoints to node borders","text":"<pre><code>momapy tidy snap-arcs my_map.sbgn -o tidy_map.sbgn\n</code></pre>"},{"location":"cli/#read-from-stdin-and-write-to-stdout-pipeline","title":"Read from stdin and write to stdout (pipeline)","text":"<pre><code>cat my_map.sbgn | momapy tidy all &gt; tidy_map.sbgn\n</code></pre>"},{"location":"cli/#subcommand-style","title":"Subcommand: <code>style</code>","text":"<p>Applies CSS stylesheets (and/or built-in presets) to a map, baking the styles into the map data, and outputs the result. Multiple <code>-s</code> and <code>-p</code> flags can be interleaved; stylesheets are merged left-to-right (later rules override earlier ones).</p>"},{"location":"cli/#arguments_4","title":"Arguments","text":"Argument Description <code>input_file_path</code> Input file path (reads from stdin if omitted)"},{"location":"cli/#options_4","title":"Options","text":"Option Short Description <code>--output-file-path</code> <code>-o</code> Output file path (default: stdout) <code>--text</code> <code>-T</code> Write human-readable XML to stdout instead of a pickled <code>ReaderResult</code> (the default when piping); no effect with <code>-o</code> <code>--style-sheet-file-path</code> <code>-s</code> Custom CSS file path (repeatable) <code>--preset</code> <code>-p</code> Built-in preset name (repeatable); see <code>momapy list styles</code>"},{"location":"cli/#examples_5","title":"Examples","text":""},{"location":"cli/#apply-a-custom-stylesheet","title":"Apply a custom stylesheet","text":"<pre><code>momapy style my_map.sbgn -s my_style.css -o styled_map.sbgn\n</code></pre>"},{"location":"cli/#apply-a-built-in-preset","title":"Apply a built-in preset","text":"<pre><code>momapy style my_map.sbgn -p sbgned -o styled_map.sbgn\n</code></pre>"},{"location":"cli/#combine-a-preset-with-a-custom-override","title":"Combine a preset with a custom override","text":"<pre><code>momapy style my_map.sbgn -p cs_default -s overrides.css -o styled_map.sbgn\n</code></pre>"},{"location":"cli/#read-from-stdin","title":"Read from stdin","text":"<pre><code>cat my_map.sbgn | momapy style -p newt &gt; styled_map.sbgn\n</code></pre>"},{"location":"cli/#subcommand-visualize","title":"Subcommand: <code>visualize</code>","text":"<p>Opens an interactive viewer for a molecular map in the default web browser. The viewer is a self-contained HTML page with the map rendered as inline SVG, featuring:</p> <ul> <li>Pan and zoom \u2014 click and drag to pan, scroll to zoom, double-click to reset</li> <li>Hover tooltips \u2014 hover over any element to see its type, layout ID, model ID, and label</li> <li>Click to inspect \u2014 click an element to pin its info in a bottom panel with selectable/copyable text</li> <li>Search \u2014 search by label or ID; matching elements and their descendants stay visible while everything else is dimmed. Press Enter to pan to the first match, Escape to clear.</li> <li>Keyboard shortcuts \u2014 Ctrl+F / Cmd+F to focus the search bar</li> </ul>"},{"location":"cli/#arguments_5","title":"Arguments","text":"Argument Description <code>input_file_path</code> Input file path (SBGN-ML or CellDesigner format; reads from stdin if omitted)"},{"location":"cli/#options_5","title":"Options","text":"Option Short Description <code>--tidy</code> <code>-t</code> Tidy the map (reroute arcs, fit labels, etc.) <code>--to-top-left</code> <code>-l</code> Move elements to the top-left of the page <code>--style-sheet-file-path</code> <code>-s</code> Custom CSS style sheet file path (repeatable) <code>--preset</code> <code>-p</code> Built-in preset name (repeatable); see <code>momapy list styles</code>"},{"location":"cli/#examples_6","title":"Examples","text":""},{"location":"cli/#basic-visualization","title":"Basic visualization","text":"<pre><code>momapy visualize my_map.sbgn\n</code></pre>"},{"location":"cli/#visualize-with-tidy-and-style-sheet","title":"Visualize with tidy and style sheet","text":"<pre><code>momapy visualize my_map.xml -t -s custom_styles.css\n</code></pre>"},{"location":"cli/#getting-help","title":"Getting Help","text":"<p>Display help information:</p> <pre><code>momapy --help\nmomapy render --help\nmomapy export --help\nmomapy info --help\nmomapy list --help\nmomapy tidy --help\nmomapy style --help\nmomapy visualize --help\n</code></pre>"},{"location":"styling/","title":"Styling Guide","text":""},{"location":"styling/#overview","title":"Overview","text":"<p>momapy supports CSS-like stylesheets for customizing the appearance of molecular maps. Stylesheets allow you to control colors, fonts, strokes, and other visual properties of map elements without modifying the underlying map data.</p> <p>Stylesheets are applied during rendering and can be used via the command line (<code>-s</code> flag) or the Python API.</p>"},{"location":"styling/#basic-syntax","title":"Basic Syntax","text":"<p>Stylesheets follow a CSS-like syntax with rules, selectors, and declaration blocks:</p> <pre><code>MacromoleculeLayout {\n    fill: royalblue;\n    stroke: white;\n    stroke-width: 2.0;\n}\n</code></pre> <p>A rule consists of:</p> <ul> <li>Selector: The element(s) to style (e.g., <code>MacromoleculeLayout</code>)</li> <li>Declaration block: Properties and values inside <code>{ }</code></li> <li>Properties: Visual attributes (e.g., <code>fill</code>, <code>stroke-width</code>)</li> <li>Values: The setting for each property (e.g., <code>royalblue</code>, <code>2.0</code>)</li> </ul>"},{"location":"styling/#selectors","title":"Selectors","text":""},{"location":"styling/#type-selector","title":"Type Selector","text":"<p>Matches elements by their exact class name:</p> <pre><code>MacromoleculeLayout {\n    fill: lightblue;\n}\n</code></pre> <p>This matches only <code>MacromoleculeLayout</code> elements, not subclasses.</p>"},{"location":"styling/#class-selector","title":"Class Selector","text":"<p>Matches a class and all its subclasses (prefix with dot):</p> <pre><code>.Shape {\n    stroke-width: 2.0;\n}\n</code></pre> <p>This matches <code>Shape</code> and any subclass like <code>Rectangle</code>, <code>Ellipse</code>, etc.</p>"},{"location":"styling/#id-selector","title":"ID Selector","text":"<p>Matches an element by its <code>id_</code> attribute:</p> <pre><code>#my_protein {\n    fill: red;\n}\n</code></pre>"},{"location":"styling/#child-selector","title":"Child Selector","text":"<p>Matches direct children only (use <code>&gt;</code>):</p> <pre><code>StateVariableLayout &gt; TextLayout {\n    font-size: 8.0;\n}\n</code></pre>"},{"location":"styling/#descendant-selector","title":"Descendant Selector","text":"<p>Matches elements at any depth:</p> <pre><code>GroupLayout TextLayout {\n    fill: white;\n}\n</code></pre>"},{"location":"styling/#or-selector","title":"Or Selector","text":"<p>Matches any of multiple selectors (comma-separated):</p> <pre><code>MacromoleculeLayout, SimpleChemicalLayout {\n    stroke: black;\n}\n</code></pre>"},{"location":"styling/#value-types","title":"Value Types","text":""},{"location":"styling/#numbers","title":"Numbers","text":"<ul> <li>Floats: <code>2.0</code>, <code>10.5</code></li> <li>Integers: <code>42</code></li> </ul>"},{"location":"styling/#strings","title":"Strings","text":"<p>Quoted strings for font names and other text values:</p> <pre><code>font-family: \"DejaVu Sans\";\n</code></pre>"},{"location":"styling/#colors","title":"Colors","text":"<p>Colors are given as names (any color defined in <code>momapy.coloring</code>). Hex values are not supported:</p> <pre><code>fill: royalblue;\nstroke: tomato;\n</code></pre>"},{"location":"styling/#special-values","title":"Special Values","text":"<ul> <li><code>none</code>: No value (e.g., <code>fill: none;</code>)</li> <li><code>unset</code>: Reset to default/inherited value</li> </ul>"},{"location":"styling/#lists","title":"Lists","text":"<p>Comma-separated lists:</p> <pre><code>stroke-dasharray: 5, 5;\n</code></pre>"},{"location":"styling/#filters","title":"Filters","text":"<p>Drop shadow effect:</p> <pre><code>filter: drop-shadow(2.0, 2.0, 3.0, 0.5, gray);\n</code></pre> <p>Parameters: <code>drop-shadow(dx, dy, std_dev, opacity, color)</code></p>"},{"location":"styling/#common-properties","title":"Common Properties","text":""},{"location":"styling/#drawing-properties","title":"Drawing Properties","text":"Property Description Example <code>fill</code> Fill color <code>fill: royalblue;</code> <code>stroke</code> Stroke/border color <code>stroke: black;</code> <code>stroke-width</code> Stroke thickness <code>stroke-width: 2.0;</code> <code>stroke-dasharray</code> Dash pattern <code>stroke-dasharray: 5, 5;</code> <code>filter</code> Visual effects <code>filter: drop-shadow(2.0, 2.0, 3.0, 0.5, gray);</code>"},{"location":"styling/#text-properties","title":"Text Properties","text":"Property Description Example <code>font-family</code> Font name <code>font-family: \"DejaVu Sans\";</code> <code>font-size</code> Font size <code>font-size: 14.0;</code> <code>fill</code> Text color <code>fill: white;</code> <code>font-style</code> Font style <code>font-style: \"italic\";</code> <code>font-weight</code> Font weight (numeric) <code>font-weight: 700;</code>"},{"location":"styling/#shape-properties","title":"Shape Properties","text":"Property Description Example <code>width</code> Element width <code>width: 100.0;</code> <code>height</code> Element height <code>height: 60.0;</code> <code>rounded-corners</code> Corner radius <code>rounded-corners: 5.0;</code> <code>cut-corners</code> Cut corner size <code>cut-corners: 10.0;</code> <p>Note: Some node and arc classes have specific attributes that define their shape geometry. For example, <code>angle</code> and <code>offset</code> are parameters used by certain shape types (like hexagons with specific corner angles) and are not rotation angles or position offsets. Refer to the specific node or arc class implementation to see which shape-specific properties are available.</p>"},{"location":"styling/#connector-properties","title":"Connector Properties","text":"Property Description Example <code>left-connector-length</code> Left connector size <code>left-connector-length: 10.0;</code> <code>left-connector-stroke</code> Left connector color <code>left-connector-stroke: gray;</code> <code>right-connector-length</code> Right connector size <code>right-connector-length: 10.0;</code> <code>right-connector-stroke</code> Right connector color <code>right-connector-stroke: gray;</code>"},{"location":"styling/#arrowhead-properties","title":"Arrowhead Properties","text":"Property Description Example <code>arrowhead-fill</code> Arrowhead fill color <code>arrowhead-fill: black;</code> <code>arrowhead-stroke</code> Arrowhead stroke color <code>arrowhead-stroke: gray;</code> <code>arrowhead-stroke-width</code> Arrowhead stroke width <code>arrowhead-stroke-width: 1.5;</code> <p>Note: Not all arcs have arrowheads. For example, <code>ConsumptionLayout</code> is rendered as a simple line (polyline) without an arrowhead.</p>"},{"location":"styling/#path-properties","title":"Path Properties","text":"Property Description Example <code>path-stroke</code> Path color <code>path-stroke: gray;</code> <code>path-stroke-width</code> Path thickness <code>path-stroke-width: 1.5;</code> <code>end-shorten</code> Shorten path at end <code>end-shorten: 1.0;</code>"},{"location":"styling/#group-properties","title":"Group Properties","text":"<p>Prefix with <code>group-</code> to style child elements collectively:</p> <pre><code>MacromoleculeLayout {\n    group-stroke: black;\n    group-fill: lightgray;\n}\n</code></pre>"},{"location":"styling/#imports","title":"Imports","text":"<p>Stylesheets can import other stylesheets using the <code>@import</code> rule:</p> <pre><code>@import \"base.css\";\n@import \"overrides.css\";\n\nMacromoleculeLayout {\n    fill: blue;\n}\n</code></pre> <p>Imports are processed in order, with later rules overriding earlier ones.</p>"},{"location":"styling/#usage","title":"Usage","text":""},{"location":"styling/#command-line","title":"Command Line","text":"<p>Apply a stylesheet when rendering:</p> <pre><code>momapy render map.sbgn -o output.svg -s my_style.css\n</code></pre> <p>Apply a stylesheet when exporting:</p> <pre><code>momapy export map.sbgn -o output.sbgn -s my_style.css\n</code></pre> <p>Apply multiple stylesheets (applied in order):</p> <pre><code>momapy render map.sbgn -o output.svg -s base.css -s overrides.css\n</code></pre>"},{"location":"styling/#python-api","title":"Python API","text":"<p>Load from file:</p> <pre><code>from momapy.styling import StyleSheet\n\nstyle_sheet = StyleSheet.from_file(\"my_style.css\")\n</code></pre> <p>Load from string:</p> <pre><code>style_sheet = StyleSheet.from_string(\"\"\"\nMacromoleculeLayout {\n    fill: blue;\n}\n\"\"\")\n</code></pre> <p>Note: Both <code>from_file()</code> and <code>from_string()</code> raise a <code>ParseException</code> if the CSS cannot be parsed. Selectors that do not match any element are silently ignored, but a property name that is not a field of a matched element raises <code>AttributeError</code> when the stylesheet is applied. Pass <code>strict=False</code> to <code>apply_style_sheet()</code> to skip such unknown properties instead.</p> <p>Bake the styles into the map. <code>apply_style_sheet</code> returns a new, styled copy (the map is frozen), so capture the return value:</p> <pre><code>from momapy.styling import apply_style_sheet\n\nstyled_map = apply_style_sheet(map_, style_sheet)\n</code></pre> <p>Or apply during rendering, leaving the map unchanged:</p> <pre><code>from momapy.rendering import render_map\n\nrender_map(map_, \"output.svg\", style_sheet=style_sheet)\n</code></pre>"},{"location":"styling/#complete-example","title":"Complete Example","text":"<p>Here's a custom stylesheet for some layout elements of SBGN PD (note that the grammar has no comment syntax, so the rules speak for themselves):</p> <pre><code>@import \"my_base_stylesheet.css\";\n\nStateVariableLayout {\n    fill: royalblue;\n    stroke: white;\n    stroke-width: 1.5;\n}\n\nStateVariableLayout &gt; TextLayout {\n    font-size: 7.0;\n    fill: white;\n}\n\nMacromoleculeLayout {\n    fill: royalblue;\n    stroke: white;\n    stroke-width: 2.0;\n}\n\nMacromoleculeLayout &gt; TextLayout {\n    font-size: 14.0;\n    fill: white;\n}\n\nSimpleChemicalLayout {\n    fill: gold;\n    stroke: white;\n    stroke-width: 2.0;\n}\n\nSimpleChemicalLayout &gt; TextLayout {\n    font-size: 10.0;\n    fill: black;\n}\n\nGenericProcessLayout {\n    fill: gray;\n    stroke: white;\n    right-connector-stroke: gray;\n    right-connector-length: 10.0;\n    left-connector-stroke: gray;\n    left-connector-length: 10.0;\n}\n\nConsumptionLayout {\n    path-stroke: gray;\n    path-stroke-width: 1.5;\n    end-shorten: 1.0;\n}\n\nProductionLayout {\n    arrowhead-fill: gray;\n    arrowhead-stroke: gray;\n    path-stroke: gray;\n    path-stroke-width: 1.5;\n    end-shorten: 1.0;\n}\n</code></pre>"},{"location":"api_reference/","title":"API reference","text":"<p>The momapy public API is organized into the following subpackages.</p>"},{"location":"api_reference/#core-library","title":"Core library","text":"<ul> <li>Core \u2014 base classes for <code>Map</code>, <code>Model</code>, <code>Layout</code>, <code>LayoutElement</code>, <code>Node</code>, <code>Arc</code></li> <li>Builder \u2014 builder pattern for modifying frozen dataclasses</li> <li>Drawing \u2014 drawing primitives (<code>Path</code>, <code>Rectangle</code>, <code>Ellipse</code>, <code>Text</code>, <code>Group</code>)</li> <li>Coloring \u2014 colors and named color constants</li> <li>Geometry \u2014 <code>Point</code>, <code>Bbox</code>, path operations</li> <li>Positioning \u2014 layout positioning helpers</li> <li>Styling \u2014 CSS-like stylesheet parsing and application</li> <li>I/O \u2014 reader/writer registries and entry points</li> </ul>"},{"location":"api_reference/#rendering","title":"Rendering","text":"<ul> <li>Core \u2014 renderer registry and render functions</li> <li>Skia \u2014 Skia backend</li> <li>Cairo \u2014 Cairo backend</li> <li>SVG-native \u2014 native SVG backend</li> </ul>"},{"location":"api_reference/#sbgn","title":"SBGN","text":"<ul> <li>SBGN \u2014 base SBGN classes</li> <li>PD \u2014 Process Description glyphs</li> <li>AF \u2014 Activity Flow glyphs</li> <li>SBGN-ML I/O \u2014 SBGN-ML reader/writer</li> </ul>"},{"location":"api_reference/#celldesigner","title":"CellDesigner","text":"<ul> <li>CellDesigner \u2014 CellDesigner model and layout</li> <li>CellDesigner I/O \u2014 CellDesigner reader/writer</li> </ul>"},{"location":"api_reference/#sbml","title":"SBML","text":"<ul> <li>SBML \u2014 SBML model classes</li> <li>SBML I/O \u2014 SBML reader</li> </ul>"},{"location":"api_reference/builder/","title":"Builder","text":""},{"location":"api_reference/builder/#momapy.builder","title":"momapy.builder","text":"<p>Classes and functions for building objects from dataclasses.</p> <p>This module provides functionality to automatically generate builder classes from dataclasses, allowing for flexible object construction and transformation between objects and their builder representations.</p> <p>Examples:</p> <pre><code>import dataclasses\n@dataclasses.dataclass\nclass Person:\n    name: str\n    age: int\n\n# Create a builder class automatically\nBuilder = get_or_make_builder_cls(Person)\nbuilder = Builder(name=\"John\", age=30)\nperson = builder.build()\nprint(person)\n</code></pre> <p>Classes:</p> Name Description <code>Builder</code> <p>Abstract base class for builder objects.</p> <p>Functions:</p> Name Description <code>builder_from_object</code> <p>Convert an object (or collection of objects) to builder(s).</p> <code>get_builder_cls</code> <p>Get the registered builder class for a given class.</p> <code>get_or_make_builder_cls</code> <p>Get an existing builder class or create a new one.</p> <code>has_builder_cls</code> <p>Check if a builder class is registered for a given class.</p> <code>isinstance_or_builder</code> <p>Check if object is instance of class or its builder class.</p> <code>issubclass_or_builder</code> <p>Check if class is subclass of class or its builder class.</p> <code>new_builder_object</code> <p>Create a new builder instance for a given class.</p> <code>object_from_builder</code> <p>Convert a builder (or collection of builders) to actual object(s).</p> <code>register_builder_cls</code> <p>Register a builder class.</p> <code>super_or_builder</code> <p>Get super() proxy for a class or its builder class.</p>"},{"location":"api_reference/builder/#momapy.builder.Builder","title":"Builder","text":"<p>               Bases: <code>ABC</code></p> <p>Abstract base class for builder objects.</p> <p>Builder classes are automatically generated from dataclasses to provide a mutable representation for constructing immutable objects.</p> <p>Attributes:</p> Name Type Description <code>_cls_to_build</code> <code>type</code> <p>The class that this builder constructs.</p> <p>Methods:</p> Name Description <code>build</code> <p>Build and return an object from the builder.</p> <code>from_object</code> <p>Create a builder from an existing object.</p>"},{"location":"api_reference/builder/#momapy.builder.Builder.build","title":"build  <code>abstractmethod</code>","text":"<pre><code>build(builder_to_object: dict[int, Any] | None = None) -&gt; Any\n</code></pre> <p>Build and return an object from the builder.</p> <p>Parameters:</p> Name Type Description Default <code>builder_to_object</code> <code>dict[int, Any] | None</code> <p>Optional cache mapping builder ids to built objects for handling circular references.</p> <code>None</code> <p>Returns:</p> Type Description <code>Any</code> <p>The constructed object of type <code>_cls_to_build</code>.</p> Source code in <code>src/momapy/builder.py</code> <pre><code>@abc.abstractmethod\ndef build(\n    self,\n    builder_to_object: dict[int, typing.Any] | None = None,\n) -&gt; typing.Any:\n    \"\"\"Build and return an object from the builder.\n\n    Args:\n        builder_to_object: Optional cache mapping builder ids to built\n            objects for handling circular references.\n\n    Returns:\n        The constructed object of type `_cls_to_build`.\n    \"\"\"\n    pass\n</code></pre>"},{"location":"api_reference/builder/#momapy.builder.Builder.from_object","title":"from_object  <code>abstractmethod</code> <code>classmethod</code>","text":"<pre><code>from_object(obj: Any, object_to_builder: dict[int, Builder] | None = None) -&gt; Self\n</code></pre> <p>Create a builder from an existing object.</p> <p>Parameters:</p> Name Type Description Default <code>obj</code> <code>Any</code> <p>The object to convert to a builder.</p> required <code>object_to_builder</code> <code>dict[int, Builder] | None</code> <p>Optional cache mapping object ids to builders for handling circular references.</p> <code>None</code> <p>Returns:</p> Type Description <code>Self</code> <p>A builder instance representing the input object.</p> Source code in <code>src/momapy/builder.py</code> <pre><code>@classmethod\n@abc.abstractmethod\ndef from_object(\n    cls,\n    obj: typing.Any,\n    object_to_builder: dict[int, \"Builder\"] | None = None,\n) -&gt; typing_extensions.Self:\n    \"\"\"Create a builder from an existing object.\n\n    Args:\n        obj: The object to convert to a builder.\n        object_to_builder: Optional cache mapping object ids to builders\n            for handling circular references.\n\n    Returns:\n        A builder instance representing the input object.\n    \"\"\"\n    pass\n</code></pre>"},{"location":"api_reference/builder/#momapy.builder.builder_from_object","title":"builder_from_object","text":"<pre><code>builder_from_object(obj: Any, object_to_builder: dict[int, Builder] | None = None) -&gt; Builder\n</code></pre> <p>Convert an object (or collection of objects) to builder(s).</p> <p>Recursively converts class instances to their corresponding builder objects. Handles collections (list, tuple, set, dict) and circular references. Immutable collection types are converted to their mutable counterparts: tuple \u2192 list, frozenset \u2192 set, frozendict \u2192 dict. Dictionary keys are left as-is (only values are converted to builders).</p> <p>Parameters:</p> Name Type Description Default <code>obj</code> <code>Any</code> <p>An object instance, collection of objects, or any value.</p> required <code>object_to_builder</code> <code>dict[int, Builder] | None</code> <p>Optional cache mapping object ids to already-created builders for handling circular references.</p> <code>None</code> <p>Returns:</p> Type Description <code>Builder</code> <p>The builder object, collection of builders, or the original value</p> <code>Builder</code> <p>if no builder class exists.</p> <p>Examples:</p> <pre><code>import dataclasses\n@dataclasses.dataclass\nclass Point:\n    x: float\n    y: float\n\npoint = Point(x=1.0, y=2.0)\nbuilder = builder_from_object(point)\nprint(type(builder).__name__)\n</code></pre> Source code in <code>src/momapy/builder.py</code> <pre><code>def builder_from_object(\n    obj: typing.Any,\n    object_to_builder: dict[int, \"Builder\"] | None = None,\n) -&gt; Builder:\n    \"\"\"Convert an object (or collection of objects) to builder(s).\n\n    Recursively converts class instances to their corresponding builder objects.\n    Handles collections (list, tuple, set, dict) and circular references.\n    Immutable collection types are converted to their mutable counterparts:\n    tuple \u2192 list, frozenset \u2192 set, frozendict \u2192 dict. Dictionary keys are left\n    as-is (only values are converted to builders).\n\n    Args:\n        obj: An object instance, collection of objects, or any value.\n        object_to_builder: Optional cache mapping object ids to already-created\n            builders for handling circular references.\n\n    Returns:\n        The builder object, collection of builders, or the original value\n        if no builder class exists.\n\n    Examples:\n        ```python\n        import dataclasses\n        @dataclasses.dataclass\n        class Point:\n            x: float\n            y: float\n\n        point = Point(x=1.0, y=2.0)\n        builder = builder_from_object(point)\n        print(type(builder).__name__)\n        ```\n    \"\"\"\n    if object_to_builder is not None:\n        builder = object_to_builder.get(id(obj))\n        if builder is not None:\n            return builder\n    else:\n        object_to_builder = {}\n    cls = get_or_make_builder_cls(type(obj))\n    if issubclass(cls, Builder):\n        return cls.from_object(\n            obj=obj,\n            object_to_builder=object_to_builder,\n        )\n    if isinstance(obj, (list, tuple, set, frozenset)):\n        builder_type = _immutable_collection_to_builder.get(type(obj), type(obj))\n        return builder_type(\n            [\n                builder_from_object(\n                    obj=e,\n                    object_to_builder=object_to_builder,\n                )\n                for e in obj\n            ]\n        )\n    elif isinstance(obj, (dict, frozendict.frozendict)):\n        builder_type = _immutable_collection_to_builder.get(type(obj), type(obj))\n        return builder_type(\n            [\n                (\n                    k,\n                    builder_from_object(\n                        obj=v,\n                        object_to_builder=object_to_builder,\n                    ),\n                )\n                for k, v in obj.items()\n            ]\n        )\n    return obj\n</code></pre>"},{"location":"api_reference/builder/#momapy.builder.get_builder_cls","title":"get_builder_cls","text":"<pre><code>get_builder_cls(cls: Type) -&gt; Type | None\n</code></pre> <p>Get the registered builder class for a given class.</p> <p>Parameters:</p> Name Type Description Default <code>cls</code> <code>Type</code> <p>The class to get the builder for.</p> required <p>Returns:</p> Type Description <code>Type | None</code> <p>The builder class if registered, None otherwise.</p> Source code in <code>src/momapy/builder.py</code> <pre><code>def get_builder_cls(cls: typing.Type) -&gt; typing.Type | None:\n    \"\"\"Get the registered builder class for a given class.\n\n    Args:\n        cls: The class to get the builder for.\n\n    Returns:\n        The builder class if registered, None otherwise.\n    \"\"\"\n    return _builders.get(cls)\n</code></pre>"},{"location":"api_reference/builder/#momapy.builder.get_or_make_builder_cls","title":"get_or_make_builder_cls","text":"<pre><code>get_or_make_builder_cls(cls: Type, builder_fields: Collection[tuple[str, Type, Field]] | None = None, builder_bases: Collection[Type] | None = None, builder_namespace: dict[str, Any] | None = None) -&gt; Type\n</code></pre> <p>Get an existing builder class or create a new one.</p> <p>Returns the registered builder class for the given class if it exists, otherwise creates and registers a new builder class.</p> <p>Parameters:</p> Name Type Description Default <code>cls</code> <code>Type</code> <p>The class to get or create a builder for.</p> required <code>builder_fields</code> <code>Collection[tuple[str, Type, Field]] | None</code> <p>Optional collection of additional fields to include in the builder class.</p> <code>None</code> <code>builder_bases</code> <code>Collection[Type] | None</code> <p>Optional collection of base classes for the builder.</p> <code>None</code> <code>builder_namespace</code> <code>dict[str, Any] | None</code> <p>Optional namespace dictionary for the builder class.</p> <code>None</code> <p>Returns:</p> Type Description <code>Type</code> <p>The builder class for the given class, or the original class if</p> <code>Type</code> <p>it's not a dataclass.</p> <p>Examples:</p> <pre><code>import dataclasses\n@dataclasses.dataclass\nclass Point:\n    x: float\n    y: float\n\nPointBuilder = get_or_make_builder_cls(Point)\nprint(PointBuilder.__name__)\n</code></pre> Source code in <code>src/momapy/builder.py</code> <pre><code>def get_or_make_builder_cls(\n    cls: typing.Type,\n    builder_fields: (\n        typing.Collection[tuple[str, typing.Type, dataclasses.Field]] | None\n    ) = None,\n    builder_bases: typing.Collection[typing.Type] | None = None,\n    builder_namespace: dict[str, typing.Any] | None = None,\n) -&gt; typing.Type:\n    \"\"\"Get an existing builder class or create a new one.\n\n    Returns the registered builder class for the given class if it exists,\n    otherwise creates and registers a new builder class.\n\n    Args:\n        cls: The class to get or create a builder for.\n        builder_fields: Optional collection of additional fields to include\n            in the builder class.\n        builder_bases: Optional collection of base classes for the builder.\n        builder_namespace: Optional namespace dictionary for the builder class.\n\n    Returns:\n        The builder class for the given class, or the original class if\n        it's not a dataclass.\n\n    Examples:\n        ```python\n        import dataclasses\n        @dataclasses.dataclass\n        class Point:\n            x: float\n            y: float\n\n        PointBuilder = get_or_make_builder_cls(Point)\n        print(PointBuilder.__name__)\n        ```\n    \"\"\"\n    builder_cls = get_builder_cls(cls)\n    if builder_cls is None:\n        if dataclasses.is_dataclass(cls):\n            builder_cls = _make_builder_cls(\n                cls, builder_fields, builder_bases, builder_namespace\n            )\n            register_builder_cls(builder_cls)\n        else:\n            builder_cls = cls\n    return builder_cls\n</code></pre>"},{"location":"api_reference/builder/#momapy.builder.has_builder_cls","title":"has_builder_cls","text":"<pre><code>has_builder_cls(cls: Type) -&gt; bool\n</code></pre> <p>Check if a builder class is registered for a given class.</p> <p>Parameters:</p> Name Type Description Default <code>cls</code> <code>Type</code> <p>The class to check.</p> required <p>Returns:</p> Type Description <code>bool</code> <p>True if a builder class is registered, False otherwise.</p> Source code in <code>src/momapy/builder.py</code> <pre><code>def has_builder_cls(cls: typing.Type) -&gt; bool:\n    \"\"\"Check if a builder class is registered for a given class.\n\n    Args:\n        cls: The class to check.\n\n    Returns:\n        True if a builder class is registered, False otherwise.\n    \"\"\"\n    return cls in _builders\n</code></pre>"},{"location":"api_reference/builder/#momapy.builder.isinstance_or_builder","title":"isinstance_or_builder","text":"<pre><code>isinstance_or_builder(obj: Any, type_: type | tuple[type, ...]) -&gt; bool\n</code></pre> <p>Check if object is instance of class or its builder class.</p> <p>Extends isinstance() to also check against registered builder classes.</p> <p>Parameters:</p> Name Type Description Default <code>obj</code> <code>Any</code> <p>The object to check.</p> required <code>type_</code> <code>type | tuple[type, ...]</code> <p>The type or tuple of types to check against.</p> required <p>Returns:</p> Type Description <code>bool</code> <p>True if obj is an instance of type_ or its builder class(es).</p> <p>Examples:</p> <pre><code>import dataclasses\n@dataclasses.dataclass\nclass Point:\n    x: float\n    y: float\n\nPointBuilder = get_or_make_builder_cls(Point)\nbuilder = PointBuilder(x=1.0, y=2.0)\nisinstance_or_builder(builder, Point)\n</code></pre> Source code in <code>src/momapy/builder.py</code> <pre><code>def isinstance_or_builder(obj: typing.Any, type_: type | tuple[type, ...]) -&gt; bool:\n    \"\"\"Check if object is instance of class or its builder class.\n\n    Extends isinstance() to also check against registered builder classes.\n\n    Args:\n        obj: The object to check.\n        type_: The type or tuple of types to check against.\n\n    Returns:\n        True if obj is an instance of type_ or its builder class(es).\n\n    Examples:\n        ```python\n        import dataclasses\n        @dataclasses.dataclass\n        class Point:\n            x: float\n            y: float\n\n        PointBuilder = get_or_make_builder_cls(Point)\n        builder = PointBuilder(x=1.0, y=2.0)\n        isinstance_or_builder(builder, Point)\n        ```\n    \"\"\"\n    if isinstance(type_, type):\n        type_ = (type_,)\n    type_ += tuple([get_or_make_builder_cls(t) for t in type_])\n    return isinstance(obj, type_)\n</code></pre>"},{"location":"api_reference/builder/#momapy.builder.issubclass_or_builder","title":"issubclass_or_builder","text":"<pre><code>issubclass_or_builder(cls: type, type_: type | tuple[type, ...]) -&gt; bool\n</code></pre> <p>Check if class is subclass of class or its builder class.</p> <p>Extends issubclass() to also check against registered builder classes.</p> <p>Parameters:</p> Name Type Description Default <code>cls</code> <code>type</code> <p>The class to check.</p> required <code>type_</code> <code>type | tuple[type, ...]</code> <p>The type or tuple of types to check against.</p> required <p>Returns:</p> Type Description <code>bool</code> <p>True if cls is a subclass of type_ or its builder class(es).</p> Source code in <code>src/momapy/builder.py</code> <pre><code>def issubclass_or_builder(cls: type, type_: type | tuple[type, ...]) -&gt; bool:\n    \"\"\"Check if class is subclass of class or its builder class.\n\n    Extends issubclass() to also check against registered builder classes.\n\n    Args:\n        cls: The class to check.\n        type_: The type or tuple of types to check against.\n\n    Returns:\n        True if cls is a subclass of type_ or its builder class(es).\n    \"\"\"\n    if isinstance(type_, type):\n        type_ = (type_,)\n    type_ += tuple([get_or_make_builder_cls(t) for t in type_])\n    return issubclass(cls, type_)\n</code></pre>"},{"location":"api_reference/builder/#momapy.builder.new_builder_object","title":"new_builder_object","text":"<pre><code>new_builder_object(cls: Type, *args: Any, **kwargs: Any) -&gt; Builder\n</code></pre> <p>Create a new builder instance for a given class.</p> <p>Parameters:</p> Name Type Description Default <code>cls</code> <code>Type</code> <p>The class to build (or a builder class).</p> required <code>*args</code> <code>Any</code> <p>Positional arguments for the builder.</p> <code>()</code> <code>**kwargs</code> <code>Any</code> <p>Keyword arguments for the builder.</p> <code>{}</code> <p>Returns:</p> Type Description <code>Builder</code> <p>A new builder instance.</p> <p>Examples:</p> <pre><code>import dataclasses\n@dataclasses.dataclass\nclass Person:\n    name: str\n    age: int = 0\n\nbuilder = new_builder_object(Person, name=\"Alice\")\nprint(builder.name)\n</code></pre> Source code in <code>src/momapy/builder.py</code> <pre><code>def new_builder_object(\n    cls: typing.Type, *args: typing.Any, **kwargs: typing.Any\n) -&gt; Builder:\n    \"\"\"Create a new builder instance for a given class.\n\n    Args:\n        cls: The class to build (or a builder class).\n        *args: Positional arguments for the builder.\n        **kwargs: Keyword arguments for the builder.\n\n    Returns:\n        A new builder instance.\n\n    Examples:\n        ```python\n        import dataclasses\n        @dataclasses.dataclass\n        class Person:\n            name: str\n            age: int = 0\n\n        builder = new_builder_object(Person, name=\"Alice\")\n        print(builder.name)\n        ```\n    \"\"\"\n    if not issubclass(cls, Builder):\n        cls = get_or_make_builder_cls(cls)\n    return cls(*args, **kwargs)\n</code></pre>"},{"location":"api_reference/builder/#momapy.builder.object_from_builder","title":"object_from_builder","text":"<pre><code>object_from_builder(builder: Any, builder_to_object: dict[int, Any] | None = None) -&gt; Any\n</code></pre> <p>Convert a builder (or collection of builders) to actual object(s).</p> <p>Recursively converts builder objects to their corresponding class instances. Handles collections (list, tuple, set, dict) and circular references. Mutable collection types are converted to their immutable counterparts: list \u2192 tuple, set \u2192 frozenset, dict \u2192 frozendict.</p> <p>Parameters:</p> Name Type Description Default <code>builder</code> <code>Any</code> <p>A builder instance, collection of builders, or regular object.</p> required <code>builder_to_object</code> <code>dict[int, Any] | None</code> <p>Optional cache mapping builder ids to already-built objects for handling circular references.</p> <code>None</code> <p>Returns:</p> Type Description <code>Any</code> <p>The built object, collection of built objects, or the original value</p> <code>Any</code> <p>if not a builder.</p> <p>Examples:</p> <pre><code>import dataclasses\n@dataclasses.dataclass\nclass Point:\n    x: float\n    y: float\n\nPointBuilder = get_or_make_builder_cls(Point)\nbuilder = PointBuilder(x=1.0, y=2.0)\npoint = object_from_builder(builder)\nprint(point)\n</code></pre> Source code in <code>src/momapy/builder.py</code> <pre><code>def object_from_builder(\n    builder: typing.Any,\n    builder_to_object: dict[int, typing.Any] | None = None,\n) -&gt; typing.Any:\n    \"\"\"Convert a builder (or collection of builders) to actual object(s).\n\n    Recursively converts builder objects to their corresponding class instances.\n    Handles collections (list, tuple, set, dict) and circular references.\n    Mutable collection types are converted to their immutable counterparts:\n    list \u2192 tuple, set \u2192 frozenset, dict \u2192 frozendict.\n\n    Args:\n        builder: A builder instance, collection of builders, or regular object.\n        builder_to_object: Optional cache mapping builder ids to already-built\n            objects for handling circular references.\n\n    Returns:\n        The built object, collection of built objects, or the original value\n        if not a builder.\n\n    Examples:\n        ```python\n        import dataclasses\n        @dataclasses.dataclass\n        class Point:\n            x: float\n            y: float\n\n        PointBuilder = get_or_make_builder_cls(Point)\n        builder = PointBuilder(x=1.0, y=2.0)\n        point = object_from_builder(builder)\n        print(point)\n        ```\n    \"\"\"\n    if builder_to_object is not None:\n        if id(builder) in builder_to_object:\n            return builder_to_object[id(builder)]\n    else:\n        builder_to_object = {}\n    if isinstance(builder, Builder):\n        obj = builder.build(\n            builder_to_object=builder_to_object,\n        )\n        builder_to_object[id(builder)] = obj\n        return obj\n    if isinstance(builder, (list, tuple, set, frozenset)):\n        target = _builder_collection_to_immutable.get(type(builder), type(builder))\n        return target(\n            [\n                object_from_builder(\n                    builder=e,\n                    builder_to_object=builder_to_object,\n                )\n                for e in builder\n            ]\n        )\n    elif isinstance(builder, (dict, frozendict.frozendict)):\n        target = _builder_collection_to_immutable.get(type(builder), type(builder))\n        return target(\n            [\n                (\n                    object_from_builder(\n                        builder=k,\n                        builder_to_object=builder_to_object,\n                    ),\n                    object_from_builder(\n                        builder=v,\n                        builder_to_object=builder_to_object,\n                    ),\n                )\n                for k, v in builder.items()\n            ]\n        )\n    return builder\n</code></pre>"},{"location":"api_reference/builder/#momapy.builder.register_builder_cls","title":"register_builder_cls","text":"<pre><code>register_builder_cls(builder_cls: Type) -&gt; None\n</code></pre> <p>Register a builder class.</p> <p>Registers a builder class so it can be looked up by its target class.</p> <p>Parameters:</p> Name Type Description Default <code>builder_cls</code> <code>Type</code> <p>The builder class to register. Must have a <code>_cls_to_build</code> attribute indicating the target class.</p> required Source code in <code>src/momapy/builder.py</code> <pre><code>def register_builder_cls(builder_cls: typing.Type) -&gt; None:\n    \"\"\"Register a builder class.\n\n    Registers a builder class so it can be looked up by its target class.\n\n    Args:\n        builder_cls: The builder class to register. Must have a `_cls_to_build`\n            attribute indicating the target class.\n    \"\"\"\n    _builders[builder_cls._cls_to_build] = builder_cls\n</code></pre>"},{"location":"api_reference/builder/#momapy.builder.super_or_builder","title":"super_or_builder","text":"<pre><code>super_or_builder(type_: Type, obj: Any) -&gt; super\n</code></pre> <p>Get super() proxy for a class or its builder class.</p> <p>Attempts to get the super() proxy for the given type. If that fails, tries with the builder class of the given type.</p> <p>Parameters:</p> Name Type Description Default <code>type_</code> <code>Type</code> <p>The class to get super() for.</p> required <code>obj</code> <code>Any</code> <p>The object to get the super proxy of.</p> required <p>Returns:</p> Type Description <code>super</code> <p>A super() proxy object.</p> Source code in <code>src/momapy/builder.py</code> <pre><code>def super_or_builder(type_: typing.Type, obj: typing.Any) -&gt; super:\n    \"\"\"Get super() proxy for a class or its builder class.\n\n    Attempts to get the super() proxy for the given type. If that fails,\n    tries with the builder class of the given type.\n\n    Args:\n        type_: The class to get super() for.\n        obj: The object to get the super proxy of.\n\n    Returns:\n        A super() proxy object.\n    \"\"\"\n    try:\n        return super(type_, obj)\n    except TypeError:\n        builder = get_or_make_builder_cls(type_)\n        return super(builder, obj)\n</code></pre>"},{"location":"api_reference/coloring/","title":"Coloring","text":""},{"location":"api_reference/coloring/#momapy.coloring","title":"momapy.coloring","text":"<p>Color definitions and utilities for momapy.</p> <p>This module provides the Color class for representing colors with RGBA components, along with a comprehensive set of predefined named colors. Colors can be created from various formats (RGB, RGBA, HEX, HEXA) and converted between formats.</p> <p>Examples:</p> <pre><code>from momapy.coloring import Color, red, blue\n\n# Using predefined colors\ncolor = red\n\n# Creating from hex\ncolor = Color.from_hex(\"#FF5733\")\n\n# Creating from RGB\ncolor = Color.from_rgb(255, 87, 51)\n\n# With alpha transparency\ncolor = Color.from_rgba(255, 87, 51, 0.5)\n\n# Convert to different formats\ncolor.to_hex()\ncolor.to_rgba()\n</code></pre> <p>Classes:</p> Name Description <code>Color</code> <p>Represents an RGBA color.</p> <p>Functions:</p> Name Description <code>get_color</code> <p>Return the named color with the given name.</p> <code>has_color</code> <p>Check if a named color exists.</p> <code>list_colors</code> <p>Return the names of all available named colors.</p> <code>print_colors</code> <p>Print all available named colors with their names.</p> <p>Attributes:</p> Name Type Description <code>aliceblue</code> <p>The color aliceblue | aliceblue</p> <code>antiquewhite</code> <p>The color antiquewhite | antiquewhite</p> <code>aqua</code> <p>The color aqua | aqua</p> <code>aquamarine</code> <p>The color aquamarine | aquamarine</p> <code>azure</code> <p>The color azure | azure</p> <code>beige</code> <p>The color beige | beige</p> <code>bisque</code> <p>The color bisque | bisque</p> <code>black</code> <p>The color black | black</p> <code>blanchedalmond</code> <p>The color blanchedalmond | blanchedalmond</p> <code>blue</code> <p>The color blue | blue</p> <code>blueviolet</code> <p>The color blueviolet | blueviolet</p> <code>brown</code> <p>The color brown | brown</p> <code>burlywood</code> <p>The color burlywood | burlywood</p> <code>cadetblue</code> <p>The color cadetblue | cadetblue</p> <code>chartreuse</code> <p>The color chartreuse | chartreuse</p> <code>chocolate</code> <p>The color chocolate | chocolate</p> <code>coral</code> <p>The color coral | coral</p> <code>cornflowerblue</code> <p>The color cornflowerblue | cornflowerblue</p> <code>cornsilk</code> <p>The color cornsilk | cornsilk</p> <code>crimson</code> <p>The color crimson | crimson</p> <code>cyan</code> <p>The color cyan | cyan</p> <code>darkblue</code> <p>The color darkblue | darkblue</p> <code>darkcyan</code> <p>The color darkcyan | darkcyan</p> <code>darkgoldenrod</code> <p>The color darkgoldenrod | darkgoldenrod</p> <code>darkgray</code> <p>The color darkgray | darkgray</p> <code>darkgreen</code> <p>The color darkgreen | darkgreen</p> <code>darkgrey</code> <p>The color darkgrey | darkgrey</p> <code>darkkhaki</code> <p>The color darkkhaki | darkkhaki</p> <code>darkmagenta</code> <p>The color darkmagenta | darkmagenta</p> <code>darkolivegreen</code> <p>The color darkolivegreen | darkolivegreen</p> <code>darkorange</code> <p>The color darkorange | darkorange</p> <code>darkorchid</code> <p>The color darkorchid | darkorchid</p> <code>darkred</code> <p>The color darkred | darkred</p> <code>darksalmon</code> <p>The color darksalmon | darksalmon</p> <code>darkseagreen</code> <p>The color darkseagreen | darkseagreen</p> <code>darkslateblue</code> <p>The color darkslateblue | darkslateblue</p> <code>darkslategray</code> <p>The color darkslategray | darkslategray</p> <code>darkturquoise</code> <p>The color darkturquoise | darkturquoise</p> <code>darkviolet</code> <p>The color darkviolet | darkviolet</p> <code>deeppink</code> <p>The color deeppink | deeppink</p> <code>deepskyblue</code> <p>The color deepskyblue | deepskyblue</p> <code>dimgray</code> <p>The color dimgray | dimgray</p> <code>dimgrey</code> <p>The color dimgrey | dimgrey</p> <code>dodgerblue</code> <p>The color dodgerblue | dodgerblue</p> <code>firebrick</code> <p>The color firebrick | firebrick</p> <code>floralwhite</code> <p>The color floralwhite | floralwhite</p> <code>forestgreen</code> <p>The color forestgreen | forestgreen</p> <code>fuchsia</code> <p>The color fuchsia | fuchsia</p> <code>gainsboro</code> <p>The color gainsboro | gainsboro</p> <code>ghostwhite</code> <p>The color ghostwhite | ghostwhite</p> <code>gold</code> <p>The color gold | gold</p> <code>goldenrod</code> <p>The color goldenrod | goldenrod</p> <code>gray</code> <p>The color gray | gray</p> <code>green</code> <p>The color green | green</p> <code>greenyellow</code> <p>The color greenyellow | greenyellow</p> <code>grey</code> <p>The color grey | grey</p> <code>honeydew</code> <p>The color honeydew | honeydew</p> <code>hotpink</code> <p>The color hotpink | hotpink</p> <code>indianred</code> <p>The color indianred | indianred</p> <code>indigo</code> <p>The color indigo | indigo</p> <code>ivory</code> <p>The color ivory | ivory</p> <code>khaki</code> <p>The color khaki | khaki</p> <code>lavender</code> <p>The color lavender | lavender</p> <code>lavenderblush</code> <p>The color lavenderblush | lavenderblush</p> <code>lawngreen</code> <p>The color lawngreen | lawngreen</p> <code>lemonchiffon</code> <p>The color lemonchiffon | lemonchiffon</p> <code>lightblue</code> <p>The color lightblue | lightblue</p> <code>lightcoral</code> <p>The color lightcoral | lightcoral</p> <code>lightcyan</code> <p>The color lightcyan | lightcyan</p> <code>lightgoldenrodyellow</code> <p>The color lightgoldenrodyellow | lightgoldenrodyellow</p> <code>lightgray</code> <p>The color lightgray | lightgray</p> <code>lightgreen</code> <p>The color lightgreen | lightgreen</p> <code>lightgrey</code> <p>The color lightgrey | lightgrey</p> <code>lightpink</code> <p>The color lightpink | lightpink</p> <code>lightsalmon</code> <p>The color lightsalmon | lightsalmon</p> <code>lightseagreen</code> <p>The color lightseagreen | lightseagreen</p> <code>lightskyblue</code> <p>The color lightskyblue | lightskyblue</p> <code>lightslategray</code> <p>The color lightslategray | lightslategray</p> <code>lightsteelblue</code> <p>The color lightsteelblue | lightsteelblue</p> <code>lightyellow</code> <p>The color lightyellow | lightyellow</p> <code>lime</code> <p>The color lime | lime</p> <code>limegreen</code> <p>The color limegreen | limegreen</p> <code>linen</code> <p>The color linen | linen</p> <code>magenta</code> <p>The color magenta | magenta</p> <code>maroon</code> <p>The color maroon | maroon</p> <code>mediumaquamarine</code> <p>The color mediumaquamarine | mediumaquamarine</p> <code>mediumblue</code> <p>The color mediumblue | mediumblue</p> <code>mediumorchid</code> <p>The color mediumorchid | mediumorchid</p> <code>mediumpurple</code> <p>The color mediumpurple | mediumpurple</p> <code>mediumseagreen</code> <p>The color mediumseagreen | mediumseagreen</p> <code>mediumslateblue</code> <p>The color mediumslateblue | mediumslateblue</p> <code>mediumspringgreen</code> <p>The color mediumspringgreen | mediumspringgreen</p> <code>mediumturquoise</code> <p>The color mediumturquoise | mediumturquoise</p> <code>mediumvioletred</code> <p>The color mediumvioletred | mediumvioletred</p> <code>midnightblue</code> <p>The color midnightblue | midnightblue</p> <code>mintcream</code> <p>The color mintcream | mintcream</p> <code>mistyrose</code> <p>The color mistyrose | mistyrose</p> <code>moccasin</code> <p>The color moccasin | moccasin</p> <code>navajowhite</code> <p>The color navajowhite | navajowhite</p> <code>navy</code> <p>The color navy | navy</p> <code>oldlace</code> <p>The color oldlace | oldlace</p> <code>olive</code> <p>The color olive | olive</p> <code>olivedrab</code> <p>The color olivedrab | olivedrab</p> <code>orange</code> <p>The color orange | orange</p> <code>orangered</code> <p>The color orangered | orangered</p> <code>orchid</code> <p>The color orchid | orchid</p> <code>palegoldenrod</code> <p>The color palegoldenrod | palegoldenrod</p> <code>palegreen</code> <p>The color palegreen | palegreen</p> <code>paleturquoise</code> <p>The color paleturquoise | paleturquoise</p> <code>palevioletred</code> <p>The color palevioletred | palevioletred</p> <code>papayawhip</code> <p>The color papayawhip | papayawhip</p> <code>peachpuff</code> <p>The color peachpuff | peachpuff</p> <code>peru</code> <p>The color peru | peru</p> <code>pink</code> <p>The color pink | pink</p> <code>plum</code> <p>The color plum | plum</p> <code>powderblue</code> <p>The color powderblue | powderblue</p> <code>purple</code> <p>The color purple | purple</p> <code>red</code> <p>The color red | red</p> <code>rosybrown</code> <p>The color rosybrown | rosybrown</p> <code>royalblue</code> <p>The color royalblue | royalblue</p> <code>saddlebrown</code> <p>The color saddlebrown | saddlebrown</p> <code>salmon</code> <p>The color salmon | salmon</p> <code>sandybrown</code> <p>The color sandybrown | sandybrown</p> <code>seagreen</code> <p>The color seagreen | seagreen</p> <code>seashell</code> <p>The color seashell | seashell</p> <code>sienna</code> <p>The color sienna | sienna</p> <code>silver</code> <p>The color silver | silver</p> <code>skyblue</code> <p>The color skyblue | skyblue</p> <code>slateblue</code> <p>The color slateblue | slateblue</p> <code>slategray</code> <p>The color slategray | slategray</p> <code>snow</code> <p>The color snow | snow</p> <code>springgreen</code> <p>The color springgreen | springgreen</p> <code>steelblue</code> <p>The color steelblue | steelblue</p> <code>tan</code> <p>The color tan | tan</p> <code>teal</code> <p>The color teal | teal</p> <code>thistle</code> <p>The color thistle | thistle</p> <code>tomato</code> <p>The color tomato | tomato</p> <code>turquoise</code> <p>The color turquoise | turquoise</p> <code>violet</code> <p>The color violet | violet</p> <code>wheat</code> <p>The color wheat | wheat</p> <code>white</code> <p>The color white | white</p> <code>whitesmoke</code> <p>The color whitesmoke | whitesmoke</p> <code>yellow</code> <p>The color yellow | yellow</p> <code>yellowgreen</code> <p>The color yellowgreen | yellowgreen</p>"},{"location":"api_reference/coloring/#momapy.coloring.Color","title":"Color  <code>dataclass</code>","text":"<pre><code>Color(red: int, green: int, blue: int, alpha: float = 1.0)\n</code></pre> <p>               Bases: <code>object</code></p> <p>Represents an RGBA color.</p> <p>Examples:</p> <pre><code>color = Color(255, 87, 51)\ncolor.red\ncolor_with_alpha = Color(255, 87, 51, 0.5)\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>red</code> <code>int</code> <p>The red component of the color</p> required <code>green</code> <code>int</code> <p>The green component of the color</p> required <code>blue</code> <code>int</code> <p>The blue component of the color</p> required <code>alpha</code> <code>float</code> <p>The alpha component of the color</p> <code>1.0</code> <p>Methods:</p> Name Description <code>__or__</code> <p>Return a new color with the specified alpha value.</p> <code>from_hex</code> <p>Create a color from a hexadecimal string.</p> <code>from_hexa</code> <p>Create a color from a hexadecimal string with alpha.</p> <code>from_rgb</code> <p>Create a color from RGB components.</p> <code>from_rgba</code> <p>Create a color from RGBA components.</p> <code>to_hex</code> <p>Return the color as hexadecimal string.</p> <code>to_hexa</code> <p>Return the color as hexadecimal string with alpha.</p> <code>to_rgb</code> <p>Return the color as RGB tuple.</p> <code>to_rgba</code> <p>Return the color as RGBA tuple.</p> <code>with_alpha</code> <p>Return a copy of the color with the specified alpha.</p>"},{"location":"api_reference/coloring/#momapy.coloring.Color.__or__","title":"__or__","text":"<pre><code>__or__(alpha: float) -&gt; Self\n</code></pre> <p>Return a new color with the specified alpha value.</p> <p>The <code>|</code> operator takes alpha on the same 0.0-1.0 scale used by the constructor, <code>from_rgba</code>, and <code>with_alpha</code>, e.g. <code>red | 0.5</code> yields an alpha of <code>0.5</code>.</p> <p>Parameters:</p> Name Type Description Default <code>alpha</code> <code>float</code> <p>Alpha value (0.0-1.0).</p> required <p>Returns:</p> Type Description <code>Self</code> <p>A new Color with the specified alpha.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If alpha is not between 0.0 and 1.0.</p> Source code in <code>src/momapy/coloring.py</code> <pre><code>def __or__(self, alpha: float) -&gt; typing_extensions.Self:\n    \"\"\"Return a new color with the specified alpha value.\n\n    The `|` operator takes alpha on the same 0.0-1.0 scale used by the\n    constructor, `from_rgba`, and `with_alpha`, e.g. ``red | 0.5`` yields\n    an alpha of ``0.5``.\n\n    Args:\n        alpha: Alpha value (0.0-1.0).\n\n    Returns:\n        A new Color with the specified alpha.\n\n    Raises:\n        ValueError: If alpha is not between 0.0 and 1.0.\n    \"\"\"\n    if alpha &lt; 0 or alpha &gt; 1:\n        raise ValueError(\"alpha should be a number between 0.0 and 1.0\")\n    return dataclasses.replace(self, alpha=alpha)\n</code></pre>"},{"location":"api_reference/coloring/#momapy.coloring.Color.from_hex","title":"from_hex  <code>classmethod</code>","text":"<pre><code>from_hex(color_str: str) -&gt; Self\n</code></pre> <p>Create a color from a hexadecimal string.</p> <p>Parameters:</p> Name Type Description Default <code>color_str</code> <code>str</code> <p>Hex color string (e.g., \"#FF5733\" or \"FF5733\").</p> required <p>Returns:</p> Type Description <code>Self</code> <p>A new Color instance.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the hex string is invalid.</p> <p>Examples:</p> <pre><code>Color.from_hex(\"#FF5733\")\n</code></pre> Source code in <code>src/momapy/coloring.py</code> <pre><code>@classmethod\ndef from_hex(cls, color_str: str) -&gt; typing_extensions.Self:\n    \"\"\"Create a color from a hexadecimal string.\n\n    Args:\n        color_str: Hex color string (e.g., \"#FF5733\" or \"FF5733\").\n\n    Returns:\n        A new Color instance.\n\n    Raises:\n        ValueError: If the hex string is invalid.\n\n    Examples:\n        ```python\n        Color.from_hex(\"#FF5733\")\n        ```\n    \"\"\"\n    color_str = color_str.lstrip(\"#\")\n    if len(color_str) != 6:\n        raise ValueError(f\"invalid hexadecimal RGB value {color_str}\")\n    red = int(color_str[:2], 16)\n    green = int(color_str[2:4], 16)\n    blue = int(color_str[4:6], 16)\n    return cls(red, green, blue)\n</code></pre>"},{"location":"api_reference/coloring/#momapy.coloring.Color.from_hexa","title":"from_hexa  <code>classmethod</code>","text":"<pre><code>from_hexa(color_str: str) -&gt; Self\n</code></pre> <p>Create a color from a hexadecimal string with alpha.</p> <p>Parameters:</p> Name Type Description Default <code>color_str</code> <code>str</code> <p>Hex color string with alpha (e.g., \"#FF5733FF\").</p> required <p>Returns:</p> Type Description <code>Self</code> <p>A new Color instance.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the hex string is invalid.</p> <p>Examples:</p> <pre><code>Color.from_hexa(\"#FF573380\")\n</code></pre> Source code in <code>src/momapy/coloring.py</code> <pre><code>@classmethod\ndef from_hexa(cls, color_str: str) -&gt; typing_extensions.Self:\n    \"\"\"Create a color from a hexadecimal string with alpha.\n\n    Args:\n        color_str: Hex color string with alpha (e.g., \"#FF5733FF\").\n\n    Returns:\n        A new Color instance.\n\n    Raises:\n        ValueError: If the hex string is invalid.\n\n    Examples:\n        ```python\n        Color.from_hexa(\"#FF573380\")\n        ```\n    \"\"\"\n    color_str = color_str.lstrip(\"#\")\n    if len(color_str) != 8:\n        raise ValueError(f\"invalid hexadecimal RGBA value {color_str}\")\n    red = int(color_str[:2], 16)\n    green = int(color_str[2:4], 16)\n    blue = int(color_str[4:6], 16)\n    alpha = int(color_str[6:], 16) / 255\n    return cls(red, green, blue, alpha)\n</code></pre>"},{"location":"api_reference/coloring/#momapy.coloring.Color.from_rgb","title":"from_rgb  <code>classmethod</code>","text":"<pre><code>from_rgb(red: float, green: float, blue: float, rgb_range: tuple[float, float] = (0, 255)) -&gt; Self\n</code></pre> <p>Create a color from RGB components.</p> <p>The <code>rgb_range</code> argument mirrors :meth:<code>to_rgb</code>, so <code>from_rgb(*color.to_rgb(rgb_range), rgb_range)</code> round-trips.</p> <p>Parameters:</p> Name Type Description Default <code>red</code> <code>float</code> <p>Red component, expressed in <code>rgb_range</code> units.</p> required <code>green</code> <code>float</code> <p>Green component, expressed in <code>rgb_range</code> units.</p> required <code>blue</code> <code>float</code> <p>Blue component, expressed in <code>rgb_range</code> units.</p> required <code>rgb_range</code> <code>tuple[float, float]</code> <p>Range that the RGB components are expressed in, defaults to (0, 255).</p> <code>(0, 255)</code> <p>Returns:</p> Type Description <code>Self</code> <p>A new Color instance with alpha=1.0, normalized to 0-255 RGB.</p> <p>Examples:</p> <pre><code>Color.from_rgb(255, 87, 51)\nColor.from_rgb(1.0, 0.34, 0.2, rgb_range=(0, 1))\n</code></pre> Source code in <code>src/momapy/coloring.py</code> <pre><code>@classmethod\ndef from_rgb(\n    cls,\n    red: float,\n    green: float,\n    blue: float,\n    rgb_range: tuple[float, float] = (0, 255),\n) -&gt; typing_extensions.Self:\n    \"\"\"Create a color from RGB components.\n\n    The ``rgb_range`` argument mirrors :meth:`to_rgb`, so\n    ``from_rgb(*color.to_rgb(rgb_range), rgb_range)`` round-trips.\n\n    Args:\n        red: Red component, expressed in `rgb_range` units.\n        green: Green component, expressed in `rgb_range` units.\n        blue: Blue component, expressed in `rgb_range` units.\n        rgb_range: Range that the RGB components are expressed in,\n            defaults to (0, 255).\n\n    Returns:\n        A new Color instance with alpha=1.0, normalized to 0-255 RGB.\n\n    Examples:\n        ```python\n        Color.from_rgb(255, 87, 51)\n        Color.from_rgb(1.0, 0.34, 0.2, rgb_range=(0, 1))\n        ```\n    \"\"\"\n    rgb_width = rgb_range[1] - rgb_range[0]\n    return cls(\n        round((red - rgb_range[0]) / rgb_width * 255),\n        round((green - rgb_range[0]) / rgb_width * 255),\n        round((blue - rgb_range[0]) / rgb_width * 255),\n    )\n</code></pre>"},{"location":"api_reference/coloring/#momapy.coloring.Color.from_rgba","title":"from_rgba  <code>classmethod</code>","text":"<pre><code>from_rgba(red: float, green: float, blue: float, alpha: float, rgb_range: tuple[float, float] = (0, 255), alpha_range: tuple[float, float] = (0.0, 1.0)) -&gt; Self\n</code></pre> <p>Create a color from RGBA components.</p> <p>The <code>rgb_range</code> and <code>alpha_range</code> arguments mirror :meth:<code>to_rgba</code>, so <code>from_rgba(*color.to_rgba(rgb_range, alpha_range), rgb_range, alpha_range)</code> round-trips.</p> <p>Parameters:</p> Name Type Description Default <code>red</code> <code>float</code> <p>Red component, expressed in <code>rgb_range</code> units.</p> required <code>green</code> <code>float</code> <p>Green component, expressed in <code>rgb_range</code> units.</p> required <code>blue</code> <code>float</code> <p>Blue component, expressed in <code>rgb_range</code> units.</p> required <code>alpha</code> <code>float</code> <p>Alpha component, expressed in <code>alpha_range</code> units.</p> required <code>rgb_range</code> <code>tuple[float, float]</code> <p>Range that the RGB components are expressed in, defaults to (0, 255).</p> <code>(0, 255)</code> <code>alpha_range</code> <code>tuple[float, float]</code> <p>Range that <code>alpha</code> is expressed in, defaults to (0.0, 1.0).</p> <code>(0.0, 1.0)</code> <p>Returns:</p> Type Description <code>Self</code> <p>A new Color instance, normalized to 0-255 RGB / 0.0-1.0 alpha.</p> <p>Examples:</p> <pre><code>Color.from_rgba(255, 87, 51, 0.5)\nColor.from_rgba(1.0, 0.34, 0.2, 0.5, rgb_range=(0, 1))\n</code></pre> Source code in <code>src/momapy/coloring.py</code> <pre><code>@classmethod\ndef from_rgba(\n    cls,\n    red: float,\n    green: float,\n    blue: float,\n    alpha: float,\n    rgb_range: tuple[float, float] = (0, 255),\n    alpha_range: tuple[float, float] = (0.0, 1.0),\n) -&gt; typing_extensions.Self:\n    \"\"\"Create a color from RGBA components.\n\n    The ``rgb_range`` and ``alpha_range`` arguments mirror\n    :meth:`to_rgba`, so ``from_rgba(*color.to_rgba(rgb_range, alpha_range),\n    rgb_range, alpha_range)`` round-trips.\n\n    Args:\n        red: Red component, expressed in `rgb_range` units.\n        green: Green component, expressed in `rgb_range` units.\n        blue: Blue component, expressed in `rgb_range` units.\n        alpha: Alpha component, expressed in `alpha_range` units.\n        rgb_range: Range that the RGB components are expressed in,\n            defaults to (0, 255).\n        alpha_range: Range that `alpha` is expressed in, defaults to\n            (0.0, 1.0).\n\n    Returns:\n        A new Color instance, normalized to 0-255 RGB / 0.0-1.0 alpha.\n\n    Examples:\n        ```python\n        Color.from_rgba(255, 87, 51, 0.5)\n        Color.from_rgba(1.0, 0.34, 0.2, 0.5, rgb_range=(0, 1))\n        ```\n    \"\"\"\n    rgb_width = rgb_range[1] - rgb_range[0]\n    alpha_width = alpha_range[1] - alpha_range[0]\n    return cls(\n        round((red - rgb_range[0]) / rgb_width * 255),\n        round((green - rgb_range[0]) / rgb_width * 255),\n        round((blue - rgb_range[0]) / rgb_width * 255),\n        (alpha - alpha_range[0]) / alpha_width,\n    )\n</code></pre>"},{"location":"api_reference/coloring/#momapy.coloring.Color.to_hex","title":"to_hex","text":"<pre><code>to_hex() -&gt; str\n</code></pre> <p>Return the color as hexadecimal string.</p> <p>Returns:</p> Type Description <code>str</code> <p>Hex color string (e.g., \"#ff5733\").</p> <p>Examples:</p> <pre><code>color = Color(255, 87, 51)\ncolor.to_hex()\n</code></pre> Source code in <code>src/momapy/coloring.py</code> <pre><code>def to_hex(self) -&gt; str:\n    \"\"\"Return the color as hexadecimal string.\n\n    Returns:\n        Hex color string (e.g., \"#ff5733\").\n\n    Examples:\n        ```python\n        color = Color(255, 87, 51)\n        color.to_hex()\n        ```\n    \"\"\"\n    color_str = f\"#{format(self.red, '02x')}{format(self.green, '02x')}{format(self.blue, '02x')}\"\n    return color_str\n</code></pre>"},{"location":"api_reference/coloring/#momapy.coloring.Color.to_hexa","title":"to_hexa","text":"<pre><code>to_hexa() -&gt; str\n</code></pre> <p>Return the color as hexadecimal string with alpha.</p> <p>The alpha byte is rounded (not truncated), so the <code>from_hexa</code> / <code>to_hexa</code> round-trip is exact for every 8-bit alpha value.</p> <p>Returns:</p> Type Description <code>str</code> <p>Hex color string with alpha (e.g., \"#ff5733ff\").</p> <p>Examples:</p> <pre><code>color = Color(255, 87, 51, 0.5)\ncolor.to_hexa()\n</code></pre> Source code in <code>src/momapy/coloring.py</code> <pre><code>def to_hexa(self) -&gt; str:\n    \"\"\"Return the color as hexadecimal string with alpha.\n\n    The alpha byte is rounded (not truncated), so the\n    ``from_hexa`` / ``to_hexa`` round-trip is exact for every 8-bit\n    alpha value.\n\n    Returns:\n        Hex color string with alpha (e.g., \"#ff5733ff\").\n\n    Examples:\n        ```python\n        color = Color(255, 87, 51, 0.5)\n        color.to_hexa()\n        ```\n    \"\"\"\n    color_str = f\"{self.to_hex()}{format(round(self.alpha * 255), '02x')}\"\n    return color_str\n</code></pre>"},{"location":"api_reference/coloring/#momapy.coloring.Color.to_rgb","title":"to_rgb","text":"<pre><code>to_rgb(rgb_range: tuple[float, float] = (0, 255), round_to_int: bool = False) -&gt; tuple[int | float, int | float, int | float]\n</code></pre> <p>Return the color as RGB tuple.</p> <p>The components are sampled onto <code>rgb_range</code> and returned as floats by default. Pass <code>round_to_int=True</code> to round them to integers (e.g. for an 8-bit <code>(0, 255)</code> range).</p> <p>Parameters:</p> Name Type Description Default <code>rgb_range</code> <code>tuple[float, float]</code> <p>Range for RGB components, defaults to (0, 255).</p> <code>(0, 255)</code> <code>round_to_int</code> <code>bool</code> <p>If True, round the components to integers. Defaults to False.</p> <code>False</code> <p>Returns:</p> Type Description <code>tuple[int | float, int | float, int | float]</code> <p>Tuple of (red, green, blue).</p> <p>Examples:</p> <pre><code>color = Color(255, 128, 0)\ncolor.to_rgb()\ncolor.to_rgb(round_to_int=True)\n</code></pre> Source code in <code>src/momapy/coloring.py</code> <pre><code>def to_rgb(\n    self,\n    rgb_range: tuple[float, float] = (0, 255),\n    round_to_int: bool = False,\n) -&gt; tuple[int | float, int | float, int | float]:\n    \"\"\"Return the color as RGB tuple.\n\n    The components are sampled onto ``rgb_range`` and returned as floats by\n    default. Pass ``round_to_int=True`` to round them to integers (e.g. for\n    an 8-bit ``(0, 255)`` range).\n\n    Args:\n        rgb_range: Range for RGB components, defaults to (0, 255).\n        round_to_int: If True, round the components to integers. Defaults to\n            False.\n\n    Returns:\n        Tuple of (red, green, blue).\n\n    Examples:\n        ```python\n        color = Color(255, 128, 0)\n        color.to_rgb()\n        color.to_rgb(round_to_int=True)\n        ```\n    \"\"\"\n    width = rgb_range[1] - rgb_range[0]\n    red = rgb_range[0] + (self.red / 255) * width\n    green = rgb_range[0] + (self.green / 255) * width\n    blue = rgb_range[0] + (self.blue / 255) * width\n    if round_to_int:\n        red = int(round(red))\n        green = int(round(green))\n        blue = int(round(blue))\n    return (red, green, blue)\n</code></pre>"},{"location":"api_reference/coloring/#momapy.coloring.Color.to_rgba","title":"to_rgba","text":"<pre><code>to_rgba(rgb_range: tuple[float, float] = (0, 255), alpha_range: tuple[float, float] = (0.0, 1.0), rgba_range: tuple[float, float] | None = None, round_to_int: bool = False) -&gt; tuple[int | float, int | float, int | float, float]\n</code></pre> <p>Return the color as RGBA tuple.</p> <p>The RGB components are sampled onto <code>rgb_range</code> and returned as floats by default. Pass <code>round_to_int=True</code> to round them to integers (e.g. for an 8-bit <code>(0, 255)</code> range).</p> <p>Parameters:</p> Name Type Description Default <code>rgb_range</code> <code>tuple[float, float]</code> <p>Range for RGB components, defaults to (0, 255).</p> <code>(0, 255)</code> <code>alpha_range</code> <code>tuple[float, float]</code> <p>Range for alpha component, defaults to (0.0, 1.0).</p> <code>(0.0, 1.0)</code> <code>rgba_range</code> <code>tuple[float, float] | None</code> <p>Override range for all components.</p> <code>None</code> <code>round_to_int</code> <code>bool</code> <p>If True, round the RGB components to integers. Defaults to False.</p> <code>False</code> <p>Returns:</p> Type Description <code>tuple[int | float, int | float, int | float, float]</code> <p>Tuple of (red, green, blue, alpha).</p> <p>Examples:</p> <pre><code>color = Color(255, 128, 0)\ncolor.to_rgba()\ncolor.to_rgba(rgb_range=(0.0, 1.0))\ncolor.to_rgba(round_to_int=True)\n</code></pre> Source code in <code>src/momapy/coloring.py</code> <pre><code>def to_rgba(\n    self,\n    rgb_range: tuple[float, float] = (0, 255),\n    alpha_range: tuple[float, float] = (0.0, 1.0),\n    rgba_range: tuple[float, float] | None = None,\n    round_to_int: bool = False,\n) -&gt; tuple[int | float, int | float, int | float, float]:\n    \"\"\"Return the color as RGBA tuple.\n\n    The RGB components are sampled onto ``rgb_range`` and returned as floats\n    by default. Pass ``round_to_int=True`` to round them to integers (e.g.\n    for an 8-bit ``(0, 255)`` range).\n\n    Args:\n        rgb_range: Range for RGB components, defaults to (0, 255).\n        alpha_range: Range for alpha component, defaults to (0.0, 1.0).\n        rgba_range: Override range for all components.\n        round_to_int: If True, round the RGB components to integers.\n            Defaults to False.\n\n    Returns:\n        Tuple of (red, green, blue, alpha).\n\n    Examples:\n        ```python\n        color = Color(255, 128, 0)\n        color.to_rgba()\n        color.to_rgba(rgb_range=(0.0, 1.0))\n        color.to_rgba(round_to_int=True)\n        ```\n    \"\"\"\n    if rgba_range is not None:\n        rgb_range = rgba_range\n        alpha_range = rgba_range\n    rgb_width = rgb_range[1] - rgb_range[0]\n    alpha_width = alpha_range[1] - alpha_range[0]\n    red = rgb_range[0] + (self.red / 255) * rgb_width\n    green = rgb_range[0] + (self.green / 255) * rgb_width\n    blue = rgb_range[0] + (self.blue / 255) * rgb_width\n    alpha = alpha_range[0] + self.alpha * alpha_width\n    if round_to_int:\n        red = int(round(red))\n        green = int(round(green))\n        blue = int(round(blue))\n    return (red, green, blue, alpha)\n</code></pre>"},{"location":"api_reference/coloring/#momapy.coloring.Color.with_alpha","title":"with_alpha","text":"<pre><code>with_alpha(alpha: float, alpha_range: tuple[float, float] = (0, 1)) -&gt; Self\n</code></pre> <p>Return a copy of the color with the specified alpha.</p> <p>Parameters:</p> Name Type Description Default <code>alpha</code> <code>float</code> <p>Alpha value, expressed in <code>alpha_range</code> units.</p> required <code>alpha_range</code> <code>tuple[float, float]</code> <p>Range that <code>alpha</code> is expressed in, defaults to (0, 1).</p> <code>(0, 1)</code> <p>Returns:</p> Type Description <code>Self</code> <p>A new Color with the specified alpha, normalized to 0.0-1.0.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If <code>alpha</code> falls outside <code>alpha_range</code>.</p> Source code in <code>src/momapy/coloring.py</code> <pre><code>def with_alpha(\n    self, alpha: float, alpha_range: tuple[float, float] = (0, 1)\n) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the color with the specified alpha.\n\n    Args:\n        alpha: Alpha value, expressed in `alpha_range` units.\n        alpha_range: Range that `alpha` is expressed in, defaults to\n            (0, 1).\n\n    Returns:\n        A new Color with the specified alpha, normalized to 0.0-1.0.\n\n    Raises:\n        ValueError: If `alpha` falls outside `alpha_range`.\n    \"\"\"\n    alpha_width = alpha_range[1] - alpha_range[0]\n    if alpha &lt; alpha_range[0] or alpha &gt; alpha_range[1]:\n        raise ValueError(\n            f\"alpha should be a number between {alpha_range[0]} and \"\n            f\"{alpha_range[1]}\"\n        )\n    return dataclasses.replace(self, alpha=(alpha - alpha_range[0]) / alpha_width)\n</code></pre>"},{"location":"api_reference/coloring/#momapy.coloring.aliceblue","title":"aliceblue  <code>module-attribute</code>","text":"<pre><code>aliceblue = Color.from_rgb(240, 248, 255)\n</code></pre> <p>The color aliceblue | aliceblue</p>"},{"location":"api_reference/coloring/#momapy.coloring.antiquewhite","title":"antiquewhite  <code>module-attribute</code>","text":"<pre><code>antiquewhite = Color.from_rgb(250, 235, 215)\n</code></pre> <p>The color antiquewhite | antiquewhite</p>"},{"location":"api_reference/coloring/#momapy.coloring.aqua","title":"aqua  <code>module-attribute</code>","text":"<pre><code>aqua = Color.from_rgb(0, 255, 255)\n</code></pre> <p>The color aqua | aqua</p>"},{"location":"api_reference/coloring/#momapy.coloring.aquamarine","title":"aquamarine  <code>module-attribute</code>","text":"<pre><code>aquamarine = Color.from_rgb(127, 255, 212)\n</code></pre> <p>The color aquamarine | aquamarine</p>"},{"location":"api_reference/coloring/#momapy.coloring.azure","title":"azure  <code>module-attribute</code>","text":"<pre><code>azure = Color.from_rgb(240, 255, 255)\n</code></pre> <p>The color azure | azure</p>"},{"location":"api_reference/coloring/#momapy.coloring.beige","title":"beige  <code>module-attribute</code>","text":"<pre><code>beige = Color.from_rgb(245, 245, 220)\n</code></pre> <p>The color beige | beige</p>"},{"location":"api_reference/coloring/#momapy.coloring.bisque","title":"bisque  <code>module-attribute</code>","text":"<pre><code>bisque = Color.from_rgb(255, 228, 196)\n</code></pre> <p>The color bisque | bisque</p>"},{"location":"api_reference/coloring/#momapy.coloring.black","title":"black  <code>module-attribute</code>","text":"<pre><code>black = Color.from_rgb(0, 0, 0)\n</code></pre> <p>The color black | black</p>"},{"location":"api_reference/coloring/#momapy.coloring.blanchedalmond","title":"blanchedalmond  <code>module-attribute</code>","text":"<pre><code>blanchedalmond = Color.from_rgb(255, 235, 205)\n</code></pre> <p>The color blanchedalmond | blanchedalmond</p>"},{"location":"api_reference/coloring/#momapy.coloring.blue","title":"blue  <code>module-attribute</code>","text":"<pre><code>blue = Color.from_rgb(0, 0, 255)\n</code></pre> <p>The color blue | blue</p>"},{"location":"api_reference/coloring/#momapy.coloring.blueviolet","title":"blueviolet  <code>module-attribute</code>","text":"<pre><code>blueviolet = Color.from_rgb(138, 43, 226)\n</code></pre> <p>The color blueviolet | blueviolet</p>"},{"location":"api_reference/coloring/#momapy.coloring.brown","title":"brown  <code>module-attribute</code>","text":"<pre><code>brown = Color.from_rgb(165, 42, 42)\n</code></pre> <p>The color brown | brown</p>"},{"location":"api_reference/coloring/#momapy.coloring.burlywood","title":"burlywood  <code>module-attribute</code>","text":"<pre><code>burlywood = Color.from_rgb(222, 184, 135)\n</code></pre> <p>The color burlywood | burlywood</p>"},{"location":"api_reference/coloring/#momapy.coloring.cadetblue","title":"cadetblue  <code>module-attribute</code>","text":"<pre><code>cadetblue = Color.from_rgb(95, 158, 160)\n</code></pre> <p>The color cadetblue | cadetblue</p>"},{"location":"api_reference/coloring/#momapy.coloring.chartreuse","title":"chartreuse  <code>module-attribute</code>","text":"<pre><code>chartreuse = Color.from_rgb(127, 255, 0)\n</code></pre> <p>The color chartreuse | chartreuse</p>"},{"location":"api_reference/coloring/#momapy.coloring.chocolate","title":"chocolate  <code>module-attribute</code>","text":"<pre><code>chocolate = Color.from_rgb(210, 105, 30)\n</code></pre> <p>The color chocolate | chocolate</p>"},{"location":"api_reference/coloring/#momapy.coloring.coral","title":"coral  <code>module-attribute</code>","text":"<pre><code>coral = Color.from_rgb(255, 127, 80)\n</code></pre> <p>The color coral | coral</p>"},{"location":"api_reference/coloring/#momapy.coloring.cornflowerblue","title":"cornflowerblue  <code>module-attribute</code>","text":"<pre><code>cornflowerblue = Color.from_rgb(100, 149, 237)\n</code></pre> <p>The color cornflowerblue | cornflowerblue</p>"},{"location":"api_reference/coloring/#momapy.coloring.cornsilk","title":"cornsilk  <code>module-attribute</code>","text":"<pre><code>cornsilk = Color.from_rgb(255, 248, 220)\n</code></pre> <p>The color cornsilk | cornsilk</p>"},{"location":"api_reference/coloring/#momapy.coloring.crimson","title":"crimson  <code>module-attribute</code>","text":"<pre><code>crimson = Color.from_rgb(220, 20, 60)\n</code></pre> <p>The color crimson | crimson</p>"},{"location":"api_reference/coloring/#momapy.coloring.cyan","title":"cyan  <code>module-attribute</code>","text":"<pre><code>cyan = Color.from_rgb(0, 255, 255)\n</code></pre> <p>The color cyan | cyan</p>"},{"location":"api_reference/coloring/#momapy.coloring.darkblue","title":"darkblue  <code>module-attribute</code>","text":"<pre><code>darkblue = Color.from_rgb(0, 0, 139)\n</code></pre> <p>The color darkblue | darkblue</p>"},{"location":"api_reference/coloring/#momapy.coloring.darkcyan","title":"darkcyan  <code>module-attribute</code>","text":"<pre><code>darkcyan = Color.from_rgb(0, 139, 139)\n</code></pre> <p>The color darkcyan | darkcyan</p>"},{"location":"api_reference/coloring/#momapy.coloring.darkgoldenrod","title":"darkgoldenrod  <code>module-attribute</code>","text":"<pre><code>darkgoldenrod = Color.from_rgb(184, 134, 11)\n</code></pre> <p>The color darkgoldenrod | darkgoldenrod</p>"},{"location":"api_reference/coloring/#momapy.coloring.darkgray","title":"darkgray  <code>module-attribute</code>","text":"<pre><code>darkgray = Color.from_rgb(169, 169, 169)\n</code></pre> <p>The color darkgray | darkgray</p>"},{"location":"api_reference/coloring/#momapy.coloring.darkgreen","title":"darkgreen  <code>module-attribute</code>","text":"<pre><code>darkgreen = Color.from_rgb(0, 100, 0)\n</code></pre> <p>The color darkgreen | darkgreen</p>"},{"location":"api_reference/coloring/#momapy.coloring.darkgrey","title":"darkgrey  <code>module-attribute</code>","text":"<pre><code>darkgrey = Color.from_rgb(169, 169, 169)\n</code></pre> <p>The color darkgrey | darkgrey</p>"},{"location":"api_reference/coloring/#momapy.coloring.darkkhaki","title":"darkkhaki  <code>module-attribute</code>","text":"<pre><code>darkkhaki = Color.from_rgb(189, 183, 107)\n</code></pre> <p>The color darkkhaki | darkkhaki</p>"},{"location":"api_reference/coloring/#momapy.coloring.darkmagenta","title":"darkmagenta  <code>module-attribute</code>","text":"<pre><code>darkmagenta = Color.from_rgb(139, 0, 139)\n</code></pre> <p>The color darkmagenta | darkmagenta</p>"},{"location":"api_reference/coloring/#momapy.coloring.darkolivegreen","title":"darkolivegreen  <code>module-attribute</code>","text":"<pre><code>darkolivegreen = Color.from_rgb(85, 107, 47)\n</code></pre> <p>The color darkolivegreen | darkolivegreen</p>"},{"location":"api_reference/coloring/#momapy.coloring.darkorange","title":"darkorange  <code>module-attribute</code>","text":"<pre><code>darkorange = Color.from_rgb(255, 140, 0)\n</code></pre> <p>The color darkorange | darkorange</p>"},{"location":"api_reference/coloring/#momapy.coloring.darkorchid","title":"darkorchid  <code>module-attribute</code>","text":"<pre><code>darkorchid = Color.from_rgb(153, 50, 204)\n</code></pre> <p>The color darkorchid | darkorchid</p>"},{"location":"api_reference/coloring/#momapy.coloring.darkred","title":"darkred  <code>module-attribute</code>","text":"<pre><code>darkred = Color.from_rgb(139, 0, 0)\n</code></pre> <p>The color darkred | darkred</p>"},{"location":"api_reference/coloring/#momapy.coloring.darksalmon","title":"darksalmon  <code>module-attribute</code>","text":"<pre><code>darksalmon = Color.from_rgb(233, 150, 122)\n</code></pre> <p>The color darksalmon | darksalmon</p>"},{"location":"api_reference/coloring/#momapy.coloring.darkseagreen","title":"darkseagreen  <code>module-attribute</code>","text":"<pre><code>darkseagreen = Color.from_rgb(143, 188, 143)\n</code></pre> <p>The color darkseagreen | darkseagreen</p>"},{"location":"api_reference/coloring/#momapy.coloring.darkslateblue","title":"darkslateblue  <code>module-attribute</code>","text":"<pre><code>darkslateblue = Color.from_rgb(72, 61, 139)\n</code></pre> <p>The color darkslateblue | darkslateblue</p>"},{"location":"api_reference/coloring/#momapy.coloring.darkslategray","title":"darkslategray  <code>module-attribute</code>","text":"<pre><code>darkslategray = Color.from_rgb(47, 79, 79)\n</code></pre> <p>The color darkslategray | darkslategray</p>"},{"location":"api_reference/coloring/#momapy.coloring.darkturquoise","title":"darkturquoise  <code>module-attribute</code>","text":"<pre><code>darkturquoise = Color.from_rgb(0, 206, 209)\n</code></pre> <p>The color darkturquoise | darkturquoise</p>"},{"location":"api_reference/coloring/#momapy.coloring.darkviolet","title":"darkviolet  <code>module-attribute</code>","text":"<pre><code>darkviolet = Color.from_rgb(148, 0, 211)\n</code></pre> <p>The color darkviolet | darkviolet</p>"},{"location":"api_reference/coloring/#momapy.coloring.deeppink","title":"deeppink  <code>module-attribute</code>","text":"<pre><code>deeppink = Color.from_rgb(255, 20, 147)\n</code></pre> <p>The color deeppink | deeppink</p>"},{"location":"api_reference/coloring/#momapy.coloring.deepskyblue","title":"deepskyblue  <code>module-attribute</code>","text":"<pre><code>deepskyblue = Color.from_rgb(0, 191, 255)\n</code></pre> <p>The color deepskyblue | deepskyblue</p>"},{"location":"api_reference/coloring/#momapy.coloring.dimgray","title":"dimgray  <code>module-attribute</code>","text":"<pre><code>dimgray = Color.from_rgb(105, 105, 105)\n</code></pre> <p>The color dimgray | dimgray</p>"},{"location":"api_reference/coloring/#momapy.coloring.dimgrey","title":"dimgrey  <code>module-attribute</code>","text":"<pre><code>dimgrey = Color.from_rgb(105, 105, 105)\n</code></pre> <p>The color dimgrey | dimgrey</p>"},{"location":"api_reference/coloring/#momapy.coloring.dodgerblue","title":"dodgerblue  <code>module-attribute</code>","text":"<pre><code>dodgerblue = Color.from_rgb(30, 144, 255)\n</code></pre> <p>The color dodgerblue | dodgerblue</p>"},{"location":"api_reference/coloring/#momapy.coloring.firebrick","title":"firebrick  <code>module-attribute</code>","text":"<pre><code>firebrick = Color.from_rgb(178, 34, 34)\n</code></pre> <p>The color firebrick | firebrick</p>"},{"location":"api_reference/coloring/#momapy.coloring.floralwhite","title":"floralwhite  <code>module-attribute</code>","text":"<pre><code>floralwhite = Color.from_rgb(255, 250, 240)\n</code></pre> <p>The color floralwhite | floralwhite</p>"},{"location":"api_reference/coloring/#momapy.coloring.forestgreen","title":"forestgreen  <code>module-attribute</code>","text":"<pre><code>forestgreen = Color.from_rgb(34, 139, 34)\n</code></pre> <p>The color forestgreen | forestgreen</p>"},{"location":"api_reference/coloring/#momapy.coloring.fuchsia","title":"fuchsia  <code>module-attribute</code>","text":"<pre><code>fuchsia = Color.from_rgb(255, 0, 255)\n</code></pre> <p>The color fuchsia | fuchsia</p>"},{"location":"api_reference/coloring/#momapy.coloring.gainsboro","title":"gainsboro  <code>module-attribute</code>","text":"<pre><code>gainsboro = Color.from_rgb(220, 220, 220)\n</code></pre> <p>The color gainsboro | gainsboro</p>"},{"location":"api_reference/coloring/#momapy.coloring.get_color","title":"get_color","text":"<pre><code>get_color(color_name: str) -&gt; Color\n</code></pre> <p>Return the named color with the given name.</p> <p>Parameters:</p> Name Type Description Default <code>color_name</code> <code>str</code> <p>Name of the color to look up.</p> required <p>Returns:</p> Type Description <code>Color</code> <p>The corresponding Color.</p> <p>Raises:</p> Type Description <code>KeyError</code> <p>If no color with that name exists.</p> <p>Examples:</p> <pre><code>get_color(\"red\")\n</code></pre> Source code in <code>src/momapy/coloring.py</code> <pre><code>def get_color(color_name: str) -&gt; Color:\n    \"\"\"Return the named color with the given name.\n\n    Args:\n        color_name: Name of the color to look up.\n\n    Returns:\n        The corresponding Color.\n\n    Raises:\n        KeyError: If no color with that name exists.\n\n    Examples:\n        ```python\n        get_color(\"red\")\n        ```\n    \"\"\"\n    return _NAME_TO_COLOR[color_name]\n</code></pre>"},{"location":"api_reference/coloring/#momapy.coloring.ghostwhite","title":"ghostwhite  <code>module-attribute</code>","text":"<pre><code>ghostwhite = Color.from_rgb(248, 248, 255)\n</code></pre> <p>The color ghostwhite | ghostwhite</p>"},{"location":"api_reference/coloring/#momapy.coloring.gold","title":"gold  <code>module-attribute</code>","text":"<pre><code>gold = Color.from_rgb(255, 215, 0)\n</code></pre> <p>The color gold | gold</p>"},{"location":"api_reference/coloring/#momapy.coloring.goldenrod","title":"goldenrod  <code>module-attribute</code>","text":"<pre><code>goldenrod = Color.from_rgb(218, 165, 32)\n</code></pre> <p>The color goldenrod | goldenrod</p>"},{"location":"api_reference/coloring/#momapy.coloring.gray","title":"gray  <code>module-attribute</code>","text":"<pre><code>gray = Color.from_rgb(128, 128, 128)\n</code></pre> <p>The color gray | gray</p>"},{"location":"api_reference/coloring/#momapy.coloring.green","title":"green  <code>module-attribute</code>","text":"<pre><code>green = Color.from_rgb(0, 128, 0)\n</code></pre> <p>The color green | green</p>"},{"location":"api_reference/coloring/#momapy.coloring.greenyellow","title":"greenyellow  <code>module-attribute</code>","text":"<pre><code>greenyellow = Color.from_rgb(173, 255, 47)\n</code></pre> <p>The color greenyellow | greenyellow</p>"},{"location":"api_reference/coloring/#momapy.coloring.grey","title":"grey  <code>module-attribute</code>","text":"<pre><code>grey = Color.from_rgb(128, 128, 128)\n</code></pre> <p>The color grey | grey</p>"},{"location":"api_reference/coloring/#momapy.coloring.has_color","title":"has_color","text":"<pre><code>has_color(color_name: str) -&gt; bool\n</code></pre> <p>Check if a named color exists.</p> <p>Parameters:</p> Name Type Description Default <code>color_name</code> <code>str</code> <p>Name of the color to check.</p> required <p>Returns:</p> Type Description <code>bool</code> <p>True if the color exists, False otherwise.</p> <p>Examples:</p> <pre><code>has_color(\"red\")\nhas_color(\"not_a_color\")\n</code></pre> Source code in <code>src/momapy/coloring.py</code> <pre><code>def has_color(color_name: str) -&gt; bool:\n    \"\"\"Check if a named color exists.\n\n    Args:\n        color_name: Name of the color to check.\n\n    Returns:\n        True if the color exists, False otherwise.\n\n    Examples:\n        ```python\n        has_color(\"red\")\n        has_color(\"not_a_color\")\n        ```\n    \"\"\"\n    return color_name in _NAME_TO_COLOR\n</code></pre>"},{"location":"api_reference/coloring/#momapy.coloring.honeydew","title":"honeydew  <code>module-attribute</code>","text":"<pre><code>honeydew = Color.from_rgb(240, 255, 240)\n</code></pre> <p>The color honeydew | honeydew</p>"},{"location":"api_reference/coloring/#momapy.coloring.hotpink","title":"hotpink  <code>module-attribute</code>","text":"<pre><code>hotpink = Color.from_rgb(255, 105, 180)\n</code></pre> <p>The color hotpink | hotpink</p>"},{"location":"api_reference/coloring/#momapy.coloring.indianred","title":"indianred  <code>module-attribute</code>","text":"<pre><code>indianred = Color.from_rgb(205, 92, 92)\n</code></pre> <p>The color indianred | indianred</p>"},{"location":"api_reference/coloring/#momapy.coloring.indigo","title":"indigo  <code>module-attribute</code>","text":"<pre><code>indigo = Color.from_rgb(75, 0, 130)\n</code></pre> <p>The color indigo | indigo</p>"},{"location":"api_reference/coloring/#momapy.coloring.ivory","title":"ivory  <code>module-attribute</code>","text":"<pre><code>ivory = Color.from_rgb(255, 255, 240)\n</code></pre> <p>The color ivory | ivory</p>"},{"location":"api_reference/coloring/#momapy.coloring.khaki","title":"khaki  <code>module-attribute</code>","text":"<pre><code>khaki = Color.from_rgb(240, 230, 140)\n</code></pre> <p>The color khaki | khaki</p>"},{"location":"api_reference/coloring/#momapy.coloring.lavender","title":"lavender  <code>module-attribute</code>","text":"<pre><code>lavender = Color.from_rgb(230, 230, 250)\n</code></pre> <p>The color lavender | lavender</p>"},{"location":"api_reference/coloring/#momapy.coloring.lavenderblush","title":"lavenderblush  <code>module-attribute</code>","text":"<pre><code>lavenderblush = Color.from_rgb(255, 240, 245)\n</code></pre> <p>The color lavenderblush | lavenderblush</p>"},{"location":"api_reference/coloring/#momapy.coloring.lawngreen","title":"lawngreen  <code>module-attribute</code>","text":"<pre><code>lawngreen = Color.from_rgb(124, 252, 0)\n</code></pre> <p>The color lawngreen | lawngreen</p>"},{"location":"api_reference/coloring/#momapy.coloring.lemonchiffon","title":"lemonchiffon  <code>module-attribute</code>","text":"<pre><code>lemonchiffon = Color.from_rgb(255, 250, 205)\n</code></pre> <p>The color lemonchiffon | lemonchiffon</p>"},{"location":"api_reference/coloring/#momapy.coloring.lightblue","title":"lightblue  <code>module-attribute</code>","text":"<pre><code>lightblue = Color.from_rgb(173, 216, 230)\n</code></pre> <p>The color lightblue | lightblue</p>"},{"location":"api_reference/coloring/#momapy.coloring.lightcoral","title":"lightcoral  <code>module-attribute</code>","text":"<pre><code>lightcoral = Color.from_rgb(240, 128, 128)\n</code></pre> <p>The color lightcoral | lightcoral</p>"},{"location":"api_reference/coloring/#momapy.coloring.lightcyan","title":"lightcyan  <code>module-attribute</code>","text":"<pre><code>lightcyan = Color.from_rgb(224, 255, 255)\n</code></pre> <p>The color lightcyan | lightcyan</p>"},{"location":"api_reference/coloring/#momapy.coloring.lightgoldenrodyellow","title":"lightgoldenrodyellow  <code>module-attribute</code>","text":"<pre><code>lightgoldenrodyellow = Color.from_rgb(250, 250, 210)\n</code></pre> <p>The color lightgoldenrodyellow | lightgoldenrodyellow</p>"},{"location":"api_reference/coloring/#momapy.coloring.lightgray","title":"lightgray  <code>module-attribute</code>","text":"<pre><code>lightgray = Color.from_rgb(211, 211, 211)\n</code></pre> <p>The color lightgray | lightgray</p>"},{"location":"api_reference/coloring/#momapy.coloring.lightgreen","title":"lightgreen  <code>module-attribute</code>","text":"<pre><code>lightgreen = Color.from_rgb(144, 238, 144)\n</code></pre> <p>The color lightgreen | lightgreen</p>"},{"location":"api_reference/coloring/#momapy.coloring.lightgrey","title":"lightgrey  <code>module-attribute</code>","text":"<pre><code>lightgrey = Color.from_rgb(211, 211, 211)\n</code></pre> <p>The color lightgrey | lightgrey</p>"},{"location":"api_reference/coloring/#momapy.coloring.lightpink","title":"lightpink  <code>module-attribute</code>","text":"<pre><code>lightpink = Color.from_rgb(255, 182, 193)\n</code></pre> <p>The color lightpink | lightpink</p>"},{"location":"api_reference/coloring/#momapy.coloring.lightsalmon","title":"lightsalmon  <code>module-attribute</code>","text":"<pre><code>lightsalmon = Color.from_rgb(255, 160, 122)\n</code></pre> <p>The color lightsalmon | lightsalmon</p>"},{"location":"api_reference/coloring/#momapy.coloring.lightseagreen","title":"lightseagreen  <code>module-attribute</code>","text":"<pre><code>lightseagreen = Color.from_rgb(32, 178, 170)\n</code></pre> <p>The color lightseagreen | lightseagreen</p>"},{"location":"api_reference/coloring/#momapy.coloring.lightskyblue","title":"lightskyblue  <code>module-attribute</code>","text":"<pre><code>lightskyblue = Color.from_rgb(135, 206, 250)\n</code></pre> <p>The color lightskyblue | lightskyblue</p>"},{"location":"api_reference/coloring/#momapy.coloring.lightslategray","title":"lightslategray  <code>module-attribute</code>","text":"<pre><code>lightslategray = Color.from_rgb(119, 136, 153)\n</code></pre> <p>The color lightslategray | lightslategray</p>"},{"location":"api_reference/coloring/#momapy.coloring.lightsteelblue","title":"lightsteelblue  <code>module-attribute</code>","text":"<pre><code>lightsteelblue = Color.from_rgb(176, 196, 222)\n</code></pre> <p>The color lightsteelblue | lightsteelblue</p>"},{"location":"api_reference/coloring/#momapy.coloring.lightyellow","title":"lightyellow  <code>module-attribute</code>","text":"<pre><code>lightyellow = Color.from_rgb(255, 255, 224)\n</code></pre> <p>The color lightyellow | lightyellow</p>"},{"location":"api_reference/coloring/#momapy.coloring.lime","title":"lime  <code>module-attribute</code>","text":"<pre><code>lime = Color.from_rgb(0, 255, 0)\n</code></pre> <p>The color lime | lime</p>"},{"location":"api_reference/coloring/#momapy.coloring.limegreen","title":"limegreen  <code>module-attribute</code>","text":"<pre><code>limegreen = Color.from_rgb(50, 205, 50)\n</code></pre> <p>The color limegreen | limegreen</p>"},{"location":"api_reference/coloring/#momapy.coloring.linen","title":"linen  <code>module-attribute</code>","text":"<pre><code>linen = Color.from_rgb(250, 240, 230)\n</code></pre> <p>The color linen | linen</p>"},{"location":"api_reference/coloring/#momapy.coloring.list_colors","title":"list_colors","text":"<pre><code>list_colors() -&gt; list[str]\n</code></pre> <p>Return the names of all available named colors.</p> <p>Returns:</p> Type Description <code>list[str]</code> <p>List of color names for all predefined colors.</p> <p>Examples:</p> <pre><code>colors = list_colors()\nlen(colors) &gt; 0\n</code></pre> Source code in <code>src/momapy/coloring.py</code> <pre><code>def list_colors() -&gt; list[str]:\n    \"\"\"Return the names of all available named colors.\n\n    Returns:\n        List of color names for all predefined colors.\n\n    Examples:\n        ```python\n        colors = list_colors()\n        len(colors) &gt; 0\n        ```\n    \"\"\"\n    return list(_NAME_TO_COLOR.keys())\n</code></pre>"},{"location":"api_reference/coloring/#momapy.coloring.magenta","title":"magenta  <code>module-attribute</code>","text":"<pre><code>magenta = Color.from_rgb(255, 0, 255)\n</code></pre> <p>The color magenta | magenta</p>"},{"location":"api_reference/coloring/#momapy.coloring.maroon","title":"maroon  <code>module-attribute</code>","text":"<pre><code>maroon = Color.from_rgb(128, 0, 0)\n</code></pre> <p>The color maroon | maroon</p>"},{"location":"api_reference/coloring/#momapy.coloring.mediumaquamarine","title":"mediumaquamarine  <code>module-attribute</code>","text":"<pre><code>mediumaquamarine = Color.from_rgb(102, 205, 170)\n</code></pre> <p>The color mediumaquamarine | mediumaquamarine</p>"},{"location":"api_reference/coloring/#momapy.coloring.mediumblue","title":"mediumblue  <code>module-attribute</code>","text":"<pre><code>mediumblue = Color.from_rgb(0, 0, 205)\n</code></pre> <p>The color mediumblue | mediumblue</p>"},{"location":"api_reference/coloring/#momapy.coloring.mediumorchid","title":"mediumorchid  <code>module-attribute</code>","text":"<pre><code>mediumorchid = Color.from_rgb(186, 85, 211)\n</code></pre> <p>The color mediumorchid | mediumorchid</p>"},{"location":"api_reference/coloring/#momapy.coloring.mediumpurple","title":"mediumpurple  <code>module-attribute</code>","text":"<pre><code>mediumpurple = Color.from_rgb(147, 112, 219)\n</code></pre> <p>The color mediumpurple | mediumpurple</p>"},{"location":"api_reference/coloring/#momapy.coloring.mediumseagreen","title":"mediumseagreen  <code>module-attribute</code>","text":"<pre><code>mediumseagreen = Color.from_rgb(60, 179, 113)\n</code></pre> <p>The color mediumseagreen | mediumseagreen</p>"},{"location":"api_reference/coloring/#momapy.coloring.mediumslateblue","title":"mediumslateblue  <code>module-attribute</code>","text":"<pre><code>mediumslateblue = Color.from_rgb(123, 104, 238)\n</code></pre> <p>The color mediumslateblue | mediumslateblue</p>"},{"location":"api_reference/coloring/#momapy.coloring.mediumspringgreen","title":"mediumspringgreen  <code>module-attribute</code>","text":"<pre><code>mediumspringgreen = Color.from_rgb(0, 250, 154)\n</code></pre> <p>The color mediumspringgreen | mediumspringgreen</p>"},{"location":"api_reference/coloring/#momapy.coloring.mediumturquoise","title":"mediumturquoise  <code>module-attribute</code>","text":"<pre><code>mediumturquoise = Color.from_rgb(72, 209, 204)\n</code></pre> <p>The color mediumturquoise | mediumturquoise</p>"},{"location":"api_reference/coloring/#momapy.coloring.mediumvioletred","title":"mediumvioletred  <code>module-attribute</code>","text":"<pre><code>mediumvioletred = Color.from_rgb(199, 21, 133)\n</code></pre> <p>The color mediumvioletred | mediumvioletred</p>"},{"location":"api_reference/coloring/#momapy.coloring.midnightblue","title":"midnightblue  <code>module-attribute</code>","text":"<pre><code>midnightblue = Color.from_rgb(25, 25, 112)\n</code></pre> <p>The color midnightblue | midnightblue</p>"},{"location":"api_reference/coloring/#momapy.coloring.mintcream","title":"mintcream  <code>module-attribute</code>","text":"<pre><code>mintcream = Color.from_rgb(245, 255, 250)\n</code></pre> <p>The color mintcream | mintcream</p>"},{"location":"api_reference/coloring/#momapy.coloring.mistyrose","title":"mistyrose  <code>module-attribute</code>","text":"<pre><code>mistyrose = Color.from_rgb(255, 228, 225)\n</code></pre> <p>The color mistyrose | mistyrose</p>"},{"location":"api_reference/coloring/#momapy.coloring.moccasin","title":"moccasin  <code>module-attribute</code>","text":"<pre><code>moccasin = Color.from_rgb(255, 228, 181)\n</code></pre> <p>The color moccasin | moccasin</p>"},{"location":"api_reference/coloring/#momapy.coloring.navajowhite","title":"navajowhite  <code>module-attribute</code>","text":"<pre><code>navajowhite = Color.from_rgb(255, 222, 173)\n</code></pre> <p>The color navajowhite | navajowhite</p>"},{"location":"api_reference/coloring/#momapy.coloring.navy","title":"navy  <code>module-attribute</code>","text":"<pre><code>navy = Color.from_rgb(0, 0, 128)\n</code></pre> <p>The color navy | navy</p>"},{"location":"api_reference/coloring/#momapy.coloring.oldlace","title":"oldlace  <code>module-attribute</code>","text":"<pre><code>oldlace = Color.from_rgb(253, 245, 230)\n</code></pre> <p>The color oldlace | oldlace</p>"},{"location":"api_reference/coloring/#momapy.coloring.olive","title":"olive  <code>module-attribute</code>","text":"<pre><code>olive = Color.from_rgb(128, 128, 0)\n</code></pre> <p>The color olive | olive</p>"},{"location":"api_reference/coloring/#momapy.coloring.olivedrab","title":"olivedrab  <code>module-attribute</code>","text":"<pre><code>olivedrab = Color.from_rgb(107, 142, 35)\n</code></pre> <p>The color olivedrab | olivedrab</p>"},{"location":"api_reference/coloring/#momapy.coloring.orange","title":"orange  <code>module-attribute</code>","text":"<pre><code>orange = Color.from_rgb(255, 165, 0)\n</code></pre> <p>The color orange | orange</p>"},{"location":"api_reference/coloring/#momapy.coloring.orangered","title":"orangered  <code>module-attribute</code>","text":"<pre><code>orangered = Color.from_rgb(255, 69, 0)\n</code></pre> <p>The color orangered | orangered</p>"},{"location":"api_reference/coloring/#momapy.coloring.orchid","title":"orchid  <code>module-attribute</code>","text":"<pre><code>orchid = Color.from_rgb(218, 112, 214)\n</code></pre> <p>The color orchid | orchid</p>"},{"location":"api_reference/coloring/#momapy.coloring.palegoldenrod","title":"palegoldenrod  <code>module-attribute</code>","text":"<pre><code>palegoldenrod = Color.from_rgb(238, 232, 170)\n</code></pre> <p>The color palegoldenrod | palegoldenrod</p>"},{"location":"api_reference/coloring/#momapy.coloring.palegreen","title":"palegreen  <code>module-attribute</code>","text":"<pre><code>palegreen = Color.from_rgb(152, 251, 152)\n</code></pre> <p>The color palegreen | palegreen</p>"},{"location":"api_reference/coloring/#momapy.coloring.paleturquoise","title":"paleturquoise  <code>module-attribute</code>","text":"<pre><code>paleturquoise = Color.from_rgb(175, 238, 238)\n</code></pre> <p>The color paleturquoise | paleturquoise</p>"},{"location":"api_reference/coloring/#momapy.coloring.palevioletred","title":"palevioletred  <code>module-attribute</code>","text":"<pre><code>palevioletred = Color.from_rgb(219, 112, 147)\n</code></pre> <p>The color palevioletred | palevioletred</p>"},{"location":"api_reference/coloring/#momapy.coloring.papayawhip","title":"papayawhip  <code>module-attribute</code>","text":"<pre><code>papayawhip = Color.from_rgb(255, 239, 213)\n</code></pre> <p>The color papayawhip | papayawhip</p>"},{"location":"api_reference/coloring/#momapy.coloring.peachpuff","title":"peachpuff  <code>module-attribute</code>","text":"<pre><code>peachpuff = Color.from_rgb(255, 218, 185)\n</code></pre> <p>The color peachpuff | peachpuff</p>"},{"location":"api_reference/coloring/#momapy.coloring.peru","title":"peru  <code>module-attribute</code>","text":"<pre><code>peru = Color.from_rgb(205, 133, 63)\n</code></pre> <p>The color peru | peru</p>"},{"location":"api_reference/coloring/#momapy.coloring.pink","title":"pink  <code>module-attribute</code>","text":"<pre><code>pink = Color.from_rgb(255, 192, 203)\n</code></pre> <p>The color pink | pink</p>"},{"location":"api_reference/coloring/#momapy.coloring.plum","title":"plum  <code>module-attribute</code>","text":"<pre><code>plum = Color.from_rgb(221, 160, 221)\n</code></pre> <p>The color plum | plum</p>"},{"location":"api_reference/coloring/#momapy.coloring.powderblue","title":"powderblue  <code>module-attribute</code>","text":"<pre><code>powderblue = Color.from_rgb(176, 224, 230)\n</code></pre> <p>The color powderblue | powderblue</p>"},{"location":"api_reference/coloring/#momapy.coloring.print_colors","title":"print_colors","text":"<pre><code>print_colors() -&gt; None\n</code></pre> <p>Print all available named colors with their names.</p> <p>Displays color names in their actual color in the terminal.</p> <p>Examples:</p> <pre><code>print_colors()  # Prints all colors\n</code></pre> Source code in <code>src/momapy/coloring.py</code> <pre><code>def print_colors() -&gt; None:\n    \"\"\"Print all available named colors with their names.\n\n    Displays color names in their actual color in the terminal.\n\n    Examples:\n        ```python\n        print_colors()  # Prints all colors\n        ```\n    \"\"\"\n    for color_name, color in _NAME_TO_COLOR.items():\n        print(f\"\\x1b[38;2;{color.red};{color.green};{color.blue}m{color_name}\\x1b[0m\")\n</code></pre>"},{"location":"api_reference/coloring/#momapy.coloring.purple","title":"purple  <code>module-attribute</code>","text":"<pre><code>purple = Color.from_rgb(128, 0, 128)\n</code></pre> <p>The color purple | purple</p>"},{"location":"api_reference/coloring/#momapy.coloring.red","title":"red  <code>module-attribute</code>","text":"<pre><code>red = Color.from_rgb(255, 0, 0)\n</code></pre> <p>The color red | red</p>"},{"location":"api_reference/coloring/#momapy.coloring.rosybrown","title":"rosybrown  <code>module-attribute</code>","text":"<pre><code>rosybrown = Color.from_rgb(188, 143, 143)\n</code></pre> <p>The color rosybrown | rosybrown</p>"},{"location":"api_reference/coloring/#momapy.coloring.royalblue","title":"royalblue  <code>module-attribute</code>","text":"<pre><code>royalblue = Color.from_rgb(65, 105, 225)\n</code></pre> <p>The color royalblue | royalblue</p>"},{"location":"api_reference/coloring/#momapy.coloring.saddlebrown","title":"saddlebrown  <code>module-attribute</code>","text":"<pre><code>saddlebrown = Color.from_rgb(139, 69, 19)\n</code></pre> <p>The color saddlebrown | saddlebrown</p>"},{"location":"api_reference/coloring/#momapy.coloring.salmon","title":"salmon  <code>module-attribute</code>","text":"<pre><code>salmon = Color.from_rgb(250, 128, 114)\n</code></pre> <p>The color salmon | salmon</p>"},{"location":"api_reference/coloring/#momapy.coloring.sandybrown","title":"sandybrown  <code>module-attribute</code>","text":"<pre><code>sandybrown = Color.from_rgb(244, 164, 96)\n</code></pre> <p>The color sandybrown | sandybrown</p>"},{"location":"api_reference/coloring/#momapy.coloring.seagreen","title":"seagreen  <code>module-attribute</code>","text":"<pre><code>seagreen = Color.from_rgb(46, 139, 87)\n</code></pre> <p>The color seagreen | seagreen</p>"},{"location":"api_reference/coloring/#momapy.coloring.seashell","title":"seashell  <code>module-attribute</code>","text":"<pre><code>seashell = Color.from_rgb(255, 245, 238)\n</code></pre> <p>The color seashell | seashell</p>"},{"location":"api_reference/coloring/#momapy.coloring.sienna","title":"sienna  <code>module-attribute</code>","text":"<pre><code>sienna = Color.from_rgb(160, 82, 45)\n</code></pre> <p>The color sienna | sienna</p>"},{"location":"api_reference/coloring/#momapy.coloring.silver","title":"silver  <code>module-attribute</code>","text":"<pre><code>silver = Color.from_rgb(192, 192, 192)\n</code></pre> <p>The color silver | silver</p>"},{"location":"api_reference/coloring/#momapy.coloring.skyblue","title":"skyblue  <code>module-attribute</code>","text":"<pre><code>skyblue = Color.from_rgb(135, 206, 235)\n</code></pre> <p>The color skyblue | skyblue</p>"},{"location":"api_reference/coloring/#momapy.coloring.slateblue","title":"slateblue  <code>module-attribute</code>","text":"<pre><code>slateblue = Color.from_rgb(106, 90, 205)\n</code></pre> <p>The color slateblue | slateblue</p>"},{"location":"api_reference/coloring/#momapy.coloring.slategray","title":"slategray  <code>module-attribute</code>","text":"<pre><code>slategray = Color.from_rgb(112, 128, 144)\n</code></pre> <p>The color slategray | slategray</p>"},{"location":"api_reference/coloring/#momapy.coloring.snow","title":"snow  <code>module-attribute</code>","text":"<pre><code>snow = Color.from_rgb(255, 250, 250)\n</code></pre> <p>The color snow | snow</p>"},{"location":"api_reference/coloring/#momapy.coloring.springgreen","title":"springgreen  <code>module-attribute</code>","text":"<pre><code>springgreen = Color.from_rgb(0, 255, 127)\n</code></pre> <p>The color springgreen | springgreen</p>"},{"location":"api_reference/coloring/#momapy.coloring.steelblue","title":"steelblue  <code>module-attribute</code>","text":"<pre><code>steelblue = Color.from_rgb(70, 130, 180)\n</code></pre> <p>The color steelblue | steelblue</p>"},{"location":"api_reference/coloring/#momapy.coloring.tan","title":"tan  <code>module-attribute</code>","text":"<pre><code>tan = Color.from_rgb(210, 180, 140)\n</code></pre> <p>The color tan | tan</p>"},{"location":"api_reference/coloring/#momapy.coloring.teal","title":"teal  <code>module-attribute</code>","text":"<pre><code>teal = Color.from_rgb(0, 128, 128)\n</code></pre> <p>The color teal | teal</p>"},{"location":"api_reference/coloring/#momapy.coloring.thistle","title":"thistle  <code>module-attribute</code>","text":"<pre><code>thistle = Color.from_rgb(216, 191, 216)\n</code></pre> <p>The color thistle | thistle</p>"},{"location":"api_reference/coloring/#momapy.coloring.tomato","title":"tomato  <code>module-attribute</code>","text":"<pre><code>tomato = Color.from_rgb(255, 99, 71)\n</code></pre> <p>The color tomato | tomato</p>"},{"location":"api_reference/coloring/#momapy.coloring.turquoise","title":"turquoise  <code>module-attribute</code>","text":"<pre><code>turquoise = Color.from_rgb(64, 224, 208)\n</code></pre> <p>The color turquoise | turquoise</p>"},{"location":"api_reference/coloring/#momapy.coloring.violet","title":"violet  <code>module-attribute</code>","text":"<pre><code>violet = Color.from_rgb(238, 130, 238)\n</code></pre> <p>The color violet | violet</p>"},{"location":"api_reference/coloring/#momapy.coloring.wheat","title":"wheat  <code>module-attribute</code>","text":"<pre><code>wheat = Color.from_rgb(245, 222, 179)\n</code></pre> <p>The color wheat | wheat</p>"},{"location":"api_reference/coloring/#momapy.coloring.white","title":"white  <code>module-attribute</code>","text":"<pre><code>white = Color.from_rgb(255, 255, 255)\n</code></pre> <p>The color white | white</p>"},{"location":"api_reference/coloring/#momapy.coloring.whitesmoke","title":"whitesmoke  <code>module-attribute</code>","text":"<pre><code>whitesmoke = Color.from_rgb(245, 245, 245)\n</code></pre> <p>The color whitesmoke | whitesmoke</p>"},{"location":"api_reference/coloring/#momapy.coloring.yellow","title":"yellow  <code>module-attribute</code>","text":"<pre><code>yellow = Color.from_rgb(255, 255, 0)\n</code></pre> <p>The color yellow | yellow</p>"},{"location":"api_reference/coloring/#momapy.coloring.yellowgreen","title":"yellowgreen  <code>module-attribute</code>","text":"<pre><code>yellowgreen = Color.from_rgb(154, 205, 50)\n</code></pre> <p>The color yellowgreen | yellowgreen</p>"},{"location":"api_reference/core/","title":"Core","text":""},{"location":"api_reference/core/#momapy.core","title":"momapy.core","text":"<p>Core dataclasses for maps, models, and layouts.</p> <p>Modules:</p> Name Description <code>elements</code> <p>Base element classes for maps.</p> <code>fonts</code> <p>Font file lookup using uharfbuzz for metadata reading.</p> <code>layout</code> <p>Layout element hierarchy: all visual element classes.</p> <code>map</code> <p>Top-level Map class.</p> <code>mapping</code> <p>Layout-model mapping classes.</p> <code>model</code> <p>Abstract model base class.</p> <p>Classes:</p> Name Description <code>Arc</code> <p>Base class for arcs.</p> <code>Direction</code> <p>Cardinal direction a layout element points to.</p> <code>DoubleHeadedArc</code> <p>Base class for double-headed arcs.</p> <code>GroupLayout</code> <p>Base class for group layouts.</p> <code>HAlignment</code> <p>Horizontal alignment of text or content.</p> <code>Layout</code> <p>Class for layouts.</p> <code>LayoutElement</code> <p>Abstract base class for layout elements.</p> <code>LayoutModelMapping</code> <p>Mapping between model elements and layout elements.</p> <code>LayoutModelMappingBuilder</code> <p>Mutable builder for LayoutModelMapping.</p> <code>Map</code> <p>Class for maps.</p> <code>MapElement</code> <p>Base class for map elements.</p> <code>Model</code> <p>Abstract base class for models.</p> <code>ModelElement</code> <p>Base class for model elements.</p> <code>Node</code> <p>Class for nodes.</p> <code>Orientation</code> <p>Orientation along an axis.</p> <code>Shape</code> <p>Class for basic shapes.</p> <code>SingleHeadedArc</code> <p>Base class for single-headed arcs.</p> <code>TextLayout</code> <p>Class for text layouts.</p> <code>VAlignment</code> <p>Vertical alignment of text or content.</p>"},{"location":"api_reference/core/#momapy.core.Arc","title":"Arc  <code>dataclass</code>","text":"<pre><code>Arc(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, end_shorten: float = 0.0, fill: NoneValueType | Color | None = None, filter_: NoneValueType | Filter | None = None, path_fill: NoneValueType | Color | None = None, path_filter: NoneValueType | Filter | None = None, path_stroke: NoneValueType | Color | None = None, path_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, path_stroke_dashoffset: NoneValueType | float | None = None, path_stroke_width: float | None = None, path_transform: NoneValueType | tuple[Transformation, ...] | None = None, stroke: NoneValueType | Color | None = None, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: NoneValueType | float | None = None, segments: tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...] = tuple(), source: LayoutElement | None = None, start_shorten: float = 0.0, target: LayoutElement | None = None, transform: NoneValueType | tuple[Transformation, ...] | None = None)\n</code></pre> <p>               Bases: <code>GroupLayout</code></p> <p>Base class for arcs.</p> <p>An arc is a group layout drawn as a path connecting a source to a target, with its own path styling.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'4dcfcd54-1f0f-45d6-88e2-84b58402580a'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>end_shorten</code> <code>float</code> <p>The length the end of the arc will be shorten by</p> <code>0.0</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the arc</p> <code>None</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the arc</p> <code>None</code> <code>path_fill</code> <code>NoneValueType | Color | None</code> <p>The path fill color of the arc</p> <code>None</code> <code>path_filter</code> <code>NoneValueType | Filter | None</code> <p>The path filter of the arc</p> <code>None</code> <code>path_stroke</code> <code>NoneValueType | Color | None</code> <p>The path stroke color of the arc</p> <code>None</code> <code>path_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The path stroke dasharray of the arc</p> <code>None</code> <code>path_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The path stroke dashoffset of the arc</p> <code>None</code> <code>path_stroke_width</code> <code>float | None</code> <p>The path stroke width of the arc</p> <code>None</code> <code>path_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The path transform of the arc</p> <code>None</code> <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the arc</p> <code>None</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the arc</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the arc</p> <code>None</code> <code>stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the arc</p> <code>None</code> <code>segments</code> <code>tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...]</code> <p>The path segments of the arc</p> <code>&lt;dynamic&gt;</code> <code>source</code> <code>LayoutElement | None</code> <p>The source of the arc</p> <code>None</code> <code>start_shorten</code> <code>float</code> <p>The length the start of the arc will be shorten by</p> <code>0.0</code> <code>target</code> <code>LayoutElement | None</code> <p>The target of the arc</p> <code>None</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the arc</p> <code>None</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>childless</code> <p>Return a copy of the arc with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>end_point</code> <p>Return the ending point of the arc.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>fraction</code> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <code>length</code> <p>Return the total length of the arc path.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_children</code> <p>Return the self children of the arc.</p> <code>own_drawing_elements</code> <p>Return the self drawing elements of the group layout.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>points</code> <p>Return the points of the arc path.</p> <code>start_point</code> <p>Return the starting point of the arc.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p>"},{"location":"api_reference/core/#momapy.core.Arc.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Arc.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Arc.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the arc with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the arc with no children.\"\"\"\n    return dataclasses.replace(self, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Arc.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Arc.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Arc.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Arc.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Arc.end_point","title":"end_point","text":"<pre><code>end_point() -&gt; Point\n</code></pre> <p>Return the ending point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_point(self) -&gt; Point:\n    \"\"\"Return the ending point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[-1]\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Arc.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Arc.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Arc.fraction","title":"fraction","text":"<pre><code>fraction(fraction: float) -&gt; tuple[Point, float]\n</code></pre> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def fraction(self, fraction: float) -&gt; tuple[Point, float]:\n    \"\"\"Return the position and angle on the arc at a given fraction (of the total arc length).\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    current_length = 0\n    length_to_reach = fraction * self.length()\n    for segment in self.segments:\n        current_length += segment.length()\n        if current_length &gt;= length_to_reach:\n            break\n    segment_start_length = current_length - segment.length()\n    segment_fraction = (length_to_reach - segment_start_length) / segment.length()\n    position, angle = segment.get_position_and_angle_at_fraction(segment_fraction)\n    return position, angle\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Arc.length","title":"length","text":"<pre><code>length() -&gt; float\n</code></pre> <p>Return the total length of the arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def length(self) -&gt; float:\n    \"\"\"Return the total length of the arc path.\"\"\"\n    return sum([segment.length() for segment in self.segments])\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Arc.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Arc.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the arc.\"\"\"\n    return []\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Arc.own_drawing_elements","title":"own_drawing_elements  <code>abstractmethod</code>","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the self drawing elements of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>@abc.abstractmethod\ndef own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the self drawing elements of the group layout.\"\"\"\n    pass\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Arc.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Arc.points","title":"points","text":"<pre><code>points() -&gt; list[Point]\n</code></pre> <p>Return the points of the arc path.</p> <p>An arc with no segments has no points and returns an empty list.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def points(self) -&gt; list[Point]:\n    \"\"\"Return the points of the arc path.\n\n    An arc with no segments has no points and returns an empty list.\n    \"\"\"\n    if not self.segments:\n        return []\n    points = []\n    for segment in self.segments:\n        points.append(segment.p1)\n    points.append(segment.p2)\n    return points\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Arc.start_point","title":"start_point","text":"<pre><code>start_point() -&gt; Point\n</code></pre> <p>Return the starting point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_point(self) -&gt; Point:\n    \"\"\"Return the starting point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[0]\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Arc.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Direction","title":"Direction","text":"<p>               Bases: <code>Enum</code></p> <p>Cardinal direction a layout element points to.</p> <p>One of the four cardinals (up, right, down, left). For an axis (horizontal or vertical), see <code>Orientation</code>.</p>"},{"location":"api_reference/core/#momapy.core.DoubleHeadedArc","title":"DoubleHeadedArc  <code>dataclass</code>","text":"<pre><code>DoubleHeadedArc(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, end_shorten: float = 0.0, fill: NoneValueType | Color | None = None, filter_: NoneValueType | Filter | None = None, path_fill: NoneValueType | Color | None = None, path_filter: NoneValueType | Filter | None = None, path_stroke: NoneValueType | Color | None = None, path_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, path_stroke_dashoffset: NoneValueType | float | None = None, path_stroke_width: float | None = None, path_transform: NoneValueType | tuple[Transformation, ...] | None = None, stroke: NoneValueType | Color | None = None, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: NoneValueType | float | None = None, segments: tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...] = tuple(), source: LayoutElement | None = None, start_shorten: float = 0.0, target: LayoutElement | None = None, transform: NoneValueType | tuple[Transformation, ...] | None = None, end_arrowhead_fill: NoneValueType | Color | None = None, end_arrowhead_filter: NoneValueType | Filter | None = None, end_arrowhead_stroke: NoneValueType | Color | None = None, end_arrowhead_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, end_arrowhead_stroke_dashoffset: NoneValueType | float | None = None, end_arrowhead_stroke_width: NoneValueType | float | None = None, end_arrowhead_transform: NoneValueType | tuple[Transformation, ...] | None = None, start_arrowhead_fill: NoneValueType | Color | None = None, start_arrowhead_filter: NoneValueType | Filter | None = None, start_arrowhead_stroke: NoneValueType | Color | None = None, start_arrowhead_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, start_arrowhead_stroke_dashoffset: NoneValueType | float | None = None, start_arrowhead_stroke_width: NoneValueType | float | None = None, start_arrowhead_transform: NoneValueType | tuple[Transformation, ...] | None = None)\n</code></pre> <p>               Bases: <code>Arc</code></p> <p>Base class for double-headed arcs.</p> <p>A double-headed arc is formed of a path and two arrowheads, one at the start of the path and one at its end.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'4cb1088d-954b-4516-8bad-ebd4cf73cf9e'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>end_shorten</code> <code>float</code> <p>The length the end of the arc will be shorten by</p> <code>0.0</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the arc</p> <code>None</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the arc</p> <code>None</code> <code>path_fill</code> <code>NoneValueType | Color | None</code> <p>The path fill color of the arc</p> <code>None</code> <code>path_filter</code> <code>NoneValueType | Filter | None</code> <p>The path filter of the arc</p> <code>None</code> <code>path_stroke</code> <code>NoneValueType | Color | None</code> <p>The path stroke color of the arc</p> <code>None</code> <code>path_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The path stroke dasharray of the arc</p> <code>None</code> <code>path_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The path stroke dashoffset of the arc</p> <code>None</code> <code>path_stroke_width</code> <code>float | None</code> <p>The path stroke width of the arc</p> <code>None</code> <code>path_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The path transform of the arc</p> <code>None</code> <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the arc</p> <code>None</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the arc</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the arc</p> <code>None</code> <code>stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the arc</p> <code>None</code> <code>segments</code> <code>tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...]</code> <p>The path segments of the arc</p> <code>&lt;dynamic&gt;</code> <code>source</code> <code>LayoutElement | None</code> <p>The source of the arc</p> <code>None</code> <code>start_shorten</code> <code>float</code> <p>The length the start of the arc will be shorten by</p> <code>0.0</code> <code>target</code> <code>LayoutElement | None</code> <p>The target of the arc</p> <code>None</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the arc</p> <code>None</code> <code>end_arrowhead_fill</code> <code>NoneValueType | Color | None</code> <p>The end arrowhead fill color of the arc</p> <code>None</code> <code>end_arrowhead_filter</code> <code>NoneValueType | Filter | None</code> <p>The end arrowhead filter of the arc</p> <code>None</code> <code>end_arrowhead_stroke</code> <code>NoneValueType | Color | None</code> <p>The end arrowhead stroke color of the arc</p> <code>None</code> <code>end_arrowhead_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The end arrowhead stroke dasharray of the arc</p> <code>None</code> <code>end_arrowhead_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The end arrowhead stroke dashoffset of the arc</p> <code>None</code> <code>end_arrowhead_stroke_width</code> <code>NoneValueType | float | None</code> <p>The end arrowhead stroke width of the arc</p> <code>None</code> <code>end_arrowhead_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The end arrowhead transform of the arc</p> <code>None</code> <code>start_arrowhead_fill</code> <code>NoneValueType | Color | None</code> <p>The start arrowhead fill color of the arc</p> <code>None</code> <code>start_arrowhead_filter</code> <code>NoneValueType | Filter | None</code> <p>The start arrowhead filter of the arc</p> <code>None</code> <code>start_arrowhead_stroke</code> <code>NoneValueType | Color | None</code> <p>The start arrowhead stroke color of the arc</p> <code>None</code> <code>start_arrowhead_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The start arrowhead stroke dasharray of the arc</p> <code>None</code> <code>start_arrowhead_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The start arrowhead stroke dashoffset of the arc</p> <code>None</code> <code>start_arrowhead_stroke_width</code> <code>NoneValueType | float | None</code> <p>The start arrowhead stroke width of the arc</p> <code>None</code> <code>start_arrowhead_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The start arrowhead transform of the arc</p> <code>None</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>childless</code> <p>Return a copy of the arc with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>end_arrowhead_base</code> <p>Return the base anchor point of the double-headed arc end arrowhead.</p> <code>end_arrowhead_bbox</code> <p>Return the bounding box of the double-headed arc end arrowhead.</p> <code>end_arrowhead_border</code> <p>Return the point at the intersection of the drawing elements of the double-headed arc end arrowhead and the line going through the center of these drawing elements and the given point.</p> <code>end_arrowhead_drawing_elements</code> <p>Return the drawing elements of the double-headed arc end arrowhead.</p> <code>end_arrowhead_length</code> <p>Return the length of the double-headed arc end arrowhead.</p> <code>end_arrowhead_tip</code> <p>Return the tip anchor point of the double-headed arc end arrowhead.</p> <code>end_point</code> <p>Return the ending point of the arc.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>fraction</code> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <code>length</code> <p>Return the total length of the arc path.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_children</code> <p>Return the self children of the arc.</p> <code>own_drawing_elements</code> <p>Return the self drawing elements of the double-headed arc. These include the drawing elements of the arc path, the start arrowhead, and the end arrowhead.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>path_drawing_elements</code> <p>Return the drawing elements of the double-headed arc path.</p> <code>points</code> <p>Return the points of the arc path.</p> <code>start_arrowhead_base</code> <p>Return the base anchor point of the double-headed arc start arrowhead.</p> <code>start_arrowhead_bbox</code> <p>Return the bounding box of the double-headed arc start arrowhead.</p> <code>start_arrowhead_border</code> <p>Return the point at the intersection of the drawing elements of the double-headed arc start arrowhead and the line going through the center of these drawing elements and the given point.</p> <code>start_arrowhead_drawing_elements</code> <p>Return the drawing elements of the double-headed arc start arrowhead.</p> <code>start_arrowhead_length</code> <p>Return the length of the double-headed arc start arrowhead.</p> <code>start_arrowhead_tip</code> <p>Return the tip anchor point of the double-headed arc start arrowhead.</p> <code>start_point</code> <p>Return the starting point of the arc.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p>"},{"location":"api_reference/core/#momapy.core.DoubleHeadedArc.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/core/#momapy.core.DoubleHeadedArc.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/core/#momapy.core.DoubleHeadedArc.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the arc with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the arc with no children.\"\"\"\n    return dataclasses.replace(self, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/core/#momapy.core.DoubleHeadedArc.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/core/#momapy.core.DoubleHeadedArc.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/core/#momapy.core.DoubleHeadedArc.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/core/#momapy.core.DoubleHeadedArc.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/core/#momapy.core.DoubleHeadedArc.end_arrowhead_base","title":"end_arrowhead_base","text":"<pre><code>end_arrowhead_base() -&gt; Point\n</code></pre> <p>Return the base anchor point of the double-headed arc end arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_arrowhead_base(self) -&gt; Point:\n    \"\"\"Return the base anchor point of the double-headed arc end arrowhead.\"\"\"\n    arrowhead_length = self.end_arrowhead_length()\n    if arrowhead_length == 0:\n        return self.end_point()\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return self.end_arrowhead_tip() - (arrowhead_length, 0)\n    fraction = 1 - (arrowhead_length + self.end_shorten) / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/core/#momapy.core.DoubleHeadedArc.end_arrowhead_bbox","title":"end_arrowhead_bbox","text":"<pre><code>end_arrowhead_bbox() -&gt; Bbox\n</code></pre> <p>Return the bounding box of the double-headed arc end arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_arrowhead_bbox(self) -&gt; Bbox:\n    \"\"\"Return the bounding box of the double-headed arc end arrowhead.\"\"\"\n    return get_drawing_elements_bbox(self.end_arrowhead_drawing_elements())\n</code></pre>"},{"location":"api_reference/core/#momapy.core.DoubleHeadedArc.end_arrowhead_border","title":"end_arrowhead_border","text":"<pre><code>end_arrowhead_border(point: Point) -&gt; Point\n</code></pre> <p>Return the point at the intersection of the drawing elements of the double-headed arc end arrowhead and the line going through the center of these drawing elements and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_arrowhead_border(self, point: Point) -&gt; Point:\n    \"\"\"Return the point at the intersection of the drawing elements of the double-headed arc end arrowhead and the line going through the center of these drawing elements and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    point = get_drawing_elements_border(\n        self.end_arrowhead_drawing_elements(), point\n    )\n    if point is None:\n        return self.end_arrowhead_tip()\n    return point\n</code></pre>"},{"location":"api_reference/core/#momapy.core.DoubleHeadedArc.end_arrowhead_drawing_elements","title":"end_arrowhead_drawing_elements","text":"<pre><code>end_arrowhead_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the double-headed arc end arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_arrowhead_drawing_elements(\n    self,\n) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the double-headed arc end arrowhead.\"\"\"\n    drawing_elements = self._end_arrowhead_border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_end_arrowhead\",\n        elements=tuple(drawing_elements),\n        fill=self.end_arrowhead_fill,\n        filter_=self.end_arrowhead_filter,\n        id_=f\"{self.id_}_end_arrowhead\",\n        stroke=self.end_arrowhead_stroke,\n        stroke_width=self.end_arrowhead_stroke_width,\n        stroke_dasharray=self.end_arrowhead_stroke_dasharray,\n        stroke_dashoffset=self.end_arrowhead_stroke_dashoffset,\n        transform=self.end_arrowhead_transform,\n    )\n    transformation = self._get_end_arrowhead_transformation()\n    group = group.transformed(transformation)\n    return [group]\n</code></pre>"},{"location":"api_reference/core/#momapy.core.DoubleHeadedArc.end_arrowhead_length","title":"end_arrowhead_length","text":"<pre><code>end_arrowhead_length() -&gt; float\n</code></pre> <p>Return the length of the double-headed arc end arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_arrowhead_length(self) -&gt; float:\n    \"\"\"Return the length of the double-headed arc end arrowhead.\"\"\"\n    bbox = get_drawing_elements_bbox(self._end_arrowhead_border_drawing_elements())\n    if math.isnan(bbox.width):\n        return 0.0\n    return bbox.east().x\n</code></pre>"},{"location":"api_reference/core/#momapy.core.DoubleHeadedArc.end_arrowhead_tip","title":"end_arrowhead_tip","text":"<pre><code>end_arrowhead_tip() -&gt; Point\n</code></pre> <p>Return the tip anchor point of the double-headed arc end arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_arrowhead_tip(self) -&gt; Point:\n    \"\"\"Return the tip anchor point of the double-headed arc end arrowhead.\"\"\"\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return segment.p2\n    fraction = 1 - self.end_shorten / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/core/#momapy.core.DoubleHeadedArc.end_point","title":"end_point","text":"<pre><code>end_point() -&gt; Point\n</code></pre> <p>Return the ending point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_point(self) -&gt; Point:\n    \"\"\"Return the ending point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[-1]\n</code></pre>"},{"location":"api_reference/core/#momapy.core.DoubleHeadedArc.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/core/#momapy.core.DoubleHeadedArc.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/core/#momapy.core.DoubleHeadedArc.fraction","title":"fraction","text":"<pre><code>fraction(fraction: float) -&gt; tuple[Point, float]\n</code></pre> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def fraction(self, fraction: float) -&gt; tuple[Point, float]:\n    \"\"\"Return the position and angle on the arc at a given fraction (of the total arc length).\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    current_length = 0\n    length_to_reach = fraction * self.length()\n    for segment in self.segments:\n        current_length += segment.length()\n        if current_length &gt;= length_to_reach:\n            break\n    segment_start_length = current_length - segment.length()\n    segment_fraction = (length_to_reach - segment_start_length) / segment.length()\n    position, angle = segment.get_position_and_angle_at_fraction(segment_fraction)\n    return position, angle\n</code></pre>"},{"location":"api_reference/core/#momapy.core.DoubleHeadedArc.length","title":"length","text":"<pre><code>length() -&gt; float\n</code></pre> <p>Return the total length of the arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def length(self) -&gt; float:\n    \"\"\"Return the total length of the arc path.\"\"\"\n    return sum([segment.length() for segment in self.segments])\n</code></pre>"},{"location":"api_reference/core/#momapy.core.DoubleHeadedArc.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/core/#momapy.core.DoubleHeadedArc.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the arc.\"\"\"\n    return []\n</code></pre>"},{"location":"api_reference/core/#momapy.core.DoubleHeadedArc.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the self drawing elements of the double-headed arc. These include the drawing elements of the arc path, the start arrowhead, and the end arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the self drawing elements of the double-headed arc. These include the drawing elements of the arc path, the start arrowhead, and the end arrowhead.\"\"\"\n    drawing_elements = (\n        self.path_drawing_elements()\n        + self.start_arrowhead_drawing_elements()\n        + self.end_arrowhead_drawing_elements()\n    )\n\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}_own\",\n        fill=self.fill,\n        filter_=self.filter_,\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/core/#momapy.core.DoubleHeadedArc.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/core/#momapy.core.DoubleHeadedArc.path_drawing_elements","title":"path_drawing_elements","text":"<pre><code>path_drawing_elements() -&gt; list[Path]\n</code></pre> <p>Return the drawing elements of the double-headed arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def path_drawing_elements(self) -&gt; list[Path]:\n    \"\"\"Return the drawing elements of the double-headed arc path.\"\"\"\n    start_arrowhead_length = self.start_arrowhead_length()\n    end_arrowhead_length = self.end_arrowhead_length()\n    if len(self.segments) == 1:\n        segment = (\n            self.segments[0]\n            .shortened(self.start_shorten + start_arrowhead_length, \"start\")\n            .shortened(self.end_shorten + end_arrowhead_length, \"end\")\n        )\n        actions = [\n            MoveTo(segment.p1),\n            self._make_path_action_from_segment(segment),\n        ]\n    else:\n        first_segment = self.segments[0].shortened(\n            self.start_shorten + start_arrowhead_length, \"start\"\n        )\n        last_segment = self.segments[-1].shortened(\n            self.end_shorten + end_arrowhead_length, \"end\"\n        )\n        actions = [\n            MoveTo(first_segment.p1),\n            self._make_path_action_from_segment(first_segment),\n        ]\n        for segment in self.segments[1:-1]:\n            action = self._make_path_action_from_segment(segment)\n            actions.append(action)\n        actions.append(self._make_path_action_from_segment(last_segment))\n    path = Path(\n        actions=tuple(actions),\n        class_=f\"{type(self).__name__}_path\",\n        fill=self.path_fill,\n        filter_=self.path_filter,\n        id_=f\"{self.id_}_path\",\n        stroke=self.path_stroke,\n        stroke_dasharray=self.path_stroke_dasharray,\n        stroke_dashoffset=self.path_stroke_dashoffset,\n        stroke_width=self.path_stroke_width,\n        transform=self.path_transform,\n    )\n    return [path]\n</code></pre>"},{"location":"api_reference/core/#momapy.core.DoubleHeadedArc.points","title":"points","text":"<pre><code>points() -&gt; list[Point]\n</code></pre> <p>Return the points of the arc path.</p> <p>An arc with no segments has no points and returns an empty list.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def points(self) -&gt; list[Point]:\n    \"\"\"Return the points of the arc path.\n\n    An arc with no segments has no points and returns an empty list.\n    \"\"\"\n    if not self.segments:\n        return []\n    points = []\n    for segment in self.segments:\n        points.append(segment.p1)\n    points.append(segment.p2)\n    return points\n</code></pre>"},{"location":"api_reference/core/#momapy.core.DoubleHeadedArc.start_arrowhead_base","title":"start_arrowhead_base","text":"<pre><code>start_arrowhead_base() -&gt; Point\n</code></pre> <p>Return the base anchor point of the double-headed arc start arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_arrowhead_base(self) -&gt; Point:\n    \"\"\"Return the base anchor point of the double-headed arc start arrowhead.\"\"\"\n    arrowhead_length = self.start_arrowhead_length()\n    if arrowhead_length == 0:\n        return self.start_point()\n    segment = self.segments[0]\n    segment = Segment(segment.p2, segment.p1)\n    segment_length = segment.length()\n    if segment_length == 0:\n        return self.start_arrowhead_tip() + (arrowhead_length, 0)\n    fraction = 1 - (arrowhead_length + self.start_shorten) / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/core/#momapy.core.DoubleHeadedArc.start_arrowhead_bbox","title":"start_arrowhead_bbox","text":"<pre><code>start_arrowhead_bbox() -&gt; Bbox\n</code></pre> <p>Return the bounding box of the double-headed arc start arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_arrowhead_bbox(self) -&gt; Bbox:\n    \"\"\"Return the bounding box of the double-headed arc start arrowhead.\"\"\"\n    return get_drawing_elements_bbox(self.start_arrowhead_drawing_elements())\n</code></pre>"},{"location":"api_reference/core/#momapy.core.DoubleHeadedArc.start_arrowhead_border","title":"start_arrowhead_border","text":"<pre><code>start_arrowhead_border(point: Point) -&gt; Point\n</code></pre> <p>Return the point at the intersection of the drawing elements of the double-headed arc start arrowhead and the line going through the center of these drawing elements and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_arrowhead_border(self, point: Point) -&gt; Point:\n    \"\"\"Return the point at the intersection of the drawing elements of the double-headed arc start arrowhead and the line going through the center of these drawing elements and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    point = get_drawing_elements_border(\n        self.start_arrowhead_drawing_elements(), point\n    )\n    if point is None:\n        return self.start_arrowhead_tip()\n    return point\n</code></pre>"},{"location":"api_reference/core/#momapy.core.DoubleHeadedArc.start_arrowhead_drawing_elements","title":"start_arrowhead_drawing_elements","text":"<pre><code>start_arrowhead_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the double-headed arc start arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_arrowhead_drawing_elements(\n    self,\n) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the double-headed arc start arrowhead.\"\"\"\n    drawing_elements = self._start_arrowhead_border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_start_arrowhead\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}_start_arrowhead\",\n        fill=self.start_arrowhead_fill,\n        filter_=self.start_arrowhead_filter,\n        stroke=self.start_arrowhead_stroke,\n        stroke_dasharray=self.start_arrowhead_stroke_dasharray,\n        stroke_dashoffset=self.start_arrowhead_stroke_dashoffset,\n        stroke_width=self.start_arrowhead_stroke_width,\n        transform=self.start_arrowhead_transform,\n    )\n    transformation = self._get_start_arrowhead_transformation()\n    group = group.transformed(transformation)\n    return [group]\n</code></pre>"},{"location":"api_reference/core/#momapy.core.DoubleHeadedArc.start_arrowhead_length","title":"start_arrowhead_length","text":"<pre><code>start_arrowhead_length() -&gt; float\n</code></pre> <p>Return the length of the double-headed arc start arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_arrowhead_length(self) -&gt; float:\n    \"\"\"Return the length of the double-headed arc start arrowhead.\"\"\"\n    bbox = get_drawing_elements_bbox(\n        self._start_arrowhead_border_drawing_elements()\n    )\n    if math.isnan(bbox.width):\n        return 0.0\n    return abs(bbox.west().x)\n</code></pre>"},{"location":"api_reference/core/#momapy.core.DoubleHeadedArc.start_arrowhead_tip","title":"start_arrowhead_tip","text":"<pre><code>start_arrowhead_tip() -&gt; Point\n</code></pre> <p>Return the tip anchor point of the double-headed arc start arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_arrowhead_tip(self) -&gt; Point:\n    \"\"\"Return the tip anchor point of the double-headed arc start arrowhead.\"\"\"\n    segment = self.segments[0]\n    segment = Segment(segment.p2, segment.p1)\n    segment_length = segment.length()\n    if segment_length == 0:\n        return segment.p2\n    fraction = 1 - self.start_shorten / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/core/#momapy.core.DoubleHeadedArc.start_point","title":"start_point","text":"<pre><code>start_point() -&gt; Point\n</code></pre> <p>Return the starting point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_point(self) -&gt; Point:\n    \"\"\"Return the starting point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[0]\n</code></pre>"},{"location":"api_reference/core/#momapy.core.DoubleHeadedArc.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/core/#momapy.core.GroupLayout","title":"GroupLayout  <code>dataclass</code>","text":"<pre><code>GroupLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None)\n</code></pre> <p>               Bases: <code>LayoutElement</code></p> <p>Base class for group layouts.</p> <p>A group layout is a layout element grouping other layout elements, with its own drawing elements and children combined into a single group drawing element.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'7e999e52-ef44-4018-89c8-7afe452f6aba'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>childless</code> <p>Return a copy of the layout element with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_children</code> <p>Return the self children of the group layout.</p> <code>own_drawing_elements</code> <p>Return the self drawing elements of the group layout.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p>"},{"location":"api_reference/core/#momapy.core.GroupLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/core/#momapy.core.GroupLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/core/#momapy.core.GroupLayout.childless","title":"childless  <code>abstractmethod</code>","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the layout element with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>@abc.abstractmethod\ndef childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the layout element with no children.\"\"\"\n    pass\n</code></pre>"},{"location":"api_reference/core/#momapy.core.GroupLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/core/#momapy.core.GroupLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/core/#momapy.core.GroupLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/core/#momapy.core.GroupLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/core/#momapy.core.GroupLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/core/#momapy.core.GroupLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/core/#momapy.core.GroupLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/core/#momapy.core.GroupLayout.own_children","title":"own_children  <code>abstractmethod</code>","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>@abc.abstractmethod\ndef own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the group layout.\"\"\"\n    pass\n</code></pre>"},{"location":"api_reference/core/#momapy.core.GroupLayout.own_drawing_elements","title":"own_drawing_elements  <code>abstractmethod</code>","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the self drawing elements of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>@abc.abstractmethod\ndef own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the self drawing elements of the group layout.\"\"\"\n    pass\n</code></pre>"},{"location":"api_reference/core/#momapy.core.GroupLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/core/#momapy.core.GroupLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/core/#momapy.core.HAlignment","title":"HAlignment","text":"<p>               Bases: <code>Enum</code></p> <p>Horizontal alignment of text or content.</p> <p>Selects how content is positioned along the horizontal axis.</p>"},{"location":"api_reference/core/#momapy.core.Layout","title":"Layout  <code>dataclass</code>","text":"<pre><code>Layout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = NoneValue, filter_: NoneValueType | Filter | None = None, height: float, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = None, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: NoneValueType | float | None = None, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float)\n</code></pre> <p>               Bases: <code>Node</code></p> <p>Class for layouts.</p> <p>A layout is the root node holding all the visual elements of a map.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'81bc5fd9-54af-42b0-9708-afae1d575b0b'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>&lt;momapy.drawing.NoneValueType object at 0x7f8b17323e00&gt;</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> required <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>None</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the node</p> <code>None</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> required <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>angle</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Return the east anchor of the node.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>is_sublayout</code> <p>Return <code>True</code> if the layout is a sublayout of another given layout, <code>False</code> otherwise.</p> <code>label_center</code> <p>Return the label center anchor of the node.</p> <code>north</code> <p>Return the north anchor of the node.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Return the south anchor of the node.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Return the west anchor of the node.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/core/#momapy.core.Layout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Layout.angle","title":"angle","text":"<pre><code>angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Layout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Layout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Layout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Layout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Layout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Layout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Layout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Layout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Layout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Return the east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Return the east anchor of the node.\"\"\"\n    return self.own_angle(0)\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Layout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Layout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Layout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Layout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Layout.is_sublayout","title":"is_sublayout","text":"<pre><code>is_sublayout(other: Layout, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout is a sublayout of another given layout, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def is_sublayout(\n    self, other: \"Layout\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout is a sublayout of another given layout, `False` otherwise.\"\"\"\n\n    def _is_sublist(\n        list1: list[LayoutElement],\n        list2: list[LayoutElement],\n        unordered: bool = False,\n    ) -&gt; bool:\n        if not unordered:\n            i = 0\n            for elem1 in list1:\n                elem2 = list2[i]\n                while elem2 != elem1 and i &lt; len(list2) - 1:\n                    i += 1\n                    elem2 = list2[i]\n                if not elem2 == elem1:\n                    return False\n                i += 1\n        else:\n            dlist1 = collections.defaultdict(int)\n            dlist2 = collections.defaultdict(int)\n            for elem1 in list1:\n                dlist1[elem1] += 1\n            for elem2 in list2:\n                dlist2[elem2] += 1\n            for elem in dlist1:\n                if dlist1[elem] &gt; dlist2[elem]:\n                    return False\n        return True\n\n    if self.childless() != other.childless():\n        return False\n    if flattened:\n        return _is_sublist(\n            self.flattened()[1:],\n            other.flattened()[1:],\n            unordered=unordered,\n        )\n    return _is_sublist(self.children(), other.children(), unordered=unordered)\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Layout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Return the label center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Return the label center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Layout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Return the north anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Return the north anchor of the node.\"\"\"\n    return self.own_angle(90)\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Layout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Layout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Layout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Layout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Layout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Layout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Layout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Layout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Layout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Layout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Layout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Layout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Return the south anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Return the south anchor of the node.\"\"\"\n    return self.own_angle(270)\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Layout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Layout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Layout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Layout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Layout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Layout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Return the west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Return the west anchor of the node.\"\"\"\n    return self.own_angle(180)\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Layout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Layout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Layout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/core/#momapy.core.Layout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/core/#momapy.core.LayoutElement","title":"LayoutElement  <code>dataclass</code>","text":"<pre><code>LayoutElement(*, id_: str = make_uuid4_as_str())\n</code></pre> <p>               Bases: <code>MapElement</code>, <code>ABC</code></p> <p>Abstract base class for layout elements.</p> <p>Common ancestor of every element carrying the visual information of a map.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'6d7d1c46-6697-46ca-bc83-0f04c73134f4'</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>bbox</code> <p>Compute and return the bounding box of the layout element.</p> <code>childless</code> <p>Return a copy of the layout element with no children.</p> <code>children</code> <p>Return the children of the layout element.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the layout element.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p>"},{"location":"api_reference/core/#momapy.core.LayoutElement.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/core/#momapy.core.LayoutElement.bbox","title":"bbox  <code>abstractmethod</code>","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>@abc.abstractmethod\ndef bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the layout element.\"\"\"\n    pass\n</code></pre>"},{"location":"api_reference/core/#momapy.core.LayoutElement.childless","title":"childless  <code>abstractmethod</code>","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the layout element with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>@abc.abstractmethod\ndef childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the layout element with no children.\"\"\"\n    pass\n</code></pre>"},{"location":"api_reference/core/#momapy.core.LayoutElement.children","title":"children  <code>abstractmethod</code>","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>@abc.abstractmethod\ndef children(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the children of the layout element.\"\"\"\n    pass\n</code></pre>"},{"location":"api_reference/core/#momapy.core.LayoutElement.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/core/#momapy.core.LayoutElement.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/core/#momapy.core.LayoutElement.drawing_elements","title":"drawing_elements  <code>abstractmethod</code>","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>@abc.abstractmethod\ndef drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the layout element.\"\"\"\n    pass\n</code></pre>"},{"location":"api_reference/core/#momapy.core.LayoutElement.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/core/#momapy.core.LayoutElement.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/core/#momapy.core.LayoutElement.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/core/#momapy.core.LayoutModelMapping","title":"LayoutModelMapping","text":"<pre><code>LayoutModelMapping(*args, **kwargs)\n</code></pre> <p>               Bases: <code>FrozenIdentitySurjectionDict</code></p> <p>Mapping between model elements and layout elements.</p> <p>A Map can draw the same model element several times, so the relation between layouts and model elements is a many-to-one mapping whose keys are layout elements and whose values are model elements. Two kinds of keys are used:</p> <ul> <li>Singleton key \u2014 a single LayoutElement   that represents a model element on its own (a macromolecule   glyph, a compartment, a state variable, a modulation arc when   no cluster is needed).</li> <li>Frozenset key \u2014 a <code>frozenset</code> of several layout elements   that jointly represent one model element. Used whenever a   model concept is drawn as a cluster of shapes: a process and   its participant arcs and target layouts, a logical operator and   its input arcs and targets, a modulation arc with its source and   target clusters, a tag or terminal with its reference arcs.</li> </ul> <p>When the key is a frozenset, it is useful to designate one of the layouts as the anchor \u2014 the element that stands for the cluster on its own. The anchor is typically the \"central\" layout (the process glyph for a process, the operator glyph for a logical operator, the modulation arc for a modulation, the tag glyph for a tag). Anchors are registered through the <code>anchor</code> argument of add_mapping. Once registered, get_mapping resolves the anchor back to the model element stored under the frozenset key, and other composite keys can reference the cluster by its anchor rather than by the whole frozenset.</p> <p>See the SBGN-PD, SBGN-AF, and CellDesigner module documentation for the per-model-element catalogue of key shapes and anchors.</p> <p>Initialize the mapping and its frozen singleton-to-key index.</p> <p>Methods:</p> Name Description <code>__reduce__</code> <p>Pickle hook that preserves <code>_singleton_to_key</code> across round-trips.</p> <code>__setstate__</code> <p>Restore <code>_singleton_to_key</code> after <code>__reduce__</code>-driven unpickle.</p> <code>get_child_layout_elements</code> <p>Return the layout elements representing <code>child_model_element</code> under <code>parent_model_element</code>.</p> <code>get_mapping</code> <p>Return the model element or layout elements mapped to <code>map_element</code>.</p> <code>is_submapping</code> <p>Return <code>True</code> if the mapping is a submapping of another <code>LayoutModelMapping</code>, <code>False</code> otherwise.</p> <p>Attributes:</p> Name Type Description <code>inverse</code> <code>frozendict</code> <p>Return the value-&gt;keys inverse as a <code>frozendict[K, frozenset]</code>.</p> Source code in <code>src/momapy/core/mapping.py</code> <pre><code>def __init__(self, *args, **kwargs) -&gt; None:\n    \"\"\"Initialize the mapping and its frozen singleton-to-key index.\"\"\"\n    super().__init__(*args, **kwargs)\n    object.__setattr__(\n        self,\n        \"_singleton_to_key\",\n        FrozenSurjectionDict(),\n    )\n</code></pre>"},{"location":"api_reference/core/#momapy.core.LayoutModelMapping.__reduce__","title":"__reduce__","text":"<pre><code>__reduce__() -&gt; tuple[Any, ...]\n</code></pre> <p>Pickle hook that preserves <code>_singleton_to_key</code> across round-trips.</p> <p>The inherited <code>frozendict.__reduce__</code> only serialises the dict contents, which drops the anchor table added by this subclass.</p> Source code in <code>src/momapy/core/mapping.py</code> <pre><code>def __reduce__(self) -&gt; tuple[typing.Any, ...]:\n    \"\"\"Pickle hook that preserves `_singleton_to_key` across round-trips.\n\n    The inherited `frozendict.__reduce__` only serialises the dict\n    contents, which drops the anchor table added by this subclass.\n    \"\"\"\n    return (\n        type(self),\n        (dict(self),),\n        {\"_singleton_to_key\": dict(self._singleton_to_key)},\n    )\n</code></pre>"},{"location":"api_reference/core/#momapy.core.LayoutModelMapping.__setstate__","title":"__setstate__","text":"<pre><code>__setstate__(state: dict[str, Any]) -&gt; None\n</code></pre> <p>Restore <code>_singleton_to_key</code> after <code>__reduce__</code>-driven unpickle.</p> Source code in <code>src/momapy/core/mapping.py</code> <pre><code>def __setstate__(self, state: dict[str, typing.Any]) -&gt; None:\n    \"\"\"Restore `_singleton_to_key` after `__reduce__`-driven unpickle.\"\"\"\n    object.__setattr__(\n        self,\n        \"_singleton_to_key\",\n        FrozenSurjectionDict(state[\"_singleton_to_key\"]),\n    )\n</code></pre>"},{"location":"api_reference/core/#momapy.core.LayoutModelMapping.get_child_layout_elements","title":"get_child_layout_elements","text":"<pre><code>get_child_layout_elements(child_model_element: ModelElement, parent_model_element: ModelElement) -&gt; list[LayoutElement]\n</code></pre> <p>Return the layout elements representing <code>child_model_element</code> under <code>parent_model_element</code>.</p> <p>Computes the intersection of two sets:</p> <ul> <li><code>S1</code>: layouts that belong under <code>parent_model_element</code> \u2014 the   children of each container layout mapped to the parent, plus the   members of each frozenset key mapped to the parent.</li> <li><code>S2</code>: layouts that represent <code>child_model_element</code> \u2014 each   singleton layout mapped to the child, plus the anchors of each   frozenset key mapped to the child.</li> </ul> <p>The inverse is identity-keyed, so two content-equal but id-distinct model instances are not aliased: layouts under one parent do not cross-pollute the result for the sibling instance.</p> Source code in <code>src/momapy/core/mapping.py</code> <pre><code>def get_child_layout_elements(\n    self,\n    child_model_element: \"ModelElement\",\n    parent_model_element: \"ModelElement\",\n) -&gt; \"list[LayoutElement]\":\n    \"\"\"Return the layout elements representing ``child_model_element`` under ``parent_model_element``.\n\n    Computes the intersection of two sets:\n\n    - ``S1``: layouts that belong under ``parent_model_element`` \u2014 the\n      children of each container layout mapped to the parent, plus the\n      members of each frozenset key mapped to the parent.\n    - ``S2``: layouts that represent ``child_model_element`` \u2014 each\n      singleton layout mapped to the child, plus the anchors of each\n      frozenset key mapped to the child.\n\n    The inverse is identity-keyed, so two content-equal but id-distinct\n    model instances are not aliased: layouts under one parent do not\n    cross-pollute the result for the sibling instance.\n    \"\"\"\n    child_s2 = set()\n    for key in self.inverse.get(id(child_model_element), ()):\n        if isinstance(key, frozenset):\n            for anchor in self._singleton_to_key.inverse.get(key, []):\n                child_s2.add(anchor)\n        else:\n            child_s2.add(key)\n    parent_s1 = set()\n    for parent_layout in self.inverse.get(id(parent_model_element), ()):\n        if isinstance(parent_layout, frozenset):\n            parent_s1 |= parent_layout\n        elif hasattr(parent_layout, \"layout_elements\"):\n            parent_s1.update(parent_layout.layout_elements)\n    return list(parent_s1 &amp; child_s2)\n</code></pre>"},{"location":"api_reference/core/#momapy.core.LayoutModelMapping.get_mapping","title":"get_mapping","text":"<pre><code>get_mapping(map_element: MapElement) -&gt; ModelElement | list[LayoutElement | frozenset[LayoutElement]] | None\n</code></pre> <p>Return the model element or layout elements mapped to <code>map_element</code>.</p> <p>Lookup order: 1. Direct key: <code>map_element</code> is a singleton or frozenset key in the    mapping; returns the associated model element directly. 2. Inverse: <code>map_element</code> is a model element; returns the list of    layout elements (or frozenset keys) whose stored model value    is <code>map_element</code> by object identity. Two content-equal    but id-distinct model instances are not aliased. 3. Anchor fallback: <code>map_element</code> was registered as the anchor of a    frozenset key via the <code>anchor</code> argument of <code>add_mapping</code>;    returns the model element stored under that frozenset key.</p> <p>Returns <code>None</code> when no match is found.</p> Source code in <code>src/momapy/core/mapping.py</code> <pre><code>def get_mapping(\n    self,\n    map_element: \"MapElement\",\n) -&gt; \"ModelElement | list[LayoutElement | frozenset[LayoutElement]] | None\":\n    \"\"\"Return the model element or layout elements mapped to `map_element`.\n\n    Lookup order:\n    1. Direct key: `map_element` is a singleton or frozenset key in the\n       mapping; returns the associated model element directly.\n    2. Inverse: `map_element` is a model element; returns the list of\n       layout elements (or frozenset keys) whose stored model value\n       **is** ``map_element`` by object identity. Two content-equal\n       but id-distinct model instances are *not* aliased.\n    3. Anchor fallback: `map_element` was registered as the anchor of a\n       frozenset key via the ``anchor`` argument of ``add_mapping``;\n       returns the model element stored under that frozenset key.\n\n    Returns ``None`` when no match is found.\n    \"\"\"\n    if map_element in self:\n        return self[map_element]\n    result = self.inverse.get(id(map_element))\n    if result:\n        return list(result)\n    key = self._singleton_to_key.get(map_element)\n    if key is not None:\n        return self[key]\n    return None\n</code></pre>"},{"location":"api_reference/core/#momapy.core.LayoutModelMapping.inverse","title":"inverse  <code>property</code>","text":"<pre><code>inverse: frozendict\n</code></pre> <p>Return the value-&gt;keys inverse as a <code>frozendict[K, frozenset]</code>.</p> <p><code>K</code> is the value (equality variants) or <code>id(value)</code> (identity variants); each bucket is a <code>frozenset</code> of forward keys. It is precomputed once at construction and returned in O(1).</p> <p>Returns:</p> Type Description <code>frozendict</code> <p>A <code>frozendict</code> mapping each value (or <code>id(value)</code>) to the</p> <code>frozendict</code> <p><code>frozenset</code> of keys that map to it.</p>"},{"location":"api_reference/core/#momapy.core.LayoutModelMapping.is_submapping","title":"is_submapping","text":"<pre><code>is_submapping(other: LayoutModelMapping) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the mapping is a submapping of another <code>LayoutModelMapping</code>, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/mapping.py</code> <pre><code>def is_submapping(self, other: \"LayoutModelMapping\") -&gt; bool:\n    \"\"\"Return `True` if the mapping is a submapping of another `LayoutModelMapping`, `False` otherwise.\"\"\"\n    return self.items() &lt;= other.items()\n</code></pre>"},{"location":"api_reference/core/#momapy.core.LayoutModelMappingBuilder","title":"LayoutModelMappingBuilder","text":"<pre><code>LayoutModelMappingBuilder(*args, **kwargs)\n</code></pre> <p>               Bases: <code>IdentitySurjectionDict</code>, <code>Builder</code></p> <p>Mutable builder for LayoutModelMapping.</p> <p>Builds a layout-to-model mapping incrementally, then produces a frozen LayoutModelMapping via <code>build()</code>. It keeps the same key conventions as the frozen mapping (singleton layout keys and frozenset keys, each frozenset anchored in <code>_singleton_to_key</code>), but uses mutable containers so elements can be added or replaced while a map is being constructed.</p> <p>Initialize the mapping builder and its singleton-to-key index.</p> <p>Methods:</p> Name Description <code>__delitem__</code> <p>Delete a key, updating the identity-keyed inverse index.</p> <code>__reduce__</code> <p>Pickle hook that preserves <code>_singleton_to_key</code> across round-trips.</p> <code>__setitem__</code> <p>Set a key to a value, updating the identity-keyed inverse index.</p> <code>__setstate__</code> <p>Restore <code>_singleton_to_key</code> after <code>__reduce__</code>-driven unpickle.</p> <code>add_mapping</code> <p>Add a layout-element to model-element entry to the mapping.</p> <code>build</code> <p>Build a frozen <code>LayoutModelMapping</code> from this builder.</p> <code>from_object</code> <p>Build a <code>LayoutModelMappingBuilder</code> from a frozen mapping.</p> <code>get_child_layout_elements</code> <p>Return the layout elements representing <code>child_model_element</code> under <code>parent_model_element</code>.</p> <code>get_mapping</code> <p>Return the element(s) mapped to the given map element.</p> <p>Attributes:</p> Name Type Description <code>inverse</code> <code>frozendict</code> <p>Return the value-&gt;keys inverse as a <code>frozendict[K, frozenset]</code>.</p> Source code in <code>src/momapy/core/mapping.py</code> <pre><code>def __init__(self, *args, **kwargs) -&gt; None:\n    \"\"\"Initialize the mapping builder and its singleton-to-key index.\"\"\"\n    super().__init__(*args, **kwargs)\n    self._singleton_to_key = SurjectionDict()\n</code></pre>"},{"location":"api_reference/core/#momapy.core.LayoutModelMappingBuilder.__delitem__","title":"__delitem__","text":"<pre><code>__delitem__(key: Any) -&gt; None\n</code></pre> <p>Delete a key, updating the identity-keyed inverse index.</p> Source code in <code>src/momapy/utils.py</code> <pre><code>def __delitem__(self, key: typing.Any) -&gt; None:\n    \"\"\"Delete a key, updating the identity-keyed inverse index.\"\"\"\n    value = self[key]\n    value_identity = id(value)\n    super().__delitem__(key)\n    if value_identity in self._identity_inverse:\n        self._identity_inverse[value_identity].discard(key)\n        if not self._identity_inverse[value_identity]:\n            del self._identity_inverse[value_identity]\n</code></pre>"},{"location":"api_reference/core/#momapy.core.LayoutModelMappingBuilder.__reduce__","title":"__reduce__","text":"<pre><code>__reduce__() -&gt; tuple[Any, ...]\n</code></pre> <p>Pickle hook that preserves <code>_singleton_to_key</code> across round-trips.</p> <p>The default <code>dict</code>-subclass pickle emits SETITEMS before BUILD, so <code>IdentitySurjectionDict.__setitem__</code> fires before <code>_identity_inverse</code> exists and crashes. Routing through <code>__init__</code> fixes both that and the <code>_singleton_to_key</code> loss.</p> Source code in <code>src/momapy/core/mapping.py</code> <pre><code>def __reduce__(self) -&gt; tuple[typing.Any, ...]:\n    \"\"\"Pickle hook that preserves `_singleton_to_key` across round-trips.\n\n    The default `dict`-subclass pickle emits SETITEMS before BUILD, so\n    `IdentitySurjectionDict.__setitem__` fires before\n    `_identity_inverse` exists and crashes. Routing through\n    `__init__` fixes both that and the `_singleton_to_key` loss.\n    \"\"\"\n    return (\n        type(self),\n        (dict(self),),\n        {\"_singleton_to_key\": dict(self._singleton_to_key)},\n    )\n</code></pre>"},{"location":"api_reference/core/#momapy.core.LayoutModelMappingBuilder.__setitem__","title":"__setitem__","text":"<pre><code>__setitem__(key: Any, value: Any) -&gt; None\n</code></pre> <p>Set a key to a value, updating the identity-keyed inverse index.</p> Source code in <code>src/momapy/utils.py</code> <pre><code>def __setitem__(self, key: typing.Any, value: typing.Any) -&gt; None:\n    \"\"\"Set a key to a value, updating the identity-keyed inverse index.\"\"\"\n    if key in self:\n        old_value = self[key]\n        old_identity = id(old_value)\n        if old_identity in self._identity_inverse:\n            self._identity_inverse[old_identity].discard(key)\n            if not self._identity_inverse[old_identity]:\n                del self._identity_inverse[old_identity]\n    super().__setitem__(key, value)\n    self._identity_inverse.setdefault(id(value), set()).add(key)\n</code></pre>"},{"location":"api_reference/core/#momapy.core.LayoutModelMappingBuilder.__setstate__","title":"__setstate__","text":"<pre><code>__setstate__(state: dict[str, Any]) -&gt; None\n</code></pre> <p>Restore <code>_singleton_to_key</code> after <code>__reduce__</code>-driven unpickle.</p> Source code in <code>src/momapy/core/mapping.py</code> <pre><code>def __setstate__(self, state: dict[str, typing.Any]) -&gt; None:\n    \"\"\"Restore `_singleton_to_key` after `__reduce__`-driven unpickle.\"\"\"\n    self._singleton_to_key = SurjectionDict(state[\"_singleton_to_key\"])\n</code></pre>"},{"location":"api_reference/core/#momapy.core.LayoutModelMappingBuilder.add_mapping","title":"add_mapping","text":"<pre><code>add_mapping(layout_element: LayoutElement, model_element: ModelElement, anchor: LayoutElement | None = None) -&gt; None\n</code></pre> <p>Add a layout-element to model-element entry to the mapping.</p> <p>The mapping is many-to-one: several layout elements may map to the same model element, so adding a new entry for a model element that is already mapped simply registers an additional layout key \u2014 existing keys are kept.</p> <p>Parameters:</p> Name Type Description Default <code>layout_element</code> <code>LayoutElement</code> <p>The layout element (or frozenset of layout elements) to use as the key.</p> required <code>model_element</code> <code>ModelElement</code> <p>The model element to associate with the layout element.</p> required <code>anchor</code> <code>LayoutElement | None</code> <p>When <code>layout_element</code> is a frozenset and this argument is provided, registers <code>anchor</code> as the anchor of the frozenset key. This allows <code>get_mapping(anchor)</code> to resolve to the model element stored under the frozenset key, even when the same element participates in other frozensets (e.g. a logical operator that is both the anchor of its own gate frozenset and a participant in a modulation frozenset).</p> <code>None</code> Source code in <code>src/momapy/core/mapping.py</code> <pre><code>def add_mapping(\n    self,\n    layout_element: \"LayoutElement\",\n    model_element: \"ModelElement\",\n    anchor: \"LayoutElement | None\" = None,\n) -&gt; None:\n    \"\"\"Add a layout-element to model-element entry to the mapping.\n\n    The mapping is many-to-one: several layout elements may map to the\n    same model element, so adding a new entry for a model element that is\n    already mapped simply registers an additional layout key \u2014 existing\n    keys are kept.\n\n    Args:\n        layout_element: The layout element (or frozenset of layout\n            elements) to use as the key.\n        model_element: The model element to associate with the layout\n            element.\n        anchor: When ``layout_element`` is a frozenset and this argument\n            is provided, registers ``anchor`` as the anchor of the\n            frozenset key. This allows ``get_mapping(anchor)`` to resolve\n            to the model element stored under the frozenset key, even when\n            the same element participates in other frozensets (e.g. a\n            logical operator that is both the anchor of its own gate\n            frozenset and a participant in a modulation frozenset).\n    \"\"\"\n    self[layout_element] = model_element\n    if anchor is not None:\n        self._singleton_to_key[anchor] = layout_element\n</code></pre>"},{"location":"api_reference/core/#momapy.core.LayoutModelMappingBuilder.build","title":"build","text":"<pre><code>build(builder_to_object: dict[int, Any] | None = None) -&gt; LayoutModelMapping\n</code></pre> <p>Build a frozen <code>LayoutModelMapping</code> from this builder.</p> <p>Parameters:</p> Name Type Description Default <code>builder_to_object</code> <code>dict[int, Any] | None</code> <p>Optional cache mapping builder <code>id()</code> to already-built objects, to preserve shared references.</p> <code>None</code> <p>Returns:</p> Type Description <code>LayoutModelMapping</code> <p>A frozen <code>LayoutModelMapping</code> with all keys and values built</p> <code>LayoutModelMapping</code> <p>from their builders.</p> Source code in <code>src/momapy/core/mapping.py</code> <pre><code>def build(\n    self,\n    builder_to_object: dict[int, typing.Any] | None = None,\n) -&gt; LayoutModelMapping:\n    \"\"\"Build a frozen `LayoutModelMapping` from this builder.\n\n    Args:\n        builder_to_object: Optional cache mapping builder `id()` to\n            already-built objects, to preserve shared references.\n\n    Returns:\n        A frozen `LayoutModelMapping` with all keys and values built\n        from their builders.\n    \"\"\"\n    mapping = self._cls_to_build(\n        {\n            object_from_builder(\n                key, builder_to_object=builder_to_object\n            ): object_from_builder(value, builder_to_object=builder_to_object)\n            for key, value in self.items()\n        }\n    )\n    singleton_to_key = FrozenSurjectionDict(\n        {\n            object_from_builder(\n                singleton, builder_to_object=builder_to_object\n            ): object_from_builder(key, builder_to_object=builder_to_object)\n            for singleton, key in self._singleton_to_key.items()\n        }\n    )\n    object.__setattr__(mapping, \"_singleton_to_key\", singleton_to_key)\n    return mapping\n</code></pre>"},{"location":"api_reference/core/#momapy.core.LayoutModelMappingBuilder.from_object","title":"from_object  <code>classmethod</code>","text":"<pre><code>from_object(obj: LayoutModelMapping, object_to_builder: dict[int, Builder] | None = None) -&gt; Self\n</code></pre> <p>Build a <code>LayoutModelMappingBuilder</code> from a frozen mapping.</p> <p>Parameters:</p> Name Type Description Default <code>obj</code> <code>LayoutModelMapping</code> <p>The frozen <code>LayoutModelMapping</code> to convert.</p> required <code>object_to_builder</code> <code>dict[int, Builder] | None</code> <p>Optional cache mapping object <code>id()</code> to already-created builders, to preserve shared references.</p> <code>None</code> <p>Returns:</p> Type Description <code>Self</code> <p>A <code>LayoutModelMappingBuilder</code> mirroring <code>obj</code>.</p> Source code in <code>src/momapy/core/mapping.py</code> <pre><code>@classmethod\ndef from_object(\n    cls,\n    obj: LayoutModelMapping,\n    object_to_builder: \"dict[int, Builder] | None\" = None,\n) -&gt; typing_extensions.Self:\n    \"\"\"Build a `LayoutModelMappingBuilder` from a frozen mapping.\n\n    Args:\n        obj: The frozen `LayoutModelMapping` to convert.\n        object_to_builder: Optional cache mapping object `id()` to\n            already-created builders, to preserve shared references.\n\n    Returns:\n        A `LayoutModelMappingBuilder` mirroring `obj`.\n    \"\"\"\n    items = []\n    for key, value in obj.items():\n        if isinstance(key, frozenset):\n            new_key = frozenset(\n                [\n                    builder_from_object(\n                        element, object_to_builder=object_to_builder\n                    )\n                    for element in key\n                ]\n            )\n        else:\n            new_key = builder_from_object(key, object_to_builder=object_to_builder)\n        new_value = builder_from_object(value, object_to_builder=object_to_builder)\n        items.append(\n            (\n                new_key,\n                new_value,\n            )\n        )\n    builder = cls(items)\n    singleton_to_key_items = {}\n    for singleton, key in obj._singleton_to_key.items():\n        new_singleton = builder_from_object(\n            singleton, object_to_builder=object_to_builder\n        )\n        if isinstance(key, frozenset):\n            new_key = frozenset(\n                builder_from_object(element, object_to_builder=object_to_builder)\n                for element in key\n            )\n        else:\n            new_key = builder_from_object(key, object_to_builder=object_to_builder)\n        singleton_to_key_items[new_singleton] = new_key\n    builder._singleton_to_key = SurjectionDict(singleton_to_key_items)\n    return builder\n</code></pre>"},{"location":"api_reference/core/#momapy.core.LayoutModelMappingBuilder.get_child_layout_elements","title":"get_child_layout_elements","text":"<pre><code>get_child_layout_elements(child_model_element: ModelElement, parent_model_element: ModelElement) -&gt; list[LayoutElement]\n</code></pre> <p>Return the layout elements representing <code>child_model_element</code> under <code>parent_model_element</code>.</p> Source code in <code>src/momapy/core/mapping.py</code> <pre><code>def get_child_layout_elements(\n    self,\n    child_model_element: \"ModelElement\",\n    parent_model_element: \"ModelElement\",\n) -&gt; \"list[LayoutElement]\":\n    \"\"\"Return the layout elements representing ``child_model_element`` under ``parent_model_element``.\"\"\"\n    child_s2 = set()\n    for key in self.inverse.get(id(child_model_element), ()):\n        if isinstance(key, frozenset):\n            for anchor in self._singleton_to_key.inverse.get(key, []):\n                child_s2.add(anchor)\n        else:\n            child_s2.add(key)\n    parent_s1 = set()\n    for parent_layout in self.inverse.get(id(parent_model_element), ()):\n        if isinstance(parent_layout, frozenset):\n            parent_s1 |= parent_layout\n        elif hasattr(parent_layout, \"layout_elements\"):\n            parent_s1.update(parent_layout.layout_elements)\n    return list(parent_s1 &amp; child_s2)\n</code></pre>"},{"location":"api_reference/core/#momapy.core.LayoutModelMappingBuilder.get_mapping","title":"get_mapping","text":"<pre><code>get_mapping(map_element: MapElement) -&gt; ModelElement | list[LayoutElement | frozenset[LayoutElement]] | None\n</code></pre> <p>Return the element(s) mapped to the given map element.</p> <p>If <code>map_element</code> is a key (a layout element or frozenset of layout elements), returns the model element it maps to. If it is a model element, returns the list of layout keys mapped to it.</p> <p>Parameters:</p> Name Type Description Default <code>map_element</code> <code>MapElement</code> <p>The layout or model element to look up.</p> required <p>Returns:</p> Type Description <code>ModelElement | list[LayoutElement | frozenset[LayoutElement]] | None</code> <p>The mapped model element, the list of mapped layout keys, or</p> <code>ModelElement | list[LayoutElement | frozenset[LayoutElement]] | None</code> <p><code>None</code> if the element is not in the mapping.</p> Source code in <code>src/momapy/core/mapping.py</code> <pre><code>def get_mapping(\n    self,\n    map_element: \"MapElement\",\n) -&gt; \"ModelElement | list[LayoutElement | frozenset[LayoutElement]] | None\":\n    \"\"\"Return the element(s) mapped to the given map element.\n\n    If `map_element` is a key (a layout element or frozenset of layout\n    elements), returns the model element it maps to. If it is a model\n    element, returns the list of layout keys mapped to it.\n\n    Args:\n        map_element: The layout or model element to look up.\n\n    Returns:\n        The mapped model element, the list of mapped layout keys, or\n        `None` if the element is not in the mapping.\n    \"\"\"\n    if map_element in self:\n        return self[map_element]\n    result = self.inverse.get(id(map_element))\n    if result:\n        return list(result)\n    return None\n</code></pre>"},{"location":"api_reference/core/#momapy.core.LayoutModelMappingBuilder.inverse","title":"inverse  <code>property</code>","text":"<pre><code>inverse: frozendict\n</code></pre> <p>Return the value-&gt;keys inverse as a <code>frozendict[K, frozenset]</code>.</p> <p><code>K</code> is the value (equality variants) or <code>id(value)</code> (identity variants); each bucket is a <code>frozenset</code> of forward keys. This is a fresh snapshot built on each access from the internally maintained index, so it is safe to read while the mapping is mutated.</p> <p>Returns:</p> Type Description <code>frozendict</code> <p>A <code>frozendict</code> mapping each value (or <code>id(value)</code>) to the</p> <code>frozendict</code> <p><code>frozenset</code> of keys that map to it.</p>"},{"location":"api_reference/core/#momapy.core.Map","title":"Map  <code>dataclass</code>","text":"<pre><code>Map(*, id_: str = make_uuid4_as_str(), model: Model | None = None, layout: Layout | None = None, layout_model_mapping: LayoutModelMapping | None = None)\n</code></pre> <p>               Bases: <code>MapElement</code></p> <p>Class for maps.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'ff17b81b-58c7-46eb-873d-45db22c7fd1f'</code> <code>model</code> <code>Model | None</code> <p>The model of the map</p> <code>None</code> <code>layout</code> <code>Layout | None</code> <p>The layout of the map</p> <code>None</code> <code>layout_model_mapping</code> <code>LayoutModelMapping | None</code> <p>The layout model mapping of the map</p> <code>None</code> <p>Methods:</p> Name Description <code>get_mapping</code> <p>Return the model element or layout elements mapped to <code>map_element</code>.</p> <code>is_submap</code> <p>Return <code>True</code> if the <code>Map</code> is a submap of another given map, <code>False</code> otherwise.</p>"},{"location":"api_reference/core/#momapy.core.Map.get_mapping","title":"get_mapping","text":"<pre><code>get_mapping(map_element: MapElement) -&gt; ModelElement | list[LayoutElement | frozenset[LayoutElement]] | None\n</code></pre> <p>Return the model element or layout elements mapped to <code>map_element</code>.</p> <p>The lookup is bidirectional: a layout key (a singleton or frozenset) resolves to its model element, and a model element resolves to the list of layout keys mapped to it. Forwards to <code>layout_model_mapping.get_mapping</code>.</p> <p>Returns <code>None</code> when the map has no <code>layout_model_mapping</code> (for example a layout-less map such as one read from SBML).</p> Source code in <code>src/momapy/core/map.py</code> <pre><code>def get_mapping(\n    self,\n    map_element: \"MapElement\",\n) -&gt; \"ModelElement | list[LayoutElement | frozenset[LayoutElement]] | None\":\n    \"\"\"Return the model element or layout elements mapped to `map_element`.\n\n    The lookup is bidirectional: a layout key (a singleton or frozenset)\n    resolves to its model element, and a model element resolves to the\n    list of layout keys mapped to it. Forwards to\n    `layout_model_mapping.get_mapping`.\n\n    Returns `None` when the map has no `layout_model_mapping` (for\n    example a layout-less map such as one read from SBML).\n    \"\"\"\n    if self.layout_model_mapping is None:\n        return None\n    return self.layout_model_mapping.get_mapping(map_element)\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Map.is_submap","title":"is_submap","text":"<pre><code>is_submap(other: Map) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the <code>Map</code> is a submap of another given map, <code>False</code> otherwise.</p> <p>A complete map is never a submap of an incomplete one: if either <code>self</code> or <code>other</code> has a <code>None</code> model, layout, or layout-model mapping, this returns <code>False</code>.</p> Source code in <code>src/momapy/core/map.py</code> <pre><code>def is_submap(self, other: \"Map\") -&gt; bool:\n    \"\"\"Return `True` if the `Map` is a submap of another given map, `False` otherwise.\n\n    A complete map is never a submap of an incomplete one: if either `self`\n    or `other` has a `None` model, layout, or layout-model mapping, this\n    returns `False`.\n    \"\"\"\n    if (\n        self.model is None\n        or self.layout is None\n        or self.layout_model_mapping is None\n        or other.model is None\n        or other.layout is None\n        or other.layout_model_mapping is None\n    ):\n        return False\n    return (\n        self.model.is_submodel(other.model)\n        and self.layout.is_sublayout(other.layout)\n        and self.layout_model_mapping.is_submapping(other.layout_model_mapping)\n    )\n</code></pre>"},{"location":"api_reference/core/#momapy.core.MapElement","title":"MapElement  <code>dataclass</code>","text":"<pre><code>MapElement(*, id_: str = make_uuid4_as_str())\n</code></pre> <p>Base class for map elements.</p> <p>Common ancestor of every model and layout element making up a map.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'49fa464b-94af-42f4-bf3d-001911787fd4'</code>"},{"location":"api_reference/core/#momapy.core.Model","title":"Model  <code>dataclass</code>","text":"<pre><code>Model(*, id_: str = make_uuid4_as_str())\n</code></pre> <p>               Bases: <code>MapElement</code>, <code>ABC</code></p> <p>Abstract base class for models.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'2275dea3-a142-435b-be9c-8450b485d8de'</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from this <code>Model</code>.</p> <code>is_submodel</code> <p>Return whether this model is a submodel of another model.</p>"},{"location":"api_reference/core/#momapy.core.Model.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from this <code>Model</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers of the <code>Model</code>, deduplicating by object identity. The <code>Model</code> itself is not a <code>ModelElement</code> and is not included.</p> <p>This mirrors <code>ModelElement.descendants</code> and contrasts with <code>LayoutElement.descendants</code>, which walks the explicit <code>children()</code> tree (visual containment) rather than reference reachability.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order.</p> Source code in <code>src/momapy/core/model.py</code> <pre><code>def descendants(self) -&gt; list[ModelElement]:\n    \"\"\"Return every `ModelElement` reachable from this `Model`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers of the `Model`, deduplicating by\n    object identity. The `Model` itself is not a `ModelElement` and is\n    not included.\n\n    This mirrors\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and contrasts with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which walks the explicit `children()` tree (visual *containment*)\n    rather than reference reachability.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order.\n    \"\"\"\n    seen: set[int] = set()\n    result: list[ModelElement] = []\n    for field in dataclasses.fields(type(self)):\n        _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Model.is_submodel","title":"is_submodel  <code>abstractmethod</code>","text":"<pre><code>is_submodel(other: Model) -&gt; bool\n</code></pre> <p>Return whether this model is a submodel of another model.</p> <p>Parameters:</p> Name Type Description Default <code>other</code> <code>Model</code> <p>The model to test against.</p> required <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> if this model is a submodel of <code>other</code>.</p> Source code in <code>src/momapy/core/model.py</code> <pre><code>@abc.abstractmethod\ndef is_submodel(self, other: \"Model\") -&gt; bool:\n    \"\"\"Return whether this model is a submodel of another model.\n\n    Args:\n        other: The model to test against.\n\n    Returns:\n        `True` if this model is a submodel of `other`.\n    \"\"\"\n    pass\n</code></pre>"},{"location":"api_reference/core/#momapy.core.ModelElement","title":"ModelElement  <code>dataclass</code>","text":"<pre><code>ModelElement(*, id_: str = make_uuid4_as_str())\n</code></pre> <p>               Bases: <code>MapElement</code></p> <p>Base class for model elements.</p> <p>Common ancestor of every element carrying the semantic information of a map.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'6fad7b93-d7f1-47b9-8669-fef0650e3443'</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/core/#momapy.core.ModelElement.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Node","title":"Node  <code>dataclass</code>","text":"<pre><code>Node(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = None, filter_: NoneValueType | Filter | None = None, height: float, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = None, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: NoneValueType | float | None = None, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float)\n</code></pre> <p>               Bases: <code>GroupLayout</code></p> <p>Class for nodes.</p> <p>A node is a group layout drawn as a shape with a position, a size, an optional label and its own border styling.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'53bf6751-ef8c-4e71-a750-ffc12e8cf1e6'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>None</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> required <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>None</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the node</p> <code>None</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> required <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>angle</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Return the east anchor of the node.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Return the label center anchor of the node.</p> <code>north</code> <p>Return the north anchor of the node.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Return the south anchor of the node.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Return the west anchor of the node.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/core/#momapy.core.Node.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Node.angle","title":"angle","text":"<pre><code>angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Node.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Node.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Node.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Node.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Node.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Node.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Node.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Node.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Node.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Return the east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Return the east anchor of the node.\"\"\"\n    return self.own_angle(0)\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Node.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Node.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Node.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Node.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Node.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Return the label center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Return the label center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Node.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Return the north anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Return the north anchor of the node.\"\"\"\n    return self.own_angle(90)\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Node.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Node.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Node.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Node.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Node.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Node.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Node.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Node.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Node.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Node.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Node.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Node.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Return the south anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Return the south anchor of the node.\"\"\"\n    return self.own_angle(270)\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Node.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Node.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Node.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Node.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Node.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Node.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Return the west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Return the west anchor of the node.\"\"\"\n    return self.own_angle(180)\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Node.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Node.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Node.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/core/#momapy.core.Node.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/core/#momapy.core.Orientation","title":"Orientation","text":"<p>               Bases: <code>Enum</code></p> <p>Orientation along an axis.</p> <p>Either horizontal or vertical. For a cardinal direction (up, right, down, left), see <code>Direction</code>.</p>"},{"location":"api_reference/core/#momapy.core.Shape","title":"Shape  <code>dataclass</code>","text":"<pre><code>Shape(*, id_: str = make_uuid4_as_str())\n</code></pre> <p>               Bases: <code>LayoutElement</code></p> <p>Class for basic shapes.</p> <p>A shape is the most simple layout element and has no children.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'74f194b1-bf97-4f46-afcd-22287eeba557'</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>bbox</code> <p>Compute and return the bounding box of the shape.</p> <code>childless</code> <p>Return a copy of the shape with no children.</p> <code>children</code> <p>Return the children of the shape.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the layout element.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p>"},{"location":"api_reference/core/#momapy.core.Shape.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Shape.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the shape.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the shape.\"\"\"\n    primitives = self.to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Shape.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the shape with no children.</p> <p>A shape has no children, so return a copy of the shape.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the shape with no children.\n\n    A shape has no children, so return a copy of the shape.\n    \"\"\"\n    return copy.deepcopy(self)\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Shape.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the shape.</p> <p>A shape has no children, so return an empty list.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the shape.\n\n    A shape has no children, so return an empty list.\n    \"\"\"\n    return []\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Shape.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Shape.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Shape.drawing_elements","title":"drawing_elements  <code>abstractmethod</code>","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>@abc.abstractmethod\ndef drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the layout element.\"\"\"\n    pass\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Shape.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Shape.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/core/#momapy.core.Shape.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/core/#momapy.core.SingleHeadedArc","title":"SingleHeadedArc  <code>dataclass</code>","text":"<pre><code>SingleHeadedArc(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, end_shorten: float = 0.0, fill: NoneValueType | Color | None = None, filter_: NoneValueType | Filter | None = None, path_fill: NoneValueType | Color | None = None, path_filter: NoneValueType | Filter | None = None, path_stroke: NoneValueType | Color | None = None, path_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, path_stroke_dashoffset: NoneValueType | float | None = None, path_stroke_width: float | None = None, path_transform: NoneValueType | tuple[Transformation, ...] | None = None, stroke: NoneValueType | Color | None = None, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: NoneValueType | float | None = None, segments: tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...] = tuple(), source: LayoutElement | None = None, start_shorten: float = 0.0, target: LayoutElement | None = None, transform: NoneValueType | tuple[Transformation, ...] | None = None, arrowhead_fill: NoneValueType | Color | None = None, arrowhead_filter: NoneValueType | Filter | None = None, arrowhead_stroke: NoneValueType | Color | None = None, arrowhead_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, arrowhead_stroke_dashoffset: NoneValueType | float | None = None, arrowhead_stroke_width: NoneValueType | float | None = None, arrowhead_transform: NoneValueType | tuple[Transformation, ...] | None = None)\n</code></pre> <p>               Bases: <code>Arc</code></p> <p>Base class for single-headed arcs.</p> <p>A single-headed arc is formed of a path and a unique arrowhead at its end.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'157e11e5-dff5-4a76-8816-6d6eec96084b'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>end_shorten</code> <code>float</code> <p>The length the end of the arc will be shorten by</p> <code>0.0</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the arc</p> <code>None</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the arc</p> <code>None</code> <code>path_fill</code> <code>NoneValueType | Color | None</code> <p>The path fill color of the arc</p> <code>None</code> <code>path_filter</code> <code>NoneValueType | Filter | None</code> <p>The path filter of the arc</p> <code>None</code> <code>path_stroke</code> <code>NoneValueType | Color | None</code> <p>The path stroke color of the arc</p> <code>None</code> <code>path_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The path stroke dasharray of the arc</p> <code>None</code> <code>path_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The path stroke dashoffset of the arc</p> <code>None</code> <code>path_stroke_width</code> <code>float | None</code> <p>The path stroke width of the arc</p> <code>None</code> <code>path_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The path transform of the arc</p> <code>None</code> <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the arc</p> <code>None</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the arc</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the arc</p> <code>None</code> <code>stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the arc</p> <code>None</code> <code>segments</code> <code>tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...]</code> <p>The path segments of the arc</p> <code>&lt;dynamic&gt;</code> <code>source</code> <code>LayoutElement | None</code> <p>The source of the arc</p> <code>None</code> <code>start_shorten</code> <code>float</code> <p>The length the start of the arc will be shorten by</p> <code>0.0</code> <code>target</code> <code>LayoutElement | None</code> <p>The target of the arc</p> <code>None</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the arc</p> <code>None</code> <code>arrowhead_fill</code> <code>NoneValueType | Color | None</code> <p>The arrowhead fill color of the arc</p> <code>None</code> <code>arrowhead_filter</code> <code>NoneValueType | Filter | None</code> <p>The arrowhead filter of the arc</p> <code>None</code> <code>arrowhead_stroke</code> <code>NoneValueType | Color | None</code> <p>The arrowhead stroke color of the arc</p> <code>None</code> <code>arrowhead_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The arrowhead stroke dasharray of the arc</p> <code>None</code> <code>arrowhead_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The arrowhead stroke dashoffset of the arc</p> <code>None</code> <code>arrowhead_stroke_width</code> <code>NoneValueType | float | None</code> <p>The arrowhead stroke width of the arc</p> <code>None</code> <code>arrowhead_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The arrowhead transform of the arc</p> <code>None</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>arrowhead_base</code> <p>Return the arrowhead base anchor point of the single-headed arc.</p> <code>arrowhead_bbox</code> <p>Return the bounding box of the single-headed arc arrowhead.</p> <code>arrowhead_border</code> <p>Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.</p> <code>arrowhead_drawing_elements</code> <p>Return the drawing elements of the single-headed arc arrowhead.</p> <code>arrowhead_length</code> <p>Return the length of the single-headed arc arrowhead.</p> <code>arrowhead_tip</code> <p>Return the arrowhead tip anchor point of the single-headed arc.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>childless</code> <p>Return a copy of the arc with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>end_point</code> <p>Return the ending point of the arc.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>fraction</code> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <code>length</code> <p>Return the total length of the arc path.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_children</code> <p>Return the self children of the arc.</p> <code>own_drawing_elements</code> <p>Return the self drawing elements of the single-headed arc.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>path_drawing_elements</code> <p>Return the drawing elements of the single-headed arc path.</p> <code>points</code> <p>Return the points of the arc path.</p> <code>start_point</code> <p>Return the starting point of the arc.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p>"},{"location":"api_reference/core/#momapy.core.SingleHeadedArc.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/core/#momapy.core.SingleHeadedArc.arrowhead_base","title":"arrowhead_base","text":"<pre><code>arrowhead_base() -&gt; Point\n</code></pre> <p>Return the arrowhead base anchor point of the single-headed arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_base(self) -&gt; Point:\n    \"\"\"Return the arrowhead base anchor point of the single-headed arc.\"\"\"\n    arrowhead_length = self.arrowhead_length()\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return self.arrowhead_tip() - (arrowhead_length, 0)\n    fraction = 1 - (arrowhead_length + self.end_shorten) / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/core/#momapy.core.SingleHeadedArc.arrowhead_bbox","title":"arrowhead_bbox","text":"<pre><code>arrowhead_bbox() -&gt; Bbox\n</code></pre> <p>Return the bounding box of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_bbox(self) -&gt; Bbox:\n    \"\"\"Return the bounding box of the single-headed arc arrowhead.\"\"\"\n    return get_drawing_elements_bbox(self.arrowhead_drawing_elements())\n</code></pre>"},{"location":"api_reference/core/#momapy.core.SingleHeadedArc.arrowhead_border","title":"arrowhead_border","text":"<pre><code>arrowhead_border(point: Point) -&gt; Point\n</code></pre> <p>Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_border(self, point: Point) -&gt; Point:\n    \"\"\"Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    point = get_drawing_elements_border(self.arrowhead_drawing_elements(), point)\n    if point is None:\n        return self.arrowhead_tip()\n    return point\n</code></pre>"},{"location":"api_reference/core/#momapy.core.SingleHeadedArc.arrowhead_drawing_elements","title":"arrowhead_drawing_elements","text":"<pre><code>arrowhead_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_drawing_elements(\n    self,\n) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the single-headed arc arrowhead.\"\"\"\n    drawing_elements = self._arrowhead_border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_arrowhead\",\n        elements=tuple(drawing_elements),\n        fill=self.arrowhead_fill,\n        filter_=self.arrowhead_filter,\n        id_=f\"{self.id_}_arrowhead\",\n        stroke=self.arrowhead_stroke,\n        stroke_dasharray=self.arrowhead_stroke_dasharray,\n        stroke_dashoffset=self.arrowhead_stroke_dashoffset,\n        stroke_width=self.arrowhead_stroke_width,\n        transform=self.arrowhead_transform,\n    )\n    transformation = self._get_arrowhead_transformation()\n    group = group.transformed(transformation)\n    return [group]\n</code></pre>"},{"location":"api_reference/core/#momapy.core.SingleHeadedArc.arrowhead_length","title":"arrowhead_length","text":"<pre><code>arrowhead_length() -&gt; float\n</code></pre> <p>Return the length of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_length(self) -&gt; float:\n    \"\"\"Return the length of the single-headed arc arrowhead.\"\"\"\n    bbox = get_drawing_elements_bbox(self._arrowhead_border_drawing_elements())\n    if math.isnan(bbox.width):\n        return 0.0\n    return bbox.east().x\n</code></pre>"},{"location":"api_reference/core/#momapy.core.SingleHeadedArc.arrowhead_tip","title":"arrowhead_tip","text":"<pre><code>arrowhead_tip() -&gt; Point\n</code></pre> <p>Return the arrowhead tip anchor point of the single-headed arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_tip(self) -&gt; Point:\n    \"\"\"Return the arrowhead tip anchor point of the single-headed arc.\"\"\"\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return segment.p2\n    fraction = 1 - self.end_shorten / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/core/#momapy.core.SingleHeadedArc.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/core/#momapy.core.SingleHeadedArc.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the arc with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the arc with no children.\"\"\"\n    return dataclasses.replace(self, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/core/#momapy.core.SingleHeadedArc.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/core/#momapy.core.SingleHeadedArc.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/core/#momapy.core.SingleHeadedArc.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/core/#momapy.core.SingleHeadedArc.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/core/#momapy.core.SingleHeadedArc.end_point","title":"end_point","text":"<pre><code>end_point() -&gt; Point\n</code></pre> <p>Return the ending point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_point(self) -&gt; Point:\n    \"\"\"Return the ending point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[-1]\n</code></pre>"},{"location":"api_reference/core/#momapy.core.SingleHeadedArc.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/core/#momapy.core.SingleHeadedArc.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/core/#momapy.core.SingleHeadedArc.fraction","title":"fraction","text":"<pre><code>fraction(fraction: float) -&gt; tuple[Point, float]\n</code></pre> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def fraction(self, fraction: float) -&gt; tuple[Point, float]:\n    \"\"\"Return the position and angle on the arc at a given fraction (of the total arc length).\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    current_length = 0\n    length_to_reach = fraction * self.length()\n    for segment in self.segments:\n        current_length += segment.length()\n        if current_length &gt;= length_to_reach:\n            break\n    segment_start_length = current_length - segment.length()\n    segment_fraction = (length_to_reach - segment_start_length) / segment.length()\n    position, angle = segment.get_position_and_angle_at_fraction(segment_fraction)\n    return position, angle\n</code></pre>"},{"location":"api_reference/core/#momapy.core.SingleHeadedArc.length","title":"length","text":"<pre><code>length() -&gt; float\n</code></pre> <p>Return the total length of the arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def length(self) -&gt; float:\n    \"\"\"Return the total length of the arc path.\"\"\"\n    return sum([segment.length() for segment in self.segments])\n</code></pre>"},{"location":"api_reference/core/#momapy.core.SingleHeadedArc.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/core/#momapy.core.SingleHeadedArc.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the arc.\"\"\"\n    return []\n</code></pre>"},{"location":"api_reference/core/#momapy.core.SingleHeadedArc.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the self drawing elements of the single-headed arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the self drawing elements of the single-headed arc.\"\"\"\n    drawing_elements = (\n        self.path_drawing_elements() + self.arrowhead_drawing_elements()\n    )\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/core/#momapy.core.SingleHeadedArc.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/core/#momapy.core.SingleHeadedArc.path_drawing_elements","title":"path_drawing_elements","text":"<pre><code>path_drawing_elements() -&gt; list[Path]\n</code></pre> <p>Return the drawing elements of the single-headed arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def path_drawing_elements(self) -&gt; list[Path]:\n    \"\"\"Return the drawing elements of the single-headed arc path.\"\"\"\n    arrowhead_length = self.arrowhead_length()\n    if len(self.segments) == 1:\n        segment = (\n            self.segments[0]\n            .shortened(self.start_shorten, \"start\")\n            .shortened(self.end_shorten + arrowhead_length, \"end\")\n        )\n        actions = [\n            MoveTo(segment.p1),\n            self._make_path_action_from_segment(segment),\n        ]\n    else:\n        first_segment = self.segments[0].shortened(self.start_shorten, \"start\")\n        last_segment = self.segments[-1].shortened(\n            self.end_shorten + arrowhead_length, \"end\"\n        )\n        actions = [\n            MoveTo(first_segment.p1),\n            self._make_path_action_from_segment(first_segment),\n        ]\n        for segment in self.segments[1:-1]:\n            action = self._make_path_action_from_segment(segment)\n            actions.append(action)\n        actions.append(self._make_path_action_from_segment(last_segment))\n    path = Path(\n        actions=tuple(actions),\n        class_=f\"{type(self).__name__}_path\",\n        fill=self.path_fill,\n        filter_=self.path_filter,\n        id_=f\"{self.id_}_path\",\n        stroke=self.path_stroke,\n        stroke_dasharray=self.path_stroke_dasharray,\n        stroke_dashoffset=self.path_stroke_dashoffset,\n        stroke_width=self.path_stroke_width,\n        transform=self.path_transform,\n    )\n    return [path]\n</code></pre>"},{"location":"api_reference/core/#momapy.core.SingleHeadedArc.points","title":"points","text":"<pre><code>points() -&gt; list[Point]\n</code></pre> <p>Return the points of the arc path.</p> <p>An arc with no segments has no points and returns an empty list.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def points(self) -&gt; list[Point]:\n    \"\"\"Return the points of the arc path.\n\n    An arc with no segments has no points and returns an empty list.\n    \"\"\"\n    if not self.segments:\n        return []\n    points = []\n    for segment in self.segments:\n        points.append(segment.p1)\n    points.append(segment.p2)\n    return points\n</code></pre>"},{"location":"api_reference/core/#momapy.core.SingleHeadedArc.start_point","title":"start_point","text":"<pre><code>start_point() -&gt; Point\n</code></pre> <p>Return the starting point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_point(self) -&gt; Point:\n    \"\"\"Return the starting point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[0]\n</code></pre>"},{"location":"api_reference/core/#momapy.core.SingleHeadedArc.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/core/#momapy.core.TextLayout","title":"TextLayout  <code>dataclass</code>","text":"<pre><code>TextLayout(*, id_: str = make_uuid4_as_str(), text: str, font_family: str = get_initial_value('font_family'), font_size: float = get_initial_value('font_size'), font_style: FontStyle = get_initial_value('font_style'), font_weight: FontWeight | int = get_initial_value('font_weight'), position: Point, width: float | None = None, height: float | None = None, horizontal_alignment: HAlignment = LEFT, vertical_alignment: VAlignment = TOP, justify: bool = False, fill: NoneValueType | Color | None = None, filter_: NoneValueType | Filter | None = None, stroke: NoneValueType | Color | None = None, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = None, text_anchor: TextAnchor | None = None, transform: NoneValueType | tuple[Transformation, ...] | None = None)\n</code></pre> <p>               Bases: <code>LayoutElement</code></p> <p>Class for text layouts.</p> <p>A text layout draws a piece of text at a position with its own font and styling.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'0ce8f4b0-d2ed-479c-913f-9aa0a2c2ad70'</code> <code>text</code> <code>str</code> <p>The text of the text layout</p> required <code>font_family</code> <code>str</code> <p>The font family of the text layout</p> <code>'DejaVu Sans'</code> <code>font_size</code> <code>float</code> <p>The font size of the text layout</p> <code>16.0</code> <code>font_style</code> <code>FontStyle</code> <p>The font style of the text layout</p> <code>&lt;FontStyle.NORMAL: 0&gt;</code> <code>font_weight</code> <code>FontWeight | int</code> <p>The font weight of the text layout</p> <code>&lt;FontWeight.NORMAL: 0&gt;</code> <code>position</code> <code>Point</code> <p>The position of the text layout</p> required <code>width</code> <code>float | None</code> <p>The width of the text layout</p> <code>None</code> <code>height</code> <code>float | None</code> <p>The height of the text layout</p> <code>None</code> <code>horizontal_alignment</code> <code>HAlignment</code> <p>The horizontal alignment of the text layout</p> <code>&lt;HAlignment.LEFT: 1&gt;</code> <code>vertical_alignment</code> <code>VAlignment</code> <p>The vertical alignment of the text layout</p> <code>&lt;VAlignment.TOP: 1&gt;</code> <code>justify</code> <code>bool</code> <p>Whether to justify the text or not</p> <code>False</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The text fill color of the text layout</p> <code>None</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the text layout</p> <code>None</code> <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The text stroke color of the text layout</p> <code>None</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The text stroke dasharray of the text layout</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The text stroke dashoffset of the text layout</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The text stroke width of the text layout</p> <code>None</code> <code>text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the text layout</p> <code>None</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the text layout</p> <code>None</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>bbox</code> <p>Compute and return the bounding box of the layout element.</p> <code>childless</code> <p>Return a copy of the text layout with no children.</p> <code>children</code> <p>Return the children of the text layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the text layout.</p> <code>east</code> <p>Return the east anchor of the text layout.</p> <code>east_north_east</code> <p>Return the east north east anchor of the text layout.</p> <code>east_south_east</code> <p>Return the east south east anchor of the text layout.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>north</code> <p>Return the north anchor of the text layout.</p> <code>north_east</code> <p>Return the north east anchor of the text layout.</p> <code>north_north_east</code> <p>Return the north north east anchor of the text layout.</p> <code>north_north_west</code> <p>Return the north north west anchor of the text layout.</p> <code>north_west</code> <p>Return the north west anchor of the text layout.</p> <code>south</code> <p>Return the south anchor of the text layout.</p> <code>south_east</code> <p>Return the south east anchor of the text layout.</p> <code>south_south_east</code> <p>Return the south south east anchor of the text layout.</p> <code>south_south_west</code> <p>Return the south south west anchor of the text layout.</p> <code>south_west</code> <p>Return the south west anchor of the text layout.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Return the west anchor of the text layout.</p> <code>west_north_west</code> <p>Return the west north west anchor of the text layout.</p> <code>west_south_west</code> <p>Return the west south west anchor of the text layout.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>Return the x coordinate of the text layout.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the text layout.</p>"},{"location":"api_reference/core/#momapy.core.TextLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/core/#momapy.core.TextLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the layout element.\"\"\"\n    line_positions = self._get_line_positions()\n    font_file_path = self._get_font_file_path(\n        self.font_family, self.font_weight, self.font_style\n    )\n    font = self._make_font(font_file_path, self.font_size)\n    font_ascent, font_descent, _ = self._get_font_parameters(font)\n    line, position, line_width = line_positions[0]\n    min_x = position.x\n    max_x = min_x + line_width\n    min_y = position.y - font_ascent\n    max_y = position.y + font_descent\n    for line, position, line_width in line_positions[1:]:\n        start_x = position.x\n        if start_x &lt; min_x:\n            min_x = start_x\n        end_x = start_x + line_width\n        if end_x &gt; max_x:\n            max_x = end_x\n        max_y = position.y + font_descent\n    return Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n</code></pre>"},{"location":"api_reference/core/#momapy.core.TextLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the text layout with no children.</p> <p>The text layout has no children, so return a copy of the text layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the text layout with no children.\n\n    The text layout has no children, so return a copy of the text layout.\n    \"\"\"\n    return copy.deepcopy(self)\n</code></pre>"},{"location":"api_reference/core/#momapy.core.TextLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the text layout.</p> <p>The text layout has no children, so return an empty list.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the text layout.\n\n    The text layout has no children, so return an empty list.\n    \"\"\"\n    return []\n</code></pre>"},{"location":"api_reference/core/#momapy.core.TextLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/core/#momapy.core.TextLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/core/#momapy.core.TextLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the text layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the text layout.\"\"\"\n    drawing_elements = []\n    lines_positions = self._get_line_positions()\n    for line, position, _ in lines_positions:\n        text = Text(\n            text=line,\n            point=position,\n        )\n        drawing_elements.append(text)\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.fill,\n        filter_=self.filter_,\n        font_family=self.font_family,\n        font_size=self.font_size,\n        font_style=self.font_style,\n        font_weight=self.font_weight,\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        text_anchor=self.text_anchor,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/core/#momapy.core.TextLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Return the east anchor of the text layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Return the east anchor of the text layout.\"\"\"\n    return self.bbox().east()\n</code></pre>"},{"location":"api_reference/core/#momapy.core.TextLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the text layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the text layout.\"\"\"\n    return self.bbox().east_north_east()\n</code></pre>"},{"location":"api_reference/core/#momapy.core.TextLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the text layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the text layout.\"\"\"\n    return self.bbox().east_south_east()\n</code></pre>"},{"location":"api_reference/core/#momapy.core.TextLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/core/#momapy.core.TextLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/core/#momapy.core.TextLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Return the north anchor of the text layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Return the north anchor of the text layout.\"\"\"\n    return self.bbox().north()\n</code></pre>"},{"location":"api_reference/core/#momapy.core.TextLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the text layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the text layout.\"\"\"\n    return self.bbox().north_east()\n</code></pre>"},{"location":"api_reference/core/#momapy.core.TextLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the text layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the text layout.\"\"\"\n    return self.bbox().north_north_east()\n</code></pre>"},{"location":"api_reference/core/#momapy.core.TextLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the text layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the text layout.\"\"\"\n    return self.bbox().north_north_west()\n</code></pre>"},{"location":"api_reference/core/#momapy.core.TextLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the text layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the text layout.\"\"\"\n    return self.bbox().north_west()\n</code></pre>"},{"location":"api_reference/core/#momapy.core.TextLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Return the south anchor of the text layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Return the south anchor of the text layout.\"\"\"\n    return self.bbox().south()\n</code></pre>"},{"location":"api_reference/core/#momapy.core.TextLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the text layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the text layout.\"\"\"\n    return self.bbox().south_east()\n</code></pre>"},{"location":"api_reference/core/#momapy.core.TextLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the text layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the text layout.\"\"\"\n    return self.bbox().south_south_east()\n</code></pre>"},{"location":"api_reference/core/#momapy.core.TextLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the text layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the text layout.\"\"\"\n    return self.bbox().south_south_west()\n</code></pre>"},{"location":"api_reference/core/#momapy.core.TextLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the text layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the text layout.\"\"\"\n    return self.bbox().south_west()\n</code></pre>"},{"location":"api_reference/core/#momapy.core.TextLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/core/#momapy.core.TextLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Return the west anchor of the text layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Return the west anchor of the text layout.\"\"\"\n    return self.bbox().west()\n</code></pre>"},{"location":"api_reference/core/#momapy.core.TextLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the text layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the text layout.\"\"\"\n    return self.bbox().west_north_west()\n</code></pre>"},{"location":"api_reference/core/#momapy.core.TextLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the text layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the text layout.\"\"\"\n    return self.bbox().west_south_west()\n</code></pre>"},{"location":"api_reference/core/#momapy.core.TextLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the text layout.</p>"},{"location":"api_reference/core/#momapy.core.TextLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the text layout.</p>"},{"location":"api_reference/core/#momapy.core.VAlignment","title":"VAlignment","text":"<p>               Bases: <code>Enum</code></p> <p>Vertical alignment of text or content.</p> <p>Selects how content is positioned along the vertical axis.</p>"},{"location":"api_reference/drawing/","title":"Drawing","text":""},{"location":"api_reference/drawing/#momapy.drawing","title":"momapy.drawing","text":"<p>SVG-like drawing elements for momapy.</p> <p>This module provides classes for creating and manipulating SVG-like drawing elements including paths, shapes, text, filters, and groups. It supports transformations, styling attributes, and conversion to geometry primitives.</p> <p>Examples:</p> <pre><code>from momapy.drawing import Path, MoveTo, LineTo, Rectangle, Text\nfrom momapy.geometry import Point\nfrom momapy.coloring import red, blue\n\n# Create a simple path\npath = Path(\n    actions=(\n        MoveTo(Point(0.0, 0.0)),\n        LineTo(Point(10.0, 10.0)),\n    ),\n    stroke=red,\n    stroke_width=2.0\n)\n\n# Create a rectangle\nrect = Rectangle(\n    point=Point(5.0, 5.0),\n    width=10.0,\n    height=10.0,\n    rx=0.0,\n    ry=0.0,\n    fill=blue,\n    stroke=red\n)\n\n# Create text\ntext = Text(\n    text=\"Hello\",\n    point=Point(10.0, 10.0),\n    font_size=14.0\n)\n</code></pre> <p>Classes:</p> Name Description <code>ClosePath</code> <p>Close the current path.</p> <code>CompositeEffect</code> <p>Composite filter effect.</p> <code>CompositionOperator</code> <p>Composition operators for filter effects.</p> <code>CurveTo</code> <p>Draw a cubic Bezier curve.</p> <code>DrawingElement</code> <p>Abstract base class for drawing elements.</p> <code>DropShadowEffect</code> <p>Drop shadow filter effect.</p> <code>EdgeMode</code> <p>Edge mode for blur effects.</p> <code>Ellipse</code> <p>Ellipse drawing element.</p> <code>EllipticalArc</code> <p>Draw an elliptical arc.</p> <code>FillRule</code> <p>Fill rule for complex shapes.</p> <code>Filter</code> <p>Filter with multiple effects.</p> <code>FilterEffect</code> <p>Abstract base class for filter effects.</p> <code>FilterEffectInput</code> <p>Filter effect input types.</p> <code>FilterUnits</code> <p>Units for filter regions.</p> <code>FloodEffect</code> <p>Flood filter effect.</p> <code>FontStyle</code> <p>Font style options.</p> <code>FontWeight</code> <p>Font weight options.</p> <code>GaussianBlurEffect</code> <p>Gaussian blur filter effect.</p> <code>Group</code> <p>Group of drawing elements.</p> <code>LineTo</code> <p>Draw a line to a point.</p> <code>MoveTo</code> <p>Move to a point (start a new subpath).</p> <code>NoneValueType</code> <p>Singleton type for the SVG <code>\"none\"</code> value (distinct from <code>None</code>).</p> <code>OffsetEffect</code> <p>Offset filter effect.</p> <code>Path</code> <p>Path drawing element composed of path actions.</p> <code>PathAction</code> <p>Abstract base class for path actions.</p> <code>QuadraticCurveTo</code> <p>Draw a quadratic Bezier curve.</p> <code>Rectangle</code> <p>Rectangle drawing element with optional rounded corners.</p> <code>Text</code> <p>Text drawing element.</p> <code>TextAnchor</code> <p>Text anchor options.</p> <p>Functions:</p> Name Description <code>drawing_elements_to_geometry</code> <p>Convert drawing elements to geometry primitives.</p> <code>get_drawing_elements_anchor_point</code> <p>Get anchor point of drawing elements.</p> <code>get_drawing_elements_angle</code> <p>Get border point at an angle from center.</p> <code>get_drawing_elements_bbox</code> <p>Get bounding box of drawing elements.</p> <code>get_drawing_elements_border</code> <p>Get border point in a direction from center.</p> <code>get_initial_value</code> <p>Get the initial value of a presentation attribute.</p> <p>Attributes:</p> Name Type Description <code>NoneValue</code> <p>A singleton value for type <code>NoneValueType</code>.</p>"},{"location":"api_reference/drawing/#momapy.drawing.ClosePath","title":"ClosePath  <code>dataclass</code>","text":"<pre><code>ClosePath()\n</code></pre> <p>               Bases: <code>PathAction</code></p> <p>Close the current path.</p> <p>Draws a straight segment back to the start of the current subpath.</p> <p>Methods:</p> Name Description <code>transformed</code> <p>Apply a transformation.</p>"},{"location":"api_reference/drawing/#momapy.drawing.ClosePath.transformed","title":"transformed","text":"<pre><code>transformed(transformation: Transformation, current_point: Point) -&gt; Self\n</code></pre> <p>Apply a transformation.</p> <p>Parameters:</p> Name Type Description Default <code>transformation</code> <code>Transformation</code> <p>The transformation (unused).</p> required <code>current_point</code> <code>Point</code> <p>Current point (unused).</p> required <p>Returns:</p> Type Description <code>Self</code> <p>A new ClosePath.</p> Source code in <code>src/momapy/drawing.py</code> <pre><code>def transformed(\n    self,\n    transformation: Transformation,\n    current_point: Point,\n) -&gt; typing_extensions.Self:\n    \"\"\"Apply a transformation.\n\n    Args:\n        transformation: The transformation (unused).\n        current_point: Current point (unused).\n\n    Returns:\n        A new ClosePath.\n    \"\"\"\n    return ClosePath()\n</code></pre>"},{"location":"api_reference/drawing/#momapy.drawing.CompositeEffect","title":"CompositeEffect  <code>dataclass</code>","text":"<pre><code>CompositeEffect(*, result: str | None = None, in_: FilterEffectInput | str | None = None, in2: FilterEffectInput | str | None = None, operator: CompositionOperator | None = OVER, k1: float = 0.0, k2: float = 0.0, k3: float = 0.0, k4: float = 0.0)\n</code></pre> <p>               Bases: <code>FilterEffect</code></p> <p>Composite filter effect.</p> <p>Combines two inputs using a composition operator. The fields <code>in_</code> and <code>in2</code> map to SVG <code>feComposite</code>'s <code>in</code> and <code>in2</code> attributes (<code>in_</code> carries a trailing underscore because <code>in</code> is a Python keyword).</p> <p>Parameters:</p> Name Type Description Default <code>result</code> <code>str | None</code> <p>The name of the result</p> <code>None</code> <code>in_</code> <code>FilterEffectInput | str | None</code> <p>The first effect input or the name of the first filter effect input</p> <code>None</code> <code>in2</code> <code>FilterEffectInput | str | None</code> <p>The second effect input or the name of the second filter effect input</p> <code>None</code> <code>operator</code> <code>CompositionOperator | None</code> <p>The operator to be used to compute the composite effect</p> <code>&lt;CompositionOperator.OVER: 'OVER'&gt;</code> <code>k1</code> <code>float</code> <p>k1 coefficient for the arithmetic operator</p> <code>0.0</code> <code>k2</code> <code>float</code> <p>k2 coefficient for the arithmetic operator</p> <code>0.0</code> <code>k3</code> <code>float</code> <p>k3 coefficient for the arithmetic operator</p> <code>0.0</code> <code>k4</code> <code>float</code> <p>k4 coefficient for the arithmetic operator</p> <code>0.0</code>"},{"location":"api_reference/drawing/#momapy.drawing.CompositionOperator","title":"CompositionOperator","text":"<p>               Bases: <code>Enum</code></p> <p>Composition operators for filter effects.</p> <p>Enumerates the ways two inputs can be combined by a composite effect.</p>"},{"location":"api_reference/drawing/#momapy.drawing.CurveTo","title":"CurveTo  <code>dataclass</code>","text":"<pre><code>CurveTo(point: Point, control_point1: Point, control_point2: Point)\n</code></pre> <p>               Bases: <code>PathAction</code></p> <p>Draw a cubic Bezier curve.</p> <p>Draws a cubic Bezier curve from the current point to the end point.</p> <p>Parameters:</p> Name Type Description Default <code>point</code> <code>Point</code> <p>The point of the curve to action</p> required <code>control_point1</code> <code>Point</code> <p>The first control point of the curve to action</p> required <code>control_point2</code> <code>Point</code> <p>The second control point of the curve to action</p> required <p>Methods:</p> Name Description <code>to_geometry</code> <p>Convert to CubicBezierCurve geometry.</p> <code>transformed</code> <p>Apply a transformation.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>X coordinate.</p> <code>y</code> <code>float</code> <p>Y coordinate.</p>"},{"location":"api_reference/drawing/#momapy.drawing.CurveTo.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry(current_point: Point) -&gt; CubicBezierCurve\n</code></pre> <p>Convert to CubicBezierCurve geometry.</p> <p>Parameters:</p> Name Type Description Default <code>current_point</code> <code>Point</code> <p>Start point.</p> required <p>Returns:</p> Type Description <code>CubicBezierCurve</code> <p>A CubicBezierCurve.</p> Source code in <code>src/momapy/drawing.py</code> <pre><code>def to_geometry(self, current_point: Point) -&gt; CubicBezierCurve:\n    \"\"\"Convert to CubicBezierCurve geometry.\n\n    Args:\n        current_point: Start point.\n\n    Returns:\n        A CubicBezierCurve.\n    \"\"\"\n    return CubicBezierCurve(\n        current_point,\n        self.point,\n        self.control_point1,\n        self.control_point2,\n    )\n</code></pre>"},{"location":"api_reference/drawing/#momapy.drawing.CurveTo.transformed","title":"transformed","text":"<pre><code>transformed(transformation: Transformation, current_point: Point) -&gt; Self\n</code></pre> <p>Apply a transformation.</p> <p>Parameters:</p> Name Type Description Default <code>transformation</code> <code>Transformation</code> <p>The transformation.</p> required <code>current_point</code> <code>Point</code> <p>Start point (unused).</p> required <p>Returns:</p> Type Description <code>Self</code> <p>A new CurveTo with transformed points.</p> Source code in <code>src/momapy/drawing.py</code> <pre><code>def transformed(\n    self,\n    transformation: Transformation,\n    current_point: Point,\n) -&gt; typing_extensions.Self:\n    \"\"\"Apply a transformation.\n\n    Args:\n        transformation: The transformation.\n        current_point: Start point (unused).\n\n    Returns:\n        A new CurveTo with transformed points.\n    \"\"\"\n    return CurveTo(\n        self.point.transformed(transformation),\n        self.control_point1.transformed(transformation),\n        self.control_point2.transformed(transformation),\n    )\n</code></pre>"},{"location":"api_reference/drawing/#momapy.drawing.CurveTo.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>X coordinate.</p>"},{"location":"api_reference/drawing/#momapy.drawing.CurveTo.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Y coordinate.</p>"},{"location":"api_reference/drawing/#momapy.drawing.DrawingElement","title":"DrawingElement  <code>dataclass</code>","text":"<pre><code>DrawingElement(*, class_: str | None = None, fill: NoneValueType | Color | None = None, fill_rule: FillRule | None = None, filter_: NoneValueType | Filter | None = None, font_family: str | None = None, font_size: float | None = None, font_style: FontStyle | None = None, font_weight: FontWeight | int | None = None, id_: str | None = None, stroke: NoneValueType | Color | None = None, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: NoneValueType | float | None = None, text_anchor: TextAnchor | None = None, transform: NoneValueType | tuple[Transformation, ...] | None = None)\n</code></pre> <p>               Bases: <code>ABC</code></p> <p>Abstract base class for drawing elements.</p> <p>Drawing elements carry the SVG-like presentation attributes shared by all primitives.</p> <p>Parameters:</p> Name Type Description Default <code>class_</code> <code>str | None</code> <p>The class name of the drawing element</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the drawing element</p> <code>None</code> <code>fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the drawing element</p> <code>None</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the drawing element</p> <code>None</code> <code>font_family</code> <code>str | None</code> <p>The font family of the drawing element</p> <code>None</code> <code>font_size</code> <code>float | None</code> <p>The font size of the drawing element</p> <code>None</code> <code>font_style</code> <code>FontStyle | None</code> <p>The font style of the drawing element</p> <code>None</code> <code>font_weight</code> <code>FontWeight | int | None</code> <p>The font weight of the drawing element</p> <code>None</code> <code>id_</code> <code>str | None</code> <p>The id of the drawing element</p> <code>None</code> <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the drawing element</p> <code>None</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the drawing element</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the drawing element</p> <code>None</code> <code>stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the drawing element</p> <code>None</code> <code>text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the drawing element</p> <code>None</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the drawing element</p> <code>None</code> <p>Methods:</p> Name Description <code>bbox</code> <p>Get the bounding box.</p> <code>get_filter_region</code> <p>Get the filter region.</p> <code>to_geometry</code> <p>Convert to a list of geometry primitives.</p>"},{"location":"api_reference/drawing/#momapy.drawing.DrawingElement.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Get the bounding box.</p> <p>Returns:</p> Type Description <code>Bbox</code> <p>The bounding box.</p> Source code in <code>src/momapy/drawing.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Get the bounding box.\n\n    Returns:\n        The bounding box.\n    \"\"\"\n    primitives = self.to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0.0, 0.0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/drawing/#momapy.drawing.DrawingElement.get_filter_region","title":"get_filter_region","text":"<pre><code>get_filter_region() -&gt; Bbox | None\n</code></pre> <p>Get the filter region.</p> <p>Numeric <code>x</code>/<code>y</code>/<code>width</code>/<code>height</code> are interpreted as fractions of the element bounding box under <code>OBJECT_BOUNDING_BOX</code>, and as absolute user-space coordinates under <code>USER_SPACE_ON_USE</code>. Percentage-string values (e.g. the default <code>\"-10%\"</code>/<code>\"120%\"</code>) are always resolved relative to the element bounding box, under either unit system.</p> <p>Returns:</p> Type Description <code>Bbox | None</code> <p>The filter region bbox, or <code>None</code> when the element has no</p> <code>Bbox | None</code> <p>filter (<code>filter_</code> is <code>None</code> or <code>NoneValue</code>).</p> Source code in <code>src/momapy/drawing.py</code> <pre><code>def get_filter_region(self) -&gt; Bbox | None:\n    \"\"\"Get the filter region.\n\n    Numeric ``x``/``y``/``width``/``height`` are interpreted as fractions of\n    the element bounding box under ``OBJECT_BOUNDING_BOX``, and as absolute\n    user-space coordinates under ``USER_SPACE_ON_USE``. Percentage-string\n    values (e.g. the default ``\"-10%\"``/``\"120%\"``) are always resolved\n    relative to the element bounding box, under either unit system.\n\n    Returns:\n        The filter region bbox, or `None` when the element has no\n        filter (`filter_` is `None` or `NoneValue`).\n    \"\"\"\n    if self.filter_ is None or self.filter_ is NoneValue:\n        return None\n    bbox = self.bbox()\n    north_west = bbox.north_west()\n    object_bounding_box = (\n        self.filter_.filter_units == FilterUnits.OBJECT_BOUNDING_BOX\n    )\n\n    def resolve_position(value: float | str, origin: float, extent: float) -&gt; float:\n        if isinstance(value, str):\n            return origin + extent * (float(value.rstrip(\"%\")) / 100)\n        if object_bounding_box:\n            return origin + extent * value\n        return value\n\n    def resolve_size(value: float | str, extent: float) -&gt; float:\n        if isinstance(value, str):\n            return extent * (float(value.rstrip(\"%\")) / 100)\n        if object_bounding_box:\n            return extent * value\n        return value\n\n    px = resolve_position(self.filter_.x, north_west.x, bbox.width)\n    py = resolve_position(self.filter_.y, north_west.y, bbox.height)\n    width = resolve_size(self.filter_.width, bbox.width)\n    height = resolve_size(self.filter_.height, bbox.height)\n    return Bbox(\n        Point(px + width / 2, py + height / 2),\n        width,\n        height,\n    )\n</code></pre>"},{"location":"api_reference/drawing/#momapy.drawing.DrawingElement.to_geometry","title":"to_geometry  <code>abstractmethod</code>","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Convert to a list of geometry primitives.</p> <p>Returns:</p> Type Description <code>list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]</code> <p>A list of geometry primitives.</p> Source code in <code>src/momapy/drawing.py</code> <pre><code>@abc.abstractmethod\ndef to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Convert to a list of geometry primitives.\n\n    Returns:\n        A list of geometry primitives.\n    \"\"\"\n    pass\n</code></pre>"},{"location":"api_reference/drawing/#momapy.drawing.DropShadowEffect","title":"DropShadowEffect  <code>dataclass</code>","text":"<pre><code>DropShadowEffect(*, result: str | None = None, dx: float = 0.0, dy: float = 0.0, std_deviation: float = 0.0, flood_opacity: float = 1.0, flood_color: Color = black)\n</code></pre> <p>               Bases: <code>FilterEffect</code></p> <p>Drop shadow filter effect.</p> <p>Casts a blurred, offset, colored copy of the source graphic behind it.</p> <p>Parameters:</p> Name Type Description Default <code>result</code> <code>str | None</code> <p>The name of the result</p> <code>None</code> <code>dx</code> <code>float</code> <p>The horizontal offset of the shadow</p> <code>0.0</code> <code>dy</code> <code>float</code> <p>The vertical offset of the shadow</p> <code>0.0</code> <code>std_deviation</code> <code>float</code> <p>The standard deviation to be used to compute the shadow</p> <code>0.0</code> <code>flood_opacity</code> <code>float</code> <p>The flood opacity of the shadow</p> <code>1.0</code> <code>flood_color</code> <code>Color</code> <p>The color of the shadow</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <p>Methods:</p> Name Description <code>to_compat</code> <p>Convert to more compatible filter effects.</p>"},{"location":"api_reference/drawing/#momapy.drawing.DropShadowEffect.to_compat","title":"to_compat","text":"<pre><code>to_compat() -&gt; list[FilterEffect]\n</code></pre> <p>Convert to more compatible filter effects.</p> <p>Returns:</p> Type Description <code>list[FilterEffect]</code> <p>List of equivalent filter effects.</p> Source code in <code>src/momapy/drawing.py</code> <pre><code>def to_compat(self) -&gt; list[FilterEffect]:\n    \"\"\"Convert to more compatible filter effects.\n\n    Returns:\n        List of equivalent filter effects.\n    \"\"\"\n    flood_effect = FloodEffect(\n        result=make_uuid4_as_str(),\n        flood_opacity=self.flood_opacity,\n        flood_color=self.flood_color,\n    )\n    composite_effect1 = CompositeEffect(\n        in_=flood_effect.result,\n        in2=FilterEffectInput.SOURCE_GRAPHIC,\n        operator=CompositionOperator.IN,\n        result=make_uuid4_as_str(),\n    )\n    gaussian_blur_effect = GaussianBlurEffect(\n        in_=composite_effect1.result,\n        std_deviation=self.std_deviation,\n        result=make_uuid4_as_str(),\n    )\n    offset_effect = OffsetEffect(\n        in_=gaussian_blur_effect.result,\n        dx=self.dx,\n        dy=self.dy,\n        result=make_uuid4_as_str(),\n    )\n    composite_effect2 = CompositeEffect(\n        in_=FilterEffectInput.SOURCE_GRAPHIC,\n        in2=offset_effect.result,\n        operator=CompositionOperator.OVER,\n        result=self.result,\n    )\n    effects = [\n        flood_effect,\n        composite_effect1,\n        gaussian_blur_effect,\n        offset_effect,\n        composite_effect2,\n    ]\n    return effects\n</code></pre>"},{"location":"api_reference/drawing/#momapy.drawing.EdgeMode","title":"EdgeMode","text":"<p>               Bases: <code>Enum</code></p> <p>Edge mode for blur effects.</p> <p>Enumerates how a blur effect treats pixels beyond the input edges.</p>"},{"location":"api_reference/drawing/#momapy.drawing.Ellipse","title":"Ellipse  <code>dataclass</code>","text":"<pre><code>Ellipse(*, class_: str | None = None, fill: NoneValueType | Color | None = None, fill_rule: FillRule | None = None, filter_: NoneValueType | Filter | None = None, font_family: str | None = None, font_size: float | None = None, font_style: FontStyle | None = None, font_weight: FontWeight | int | None = None, id_: str | None = None, stroke: NoneValueType | Color | None = None, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: NoneValueType | float | None = None, text_anchor: TextAnchor | None = None, transform: NoneValueType | tuple[Transformation, ...] | None = None, point: Point, rx: float, ry: float)\n</code></pre> <p>               Bases: <code>DrawingElement</code></p> <p>Ellipse drawing element.</p> <p>Draws an ellipse from a center point and two radii.</p> <p>Parameters:</p> Name Type Description Default <code>class_</code> <code>str | None</code> <p>The class name of the drawing element</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the drawing element</p> <code>None</code> <code>fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the drawing element</p> <code>None</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the drawing element</p> <code>None</code> <code>font_family</code> <code>str | None</code> <p>The font family of the drawing element</p> <code>None</code> <code>font_size</code> <code>float | None</code> <p>The font size of the drawing element</p> <code>None</code> <code>font_style</code> <code>FontStyle | None</code> <p>The font style of the drawing element</p> <code>None</code> <code>font_weight</code> <code>FontWeight | int | None</code> <p>The font weight of the drawing element</p> <code>None</code> <code>id_</code> <code>str | None</code> <p>The id of the drawing element</p> <code>None</code> <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the drawing element</p> <code>None</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the drawing element</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the drawing element</p> <code>None</code> <code>stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the drawing element</p> <code>None</code> <code>text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the drawing element</p> <code>None</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the drawing element</p> <code>None</code> <code>point</code> <code>Point</code> <p>The point of the ellipse</p> required <code>rx</code> <code>float</code> <p>The x-radius of the ellipse</p> required <code>ry</code> <code>float</code> <p>The y-radius of the ellipse</p> required <p>Methods:</p> Name Description <code>__post_init__</code> <p>Round the radii to the rounding precision after initialization.</p> <code>bbox</code> <p>Get the bounding box.</p> <code>get_filter_region</code> <p>Get the filter region.</p> <code>to_geometry</code> <p>Convert to a list of geometry primitives.</p> <code>to_path</code> <p>Convert to a Path.</p> <code>transformed</code> <p>Apply a transformation.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>X coordinate of center.</p> <code>y</code> <code>float</code> <p>Y coordinate of center.</p>"},{"location":"api_reference/drawing/#momapy.drawing.Ellipse.__post_init__","title":"__post_init__","text":"<pre><code>__post_init__() -&gt; None\n</code></pre> <p>Round the radii to the rounding precision after initialization.</p> Source code in <code>src/momapy/drawing.py</code> <pre><code>def __post_init__(self) -&gt; None:\n    \"\"\"Round the radii to the rounding precision after initialization.\"\"\"\n    object.__setattr__(self, \"rx\", round(self.rx, ROUNDING))\n    object.__setattr__(self, \"ry\", round(self.ry, ROUNDING))\n</code></pre>"},{"location":"api_reference/drawing/#momapy.drawing.Ellipse.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Get the bounding box.</p> <p>Returns:</p> Type Description <code>Bbox</code> <p>The bounding box.</p> Source code in <code>src/momapy/drawing.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Get the bounding box.\n\n    Returns:\n        The bounding box.\n    \"\"\"\n    primitives = self.to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0.0, 0.0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/drawing/#momapy.drawing.Ellipse.get_filter_region","title":"get_filter_region","text":"<pre><code>get_filter_region() -&gt; Bbox | None\n</code></pre> <p>Get the filter region.</p> <p>Numeric <code>x</code>/<code>y</code>/<code>width</code>/<code>height</code> are interpreted as fractions of the element bounding box under <code>OBJECT_BOUNDING_BOX</code>, and as absolute user-space coordinates under <code>USER_SPACE_ON_USE</code>. Percentage-string values (e.g. the default <code>\"-10%\"</code>/<code>\"120%\"</code>) are always resolved relative to the element bounding box, under either unit system.</p> <p>Returns:</p> Type Description <code>Bbox | None</code> <p>The filter region bbox, or <code>None</code> when the element has no</p> <code>Bbox | None</code> <p>filter (<code>filter_</code> is <code>None</code> or <code>NoneValue</code>).</p> Source code in <code>src/momapy/drawing.py</code> <pre><code>def get_filter_region(self) -&gt; Bbox | None:\n    \"\"\"Get the filter region.\n\n    Numeric ``x``/``y``/``width``/``height`` are interpreted as fractions of\n    the element bounding box under ``OBJECT_BOUNDING_BOX``, and as absolute\n    user-space coordinates under ``USER_SPACE_ON_USE``. Percentage-string\n    values (e.g. the default ``\"-10%\"``/``\"120%\"``) are always resolved\n    relative to the element bounding box, under either unit system.\n\n    Returns:\n        The filter region bbox, or `None` when the element has no\n        filter (`filter_` is `None` or `NoneValue`).\n    \"\"\"\n    if self.filter_ is None or self.filter_ is NoneValue:\n        return None\n    bbox = self.bbox()\n    north_west = bbox.north_west()\n    object_bounding_box = (\n        self.filter_.filter_units == FilterUnits.OBJECT_BOUNDING_BOX\n    )\n\n    def resolve_position(value: float | str, origin: float, extent: float) -&gt; float:\n        if isinstance(value, str):\n            return origin + extent * (float(value.rstrip(\"%\")) / 100)\n        if object_bounding_box:\n            return origin + extent * value\n        return value\n\n    def resolve_size(value: float | str, extent: float) -&gt; float:\n        if isinstance(value, str):\n            return extent * (float(value.rstrip(\"%\")) / 100)\n        if object_bounding_box:\n            return extent * value\n        return value\n\n    px = resolve_position(self.filter_.x, north_west.x, bbox.width)\n    py = resolve_position(self.filter_.y, north_west.y, bbox.height)\n    width = resolve_size(self.filter_.width, bbox.width)\n    height = resolve_size(self.filter_.height, bbox.height)\n    return Bbox(\n        Point(px + width / 2, py + height / 2),\n        width,\n        height,\n    )\n</code></pre>"},{"location":"api_reference/drawing/#momapy.drawing.Ellipse.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Convert to a list of geometry primitives.</p> <p>Returns:</p> Type Description <code>list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]</code> <p>A list of geometry primitives.</p> Source code in <code>src/momapy/drawing.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Convert to a list of geometry primitives.\n\n    Returns:\n        A list of geometry primitives.\n    \"\"\"\n    return self.to_path().to_geometry()\n</code></pre>"},{"location":"api_reference/drawing/#momapy.drawing.Ellipse.to_path","title":"to_path","text":"<pre><code>to_path() -&gt; Path\n</code></pre> <p>Convert to a Path.</p> <p>Returns:</p> Type Description <code>Path</code> <p>A Path approximating the ellipse.</p> Source code in <code>src/momapy/drawing.py</code> <pre><code>def to_path(self) -&gt; Path:\n    \"\"\"Convert to a Path.\n\n    Returns:\n        A Path approximating the ellipse.\n    \"\"\"\n    north = self.point + (0, -self.ry)\n    east = self.point + (self.rx, 0)\n    south = self.point + (0, self.ry)\n    west = self.point - (self.rx, 0)\n    actions = [\n        MoveTo(north),\n        EllipticalArc(east, self.rx, self.ry, 0, 0, 1),\n        EllipticalArc(south, self.rx, self.ry, 0, 0, 1),\n        EllipticalArc(west, self.rx, self.ry, 0, 0, 1),\n        EllipticalArc(north, self.rx, self.ry, 0, 0, 1),\n        ClosePath(),\n    ]\n    path = Path(\n        stroke_width=self.stroke_width,\n        stroke=self.stroke,\n        fill=self.fill,\n        transform=self.transform,\n        filter_=self.filter_,\n        actions=actions,\n    )\n    return path\n</code></pre>"},{"location":"api_reference/drawing/#momapy.drawing.Ellipse.transformed","title":"transformed","text":"<pre><code>transformed(transformation: Transformation) -&gt; Path\n</code></pre> <p>Apply a transformation.</p> <p>Parameters:</p> Name Type Description Default <code>transformation</code> <code>Transformation</code> <p>The transformation.</p> required <p>Returns:</p> Type Description <code>Path</code> <p>A transformed Path.</p> Source code in <code>src/momapy/drawing.py</code> <pre><code>def transformed(self, transformation: Transformation) -&gt; Path:\n    \"\"\"Apply a transformation.\n\n    Args:\n        transformation: The transformation.\n\n    Returns:\n        A transformed Path.\n    \"\"\"\n    path = self.to_path()\n    return path.transformed(transformation)\n</code></pre>"},{"location":"api_reference/drawing/#momapy.drawing.Ellipse.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>X coordinate of center.</p>"},{"location":"api_reference/drawing/#momapy.drawing.Ellipse.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Y coordinate of center.</p>"},{"location":"api_reference/drawing/#momapy.drawing.EllipticalArc","title":"EllipticalArc  <code>dataclass</code>","text":"<pre><code>EllipticalArc(point: Point, rx: float, ry: float, x_axis_rotation: float, arc_flag: int, sweep_flag: int)\n</code></pre> <p>               Bases: <code>PathAction</code></p> <p>Draw an elliptical arc.</p> <p>Draws an arc of an ellipse from the current point to the end point.</p> <p>Parameters:</p> Name Type Description Default <code>point</code> <code>Point</code> <p>The point of the elliptical arc action</p> required <code>rx</code> <code>float</code> <p>The x-radius of the elliptical arc action</p> required <code>ry</code> <code>float</code> <p>The y-radius of the elliptical arc action</p> required <code>x_axis_rotation</code> <code>float</code> <p>The x axis rotation of the elliptical arc action</p> required <code>arc_flag</code> <code>int</code> <p>The arc flag of the elliptical arc action</p> required <code>sweep_flag</code> <code>int</code> <p>The sweep flag of the elliptical arc action</p> required <p>Methods:</p> Name Description <code>to_geometry</code> <p>Convert to geometry.</p> <code>transformed</code> <p>Apply a transformation.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>X coordinate.</p> <code>y</code> <code>float</code> <p>Y coordinate.</p>"},{"location":"api_reference/drawing/#momapy.drawing.EllipticalArc.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry(current_point: Point) -&gt; EllipticalArc\n</code></pre> <p>Convert to geometry.</p> <p>Parameters:</p> Name Type Description Default <code>current_point</code> <code>Point</code> <p>Start point.</p> required <p>Returns:</p> Type Description <code>EllipticalArc</code> <p>An EllipticalArc geometry.</p> Source code in <code>src/momapy/drawing.py</code> <pre><code>def to_geometry(self, current_point: Point) -&gt; GeometryEllipticalArc:\n    \"\"\"Convert to geometry.\n\n    Args:\n        current_point: Start point.\n\n    Returns:\n        An EllipticalArc geometry.\n    \"\"\"\n    return GeometryEllipticalArc(\n        current_point,\n        self.point,\n        self.rx,\n        self.ry,\n        self.x_axis_rotation,\n        self.arc_flag,\n        self.sweep_flag,\n    )\n</code></pre>"},{"location":"api_reference/drawing/#momapy.drawing.EllipticalArc.transformed","title":"transformed","text":"<pre><code>transformed(transformation: Transformation, current_point: Point) -&gt; Self\n</code></pre> <p>Apply a transformation.</p> <p>Parameters:</p> Name Type Description Default <code>transformation</code> <code>Transformation</code> <p>The transformation.</p> required <code>current_point</code> <code>Point</code> <p>Start point.</p> required <p>Returns:</p> Type Description <code>Self</code> <p>A new EllipticalArc with transformed parameters.</p> Source code in <code>src/momapy/drawing.py</code> <pre><code>def transformed(\n    self,\n    transformation: Transformation,\n    current_point: Point,\n) -&gt; typing_extensions.Self:\n    \"\"\"Apply a transformation.\n\n    Args:\n        transformation: The transformation.\n        current_point: Start point.\n\n    Returns:\n        A new EllipticalArc with transformed parameters.\n    \"\"\"\n    rotation = self.x_axis_rotation\n    east = Point(\n        math.cos(rotation) * self.rx,\n        math.sin(rotation) * self.rx,\n    )\n    north = Point(\n        -math.sin(rotation) * self.ry,\n        math.cos(rotation) * self.ry,\n    )\n    new_center = Point(0.0, 0.0).transformed(transformation)\n    new_east = east.transformed(transformation)\n    new_north = north.transformed(transformation)\n    new_rx = Segment(new_center, new_east).length()\n    new_ry = Segment(new_center, new_north).length()\n    new_end_point = self.point.transformed(transformation)\n    new_x_axis_rotation = Line(new_center, new_east).get_angle_to_horizontal()\n    return EllipticalArc(\n        new_end_point,\n        new_rx,\n        new_ry,\n        new_x_axis_rotation,\n        self.arc_flag,\n        self.sweep_flag,\n    )\n</code></pre>"},{"location":"api_reference/drawing/#momapy.drawing.EllipticalArc.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>X coordinate.</p>"},{"location":"api_reference/drawing/#momapy.drawing.EllipticalArc.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Y coordinate.</p>"},{"location":"api_reference/drawing/#momapy.drawing.FillRule","title":"FillRule","text":"<p>               Bases: <code>Enum</code></p> <p>Fill rule for complex shapes.</p> <p>Enumerates the algorithms used to determine the interior of a shape.</p>"},{"location":"api_reference/drawing/#momapy.drawing.Filter","title":"Filter  <code>dataclass</code>","text":"<pre><code>Filter(*, id_: str = make_uuid4_as_str(), filter_units: FilterUnits = OBJECT_BOUNDING_BOX, effects: tuple[FilterEffect, ...] = tuple(), width: float | str = '120%', height: float | str = '120%', x: float | str = '-10%', y: float | str = '-10%')\n</code></pre> <p>               Bases: <code>object</code></p> <p>Filter with multiple effects.</p> <p>A filter applies a chain of effects to a drawing element within a region.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>Generate a UUID4 as a string.</p> <p>Returns:     A string representation of a random UUID (version 4).</p> <p>Examples:     <pre><code>uuid_str = make_uuid4_as_str()\nisinstance(uuid_str, str)\nlen(uuid_str)  # Standard UUID string length\n</code></pre></p> <code>'1f913308-2621-4dc0-b448-6d17bcb34d3a'</code> <code>filter_units</code> <code>FilterUnits</code> <p>The units of the filter region</p> <code>&lt;FilterUnits.OBJECT_BOUNDING_BOX: 'OBJECT_BOUNDING_BOX'&gt;</code> <code>effects</code> <code>tuple[FilterEffect, ...]</code> <p>Built-in immutable sequence.</p> <p>If no argument is given, the constructor returns an empty tuple. If iterable is specified the tuple is initialized from iterable's items.</p> <p>If the argument is a tuple, the return value is the same object.</p> <code>&lt;dynamic&gt;</code> <code>width</code> <code>float | str</code> <p>The width of the filter region</p> <code>'120%'</code> <code>height</code> <code>float | str</code> <p>The height of the filter region</p> <code>'120%'</code> <code>x</code> <code>float | str</code> <p>The x position of the filter region</p> <code>'-10%'</code> <code>y</code> <code>float | str</code> <p>The y position of the filter region</p> <code>'-10%'</code> <p>Methods:</p> Name Description <code>to_compat</code> <p>Convert to compatible filter with simpler effects.</p>"},{"location":"api_reference/drawing/#momapy.drawing.Filter.to_compat","title":"to_compat","text":"<pre><code>to_compat() -&gt; Self\n</code></pre> <p>Convert to compatible filter with simpler effects.</p> <p>Returns:</p> Type Description <code>Self</code> <p>A filter with effects replaced by simpler equivalents.</p> Source code in <code>src/momapy/drawing.py</code> <pre><code>def to_compat(self) -&gt; typing_extensions.Self:\n    \"\"\"Convert to compatible filter with simpler effects.\n\n    Returns:\n        A filter with effects replaced by simpler equivalents.\n    \"\"\"\n    effects = []\n    for effect in self.effects:\n        if hasattr(effect, \"to_compat\"):\n            effects += effect.to_compat()\n        else:\n            effects.append(effect)\n    return dataclasses.replace(self, effects=tuple(effects))\n</code></pre>"},{"location":"api_reference/drawing/#momapy.drawing.FilterEffect","title":"FilterEffect  <code>dataclass</code>","text":"<pre><code>FilterEffect(*, result: str | None = None)\n</code></pre> <p>               Bases: <code>ABC</code></p> <p>Abstract base class for filter effects.</p> <p>Filter effects are the individual operations composed inside a filter.</p> <p>Parameters:</p> Name Type Description Default <code>result</code> <code>str | None</code> <p>The name of the result</p> <code>None</code>"},{"location":"api_reference/drawing/#momapy.drawing.FilterEffectInput","title":"FilterEffectInput","text":"<p>               Bases: <code>Enum</code></p> <p>Filter effect input types.</p> <p>Enumerates the predefined source images that a filter effect can read from.</p>"},{"location":"api_reference/drawing/#momapy.drawing.FilterUnits","title":"FilterUnits","text":"<p>               Bases: <code>Enum</code></p> <p>Units for filter regions.</p> <p>Enumerates the coordinate systems in which a filter region is expressed.</p>"},{"location":"api_reference/drawing/#momapy.drawing.FloodEffect","title":"FloodEffect  <code>dataclass</code>","text":"<pre><code>FloodEffect(*, result: str | None = None, flood_color: Color = black, flood_opacity: float = 1.0)\n</code></pre> <p>               Bases: <code>FilterEffect</code></p> <p>Flood filter effect.</p> <p>Fills the filter region with a solid color at a given opacity.</p> <p>Parameters:</p> Name Type Description Default <code>result</code> <code>str | None</code> <p>The name of the result</p> <code>None</code> <code>flood_color</code> <code>Color</code> <p>The color of the flood effect</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>flood_opacity</code> <code>float</code> <p>The opacity of the flood effect</p> <code>1.0</code>"},{"location":"api_reference/drawing/#momapy.drawing.FontStyle","title":"FontStyle","text":"<p>               Bases: <code>Enum</code></p> <p>Font style options.</p> <p>Enumerates the slant variants a font can be rendered with.</p>"},{"location":"api_reference/drawing/#momapy.drawing.FontWeight","title":"FontWeight","text":"<p>               Bases: <code>Enum</code></p> <p>Font weight options.</p> <p>Enumerates the thickness variants a font can be rendered with.</p>"},{"location":"api_reference/drawing/#momapy.drawing.GaussianBlurEffect","title":"GaussianBlurEffect  <code>dataclass</code>","text":"<pre><code>GaussianBlurEffect(*, result: str | None = None, in_: FilterEffectInput | str | None = None, std_deviation: float = 0.0, edge_mode: NoneValueType | EdgeMode = NoneValue)\n</code></pre> <p>               Bases: <code>FilterEffect</code></p> <p>Gaussian blur filter effect.</p> <p>Blurs its input with a Gaussian kernel of the given standard deviation.</p> <p>Parameters:</p> Name Type Description Default <code>result</code> <code>str | None</code> <p>The name of the result</p> <code>None</code> <code>in_</code> <code>FilterEffectInput | str | None</code> <p>The effect input or the name of the filter effect input</p> <code>None</code> <code>std_deviation</code> <code>float</code> <p>The standard deviation of the Gaussian blur</p> <code>0.0</code> <code>edge_mode</code> <code>NoneValueType | EdgeMode</code> <p>The edge handling mode of the blur effect</p> <code>&lt;momapy.drawing.NoneValueType object at 0x7f8b17323e00&gt;</code>"},{"location":"api_reference/drawing/#momapy.drawing.Group","title":"Group  <code>dataclass</code>","text":"<pre><code>Group(*, class_: str | None = None, fill: NoneValueType | Color | None = None, fill_rule: FillRule | None = None, filter_: NoneValueType | Filter | None = None, font_family: str | None = None, font_size: float | None = None, font_style: FontStyle | None = None, font_weight: FontWeight | int | None = None, id_: str | None = None, stroke: NoneValueType | Color | None = None, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: NoneValueType | float | None = None, text_anchor: TextAnchor | None = None, transform: NoneValueType | tuple[Transformation, ...] | None = None, elements: tuple[DrawingElement, ...] = tuple())\n</code></pre> <p>               Bases: <code>DrawingElement</code></p> <p>Group of drawing elements.</p> <p>Bundles several drawing elements so they share presentation attributes.</p> <p>Parameters:</p> Name Type Description Default <code>class_</code> <code>str | None</code> <p>The class name of the drawing element</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the drawing element</p> <code>None</code> <code>fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the drawing element</p> <code>None</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the drawing element</p> <code>None</code> <code>font_family</code> <code>str | None</code> <p>The font family of the drawing element</p> <code>None</code> <code>font_size</code> <code>float | None</code> <p>The font size of the drawing element</p> <code>None</code> <code>font_style</code> <code>FontStyle | None</code> <p>The font style of the drawing element</p> <code>None</code> <code>font_weight</code> <code>FontWeight | int | None</code> <p>The font weight of the drawing element</p> <code>None</code> <code>id_</code> <code>str | None</code> <p>The id of the drawing element</p> <code>None</code> <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the drawing element</p> <code>None</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the drawing element</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the drawing element</p> <code>None</code> <code>stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the drawing element</p> <code>None</code> <code>text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the drawing element</p> <code>None</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the drawing element</p> <code>None</code> <code>elements</code> <code>tuple[DrawingElement, ...]</code> <p>The elements of the group element</p> <code>&lt;dynamic&gt;</code> <p>Methods:</p> Name Description <code>bbox</code> <p>Get the bounding box.</p> <code>get_filter_region</code> <p>Get the filter region.</p> <code>to_geometry</code> <p>Convert to a list of geometry primitives.</p> <code>transformed</code> <p>Apply a transformation to all elements.</p>"},{"location":"api_reference/drawing/#momapy.drawing.Group.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Get the bounding box.</p> <p>Returns:</p> Type Description <code>Bbox</code> <p>The bounding box.</p> Source code in <code>src/momapy/drawing.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Get the bounding box.\n\n    Returns:\n        The bounding box.\n    \"\"\"\n    primitives = self.to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0.0, 0.0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/drawing/#momapy.drawing.Group.get_filter_region","title":"get_filter_region","text":"<pre><code>get_filter_region() -&gt; Bbox | None\n</code></pre> <p>Get the filter region.</p> <p>Numeric <code>x</code>/<code>y</code>/<code>width</code>/<code>height</code> are interpreted as fractions of the element bounding box under <code>OBJECT_BOUNDING_BOX</code>, and as absolute user-space coordinates under <code>USER_SPACE_ON_USE</code>. Percentage-string values (e.g. the default <code>\"-10%\"</code>/<code>\"120%\"</code>) are always resolved relative to the element bounding box, under either unit system.</p> <p>Returns:</p> Type Description <code>Bbox | None</code> <p>The filter region bbox, or <code>None</code> when the element has no</p> <code>Bbox | None</code> <p>filter (<code>filter_</code> is <code>None</code> or <code>NoneValue</code>).</p> Source code in <code>src/momapy/drawing.py</code> <pre><code>def get_filter_region(self) -&gt; Bbox | None:\n    \"\"\"Get the filter region.\n\n    Numeric ``x``/``y``/``width``/``height`` are interpreted as fractions of\n    the element bounding box under ``OBJECT_BOUNDING_BOX``, and as absolute\n    user-space coordinates under ``USER_SPACE_ON_USE``. Percentage-string\n    values (e.g. the default ``\"-10%\"``/``\"120%\"``) are always resolved\n    relative to the element bounding box, under either unit system.\n\n    Returns:\n        The filter region bbox, or `None` when the element has no\n        filter (`filter_` is `None` or `NoneValue`).\n    \"\"\"\n    if self.filter_ is None or self.filter_ is NoneValue:\n        return None\n    bbox = self.bbox()\n    north_west = bbox.north_west()\n    object_bounding_box = (\n        self.filter_.filter_units == FilterUnits.OBJECT_BOUNDING_BOX\n    )\n\n    def resolve_position(value: float | str, origin: float, extent: float) -&gt; float:\n        if isinstance(value, str):\n            return origin + extent * (float(value.rstrip(\"%\")) / 100)\n        if object_bounding_box:\n            return origin + extent * value\n        return value\n\n    def resolve_size(value: float | str, extent: float) -&gt; float:\n        if isinstance(value, str):\n            return extent * (float(value.rstrip(\"%\")) / 100)\n        if object_bounding_box:\n            return extent * value\n        return value\n\n    px = resolve_position(self.filter_.x, north_west.x, bbox.width)\n    py = resolve_position(self.filter_.y, north_west.y, bbox.height)\n    width = resolve_size(self.filter_.width, bbox.width)\n    height = resolve_size(self.filter_.height, bbox.height)\n    return Bbox(\n        Point(px + width / 2, py + height / 2),\n        width,\n        height,\n    )\n</code></pre>"},{"location":"api_reference/drawing/#momapy.drawing.Group.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Convert to a list of geometry primitives.</p> <p>Returns:</p> Type Description <code>list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]</code> <p>A list of geometry primitives from all child elements.</p> Source code in <code>src/momapy/drawing.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Convert to a list of geometry primitives.\n\n    Returns:\n        A list of geometry primitives from all child elements.\n    \"\"\"\n    return drawing_elements_to_geometry(self.elements)\n</code></pre>"},{"location":"api_reference/drawing/#momapy.drawing.Group.transformed","title":"transformed","text":"<pre><code>transformed(transformation: Transformation) -&gt; Self\n</code></pre> <p>Apply a transformation to all elements.</p> <p>Parameters:</p> Name Type Description Default <code>transformation</code> <code>Transformation</code> <p>The transformation.</p> required <p>Returns:</p> Type Description <code>Self</code> <p>A new Group with transformed elements.</p> Source code in <code>src/momapy/drawing.py</code> <pre><code>def transformed(self, transformation: Transformation) -&gt; typing_extensions.Self:\n    \"\"\"Apply a transformation to all elements.\n\n    Args:\n        transformation: The transformation.\n\n    Returns:\n        A new Group with transformed elements.\n    \"\"\"\n    elements = []\n    for element in self.elements:\n        elements.append(element.transformed(transformation))\n    return dataclasses.replace(self, elements=tuple(elements))\n</code></pre>"},{"location":"api_reference/drawing/#momapy.drawing.LineTo","title":"LineTo  <code>dataclass</code>","text":"<pre><code>LineTo(point: Point)\n</code></pre> <p>               Bases: <code>PathAction</code></p> <p>Draw a line to a point.</p> <p>Draws a straight segment from the current point to the end point.</p> <p>Parameters:</p> Name Type Description Default <code>point</code> <code>Point</code> <p>The point of the line to action</p> required <p>Methods:</p> Name Description <code>to_geometry</code> <p>Convert to a segment.</p> <code>transformed</code> <p>Apply a transformation.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>X coordinate.</p> <code>y</code> <code>float</code> <p>Y coordinate.</p>"},{"location":"api_reference/drawing/#momapy.drawing.LineTo.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry(current_point: Point) -&gt; Segment\n</code></pre> <p>Convert to a segment.</p> <p>Parameters:</p> Name Type Description Default <code>current_point</code> <code>Point</code> <p>Start point.</p> required <p>Returns:</p> Type Description <code>Segment</code> <p>A Segment from current_point to self.point.</p> Source code in <code>src/momapy/drawing.py</code> <pre><code>def to_geometry(self, current_point: Point) -&gt; Segment:\n    \"\"\"Convert to a segment.\n\n    Args:\n        current_point: Start point.\n\n    Returns:\n        A Segment from current_point to self.point.\n    \"\"\"\n    return Segment(current_point, self.point)\n</code></pre>"},{"location":"api_reference/drawing/#momapy.drawing.LineTo.transformed","title":"transformed","text":"<pre><code>transformed(transformation: Transformation, current_point: Point) -&gt; Self\n</code></pre> <p>Apply a transformation.</p> <p>Parameters:</p> Name Type Description Default <code>transformation</code> <code>Transformation</code> <p>The transformation.</p> required <code>current_point</code> <code>Point</code> <p>Current point (unused).</p> required <p>Returns:</p> Type Description <code>Self</code> <p>A new LineTo with transformed point.</p> Source code in <code>src/momapy/drawing.py</code> <pre><code>def transformed(\n    self,\n    transformation: Transformation,\n    current_point: Point,\n) -&gt; typing_extensions.Self:\n    \"\"\"Apply a transformation.\n\n    Args:\n        transformation: The transformation.\n        current_point: Current point (unused).\n\n    Returns:\n        A new LineTo with transformed point.\n    \"\"\"\n    return LineTo(self.point.transformed(transformation))\n</code></pre>"},{"location":"api_reference/drawing/#momapy.drawing.LineTo.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>X coordinate.</p>"},{"location":"api_reference/drawing/#momapy.drawing.LineTo.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Y coordinate.</p>"},{"location":"api_reference/drawing/#momapy.drawing.MoveTo","title":"MoveTo  <code>dataclass</code>","text":"<pre><code>MoveTo(point: Point)\n</code></pre> <p>               Bases: <code>PathAction</code></p> <p>Move to a point (start a new subpath).</p> <p>Repositions the current point without drawing, starting a new subpath.</p> <p>Parameters:</p> Name Type Description Default <code>point</code> <code>Point</code> <p>The point of the move to action</p> required <p>Methods:</p> Name Description <code>transformed</code> <p>Apply a transformation.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>X coordinate.</p> <code>y</code> <code>float</code> <p>Y coordinate.</p>"},{"location":"api_reference/drawing/#momapy.drawing.MoveTo.transformed","title":"transformed","text":"<pre><code>transformed(transformation: Transformation, current_point: Point) -&gt; Self\n</code></pre> <p>Apply a transformation.</p> <p>Parameters:</p> Name Type Description Default <code>transformation</code> <code>Transformation</code> <p>The transformation.</p> required <code>current_point</code> <code>Point</code> <p>Current point (unused).</p> required <p>Returns:</p> Type Description <code>Self</code> <p>A new MoveTo with transformed point.</p> Source code in <code>src/momapy/drawing.py</code> <pre><code>def transformed(\n    self,\n    transformation: Transformation,\n    current_point: Point,\n) -&gt; typing_extensions.Self:\n    \"\"\"Apply a transformation.\n\n    Args:\n        transformation: The transformation.\n        current_point: Current point (unused).\n\n    Returns:\n        A new MoveTo with transformed point.\n    \"\"\"\n    return MoveTo(self.point.transformed(transformation))\n</code></pre>"},{"location":"api_reference/drawing/#momapy.drawing.MoveTo.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>X coordinate.</p>"},{"location":"api_reference/drawing/#momapy.drawing.MoveTo.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Y coordinate.</p>"},{"location":"api_reference/drawing/#momapy.drawing.NoneValue","title":"NoneValue  <code>module-attribute</code>","text":"<pre><code>NoneValue = NoneValueType()\n</code></pre> <p>A singleton value for type <code>NoneValueType</code>.</p>"},{"location":"api_reference/drawing/#momapy.drawing.NoneValueType","title":"NoneValueType","text":"<p>               Bases: <code>object</code></p> <p>Singleton type for the SVG <code>\"none\"</code> value (distinct from <code>None</code>).</p> <p>Methods:</p> Name Description <code>__copy__</code> <p>Return the singleton itself, as it is immutable.</p> <code>__deepcopy__</code> <p>Return the singleton itself, as it is immutable.</p> <code>__eq__</code> <p>Return whether the other object is also the <code>NoneValue</code> singleton.</p> <code>__hash__</code> <p>Return the hash of the <code>NoneValue</code> singleton.</p> <code>__reduce__</code> <p>Pickle to the module-level <code>NoneValue</code> singleton.</p>"},{"location":"api_reference/drawing/#momapy.drawing.NoneValueType.__copy__","title":"__copy__","text":"<pre><code>__copy__() -&gt; NoneValueType\n</code></pre> <p>Return the singleton itself, as it is immutable.</p> Source code in <code>src/momapy/drawing.py</code> <pre><code>def __copy__(self) -&gt; \"NoneValueType\":\n    \"\"\"Return the singleton itself, as it is immutable.\"\"\"\n    return self\n</code></pre>"},{"location":"api_reference/drawing/#momapy.drawing.NoneValueType.__deepcopy__","title":"__deepcopy__","text":"<pre><code>__deepcopy__(memo: Any) -&gt; NoneValueType\n</code></pre> <p>Return the singleton itself, as it is immutable.</p> Source code in <code>src/momapy/drawing.py</code> <pre><code>def __deepcopy__(self, memo: typing.Any) -&gt; \"NoneValueType\":\n    \"\"\"Return the singleton itself, as it is immutable.\"\"\"\n    return self\n</code></pre>"},{"location":"api_reference/drawing/#momapy.drawing.NoneValueType.__eq__","title":"__eq__","text":"<pre><code>__eq__(other: Any) -&gt; bool\n</code></pre> <p>Return whether the other object is also the <code>NoneValue</code> singleton.</p> Source code in <code>src/momapy/drawing.py</code> <pre><code>def __eq__(self, other: typing.Any) -&gt; bool:\n    \"\"\"Return whether the other object is also the `NoneValue` singleton.\"\"\"\n    return type(self) is type(other)\n</code></pre>"},{"location":"api_reference/drawing/#momapy.drawing.NoneValueType.__hash__","title":"__hash__","text":"<pre><code>__hash__() -&gt; int\n</code></pre> <p>Return the hash of the <code>NoneValue</code> singleton.</p> Source code in <code>src/momapy/drawing.py</code> <pre><code>def __hash__(self) -&gt; int:\n    \"\"\"Return the hash of the `NoneValue` singleton.\"\"\"\n    return id(NoneValue)\n</code></pre>"},{"location":"api_reference/drawing/#momapy.drawing.NoneValueType.__reduce__","title":"__reduce__","text":"<pre><code>__reduce__() -&gt; str\n</code></pre> <p>Pickle to the module-level <code>NoneValue</code> singleton.</p> <p>Returning the global's name keeps the singleton stable across a pickle round-trip; without it, unpickling builds a fresh <code>NoneValueType</code> instance and <code>value is NoneValue</code> checks (used in the renderers, writers, and core) silently fail. This mirrors <code>__copy__</code>/<code>__deepcopy__</code>, which already return <code>self</code>.</p> Source code in <code>src/momapy/drawing.py</code> <pre><code>def __reduce__(self) -&gt; str:\n    \"\"\"Pickle to the module-level `NoneValue` singleton.\n\n    Returning the global's name keeps the singleton stable across a\n    pickle round-trip; without it, unpickling builds a fresh\n    `NoneValueType` instance and `value is NoneValue` checks (used in the\n    renderers, writers, and core) silently fail. This mirrors\n    `__copy__`/`__deepcopy__`, which already return `self`.\n    \"\"\"\n    return \"NoneValue\"\n</code></pre>"},{"location":"api_reference/drawing/#momapy.drawing.OffsetEffect","title":"OffsetEffect  <code>dataclass</code>","text":"<pre><code>OffsetEffect(*, result: str | None = None, in_: FilterEffectInput | str | None = None, dx: float = 0.0, dy: float = 0.0)\n</code></pre> <p>               Bases: <code>FilterEffect</code></p> <p>Offset filter effect.</p> <p>Shifts its input by a horizontal and vertical offset.</p> <p>Parameters:</p> Name Type Description Default <code>result</code> <code>str | None</code> <p>The name of the result</p> <code>None</code> <code>in_</code> <code>FilterEffectInput | str | None</code> <p>The effect input or the name of the filter effect input</p> <code>None</code> <code>dx</code> <code>float</code> <p>The horizontal offset of the offset effect</p> <code>0.0</code> <code>dy</code> <code>float</code> <p>The vertical offset of the offset effect</p> <code>0.0</code>"},{"location":"api_reference/drawing/#momapy.drawing.Path","title":"Path  <code>dataclass</code>","text":"<pre><code>Path(*, class_: str | None = None, fill: NoneValueType | Color | None = None, fill_rule: FillRule | None = None, filter_: NoneValueType | Filter | None = None, font_family: str | None = None, font_size: float | None = None, font_style: FontStyle | None = None, font_weight: FontWeight | int | None = None, id_: str | None = None, stroke: NoneValueType | Color | None = None, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: NoneValueType | float | None = None, text_anchor: TextAnchor | None = None, transform: NoneValueType | tuple[Transformation, ...] | None = None, actions: tuple[PathAction, ...] = tuple())\n</code></pre> <p>               Bases: <code>DrawingElement</code></p> <p>Path drawing element composed of path actions.</p> <p>A path is drawn by executing its sequence of path actions in order.</p> <p>Parameters:</p> Name Type Description Default <code>class_</code> <code>str | None</code> <p>The class name of the drawing element</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the drawing element</p> <code>None</code> <code>fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the drawing element</p> <code>None</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the drawing element</p> <code>None</code> <code>font_family</code> <code>str | None</code> <p>The font family of the drawing element</p> <code>None</code> <code>font_size</code> <code>float | None</code> <p>The font size of the drawing element</p> <code>None</code> <code>font_style</code> <code>FontStyle | None</code> <p>The font style of the drawing element</p> <code>None</code> <code>font_weight</code> <code>FontWeight | int | None</code> <p>The font weight of the drawing element</p> <code>None</code> <code>id_</code> <code>str | None</code> <p>The id of the drawing element</p> <code>None</code> <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the drawing element</p> <code>None</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the drawing element</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the drawing element</p> <code>None</code> <code>stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the drawing element</p> <code>None</code> <code>text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the drawing element</p> <code>None</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the drawing element</p> <code>None</code> <code>actions</code> <code>tuple[PathAction, ...]</code> <p>The actions of the path</p> <code>&lt;dynamic&gt;</code> <p>Methods:</p> Name Description <code>bbox</code> <p>Get the bounding box.</p> <code>get_filter_region</code> <p>Get the filter region.</p> <code>to_geometry</code> <p>Convert to a list of geometry primitives.</p> <code>transformed</code> <p>Apply a transformation to all actions.</p>"},{"location":"api_reference/drawing/#momapy.drawing.Path.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Get the bounding box.</p> <p>Returns:</p> Type Description <code>Bbox</code> <p>The bounding box.</p> Source code in <code>src/momapy/drawing.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Get the bounding box.\n\n    Returns:\n        The bounding box.\n    \"\"\"\n    primitives = self.to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0.0, 0.0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/drawing/#momapy.drawing.Path.get_filter_region","title":"get_filter_region","text":"<pre><code>get_filter_region() -&gt; Bbox | None\n</code></pre> <p>Get the filter region.</p> <p>Numeric <code>x</code>/<code>y</code>/<code>width</code>/<code>height</code> are interpreted as fractions of the element bounding box under <code>OBJECT_BOUNDING_BOX</code>, and as absolute user-space coordinates under <code>USER_SPACE_ON_USE</code>. Percentage-string values (e.g. the default <code>\"-10%\"</code>/<code>\"120%\"</code>) are always resolved relative to the element bounding box, under either unit system.</p> <p>Returns:</p> Type Description <code>Bbox | None</code> <p>The filter region bbox, or <code>None</code> when the element has no</p> <code>Bbox | None</code> <p>filter (<code>filter_</code> is <code>None</code> or <code>NoneValue</code>).</p> Source code in <code>src/momapy/drawing.py</code> <pre><code>def get_filter_region(self) -&gt; Bbox | None:\n    \"\"\"Get the filter region.\n\n    Numeric ``x``/``y``/``width``/``height`` are interpreted as fractions of\n    the element bounding box under ``OBJECT_BOUNDING_BOX``, and as absolute\n    user-space coordinates under ``USER_SPACE_ON_USE``. Percentage-string\n    values (e.g. the default ``\"-10%\"``/``\"120%\"``) are always resolved\n    relative to the element bounding box, under either unit system.\n\n    Returns:\n        The filter region bbox, or `None` when the element has no\n        filter (`filter_` is `None` or `NoneValue`).\n    \"\"\"\n    if self.filter_ is None or self.filter_ is NoneValue:\n        return None\n    bbox = self.bbox()\n    north_west = bbox.north_west()\n    object_bounding_box = (\n        self.filter_.filter_units == FilterUnits.OBJECT_BOUNDING_BOX\n    )\n\n    def resolve_position(value: float | str, origin: float, extent: float) -&gt; float:\n        if isinstance(value, str):\n            return origin + extent * (float(value.rstrip(\"%\")) / 100)\n        if object_bounding_box:\n            return origin + extent * value\n        return value\n\n    def resolve_size(value: float | str, extent: float) -&gt; float:\n        if isinstance(value, str):\n            return extent * (float(value.rstrip(\"%\")) / 100)\n        if object_bounding_box:\n            return extent * value\n        return value\n\n    px = resolve_position(self.filter_.x, north_west.x, bbox.width)\n    py = resolve_position(self.filter_.y, north_west.y, bbox.height)\n    width = resolve_size(self.filter_.width, bbox.width)\n    height = resolve_size(self.filter_.height, bbox.height)\n    return Bbox(\n        Point(px + width / 2, py + height / 2),\n        width,\n        height,\n    )\n</code></pre>"},{"location":"api_reference/drawing/#momapy.drawing.Path.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Convert to a list of geometry primitives.</p> <p>Returns:</p> Type Description <code>list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]</code> <p>A list of Segment, QuadraticBezierCurve, CubicBezierCurve,</p> <code>list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]</code> <p>or EllipticalArc objects.</p> Source code in <code>src/momapy/drawing.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Convert to a list of geometry primitives.\n\n    Returns:\n        A list of Segment, QuadraticBezierCurve, CubicBezierCurve,\n        or EllipticalArc objects.\n    \"\"\"\n    primitives = []\n    current_point = Point(0.0, 0.0)\n    initial_point = current_point\n    for action in self.actions:\n        if isinstance(action, MoveTo):\n            current_point = action.point\n            initial_point = current_point\n        elif isinstance(action, ClosePath):\n            if (\n                current_point.x != initial_point.x\n                or current_point.y != initial_point.y\n            ):\n                primitives.append(Segment(current_point, initial_point))\n            current_point = initial_point\n        else:\n            primitives.append(action.to_geometry(current_point))\n            current_point = action.point\n    return primitives\n</code></pre>"},{"location":"api_reference/drawing/#momapy.drawing.Path.transformed","title":"transformed","text":"<pre><code>transformed(transformation: Transformation) -&gt; Self\n</code></pre> <p>Apply a transformation to all actions.</p> <p>Parameters:</p> Name Type Description Default <code>transformation</code> <code>Transformation</code> <p>The transformation.</p> required <p>Returns:</p> Type Description <code>Self</code> <p>A new Path with transformed actions.</p> Source code in <code>src/momapy/drawing.py</code> <pre><code>def transformed(self, transformation: Transformation) -&gt; typing_extensions.Self:\n    \"\"\"Apply a transformation to all actions.\n\n    Args:\n        transformation: The transformation.\n\n    Returns:\n        A new Path with transformed actions.\n    \"\"\"\n    actions = []\n    current_point = None\n    for action in self.actions:\n        new_action = action.transformed(transformation, current_point)\n        actions.append(new_action)\n        if hasattr(action, \"point\"):\n            current_point = action.point\n        else:\n            current_point = None\n    return dataclasses.replace(self, actions=tuple(actions))\n</code></pre>"},{"location":"api_reference/drawing/#momapy.drawing.PathAction","title":"PathAction  <code>dataclass</code>","text":"<pre><code>PathAction()\n</code></pre> <p>               Bases: <code>ABC</code></p> <p>Abstract base class for path actions.</p> <p>Path actions are the individual drawing commands that make up a path.</p>"},{"location":"api_reference/drawing/#momapy.drawing.QuadraticCurveTo","title":"QuadraticCurveTo  <code>dataclass</code>","text":"<pre><code>QuadraticCurveTo(point: Point, control_point: Point)\n</code></pre> <p>               Bases: <code>PathAction</code></p> <p>Draw a quadratic Bezier curve.</p> <p>Draws a quadratic Bezier curve from the current point to the end point.</p> <p>Parameters:</p> Name Type Description Default <code>point</code> <code>Point</code> <p>The point of the quadratic curve to action</p> required <code>control_point</code> <code>Point</code> <p>The control point of the quadratic curve to action</p> required <p>Methods:</p> Name Description <code>to_curve_to</code> <p>Convert to cubic CurveTo.</p> <code>to_geometry</code> <p>Convert to QuadraticBezierCurve geometry.</p> <code>transformed</code> <p>Apply a transformation.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>X coordinate.</p> <code>y</code> <code>float</code> <p>Y coordinate.</p>"},{"location":"api_reference/drawing/#momapy.drawing.QuadraticCurveTo.to_curve_to","title":"to_curve_to","text":"<pre><code>to_curve_to(current_point: Point) -&gt; CurveTo\n</code></pre> <p>Convert to cubic CurveTo.</p> <p>Parameters:</p> Name Type Description Default <code>current_point</code> <code>Point</code> <p>Start point.</p> required <p>Returns:</p> Type Description <code>CurveTo</code> <p>An equivalent CurveTo.</p> Source code in <code>src/momapy/drawing.py</code> <pre><code>def to_curve_to(self, current_point: Point) -&gt; CurveTo:\n    \"\"\"Convert to cubic CurveTo.\n\n    Args:\n        current_point: Start point.\n\n    Returns:\n        An equivalent CurveTo.\n    \"\"\"\n    p1 = current_point\n    p2 = self.point\n    control_point1 = p1 + (self.control_point - p1) * (2 / 3)\n    control_point2 = p2 + (self.control_point - p2) * (2 / 3)\n    return CurveTo(p2, control_point1, control_point2)\n</code></pre>"},{"location":"api_reference/drawing/#momapy.drawing.QuadraticCurveTo.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry(current_point: Point) -&gt; QuadraticBezierCurve\n</code></pre> <p>Convert to QuadraticBezierCurve geometry.</p> <p>Parameters:</p> Name Type Description Default <code>current_point</code> <code>Point</code> <p>Start point.</p> required <p>Returns:</p> Type Description <code>QuadraticBezierCurve</code> <p>A QuadraticBezierCurve.</p> Source code in <code>src/momapy/drawing.py</code> <pre><code>def to_geometry(self, current_point: Point) -&gt; QuadraticBezierCurve:\n    \"\"\"Convert to QuadraticBezierCurve geometry.\n\n    Args:\n        current_point: Start point.\n\n    Returns:\n        A QuadraticBezierCurve.\n    \"\"\"\n    return QuadraticBezierCurve(\n        current_point,\n        self.point,\n        self.control_point,\n    )\n</code></pre>"},{"location":"api_reference/drawing/#momapy.drawing.QuadraticCurveTo.transformed","title":"transformed","text":"<pre><code>transformed(transformation: Transformation, current_point: Point) -&gt; Self\n</code></pre> <p>Apply a transformation.</p> <p>Parameters:</p> Name Type Description Default <code>transformation</code> <code>Transformation</code> <p>The transformation.</p> required <code>current_point</code> <code>Point</code> <p>Start point (unused).</p> required <p>Returns:</p> Type Description <code>Self</code> <p>A new QuadraticCurveTo with transformed points.</p> Source code in <code>src/momapy/drawing.py</code> <pre><code>def transformed(\n    self,\n    transformation: Transformation,\n    current_point: Point,\n) -&gt; typing_extensions.Self:\n    \"\"\"Apply a transformation.\n\n    Args:\n        transformation: The transformation.\n        current_point: Start point (unused).\n\n    Returns:\n        A new QuadraticCurveTo with transformed points.\n    \"\"\"\n    return QuadraticCurveTo(\n        self.point.transformed(transformation),\n        self.control_point.transformed(transformation),\n    )\n</code></pre>"},{"location":"api_reference/drawing/#momapy.drawing.QuadraticCurveTo.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>X coordinate.</p>"},{"location":"api_reference/drawing/#momapy.drawing.QuadraticCurveTo.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Y coordinate.</p>"},{"location":"api_reference/drawing/#momapy.drawing.Rectangle","title":"Rectangle  <code>dataclass</code>","text":"<pre><code>Rectangle(*, class_: str | None = None, fill: NoneValueType | Color | None = None, fill_rule: FillRule | None = None, filter_: NoneValueType | Filter | None = None, font_family: str | None = None, font_size: float | None = None, font_style: FontStyle | None = None, font_weight: FontWeight | int | None = None, id_: str | None = None, stroke: NoneValueType | Color | None = None, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: NoneValueType | float | None = None, text_anchor: TextAnchor | None = None, transform: NoneValueType | tuple[Transformation, ...] | None = None, point: Point, width: float, height: float, rx: float, ry: float)\n</code></pre> <p>               Bases: <code>DrawingElement</code></p> <p>Rectangle drawing element with optional rounded corners.</p> <p>Draws a rectangle from a corner point, a size, and corner radii.</p> <p>Parameters:</p> Name Type Description Default <code>class_</code> <code>str | None</code> <p>The class name of the drawing element</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the drawing element</p> <code>None</code> <code>fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the drawing element</p> <code>None</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the drawing element</p> <code>None</code> <code>font_family</code> <code>str | None</code> <p>The font family of the drawing element</p> <code>None</code> <code>font_size</code> <code>float | None</code> <p>The font size of the drawing element</p> <code>None</code> <code>font_style</code> <code>FontStyle | None</code> <p>The font style of the drawing element</p> <code>None</code> <code>font_weight</code> <code>FontWeight | int | None</code> <p>The font weight of the drawing element</p> <code>None</code> <code>id_</code> <code>str | None</code> <p>The id of the drawing element</p> <code>None</code> <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the drawing element</p> <code>None</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the drawing element</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the drawing element</p> <code>None</code> <code>stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the drawing element</p> <code>None</code> <code>text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the drawing element</p> <code>None</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the drawing element</p> <code>None</code> <code>point</code> <code>Point</code> <p>The point of the rectangle</p> required <code>width</code> <code>float</code> <p>The width of the rectangle</p> required <code>height</code> <code>float</code> <p>The height of the rectangle</p> required <code>rx</code> <code>float</code> <p>The x-radius of the rounded corners of the rectangle</p> required <code>ry</code> <code>float</code> <p>The y-radius of the rounded corners of the rectangle</p> required <p>Methods:</p> Name Description <code>__post_init__</code> <p>Round the width and height to the rounding precision after initialization.</p> <code>bbox</code> <p>Get the bounding box.</p> <code>get_filter_region</code> <p>Get the filter region.</p> <code>to_geometry</code> <p>Convert to a list of geometry primitives.</p> <code>to_path</code> <p>Convert to a Path.</p> <code>transformed</code> <p>Apply a transformation.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>X coordinate of top-left.</p> <code>y</code> <code>float</code> <p>Y coordinate of top-left.</p>"},{"location":"api_reference/drawing/#momapy.drawing.Rectangle.__post_init__","title":"__post_init__","text":"<pre><code>__post_init__() -&gt; None\n</code></pre> <p>Round the width and height to the rounding precision after initialization.</p> Source code in <code>src/momapy/drawing.py</code> <pre><code>def __post_init__(self) -&gt; None:\n    \"\"\"Round the width and height to the rounding precision after initialization.\"\"\"\n    object.__setattr__(self, \"width\", round(self.width, ROUNDING))\n    object.__setattr__(self, \"height\", round(self.height, ROUNDING))\n    object.__setattr__(self, \"rx\", round(self.rx, ROUNDING))\n    object.__setattr__(self, \"ry\", round(self.ry, ROUNDING))\n</code></pre>"},{"location":"api_reference/drawing/#momapy.drawing.Rectangle.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Get the bounding box.</p> <p>Returns:</p> Type Description <code>Bbox</code> <p>The bounding box.</p> Source code in <code>src/momapy/drawing.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Get the bounding box.\n\n    Returns:\n        The bounding box.\n    \"\"\"\n    primitives = self.to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0.0, 0.0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/drawing/#momapy.drawing.Rectangle.get_filter_region","title":"get_filter_region","text":"<pre><code>get_filter_region() -&gt; Bbox | None\n</code></pre> <p>Get the filter region.</p> <p>Numeric <code>x</code>/<code>y</code>/<code>width</code>/<code>height</code> are interpreted as fractions of the element bounding box under <code>OBJECT_BOUNDING_BOX</code>, and as absolute user-space coordinates under <code>USER_SPACE_ON_USE</code>. Percentage-string values (e.g. the default <code>\"-10%\"</code>/<code>\"120%\"</code>) are always resolved relative to the element bounding box, under either unit system.</p> <p>Returns:</p> Type Description <code>Bbox | None</code> <p>The filter region bbox, or <code>None</code> when the element has no</p> <code>Bbox | None</code> <p>filter (<code>filter_</code> is <code>None</code> or <code>NoneValue</code>).</p> Source code in <code>src/momapy/drawing.py</code> <pre><code>def get_filter_region(self) -&gt; Bbox | None:\n    \"\"\"Get the filter region.\n\n    Numeric ``x``/``y``/``width``/``height`` are interpreted as fractions of\n    the element bounding box under ``OBJECT_BOUNDING_BOX``, and as absolute\n    user-space coordinates under ``USER_SPACE_ON_USE``. Percentage-string\n    values (e.g. the default ``\"-10%\"``/``\"120%\"``) are always resolved\n    relative to the element bounding box, under either unit system.\n\n    Returns:\n        The filter region bbox, or `None` when the element has no\n        filter (`filter_` is `None` or `NoneValue`).\n    \"\"\"\n    if self.filter_ is None or self.filter_ is NoneValue:\n        return None\n    bbox = self.bbox()\n    north_west = bbox.north_west()\n    object_bounding_box = (\n        self.filter_.filter_units == FilterUnits.OBJECT_BOUNDING_BOX\n    )\n\n    def resolve_position(value: float | str, origin: float, extent: float) -&gt; float:\n        if isinstance(value, str):\n            return origin + extent * (float(value.rstrip(\"%\")) / 100)\n        if object_bounding_box:\n            return origin + extent * value\n        return value\n\n    def resolve_size(value: float | str, extent: float) -&gt; float:\n        if isinstance(value, str):\n            return extent * (float(value.rstrip(\"%\")) / 100)\n        if object_bounding_box:\n            return extent * value\n        return value\n\n    px = resolve_position(self.filter_.x, north_west.x, bbox.width)\n    py = resolve_position(self.filter_.y, north_west.y, bbox.height)\n    width = resolve_size(self.filter_.width, bbox.width)\n    height = resolve_size(self.filter_.height, bbox.height)\n    return Bbox(\n        Point(px + width / 2, py + height / 2),\n        width,\n        height,\n    )\n</code></pre>"},{"location":"api_reference/drawing/#momapy.drawing.Rectangle.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Convert to a list of geometry primitives.</p> <p>Returns:</p> Type Description <code>list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]</code> <p>A list of geometry primitives.</p> Source code in <code>src/momapy/drawing.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Convert to a list of geometry primitives.\n\n    Returns:\n        A list of geometry primitives.\n    \"\"\"\n    return self.to_path().to_geometry()\n</code></pre>"},{"location":"api_reference/drawing/#momapy.drawing.Rectangle.to_path","title":"to_path","text":"<pre><code>to_path() -&gt; Path\n</code></pre> <p>Convert to a Path.</p> <p>Returns:</p> Type Description <code>Path</code> <p>A Path representing the rectangle.</p> Source code in <code>src/momapy/drawing.py</code> <pre><code>def to_path(self) -&gt; Path:\n    \"\"\"Convert to a Path.\n\n    Returns:\n        A Path representing the rectangle.\n    \"\"\"\n    x = self.point.x\n    y = self.point.y\n    rx = self.rx\n    ry = self.ry\n    width = self.width\n    height = self.height\n    actions = [\n        MoveTo(Point(x + rx, y)),\n        LineTo(Point(x + width - rx, y)),\n    ]\n    if rx &gt; 0 and ry &gt; 0:\n        actions.append(EllipticalArc(Point(x + width, y + ry), rx, ry, 0, 0, 1))\n    actions.append(LineTo(Point(x + width, y + height - ry)))\n    if rx &gt; 0 and ry &gt; 0:\n        actions.append(\n            EllipticalArc(\n                Point(x + width - rx, y + height),\n                rx,\n                ry,\n                0,\n                0,\n                1,\n            )\n        )\n    actions.append(LineTo(Point(x + rx, y + height)))\n    if rx &gt; 0 and ry &gt; 0:\n        actions.append(EllipticalArc(Point(x, y + height - ry), rx, ry, 0, 0, 1))\n    actions.append(LineTo(Point(x, y + ry)))\n    if rx &gt; 0 and ry &gt; 0:\n        actions.append(EllipticalArc(Point(x + rx, y), rx, ry, 0, 0, 1))\n    actions.append(ClosePath())\n    path = Path(\n        stroke_width=self.stroke_width,\n        stroke=self.stroke,\n        fill=self.fill,\n        transform=self.transform,\n        filter_=self.filter_,\n        actions=actions,\n    )\n    return path\n</code></pre>"},{"location":"api_reference/drawing/#momapy.drawing.Rectangle.transformed","title":"transformed","text":"<pre><code>transformed(transformation: Transformation) -&gt; Path\n</code></pre> <p>Apply a transformation.</p> <p>Parameters:</p> Name Type Description Default <code>transformation</code> <code>Transformation</code> <p>The transformation.</p> required <p>Returns:</p> Type Description <code>Path</code> <p>A transformed Path.</p> Source code in <code>src/momapy/drawing.py</code> <pre><code>def transformed(self, transformation: Transformation) -&gt; Path:\n    \"\"\"Apply a transformation.\n\n    Args:\n        transformation: The transformation.\n\n    Returns:\n        A transformed Path.\n    \"\"\"\n    path = self.to_path()\n    return path.transformed(transformation)\n</code></pre>"},{"location":"api_reference/drawing/#momapy.drawing.Rectangle.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>X coordinate of top-left.</p>"},{"location":"api_reference/drawing/#momapy.drawing.Rectangle.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Y coordinate of top-left.</p>"},{"location":"api_reference/drawing/#momapy.drawing.Text","title":"Text  <code>dataclass</code>","text":"<pre><code>Text(*, class_: str | None = None, fill: NoneValueType | Color | None = None, fill_rule: FillRule | None = None, filter_: NoneValueType | Filter | None = None, font_family: str | None = None, font_size: float | None = None, font_style: FontStyle | None = None, font_weight: FontWeight | int | None = None, id_: str | None = None, stroke: NoneValueType | Color | None = None, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: NoneValueType | float | None = None, text_anchor: TextAnchor | None = None, transform: NoneValueType | tuple[Transformation, ...] | None = None, text: str, point: Point)\n</code></pre> <p>               Bases: <code>DrawingElement</code></p> <p>Text drawing element.</p> <p>Renders a string of text at a given position.</p> <p>Parameters:</p> Name Type Description Default <code>class_</code> <code>str | None</code> <p>The class name of the drawing element</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the drawing element</p> <code>None</code> <code>fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the drawing element</p> <code>None</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the drawing element</p> <code>None</code> <code>font_family</code> <code>str | None</code> <p>The font family of the drawing element</p> <code>None</code> <code>font_size</code> <code>float | None</code> <p>The font size of the drawing element</p> <code>None</code> <code>font_style</code> <code>FontStyle | None</code> <p>The font style of the drawing element</p> <code>None</code> <code>font_weight</code> <code>FontWeight | int | None</code> <p>The font weight of the drawing element</p> <code>None</code> <code>id_</code> <code>str | None</code> <p>The id of the drawing element</p> <code>None</code> <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the drawing element</p> <code>None</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the drawing element</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the drawing element</p> <code>None</code> <code>stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the drawing element</p> <code>None</code> <code>text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the drawing element</p> <code>None</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the drawing element</p> <code>None</code> <code>text</code> <code>str</code> <p>The value of the text element</p> required <code>point</code> <code>Point</code> <p>The position of the text element</p> required <p>Methods:</p> Name Description <code>bbox</code> <p>Get the bounding box.</p> <code>get_filter_region</code> <p>Get the filter region.</p> <code>to_geometry</code> <p>Convert to a list of geometry primitives.</p> <code>transformed</code> <p>Apply a transformation.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>X coordinate of the text position.</p> <code>y</code> <code>float</code> <p>Y coordinate of the text position.</p>"},{"location":"api_reference/drawing/#momapy.drawing.Text.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Get the bounding box.</p> <p>Returns:</p> Type Description <code>Bbox</code> <p>The bounding box.</p> Source code in <code>src/momapy/drawing.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Get the bounding box.\n\n    Returns:\n        The bounding box.\n    \"\"\"\n    primitives = self.to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0.0, 0.0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/drawing/#momapy.drawing.Text.get_filter_region","title":"get_filter_region","text":"<pre><code>get_filter_region() -&gt; Bbox | None\n</code></pre> <p>Get the filter region.</p> <p>Numeric <code>x</code>/<code>y</code>/<code>width</code>/<code>height</code> are interpreted as fractions of the element bounding box under <code>OBJECT_BOUNDING_BOX</code>, and as absolute user-space coordinates under <code>USER_SPACE_ON_USE</code>. Percentage-string values (e.g. the default <code>\"-10%\"</code>/<code>\"120%\"</code>) are always resolved relative to the element bounding box, under either unit system.</p> <p>Returns:</p> Type Description <code>Bbox | None</code> <p>The filter region bbox, or <code>None</code> when the element has no</p> <code>Bbox | None</code> <p>filter (<code>filter_</code> is <code>None</code> or <code>NoneValue</code>).</p> Source code in <code>src/momapy/drawing.py</code> <pre><code>def get_filter_region(self) -&gt; Bbox | None:\n    \"\"\"Get the filter region.\n\n    Numeric ``x``/``y``/``width``/``height`` are interpreted as fractions of\n    the element bounding box under ``OBJECT_BOUNDING_BOX``, and as absolute\n    user-space coordinates under ``USER_SPACE_ON_USE``. Percentage-string\n    values (e.g. the default ``\"-10%\"``/``\"120%\"``) are always resolved\n    relative to the element bounding box, under either unit system.\n\n    Returns:\n        The filter region bbox, or `None` when the element has no\n        filter (`filter_` is `None` or `NoneValue`).\n    \"\"\"\n    if self.filter_ is None or self.filter_ is NoneValue:\n        return None\n    bbox = self.bbox()\n    north_west = bbox.north_west()\n    object_bounding_box = (\n        self.filter_.filter_units == FilterUnits.OBJECT_BOUNDING_BOX\n    )\n\n    def resolve_position(value: float | str, origin: float, extent: float) -&gt; float:\n        if isinstance(value, str):\n            return origin + extent * (float(value.rstrip(\"%\")) / 100)\n        if object_bounding_box:\n            return origin + extent * value\n        return value\n\n    def resolve_size(value: float | str, extent: float) -&gt; float:\n        if isinstance(value, str):\n            return extent * (float(value.rstrip(\"%\")) / 100)\n        if object_bounding_box:\n            return extent * value\n        return value\n\n    px = resolve_position(self.filter_.x, north_west.x, bbox.width)\n    py = resolve_position(self.filter_.y, north_west.y, bbox.height)\n    width = resolve_size(self.filter_.width, bbox.width)\n    height = resolve_size(self.filter_.height, bbox.height)\n    return Bbox(\n        Point(px + width / 2, py + height / 2),\n        width,\n        height,\n    )\n</code></pre>"},{"location":"api_reference/drawing/#momapy.drawing.Text.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Convert to a list of geometry primitives.</p> <p>Text has no geometry primitives.</p> <p>Returns:</p> Type Description <code>list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]</code> <p>An empty list.</p> Source code in <code>src/momapy/drawing.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Convert to a list of geometry primitives.\n\n    Text has no geometry primitives.\n\n    Returns:\n        An empty list.\n    \"\"\"\n    return []\n</code></pre>"},{"location":"api_reference/drawing/#momapy.drawing.Text.transformed","title":"transformed","text":"<pre><code>transformed(transformation: Transformation) -&gt; Self\n</code></pre> <p>Apply a transformation.</p> <p>Returns an unmodified copy: text is positioned by its <code>point</code> and the renderer applies any <code>transform</code> attribute at render time, so the transformation is not baked into the returned element.</p> <p>Parameters:</p> Name Type Description Default <code>transformation</code> <code>Transformation</code> <p>The transformation (unused).</p> required <p>Returns:</p> Type Description <code>Self</code> <p>A copy of the text element.</p> Source code in <code>src/momapy/drawing.py</code> <pre><code>def transformed(self, transformation: Transformation) -&gt; typing_extensions.Self:\n    \"\"\"Apply a transformation.\n\n    Returns an unmodified copy: text is positioned by its ``point`` and\n    the renderer applies any ``transform`` attribute at render time, so\n    the transformation is not baked into the returned element.\n\n    Args:\n        transformation: The transformation (unused).\n\n    Returns:\n        A copy of the text element.\n    \"\"\"\n    return copy.deepcopy(self)\n</code></pre>"},{"location":"api_reference/drawing/#momapy.drawing.Text.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>X coordinate of the text position.</p>"},{"location":"api_reference/drawing/#momapy.drawing.Text.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Y coordinate of the text position.</p>"},{"location":"api_reference/drawing/#momapy.drawing.TextAnchor","title":"TextAnchor","text":"<p>               Bases: <code>Enum</code></p> <p>Text anchor options.</p> <p>Enumerates how text is aligned relative to its anchor point.</p>"},{"location":"api_reference/drawing/#momapy.drawing.drawing_elements_to_geometry","title":"drawing_elements_to_geometry","text":"<pre><code>drawing_elements_to_geometry(drawing_elements: Sequence[DrawingElement]) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Convert drawing elements to geometry primitives.</p> <p>Parameters:</p> Name Type Description Default <code>drawing_elements</code> <code>Sequence[DrawingElement]</code> <p>Sequence of drawing elements.</p> required <p>Returns:</p> Type Description <code>list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]</code> <p>A list of geometry primitives.</p> Source code in <code>src/momapy/drawing.py</code> <pre><code>def drawing_elements_to_geometry(\n    drawing_elements: collections.abc.Sequence[DrawingElement],\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc]:\n    \"\"\"Convert drawing elements to geometry primitives.\n\n    Args:\n        drawing_elements: Sequence of drawing elements.\n\n    Returns:\n        A list of geometry primitives.\n    \"\"\"\n    primitives = []\n    for drawing_element in drawing_elements:\n        primitives.extend(drawing_element.to_geometry())\n    return primitives\n</code></pre>"},{"location":"api_reference/drawing/#momapy.drawing.get_drawing_elements_anchor_point","title":"get_drawing_elements_anchor_point","text":"<pre><code>get_drawing_elements_anchor_point(drawing_elements: Sequence[DrawingElement], anchor_point: str, center: Point | None = None) -&gt; Point | None\n</code></pre> <p>Get anchor point of drawing elements.</p> <p>Parameters:</p> Name Type Description Default <code>drawing_elements</code> <code>Sequence[DrawingElement]</code> <p>Drawing elements.</p> required <code>anchor_point</code> <code>str</code> <p>Name of anchor point.</p> required <code>center</code> <code>Point | None</code> <p>Optional center point.</p> <code>None</code> <p>Returns:</p> Type Description <code>Point | None</code> <p>The anchor point, or None.</p> Source code in <code>src/momapy/drawing.py</code> <pre><code>def get_drawing_elements_anchor_point(\n    drawing_elements: collections.abc.Sequence[DrawingElement],\n    anchor_point: str,\n    center: Point | None = None,\n) -&gt; Point | None:\n    \"\"\"Get anchor point of drawing elements.\n\n    Args:\n        drawing_elements: Drawing elements.\n        anchor_point: Name of anchor point.\n        center: Optional center point.\n\n    Returns:\n        The anchor point, or None.\n    \"\"\"\n    primitives = drawing_elements_to_geometry(drawing_elements)\n    return get_primitives_anchor_point(\n        primitives=primitives, anchor_point=anchor_point, center=center\n    )\n</code></pre>"},{"location":"api_reference/drawing/#momapy.drawing.get_drawing_elements_angle","title":"get_drawing_elements_angle","text":"<pre><code>get_drawing_elements_angle(drawing_elements: Sequence[DrawingElement], angle: float, unit: Literal['degrees', 'radians'] = 'degrees', center: Point | None = None) -&gt; Point | None\n</code></pre> <p>Get border point at an angle from center.</p> <p>Parameters:</p> Name Type Description Default <code>drawing_elements</code> <code>Sequence[DrawingElement]</code> <p>Drawing elements.</p> required <code>angle</code> <code>float</code> <p>The angle.</p> required <code>unit</code> <code>Literal['degrees', 'radians']</code> <p>Unit ('degrees' or 'radians').</p> <code>'degrees'</code> <code>center</code> <code>Point | None</code> <p>Optional center point.</p> <code>None</code> <p>Returns:</p> Type Description <code>Point | None</code> <p>The border point or None.</p> Source code in <code>src/momapy/drawing.py</code> <pre><code>def get_drawing_elements_angle(\n    drawing_elements: collections.abc.Sequence[DrawingElement],\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n    center: Point | None = None,\n) -&gt; Point | None:\n    \"\"\"Get border point at an angle from center.\n\n    Args:\n        drawing_elements: Drawing elements.\n        angle: The angle.\n        unit: Unit ('degrees' or 'radians').\n        center: Optional center point.\n\n    Returns:\n        The border point or None.\n    \"\"\"\n    primitives = drawing_elements_to_geometry(drawing_elements)\n    return get_primitives_angle(\n        primitives=primitives, angle=angle, unit=unit, center=center\n    )\n</code></pre>"},{"location":"api_reference/drawing/#momapy.drawing.get_drawing_elements_bbox","title":"get_drawing_elements_bbox","text":"<pre><code>get_drawing_elements_bbox(drawing_elements: Sequence[DrawingElement]) -&gt; Bbox\n</code></pre> <p>Get bounding box of drawing elements.</p> <p>Parameters:</p> Name Type Description Default <code>drawing_elements</code> <code>Sequence[DrawingElement]</code> <p>Drawing elements.</p> required <p>Returns:</p> Type Description <code>Bbox</code> <p>The bounding box.</p> Source code in <code>src/momapy/drawing.py</code> <pre><code>def get_drawing_elements_bbox(\n    drawing_elements: collections.abc.Sequence[DrawingElement],\n) -&gt; Bbox:\n    \"\"\"Get bounding box of drawing elements.\n\n    Args:\n        drawing_elements: Drawing elements.\n\n    Returns:\n        The bounding box.\n    \"\"\"\n    primitives = drawing_elements_to_geometry(drawing_elements)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/drawing/#momapy.drawing.get_drawing_elements_border","title":"get_drawing_elements_border","text":"<pre><code>get_drawing_elements_border(drawing_elements: Sequence[DrawingElement], point: Point, center: Point | None = None) -&gt; Point | None\n</code></pre> <p>Get border point in a direction from center.</p> <p>Parameters:</p> Name Type Description Default <code>drawing_elements</code> <code>Sequence[DrawingElement]</code> <p>Drawing elements.</p> required <code>point</code> <code>Point</code> <p>Direction point.</p> required <code>center</code> <code>Point | None</code> <p>Optional center point.</p> <code>None</code> <p>Returns:</p> Type Description <code>Point | None</code> <p>The border point or None.</p> Source code in <code>src/momapy/drawing.py</code> <pre><code>def get_drawing_elements_border(\n    drawing_elements: collections.abc.Sequence[DrawingElement],\n    point: Point,\n    center: Point | None = None,\n) -&gt; Point | None:\n    \"\"\"Get border point in a direction from center.\n\n    Args:\n        drawing_elements: Drawing elements.\n        point: Direction point.\n        center: Optional center point.\n\n    Returns:\n        The border point or None.\n    \"\"\"\n    primitives = drawing_elements_to_geometry(drawing_elements)\n    return get_primitives_border(primitives=primitives, point=point, center=center)\n</code></pre>"},{"location":"api_reference/drawing/#momapy.drawing.get_initial_value","title":"get_initial_value","text":"<pre><code>get_initial_value(attr_name: str) -&gt; Any\n</code></pre> <p>Get the initial value of a presentation attribute.</p> <p>Parameters:</p> Name Type Description Default <code>attr_name</code> <code>str</code> <p>Name of the attribute.</p> required <p>Returns:</p> Type Description <code>Any</code> <p>The initial value.</p> <p>Raises:</p> Type Description <code>KeyError</code> <p>If <code>attr_name</code> is not a known presentation attribute.</p> Source code in <code>src/momapy/drawing.py</code> <pre><code>def get_initial_value(attr_name: str) -&gt; typing.Any:\n    \"\"\"Get the initial value of a presentation attribute.\n\n    Args:\n        attr_name: Name of the attribute.\n\n    Returns:\n        The initial value.\n\n    Raises:\n        KeyError: If `attr_name` is not a known presentation attribute.\n    \"\"\"\n    attr_value = PRESENTATION_ATTRIBUTES[attr_name][\"initial\"]\n    if attr_value is None:\n        attr_value = INITIAL_VALUES[attr_name]\n    return attr_value\n</code></pre>"},{"location":"api_reference/geometry/","title":"Geometry","text":""},{"location":"api_reference/geometry/#momapy.geometry","title":"momapy.geometry","text":"<p>Geometric primitives and transformations for momapy.</p> <p>This module provides geometric classes and functions for working with points, lines, segments, curves, and transformations. It includes support for Bezier curves, elliptical arcs, and various geometric operations.</p> <p>Examples:</p> <pre><code>import math\n\nfrom momapy.geometry import Point, Line, Segment, Rotation\n\n# Create points\np1 = Point(0.0, 0.0)\np2 = Point(10.0, 10.0)\n\n# Create a line\nline = Line(p1, p2)\nline.slope()\n\n# Create a segment\nsegment = Segment(p1, p2)\nsegment.length()\n\n# Apply transformations\nrotated = p1.transformed(Rotation(math.pi / 2, p2))\n</code></pre> <p>Classes:</p> Name Description <code>Bbox</code> <p>Represents a bounding box.</p> <code>CubicBezierCurve</code> <p>Represents a cubic Bezier curve.</p> <code>EllipticalArc</code> <p>Represents an elliptical arc.</p> <code>GeometryObject</code> <p>Abstract base class for all geometry objects.</p> <code>Line</code> <p>Represents an infinite line defined by two points.</p> <code>MatrixTransformation</code> <p>Represents a transformation as a 3x3 matrix.</p> <code>Point</code> <p>Represents a 2D point with x and y coordinates.</p> <code>QuadraticBezierCurve</code> <p>Represents a quadratic Bezier curve.</p> <code>Rotation</code> <p>Represents a rotation transformation.</p> <code>Scaling</code> <p>Represents a scaling transformation.</p> <code>Segment</code> <p>Represents a line segment between two points.</p> <code>Transformation</code> <p>Abstract base class for geometric transformations.</p> <code>Translation</code> <p>Represents a translation transformation.</p> <p>Functions:</p> Name Description <code>get_normalized_angle</code> <p>Normalize an angle to [0, 2*pi).</p> <code>get_primitives_anchor_point</code> <p>Get an anchor point of geometry primitives.</p> <code>get_primitives_angle</code> <p>Get the border point at a given angle.</p> <code>get_primitives_border</code> <p>Get the border point of geometry primitives in a given direction.</p> <code>get_transformation_for_frame</code> <p>Get transformation for a frame defined by origin and axes.</p> <p>Attributes:</p> Name Type Description <code>COMPASS_ANCHOR_NAMES</code> <code>tuple[str, ...]</code> <p>The 16 compass anchor-point names supported by <code>Bbox</code>/<code>Node</code> (excluding</p>"},{"location":"api_reference/geometry/#momapy.geometry.Bbox","title":"Bbox  <code>dataclass</code>","text":"<pre><code>Bbox(position: Point, width: float, height: float)\n</code></pre> <p>               Bases: <code>object</code></p> <p>Represents a bounding box.</p> <p>Examples:</p> <pre><code>bbox = Bbox(Point(5.0, 5.0), 10, 10)\nbbox.north_west()\nbbox.south_east()\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>position</code> <code>Point</code> <p>Center point.</p> required <code>width</code> <code>float</code> <p>Width of the box.</p> required <code>height</code> <code>float</code> <p>Height of the box.</p> required <p>Methods:</p> Name Description <code>__post_init__</code> <p>Round the width and height to the rounding precision after initialization.</p> <code>anchor_point</code> <p>Get a named anchor point.</p> <code>around_points</code> <p>Create a minimal Bbox enclosing all given points.</p> <code>center</code> <p>Get the center point.</p> <code>east</code> <p>Get the east (right center) point.</p> <code>east_north_east</code> <p>Get the east-north-east point.</p> <code>east_south_east</code> <p>Get the east-south-east point.</p> <code>isnan</code> <p>Check if the position has NaN coordinates.</p> <code>north</code> <p>Get the north (top center) point.</p> <code>north_east</code> <p>Get the north-east corner.</p> <code>north_north_east</code> <p>Get the north-north-east point.</p> <code>north_north_west</code> <p>Get the north-north-west point.</p> <code>north_west</code> <p>Get the north-west corner.</p> <code>size</code> <p>Get the size as (width, height).</p> <code>south</code> <p>Get the south (bottom center) point.</p> <code>south_east</code> <p>Get the south-east corner.</p> <code>south_south_east</code> <p>Get the south-south-east point.</p> <code>south_south_west</code> <p>Get the south-south-west point.</p> <code>south_west</code> <p>Get the south-west corner.</p> <code>union</code> <p>Create a Bbox enclosing all given bounding boxes.</p> <code>west</code> <p>Get the west (left center) point.</p> <code>west_north_west</code> <p>Get the west-north-west point.</p> <code>west_south_west</code> <p>Get the west-south-west point.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>The x coordinate of the center.</p> <code>y</code> <code>float</code> <p>The y coordinate of the center.</p>"},{"location":"api_reference/geometry/#momapy.geometry.Bbox.__post_init__","title":"__post_init__","text":"<pre><code>__post_init__() -&gt; None\n</code></pre> <p>Round the width and height to the rounding precision after initialization.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def __post_init__(self) -&gt; None:\n    \"\"\"Round the width and height to the rounding precision after initialization.\"\"\"\n    object.__setattr__(self, \"width\", round(self.width, ROUNDING))\n    object.__setattr__(self, \"height\", round(self.height, ROUNDING))\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.Bbox.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_point: str) -&gt; Point\n</code></pre> <p>Get a named anchor point.</p> <p>Parameters:</p> Name Type Description Default <code>anchor_point</code> <code>str</code> <p>Name like 'north', 'south_east', 'center', etc.</p> required <p>Returns:</p> Type Description <code>Point</code> <p>The anchor point.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def anchor_point(self, anchor_point: str) -&gt; Point:\n    \"\"\"Get a named anchor point.\n\n    Args:\n        anchor_point: Name like 'north', 'south_east', 'center', etc.\n\n    Returns:\n        The anchor point.\n    \"\"\"\n    return getattr(self, anchor_point)()\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.Bbox.around_points","title":"around_points  <code>classmethod</code>","text":"<pre><code>around_points(points: Iterable[Point]) -&gt; Bbox\n</code></pre> <p>Create a minimal Bbox enclosing all given points.</p> <p>Parameters:</p> Name Type Description Default <code>points</code> <code>Iterable[Point]</code> <p>An iterable of Point objects.</p> required <p>Returns:</p> Type Description <code>Bbox</code> <p>A new Bbox that tightly encloses all input points.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the iterable is empty.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>@classmethod\ndef around_points(cls, points: collections.abc.Iterable[Point]) -&gt; \"Bbox\":\n    \"\"\"Create a minimal Bbox enclosing all given points.\n\n    Args:\n        points: An iterable of Point objects.\n\n    Returns:\n        A new Bbox that tightly encloses all input points.\n\n    Raises:\n        ValueError: If the iterable is empty.\n    \"\"\"\n    it = iter(points)\n    try:\n        first = next(it)\n    except StopIteration:\n        raise ValueError(\"points must contain at least one point\")\n    min_x = max_x = first.x\n    min_y = max_y = first.y\n    for p in it:\n        if p.x &lt; min_x:\n            min_x = p.x\n        elif p.x &gt; max_x:\n            max_x = p.x\n        if p.y &lt; min_y:\n            min_y = p.y\n        elif p.y &gt; max_y:\n            max_y = p.y\n    return cls(\n        Point((min_x + max_x) / 2, (min_y + max_y) / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.Bbox.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Get the center point.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Get the center point.\"\"\"\n    return Point(self.x, self.y)\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.Bbox.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Get the east (right center) point.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Get the east (right center) point.\"\"\"\n    return Point(self.x + self.width / 2, self.y)\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.Bbox.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Get the east-north-east point.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Get the east-north-east point.\"\"\"\n    return Point(self.x + self.width / 2, self.y - self.height / 4)\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.Bbox.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Get the east-south-east point.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Get the east-south-east point.\"\"\"\n    return Point(self.x + self.width / 2, self.y + self.height / 4)\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.Bbox.isnan","title":"isnan","text":"<pre><code>isnan() -&gt; bool\n</code></pre> <p>Check if the position has NaN coordinates.</p> <p>Returns:</p> Type Description <code>bool</code> <p>True if position has NaN.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def isnan(self) -&gt; bool:\n    \"\"\"Check if the position has NaN coordinates.\n\n    Returns:\n        True if position has NaN.\n    \"\"\"\n    return self.position.isnan()\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.Bbox.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Get the north (top center) point.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Get the north (top center) point.\"\"\"\n    return Point(self.x, self.y - self.height / 2)\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.Bbox.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Get the north-east corner.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Get the north-east corner.\"\"\"\n    return Point(self.x + self.width / 2, self.y - self.height / 2)\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.Bbox.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Get the north-north-east point.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Get the north-north-east point.\"\"\"\n    return Point(self.x + self.width / 4, self.y - self.height / 2)\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.Bbox.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Get the north-north-west point.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Get the north-north-west point.\"\"\"\n    return Point(self.x - self.width / 4, self.y - self.height / 2)\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.Bbox.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Get the north-west corner.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Get the north-west corner.\"\"\"\n    return Point(self.x - self.width / 2, self.y - self.height / 2)\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.Bbox.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Get the size as (width, height).</p> <p>Returns:</p> Type Description <code>tuple[float, float]</code> <p>Tuple of (width, height).</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Get the size as (width, height).\n\n    Returns:\n        Tuple of (width, height).\n    \"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.Bbox.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Get the south (bottom center) point.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Get the south (bottom center) point.\"\"\"\n    return Point(self.x, self.y + self.height / 2)\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.Bbox.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Get the south-east corner.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Get the south-east corner.\"\"\"\n    return Point(self.x + self.width / 2, self.y + self.height / 2)\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.Bbox.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Get the south-south-east point.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Get the south-south-east point.\"\"\"\n    return Point(self.x + self.width / 4, self.y + self.height / 2)\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.Bbox.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Get the south-south-west point.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Get the south-south-west point.\"\"\"\n    return Point(self.x - self.width / 4, self.y + self.height / 2)\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.Bbox.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Get the south-west corner.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Get the south-west corner.\"\"\"\n    return Point(self.x - self.width / 2, self.y + self.height / 2)\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.Bbox.union","title":"union  <code>classmethod</code>","text":"<pre><code>union(bboxes: list[Bbox]) -&gt; Bbox\n</code></pre> <p>Create a Bbox enclosing all given bounding boxes.</p> <p>Parameters:</p> Name Type Description Default <code>bboxes</code> <code>list[Bbox]</code> <p>List of Bbox objects to merge.</p> required <p>Returns:</p> Type Description <code>Bbox</code> <p>A new Bbox enclosing all input bboxes.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>@classmethod\ndef union(cls, bboxes: list[\"Bbox\"]) -&gt; \"Bbox\":\n    \"\"\"Create a Bbox enclosing all given bounding boxes.\n\n    Args:\n        bboxes: List of Bbox objects to merge.\n\n    Returns:\n        A new Bbox enclosing all input bboxes.\n    \"\"\"\n    if not bboxes:\n        return cls(Point(0.0, 0.0), 0, 0)\n    corners = []\n    for bbox in bboxes:\n        corners.append(bbox.north_west())\n        corners.append(bbox.south_east())\n    return cls.around_points(corners)\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.Bbox.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Get the west (left center) point.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Get the west (left center) point.\"\"\"\n    return Point(self.x - self.width / 2, self.y)\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.Bbox.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Get the west-north-west point.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Get the west-north-west point.\"\"\"\n    return Point(self.x - self.width / 2, self.y - self.height / 4)\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.Bbox.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Get the west-south-west point.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Get the west-south-west point.\"\"\"\n    return Point(self.x - self.width / 2, self.y + self.height / 4)\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.Bbox.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>The x coordinate of the center.</p>"},{"location":"api_reference/geometry/#momapy.geometry.Bbox.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>The y coordinate of the center.</p>"},{"location":"api_reference/geometry/#momapy.geometry.COMPASS_ANCHOR_NAMES","title":"COMPASS_ANCHOR_NAMES  <code>module-attribute</code>","text":"<pre><code>COMPASS_ANCHOR_NAMES: tuple[str, ...] = ('north_west', 'north_north_west', 'north', 'north_north_east', 'north_east', 'east_north_east', 'east', 'east_south_east', 'south_east', 'south_south_east', 'south', 'south_south_west', 'south_west', 'west_south_west', 'west', 'west_north_west')\n</code></pre> <p>The 16 compass anchor-point names supported by <code>Bbox</code>/<code>Node</code> (excluding <code>center</code>), in clockwise order from north-west.</p>"},{"location":"api_reference/geometry/#momapy.geometry.CubicBezierCurve","title":"CubicBezierCurve  <code>dataclass</code>","text":"<pre><code>CubicBezierCurve(p1: Point, p2: Point, control_point1: Point, control_point2: Point)\n</code></pre> <p>               Bases: <code>GeometryObject</code></p> <p>Represents a cubic Bezier curve.</p> <p>Examples:</p> <pre><code>curve = CubicBezierCurve(\n    Point(0.0, 0.0),\n    Point(10.0, 0.0),\n    Point(3.0, 5.0),\n    Point(7.0, 5.0)\n)\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>p1</code> <code>Point</code> <p>Start point.</p> required <code>p2</code> <code>Point</code> <p>End point.</p> required <code>control_point1</code> <code>Point</code> <p>First control point.</p> required <code>control_point2</code> <code>Point</code> <p>Second control point.</p> required <p>Methods:</p> Name Description <code>bbox</code> <p>Get the bounding box of the curve.</p> <code>derivative</code> <p>Compute the derivative at parameter t.</p> <code>evaluate</code> <p>Evaluate the curve at parameter t.</p> <code>evaluate_multi</code> <p>Evaluate the curve at multiple parameters.</p> <code>get_angle_at_fraction</code> <p>Get angle at a fraction of the arc length.</p> <code>get_intersection_with_line</code> <p>Get intersection with a line.</p> <code>get_position_and_angle_at_fraction</code> <p>Get both position and angle at a fraction of the arc length.</p> <code>get_position_at_fraction</code> <p>Get point at a fraction of the arc length.</p> <code>length</code> <p>Calculate the arc length of the curve.</p> <code>reversed</code> <p>Return a reversed copy of the curve.</p> <code>shortened</code> <p>Return a shortened copy of the curve.</p> <code>split</code> <p>Split the curve at parameter t using De Casteljau subdivision.</p> <code>transformed</code> <p>Apply a transformation to this curve.</p>"},{"location":"api_reference/geometry/#momapy.geometry.CubicBezierCurve.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Get the bounding box of the curve.</p> <p>Returns:</p> Type Description <code>Bbox</code> <p>A Bbox enclosing the curve.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def bbox(self) -&gt; \"Bbox\":\n    \"\"\"Get the bounding box of the curve.\n\n    Returns:\n        A Bbox enclosing the curve.\n    \"\"\"\n    xs = [self.p1.x, self.p2.x]\n    ys = [self.p1.y, self.p2.y]\n    for values, candidates in [\n        (\n            (self.p1.x, self.control_point1.x, self.control_point2.x, self.p2.x),\n            xs,\n        ),\n        (\n            (self.p1.y, self.control_point1.y, self.control_point2.y, self.p2.y),\n            ys,\n        ),\n    ]:\n        p0, p1, p2, p3 = values\n        a = -3 * p0 + 9 * p1 - 9 * p2 + 3 * p3\n        b = 6 * p0 - 12 * p1 + 6 * p2\n        c = -3 * p0 + 3 * p1\n        if abs(a) &lt; _ZERO_TOLERANCE:\n            if abs(b) &gt; _ZERO_TOLERANCE:\n                t = -c / b\n                if 0 &lt; t &lt; 1:\n                    point = self.evaluate(t)\n                    candidates.append(point.x if candidates is xs else point.y)\n        else:\n            disc = b * b - 4 * a * c\n            if disc &gt;= 0:\n                sqrt_disc = math.sqrt(disc)\n                for t in [(-b + sqrt_disc) / (2 * a), (-b - sqrt_disc) / (2 * a)]:\n                    if 0 &lt; t &lt; 1:\n                        point = self.evaluate(t)\n                        candidates.append(point.x if candidates is xs else point.y)\n    min_x, max_x = min(xs), max(xs)\n    min_y, max_y = min(ys), max(ys)\n    return Bbox(\n        Point(min_x, min_y),\n        max_x - min_x,\n        max_y - min_y,\n    )\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.CubicBezierCurve.derivative","title":"derivative","text":"<pre><code>derivative(t: float) -&gt; tuple[float, float]\n</code></pre> <p>Compute the derivative at parameter t.</p> <p>Parameters:</p> Name Type Description Default <code>t</code> <code>float</code> <p>Parameter value from 0 to 1.</p> required <p>Returns:</p> Type Description <code>tuple[float, float]</code> <p>Tuple of (dx/dt, dy/dt).</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def derivative(self, t: float) -&gt; tuple[float, float]:\n    \"\"\"Compute the derivative at parameter t.\n\n    Args:\n        t: Parameter value from 0 to 1.\n\n    Returns:\n        Tuple of (dx/dt, dy/dt).\n    \"\"\"\n    u = 1 - t\n    dx = (\n        3 * u * u * (self.control_point1.x - self.p1.x)\n        + 6 * u * t * (self.control_point2.x - self.control_point1.x)\n        + 3 * t * t * (self.p2.x - self.control_point2.x)\n    )\n    dy = (\n        3 * u * u * (self.control_point1.y - self.p1.y)\n        + 6 * u * t * (self.control_point2.y - self.control_point1.y)\n        + 3 * t * t * (self.p2.y - self.control_point2.y)\n    )\n    return (dx, dy)\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.CubicBezierCurve.evaluate","title":"evaluate","text":"<pre><code>evaluate(t: float) -&gt; Point\n</code></pre> <p>Evaluate the curve at parameter t.</p> <p>Parameters:</p> Name Type Description Default <code>t</code> <code>float</code> <p>Parameter value from 0 to 1.</p> required <p>Returns:</p> Type Description <code>Point</code> <p>The point at parameter t.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def evaluate(self, t: float) -&gt; Point:\n    \"\"\"Evaluate the curve at parameter t.\n\n    Args:\n        t: Parameter value from 0 to 1.\n\n    Returns:\n        The point at parameter t.\n    \"\"\"\n    return self.evaluate_multi([t])[0]\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.CubicBezierCurve.evaluate_multi","title":"evaluate_multi","text":"<pre><code>evaluate_multi(t_sequence: Sequence[float]) -&gt; list[Point]\n</code></pre> <p>Evaluate the curve at multiple parameters.</p> <p>Parameters:</p> Name Type Description Default <code>t_sequence</code> <code>Sequence[float]</code> <p>Sequence of parameter values.</p> required <p>Returns:</p> Type Description <code>list[Point]</code> <p>List of points.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def evaluate_multi(\n    self, t_sequence: collections.abc.Sequence[float]\n) -&gt; list[Point]:\n    \"\"\"Evaluate the curve at multiple parameters.\n\n    Args:\n        t_sequence: Sequence of parameter values.\n\n    Returns:\n        List of points.\n    \"\"\"\n    t = numpy.asarray(t_sequence, dtype=\"double\")\n    u = 1 - t\n    u2 = u * u\n    t2 = t * t\n    x = (\n        u2 * u * self.p1.x\n        + 3 * u2 * t * self.control_point1.x\n        + 3 * u * t2 * self.control_point2.x\n        + t2 * t * self.p2.x\n    )\n    y = (\n        u2 * u * self.p1.y\n        + 3 * u2 * t * self.control_point1.y\n        + 3 * u * t2 * self.control_point2.y\n        + t2 * t * self.p2.y\n    )\n    return [Point(float(xi), float(yi)) for xi, yi in zip(x, y)]\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.CubicBezierCurve.get_angle_at_fraction","title":"get_angle_at_fraction","text":"<pre><code>get_angle_at_fraction(fraction: float) -&gt; float\n</code></pre> <p>Get angle at a fraction of the arc length.</p> <p>Parameters:</p> Name Type Description Default <code>fraction</code> <code>float</code> <p>Fraction from 0 to 1.</p> required <p>Returns:</p> Type Description <code>float</code> <p>Angle in radians.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def get_angle_at_fraction(self, fraction: float) -&gt; float:\n    \"\"\"Get angle at a fraction of the arc length.\n\n    Args:\n        fraction: Fraction from 0 to 1.\n\n    Returns:\n        Angle in radians.\n    \"\"\"\n    total = self.length()\n    if fraction &lt;= 0:\n        t = 0.0\n    elif fraction &gt;= 1:\n        t = 1.0\n    else:\n        t = _find_t_at_arc_length_fraction(self.derivative, total, fraction)\n    dx, dy = self.derivative(t)\n    return math.atan2(dy, dx)\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.CubicBezierCurve.get_intersection_with_line","title":"get_intersection_with_line","text":"<pre><code>get_intersection_with_line(line: Line) -&gt; list[Point]\n</code></pre> <p>Get intersection with a line.</p> <p>Parameters:</p> Name Type Description Default <code>line</code> <code>Line</code> <p>The line to intersect with.</p> required <p>Returns:</p> Type Description <code>list[Point]</code> <p>List of intersection points.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def get_intersection_with_line(self, line: Line) -&gt; list[Point]:\n    \"\"\"Get intersection with a line.\n\n    Args:\n        line: The line to intersect with.\n\n    Returns:\n        List of intersection points.\n    \"\"\"\n    a = line.p2.y - line.p1.y\n    b = line.p1.x - line.p2.x\n    c = line.p2.x * line.p1.y - line.p1.x * line.p2.y\n    p0x, p0y = self.p1.x, self.p1.y\n    p1x, p1y = self.control_point1.x, self.control_point1.y\n    p2x, p2y = self.control_point2.x, self.control_point2.y\n    p3x, p3y = self.p2.x, self.p2.y\n    c3x = -p0x + 3 * p1x - 3 * p2x + p3x\n    c2x = 3 * p0x - 6 * p1x + 3 * p2x\n    c1x = -3 * p0x + 3 * p1x\n    c0x = p0x\n    c3y = -p0y + 3 * p1y - 3 * p2y + p3y\n    c2y = 3 * p0y - 6 * p1y + 3 * p2y\n    c1y = -3 * p0y + 3 * p1y\n    c0y = p0y\n    coeff3 = a * c3x + b * c3y\n    coeff2 = a * c2x + b * c2y\n    coeff1 = a * c1x + b * c1y\n    coeff0 = a * c0x + b * c0y + c\n    roots = numpy.roots([coeff3, coeff2, coeff1, coeff0])\n    result = []\n    for root in roots:\n        if abs(root.imag) &lt; _CONVERGENCE_TOLERANCE:\n            t = root.real\n            if -_PARAMETER_TOLERANCE &lt;= t &lt;= 1 + _PARAMETER_TOLERANCE:\n                t = max(0.0, min(1.0, t))\n                result.append(self.evaluate(t))\n    return result\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.CubicBezierCurve.get_position_and_angle_at_fraction","title":"get_position_and_angle_at_fraction","text":"<pre><code>get_position_and_angle_at_fraction(fraction: float) -&gt; tuple[Point, float]\n</code></pre> <p>Get both position and angle at a fraction of the arc length.</p> <p>Parameters:</p> Name Type Description Default <code>fraction</code> <code>float</code> <p>Fraction from 0 to 1.</p> required <p>Returns:</p> Type Description <code>tuple[Point, float]</code> <p>Tuple of (point, angle_in_radians).</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def get_position_and_angle_at_fraction(\n    self, fraction: float\n) -&gt; tuple[Point, float]:\n    \"\"\"Get both position and angle at a fraction of the arc length.\n\n    Args:\n        fraction: Fraction from 0 to 1.\n\n    Returns:\n        Tuple of (point, angle_in_radians).\n    \"\"\"\n    total = self.length()\n    if fraction &lt;= 0:\n        t = 0.0\n    elif fraction &gt;= 1:\n        t = 1.0\n    else:\n        t = _find_t_at_arc_length_fraction(self.derivative, total, fraction)\n    pos = self.evaluate(t)\n    dx, dy = self.derivative(t)\n    angle = math.atan2(dy, dx)\n    return (pos, angle)\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.CubicBezierCurve.get_position_at_fraction","title":"get_position_at_fraction","text":"<pre><code>get_position_at_fraction(fraction: float) -&gt; Point\n</code></pre> <p>Get point at a fraction of the arc length.</p> <p>Parameters:</p> Name Type Description Default <code>fraction</code> <code>float</code> <p>Fraction from 0 to 1.</p> required <p>Returns:</p> Type Description <code>Point</code> <p>The point at that fraction.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def get_position_at_fraction(self, fraction: float) -&gt; Point:\n    \"\"\"Get point at a fraction of the arc length.\n\n    Args:\n        fraction: Fraction from 0 to 1.\n\n    Returns:\n        The point at that fraction.\n    \"\"\"\n    if fraction &lt;= 0:\n        return Point(self.p1.x, self.p1.y)\n    if fraction &gt;= 1:\n        return Point(self.p2.x, self.p2.y)\n    total = self.length()\n    t = _find_t_at_arc_length_fraction(self.derivative, total, fraction)\n    return self.evaluate(t)\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.CubicBezierCurve.length","title":"length","text":"<pre><code>length() -&gt; float\n</code></pre> <p>Calculate the arc length of the curve.</p> <p>Returns:</p> Type Description <code>float</code> <p>The arc length.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def length(self) -&gt; float:\n    \"\"\"Calculate the arc length of the curve.\n\n    Returns:\n        The arc length.\n    \"\"\"\n    return _arc_length(self.derivative, 0.0, 1.0)\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.CubicBezierCurve.reversed","title":"reversed","text":"<pre><code>reversed() -&gt; CubicBezierCurve\n</code></pre> <p>Return a reversed copy of the curve.</p> <p>Returns:</p> Type Description <code>CubicBezierCurve</code> <p>A new CubicBezierCurve going in reverse direction.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def reversed(self) -&gt; \"CubicBezierCurve\":\n    \"\"\"Return a reversed copy of the curve.\n\n    Returns:\n        A new CubicBezierCurve going in reverse direction.\n    \"\"\"\n    return CubicBezierCurve(\n        self.p2, self.p1, self.control_point2, self.control_point1\n    )\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.CubicBezierCurve.shortened","title":"shortened","text":"<pre><code>shortened(length: float, start_or_end: Literal['start', 'end'] = 'end') -&gt; CubicBezierCurve\n</code></pre> <p>Return a shortened copy of the curve.</p> <p>Parameters:</p> Name Type Description Default <code>length</code> <code>float</code> <p>Amount to shorten by.</p> required <code>start_or_end</code> <code>Literal['start', 'end']</code> <p>Which end to shorten from.</p> <code>'end'</code> <p>Returns:</p> Type Description <code>CubicBezierCurve</code> <p>A new shortened CubicBezierCurve.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def shortened(\n    self, length: float, start_or_end: typing.Literal[\"start\", \"end\"] = \"end\"\n) -&gt; \"CubicBezierCurve\":\n    \"\"\"Return a shortened copy of the curve.\n\n    Args:\n        length: Amount to shorten by.\n        start_or_end: Which end to shorten from.\n\n    Returns:\n        A new shortened CubicBezierCurve.\n    \"\"\"\n    if length == 0 or self.length() == 0:\n        return copy.deepcopy(self)\n    if start_or_end == \"start\":\n        return self.reversed().shortened(length).reversed()\n    total_length = self.length()\n    if length &gt; total_length:\n        length = total_length\n    fraction = 1 - length / total_length\n    t = _find_t_at_arc_length_fraction(self.derivative, total_length, fraction)\n    left, _ = self.split(t)\n    return left\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.CubicBezierCurve.split","title":"split","text":"<pre><code>split(t: float) -&gt; tuple[CubicBezierCurve, CubicBezierCurve]\n</code></pre> <p>Split the curve at parameter t using De Casteljau subdivision.</p> <p>Parameters:</p> Name Type Description Default <code>t</code> <code>float</code> <p>Parameter value from 0 to 1.</p> required <p>Returns:</p> Type Description <code>tuple[CubicBezierCurve, CubicBezierCurve]</code> <p>Tuple of two CubicBezierCurves.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def split(self, t: float) -&gt; tuple[\"CubicBezierCurve\", \"CubicBezierCurve\"]:\n    \"\"\"Split the curve at parameter t using De Casteljau subdivision.\n\n    Args:\n        t: Parameter value from 0 to 1.\n\n    Returns:\n        Tuple of two CubicBezierCurves.\n    \"\"\"\n    u = 1 - t\n    # Level 1\n    m0x = u * self.p1.x + t * self.control_point1.x\n    m0y = u * self.p1.y + t * self.control_point1.y\n    m1x = u * self.control_point1.x + t * self.control_point2.x\n    m1y = u * self.control_point1.y + t * self.control_point2.y\n    m2x = u * self.control_point2.x + t * self.p2.x\n    m2y = u * self.control_point2.y + t * self.p2.y\n    # Level 2\n    n0x = u * m0x + t * m1x\n    n0y = u * m0y + t * m1y\n    n1x = u * m1x + t * m2x\n    n1y = u * m1y + t * m2y\n    # Level 3 (point on curve)\n    px = u * n0x + t * n1x\n    py = u * n0y + t * n1y\n    mid = Point(px, py)\n    left = CubicBezierCurve(self.p1, mid, Point(m0x, m0y), Point(n0x, n0y))\n    right = CubicBezierCurve(mid, self.p2, Point(n1x, n1y), Point(m2x, m2y))\n    return (left, right)\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.CubicBezierCurve.transformed","title":"transformed","text":"<pre><code>transformed(transformation: Transformation) -&gt; CubicBezierCurve\n</code></pre> <p>Apply a transformation to this curve.</p> <p>Parameters:</p> Name Type Description Default <code>transformation</code> <code>Transformation</code> <p>The transformation to apply.</p> required <p>Returns:</p> Type Description <code>CubicBezierCurve</code> <p>A new transformed CubicBezierCurve.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def transformed(self, transformation: \"Transformation\") -&gt; \"CubicBezierCurve\":\n    \"\"\"Apply a transformation to this curve.\n\n    Args:\n        transformation: The transformation to apply.\n\n    Returns:\n        A new transformed CubicBezierCurve.\n    \"\"\"\n    return CubicBezierCurve(\n        self.p1.transformed(transformation),\n        self.p2.transformed(transformation),\n        self.control_point1.transformed(transformation),\n        self.control_point2.transformed(transformation),\n    )\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.EllipticalArc","title":"EllipticalArc  <code>dataclass</code>","text":"<pre><code>EllipticalArc(p1: Point, p2: Point, rx: float, ry: float, x_axis_rotation: float, arc_flag: int, sweep_flag: int)\n</code></pre> <p>               Bases: <code>GeometryObject</code></p> <p>Represents an elliptical arc.</p> <p>Examples:</p> <pre><code>arc = EllipticalArc(\n    Point(0.0, 0.0), Point(10.0, 0.0), 5, 5, 0, 0, 1\n)\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>p1</code> <code>Point</code> <p>Start point.</p> required <code>p2</code> <code>Point</code> <p>End point.</p> required <code>rx</code> <code>float</code> <p>X-radius of the ellipse.</p> required <code>ry</code> <code>float</code> <p>Y-radius of the ellipse.</p> required <code>x_axis_rotation</code> <code>float</code> <p>Rotation of the x-axis in radians.</p> required <code>arc_flag</code> <code>int</code> <p>Large arc flag (0 or 1).</p> required <code>sweep_flag</code> <code>int</code> <p>Sweep flag (0 or 1).</p> required <p>Methods:</p> Name Description <code>__post_init__</code> <p>Round the radii to the rounding precision after initialization.</p> <code>bbox</code> <p>Get the bounding box of the arc.</p> <code>derivative</code> <p>Compute the derivative of the arc at parameter t.</p> <code>evaluate</code> <p>Evaluate the arc at parameter t using center parameterization.</p> <code>get_angle_at_fraction</code> <p>Get angle at a fraction along the arc.</p> <code>get_center</code> <p>Get the center point of the ellipse.</p> <code>get_center_parameterization</code> <p>Get the center parameterization of the arc.</p> <code>get_intersection_with_line</code> <p>Get intersection with a line.</p> <code>get_position_and_angle_at_fraction</code> <p>Get both position and angle at a fraction.</p> <code>get_position_at_fraction</code> <p>Get point at a fraction along the arc.</p> <code>length</code> <p>Calculate the length of the arc.</p> <code>reversed</code> <p>Return a reversed copy of the arc.</p> <code>shortened</code> <p>Return a shortened copy of the arc.</p> <code>transformed</code> <p>Apply a transformation to this arc.</p>"},{"location":"api_reference/geometry/#momapy.geometry.EllipticalArc.__post_init__","title":"__post_init__","text":"<pre><code>__post_init__() -&gt; None\n</code></pre> <p>Round the radii to the rounding precision after initialization.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def __post_init__(self) -&gt; None:\n    \"\"\"Round the radii to the rounding precision after initialization.\"\"\"\n    object.__setattr__(self, \"rx\", round(self.rx, ROUNDING))\n    object.__setattr__(self, \"ry\", round(self.ry, ROUNDING))\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.EllipticalArc.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Get the bounding box of the arc.</p> <p>Returns:</p> Type Description <code>Bbox</code> <p>A Bbox enclosing the arc.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def bbox(self) -&gt; \"Bbox\":\n    \"\"\"Get the bounding box of the arc.\n\n    Returns:\n        A Bbox enclosing the arc.\n    \"\"\"\n    if self._is_degenerate_line():\n        return Bbox.around_points([self.p1, self.p2])\n    cx, cy, rx, ry, sigma, theta1, theta2, delta_theta = (\n        self.get_center_parameterization()\n    )\n    cos_sigma = math.cos(sigma)\n    sin_sigma = math.sin(sigma)\n    # Extrema of x(theta): dx/dtheta = 0\n    # -rx*sin(theta)*cos(sigma) - ry*cos(theta)*sin(sigma) = 0\n    # tan(theta) = -(ry*sin(sigma)) / (rx*cos(sigma))\n    theta_x = math.atan2(-ry * sin_sigma, rx * cos_sigma)\n    # Extrema of y(theta): dy/dtheta = 0\n    # -rx*sin(theta)*sin(sigma) + ry*cos(theta)*cos(sigma) = 0\n    # tan(theta) = (ry*cos(sigma)) / (rx*sin(sigma))\n    theta_y = math.atan2(ry * cos_sigma, rx * sin_sigma)\n    # Collect candidate points: endpoints + extrema within arc range\n    points = [self.p1, self.p2]\n    for theta_base in (theta_x, theta_y):\n        # Check both solutions (theta_base and theta_base + pi)\n        for candidate in (theta_base, theta_base + math.pi):\n            # Normalize candidate into [theta_min - 2pi, theta_max + 2pi]\n            # and check if it falls within range\n            normalized = candidate\n            while normalized &lt; theta1 - 2 * math.pi:\n                normalized += 2 * math.pi\n            while normalized &gt; theta1 + delta_theta + 2 * math.pi:\n                normalized -= 2 * math.pi\n            # Try multiple periods\n            for offset in (\n                -2 * math.pi,\n                0,\n                2 * math.pi,\n            ):\n                theta_c = normalized + offset\n                t = (theta_c - theta1) / delta_theta\n                if -_PARAMETER_TOLERANCE &lt;= t &lt;= 1 + _PARAMETER_TOLERANCE:\n                    points.append(self.evaluate(max(0, min(1, t))))\n    return Bbox.around_points(points)\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.EllipticalArc.derivative","title":"derivative","text":"<pre><code>derivative(t: float) -&gt; tuple[float, float]\n</code></pre> <p>Compute the derivative of the arc at parameter t.</p> <p>Parameters:</p> Name Type Description Default <code>t</code> <code>float</code> <p>Parameter value from 0 to 1.</p> required <p>Returns:</p> Type Description <code>tuple[float, float]</code> <p>Tuple of (dx, dy) derivatives.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def derivative(self, t: float) -&gt; tuple[float, float]:\n    \"\"\"Compute the derivative of the arc at parameter t.\n\n    Args:\n        t: Parameter value from 0 to 1.\n\n    Returns:\n        Tuple of (dx, dy) derivatives.\n    \"\"\"\n    if self._is_degenerate_line():\n        return (self.p2.x - self.p1.x, self.p2.y - self.p1.y)\n    cx, cy, rx, ry, sigma, theta1, theta2, delta_theta = (\n        self.get_center_parameterization()\n    )\n    theta = theta1 + t * delta_theta\n    cos_sigma = math.cos(sigma)\n    sin_sigma = math.sin(sigma)\n    cos_theta = math.cos(theta)\n    sin_theta = math.sin(theta)\n    dx = delta_theta * (-rx * sin_theta * cos_sigma - ry * cos_theta * sin_sigma)\n    dy = delta_theta * (-rx * sin_theta * sin_sigma + ry * cos_theta * cos_sigma)\n    return (dx, dy)\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.EllipticalArc.evaluate","title":"evaluate","text":"<pre><code>evaluate(t: float) -&gt; Point\n</code></pre> <p>Evaluate the arc at parameter t using center parameterization.</p> <p>Parameters:</p> Name Type Description Default <code>t</code> <code>float</code> <p>Parameter value from 0 to 1.</p> required <p>Returns:</p> Type Description <code>Point</code> <p>The point at parameter t.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def evaluate(self, t: float) -&gt; Point:\n    \"\"\"Evaluate the arc at parameter t using center parameterization.\n\n    Args:\n        t: Parameter value from 0 to 1.\n\n    Returns:\n        The point at parameter t.\n    \"\"\"\n    if self._is_degenerate_line():\n        return Point(\n            self.p1.x + t * (self.p2.x - self.p1.x),\n            self.p1.y + t * (self.p2.y - self.p1.y),\n        )\n    cx, cy, rx, ry, sigma, theta1, theta2, delta_theta = (\n        self.get_center_parameterization()\n    )\n    theta = theta1 + t * delta_theta\n    cos_sigma = math.cos(sigma)\n    sin_sigma = math.sin(sigma)\n    cos_theta = math.cos(theta)\n    sin_theta = math.sin(theta)\n    x = cx + rx * cos_theta * cos_sigma - ry * sin_theta * sin_sigma\n    y = cy + rx * cos_theta * sin_sigma + ry * sin_theta * cos_sigma\n    return Point(x, y)\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.EllipticalArc.get_angle_at_fraction","title":"get_angle_at_fraction","text":"<pre><code>get_angle_at_fraction(fraction: float) -&gt; float\n</code></pre> <p>Get angle at a fraction along the arc.</p> <p>Parameters:</p> Name Type Description Default <code>fraction</code> <code>float</code> <p>Fraction from 0 to 1.</p> required <p>Returns:</p> Type Description <code>float</code> <p>Angle in radians.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def get_angle_at_fraction(self, fraction: float) -&gt; float:\n    \"\"\"Get angle at a fraction along the arc.\n\n    Args:\n        fraction: Fraction from 0 to 1.\n\n    Returns:\n        Angle in radians.\n    \"\"\"\n    total = self.length()\n    if total == 0:\n        return 0.0\n    if fraction &lt;= 0:\n        t = 0.0\n    elif fraction &gt;= 1:\n        t = 1.0\n    else:\n        t = _find_t_at_arc_length_fraction(self.derivative, total, fraction)\n    dx, dy = self.derivative(t)\n    return math.atan2(dy, dx)\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.EllipticalArc.get_center","title":"get_center","text":"<pre><code>get_center() -&gt; Point\n</code></pre> <p>Get the center point of the ellipse.</p> <p>Returns:</p> Type Description <code>Point</code> <p>The center point. For a degenerate zero-radius arc (a straight</p> <code>Point</code> <p>line) there is no ellipse center, so the midpoint of the endpoints</p> <code>Point</code> <p>is returned.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def get_center(self) -&gt; Point:\n    \"\"\"Get the center point of the ellipse.\n\n    Returns:\n        The center point. For a degenerate zero-radius arc (a straight\n        line) there is no ellipse center, so the midpoint of the endpoints\n        is returned.\n    \"\"\"\n    if self._is_degenerate_line():\n        return Point((self.p1.x + self.p2.x) / 2, (self.p1.y + self.p2.y) / 2)\n    cx, cy, *_ = self.get_center_parameterization()\n    return Point(cx, cy)\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.EllipticalArc.get_center_parameterization","title":"get_center_parameterization","text":"<pre><code>get_center_parameterization() -&gt; tuple[float, float, float, float, float, float, float, float]\n</code></pre> <p>Get the center parameterization of the arc.</p> <p>Returns:</p> Type Description <code>tuple[float, float, float, float, float, float, float, float]</code> <p>Tuple of (cx, cy, rx, ry, sigma, theta1, theta2, delta_theta).</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def get_center_parameterization(\n    self,\n) -&gt; tuple[float, float, float, float, float, float, float, float]:\n    \"\"\"Get the center parameterization of the arc.\n\n    Returns:\n        Tuple of (cx, cy, rx, ry, sigma, theta1, theta2, delta_theta).\n    \"\"\"\n    sigma = self.x_axis_rotation\n    if self.p1 == self.p2:\n        # Degenerate arc (coincident endpoints): collapse to the start\n        # point with zero radii and zero sweep. This keeps evaluate/\n        # derivative/length well-defined (position p1, derivative 0,\n        # length 0) instead of dividing by zero below.\n        return (self.p1.x, self.p1.y, 0.0, 0.0, sigma, 0.0, 0.0, 0.0)\n    if self.rx == 0 or self.ry == 0:\n        raise ValueError(\n            \"elliptical arc with a zero radius and distinct endpoints \"\n            \"has no center parameterization\"\n        )\n    x1, y1 = self.p1.x, self.p1.y\n    x2, y2 = self.p2.x, self.p2.y\n    rx = self.rx\n    ry = self.ry\n    fa = self.arc_flag\n    fs = self.sweep_flag\n    x1p = math.cos(sigma) * ((x1 - x2) / 2) + math.sin(sigma) * ((y1 - y2) / 2)\n    y1p = -math.sin(sigma) * ((x1 - x2) / 2) + math.cos(sigma) * ((y1 - y2) / 2)\n    radii_correction = x1p**2 / rx**2 + y1p**2 / ry**2\n    if radii_correction &gt; 1:\n        rx = math.sqrt(radii_correction) * rx\n        ry = math.sqrt(radii_correction) * ry\n    r = rx**2 * ry**2 - rx**2 * y1p**2 - ry**2 * x1p**2\n    if r &lt; 0:\n        r = 0\n    a = math.sqrt(r / (rx**2 * y1p**2 + ry**2 * x1p**2))\n    if fa == fs:\n        a = -a\n    cxp = a * rx * y1p / ry\n    cyp = -a * ry * x1p / rx\n    cx = math.cos(sigma) * cxp - math.sin(sigma) * cyp + (x1 + x2) / 2\n    cy = math.sin(sigma) * cxp + math.cos(sigma) * cyp + (y1 + y2) / 2\n    theta1 = _get_angle_between_segments(\n        Segment(Point(0.0, 0.0), Point(1.0, 0.0)),\n        Segment(Point(0.0, 0.0), Point((x1p - cxp) / rx, (y1p - cyp) / ry)),\n    )\n    delta_theta = _get_angle_between_segments(\n        Segment(Point(0.0, 0.0), Point((x1p - cxp) / rx, (y1p - cyp) / ry)),\n        Segment(Point(0.0, 0.0), Point(-(x1p + cxp) / rx, -(y1p + cyp) / ry)),\n    )\n    if fs == 0 and delta_theta &gt; 0:\n        delta_theta -= 2 * math.pi\n    elif fs == 1 and delta_theta &lt; 0:\n        delta_theta += 2 * math.pi\n    theta2 = theta1 + delta_theta\n    return cx, cy, rx, ry, sigma, theta1, theta2, delta_theta\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.EllipticalArc.get_intersection_with_line","title":"get_intersection_with_line","text":"<pre><code>get_intersection_with_line(line: Line) -&gt; list[Point]\n</code></pre> <p>Get intersection with a line.</p> <p>Uses analytical ellipse-line intersection by solving Acos(theta) + Bsin(theta) + C = 0 via atan2.</p> <p>Parameters:</p> Name Type Description Default <code>line</code> <code>Line</code> <p>The line to intersect with.</p> required <p>Returns:</p> Type Description <code>list[Point]</code> <p>List of intersection points.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def get_intersection_with_line(self, line: Line) -&gt; list[Point]:\n    \"\"\"Get intersection with a line.\n\n    Uses analytical ellipse-line intersection by solving\n    A*cos(theta) + B*sin(theta) + C = 0 via atan2.\n\n    Args:\n        line: The line to intersect with.\n\n    Returns:\n        List of intersection points.\n    \"\"\"\n    if self._is_degenerate_line():\n        return Segment(self.p1, self.p2).get_intersection_with_line(line)\n    cx, cy, rx, ry, sigma, theta1, theta2, delta_theta = (\n        self.get_center_parameterization()\n    )\n    # Line in implicit form: a*x + b*y + c = 0\n    dx = line.p2.x - line.p1.x\n    dy = line.p2.y - line.p1.y\n    a = -dy\n    b = dx\n    c = -(a * line.p1.x + b * line.p1.y)\n    cos_sigma = math.cos(sigma)\n    sin_sigma = math.sin(sigma)\n    # Substitute parametric arc equations into line equation\n    A = a * rx * cos_sigma + b * rx * sin_sigma\n    B = a * (-ry * sin_sigma) + b * ry * cos_sigma\n    C = a * cx + b * cy + c\n    R = math.sqrt(A * A + B * B)\n    if R &lt; _ZERO_TOLERANCE:\n        return []\n    ratio = -C / R\n    if abs(ratio) &gt; 1 + _ZERO_TOLERANCE:\n        return []\n    ratio = max(-1.0, min(1.0, ratio))\n    phi = math.atan2(B, A)\n    acos_val = math.acos(ratio)\n    candidates = [phi + acos_val, phi - acos_val]\n    result = []\n    for theta_candidate in candidates:\n        # Normalize into range by trying multiple periods\n        for k in range(-3, 4):\n            theta_c = theta_candidate + k * 2 * math.pi\n            t = (theta_c - theta1) / delta_theta\n            if -_PARAMETER_TOLERANCE &lt;= t &lt;= 1 + _PARAMETER_TOLERANCE:\n                t = max(0.0, min(1.0, t))\n                point = self.evaluate(t)\n                # Avoid duplicates\n                is_dup = False\n                for existing in result:\n                    if (\n                        abs(existing.x - point.x) &lt; _ROUNDING_TOLERANCE\n                        and abs(existing.y - point.y) &lt; _ROUNDING_TOLERANCE\n                    ):\n                        is_dup = True\n                        break\n                if not is_dup:\n                    result.append(point)\n    return result\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.EllipticalArc.get_position_and_angle_at_fraction","title":"get_position_and_angle_at_fraction","text":"<pre><code>get_position_and_angle_at_fraction(fraction: float) -&gt; tuple[Point, float]\n</code></pre> <p>Get both position and angle at a fraction.</p> <p>Parameters:</p> Name Type Description Default <code>fraction</code> <code>float</code> <p>Fraction from 0 to 1.</p> required <p>Returns:</p> Type Description <code>tuple[Point, float]</code> <p>Tuple of (point, angle).</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def get_position_and_angle_at_fraction(\n    self, fraction: float\n) -&gt; tuple[Point, float]:\n    \"\"\"Get both position and angle at a fraction.\n\n    Args:\n        fraction: Fraction from 0 to 1.\n\n    Returns:\n        Tuple of (point, angle).\n    \"\"\"\n    total = self.length()\n    if total == 0:\n        return (Point(self.p1.x, self.p1.y), 0.0)\n    if fraction &lt;= 0:\n        t = 0.0\n    elif fraction &gt;= 1:\n        t = 1.0\n    else:\n        t = _find_t_at_arc_length_fraction(self.derivative, total, fraction)\n    pos = self.evaluate(t)\n    dx, dy = self.derivative(t)\n    angle = math.atan2(dy, dx)\n    return (pos, angle)\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.EllipticalArc.get_position_at_fraction","title":"get_position_at_fraction","text":"<pre><code>get_position_at_fraction(fraction: float) -&gt; Point\n</code></pre> <p>Get point at a fraction along the arc.</p> <p>Parameters:</p> Name Type Description Default <code>fraction</code> <code>float</code> <p>Fraction from 0 to 1.</p> required <p>Returns:</p> Type Description <code>Point</code> <p>The point at that fraction.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def get_position_at_fraction(self, fraction: float) -&gt; Point:\n    \"\"\"Get point at a fraction along the arc.\n\n    Args:\n        fraction: Fraction from 0 to 1.\n\n    Returns:\n        The point at that fraction.\n    \"\"\"\n    if fraction &lt;= 0:\n        return Point(self.p1.x, self.p1.y)\n    if fraction &gt;= 1:\n        return Point(self.p2.x, self.p2.y)\n    total = self.length()\n    if total == 0:\n        return Point(self.p1.x, self.p1.y)\n    t = _find_t_at_arc_length_fraction(self.derivative, total, fraction)\n    return self.evaluate(t)\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.EllipticalArc.length","title":"length","text":"<pre><code>length() -&gt; float\n</code></pre> <p>Calculate the length of the arc.</p> <p>Returns:</p> Type Description <code>float</code> <p>The arc length.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def length(self) -&gt; float:\n    \"\"\"Calculate the length of the arc.\n\n    Returns:\n        The arc length.\n    \"\"\"\n    return _arc_length(self.derivative, 0.0, 1.0)\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.EllipticalArc.reversed","title":"reversed","text":"<pre><code>reversed() -&gt; EllipticalArc\n</code></pre> <p>Return a reversed copy of the arc.</p> <p>Returns:</p> Type Description <code>EllipticalArc</code> <p>A new EllipticalArc going in reverse direction.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def reversed(self) -&gt; \"EllipticalArc\":\n    \"\"\"Return a reversed copy of the arc.\n\n    Returns:\n        A new EllipticalArc going in reverse direction.\n    \"\"\"\n    return EllipticalArc(\n        self.p2,\n        self.p1,\n        self.rx,\n        self.ry,\n        self.x_axis_rotation,\n        self.arc_flag,\n        abs(self.sweep_flag - 1),\n    )\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.EllipticalArc.shortened","title":"shortened","text":"<pre><code>shortened(length: float, start_or_end: Literal['start', 'end'] = 'end') -&gt; EllipticalArc\n</code></pre> <p>Return a shortened copy of the arc.</p> <p>Parameters:</p> Name Type Description Default <code>length</code> <code>float</code> <p>Amount to shorten by.</p> required <code>start_or_end</code> <code>Literal['start', 'end']</code> <p>Which end to shorten from.</p> <code>'end'</code> <p>Returns:</p> Type Description <code>EllipticalArc</code> <p>A new shortened EllipticalArc.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def shortened(\n    self,\n    length: float,\n    start_or_end: typing.Literal[\"start\", \"end\"] = \"end\",\n) -&gt; \"EllipticalArc\":\n    \"\"\"Return a shortened copy of the arc.\n\n    Args:\n        length: Amount to shorten by.\n        start_or_end: Which end to shorten from.\n\n    Returns:\n        A new shortened EllipticalArc.\n    \"\"\"\n    total_length = self.length()\n    if length == 0 or total_length == 0:\n        return copy.deepcopy(self)\n    if start_or_end == \"start\":\n        return self.reversed().shortened(length).reversed()\n    if length &gt; total_length:\n        length = total_length\n    fraction = 1 - length / total_length\n    point = self.get_position_at_fraction(fraction)\n    return dataclasses.replace(self, p2=point)\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.EllipticalArc.transformed","title":"transformed","text":"<pre><code>transformed(transformation: Transformation) -&gt; EllipticalArc\n</code></pre> <p>Apply a transformation to this arc.</p> <p>Parameters:</p> Name Type Description Default <code>transformation</code> <code>Transformation</code> <p>The transformation to apply.</p> required <p>Returns:</p> Type Description <code>EllipticalArc</code> <p>A new transformed EllipticalArc.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def transformed(self, transformation: \"Transformation\") -&gt; \"EllipticalArc\":\n    \"\"\"Apply a transformation to this arc.\n\n    Args:\n        transformation: The transformation to apply.\n\n    Returns:\n        A new transformed EllipticalArc.\n    \"\"\"\n    rotation = self.x_axis_rotation\n    east = Point(\n        math.cos(rotation) * self.rx,\n        math.sin(rotation) * self.rx,\n    )\n    north = Point(\n        -math.sin(rotation) * self.ry,\n        math.cos(rotation) * self.ry,\n    )\n    new_center = Point(0.0, 0.0).transformed(transformation)\n    new_east = east.transformed(transformation)\n    new_north = north.transformed(transformation)\n    new_rx = Segment(new_center, new_east).length()\n    new_ry = Segment(new_center, new_north).length()\n    new_start_point = self.p1.transformed(transformation)\n    new_end_point = self.p2.transformed(transformation)\n    new_x_axis_rotation = Line(new_center, new_east).get_angle_to_horizontal()\n    return EllipticalArc(\n        p1=new_start_point,\n        p2=new_end_point,\n        rx=new_rx,\n        ry=new_ry,\n        x_axis_rotation=new_x_axis_rotation,\n        arc_flag=self.arc_flag,\n        sweep_flag=self.sweep_flag,\n    )\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.GeometryObject","title":"GeometryObject  <code>dataclass</code>","text":"<pre><code>GeometryObject()\n</code></pre> <p>               Bases: <code>ABC</code></p> <p>Abstract base class for all geometry objects.</p>"},{"location":"api_reference/geometry/#momapy.geometry.Line","title":"Line  <code>dataclass</code>","text":"<pre><code>Line(p1: Point, p2: Point)\n</code></pre> <p>               Bases: <code>GeometryObject</code></p> <p>Represents an infinite line defined by two points.</p> <p>Examples:</p> <pre><code>line = Line(Point(0.0, 0.0), Point(10.0, 10.0))\nline.slope()\nline.intercept()\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>p1</code> <code>Point</code> <p>First point on the line.</p> required <code>p2</code> <code>Point</code> <p>Second point on the line.</p> required <p>Methods:</p> Name Description <code>get_angle_to_horizontal</code> <p>Get the angle of the line relative to horizontal.</p> <code>get_distance_to_point</code> <p>Get perpendicular distance from a point to this line.</p> <code>get_intersection_with_line</code> <p>Get intersection with another line.</p> <code>has_point</code> <p>Check if a point lies on this line.</p> <code>intercept</code> <p>Calculate the y-intercept of the line.</p> <code>is_coincident_to_line</code> <p>Check if this line is coincident with another.</p> <code>is_parallel_to_line</code> <p>Check if this line is parallel to another.</p> <code>reversed</code> <p>Return a reversed copy of the line.</p> <code>slope</code> <p>Calculate the slope of the line.</p> <code>transformed</code> <p>Apply a transformation to this line.</p>"},{"location":"api_reference/geometry/#momapy.geometry.Line.get_angle_to_horizontal","title":"get_angle_to_horizontal","text":"<pre><code>get_angle_to_horizontal() -&gt; float\n</code></pre> <p>Get the angle of the line relative to horizontal.</p> <p>Returns:</p> Type Description <code>float</code> <p>Angle in radians.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def get_angle_to_horizontal(self) -&gt; float:\n    \"\"\"Get the angle of the line relative to horizontal.\n\n    Returns:\n        Angle in radians.\n    \"\"\"\n    angle = math.atan2(self.p2.y - self.p1.y, self.p2.x - self.p1.x)\n    return get_normalized_angle(angle)\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.Line.get_distance_to_point","title":"get_distance_to_point","text":"<pre><code>get_distance_to_point(point: Point) -&gt; float\n</code></pre> <p>Get perpendicular distance from a point to this line.</p> <p>Parameters:</p> Name Type Description Default <code>point</code> <code>Point</code> <p>The point to measure from.</p> required <p>Returns:</p> Type Description <code>float</code> <p>The perpendicular distance.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def get_distance_to_point(self, point: Point) -&gt; float:\n    \"\"\"Get perpendicular distance from a point to this line.\n\n    Args:\n        point: The point to measure from.\n\n    Returns:\n        The perpendicular distance.\n    \"\"\"\n    return abs(\n        (self.p2.x - self.p1.x) * (self.p1.y - point.y)\n        - (self.p1.x - point.x) * (self.p2.y - self.p1.y)\n    ) / math.sqrt((self.p2.x - self.p1.x) ** 2 + (self.p2.y - self.p1.y) ** 2)\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.Line.get_intersection_with_line","title":"get_intersection_with_line","text":"<pre><code>get_intersection_with_line(line: Line) -&gt; list[Line] | list[Point]\n</code></pre> <p>Get intersection with another line.</p> <p>Parameters:</p> Name Type Description Default <code>line</code> <code>Line</code> <p>The other line.</p> required <p>Returns:</p> Type Description <code>list[Line] | list[Point]</code> <p>List containing intersection point(s) or the coincident line.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def get_intersection_with_line(self, line: \"Line\") -&gt; list[\"Line\"] | list[Point]:\n    \"\"\"Get intersection with another line.\n\n    Args:\n        line: The other line.\n\n    Returns:\n        List containing intersection point(s) or the coincident line.\n    \"\"\"\n    slope1 = self.slope()\n    intercept1 = self.intercept()\n    slope2 = line.slope()\n    intercept2 = line.intercept()\n    if self.is_coincident_to_line(line):\n        intersection = [copy.deepcopy(self)]\n    elif self.is_parallel_to_line(line):\n        intersection = []\n    elif math.isnan(slope1):\n        intersection = [Point(self.p1.x, slope2 * self.p1.x + intercept2)]\n    elif math.isnan(slope2):\n        intersection = [Point(line.p1.x, slope1 * line.p1.x + intercept1)]\n    else:\n        d = (self.p1.x - self.p2.x) * (line.p1.y - line.p2.y) - (\n            self.p1.y - self.p2.y\n        ) * (line.p1.x - line.p2.x)\n        px = (\n            (self.p1.x * self.p2.y - self.p1.y * self.p2.x)\n            * (line.p1.x - line.p2.x)\n            - (self.p1.x - self.p2.x)\n            * (line.p1.x * line.p2.y - line.p1.y * line.p2.x)\n        ) / d\n        py = (\n            (self.p1.x * self.p2.y - self.p1.y * self.p2.x)\n            * (line.p1.y - line.p2.y)\n            - (self.p1.y - self.p2.y)\n            * (line.p1.x * line.p2.y - line.p1.y * line.p2.x)\n        ) / d\n        intersection = [Point(px, py)]\n    return intersection\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.Line.has_point","title":"has_point","text":"<pre><code>has_point(point: Point, max_distance: float = _ROUNDING_TOLERANCE) -&gt; bool\n</code></pre> <p>Check if a point lies on this line.</p> <p>Parameters:</p> Name Type Description Default <code>point</code> <code>Point</code> <p>The point to check.</p> required <code>max_distance</code> <code>float</code> <p>Maximum allowed distance from line.</p> <code>_ROUNDING_TOLERANCE</code> <p>Returns:</p> Type Description <code>bool</code> <p>True if point is on the line within tolerance.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def has_point(\n    self, point: Point, max_distance: float = _ROUNDING_TOLERANCE\n) -&gt; bool:\n    \"\"\"Check if a point lies on this line.\n\n    Args:\n        point: The point to check.\n        max_distance: Maximum allowed distance from line.\n\n    Returns:\n        True if point is on the line within tolerance.\n    \"\"\"\n    return self.get_distance_to_point(point) &lt;= max_distance\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.Line.intercept","title":"intercept","text":"<pre><code>intercept() -&gt; float\n</code></pre> <p>Calculate the y-intercept of the line.</p> <p>Returns:</p> Type Description <code>float</code> <p>The y-intercept, or NaN if vertical.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def intercept(self) -&gt; float:\n    \"\"\"Calculate the y-intercept of the line.\n\n    Returns:\n        The y-intercept, or NaN if vertical.\n    \"\"\"\n    slope = self.slope()\n    if not math.isnan(slope):\n        return round(self.p1.y - slope * self.p1.x, ROUNDING)\n    return float(\"NAN\")\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.Line.is_coincident_to_line","title":"is_coincident_to_line","text":"<pre><code>is_coincident_to_line(line: Line) -&gt; bool\n</code></pre> <p>Check if this line is coincident with another.</p> <p>Parameters:</p> Name Type Description Default <code>line</code> <code>Line</code> <p>The other line.</p> required <p>Returns:</p> Type Description <code>bool</code> <p>True if coincident (same infinite line).</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def is_coincident_to_line(self, line: \"Line\") -&gt; bool:\n    \"\"\"Check if this line is coincident with another.\n\n    Args:\n        line: The other line.\n\n    Returns:\n        True if coincident (same infinite line).\n    \"\"\"\n    slope1 = self.slope()\n    intercept1 = self.intercept()\n    slope2 = line.slope()\n    intercept2 = line.intercept()\n    return (\n        math.isnan(slope1)\n        and math.isnan(slope2)\n        and self.p1.x == line.p1.x\n        or slope1 == slope2\n        and intercept1 == intercept2\n    )\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.Line.is_parallel_to_line","title":"is_parallel_to_line","text":"<pre><code>is_parallel_to_line(line: Line) -&gt; bool\n</code></pre> <p>Check if this line is parallel to another.</p> <p>Parameters:</p> Name Type Description Default <code>line</code> <code>Line</code> <p>The other line.</p> required <p>Returns:</p> Type Description <code>bool</code> <p>True if parallel.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def is_parallel_to_line(self, line: \"Line\") -&gt; bool:\n    \"\"\"Check if this line is parallel to another.\n\n    Args:\n        line: The other line.\n\n    Returns:\n        True if parallel.\n    \"\"\"\n    slope1 = self.slope()\n    slope2 = line.slope()\n    if math.isnan(slope1) and math.isnan(slope2):\n        return True\n    return slope1 == slope2\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.Line.reversed","title":"reversed","text":"<pre><code>reversed() -&gt; Line\n</code></pre> <p>Return a reversed copy of the line.</p> <p>Returns:</p> Type Description <code>Line</code> <p>A new Line with p1 and p2 swapped.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def reversed(self) -&gt; \"Line\":\n    \"\"\"Return a reversed copy of the line.\n\n    Returns:\n        A new Line with p1 and p2 swapped.\n    \"\"\"\n    return Line(self.p2, self.p1)\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.Line.slope","title":"slope","text":"<pre><code>slope() -&gt; float\n</code></pre> <p>Calculate the slope of the line.</p> <p>Returns:</p> Type Description <code>float</code> <p>The slope, or NaN if vertical.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def slope(self) -&gt; float:\n    \"\"\"Calculate the slope of the line.\n\n    Returns:\n        The slope, or NaN if vertical.\n    \"\"\"\n    if self.p1.x != self.p2.x:\n        return round((self.p2.y - self.p1.y) / (self.p2.x - self.p1.x), ROUNDING)\n    return float(\"NAN\")\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.Line.transformed","title":"transformed","text":"<pre><code>transformed(transformation: Transformation) -&gt; Line\n</code></pre> <p>Apply a transformation to this line.</p> <p>Parameters:</p> Name Type Description Default <code>transformation</code> <code>Transformation</code> <p>The transformation to apply.</p> required <p>Returns:</p> Type Description <code>Line</code> <p>A new transformed Line.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def transformed(self, transformation: \"Transformation\") -&gt; \"Line\":\n    \"\"\"Apply a transformation to this line.\n\n    Args:\n        transformation: The transformation to apply.\n\n    Returns:\n        A new transformed Line.\n    \"\"\"\n    return Line(\n        self.p1.transformed(transformation),\n        self.p2.transformed(transformation),\n    )\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.MatrixTransformation","title":"MatrixTransformation  <code>dataclass</code>","text":"<pre><code>MatrixTransformation(m: NDArray)\n</code></pre> <p>               Bases: <code>Transformation</code></p> <p>Represents a transformation as a 3x3 matrix.</p> <p>Parameters:</p> Name Type Description Default <code>m</code> <code>NDArray</code> <p>The 3x3 transformation matrix.</p> required <p>Methods:</p> Name Description <code>__mul__</code> <p>Return the composition of the transformation with another transformation.</p> <code>inverted</code> <p>Get the inverse transformation.</p> <code>to_matrix</code> <p>Get the matrix representation.</p>"},{"location":"api_reference/geometry/#momapy.geometry.MatrixTransformation.__mul__","title":"__mul__","text":"<pre><code>__mul__(other: Transformation) -&gt; MatrixTransformation\n</code></pre> <p>Return the composition of the transformation with another transformation.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def __mul__(self, other: \"Transformation\") -&gt; \"MatrixTransformation\":\n    \"\"\"Return the composition of the transformation with another transformation.\"\"\"\n    return MatrixTransformation(numpy.matmul(self.to_matrix(), other.to_matrix()))\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.MatrixTransformation.inverted","title":"inverted","text":"<pre><code>inverted() -&gt; Transformation\n</code></pre> <p>Get the inverse transformation.</p> <p>Returns:</p> Type Description <code>Transformation</code> <p>The inverse matrix transformation.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def inverted(self) -&gt; Transformation:\n    \"\"\"Get the inverse transformation.\n\n    Returns:\n        The inverse matrix transformation.\n    \"\"\"\n    return MatrixTransformation(numpy.linalg.inv(self.m))\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.MatrixTransformation.to_matrix","title":"to_matrix","text":"<pre><code>to_matrix() -&gt; NDArray\n</code></pre> <p>Get the matrix representation.</p> <p>Returns:</p> Type Description <code>NDArray</code> <p>The 3x3 matrix.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def to_matrix(self) -&gt; numpy.typing.NDArray:\n    \"\"\"Get the matrix representation.\n\n    Returns:\n        The 3x3 matrix.\n    \"\"\"\n    return self.m\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.Point","title":"Point  <code>dataclass</code>","text":"<pre><code>Point(x: float, y: float)\n</code></pre> <p>               Bases: <code>GeometryObject</code></p> <p>Represents a 2D point with x and y coordinates.</p> <p>Examples:</p> <pre><code>p = Point(10.0, 20.0)\np.x\np + (5, 5)\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>x</code> <code>float</code> <p>The x-coordinate.</p> required <code>y</code> <code>float</code> <p>The y-coordinate.</p> required <p>Methods:</p> Name Description <code>__add__</code> <p>Return the sum of the point and another point or <code>(x, y)</code> pair.</p> <code>__iter__</code> <p>Yield the x and y coordinates of the point in order.</p> <code>__mul__</code> <p>Return the point scaled by a scalar.</p> <code>__post_init__</code> <p>Round the coordinates to the rounding precision after initialization.</p> <code>__sub__</code> <p>Return the difference of the point and another point or <code>(x, y)</code> pair.</p> <code>__truediv__</code> <p>Return the point divided by a scalar.</p> <code>bbox</code> <p>Get the bounding box of this point.</p> <code>from_tuple</code> <p>Create a Point from a tuple.</p> <code>get_angle_to_horizontal</code> <p>Get the angle from origin to this point relative to horizontal.</p> <code>get_intersection_with_line</code> <p>Get intersection points with a line.</p> <code>isnan</code> <p>Check if either coordinate is NaN.</p> <code>reversed</code> <p>Return a reversed copy (identity for points).</p> <code>round</code> <p>Round coordinates to specified digits.</p> <code>to_matrix</code> <p>Convert to a 3x1 numpy matrix for transformation operations.</p> <code>to_tuple</code> <p>Convert to a tuple.</p> <code>transformed</code> <p>Apply a transformation to this point.</p>"},{"location":"api_reference/geometry/#momapy.geometry.Point.__add__","title":"__add__","text":"<pre><code>__add__(xy: Point | tuple[float, float]) -&gt; Point\n</code></pre> <p>Return the sum of the point and another point or <code>(x, y)</code> pair.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def __add__(self, xy: \"Point | tuple[float, float]\") -&gt; \"Point\":\n    \"\"\"Return the sum of the point and another point or `(x, y)` pair.\"\"\"\n    if isinstance_or_builder(xy, Point):\n        xy = (xy.x, xy.y)\n    return Point(self.x + xy[0], self.y + xy[1])\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.Point.__iter__","title":"__iter__","text":"<pre><code>__iter__() -&gt; Iterator[float]\n</code></pre> <p>Yield the x and y coordinates of the point in order.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def __iter__(self) -&gt; collections.abc.Iterator[float]:\n    \"\"\"Yield the x and y coordinates of the point in order.\"\"\"\n    yield self.x\n    yield self.y\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.Point.__mul__","title":"__mul__","text":"<pre><code>__mul__(scalar: float) -&gt; Point\n</code></pre> <p>Return the point scaled by a scalar.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def __mul__(self, scalar: float) -&gt; \"Point\":\n    \"\"\"Return the point scaled by a scalar.\"\"\"\n    return Point(self.x * scalar, self.y * scalar)\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.Point.__post_init__","title":"__post_init__","text":"<pre><code>__post_init__() -&gt; None\n</code></pre> <p>Round the coordinates to the rounding precision after initialization.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def __post_init__(self) -&gt; None:\n    \"\"\"Round the coordinates to the rounding precision after initialization.\"\"\"\n    object.__setattr__(self, \"x\", round(self.x, ROUNDING))\n    object.__setattr__(self, \"y\", round(self.y, ROUNDING))\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.Point.__sub__","title":"__sub__","text":"<pre><code>__sub__(xy: Point | tuple[float, float]) -&gt; Point\n</code></pre> <p>Return the difference of the point and another point or <code>(x, y)</code> pair.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def __sub__(self, xy: \"Point | tuple[float, float]\") -&gt; \"Point\":\n    \"\"\"Return the difference of the point and another point or `(x, y)` pair.\"\"\"\n    if isinstance_or_builder(xy, Point):\n        xy = (xy.x, xy.y)\n    return Point(self.x - xy[0], self.y - xy[1])\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.Point.__truediv__","title":"__truediv__","text":"<pre><code>__truediv__(scalar: float) -&gt; Point\n</code></pre> <p>Return the point divided by a scalar.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def __truediv__(self, scalar: float) -&gt; \"Point\":\n    \"\"\"Return the point divided by a scalar.\"\"\"\n    return Point(self.x / scalar, self.y / scalar)\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.Point.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Get the bounding box of this point.</p> <p>Returns:</p> Type Description <code>Bbox</code> <p>A Bbox with zero width and height.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def bbox(self) -&gt; \"Bbox\":\n    \"\"\"Get the bounding box of this point.\n\n    Returns:\n        A Bbox with zero width and height.\n    \"\"\"\n    return Bbox(copy.deepcopy(self), 0, 0)\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.Point.from_tuple","title":"from_tuple  <code>classmethod</code>","text":"<pre><code>from_tuple(t: tuple[float, float]) -&gt; Self\n</code></pre> <p>Create a Point from a tuple.</p> <p>Parameters:</p> Name Type Description Default <code>t</code> <code>tuple[float, float]</code> <p>Tuple (x, y).</p> required <p>Returns:</p> Type Description <code>Self</code> <p>A new Point.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>@classmethod\ndef from_tuple(cls, t: tuple[float, float]) -&gt; typing_extensions.Self:\n    \"\"\"Create a Point from a tuple.\n\n    Args:\n        t: Tuple (x, y).\n\n    Returns:\n        A new Point.\n    \"\"\"\n    return cls(t[0], t[1])\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.Point.get_angle_to_horizontal","title":"get_angle_to_horizontal","text":"<pre><code>get_angle_to_horizontal() -&gt; float\n</code></pre> <p>Get the angle from origin to this point relative to horizontal.</p> <p>Returns:</p> Type Description <code>float</code> <p>Angle in radians.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def get_angle_to_horizontal(self) -&gt; float:\n    \"\"\"Get the angle from origin to this point relative to horizontal.\n\n    Returns:\n        Angle in radians.\n    \"\"\"\n    angle = math.atan2(self.y, self.x)\n    return get_normalized_angle(angle)\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.Point.get_intersection_with_line","title":"get_intersection_with_line","text":"<pre><code>get_intersection_with_line(line: Line) -&gt; list[Point]\n</code></pre> <p>Get intersection points with a line.</p> <p>Parameters:</p> Name Type Description Default <code>line</code> <code>Line</code> <p>The line to intersect with.</p> required <p>Returns:</p> Type Description <code>list[Point]</code> <p>List of intersection points (empty if no intersection).</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def get_intersection_with_line(self, line: \"Line\") -&gt; list[\"Point\"]:\n    \"\"\"Get intersection points with a line.\n\n    Args:\n        line: The line to intersect with.\n\n    Returns:\n        List of intersection points (empty if no intersection).\n    \"\"\"\n    if line.has_point(self):\n        return [self]\n    return []\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.Point.isnan","title":"isnan","text":"<pre><code>isnan() -&gt; bool\n</code></pre> <p>Check if either coordinate is NaN.</p> <p>Returns:</p> Type Description <code>bool</code> <p>True if x or y is NaN.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def isnan(self) -&gt; bool:\n    \"\"\"Check if either coordinate is NaN.\n\n    Returns:\n        True if x or y is NaN.\n    \"\"\"\n    return math.isnan(self.x) or math.isnan(self.y)\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.Point.reversed","title":"reversed","text":"<pre><code>reversed() -&gt; Point\n</code></pre> <p>Return a reversed copy (identity for points).</p> <p>Returns:</p> Type Description <code>Point</code> <p>A copy of the point.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def reversed(self) -&gt; \"Point\":\n    \"\"\"Return a reversed copy (identity for points).\n\n    Returns:\n        A copy of the point.\n    \"\"\"\n    return Point(self.x, self.y)\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.Point.round","title":"round","text":"<pre><code>round(ndigits: int | None = ROUNDING) -&gt; Point\n</code></pre> <p>Round coordinates to specified digits.</p> <p>Parameters:</p> Name Type Description Default <code>ndigits</code> <code>int | None</code> <p>Number of decimal places. Defaults to the module <code>ROUNDING</code>. Passing <code>None</code> truncates the coordinates to <code>int</code> (the standard-library <code>round(x, None)</code> behavior).</p> <code>ROUNDING</code> <p>Returns:</p> Type Description <code>Point</code> <p>A new Point with rounded coordinates.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def round(self, ndigits: int | None = ROUNDING) -&gt; \"Point\":\n    \"\"\"Round coordinates to specified digits.\n\n    Args:\n        ndigits: Number of decimal places. Defaults to the module\n            `ROUNDING`. Passing `None` truncates the coordinates to `int`\n            (the standard-library `round(x, None)` behavior).\n\n    Returns:\n        A new Point with rounded coordinates.\n    \"\"\"\n    return Point(round(self.x, ndigits), round(self.y, ndigits))\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.Point.to_matrix","title":"to_matrix","text":"<pre><code>to_matrix() -&gt; NDArray\n</code></pre> <p>Convert to a 3x1 numpy matrix for transformation operations.</p> <p>Returns:</p> Type Description <code>NDArray</code> <p>A numpy array [[x], [y], [1]].</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def to_matrix(self) -&gt; numpy.typing.NDArray:\n    \"\"\"Convert to a 3x1 numpy matrix for transformation operations.\n\n    Returns:\n        A numpy array [[x], [y], [1]].\n    \"\"\"\n    m = numpy.array([[self.x], [self.y], [1]], dtype=float)\n    return m\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.Point.to_tuple","title":"to_tuple","text":"<pre><code>to_tuple() -&gt; tuple[float, float]\n</code></pre> <p>Convert to a tuple.</p> <p>Returns:</p> Type Description <code>tuple[float, float]</code> <p>Tuple (x, y).</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def to_tuple(self) -&gt; tuple[float, float]:\n    \"\"\"Convert to a tuple.\n\n    Returns:\n        Tuple (x, y).\n    \"\"\"\n    return (\n        self.x,\n        self.y,\n    )\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.Point.transformed","title":"transformed","text":"<pre><code>transformed(transformation: Transformation) -&gt; Point\n</code></pre> <p>Apply a transformation to this point.</p> <p>Parameters:</p> Name Type Description Default <code>transformation</code> <code>Transformation</code> <p>The transformation to apply.</p> required <p>Returns:</p> Type Description <code>Point</code> <p>A new transformed Point.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def transformed(self, transformation: \"Transformation\") -&gt; \"Point\":\n    \"\"\"Apply a transformation to this point.\n\n    Args:\n        transformation: The transformation to apply.\n\n    Returns:\n        A new transformed Point.\n    \"\"\"\n    m = numpy.matmul(transformation.to_matrix(), self.to_matrix())\n    return Point(m[0][0], m[1][0])\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.QuadraticBezierCurve","title":"QuadraticBezierCurve  <code>dataclass</code>","text":"<pre><code>QuadraticBezierCurve(p1: Point, p2: Point, control_point: Point)\n</code></pre> <p>               Bases: <code>GeometryObject</code></p> <p>Represents a quadratic Bezier curve.</p> <p>Examples:</p> <pre><code>curve = QuadraticBezierCurve(\n    Point(0.0, 0.0),\n    Point(10.0, 0.0),\n    Point(5.0, 5.0)\n)\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>p1</code> <code>Point</code> <p>Start point.</p> required <code>p2</code> <code>Point</code> <p>End point.</p> required <code>control_point</code> <code>Point</code> <p>The single control point.</p> required <p>Methods:</p> Name Description <code>bbox</code> <p>Get the bounding box of the curve.</p> <code>derivative</code> <p>Compute the derivative at parameter t.</p> <code>evaluate</code> <p>Evaluate the curve at parameter t.</p> <code>evaluate_multi</code> <p>Evaluate the curve at multiple parameters.</p> <code>get_angle_at_fraction</code> <p>Get angle at a fraction of the arc length.</p> <code>get_intersection_with_line</code> <p>Get intersection with a line.</p> <code>get_position_and_angle_at_fraction</code> <p>Get both position and angle at a fraction of the arc length.</p> <code>get_position_at_fraction</code> <p>Get point at a fraction of the arc length.</p> <code>length</code> <p>Calculate the arc length of the curve.</p> <code>reversed</code> <p>Return a reversed copy of the curve.</p> <code>shortened</code> <p>Return a shortened copy of the curve.</p> <code>split</code> <p>Split the curve at parameter t using De Casteljau subdivision.</p> <code>transformed</code> <p>Apply a transformation to this curve.</p>"},{"location":"api_reference/geometry/#momapy.geometry.QuadraticBezierCurve.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Get the bounding box of the curve.</p> <p>Returns:</p> Type Description <code>Bbox</code> <p>A Bbox enclosing the curve.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def bbox(self) -&gt; \"Bbox\":\n    \"\"\"Get the bounding box of the curve.\n\n    Returns:\n        A Bbox enclosing the curve.\n    \"\"\"\n    # Extrema candidates: endpoints + roots of derivative\n    xs = [self.p1.x, self.p2.x]\n    ys = [self.p1.y, self.p2.y]\n    # dx/dt = 2(1-t)(cp-p1) + 2t(p2-cp) = 0 =&gt; t = (p1-cp)/(p1-2cp+p2)\n    for values, lst in [\n        ((self.p1.x, self.control_point.x, self.p2.x), xs),\n        ((self.p1.y, self.control_point.y, self.p2.y), ys),\n    ]:\n        a, b, c = values\n        denominator = a - 2 * b + c\n        if abs(denominator) &gt; _ZERO_TOLERANCE:\n            t = (a - b) / denominator\n            if 0 &lt; t &lt; 1:\n                point = self.evaluate(t)\n                lst.append(point.x if lst is xs else point.y)\n    min_x, max_x = min(xs), max(xs)\n    min_y, max_y = min(ys), max(ys)\n    return Bbox(\n        Point(min_x, min_y),\n        max_x - min_x,\n        max_y - min_y,\n    )\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.QuadraticBezierCurve.derivative","title":"derivative","text":"<pre><code>derivative(t: float) -&gt; tuple[float, float]\n</code></pre> <p>Compute the derivative at parameter t.</p> <p>Parameters:</p> Name Type Description Default <code>t</code> <code>float</code> <p>Parameter value from 0 to 1.</p> required <p>Returns:</p> Type Description <code>tuple[float, float]</code> <p>Tuple of (dx/dt, dy/dt).</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def derivative(self, t: float) -&gt; tuple[float, float]:\n    \"\"\"Compute the derivative at parameter t.\n\n    Args:\n        t: Parameter value from 0 to 1.\n\n    Returns:\n        Tuple of (dx/dt, dy/dt).\n    \"\"\"\n    u = 1 - t\n    dx = 2 * u * (self.control_point.x - self.p1.x) + 2 * t * (\n        self.p2.x - self.control_point.x\n    )\n    dy = 2 * u * (self.control_point.y - self.p1.y) + 2 * t * (\n        self.p2.y - self.control_point.y\n    )\n    return (dx, dy)\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.QuadraticBezierCurve.evaluate","title":"evaluate","text":"<pre><code>evaluate(t: float) -&gt; Point\n</code></pre> <p>Evaluate the curve at parameter t.</p> <p>Parameters:</p> Name Type Description Default <code>t</code> <code>float</code> <p>Parameter value from 0 to 1.</p> required <p>Returns:</p> Type Description <code>Point</code> <p>The point at parameter t.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def evaluate(self, t: float) -&gt; Point:\n    \"\"\"Evaluate the curve at parameter t.\n\n    Args:\n        t: Parameter value from 0 to 1.\n\n    Returns:\n        The point at parameter t.\n    \"\"\"\n    return self.evaluate_multi([t])[0]\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.QuadraticBezierCurve.evaluate_multi","title":"evaluate_multi","text":"<pre><code>evaluate_multi(t_sequence: Sequence[float]) -&gt; list[Point]\n</code></pre> <p>Evaluate the curve at multiple parameters.</p> <p>Parameters:</p> Name Type Description Default <code>t_sequence</code> <code>Sequence[float]</code> <p>Sequence of parameter values.</p> required <p>Returns:</p> Type Description <code>list[Point]</code> <p>List of points.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def evaluate_multi(\n    self, t_sequence: collections.abc.Sequence[float]\n) -&gt; list[Point]:\n    \"\"\"Evaluate the curve at multiple parameters.\n\n    Args:\n        t_sequence: Sequence of parameter values.\n\n    Returns:\n        List of points.\n    \"\"\"\n    t = numpy.asarray(t_sequence, dtype=\"double\")\n    u = 1 - t\n    x = u * u * self.p1.x + 2 * u * t * self.control_point.x + t * t * self.p2.x\n    y = u * u * self.p1.y + 2 * u * t * self.control_point.y + t * t * self.p2.y\n    return [Point(float(xi), float(yi)) for xi, yi in zip(x, y)]\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.QuadraticBezierCurve.get_angle_at_fraction","title":"get_angle_at_fraction","text":"<pre><code>get_angle_at_fraction(fraction: float) -&gt; float\n</code></pre> <p>Get angle at a fraction of the arc length.</p> <p>Parameters:</p> Name Type Description Default <code>fraction</code> <code>float</code> <p>Fraction from 0 to 1.</p> required <p>Returns:</p> Type Description <code>float</code> <p>Angle in radians.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def get_angle_at_fraction(self, fraction: float) -&gt; float:\n    \"\"\"Get angle at a fraction of the arc length.\n\n    Args:\n        fraction: Fraction from 0 to 1.\n\n    Returns:\n        Angle in radians.\n    \"\"\"\n    total = self.length()\n    if fraction &lt;= 0:\n        t = 0.0\n    elif fraction &gt;= 1:\n        t = 1.0\n    else:\n        t = _find_t_at_arc_length_fraction(self.derivative, total, fraction)\n    dx, dy = self.derivative(t)\n    return math.atan2(dy, dx)\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.QuadraticBezierCurve.get_intersection_with_line","title":"get_intersection_with_line","text":"<pre><code>get_intersection_with_line(line: Line) -&gt; list[Point]\n</code></pre> <p>Get intersection with a line.</p> <p>Parameters:</p> Name Type Description Default <code>line</code> <code>Line</code> <p>The line to intersect with.</p> required <p>Returns:</p> Type Description <code>list[Point]</code> <p>List of intersection points.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def get_intersection_with_line(self, line: Line) -&gt; list[Point]:\n    \"\"\"Get intersection with a line.\n\n    Args:\n        line: The line to intersect with.\n\n    Returns:\n        List of intersection points.\n    \"\"\"\n    # Line equation: a*x + b*y + c = 0\n    a = line.p2.y - line.p1.y\n    b = line.p1.x - line.p2.x\n    c = line.p2.x * line.p1.y - line.p1.x * line.p2.y\n    # Bezier: B(t) = (1-t)^2 * p0 + 2(1-t)t * p1 + t^2 * p2\n    p0x, p0y = self.p1.x, self.p1.y\n    p1x, p1y = self.control_point.x, self.control_point.y\n    p2x, p2y = self.p2.x, self.p2.y\n    # Substitute into line equation: A*t^2 + B*t + C = 0\n    ax_coeff = a * (p0x - 2 * p1x + p2x) + b * (p0y - 2 * p1y + p2y)\n    bx_coeff = a * (-2 * p0x + 2 * p1x) + b * (-2 * p0y + 2 * p1y)\n    cx_coeff = a * p0x + b * p0y + c\n    roots = []\n    if abs(ax_coeff) &lt; _ZERO_TOLERANCE:\n        if abs(bx_coeff) &gt; _ZERO_TOLERANCE:\n            roots.append(-cx_coeff / bx_coeff)\n    else:\n        disc = bx_coeff * bx_coeff - 4 * ax_coeff * cx_coeff\n        if disc &gt;= 0:\n            sqrt_disc = math.sqrt(disc)\n            roots.append((-bx_coeff + sqrt_disc) / (2 * ax_coeff))\n            roots.append((-bx_coeff - sqrt_disc) / (2 * ax_coeff))\n    result = []\n    for t in roots:\n        if -_PARAMETER_TOLERANCE &lt;= t &lt;= 1 + _PARAMETER_TOLERANCE:\n            t = max(0.0, min(1.0, t))\n            result.append(self.evaluate(t))\n    return result\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.QuadraticBezierCurve.get_position_and_angle_at_fraction","title":"get_position_and_angle_at_fraction","text":"<pre><code>get_position_and_angle_at_fraction(fraction: float) -&gt; tuple[Point, float]\n</code></pre> <p>Get both position and angle at a fraction of the arc length.</p> <p>Parameters:</p> Name Type Description Default <code>fraction</code> <code>float</code> <p>Fraction from 0 to 1.</p> required <p>Returns:</p> Type Description <code>tuple[Point, float]</code> <p>Tuple of (point, angle_in_radians).</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def get_position_and_angle_at_fraction(\n    self, fraction: float\n) -&gt; tuple[Point, float]:\n    \"\"\"Get both position and angle at a fraction of the arc length.\n\n    Args:\n        fraction: Fraction from 0 to 1.\n\n    Returns:\n        Tuple of (point, angle_in_radians).\n    \"\"\"\n    total = self.length()\n    if fraction &lt;= 0:\n        t = 0.0\n    elif fraction &gt;= 1:\n        t = 1.0\n    else:\n        t = _find_t_at_arc_length_fraction(self.derivative, total, fraction)\n    pos = self.evaluate(t)\n    dx, dy = self.derivative(t)\n    angle = math.atan2(dy, dx)\n    return (pos, angle)\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.QuadraticBezierCurve.get_position_at_fraction","title":"get_position_at_fraction","text":"<pre><code>get_position_at_fraction(fraction: float) -&gt; Point\n</code></pre> <p>Get point at a fraction of the arc length.</p> <p>Parameters:</p> Name Type Description Default <code>fraction</code> <code>float</code> <p>Fraction from 0 to 1.</p> required <p>Returns:</p> Type Description <code>Point</code> <p>The point at that fraction.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def get_position_at_fraction(self, fraction: float) -&gt; Point:\n    \"\"\"Get point at a fraction of the arc length.\n\n    Args:\n        fraction: Fraction from 0 to 1.\n\n    Returns:\n        The point at that fraction.\n    \"\"\"\n    if fraction &lt;= 0:\n        return Point(self.p1.x, self.p1.y)\n    if fraction &gt;= 1:\n        return Point(self.p2.x, self.p2.y)\n    total = self.length()\n    t = _find_t_at_arc_length_fraction(self.derivative, total, fraction)\n    return self.evaluate(t)\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.QuadraticBezierCurve.length","title":"length","text":"<pre><code>length() -&gt; float\n</code></pre> <p>Calculate the arc length of the curve.</p> <p>Returns:</p> Type Description <code>float</code> <p>The arc length.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def length(self) -&gt; float:\n    \"\"\"Calculate the arc length of the curve.\n\n    Returns:\n        The arc length.\n    \"\"\"\n    return _arc_length(self.derivative, 0.0, 1.0)\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.QuadraticBezierCurve.reversed","title":"reversed","text":"<pre><code>reversed() -&gt; QuadraticBezierCurve\n</code></pre> <p>Return a reversed copy of the curve.</p> <p>Returns:</p> Type Description <code>QuadraticBezierCurve</code> <p>A new QuadraticBezierCurve going in reverse direction.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def reversed(self) -&gt; \"QuadraticBezierCurve\":\n    \"\"\"Return a reversed copy of the curve.\n\n    Returns:\n        A new QuadraticBezierCurve going in reverse direction.\n    \"\"\"\n    return QuadraticBezierCurve(self.p2, self.p1, self.control_point)\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.QuadraticBezierCurve.shortened","title":"shortened","text":"<pre><code>shortened(length: float, start_or_end: Literal['start', 'end'] = 'end') -&gt; QuadraticBezierCurve\n</code></pre> <p>Return a shortened copy of the curve.</p> <p>Parameters:</p> Name Type Description Default <code>length</code> <code>float</code> <p>Amount to shorten by.</p> required <code>start_or_end</code> <code>Literal['start', 'end']</code> <p>Which end to shorten from.</p> <code>'end'</code> <p>Returns:</p> Type Description <code>QuadraticBezierCurve</code> <p>A new shortened QuadraticBezierCurve.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def shortened(\n    self, length: float, start_or_end: typing.Literal[\"start\", \"end\"] = \"end\"\n) -&gt; \"QuadraticBezierCurve\":\n    \"\"\"Return a shortened copy of the curve.\n\n    Args:\n        length: Amount to shorten by.\n        start_or_end: Which end to shorten from.\n\n    Returns:\n        A new shortened QuadraticBezierCurve.\n    \"\"\"\n    if length == 0 or self.length() == 0:\n        return copy.deepcopy(self)\n    if start_or_end == \"start\":\n        return self.reversed().shortened(length).reversed()\n    total_length = self.length()\n    if length &gt; total_length:\n        length = total_length\n    fraction = 1 - length / total_length\n    t = _find_t_at_arc_length_fraction(self.derivative, total_length, fraction)\n    left, _ = self.split(t)\n    return left\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.QuadraticBezierCurve.split","title":"split","text":"<pre><code>split(t: float) -&gt; tuple[QuadraticBezierCurve, QuadraticBezierCurve]\n</code></pre> <p>Split the curve at parameter t using De Casteljau subdivision.</p> <p>Parameters:</p> Name Type Description Default <code>t</code> <code>float</code> <p>Parameter value from 0 to 1.</p> required <p>Returns:</p> Type Description <code>tuple[QuadraticBezierCurve, QuadraticBezierCurve]</code> <p>Tuple of two QuadraticBezierCurves.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def split(self, t: float) -&gt; tuple[\"QuadraticBezierCurve\", \"QuadraticBezierCurve\"]:\n    \"\"\"Split the curve at parameter t using De Casteljau subdivision.\n\n    Args:\n        t: Parameter value from 0 to 1.\n\n    Returns:\n        Tuple of two QuadraticBezierCurves.\n    \"\"\"\n    u = 1 - t\n    m0x = u * self.p1.x + t * self.control_point.x\n    m0y = u * self.p1.y + t * self.control_point.y\n    m1x = u * self.control_point.x + t * self.p2.x\n    m1y = u * self.control_point.y + t * self.p2.y\n    mx = u * m0x + t * m1x\n    my = u * m0y + t * m1y\n    mid = Point(mx, my)\n    left = QuadraticBezierCurve(self.p1, mid, Point(m0x, m0y))\n    right = QuadraticBezierCurve(mid, self.p2, Point(m1x, m1y))\n    return (left, right)\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.QuadraticBezierCurve.transformed","title":"transformed","text":"<pre><code>transformed(transformation: Transformation) -&gt; QuadraticBezierCurve\n</code></pre> <p>Apply a transformation to this curve.</p> <p>Parameters:</p> Name Type Description Default <code>transformation</code> <code>Transformation</code> <p>The transformation to apply.</p> required <p>Returns:</p> Type Description <code>QuadraticBezierCurve</code> <p>A new transformed QuadraticBezierCurve.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def transformed(self, transformation: \"Transformation\") -&gt; \"QuadraticBezierCurve\":\n    \"\"\"Apply a transformation to this curve.\n\n    Args:\n        transformation: The transformation to apply.\n\n    Returns:\n        A new transformed QuadraticBezierCurve.\n    \"\"\"\n    return QuadraticBezierCurve(\n        self.p1.transformed(transformation),\n        self.p2.transformed(transformation),\n        self.control_point.transformed(transformation),\n    )\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.Rotation","title":"Rotation  <code>dataclass</code>","text":"<pre><code>Rotation(angle: float, point: Point | None = None)\n</code></pre> <p>               Bases: <code>Transformation</code></p> <p>Represents a rotation transformation.</p> <p>Examples:</p> <pre><code>rot = Rotation(math.pi / 2, Point(5.0, 5.0))\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>angle</code> <code>float</code> <p>Rotation angle in radians.</p> required <code>point</code> <code>Point | None</code> <p>Optional center of rotation (defaults to origin).</p> <code>None</code> <p>Methods:</p> Name Description <code>__mul__</code> <p>Return the composition of the transformation with another transformation.</p> <code>inverted</code> <p>Get the inverse rotation.</p> <code>to_matrix</code> <p>Convert to a rotation matrix.</p>"},{"location":"api_reference/geometry/#momapy.geometry.Rotation.__mul__","title":"__mul__","text":"<pre><code>__mul__(other: Transformation) -&gt; MatrixTransformation\n</code></pre> <p>Return the composition of the transformation with another transformation.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def __mul__(self, other: \"Transformation\") -&gt; \"MatrixTransformation\":\n    \"\"\"Return the composition of the transformation with another transformation.\"\"\"\n    return MatrixTransformation(numpy.matmul(self.to_matrix(), other.to_matrix()))\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.Rotation.inverted","title":"inverted","text":"<pre><code>inverted() -&gt; Transformation\n</code></pre> <p>Get the inverse rotation.</p> <p>Returns:</p> Type Description <code>Transformation</code> <p>A rotation by the negative angle.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def inverted(self) -&gt; Transformation:\n    \"\"\"Get the inverse rotation.\n\n    Returns:\n        A rotation by the negative angle.\n    \"\"\"\n    return Rotation(-self.angle, self.point)\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.Rotation.to_matrix","title":"to_matrix","text":"<pre><code>to_matrix() -&gt; NDArray\n</code></pre> <p>Convert to a rotation matrix.</p> <p>Returns:</p> Type Description <code>NDArray</code> <p>A 3x3 rotation matrix.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def to_matrix(self) -&gt; numpy.typing.NDArray:\n    \"\"\"Convert to a rotation matrix.\n\n    Returns:\n        A 3x3 rotation matrix.\n    \"\"\"\n    m = numpy.array(\n        [\n            [math.cos(self.angle), -math.sin(self.angle), 0],\n            [math.sin(self.angle), math.cos(self.angle), 0],\n            [0, 0, 1],\n        ],\n        dtype=float,\n    )\n    if self.point is not None:\n        translation = Translation(self.point.x, self.point.y)\n        m = numpy.matmul(\n            numpy.matmul(translation.to_matrix(), m),\n            translation.inverted().to_matrix(),\n        )\n    return m\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.Scaling","title":"Scaling  <code>dataclass</code>","text":"<pre><code>Scaling(sx: float, sy: float)\n</code></pre> <p>               Bases: <code>Transformation</code></p> <p>Represents a scaling transformation.</p> <p>Examples:</p> <pre><code>scale = Scaling(2, 2)  # Double size\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>sx</code> <code>float</code> <p>Scale factor in x direction.</p> required <code>sy</code> <code>float</code> <p>Scale factor in y direction.</p> required <p>Methods:</p> Name Description <code>__mul__</code> <p>Return the composition of the transformation with another transformation.</p> <code>inverted</code> <p>Get the inverse scaling.</p> <code>to_matrix</code> <p>Convert to a scaling matrix.</p>"},{"location":"api_reference/geometry/#momapy.geometry.Scaling.__mul__","title":"__mul__","text":"<pre><code>__mul__(other: Transformation) -&gt; MatrixTransformation\n</code></pre> <p>Return the composition of the transformation with another transformation.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def __mul__(self, other: \"Transformation\") -&gt; \"MatrixTransformation\":\n    \"\"\"Return the composition of the transformation with another transformation.\"\"\"\n    return MatrixTransformation(numpy.matmul(self.to_matrix(), other.to_matrix()))\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.Scaling.inverted","title":"inverted","text":"<pre><code>inverted() -&gt; Transformation\n</code></pre> <p>Get the inverse scaling.</p> <p>Returns:</p> Type Description <code>Transformation</code> <p>Scaling by (1/sx, 1/sy).</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def inverted(self) -&gt; Transformation:\n    \"\"\"Get the inverse scaling.\n\n    Returns:\n        Scaling by (1/sx, 1/sy).\n    \"\"\"\n    return Scaling(1 / self.sx, 1 / self.sy)\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.Scaling.to_matrix","title":"to_matrix","text":"<pre><code>to_matrix() -&gt; NDArray\n</code></pre> <p>Convert to a scaling matrix.</p> <p>Returns:</p> Type Description <code>NDArray</code> <p>A 3x3 scaling matrix.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def to_matrix(self) -&gt; numpy.typing.NDArray:\n    \"\"\"Convert to a scaling matrix.\n\n    Returns:\n        A 3x3 scaling matrix.\n    \"\"\"\n    m = numpy.array(\n        [\n            [self.sx, 0, 0],\n            [0, self.sy, 0],\n            [0, 0, 1],\n        ],\n        dtype=float,\n    )\n    return m\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.Segment","title":"Segment  <code>dataclass</code>","text":"<pre><code>Segment(p1: Point, p2: Point)\n</code></pre> <p>               Bases: <code>GeometryObject</code></p> <p>Represents a line segment between two points.</p> <p>Examples:</p> <pre><code>seg = Segment(Point(0.0, 0.0), Point(10.0, 10.0))\nseg.length()\nseg.get_position_at_fraction(0.5)\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>p1</code> <code>Point</code> <p>Start point.</p> required <code>p2</code> <code>Point</code> <p>End point.</p> required <p>Methods:</p> Name Description <code>bbox</code> <p>Get the bounding box of the segment.</p> <code>get_angle_at_fraction</code> <p>Get angle at a fraction along the segment.</p> <code>get_angle_to_horizontal</code> <p>Get the angle of the segment relative to horizontal.</p> <code>get_distance_to_point</code> <p>Get shortest distance from a point to this segment.</p> <code>get_intersection_with_line</code> <p>Get intersection with a line.</p> <code>get_position_and_angle_at_fraction</code> <p>Get both position and angle at a fraction.</p> <code>get_position_at_fraction</code> <p>Get point at a fraction along the segment.</p> <code>has_point</code> <p>Check if a point lies on this segment.</p> <code>length</code> <p>Calculate the length of the segment.</p> <code>reversed</code> <p>Return a reversed copy of the segment.</p> <code>shortened</code> <p>Return a shortened copy of the segment.</p> <code>transformed</code> <p>Apply a transformation to this segment.</p>"},{"location":"api_reference/geometry/#momapy.geometry.Segment.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Get the bounding box of the segment.</p> <p>Returns:</p> Type Description <code>Bbox</code> <p>A Bbox enclosing the segment.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def bbox(self) -&gt; \"Bbox\":\n    \"\"\"Get the bounding box of the segment.\n\n    Returns:\n        A Bbox enclosing the segment.\n    \"\"\"\n    return Bbox.around_points([self.p1, self.p2])\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.Segment.get_angle_at_fraction","title":"get_angle_at_fraction","text":"<pre><code>get_angle_at_fraction(fraction: float) -&gt; float\n</code></pre> <p>Get angle at a fraction along the segment.</p> <p>Parameters:</p> Name Type Description Default <code>fraction</code> <code>float</code> <p>Fraction from 0 to 1.</p> required <p>Returns:</p> Type Description <code>float</code> <p>Angle in radians.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def get_angle_at_fraction(self, fraction: float) -&gt; float:\n    \"\"\"Get angle at a fraction along the segment.\n\n    Args:\n        fraction: Fraction from 0 to 1.\n\n    Returns:\n        Angle in radians.\n    \"\"\"\n    return self.get_angle_to_horizontal()\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.Segment.get_angle_to_horizontal","title":"get_angle_to_horizontal","text":"<pre><code>get_angle_to_horizontal() -&gt; float\n</code></pre> <p>Get the angle of the segment relative to horizontal.</p> <p>Returns:</p> Type Description <code>float</code> <p>Angle in radians.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def get_angle_to_horizontal(self) -&gt; float:\n    \"\"\"Get the angle of the segment relative to horizontal.\n\n    Returns:\n        Angle in radians.\n    \"\"\"\n    angle = math.atan2(self.p2.y - self.p1.y, self.p2.x - self.p1.x)\n    return get_normalized_angle(angle)\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.Segment.get_distance_to_point","title":"get_distance_to_point","text":"<pre><code>get_distance_to_point(point: Point) -&gt; float\n</code></pre> <p>Get shortest distance from a point to this segment.</p> <p>Parameters:</p> Name Type Description Default <code>point</code> <code>Point</code> <p>The point to measure from.</p> required <p>Returns:</p> Type Description <code>float</code> <p>The shortest distance.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def get_distance_to_point(self, point: Point) -&gt; float:\n    \"\"\"Get shortest distance from a point to this segment.\n\n    Args:\n        point: The point to measure from.\n\n    Returns:\n        The shortest distance.\n    \"\"\"\n    point_offset_x = point.x - self.p1.x\n    point_offset_y = point.y - self.p1.y\n    segment_direction_x = self.p2.x - self.p1.x\n    segment_direction_y = self.p2.y - self.p1.y\n    dot_product = (\n        point_offset_x * segment_direction_x + point_offset_y * segment_direction_y\n    )\n    length_squared = segment_direction_x**2 + segment_direction_y**2\n    if length_squared != 0:\n        projection = dot_product / length_squared\n    else:\n        projection = -1\n    if projection &lt; 0:\n        closest_x = self.p1.x\n        closest_y = self.p1.y\n    elif projection &gt; 1:\n        closest_x = self.p2.x\n        closest_y = self.p2.y\n    else:\n        closest_x = self.p1.x + projection * segment_direction_x\n        closest_y = self.p1.y + projection * segment_direction_y\n    delta_x = point.x - closest_x\n    delta_y = point.y - closest_y\n    return math.sqrt(delta_x**2 + delta_y**2)\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.Segment.get_intersection_with_line","title":"get_intersection_with_line","text":"<pre><code>get_intersection_with_line(line: Line) -&gt; list[Point] | list[Segment]\n</code></pre> <p>Get intersection with a line.</p> <p>Computes the parametric t of the intersection along the segment directly from the line equations, avoiding intermediate coordinate products for numerical stability.</p> <p>Parameters:</p> Name Type Description Default <code>line</code> <code>Line</code> <p>The line to intersect with.</p> required <p>Returns:</p> Type Description <code>list[Point] | list[Segment]</code> <p>List of intersection points or segment if coincident.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def get_intersection_with_line(self, line: Line) -&gt; list[Point] | list[\"Segment\"]:\n    \"\"\"Get intersection with a line.\n\n    Computes the parametric t of the intersection along the segment\n    directly from the line equations, avoiding intermediate coordinate\n    products for numerical stability.\n\n    Args:\n        line: The line to intersect with.\n\n    Returns:\n        List of intersection points or segment if coincident.\n    \"\"\"\n    segment_direction_x = self.p2.x - self.p1.x\n    segment_direction_y = self.p2.y - self.p1.y\n    line_direction_x = line.p2.x - line.p1.x\n    line_direction_y = line.p2.y - line.p1.y\n    denominator = (\n        segment_direction_y * line_direction_x\n        - segment_direction_x * line_direction_y\n    )\n    if abs(denominator) &lt; _ZERO_TOLERANCE:\n        # Parallel or coincident\n        line2 = Line(self.p1, self.p2)\n        if line.is_coincident_to_line(line2):\n            return [self]\n        return []\n    origin_offset_x = line.p1.x - self.p1.x\n    origin_offset_y = line.p1.y - self.p1.y\n    t = (\n        origin_offset_y * line_direction_x - origin_offset_x * line_direction_y\n    ) / denominator\n    if -_PARAMETER_TOLERANCE &lt;= t &lt;= 1 + _PARAMETER_TOLERANCE:\n        t = max(0.0, min(1.0, t))\n        point = Point(\n            self.p1.x + t * segment_direction_x,\n            self.p1.y + t * segment_direction_y,\n        )\n        return [point]\n    return []\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.Segment.get_position_and_angle_at_fraction","title":"get_position_and_angle_at_fraction","text":"<pre><code>get_position_and_angle_at_fraction(fraction: float) -&gt; tuple[Point, float]\n</code></pre> <p>Get both position and angle at a fraction.</p> <p>Parameters:</p> Name Type Description Default <code>fraction</code> <code>float</code> <p>Fraction from 0 to 1.</p> required <p>Returns:</p> Type Description <code>tuple[Point, float]</code> <p>Tuple of (point, angle in radians).</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def get_position_and_angle_at_fraction(\n    self, fraction: float\n) -&gt; tuple[Point, float]:\n    \"\"\"Get both position and angle at a fraction.\n\n    Args:\n        fraction: Fraction from 0 to 1.\n\n    Returns:\n        Tuple of (point, angle in radians).\n    \"\"\"\n    return (\n        self.get_position_at_fraction(fraction),\n        self.get_angle_at_fraction(fraction),\n    )\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.Segment.get_position_at_fraction","title":"get_position_at_fraction","text":"<pre><code>get_position_at_fraction(fraction: float) -&gt; Point\n</code></pre> <p>Get point at a fraction along the segment.</p> <p>Parameters:</p> Name Type Description Default <code>fraction</code> <code>float</code> <p>Fraction from 0 to 1 (0 = start, 1 = end).</p> required <p>Returns:</p> Type Description <code>Point</code> <p>The point at that fraction.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def get_position_at_fraction(self, fraction: float) -&gt; Point:\n    \"\"\"Get point at a fraction along the segment.\n\n    Args:\n        fraction: Fraction from 0 to 1 (0 = start, 1 = end).\n\n    Returns:\n        The point at that fraction.\n    \"\"\"\n    x = self.p1.x + fraction * (self.p2.x - self.p1.x)\n    y = self.p1.y + fraction * (self.p2.y - self.p1.y)\n    return Point(x, y)\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.Segment.has_point","title":"has_point","text":"<pre><code>has_point(point: Point, max_distance: float = _ROUNDING_TOLERANCE) -&gt; bool\n</code></pre> <p>Check if a point lies on this segment.</p> <p>Parameters:</p> Name Type Description Default <code>point</code> <code>Point</code> <p>The point to check.</p> required <code>max_distance</code> <code>float</code> <p>Maximum allowed distance.</p> <code>_ROUNDING_TOLERANCE</code> <p>Returns:</p> Type Description <code>bool</code> <p>True if point is on the segment within tolerance.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def has_point(\n    self, point: Point, max_distance: float = _ROUNDING_TOLERANCE\n) -&gt; bool:\n    \"\"\"Check if a point lies on this segment.\n\n    Args:\n        point: The point to check.\n        max_distance: Maximum allowed distance.\n\n    Returns:\n        True if point is on the segment within tolerance.\n    \"\"\"\n    return self.get_distance_to_point(point) &lt;= max_distance\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.Segment.length","title":"length","text":"<pre><code>length() -&gt; float\n</code></pre> <p>Calculate the length of the segment.</p> <p>Returns:</p> Type Description <code>float</code> <p>The Euclidean length.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def length(self) -&gt; float:\n    \"\"\"Calculate the length of the segment.\n\n    Returns:\n        The Euclidean length.\n    \"\"\"\n    return math.sqrt((self.p2.x - self.p1.x) ** 2 + (self.p2.y - self.p1.y) ** 2)\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.Segment.reversed","title":"reversed","text":"<pre><code>reversed() -&gt; Segment\n</code></pre> <p>Return a reversed copy of the segment.</p> <p>Returns:</p> Type Description <code>Segment</code> <p>A new Segment with p1 and p2 swapped.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def reversed(self) -&gt; \"Segment\":\n    \"\"\"Return a reversed copy of the segment.\n\n    Returns:\n        A new Segment with p1 and p2 swapped.\n    \"\"\"\n    return Segment(self.p2, self.p1)\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.Segment.shortened","title":"shortened","text":"<pre><code>shortened(length: float, start_or_end: Literal['start', 'end'] = 'end') -&gt; Segment\n</code></pre> <p>Return a shortened copy of the segment.</p> <p>Parameters:</p> Name Type Description Default <code>length</code> <code>float</code> <p>Amount to shorten by.</p> required <code>start_or_end</code> <code>Literal['start', 'end']</code> <p>Which end to shorten from.</p> <code>'end'</code> <p>Returns:</p> Type Description <code>Segment</code> <p>A new shortened Segment.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def shortened(\n    self,\n    length: float,\n    start_or_end: typing.Literal[\"start\", \"end\"] = \"end\",\n) -&gt; \"Segment\":\n    \"\"\"Return a shortened copy of the segment.\n\n    Args:\n        length: Amount to shorten by.\n        start_or_end: Which end to shorten from.\n\n    Returns:\n        A new shortened Segment.\n    \"\"\"\n    total_length = self.length()\n    if length == 0 or total_length == 0:\n        return copy.deepcopy(self)\n    if start_or_end == \"start\":\n        return self.reversed().shortened(length).reversed()\n    if length &gt; total_length:\n        length = total_length\n    fraction = 1 - length / total_length\n    point = self.get_position_at_fraction(fraction)\n    return Segment(self.p1, point)\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.Segment.transformed","title":"transformed","text":"<pre><code>transformed(transformation: Transformation) -&gt; Segment\n</code></pre> <p>Apply a transformation to this segment.</p> <p>Parameters:</p> Name Type Description Default <code>transformation</code> <code>Transformation</code> <p>The transformation to apply.</p> required <p>Returns:</p> Type Description <code>Segment</code> <p>A new transformed Segment.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def transformed(self, transformation: \"Transformation\") -&gt; \"Segment\":\n    \"\"\"Apply a transformation to this segment.\n\n    Args:\n        transformation: The transformation to apply.\n\n    Returns:\n        A new transformed Segment.\n    \"\"\"\n    return Segment(\n        self.p1.transformed(transformation),\n        self.p2.transformed(transformation),\n    )\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.Transformation","title":"Transformation  <code>dataclass</code>","text":"<pre><code>Transformation()\n</code></pre> <p>               Bases: <code>ABC</code></p> <p>Abstract base class for geometric transformations.</p> <p>Methods:</p> Name Description <code>__mul__</code> <p>Return the composition of the transformation with another transformation.</p> <code>inverted</code> <p>Get the inverse transformation.</p> <code>to_matrix</code> <p>Convert to a 3x3 transformation matrix.</p>"},{"location":"api_reference/geometry/#momapy.geometry.Transformation.__mul__","title":"__mul__","text":"<pre><code>__mul__(other: Transformation) -&gt; MatrixTransformation\n</code></pre> <p>Return the composition of the transformation with another transformation.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def __mul__(self, other: \"Transformation\") -&gt; \"MatrixTransformation\":\n    \"\"\"Return the composition of the transformation with another transformation.\"\"\"\n    return MatrixTransformation(numpy.matmul(self.to_matrix(), other.to_matrix()))\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.Transformation.inverted","title":"inverted  <code>abstractmethod</code>","text":"<pre><code>inverted() -&gt; Transformation\n</code></pre> <p>Get the inverse transformation.</p> <p>Returns:</p> Type Description <code>Transformation</code> <p>The inverse transformation.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>@abc.abstractmethod\ndef inverted(self) -&gt; \"Transformation\":\n    \"\"\"Get the inverse transformation.\n\n    Returns:\n        The inverse transformation.\n    \"\"\"\n    pass\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.Transformation.to_matrix","title":"to_matrix  <code>abstractmethod</code>","text":"<pre><code>to_matrix() -&gt; NDArray\n</code></pre> <p>Convert to a 3x3 transformation matrix.</p> <p>Returns:</p> Type Description <code>NDArray</code> <p>A 3x3 numpy array.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>@abc.abstractmethod\ndef to_matrix(self) -&gt; numpy.typing.NDArray:\n    \"\"\"Convert to a 3x3 transformation matrix.\n\n    Returns:\n        A 3x3 numpy array.\n    \"\"\"\n    pass\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.Translation","title":"Translation  <code>dataclass</code>","text":"<pre><code>Translation(tx: float, ty: float)\n</code></pre> <p>               Bases: <code>Transformation</code></p> <p>Represents a translation transformation.</p> <p>Examples:</p> <pre><code>trans = Translation(10, 20)\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>tx</code> <code>float</code> <p>Translation in x direction.</p> required <code>ty</code> <code>float</code> <p>Translation in y direction.</p> required <p>Methods:</p> Name Description <code>__mul__</code> <p>Return the composition of the transformation with another transformation.</p> <code>inverted</code> <p>Get the inverse translation.</p> <code>to_matrix</code> <p>Convert to a translation matrix.</p>"},{"location":"api_reference/geometry/#momapy.geometry.Translation.__mul__","title":"__mul__","text":"<pre><code>__mul__(other: Transformation) -&gt; MatrixTransformation\n</code></pre> <p>Return the composition of the transformation with another transformation.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def __mul__(self, other: \"Transformation\") -&gt; \"MatrixTransformation\":\n    \"\"\"Return the composition of the transformation with another transformation.\"\"\"\n    return MatrixTransformation(numpy.matmul(self.to_matrix(), other.to_matrix()))\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.Translation.inverted","title":"inverted","text":"<pre><code>inverted() -&gt; Transformation\n</code></pre> <p>Get the inverse translation.</p> <p>Returns:</p> Type Description <code>Transformation</code> <p>A translation by (-tx, -ty).</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def inverted(self) -&gt; Transformation:\n    \"\"\"Get the inverse translation.\n\n    Returns:\n        A translation by (-tx, -ty).\n    \"\"\"\n    return Translation(-self.tx, -self.ty)\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.Translation.to_matrix","title":"to_matrix","text":"<pre><code>to_matrix() -&gt; NDArray\n</code></pre> <p>Convert to a translation matrix.</p> <p>Returns:</p> Type Description <code>NDArray</code> <p>A 3x3 translation matrix.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def to_matrix(self) -&gt; numpy.typing.NDArray:\n    \"\"\"Convert to a translation matrix.\n\n    Returns:\n        A 3x3 translation matrix.\n    \"\"\"\n    m = numpy.array([[1, 0, self.tx], [0, 1, self.ty], [0, 0, 1]], dtype=float)\n    return m\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.get_normalized_angle","title":"get_normalized_angle","text":"<pre><code>get_normalized_angle(angle: float) -&gt; float\n</code></pre> <p>Normalize an angle to [0, 2*pi).</p> <p>Parameters:</p> Name Type Description Default <code>angle</code> <code>float</code> <p>Angle in radians.</p> required <p>Returns:</p> Type Description <code>float</code> <p>Normalized angle.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def get_normalized_angle(angle: float) -&gt; float:\n    \"\"\"Normalize an angle to [0, 2*pi).\n\n    Args:\n        angle: Angle in radians.\n\n    Returns:\n        Normalized angle.\n    \"\"\"\n    return angle - (angle // (2 * math.pi) * (2 * math.pi))\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.get_primitives_anchor_point","title":"get_primitives_anchor_point","text":"<pre><code>get_primitives_anchor_point(primitives: list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc], anchor_point: str, center: Point | None = None) -&gt; Point | None\n</code></pre> <p>Get an anchor point of geometry primitives.</p> <p>Parameters:</p> Name Type Description Default <code>primitives</code> <code>list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]</code> <p>List of geometry primitives.</p> required <code>anchor_point</code> <code>str</code> <p>Name of anchor point.</p> required <code>center</code> <code>Point | None</code> <p>Optional center point.</p> <code>None</code> <p>Returns:</p> Type Description <code>Point | None</code> <p>The anchor point or None.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def get_primitives_anchor_point(\n    primitives: list[\n        \"Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc\"\n    ],\n    anchor_point: str,\n    center: Point | None = None,\n) -&gt; Point | None:\n    \"\"\"Get an anchor point of geometry primitives.\n\n    Args:\n        primitives: List of geometry primitives.\n        anchor_point: Name of anchor point.\n        center: Optional center point.\n\n    Returns:\n        The anchor point or None.\n    \"\"\"\n    bboxes = [p.bbox() for p in primitives]\n    bbox = Bbox.union(bboxes)\n    if center is None:\n        center = bbox.center()\n    if center.isnan():\n        return None\n    point = bbox.anchor_point(anchor_point)\n    return get_primitives_border(primitives, point, center)\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.get_primitives_angle","title":"get_primitives_angle","text":"<pre><code>get_primitives_angle(primitives: list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc], angle: float, unit: Literal['degrees', 'radians'] = 'degrees', center: Point | None = None) -&gt; Point | None\n</code></pre> <p>Get the border point at a given angle.</p> <p>Parameters:</p> Name Type Description Default <code>primitives</code> <code>list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]</code> <p>List of geometry primitives.</p> required <code>angle</code> <code>float</code> <p>The angle.</p> required <code>unit</code> <code>Literal['degrees', 'radians']</code> <p>Unit of angle ('degrees' or 'radians').</p> <code>'degrees'</code> <code>center</code> <code>Point | None</code> <p>Optional center point.</p> <code>None</code> <p>Returns:</p> Type Description <code>Point | None</code> <p>The border point or None.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def get_primitives_angle(\n    primitives: list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc],\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n    center: Point | None = None,\n) -&gt; Point | None:\n    \"\"\"Get the border point at a given angle.\n\n    Args:\n        primitives: List of geometry primitives.\n        angle: The angle.\n        unit: Unit of angle ('degrees' or 'radians').\n        center: Optional center point.\n\n    Returns:\n        The border point or None.\n    \"\"\"\n    if unit == \"degrees\":\n        angle = math.radians(angle)\n    angle = -angle\n    d = 100\n    if center is None:\n        bboxes = [p.bbox() for p in primitives]\n        bbox = Bbox.union(bboxes)\n        center = bbox.center()\n        if center.isnan():\n            return None\n    point = center + (d * math.cos(angle), d * math.sin(angle))\n    return get_primitives_border(primitives, point, center)\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.get_primitives_border","title":"get_primitives_border","text":"<pre><code>get_primitives_border(primitives: list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc], point: Point, center: Point | None = None) -&gt; Point | None\n</code></pre> <p>Get the border point of geometry primitives in a given direction.</p> <p>Parameters:</p> Name Type Description Default <code>primitives</code> <code>list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]</code> <p>List of geometry primitives.</p> required <code>point</code> <code>Point</code> <p>Direction point.</p> required <code>center</code> <code>Point | None</code> <p>Optional center point. Defaults to bbox center.</p> <code>None</code> <p>Returns:</p> Type Description <code>Point | None</code> <p>The border point or None.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def get_primitives_border(\n    primitives: list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc],\n    point: Point,\n    center: Point | None = None,\n) -&gt; Point | None:\n    \"\"\"Get the border point of geometry primitives in a given direction.\n\n    Args:\n        primitives: List of geometry primitives.\n        point: Direction point.\n        center: Optional center point. Defaults to bbox center.\n\n    Returns:\n        The border point or None.\n    \"\"\"\n    if not primitives:\n        return None\n    if center is None:\n        bboxes = [p.bbox() for p in primitives]\n        bbox = Bbox.union(bboxes)\n        center = bbox.center()\n    if center.isnan():\n        return None\n    line = Line(center, point)\n    candidate_points = []\n    for primitive in primitives:\n        candidate_points.extend(_intersect_line_with_primitive(line, primitive))\n    intersection_point = None\n    max_d = -1\n    ok_direction_exists = False\n    d1 = Segment(point, center).length()\n    for candidate_point in candidate_points:\n        d2 = Segment(candidate_point, point).length()\n        d3 = Segment(candidate_point, center).length()\n        candidate_ok_direction = not d2 &gt; d1 or d2 &lt; d3\n        if candidate_ok_direction or not ok_direction_exists:\n            if candidate_ok_direction and not ok_direction_exists:\n                ok_direction_exists = True\n                max_d = -1\n            if d3 &gt; max_d:\n                max_d = d3\n                intersection_point = candidate_point\n    return intersection_point\n</code></pre>"},{"location":"api_reference/geometry/#momapy.geometry.get_transformation_for_frame","title":"get_transformation_for_frame","text":"<pre><code>get_transformation_for_frame(origin: Point, unit_x: Point, unit_y: Point) -&gt; MatrixTransformation\n</code></pre> <p>Get transformation for a frame defined by origin and axes.</p> <p>Given a frame F defined by its origin, unit x axis vector, and unit y axis vector, returns the transformation that converts points from F coordinates to reference frame coordinates.</p> <p>Parameters:</p> Name Type Description Default <code>origin</code> <code>Point</code> <p>Origin of the frame.</p> required <code>unit_x</code> <code>Point</code> <p>Unit x-axis vector.</p> required <code>unit_y</code> <code>Point</code> <p>Unit y-axis vector.</p> required <p>Returns:</p> Type Description <code>MatrixTransformation</code> <p>The transformation matrix.</p> Source code in <code>src/momapy/geometry.py</code> <pre><code>def get_transformation_for_frame(\n    origin: Point, unit_x: Point, unit_y: Point\n) -&gt; MatrixTransformation:\n    \"\"\"Get transformation for a frame defined by origin and axes.\n\n    Given a frame F defined by its origin, unit x axis vector, and unit y axis\n    vector, returns the transformation that converts points from F coordinates\n    to reference frame coordinates.\n\n    Args:\n        origin: Origin of the frame.\n        unit_x: Unit x-axis vector.\n        unit_y: Unit y-axis vector.\n\n    Returns:\n        The transformation matrix.\n    \"\"\"\n    m = numpy.array(\n        [\n            [\n                unit_x.x - origin.x,\n                unit_y.x - origin.x,\n                origin.x,\n            ],\n            [\n                unit_x.y - origin.y,\n                unit_y.y - origin.y,\n                origin.y,\n            ],\n            [0, 0, 1],\n        ],\n        dtype=float,\n    )\n    return MatrixTransformation(m)\n</code></pre>"},{"location":"api_reference/io/","title":"I/O","text":""},{"location":"api_reference/io/#momapy.io","title":"momapy.io","text":"<p>IO subpackage for reading and writing maps.</p> <p>Provides functions and registries for reading and writing molecular maps in various formats (SBGN-ML, CellDesigner, SBML, etc.).</p> <p>Examples:</p> <pre><code>from momapy.io import get_reader, get_writer\nreader = get_reader(\"sbgnml\")\nwriter = get_writer(\"sbgnml\")\n</code></pre> <p>Modules:</p> Name Description <code>core</code> <p>Base classes and functions for reading and writing maps.</p> <code>pickle</code> <p>Pickle-based reader and writer.</p> <p>Classes:</p> Name Description <code>IOResult</code> <p>Base class for I/O results.</p> <code>Reader</code> <p>Abstract base class for map readers.</p> <code>ReaderResult</code> <p>Result from reading a map file.</p> <code>Writer</code> <p>Abstract base class for map writers.</p> <code>WriterResult</code> <p>Result from writing a map file.</p> <p>Functions:</p> Name Description <code>get_reader</code> <p>Get a reader class by name.</p> <code>get_writer</code> <p>Get a writer class by name.</p> <code>list_readers</code> <p>List all available reader names.</p> <code>list_writers</code> <p>List all available writer names.</p> <code>read</code> <p>Read a map file.</p> <code>register_lazy_reader</code> <p>Register a reader for lazy loading.</p> <code>register_lazy_writer</code> <p>Register a writer for lazy loading.</p> <code>register_reader</code> <p>Register a reader class.</p> <code>register_writer</code> <p>Register a writer class.</p> <code>write</code> <p>Write an object to a file.</p>"},{"location":"api_reference/io/#momapy.io.IOResult","title":"IOResult  <code>dataclass</code>","text":"<pre><code>IOResult()\n</code></pre> <p>Base class for I/O results.</p>"},{"location":"api_reference/io/#momapy.io.Reader","title":"Reader","text":"<p>               Bases: <code>ABC</code></p> <p>Abstract base class for map readers.</p> <p>Implementations must override read() and check_file() methods.</p> <p>Examples:</p> <pre><code>class MyFormatReader(Reader):\n    @classmethod\n    def read(cls, file_path, **options):\n\n        # Implementation\n        pass\n\n    @classmethod\n    def check_file(cls, file_path):\n        return file_path.endswith(\".myfmt\")\n</code></pre> <p>Methods:</p> Name Description <code>check_file</code> <p>Check if this reader supports the given file.</p> <code>read</code> <p>Read a file and return the result.</p>"},{"location":"api_reference/io/#momapy.io.Reader.check_file","title":"check_file  <code>abstractmethod</code> <code>classmethod</code>","text":"<pre><code>check_file(file_path: str | PathLike) -&gt; bool\n</code></pre> <p>Check if this reader supports the given file.</p> <p>Parameters:</p> Name Type Description Default <code>file_path</code> <code>str | PathLike</code> <p>Path of the file to check.</p> required <p>Returns:</p> Type Description <code>bool</code> <p>True if the file is supported by this reader.</p> Source code in <code>src/momapy/io/core.py</code> <pre><code>@classmethod\n@abc.abstractmethod\ndef check_file(cls, file_path: str | os.PathLike) -&gt; bool:\n    \"\"\"Check if this reader supports the given file.\n\n    Args:\n        file_path: Path of the file to check.\n\n    Returns:\n        True if the file is supported by this reader.\n    \"\"\"\n    pass\n</code></pre>"},{"location":"api_reference/io/#momapy.io.Reader.read","title":"read  <code>abstractmethod</code> <code>classmethod</code>","text":"<pre><code>read(file_path: str | PathLike, **options: Any) -&gt; ReaderResult\n</code></pre> <p>Read a file and return the result.</p> <p>Concrete readers accept the universal option set with these names, types, and defaults, plus a trailing <code>**options</code> so unknown keywords are accepted and ignored:</p> <ul> <li><code>return_type: Literal[\"map\", \"model\", \"layout\"] = \"map\"</code> \u2014 the shape   of <code>result.obj</code>. <code>\"map\"</code> returns a <code>Map</code> (the uniform default for   every format), <code>\"model\"</code> returns the <code>Model</code>, <code>\"layout\"</code> returns the   <code>Layout</code>. A layout-less format (SBML) raises <code>NotImplementedError</code>   for <code>\"layout\"</code> and returns a <code>Map</code> with <code>layout=None</code> for <code>\"map\"</code>.</li> <li><code>with_model: bool = True</code> \u2014 whether to build the model.</li> <li><code>with_layout: bool = True</code> \u2014 whether to build the layout (no effect   on layout-less formats).</li> <li><code>with_annotations: bool = True</code> \u2014 whether to read annotations.</li> <li><code>with_notes: bool = True</code> \u2014 whether to read notes.</li> </ul> <p>Parameters:</p> Name Type Description Default <code>file_path</code> <code>str | PathLike</code> <p>Path of the file to read.</p> required <code>options</code> <code>Any</code> <p>Reader-specific options (see the universal set above).</p> <code>{}</code> <p>Returns:</p> Type Description <code>ReaderResult</code> <p>ReaderResult containing the read object.</p> Source code in <code>src/momapy/io/core.py</code> <pre><code>@classmethod\n@abc.abstractmethod\ndef read(cls, file_path: str | os.PathLike, **options: typing.Any) -&gt; ReaderResult:\n    \"\"\"Read a file and return the result.\n\n    Concrete readers accept the universal option set with these names,\n    types, and defaults, plus a trailing `**options` so unknown keywords\n    are accepted and ignored:\n\n    - `return_type: Literal[\"map\", \"model\", \"layout\"] = \"map\"` \u2014 the shape\n      of `result.obj`. `\"map\"` returns a `Map` (the uniform default for\n      every format), `\"model\"` returns the `Model`, `\"layout\"` returns the\n      `Layout`. A layout-less format (SBML) raises `NotImplementedError`\n      for `\"layout\"` and returns a `Map` with `layout=None` for `\"map\"`.\n    - `with_model: bool = True` \u2014 whether to build the model.\n    - `with_layout: bool = True` \u2014 whether to build the layout (no effect\n      on layout-less formats).\n    - `with_annotations: bool = True` \u2014 whether to read annotations.\n    - `with_notes: bool = True` \u2014 whether to read notes.\n\n    Args:\n        file_path: Path of the file to read.\n        options: Reader-specific options (see the universal set above).\n\n    Returns:\n        ReaderResult containing the read object.\n    \"\"\"\n    pass\n</code></pre>"},{"location":"api_reference/io/#momapy.io.ReaderResult","title":"ReaderResult  <code>dataclass</code>","text":"<pre><code>ReaderResult(*, obj: Any = None, element_to_annotations: frozendict | None = None, element_to_notes: frozendict | None = None, id_to_element: frozendict | None = None, source_id_to_model_element: FrozenIdentityMultiDict | None = None, source_id_to_layout_element: FrozenSurjectionDict | None = None, source_id_to_annotations: frozendict | None = None, source_id_to_notes: frozendict | None = None, file_path: str | PathLike | None = None)\n</code></pre> <p>               Bases: <code>IOResult</code></p> <p>Result from reading a map file.</p> <p>Parameters:</p> Name Type Description Default <code>obj</code> <code>Any</code> <p>The read map object (MapElement or None).</p> <code>None</code> <code>element_to_annotations</code> <code>frozendict | None</code> <p>Mapping from map elements to their annotations.</p> <code>None</code> <code>element_to_notes</code> <code>frozendict | None</code> <p>Mapping from map elements to their notes.</p> <code>None</code> <code>id_to_element</code> <code>frozendict | None</code> <p>Mapping from momapy element IDs to elements. Contains all model and layout elements regardless of source ID provenance.</p> <code>None</code> <code>source_id_to_model_element</code> <code>FrozenIdentityMultiDict | None</code> <p>Mapping from source file IDs (e.g. XML attributes) to the set of model elements named by that ID. Only contains IDs that exist verbatim in the source file. One source ID may name several model elements \u2014 for example a CellDesigner <code>species/@id</code> can point at both the active and inactive variants of the same species. This is a <code>Mapping[str, frozenset[ModelElement]]</code>: use <code>.get(id_, frozenset())</code> to recover the model elements named by an ID, or the <code>.inverse</code> property (<code>id(element) -&gt; frozenset[str]</code>) to find every source ID that named a given model element.</p> <code>None</code> <code>source_id_to_layout_element</code> <code>FrozenSurjectionDict | None</code> <p>Mapping from source file IDs (e.g. XML attributes) to layout elements. Only contains IDs that exist verbatim in the source file. Unlike <code>source_id_to_model_element</code>, this is a <code>Mapping[str, LayoutElement]</code>: each source ID names a single layout element. Use <code>[id_]</code> / <code>.get(id_)</code> to recover the layout element named by an ID, or the <code>.inverse</code> property \u2014 an equality-keyed (not <code>id()</code>-keyed) <code>LayoutElement -&gt; frozenset[str]</code> \u2014 to find every source ID that named a given layout element.</p> <code>None</code> <code>source_id_to_annotations</code> <code>frozendict | None</code> <p>Mapping from source file IDs to the RDF/MIRIAM annotations attached to that specific source element. Parallel to <code>element_to_annotations</code> but preserves per-source granularity when several source elements deduplicate to a single model element. Combine with <code>source_id_to_model_element.inverse</code> to recover the full set of source-side annotations that merged into a given model element.</p> <code>None</code> <code>source_id_to_notes</code> <code>frozendict | None</code> <p>Mapping from source file IDs to the free text <code>&lt;notes&gt;</code> blocks attached to that specific source element. Parallel to <code>element_to_notes</code>; same per-source semantics as <code>source_id_to_annotations</code>.</p> <code>None</code> <code>file_path</code> <code>str | PathLike | None</code> <p>Path of the file that was read.</p> <code>None</code>"},{"location":"api_reference/io/#momapy.io.Writer","title":"Writer","text":"<p>               Bases: <code>ABC</code></p> <p>Abstract base class for map writers.</p> <p>Implementations must override the write() method.</p> <p>Examples:</p> <pre><code>class MyFormatWriter(Writer):\n    @classmethod\n    def write(cls, obj, file_path, **options):\n\n        # Implementation\n        pass\n</code></pre> <p>Methods:</p> Name Description <code>write</code> <p>Write an object to a file.</p>"},{"location":"api_reference/io/#momapy.io.Writer.write","title":"write  <code>abstractmethod</code> <code>classmethod</code>","text":"<pre><code>write(obj: Any, file_path: str | PathLike, **options: Any) -&gt; WriterResult\n</code></pre> <p>Write an object to a file.</p> <p>Parameters:</p> Name Type Description Default <code>obj</code> <code>Any</code> <p>Object to write.</p> required <code>file_path</code> <code>str | PathLike</code> <p>Path of the file to write to.</p> required <code>options</code> <code>Any</code> <p>Writer-specific options.</p> <code>{}</code> <p>Returns:</p> Type Description <code>WriterResult</code> <p>WriterResult containing the written object.</p> Source code in <code>src/momapy/io/core.py</code> <pre><code>@classmethod\n@abc.abstractmethod\ndef write(\n    cls,\n    obj: typing.Any,\n    file_path: str | os.PathLike,\n    **options: typing.Any,\n) -&gt; WriterResult:\n    \"\"\"Write an object to a file.\n\n    Args:\n        obj: Object to write.\n        file_path: Path of the file to write to.\n        options: Writer-specific options.\n\n    Returns:\n        WriterResult containing the written object.\n    \"\"\"\n    pass\n</code></pre>"},{"location":"api_reference/io/#momapy.io.WriterResult","title":"WriterResult  <code>dataclass</code>","text":"<pre><code>WriterResult(*, obj: Any = None, file_path: str | PathLike | None = None)\n</code></pre> <p>               Bases: <code>IOResult</code></p> <p>Result from writing a map file.</p> <p>Parameters:</p> Name Type Description Default <code>obj</code> <code>Any</code> <p>The written object.</p> <code>None</code> <code>file_path</code> <code>str | PathLike | None</code> <p>Path of the file that was written.</p> <code>None</code>"},{"location":"api_reference/io/#momapy.io.get_reader","title":"get_reader","text":"<pre><code>get_reader(name: str) -&gt; type[Reader]\n</code></pre> <p>Get a reader class by name.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Reader name (e.g., \"sbgnml\", \"celldesigner\").</p> required <p>Returns:</p> Type Description <code>type[Reader]</code> <p>Reader class for the specified format.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If no reader with that name exists.</p> <code>ImportError</code> <p>If the reader is registered but its module cannot be imported (e.g. a missing optional dependency).</p> Source code in <code>src/momapy/io/core.py</code> <pre><code>def get_reader(name: str) -&gt; type[\"Reader\"]:\n    \"\"\"Get a reader class by name.\n\n    Args:\n        name: Reader name (e.g., \"sbgnml\", \"celldesigner\").\n\n    Returns:\n        Reader class for the specified format.\n\n    Raises:\n        ValueError: If no reader with that name exists.\n        ImportError: If the reader is registered but its module cannot be\n            imported (e.g. a missing optional dependency).\n    \"\"\"\n    reader = reader_registry.get(name)\n    if reader is None:\n        available = reader_registry.list_available()\n        raise ValueError(\n            f\"No reader named '{name}'. Available readers: {', '.join(available)}\"\n        )\n    return reader\n</code></pre>"},{"location":"api_reference/io/#momapy.io.get_writer","title":"get_writer","text":"<pre><code>get_writer(name: str) -&gt; type[Writer]\n</code></pre> <p>Get a writer class by name.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Writer name (e.g., \"sbgnml\", \"pickle\").</p> required <p>Returns:</p> Type Description <code>type[Writer]</code> <p>Writer class for the specified format.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If no writer with that name exists.</p> <code>ImportError</code> <p>If the writer is registered but its module cannot be imported (e.g. a missing optional dependency).</p> Source code in <code>src/momapy/io/core.py</code> <pre><code>def get_writer(name: str) -&gt; type[\"Writer\"]:\n    \"\"\"Get a writer class by name.\n\n    Args:\n        name: Writer name (e.g., \"sbgnml\", \"pickle\").\n\n    Returns:\n        Writer class for the specified format.\n\n    Raises:\n        ValueError: If no writer with that name exists.\n        ImportError: If the writer is registered but its module cannot be\n            imported (e.g. a missing optional dependency).\n    \"\"\"\n    writer = writer_registry.get(name)\n    if writer is None:\n        available = writer_registry.list_available()\n        raise ValueError(\n            f\"No writer named '{name}'. Available writers: {', '.join(available)}\"\n        )\n    return writer\n</code></pre>"},{"location":"api_reference/io/#momapy.io.list_readers","title":"list_readers","text":"<pre><code>list_readers() -&gt; list[str]\n</code></pre> <p>List all available reader names.</p> <p>Returns:</p> Type Description <code>list[str]</code> <p>Sorted list of available reader names.</p> Source code in <code>src/momapy/io/core.py</code> <pre><code>def list_readers() -&gt; list[str]:\n    \"\"\"List all available reader names.\n\n    Returns:\n        Sorted list of available reader names.\n    \"\"\"\n    return reader_registry.list_available()\n</code></pre>"},{"location":"api_reference/io/#momapy.io.list_writers","title":"list_writers","text":"<pre><code>list_writers() -&gt; list[str]\n</code></pre> <p>List all available writer names.</p> <p>Returns:</p> Type Description <code>list[str]</code> <p>Sorted list of available writer names.</p> Source code in <code>src/momapy/io/core.py</code> <pre><code>def list_writers() -&gt; list[str]:\n    \"\"\"List all available writer names.\n\n    Returns:\n        Sorted list of available writer names.\n    \"\"\"\n    return writer_registry.list_available()\n</code></pre>"},{"location":"api_reference/io/#momapy.io.read","title":"read","text":"<pre><code>read(file_path: str | PathLike, reader: str | None = None, **options: Any) -&gt; ReaderResult\n</code></pre> <p>Read a map file.</p> <p>If reader is specified, uses that reader. Otherwise, checks registered readers in registration order and uses the first whose check_file method accepts the file. Registration order matters: a more specific reader must be registered before a more general one it specializes. For example a CellDesigner file is also a valid SBML file, so the CellDesigner reader is registered (and therefore checked) before the SBML reader.</p> <p>Return contract: the shape of <code>result.obj</code> is governed by the universal <code>return_type</code> option, uniformly across every format:</p> <ul> <li><code>return_type=\"map\"</code> (the default) returns a <code>Map</code>. This is the uniform   default for all formats; a layout-less format (SBML) returns a <code>Map</code>   with <code>layout=None</code> and <code>layout_model_mapping=None</code>.</li> <li><code>return_type=\"model\"</code> returns the bare <code>Model</code>.</li> <li><code>return_type=\"layout\"</code> returns the <code>Layout</code>; a layout-less format (SBML)   raises <code>NotImplementedError</code>.</li> </ul> <p>Universal options accepted by every reader (with these defaults): <code>return_type=\"map\"</code>, <code>with_model=True</code>, <code>with_layout=True</code>, <code>with_annotations=True</code>, <code>with_notes=True</code>. Unknown keyword options are accepted and ignored. Some formats accept extra options (for example the SBGN-ML reader's <code>xsep</code>/<code>ysep</code>). A universal option may also be inert for a given format: a layout-less format (SBML) ignores <code>with_layout</code>, since it never produces a layout.</p> <p>Parameters:</p> Name Type Description Default <code>file_path</code> <code>str | PathLike</code> <p>Path of the file to read.</p> required <code>reader</code> <code>str | None</code> <p>Name of registered reader to use (e.g., \"sbgnml\"). If None, auto-detects based on file format.</p> <code>None</code> <code>options</code> <code>Any</code> <p>Additional options passed to the reader (see the universal option set above, plus any format-specific options).</p> <code>{}</code> <p>Returns:</p> Type Description <code>ReaderResult</code> <p>ReaderResult containing the read object and metadata.</p> <p>Raises:</p> Type Description <code>FileNotFoundError</code> <p>If no file exists at <code>file_path</code>.</p> <code>ValueError</code> <p>If no suitable reader is found.</p> <code>NotImplementedError</code> <p>If <code>return_type=\"layout\"</code> is requested for a layout-less format (e.g. SBML).</p> <p>Examples:</p> <pre><code>from momapy.io import read\nresult = read(\"map.sbgn\")\nmap_obj = result.obj\n</code></pre> Source code in <code>src/momapy/io/core.py</code> <pre><code>def read(\n    file_path: str | os.PathLike,\n    reader: str | None = None,\n    **options: typing.Any,\n) -&gt; ReaderResult:\n    \"\"\"Read a map file.\n\n    If reader is specified, uses that reader. Otherwise, checks registered\n    readers in registration order and uses the first whose check_file method\n    accepts the file. Registration order matters: a more specific reader must\n    be registered before a more general one it specializes. For example a\n    CellDesigner file is also a valid SBML file, so the CellDesigner reader is\n    registered (and therefore checked) before the SBML reader.\n\n    Return contract: the shape of `result.obj` is governed by the universal\n    `return_type` option, uniformly across every format:\n\n    - `return_type=\"map\"` (the default) returns a `Map`. This is the uniform\n      default for all formats; a layout-less format (SBML) returns a `Map`\n      with `layout=None` and `layout_model_mapping=None`.\n    - `return_type=\"model\"` returns the bare `Model`.\n    - `return_type=\"layout\"` returns the `Layout`; a layout-less format (SBML)\n      raises `NotImplementedError`.\n\n    Universal options accepted by every reader (with these defaults):\n    `return_type=\"map\"`, `with_model=True`, `with_layout=True`,\n    `with_annotations=True`, `with_notes=True`. Unknown keyword options are\n    accepted and ignored. Some formats accept extra options (for example the\n    SBGN-ML reader's `xsep`/`ysep`). A universal option may also be inert for a\n    given format: a layout-less format (SBML) ignores `with_layout`, since it\n    never produces a layout.\n\n    Args:\n        file_path: Path of the file to read.\n        reader: Name of registered reader to use (e.g., \"sbgnml\"). If None,\n            auto-detects based on file format.\n        options: Additional options passed to the reader (see the universal\n            option set above, plus any format-specific options).\n\n    Returns:\n        ReaderResult containing the read object and metadata.\n\n    Raises:\n        FileNotFoundError: If no file exists at `file_path`.\n        ValueError: If no suitable reader is found.\n        NotImplementedError: If `return_type=\"layout\"` is requested for a\n            layout-less format (e.g. SBML).\n\n    Examples:\n        ```python\n        from momapy.io import read\n        result = read(\"map.sbgn\")\n        map_obj = result.obj\n        ```\n    \"\"\"\n    check_file_exists(file_path)\n    if reader is not None:\n        reader_cls = get_reader(reader)\n    else:\n        reader_cls = None\n        for name in reader_registry.list_available_in_registration_order():\n            candidate_cls = get_reader(name)\n            if candidate_cls.check_file(file_path):\n                reader_cls = candidate_cls\n                break\n        if reader_cls is None:\n            raise ValueError(\n                f\"could not find a suitable registered reader for file '{file_path}'\"\n            )\n    result = reader_cls.read(file_path, **options)\n    return result\n</code></pre>"},{"location":"api_reference/io/#momapy.io.register_lazy_reader","title":"register_lazy_reader","text":"<pre><code>register_lazy_reader(name: str, import_path: str) -&gt; None\n</code></pre> <p>Register a reader for lazy loading.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Name to register the reader under.</p> required <code>import_path</code> <code>str</code> <p>Import path in format \"module.path:ClassName\".</p> required Source code in <code>src/momapy/io/core.py</code> <pre><code>def register_lazy_reader(name: str, import_path: str) -&gt; None:\n    \"\"\"Register a reader for lazy loading.\n\n    Args:\n        name: Name to register the reader under.\n        import_path: Import path in format \"module.path:ClassName\".\n    \"\"\"\n    reader_registry.register_lazy(name, import_path)\n</code></pre>"},{"location":"api_reference/io/#momapy.io.register_lazy_writer","title":"register_lazy_writer","text":"<pre><code>register_lazy_writer(name: str, import_path: str) -&gt; None\n</code></pre> <p>Register a writer for lazy loading.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Name to register the writer under.</p> required <code>import_path</code> <code>str</code> <p>Import path in format \"module.path:ClassName\".</p> required Source code in <code>src/momapy/io/core.py</code> <pre><code>def register_lazy_writer(name: str, import_path: str) -&gt; None:\n    \"\"\"Register a writer for lazy loading.\n\n    Args:\n        name: Name to register the writer under.\n        import_path: Import path in format \"module.path:ClassName\".\n    \"\"\"\n    writer_registry.register_lazy(name, import_path)\n</code></pre>"},{"location":"api_reference/io/#momapy.io.register_reader","title":"register_reader","text":"<pre><code>register_reader(name: str, cls: type[Reader]) -&gt; None\n</code></pre> <p>Register a reader class.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Name to register the reader under.</p> required <code>cls</code> <code>type[Reader]</code> <p>Reader class (must inherit from Reader).</p> required Source code in <code>src/momapy/io/core.py</code> <pre><code>def register_reader(name: str, cls: type[\"Reader\"]) -&gt; None:\n    \"\"\"Register a reader class.\n\n    Args:\n        name: Name to register the reader under.\n        cls: Reader class (must inherit from Reader).\n    \"\"\"\n    reader_registry.register(name, cls)\n</code></pre>"},{"location":"api_reference/io/#momapy.io.register_writer","title":"register_writer","text":"<pre><code>register_writer(name: str, cls: type[Writer]) -&gt; None\n</code></pre> <p>Register a writer class.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Name to register the writer under.</p> required <code>cls</code> <code>type[Writer]</code> <p>Writer class (must inherit from Writer).</p> required Source code in <code>src/momapy/io/core.py</code> <pre><code>def register_writer(name: str, cls: type[\"Writer\"]) -&gt; None:\n    \"\"\"Register a writer class.\n\n    Args:\n        name: Name to register the writer under.\n        cls: Writer class (must inherit from Writer).\n    \"\"\"\n    writer_registry.register(name, cls)\n</code></pre>"},{"location":"api_reference/io/#momapy.io.write","title":"write","text":"<pre><code>write(obj: Any, file_path: str | PathLike, writer: str | None = None, **options: Any) -&gt; WriterResult\n</code></pre> <p>Write an object to a file.</p> <p>Parameters:</p> Name Type Description Default <code>obj</code> <code>Any</code> <p>Object to write (typically a MapElement).</p> required <code>file_path</code> <code>str | PathLike</code> <p>Path of the file to write to.</p> required <code>writer</code> <code>str | None</code> <p>Name of registered writer to use (e.g., \"sbgnml\"). When <code>None</code> (the default), the writer is inferred from the map type: SBGN PD/AF maps use <code>sbgnml</code>, CellDesigner maps use <code>celldesigner</code>, and SBML maps raise a clear \"read-only\" error.</p> <code>None</code> <code>options</code> <code>Any</code> <p>Additional options passed to the writer.</p> <code>{}</code> <p>Returns:</p> Type Description <code>WriterResult</code> <p>WriterResult containing the written object and metadata.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the writer is not found or cannot be inferred.</p> <p>Examples:</p> <pre><code>from momapy.io import write\nwrite(map_obj, \"output.sbgn\")\n</code></pre> Source code in <code>src/momapy/io/core.py</code> <pre><code>def write(\n    obj: typing.Any,\n    file_path: str | os.PathLike,\n    writer: str | None = None,\n    **options: typing.Any,\n) -&gt; WriterResult:\n    \"\"\"Write an object to a file.\n\n    Args:\n        obj: Object to write (typically a MapElement).\n        file_path: Path of the file to write to.\n        writer: Name of registered writer to use (e.g., \"sbgnml\"). When\n            ``None`` (the default), the writer is inferred from the map type:\n            SBGN PD/AF maps use ``sbgnml``, CellDesigner maps use\n            ``celldesigner``, and SBML maps raise a clear \"read-only\" error.\n        options: Additional options passed to the writer.\n\n    Returns:\n        WriterResult containing the written object and metadata.\n\n    Raises:\n        ValueError: If the writer is not found or cannot be inferred.\n\n    Examples:\n        ```python\n        from momapy.io import write\n        write(map_obj, \"output.sbgn\")\n        ```\n    \"\"\"\n    if writer is None:\n        writer = _detect_writer(obj)\n    writer_cls = get_writer(writer)\n    result = writer_cls.write(obj, file_path, **options)\n    return result\n</code></pre>"},{"location":"api_reference/positioning/","title":"Positioning","text":""},{"location":"api_reference/positioning/#momapy.positioning","title":"momapy.positioning","text":"<p>Functions for positioning layout elements and related objects relatively to other objects.</p> <p>This module provides utilities for calculating positions relative to layout elements, fitting bounding boxes around collections of elements, and setting positions of builder objects. Functions support Points, Bboxes, Nodes, and their builder variants.</p> <p>Examples:</p> <pre><code>from momapy.positioning import right_of, fit\nfrom momapy.geometry import Point\npoint = Point(0.0, 0.0)\nnew_point = right_of(point, 10)\nprint(new_point)\n</code></pre> <p>Functions:</p> Name Description <code>above_left_of</code> <p>Return a point above and to the left of the given object.</p> <code>above_of</code> <p>Return a point above the given object at the specified distance.</p> <code>above_right_of</code> <p>Return a point above and to the right of the given object.</p> <code>below_left_of</code> <p>Return a point below and to the left of the given object.</p> <code>below_of</code> <p>Return a point below the given object at the specified distance.</p> <code>below_right_of</code> <p>Return a point below and to the right of the given object.</p> <code>cross_hv_of</code> <p>Return the point at the horizontal cross of obj1 and vertical cross of obj2.</p> <code>cross_vh_of</code> <p>Return the point at the vertical cross of obj1 and horizontal cross of obj2.</p> <code>fit</code> <p>Compute a bounding box that fits the given elements with optional margins.</p> <code>fraction_of</code> <p>Return the position and angle at a given fraction along an arc.</p> <code>left_of</code> <p>Return a point to the left of the given object at the specified distance.</p> <code>mid_of</code> <p>Return the midpoint between two objects.</p> <code>right_of</code> <p>Return a point to the right of the given object at the specified distance.</p> <code>set_above_left_of</code> <p>Set obj1's position above and to the left of obj2.</p> <code>set_above_of</code> <p>Set obj1's position above obj2 at the specified distance.</p> <code>set_above_right_of</code> <p>Set obj1's position above and to the right of obj2.</p> <code>set_below_left_of</code> <p>Set obj1's position below and to the left of obj2.</p> <code>set_below_of</code> <p>Set obj1's position below obj2 at the specified distance.</p> <code>set_below_right_of</code> <p>Set obj1's position below and to the right of obj2.</p> <code>set_cross_hv_of</code> <p>Set obj1's position at the horizontal-vertical cross of obj2 and obj3.</p> <code>set_cross_vh_of</code> <p>Set obj1's position at the vertical-horizontal cross of obj2 and obj3.</p> <code>set_fit</code> <p>Set obj's dimensions and position to fit the given elements.</p> <code>set_fraction_of</code> <p>Set obj's position and rotation along an arc at the given fraction.</p> <code>set_left_of</code> <p>Set obj1's position to the left of obj2 at the specified distance.</p> <code>set_mid_of</code> <p>Set obj1's position to the midpoint between obj2 and obj3.</p> <code>set_position</code> <p>Set the position of a builder object.</p> <code>set_right_of</code> <p>Set obj1's position to the right of obj2 at the specified distance.</p>"},{"location":"api_reference/positioning/#momapy.positioning.above_left_of","title":"above_left_of","text":"<pre><code>above_left_of(obj: Point | Bbox | Node | Builder, distance_y: float, distance_x: float | None = None) -&gt; Point\n</code></pre> <p>Return a point above and to the left of the given object.</p> <p>Parameters:</p> Name Type Description Default <code>obj</code> <code>Point | Bbox | Node | Builder</code> <p>A Point, Bbox, Node, or their builder variants. For Nodes, uses the north-west anchor point.</p> required <code>distance_y</code> <code>float</code> <p>The vertical distance (northward) from the object.</p> required <code>distance_x</code> <code>float | None</code> <p>The horizontal distance (westward) from the object. If None, defaults to distance_y.</p> <code>None</code> <p>Returns:</p> Type Description <code>Point</code> <p>A new Point located above and to the left of the object.</p> <p>Raises:</p> Type Description <code>TypeError</code> <p>If obj is not a supported type.</p> <p>Examples:</p> <pre><code>point = Point(10.0, 10.0)\nabove_left_of(point, 5)\n</code></pre> Source code in <code>src/momapy/positioning.py</code> <pre><code>def above_left_of(\n    obj: (Point | Bbox | Node | Builder),\n    distance_y: float,\n    distance_x: float | None = None,\n) -&gt; Point:\n    \"\"\"Return a point above and to the left of the given object.\n\n    Args:\n        obj: A Point, Bbox, Node, or their builder variants.\n            For Nodes, uses the north-west anchor point.\n        distance_y: The vertical distance (northward) from the object.\n        distance_x: The horizontal distance (westward) from the object.\n            If None, defaults to distance_y.\n\n    Returns:\n        A new Point located above and to the left of the object.\n\n    Raises:\n        TypeError: If obj is not a supported type.\n\n    Examples:\n        ```python\n        point = Point(10.0, 10.0)\n        above_left_of(point, 5)\n        ```\n    \"\"\"\n    if distance_x is None:\n        distance_x = distance_y\n    if isinstance_or_builder(obj, Point):\n        source_point = obj\n    elif isinstance_or_builder(obj, (Bbox, Node)):\n        source_point = obj.north_west()\n    else:\n        raise TypeError(f\"{type(obj).__name__} not supported\")\n    return source_point - (distance_x, distance_y)\n</code></pre>"},{"location":"api_reference/positioning/#momapy.positioning.above_of","title":"above_of","text":"<pre><code>above_of(obj: Point | Bbox | Node | Builder, distance: float) -&gt; Point\n</code></pre> <p>Return a point above the given object at the specified distance.</p> <p>Parameters:</p> Name Type Description Default <code>obj</code> <code>Point | Bbox | Node | Builder</code> <p>A Point, Bbox, Node, or their builder variants. For Nodes, uses the north anchor point.</p> required <code>distance</code> <code>float</code> <p>The vertical distance from the object's reference point.</p> required <p>Returns:</p> Type Description <code>Point</code> <p>A new Point located above the object.</p> <p>Raises:</p> Type Description <code>TypeError</code> <p>If obj is not a supported type.</p> <p>Examples:</p> <pre><code>point = Point(0.0, 10.0)\nabove_of(point, 5)\n</code></pre> Source code in <code>src/momapy/positioning.py</code> <pre><code>def above_of(\n    obj: (Point | Bbox | Node | Builder),\n    distance: float,\n) -&gt; Point:\n    \"\"\"Return a point above the given object at the specified distance.\n\n    Args:\n        obj: A Point, Bbox, Node, or their builder variants.\n            For Nodes, uses the north anchor point.\n        distance: The vertical distance from the object's reference point.\n\n    Returns:\n        A new Point located above the object.\n\n    Raises:\n        TypeError: If obj is not a supported type.\n\n    Examples:\n        ```python\n        point = Point(0.0, 10.0)\n        above_of(point, 5)\n        ```\n    \"\"\"\n    if isinstance_or_builder(obj, Point):\n        source_point = obj\n    elif isinstance_or_builder(obj, (Bbox, Node)):\n        source_point = obj.north()\n    else:\n        raise TypeError(f\"{type(obj).__name__} not supported\")\n    return source_point - (0, distance)\n</code></pre>"},{"location":"api_reference/positioning/#momapy.positioning.above_right_of","title":"above_right_of","text":"<pre><code>above_right_of(obj: Point | Bbox | Node | Builder, distance_y: float, distance_x: float | None = None) -&gt; Point\n</code></pre> <p>Return a point above and to the right of the given object.</p> <p>Parameters:</p> Name Type Description Default <code>obj</code> <code>Point | Bbox | Node | Builder</code> <p>A Point, Bbox, Node, or their builder variants. For Nodes, uses the north-east anchor point.</p> required <code>distance_y</code> <code>float</code> <p>The vertical distance (northward) from the object.</p> required <code>distance_x</code> <code>float | None</code> <p>The horizontal distance (eastward) from the object. If None, defaults to distance_y.</p> <code>None</code> <p>Returns:</p> Type Description <code>Point</code> <p>A new Point located above and to the right of the object.</p> <p>Raises:</p> Type Description <code>TypeError</code> <p>If obj is not a supported type.</p> <p>Examples:</p> <pre><code>point = Point(0.0, 10.0)\nabove_right_of(point, 5, 10)\n</code></pre> Source code in <code>src/momapy/positioning.py</code> <pre><code>def above_right_of(\n    obj: (Point | Bbox | Node | Builder),\n    distance_y: float,\n    distance_x: float | None = None,\n) -&gt; Point:\n    \"\"\"Return a point above and to the right of the given object.\n\n    Args:\n        obj: A Point, Bbox, Node, or their builder variants.\n            For Nodes, uses the north-east anchor point.\n        distance_y: The vertical distance (northward) from the object.\n        distance_x: The horizontal distance (eastward) from the object.\n            If None, defaults to distance_y.\n\n    Returns:\n        A new Point located above and to the right of the object.\n\n    Raises:\n        TypeError: If obj is not a supported type.\n\n    Examples:\n        ```python\n        point = Point(0.0, 10.0)\n        above_right_of(point, 5, 10)\n        ```\n    \"\"\"\n    if distance_x is None:\n        distance_x = distance_y\n    if isinstance_or_builder(obj, Point):\n        source_point = obj\n    elif isinstance_or_builder(obj, (Bbox, Node)):\n        source_point = obj.north_east()\n    else:\n        raise TypeError(f\"{type(obj).__name__} not supported\")\n    return source_point + (distance_x, -distance_y)\n</code></pre>"},{"location":"api_reference/positioning/#momapy.positioning.below_left_of","title":"below_left_of","text":"<pre><code>below_left_of(obj: Point | Bbox | Node | Builder, distance_y: float, distance_x: float | None = None) -&gt; Point\n</code></pre> <p>Return a point below and to the left of the given object.</p> <p>Parameters:</p> Name Type Description Default <code>obj</code> <code>Point | Bbox | Node | Builder</code> <p>A Point, Bbox, Node, or their builder variants. For Nodes, uses the south-west anchor point.</p> required <code>distance_y</code> <code>float</code> <p>The vertical distance (southward) from the object.</p> required <code>distance_x</code> <code>float | None</code> <p>The horizontal distance (westward) from the object. If None, defaults to distance_y.</p> <code>None</code> <p>Returns:</p> Type Description <code>Point</code> <p>A new Point located below and to the left of the object.</p> <p>Raises:</p> Type Description <code>TypeError</code> <p>If obj is not a supported type.</p> <p>Examples:</p> <pre><code>point = Point(10.0, 0.0)\nbelow_left_of(point, 5)\n</code></pre> Source code in <code>src/momapy/positioning.py</code> <pre><code>def below_left_of(\n    obj: (Point | Bbox | Node | Builder),\n    distance_y: float,\n    distance_x: float | None = None,\n) -&gt; Point:\n    \"\"\"Return a point below and to the left of the given object.\n\n    Args:\n        obj: A Point, Bbox, Node, or their builder variants.\n            For Nodes, uses the south-west anchor point.\n        distance_y: The vertical distance (southward) from the object.\n        distance_x: The horizontal distance (westward) from the object.\n            If None, defaults to distance_y.\n\n    Returns:\n        A new Point located below and to the left of the object.\n\n    Raises:\n        TypeError: If obj is not a supported type.\n\n    Examples:\n        ```python\n        point = Point(10.0, 0.0)\n        below_left_of(point, 5)\n        ```\n    \"\"\"\n    if distance_x is None:\n        distance_x = distance_y\n    if isinstance_or_builder(obj, Point):\n        source_point = obj\n    elif isinstance_or_builder(obj, (Bbox, Node)):\n        source_point = obj.south_west()\n    else:\n        raise TypeError(f\"{type(obj).__name__} not supported\")\n    return source_point + (-distance_x, distance_y)\n</code></pre>"},{"location":"api_reference/positioning/#momapy.positioning.below_of","title":"below_of","text":"<pre><code>below_of(obj: Point | Bbox | Node | Builder, distance: float) -&gt; Point\n</code></pre> <p>Return a point below the given object at the specified distance.</p> <p>Parameters:</p> Name Type Description Default <code>obj</code> <code>Point | Bbox | Node | Builder</code> <p>A Point, Bbox, Node, or their builder variants. For Nodes, uses the south anchor point.</p> required <code>distance</code> <code>float</code> <p>The vertical distance from the object's reference point.</p> required <p>Returns:</p> Type Description <code>Point</code> <p>A new Point located below the object.</p> <p>Raises:</p> Type Description <code>TypeError</code> <p>If obj is not a supported type.</p> <p>Examples:</p> <pre><code>point = Point(0.0, 0.0)\nbelow_of(point, 10)\n</code></pre> Source code in <code>src/momapy/positioning.py</code> <pre><code>def below_of(\n    obj: (Point | Bbox | Node | Builder),\n    distance: float,\n) -&gt; Point:\n    \"\"\"Return a point below the given object at the specified distance.\n\n    Args:\n        obj: A Point, Bbox, Node, or their builder variants.\n            For Nodes, uses the south anchor point.\n        distance: The vertical distance from the object's reference point.\n\n    Returns:\n        A new Point located below the object.\n\n    Raises:\n        TypeError: If obj is not a supported type.\n\n    Examples:\n        ```python\n        point = Point(0.0, 0.0)\n        below_of(point, 10)\n        ```\n    \"\"\"\n    if isinstance_or_builder(obj, Point):\n        source_point = obj\n    elif isinstance_or_builder(obj, (Bbox, Node)):\n        source_point = obj.south()\n    else:\n        raise TypeError(f\"{type(obj).__name__} not supported\")\n    return source_point + (0, distance)\n</code></pre>"},{"location":"api_reference/positioning/#momapy.positioning.below_right_of","title":"below_right_of","text":"<pre><code>below_right_of(obj: Point | Bbox | Node | Builder, distance_y: float, distance_x: float | None = None) -&gt; Point\n</code></pre> <p>Return a point below and to the right of the given object.</p> <p>Parameters:</p> Name Type Description Default <code>obj</code> <code>Point | Bbox | Node | Builder</code> <p>A Point, Bbox, Node, or their builder variants. For Nodes, uses the south-east anchor point.</p> required <code>distance_y</code> <code>float</code> <p>The vertical distance (southward) from the object.</p> required <code>distance_x</code> <code>float | None</code> <p>The horizontal distance (eastward) from the object. If None, defaults to distance_y.</p> <code>None</code> <p>Returns:</p> Type Description <code>Point</code> <p>A new Point located below and to the right of the object.</p> <p>Raises:</p> Type Description <code>TypeError</code> <p>If obj is not a supported type.</p> <p>Examples:</p> <pre><code>point = Point(0.0, 0.0)\nbelow_right_of(point, 5, 10)\n</code></pre> Source code in <code>src/momapy/positioning.py</code> <pre><code>def below_right_of(\n    obj: (Point | Bbox | Node | Builder),\n    distance_y: float,\n    distance_x: float | None = None,\n) -&gt; Point:\n    \"\"\"Return a point below and to the right of the given object.\n\n    Args:\n        obj: A Point, Bbox, Node, or their builder variants.\n            For Nodes, uses the south-east anchor point.\n        distance_y: The vertical distance (southward) from the object.\n        distance_x: The horizontal distance (eastward) from the object.\n            If None, defaults to distance_y.\n\n    Returns:\n        A new Point located below and to the right of the object.\n\n    Raises:\n        TypeError: If obj is not a supported type.\n\n    Examples:\n        ```python\n        point = Point(0.0, 0.0)\n        below_right_of(point, 5, 10)\n        ```\n    \"\"\"\n    if distance_x is None:\n        distance_x = distance_y\n    if isinstance_or_builder(obj, Point):\n        source_point = obj\n    elif isinstance_or_builder(obj, (Bbox, Node)):\n        source_point = obj.south_east()\n    else:\n        raise TypeError(f\"{type(obj).__name__} not supported\")\n    return source_point + (distance_x, distance_y)\n</code></pre>"},{"location":"api_reference/positioning/#momapy.positioning.cross_hv_of","title":"cross_hv_of","text":"<pre><code>cross_hv_of(obj1: Point | Bbox | Node | Builder, obj2: Point | Bbox | Node | Builder) -&gt; Point\n</code></pre> <p>Return the point at the horizontal cross of obj1 and vertical cross of obj2.</p> <p>Creates a point using obj1's y-coordinate and obj2's x-coordinate. Useful for creating elbow connector points.</p> <p>Parameters:</p> Name Type Description Default <code>obj1</code> <code>Point | Bbox | Node | Builder</code> <p>Object providing the y-coordinate (Point, Bbox, Node, or builder).</p> required <code>obj2</code> <code>Point | Bbox | Node | Builder</code> <p>Object providing the x-coordinate (Point, Bbox, Node, or builder). For Bboxes and Nodes, uses their center points.</p> required <p>Returns:</p> Type Description <code>Point</code> <p>A Point at (obj2.x, obj1.y).</p> <p>Examples:</p> <pre><code>p1 = Point(0.0, 5.0)\np2 = Point(10.0, 0.0)\ncross_hv_of(p1, p2)\n</code></pre> Source code in <code>src/momapy/positioning.py</code> <pre><code>def cross_hv_of(\n    obj1: (Point | Bbox | Node | Builder),\n    obj2: (Point | Bbox | Node | Builder),\n) -&gt; Point:\n    \"\"\"Return the point at the horizontal cross of obj1 and vertical cross of obj2.\n\n    Creates a point using obj1's y-coordinate and obj2's x-coordinate.\n    Useful for creating elbow connector points.\n\n    Args:\n        obj1: Object providing the y-coordinate (Point, Bbox, Node, or builder).\n        obj2: Object providing the x-coordinate (Point, Bbox, Node, or builder).\n            For Bboxes and Nodes, uses their center points.\n\n    Returns:\n        A Point at (obj2.x, obj1.y).\n\n    Examples:\n        ```python\n        p1 = Point(0.0, 5.0)\n        p2 = Point(10.0, 0.0)\n        cross_hv_of(p1, p2)\n        ```\n    \"\"\"\n    if isinstance_or_builder(obj1, (Bbox, Node)):\n        obj1 = obj1.center()\n    if isinstance_or_builder(obj2, (Bbox, Node)):\n        obj2 = obj2.center()\n    y = obj1.y\n    x = obj2.x\n    return Point(x, y)\n</code></pre>"},{"location":"api_reference/positioning/#momapy.positioning.cross_vh_of","title":"cross_vh_of","text":"<pre><code>cross_vh_of(obj1: Point | Bbox | Node | Builder, obj2: Point | Bbox | Node | Builder) -&gt; Point\n</code></pre> <p>Return the point at the vertical cross of obj1 and horizontal cross of obj2.</p> <p>Creates a point using obj1's x-coordinate and obj2's y-coordinate. Useful for creating elbow connector points.</p> <p>Parameters:</p> Name Type Description Default <code>obj1</code> <code>Point | Bbox | Node | Builder</code> <p>Object providing the x-coordinate (Point, Bbox, Node, or builder).</p> required <code>obj2</code> <code>Point | Bbox | Node | Builder</code> <p>Object providing the y-coordinate (Point, Bbox, Node, or builder). For Bboxes and Nodes, uses their center points.</p> required <p>Returns:</p> Type Description <code>Point</code> <p>A Point at (obj1.x, obj2.y).</p> <p>Examples:</p> <pre><code>p1 = Point(5.0, 0.0)\np2 = Point(0.0, 10.0)\ncross_vh_of(p1, p2)\n</code></pre> Source code in <code>src/momapy/positioning.py</code> <pre><code>def cross_vh_of(\n    obj1: (Point | Bbox | Node | Builder),\n    obj2: (Point | Bbox | Node | Builder),\n) -&gt; Point:\n    \"\"\"Return the point at the vertical cross of obj1 and horizontal cross of obj2.\n\n    Creates a point using obj1's x-coordinate and obj2's y-coordinate.\n    Useful for creating elbow connector points.\n\n    Args:\n        obj1: Object providing the x-coordinate (Point, Bbox, Node, or builder).\n        obj2: Object providing the y-coordinate (Point, Bbox, Node, or builder).\n            For Bboxes and Nodes, uses their center points.\n\n    Returns:\n        A Point at (obj1.x, obj2.y).\n\n    Examples:\n        ```python\n        p1 = Point(5.0, 0.0)\n        p2 = Point(0.0, 10.0)\n        cross_vh_of(p1, p2)\n        ```\n    \"\"\"\n    if isinstance_or_builder(obj1, (Bbox, Node)):\n        obj1 = obj1.center()\n    if isinstance_or_builder(obj2, (Bbox, Node)):\n        obj2 = obj2.center()\n    x = obj1.x\n    y = obj2.y\n    return Point(x, y)\n</code></pre>"},{"location":"api_reference/positioning/#momapy.positioning.fit","title":"fit","text":"<pre><code>fit(elements: Collection[LayoutElement | Bbox | Point | Builder], xsep: float = 0.0, ysep: float = 0.0) -&gt; Bbox\n</code></pre> <p>Compute a bounding box that fits the given elements with optional margins.</p> <p>Parameters:</p> Name Type Description Default <code>elements</code> <code>Collection[LayoutElement | Bbox | Point | Builder]</code> <p>A collection of Points, Bboxes, LayoutElements, or their builder variants to fit within the bounding box.</p> required <code>xsep</code> <code>float</code> <p>Horizontal margin to add to both sides of the bounding box.</p> <code>0.0</code> <code>ysep</code> <code>float</code> <p>Vertical margin to add to both sides of the bounding box.</p> <code>0.0</code> <p>Returns:</p> Type Description <code>Bbox</code> <p>A Bbox that tightly contains all elements plus the specified margins.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If elements is empty.</p> <code>TypeError</code> <p>If an element has an unsupported type.</p> <p>Examples:</p> <pre><code>points = [Point(0.0, 0.0), Point(10.0, 10.0)]\nbbox = fit(points, xsep=2, ysep=2)\nprint(bbox.position)\n</code></pre> Source code in <code>src/momapy/positioning.py</code> <pre><code>def fit(\n    elements: collections.abc.Collection[LayoutElement | Bbox | Point | Builder],\n    xsep: float = 0.0,\n    ysep: float = 0.0,\n) -&gt; Bbox:\n    \"\"\"Compute a bounding box that fits the given elements with optional margins.\n\n    Args:\n        elements: A collection of Points, Bboxes, LayoutElements, or their\n            builder variants to fit within the bounding box.\n        xsep: Horizontal margin to add to both sides of the bounding box.\n        ysep: Vertical margin to add to both sides of the bounding box.\n\n    Returns:\n        A Bbox that tightly contains all elements plus the specified margins.\n\n    Raises:\n        ValueError: If elements is empty.\n        TypeError: If an element has an unsupported type.\n\n    Examples:\n        ```python\n        points = [Point(0.0, 0.0), Point(10.0, 10.0)]\n        bbox = fit(points, xsep=2, ysep=2)\n        print(bbox.position)\n        ```\n    \"\"\"\n    if not elements:\n        raise ValueError(\"elements must contain at least one element\")\n    bboxes = []\n    for element in elements:\n        if isinstance_or_builder(element, Point):\n            bboxes.append(Bbox.around_points([element]))\n        elif isinstance_or_builder(element, Bbox):\n            bboxes.append(element)\n        elif isinstance_or_builder(element, LayoutElement):\n            bboxes.append(element.bbox())\n        else:\n            raise TypeError(f\"{type(element)} not supported\")\n    bbox = Bbox.union(bboxes)\n    if xsep or ysep:\n        bbox = Bbox(\n            bbox.position,\n            bbox.width + 2 * xsep,\n            bbox.height + 2 * ysep,\n        )\n    return bbox\n</code></pre>"},{"location":"api_reference/positioning/#momapy.positioning.fraction_of","title":"fraction_of","text":"<pre><code>fraction_of(arc_layout_element: SingleHeadedArc | DoubleHeadedArc, fraction: float) -&gt; tuple[Point, float]\n</code></pre> <p>Return the position and angle at a given fraction along an arc.</p> <p>Parameters:</p> Name Type Description Default <code>arc_layout_element</code> <code>SingleHeadedArc | DoubleHeadedArc</code> <p>An arc layout element (SingleHeadedArc or DoubleHeadedArc).</p> required <code>fraction</code> <code>float</code> <p>A value between 0.0 and 1.0 representing the position along the arc.</p> required <p>Returns:</p> Type Description <code>tuple[Point, float]</code> <p>A tuple containing: - The Point at the specified fraction along the arc. - The angle (in radians) of the tangent at that position.</p> <p>Examples:</p> <pre><code>arc = some_single_headed_arc\npos, angle = fraction_of(arc, 0.5)  # Midpoint of arc\n</code></pre> Source code in <code>src/momapy/positioning.py</code> <pre><code>def fraction_of(\n    arc_layout_element: (SingleHeadedArc | DoubleHeadedArc),\n    fraction: float,\n) -&gt; tuple[Point, float]:\n    \"\"\"Return the position and angle at a given fraction along an arc.\n\n    Args:\n        arc_layout_element: An arc layout element (SingleHeadedArc or DoubleHeadedArc).\n        fraction: A value between 0.0 and 1.0 representing the position along the arc.\n\n    Returns:\n        A tuple containing:\n            - The Point at the specified fraction along the arc.\n            - The angle (in radians) of the tangent at that position.\n\n    Examples:\n        ```python\n        arc = some_single_headed_arc\n        pos, angle = fraction_of(arc, 0.5)  # Midpoint of arc\n        ```\n    \"\"\"\n    position, angle = arc_layout_element.fraction(fraction)\n    return position, angle\n</code></pre>"},{"location":"api_reference/positioning/#momapy.positioning.left_of","title":"left_of","text":"<pre><code>left_of(obj: Point | Bbox | Node | Builder, distance: float) -&gt; Point\n</code></pre> <p>Return a point to the left of the given object at the specified distance.</p> <p>Parameters:</p> Name Type Description Default <code>obj</code> <code>Point | Bbox | Node | Builder</code> <p>A Point, Bbox, Node, or their builder variants. For Nodes, uses the west anchor point.</p> required <code>distance</code> <code>float</code> <p>The horizontal distance from the object's reference point.</p> required <p>Returns:</p> Type Description <code>Point</code> <p>A new Point located to the left of the object.</p> <p>Raises:</p> Type Description <code>TypeError</code> <p>If obj is not a supported type.</p> <p>Examples:</p> <pre><code>point = Point(10.0, 0.0)\nleft_of(point, 5)\n</code></pre> Source code in <code>src/momapy/positioning.py</code> <pre><code>def left_of(\n    obj: (Point | Bbox | Node | Builder),\n    distance: float,\n) -&gt; Point:\n    \"\"\"Return a point to the left of the given object at the specified distance.\n\n    Args:\n        obj: A Point, Bbox, Node, or their builder variants.\n            For Nodes, uses the west anchor point.\n        distance: The horizontal distance from the object's reference point.\n\n    Returns:\n        A new Point located to the left of the object.\n\n    Raises:\n        TypeError: If obj is not a supported type.\n\n    Examples:\n        ```python\n        point = Point(10.0, 0.0)\n        left_of(point, 5)\n        ```\n    \"\"\"\n    if isinstance_or_builder(obj, Point):\n        source_point = obj\n    elif isinstance_or_builder(obj, (Bbox, Node)):\n        source_point = obj.west()\n    else:\n        raise TypeError(f\"{type(obj).__name__} not supported\")\n    return source_point - (distance, 0)\n</code></pre>"},{"location":"api_reference/positioning/#momapy.positioning.mid_of","title":"mid_of","text":"<pre><code>mid_of(obj1: Point | Bbox | Node | Builder, obj2: Point | Bbox | Node | Builder) -&gt; Point\n</code></pre> <p>Return the midpoint between two objects.</p> <p>Parameters:</p> Name Type Description Default <code>obj1</code> <code>Point | Bbox | Node | Builder</code> <p>First object (Point, Bbox, Node, or builder).</p> required <code>obj2</code> <code>Point | Bbox | Node | Builder</code> <p>Second object (Point, Bbox, Node, or builder). For Bboxes and Nodes, uses their center points.</p> required <p>Returns:</p> Type Description <code>Point</code> <p>A Point at the midpoint between the two objects.</p> <p>Examples:</p> <pre><code>p1 = Point(0.0, 0.0)\np2 = Point(10.0, 10.0)\nmid_of(p1, p2)\n</code></pre> Source code in <code>src/momapy/positioning.py</code> <pre><code>def mid_of(\n    obj1: (Point | Bbox | Node | Builder),\n    obj2: (Point | Bbox | Node | Builder),\n) -&gt; Point:\n    \"\"\"Return the midpoint between two objects.\n\n    Args:\n        obj1: First object (Point, Bbox, Node, or builder).\n        obj2: Second object (Point, Bbox, Node, or builder).\n            For Bboxes and Nodes, uses their center points.\n\n    Returns:\n        A Point at the midpoint between the two objects.\n\n    Examples:\n        ```python\n        p1 = Point(0.0, 0.0)\n        p2 = Point(10.0, 10.0)\n        mid_of(p1, p2)\n        ```\n    \"\"\"\n    if isinstance_or_builder(obj1, (Bbox, Node)):\n        obj1 = obj1.center()\n    if isinstance_or_builder(obj2, (Bbox, Node)):\n        obj2 = obj2.center()\n    segment = Segment(obj1, obj2)\n    return segment.get_position_at_fraction(0.5)\n</code></pre>"},{"location":"api_reference/positioning/#momapy.positioning.right_of","title":"right_of","text":"<pre><code>right_of(obj: Point | Bbox | Node | Builder, distance: float) -&gt; Point\n</code></pre> <p>Return a point to the right of the given object at the specified distance.</p> <p>Parameters:</p> Name Type Description Default <code>obj</code> <code>Point | Bbox | Node | Builder</code> <p>A Point, Bbox, Node, or their builder variants. For Nodes, uses the east anchor point.</p> required <code>distance</code> <code>float</code> <p>The horizontal distance from the object's reference point.</p> required <p>Returns:</p> Type Description <code>Point</code> <p>A new Point located to the right of the object.</p> <p>Raises:</p> Type Description <code>TypeError</code> <p>If obj is not a supported type.</p> <p>Examples:</p> <pre><code>point = Point(0.0, 0.0)\nright_of(point, 10)\n</code></pre> Source code in <code>src/momapy/positioning.py</code> <pre><code>def right_of(\n    obj: (Point | Bbox | Node | Builder),\n    distance: float,\n) -&gt; Point:\n    \"\"\"Return a point to the right of the given object at the specified distance.\n\n    Args:\n        obj: A Point, Bbox, Node, or their builder variants.\n            For Nodes, uses the east anchor point.\n        distance: The horizontal distance from the object's reference point.\n\n    Returns:\n        A new Point located to the right of the object.\n\n    Raises:\n        TypeError: If obj is not a supported type.\n\n    Examples:\n        ```python\n        point = Point(0.0, 0.0)\n        right_of(point, 10)\n        ```\n    \"\"\"\n    if isinstance_or_builder(obj, Point):\n        source_point = obj\n    elif isinstance_or_builder(obj, (Bbox, Node)):\n        source_point = obj.east()\n    else:\n        raise TypeError(f\"{type(obj).__name__} not supported\")\n    return source_point + (distance, 0)\n</code></pre>"},{"location":"api_reference/positioning/#momapy.positioning.set_above_left_of","title":"set_above_left_of","text":"<pre><code>set_above_left_of(obj1: Builder, obj2: Point | Bbox | Node | Builder, distance_y: float, distance_x: float | None = None, anchor: str | None = None) -&gt; None\n</code></pre> <p>Set obj1's position above and to the left of obj2.</p> <p>Parameters:</p> Name Type Description Default <code>obj1</code> <code>Builder</code> <p>The builder object to position.</p> required <code>obj2</code> <code>Point | Bbox | Node | Builder</code> <p>The reference object to position relative to.</p> required <code>distance_y</code> <code>float</code> <p>The vertical distance from obj2.</p> required <code>distance_x</code> <code>float | None</code> <p>The horizontal distance from obj2. Defaults to distance_y if None.</p> <code>None</code> <code>anchor</code> <code>str | None</code> <p>Optional anchor name on obj1 to align with the calculated position.</p> <code>None</code> Source code in <code>src/momapy/positioning.py</code> <pre><code>def set_above_left_of(\n    obj1: Builder,\n    obj2: (Point | Bbox | Node | Builder),\n    distance_y: float,\n    distance_x: float | None = None,\n    anchor: str | None = None,\n) -&gt; None:\n    \"\"\"Set obj1's position above and to the left of obj2.\n\n    Args:\n        obj1: The builder object to position.\n        obj2: The reference object to position relative to.\n        distance_y: The vertical distance from obj2.\n        distance_x: The horizontal distance from obj2. Defaults to distance_y if None.\n        anchor: Optional anchor name on obj1 to align with the calculated position.\n    \"\"\"\n    position = above_left_of(obj2, distance_y, distance_x)\n    set_position(obj1, position, anchor)\n</code></pre>"},{"location":"api_reference/positioning/#momapy.positioning.set_above_of","title":"set_above_of","text":"<pre><code>set_above_of(obj1: Builder, obj2: Point | Bbox | Node | Builder, distance: float, anchor: str | None = None) -&gt; None\n</code></pre> <p>Set obj1's position above obj2 at the specified distance.</p> <p>Parameters:</p> Name Type Description Default <code>obj1</code> <code>Builder</code> <p>The builder object to position.</p> required <code>obj2</code> <code>Point | Bbox | Node | Builder</code> <p>The reference object to position relative to.</p> required <code>distance</code> <code>float</code> <p>The vertical distance from obj2.</p> required <code>anchor</code> <code>str | None</code> <p>Optional anchor name on obj1 to align with the calculated position.</p> <code>None</code> Source code in <code>src/momapy/positioning.py</code> <pre><code>def set_above_of(\n    obj1: Builder,\n    obj2: (Point | Bbox | Node | Builder),\n    distance: float,\n    anchor: str | None = None,\n) -&gt; None:\n    \"\"\"Set obj1's position above obj2 at the specified distance.\n\n    Args:\n        obj1: The builder object to position.\n        obj2: The reference object to position relative to.\n        distance: The vertical distance from obj2.\n        anchor: Optional anchor name on obj1 to align with the calculated position.\n    \"\"\"\n    position = above_of(obj2, distance)\n    set_position(obj1, position, anchor)\n</code></pre>"},{"location":"api_reference/positioning/#momapy.positioning.set_above_right_of","title":"set_above_right_of","text":"<pre><code>set_above_right_of(obj1: Builder, obj2: Point | Bbox | Node | Builder, distance_y: float, distance_x: float | None = None, anchor: str | None = None) -&gt; None\n</code></pre> <p>Set obj1's position above and to the right of obj2.</p> <p>Parameters:</p> Name Type Description Default <code>obj1</code> <code>Builder</code> <p>The builder object to position.</p> required <code>obj2</code> <code>Point | Bbox | Node | Builder</code> <p>The reference object to position relative to.</p> required <code>distance_y</code> <code>float</code> <p>The vertical distance from obj2.</p> required <code>distance_x</code> <code>float | None</code> <p>The horizontal distance from obj2. Defaults to distance_y if None.</p> <code>None</code> <code>anchor</code> <code>str | None</code> <p>Optional anchor name on obj1 to align with the calculated position.</p> <code>None</code> Source code in <code>src/momapy/positioning.py</code> <pre><code>def set_above_right_of(\n    obj1: Builder,\n    obj2: (Point | Bbox | Node | Builder),\n    distance_y: float,\n    distance_x: float | None = None,\n    anchor: str | None = None,\n) -&gt; None:\n    \"\"\"Set obj1's position above and to the right of obj2.\n\n    Args:\n        obj1: The builder object to position.\n        obj2: The reference object to position relative to.\n        distance_y: The vertical distance from obj2.\n        distance_x: The horizontal distance from obj2. Defaults to distance_y if None.\n        anchor: Optional anchor name on obj1 to align with the calculated position.\n    \"\"\"\n    position = above_right_of(obj2, distance_y, distance_x)\n    set_position(obj1, position, anchor)\n</code></pre>"},{"location":"api_reference/positioning/#momapy.positioning.set_below_left_of","title":"set_below_left_of","text":"<pre><code>set_below_left_of(obj1: Builder, obj2: Point | Bbox | Node | Builder, distance_y: float, distance_x: float | None = None, anchor: str | None = None) -&gt; None\n</code></pre> <p>Set obj1's position below and to the left of obj2.</p> <p>Parameters:</p> Name Type Description Default <code>obj1</code> <code>Builder</code> <p>The builder object to position.</p> required <code>obj2</code> <code>Point | Bbox | Node | Builder</code> <p>The reference object to position relative to.</p> required <code>distance_y</code> <code>float</code> <p>The vertical distance from obj2.</p> required <code>distance_x</code> <code>float | None</code> <p>The horizontal distance from obj2. Defaults to distance_y if None.</p> <code>None</code> <code>anchor</code> <code>str | None</code> <p>Optional anchor name on obj1 to align with the calculated position.</p> <code>None</code> Source code in <code>src/momapy/positioning.py</code> <pre><code>def set_below_left_of(\n    obj1: Builder,\n    obj2: (Point | Bbox | Node | Builder),\n    distance_y: float,\n    distance_x: float | None = None,\n    anchor: str | None = None,\n) -&gt; None:\n    \"\"\"Set obj1's position below and to the left of obj2.\n\n    Args:\n        obj1: The builder object to position.\n        obj2: The reference object to position relative to.\n        distance_y: The vertical distance from obj2.\n        distance_x: The horizontal distance from obj2. Defaults to distance_y if None.\n        anchor: Optional anchor name on obj1 to align with the calculated position.\n    \"\"\"\n    position = below_left_of(obj2, distance_y, distance_x)\n    set_position(obj1, position, anchor)\n</code></pre>"},{"location":"api_reference/positioning/#momapy.positioning.set_below_of","title":"set_below_of","text":"<pre><code>set_below_of(obj1: Builder, obj2: Point | Bbox | Node | Builder, distance: float, anchor: str | None = None) -&gt; None\n</code></pre> <p>Set obj1's position below obj2 at the specified distance.</p> <p>Parameters:</p> Name Type Description Default <code>obj1</code> <code>Builder</code> <p>The builder object to position.</p> required <code>obj2</code> <code>Point | Bbox | Node | Builder</code> <p>The reference object to position relative to.</p> required <code>distance</code> <code>float</code> <p>The vertical distance from obj2.</p> required <code>anchor</code> <code>str | None</code> <p>Optional anchor name on obj1 to align with the calculated position.</p> <code>None</code> Source code in <code>src/momapy/positioning.py</code> <pre><code>def set_below_of(\n    obj1: Builder,\n    obj2: (Point | Bbox | Node | Builder),\n    distance: float,\n    anchor: str | None = None,\n) -&gt; None:\n    \"\"\"Set obj1's position below obj2 at the specified distance.\n\n    Args:\n        obj1: The builder object to position.\n        obj2: The reference object to position relative to.\n        distance: The vertical distance from obj2.\n        anchor: Optional anchor name on obj1 to align with the calculated position.\n    \"\"\"\n    position = below_of(obj2, distance)\n    set_position(obj1, position, anchor)\n</code></pre>"},{"location":"api_reference/positioning/#momapy.positioning.set_below_right_of","title":"set_below_right_of","text":"<pre><code>set_below_right_of(obj1: Builder, obj2: Point | Bbox | Node | Builder, distance_y: float, distance_x: float | None = None, anchor: str | None = None) -&gt; None\n</code></pre> <p>Set obj1's position below and to the right of obj2.</p> <p>Parameters:</p> Name Type Description Default <code>obj1</code> <code>Builder</code> <p>The builder object to position.</p> required <code>obj2</code> <code>Point | Bbox | Node | Builder</code> <p>The reference object to position relative to.</p> required <code>distance_y</code> <code>float</code> <p>The vertical distance from obj2.</p> required <code>distance_x</code> <code>float | None</code> <p>The horizontal distance from obj2. Defaults to distance_y if None.</p> <code>None</code> <code>anchor</code> <code>str | None</code> <p>Optional anchor name on obj1 to align with the calculated position.</p> <code>None</code> Source code in <code>src/momapy/positioning.py</code> <pre><code>def set_below_right_of(\n    obj1: Builder,\n    obj2: (Point | Bbox | Node | Builder),\n    distance_y: float,\n    distance_x: float | None = None,\n    anchor: str | None = None,\n) -&gt; None:\n    \"\"\"Set obj1's position below and to the right of obj2.\n\n    Args:\n        obj1: The builder object to position.\n        obj2: The reference object to position relative to.\n        distance_y: The vertical distance from obj2.\n        distance_x: The horizontal distance from obj2. Defaults to distance_y if None.\n        anchor: Optional anchor name on obj1 to align with the calculated position.\n    \"\"\"\n    position = below_right_of(obj2, distance_y, distance_x)\n    set_position(obj1, position, anchor)\n</code></pre>"},{"location":"api_reference/positioning/#momapy.positioning.set_cross_hv_of","title":"set_cross_hv_of","text":"<pre><code>set_cross_hv_of(obj1: Builder, obj2: Point | Bbox | Node | Builder, obj3: Point | Bbox | Node | Builder, anchor: str | None = None) -&gt; None\n</code></pre> <p>Set obj1's position at the horizontal-vertical cross of obj2 and obj3.</p> <p>Creates a point using obj2's y-coordinate and obj3's x-coordinate.</p> <p>Parameters:</p> Name Type Description Default <code>obj1</code> <code>Builder</code> <p>The builder object to position.</p> required <code>obj2</code> <code>Point | Bbox | Node | Builder</code> <p>Object providing the y-coordinate.</p> required <code>obj3</code> <code>Point | Bbox | Node | Builder</code> <p>Object providing the x-coordinate.</p> required <code>anchor</code> <code>str | None</code> <p>Optional anchor name on obj1 to align with the calculated position.</p> <code>None</code> Source code in <code>src/momapy/positioning.py</code> <pre><code>def set_cross_hv_of(\n    obj1: Builder,\n    obj2: (Point | Bbox | Node | Builder),\n    obj3: (Point | Bbox | Node | Builder),\n    anchor: str | None = None,\n) -&gt; None:\n    \"\"\"Set obj1's position at the horizontal-vertical cross of obj2 and obj3.\n\n    Creates a point using obj2's y-coordinate and obj3's x-coordinate.\n\n    Args:\n        obj1: The builder object to position.\n        obj2: Object providing the y-coordinate.\n        obj3: Object providing the x-coordinate.\n        anchor: Optional anchor name on obj1 to align with the calculated position.\n    \"\"\"\n    position = cross_hv_of(obj2, obj3)\n    set_position(obj1, position, anchor)\n</code></pre>"},{"location":"api_reference/positioning/#momapy.positioning.set_cross_vh_of","title":"set_cross_vh_of","text":"<pre><code>set_cross_vh_of(obj1: Builder, obj2: Point | Bbox | Node | Builder, obj3: Point | Bbox | Node | Builder, anchor: str | None = None) -&gt; None\n</code></pre> <p>Set obj1's position at the vertical-horizontal cross of obj2 and obj3.</p> <p>Creates a point using obj2's x-coordinate and obj3's y-coordinate.</p> <p>Parameters:</p> Name Type Description Default <code>obj1</code> <code>Builder</code> <p>The builder object to position.</p> required <code>obj2</code> <code>Point | Bbox | Node | Builder</code> <p>Object providing the x-coordinate.</p> required <code>obj3</code> <code>Point | Bbox | Node | Builder</code> <p>Object providing the y-coordinate.</p> required <code>anchor</code> <code>str | None</code> <p>Optional anchor name on obj1 to align with the calculated position.</p> <code>None</code> Source code in <code>src/momapy/positioning.py</code> <pre><code>def set_cross_vh_of(\n    obj1: Builder,\n    obj2: (Point | Bbox | Node | Builder),\n    obj3: (Point | Bbox | Node | Builder),\n    anchor: str | None = None,\n) -&gt; None:\n    \"\"\"Set obj1's position at the vertical-horizontal cross of obj2 and obj3.\n\n    Creates a point using obj2's x-coordinate and obj3's y-coordinate.\n\n    Args:\n        obj1: The builder object to position.\n        obj2: Object providing the x-coordinate.\n        obj3: Object providing the y-coordinate.\n        anchor: Optional anchor name on obj1 to align with the calculated position.\n    \"\"\"\n    position = cross_vh_of(obj2, obj3)\n    set_position(obj1, position, anchor)\n</code></pre>"},{"location":"api_reference/positioning/#momapy.positioning.set_fit","title":"set_fit","text":"<pre><code>set_fit(obj: Builder, elements: Collection[Point | Bbox | LayoutElement | Builder], xsep: float = 0.0, ysep: float = 0.0, anchor: str | None = None) -&gt; None\n</code></pre> <p>Set obj's dimensions and position to fit the given elements.</p> <p>Parameters:</p> Name Type Description Default <code>obj</code> <code>Builder</code> <p>The builder object to resize and position.</p> required <code>elements</code> <code>Collection[Point | Bbox | LayoutElement | Builder]</code> <p>Collection of elements to fit within obj.</p> required <code>xsep</code> <code>float</code> <p>Horizontal margin around the fitted elements.</p> <code>0.0</code> <code>ysep</code> <code>float</code> <p>Vertical margin around the fitted elements.</p> <code>0.0</code> <code>anchor</code> <code>str | None</code> <p>Optional anchor name on obj to align with the calculated position.</p> <code>None</code> Source code in <code>src/momapy/positioning.py</code> <pre><code>def set_fit(\n    obj: Builder,\n    elements: collections.abc.Collection[Point | Bbox | LayoutElement | Builder],\n    xsep: float = 0.0,\n    ysep: float = 0.0,\n    anchor: str | None = None,\n) -&gt; None:\n    \"\"\"Set obj's dimensions and position to fit the given elements.\n\n    Args:\n        obj: The builder object to resize and position.\n        elements: Collection of elements to fit within obj.\n        xsep: Horizontal margin around the fitted elements.\n        ysep: Vertical margin around the fitted elements.\n        anchor: Optional anchor name on obj to align with the calculated position.\n    \"\"\"\n    bbox = fit(elements, xsep, ysep)\n    obj.width = bbox.width\n    obj.height = bbox.height\n    set_position(obj, bbox.position, anchor)\n</code></pre>"},{"location":"api_reference/positioning/#momapy.positioning.set_fraction_of","title":"set_fraction_of","text":"<pre><code>set_fraction_of(obj: Builder, arc_layout_element: SingleHeadedArc | DoubleHeadedArc, fraction: float, anchor: str | None = None) -&gt; None\n</code></pre> <p>Set obj's position and rotation along an arc at the given fraction.</p> <p>Parameters:</p> Name Type Description Default <code>obj</code> <code>Builder</code> <p>The node builder to position and rotate.</p> required <code>arc_layout_element</code> <code>SingleHeadedArc | DoubleHeadedArc</code> <p>The arc to position along.</p> required <code>fraction</code> <code>float</code> <p>Position along the arc (0.0 to 1.0).</p> required <code>anchor</code> <code>str | None</code> <p>Optional anchor name on obj to align with the calculated position.</p> <code>None</code> Source code in <code>src/momapy/positioning.py</code> <pre><code>def set_fraction_of(\n    obj: Builder,\n    arc_layout_element: (SingleHeadedArc | DoubleHeadedArc),\n    fraction: float,\n    anchor: str | None = None,\n) -&gt; None:\n    \"\"\"Set obj's position and rotation along an arc at the given fraction.\n\n    Args:\n        obj: The node builder to position and rotate.\n        arc_layout_element: The arc to position along.\n        fraction: Position along the arc (0.0 to 1.0).\n        anchor: Optional anchor name on obj to align with the calculated position.\n    \"\"\"\n    position, angle = fraction_of(arc_layout_element, fraction)\n    rotation = Rotation(angle, position)\n    set_position(obj, position, anchor)\n    obj.transform = [rotation]\n</code></pre>"},{"location":"api_reference/positioning/#momapy.positioning.set_left_of","title":"set_left_of","text":"<pre><code>set_left_of(obj1: Builder, obj2: Point | Bbox | Node | Builder, distance: float, anchor: str | None = None) -&gt; None\n</code></pre> <p>Set obj1's position to the left of obj2 at the specified distance.</p> <p>Parameters:</p> Name Type Description Default <code>obj1</code> <code>Builder</code> <p>The builder object to position.</p> required <code>obj2</code> <code>Point | Bbox | Node | Builder</code> <p>The reference object to position relative to.</p> required <code>distance</code> <code>float</code> <p>The horizontal distance from obj2.</p> required <code>anchor</code> <code>str | None</code> <p>Optional anchor name on obj1 to align with the calculated position.</p> <code>None</code> Source code in <code>src/momapy/positioning.py</code> <pre><code>def set_left_of(\n    obj1: Builder,\n    obj2: (Point | Bbox | Node | Builder),\n    distance: float,\n    anchor: str | None = None,\n) -&gt; None:\n    \"\"\"Set obj1's position to the left of obj2 at the specified distance.\n\n    Args:\n        obj1: The builder object to position.\n        obj2: The reference object to position relative to.\n        distance: The horizontal distance from obj2.\n        anchor: Optional anchor name on obj1 to align with the calculated position.\n    \"\"\"\n    position = left_of(obj2, distance)\n    set_position(obj1, position, anchor)\n</code></pre>"},{"location":"api_reference/positioning/#momapy.positioning.set_mid_of","title":"set_mid_of","text":"<pre><code>set_mid_of(obj1: Builder, obj2: Point | Bbox | Node | Builder, obj3: Point | Bbox | Node | Builder, anchor: str | None = None) -&gt; None\n</code></pre> <p>Set obj1's position to the midpoint between obj2 and obj3.</p> <p>Parameters:</p> Name Type Description Default <code>obj1</code> <code>Builder</code> <p>The builder object to position.</p> required <code>obj2</code> <code>Point | Bbox | Node | Builder</code> <p>First reference object.</p> required <code>obj3</code> <code>Point | Bbox | Node | Builder</code> <p>Second reference object.</p> required <code>anchor</code> <code>str | None</code> <p>Optional anchor name on obj1 to align with the calculated position.</p> <code>None</code> Source code in <code>src/momapy/positioning.py</code> <pre><code>def set_mid_of(\n    obj1: Builder,\n    obj2: (Point | Bbox | Node | Builder),\n    obj3: (Point | Bbox | Node | Builder),\n    anchor: str | None = None,\n) -&gt; None:\n    \"\"\"Set obj1's position to the midpoint between obj2 and obj3.\n\n    Args:\n        obj1: The builder object to position.\n        obj2: First reference object.\n        obj3: Second reference object.\n        anchor: Optional anchor name on obj1 to align with the calculated position.\n    \"\"\"\n    position = mid_of(obj2, obj3)\n    set_position(obj1, position, anchor)\n</code></pre>"},{"location":"api_reference/positioning/#momapy.positioning.set_position","title":"set_position","text":"<pre><code>set_position(obj: Builder, position: Point | Builder, anchor: str | None = None) -&gt; None\n</code></pre> <p>Set the position of a builder object.</p> <p>Parameters:</p> Name Type Description Default <code>obj</code> <code>Builder</code> <p>The builder object to position (a Node or Bbox builder).</p> required <code>position</code> <code>Point | Builder</code> <p>The target position as a Point or Point builder.</p> required <code>anchor</code> <code>str | None</code> <p>Optional anchor name to align with the position. If specified, the object is shifted so that its anchor point aligns with the given position.</p> <code>None</code> <p>Examples:</p> <pre><code>builder = get_or_make_builder_cls(MyNode)()\nset_position(builder, Point(100.0, 100.0), anchor=\"center\")\n</code></pre> Source code in <code>src/momapy/positioning.py</code> <pre><code>def set_position(\n    obj: Builder,\n    position: Point | Builder,\n    anchor: str | None = None,\n) -&gt; None:\n    \"\"\"Set the position of a builder object.\n\n    Args:\n        obj: The builder object to position (a Node or Bbox builder).\n        position: The target position as a Point or Point builder.\n        anchor: Optional anchor name to align with the position.\n            If specified, the object is shifted so that its anchor point\n            aligns with the given position.\n\n    Examples:\n        ```python\n        builder = get_or_make_builder_cls(MyNode)()\n        set_position(builder, Point(100.0, 100.0), anchor=\"center\")\n        ```\n    \"\"\"\n    obj.position = position\n    if anchor is not None:\n        p = getattr(obj, anchor)()\n        obj.position += obj.position - p\n</code></pre>"},{"location":"api_reference/positioning/#momapy.positioning.set_right_of","title":"set_right_of","text":"<pre><code>set_right_of(obj1: Builder, obj2: Point | Bbox | Node | Builder, distance: float, anchor: str | None = None) -&gt; None\n</code></pre> <p>Set obj1's position to the right of obj2 at the specified distance.</p> <p>Parameters:</p> Name Type Description Default <code>obj1</code> <code>Builder</code> <p>The builder object to position.</p> required <code>obj2</code> <code>Point | Bbox | Node | Builder</code> <p>The reference object to position relative to.</p> required <code>distance</code> <code>float</code> <p>The horizontal distance from obj2.</p> required <code>anchor</code> <code>str | None</code> <p>Optional anchor name on obj1 to align with the calculated position.</p> <code>None</code> Source code in <code>src/momapy/positioning.py</code> <pre><code>def set_right_of(\n    obj1: Builder,\n    obj2: (Point | Bbox | Node | Builder),\n    distance: float,\n    anchor: str | None = None,\n) -&gt; None:\n    \"\"\"Set obj1's position to the right of obj2 at the specified distance.\n\n    Args:\n        obj1: The builder object to position.\n        obj2: The reference object to position relative to.\n        distance: The horizontal distance from obj2.\n        anchor: Optional anchor name on obj1 to align with the calculated position.\n    \"\"\"\n    position = right_of(obj2, distance)\n    set_position(obj1, position, anchor)\n</code></pre>"},{"location":"api_reference/styling/","title":"Styling","text":""},{"location":"api_reference/styling/#momapy.styling","title":"momapy.styling","text":"<p>Classes and functions for styling layout elements using style sheets.</p> <p>This subpackage provides a CSS-like styling system for layout elements, supporting:</p> <ul> <li>Style sheets with selectors (type, class, id, child, descendant, compound)</li> <li>Style collections that group related style properties</li> <li>CSS parsing from files or strings</li> <li>Style application to layout elements</li> </ul> <p>Examples:</p> <pre><code>from momapy.styling import StyleSheet, apply_style_sheet\n\nstylesheet = StyleSheet.from_file(\"styles.css\")\nstyled_element = apply_style_sheet(element, stylesheet)\n</code></pre> <p>Modules:</p> Name Description <code>core</code> <p>Implementation of the CSS-like styling system.</p> <p>Classes:</p> Name Description <code>ChildSelector</code> <p>Selector that matches elements that are direct children of a parent.</p> <code>ClassSelector</code> <p>Selector that matches elements by class (including subclasses).</p> <code>CompoundSelector</code> <p>Selector that matches only if all of its component selectors match (AND logic).</p> <code>DescendantSelector</code> <p>Selector that matches elements that are descendants of an ancestor.</p> <code>IdSelector</code> <p>Selector that matches elements by their id attribute.</p> <code>NotSelector</code> <p>Selector that matches if none of its component selectors match (NOT logic).</p> <code>OrSelector</code> <p>Selector that matches if any of its component selectors match (OR logic).</p> <code>Selector</code> <p>Abstract base class for CSS-like selectors.</p> <code>StyleCollection</code> <p>A dictionary-based collection of style properties.</p> <code>StyleSheet</code> <p>A dictionary-based stylesheet mapping selectors to style collections.</p> <code>TypeSelector</code> <p>Selector that matches elements by their exact class name.</p> <p>Functions:</p> Name Description <code>apply_style_collection</code> <p>Apply a StyleCollection to a layout element.</p> <code>apply_style_sheet</code> <p>Apply a StyleSheet to a layout element or map layout recursively.</p> <code>combine_style_sheets</code> <p>Merge multiple StyleSheets into a single StyleSheet.</p> <code>get_stylable_attributes</code> <p>Return the CSS-style names of stylable attributes of a layout element.</p>"},{"location":"api_reference/styling/#momapy.styling.ChildSelector","title":"ChildSelector  <code>dataclass</code>","text":"<pre><code>ChildSelector(parent_selector: Selector, child_selector: Selector)\n</code></pre> <p>               Bases: <code>Selector</code></p> <p>Selector that matches elements that are direct children of a parent.</p> <p>Examples:</p> <pre><code>selector = ChildSelector(TypeSelector(\"Group\"), TypeSelector(\"Rectangle\"))\nselector.select(rect, [group])  # True if rect is direct child of group\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>parent_selector</code> <code>Selector</code> <p>The parent selector</p> required <code>child_selector</code> <code>Selector</code> <p>The child selector</p> required <p>Methods:</p> Name Description <code>select</code> <p>Check if the object is a direct child matching the criteria.</p>"},{"location":"api_reference/styling/#momapy.styling.ChildSelector.select","title":"select","text":"<pre><code>select(obj: LayoutElement | Builder, ancestors: Collection[LayoutElement | Builder]) -&gt; bool\n</code></pre> <p>Check if the object is a direct child matching the criteria.</p> <p>Parameters:</p> Name Type Description Default <code>obj</code> <code>LayoutElement | Builder</code> <p>The layout element or builder to test.</p> required <code>ancestors</code> <code>Collection[LayoutElement | Builder]</code> <p>List of ancestor elements.</p> required <p>Returns:</p> Type Description <code>bool</code> <p>True if the object matches child_selector and its immediate</p> <code>bool</code> <p>parent matches parent_selector.</p> Source code in <code>src/momapy/styling/core.py</code> <pre><code>def select(\n    self,\n    obj: LayoutElement | Builder,\n    ancestors: collections.abc.Collection[LayoutElement | Builder],\n) -&gt; bool:\n    \"\"\"Check if the object is a direct child matching the criteria.\n\n    Args:\n        obj: The layout element or builder to test.\n        ancestors: List of ancestor elements.\n\n    Returns:\n        True if the object matches child_selector and its immediate\n        parent matches parent_selector.\n    \"\"\"\n    if not ancestors:\n        return False\n    return self.child_selector.select(\n        obj, ancestors\n    ) and self.parent_selector.select(ancestors[-1], ancestors[:-1])\n</code></pre>"},{"location":"api_reference/styling/#momapy.styling.ClassSelector","title":"ClassSelector  <code>dataclass</code>","text":"<pre><code>ClassSelector(class_name: str)\n</code></pre> <p>               Bases: <code>Selector</code></p> <p>Selector that matches elements by class (including subclasses).</p> <p>Examples:</p> <pre><code>selector = ClassSelector(\"Shape\")\nselector.select(some_rectangle, [])  # True if Rectangle is a Shape subclass\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>class_name</code> <code>str</code> <p>The name of the class</p> required <p>Methods:</p> Name Description <code>select</code> <p>Check if the object is an instance of the specified class.</p>"},{"location":"api_reference/styling/#momapy.styling.ClassSelector.select","title":"select","text":"<pre><code>select(obj: LayoutElement | Builder, ancestors: Collection[LayoutElement | Builder]) -&gt; bool\n</code></pre> <p>Check if the object is an instance of the specified class.</p> <p>Parameters:</p> Name Type Description Default <code>obj</code> <code>LayoutElement | Builder</code> <p>The layout element or builder to test.</p> required <code>ancestors</code> <code>Collection[LayoutElement | Builder]</code> <p>List of ancestor elements (unused for class matching).</p> required <p>Returns:</p> Type Description <code>bool</code> <p>True if the object is an instance of class_name or its subclasses.</p> Source code in <code>src/momapy/styling/core.py</code> <pre><code>def select(\n    self,\n    obj: LayoutElement | Builder,\n    ancestors: collections.abc.Collection[LayoutElement | Builder],\n) -&gt; bool:\n    \"\"\"Check if the object is an instance of the specified class.\n\n    Args:\n        obj: The layout element or builder to test.\n        ancestors: List of ancestor elements (unused for class matching).\n\n    Returns:\n        True if the object is an instance of class_name or its subclasses.\n    \"\"\"\n    for cls in type(obj).__mro__:\n        cls_name = cls.__name__\n        if cls_name == self.class_name or cls_name == f\"{self.class_name}Builder\":\n            return True\n    return False\n</code></pre>"},{"location":"api_reference/styling/#momapy.styling.CompoundSelector","title":"CompoundSelector  <code>dataclass</code>","text":"<pre><code>CompoundSelector(selectors: tuple[Selector, ...])\n</code></pre> <p>               Bases: <code>Selector</code></p> <p>Selector that matches only if all of its component selectors match (AND logic).</p> <p>Examples:</p> <pre><code>selector = CompoundSelector((TypeSelector(\"Rectangle\"), ClassSelector(\"Colored\")))\nselector.select(element, [])  # True if element is Rectangle AND Colored\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>selectors</code> <code>tuple[Selector, ...]</code> <p>The tuple of conjunct selectors</p> required <p>Methods:</p> Name Description <code>select</code> <p>Check if all selectors in the tuple match.</p>"},{"location":"api_reference/styling/#momapy.styling.CompoundSelector.select","title":"select","text":"<pre><code>select(obj: LayoutElement | Builder, ancestors: Collection[LayoutElement | Builder]) -&gt; bool\n</code></pre> <p>Check if all selectors in the tuple match.</p> <p>Parameters:</p> Name Type Description Default <code>obj</code> <code>LayoutElement | Builder</code> <p>The layout element or builder to test.</p> required <code>ancestors</code> <code>Collection[LayoutElement | Builder]</code> <p>List of ancestor elements.</p> required <p>Returns:</p> Type Description <code>bool</code> <p>True if all selectors match the object.</p> Source code in <code>src/momapy/styling/core.py</code> <pre><code>def select(\n    self,\n    obj: LayoutElement | Builder,\n    ancestors: collections.abc.Collection[LayoutElement | Builder],\n) -&gt; bool:\n    \"\"\"Check if all selectors in the tuple match.\n\n    Args:\n        obj: The layout element or builder to test.\n        ancestors: List of ancestor elements.\n\n    Returns:\n        True if all selectors match the object.\n    \"\"\"\n    return all([selector.select(obj, ancestors) for selector in self.selectors])\n</code></pre>"},{"location":"api_reference/styling/#momapy.styling.DescendantSelector","title":"DescendantSelector  <code>dataclass</code>","text":"<pre><code>DescendantSelector(ancestor_selector: Selector, descendant_selector: Selector)\n</code></pre> <p>               Bases: <code>Selector</code></p> <p>Selector that matches elements that are descendants of an ancestor.</p> <p>Examples:</p> <pre><code>selector = DescendantSelector(TypeSelector(\"Group\"), TypeSelector(\"Text\"))\nselector.select(text, [subgroup, group])  # True if text is somewhere inside group\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>ancestor_selector</code> <code>Selector</code> <p>The ancestor selector</p> required <code>descendant_selector</code> <code>Selector</code> <p>The descendant selector</p> required <p>Methods:</p> Name Description <code>select</code> <p>Check if the object is a descendant matching the criteria.</p>"},{"location":"api_reference/styling/#momapy.styling.DescendantSelector.select","title":"select","text":"<pre><code>select(obj: LayoutElement | Builder, ancestors: Collection[LayoutElement | Builder]) -&gt; bool\n</code></pre> <p>Check if the object is a descendant matching the criteria.</p> <p>Parameters:</p> Name Type Description Default <code>obj</code> <code>LayoutElement | Builder</code> <p>The layout element or builder to test.</p> required <code>ancestors</code> <code>Collection[LayoutElement | Builder]</code> <p>List of ancestor elements.</p> required <p>Returns:</p> Type Description <code>bool</code> <p>True if the object matches descendant_selector and any ancestor</p> <code>bool</code> <p>matches ancestor_selector.</p> Source code in <code>src/momapy/styling/core.py</code> <pre><code>def select(\n    self,\n    obj: LayoutElement | Builder,\n    ancestors: collections.abc.Collection[LayoutElement | Builder],\n) -&gt; bool:\n    \"\"\"Check if the object is a descendant matching the criteria.\n\n    Args:\n        obj: The layout element or builder to test.\n        ancestors: List of ancestor elements.\n\n    Returns:\n        True if the object matches descendant_selector and any ancestor\n        matches ancestor_selector.\n    \"\"\"\n    if not ancestors:\n        return False\n    return self.descendant_selector.select(obj, ancestors) and any(\n        [\n            self.ancestor_selector.select(ancestor, ancestors[:i])\n            for i, ancestor in enumerate(ancestors)\n        ]\n    )\n</code></pre>"},{"location":"api_reference/styling/#momapy.styling.IdSelector","title":"IdSelector  <code>dataclass</code>","text":"<pre><code>IdSelector(id_: str)\n</code></pre> <p>               Bases: <code>Selector</code></p> <p>Selector that matches elements by their id attribute.</p> <p>Examples:</p> <pre><code>selector = IdSelector(\"main_node\")\nselector.select(element, [])  # True if element.id_ == \"main_node\"\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id</p> required <p>Methods:</p> Name Description <code>select</code> <p>Check if the object has the specified id.</p>"},{"location":"api_reference/styling/#momapy.styling.IdSelector.select","title":"select","text":"<pre><code>select(obj: LayoutElement | Builder, ancestors: Collection[LayoutElement | Builder]) -&gt; bool\n</code></pre> <p>Check if the object has the specified id.</p> <p>Parameters:</p> Name Type Description Default <code>obj</code> <code>LayoutElement | Builder</code> <p>The layout element or builder to test.</p> required <code>ancestors</code> <code>Collection[LayoutElement | Builder]</code> <p>List of ancestor elements (unused for id matching).</p> required <p>Returns:</p> Type Description <code>bool</code> <p>True if the object has an id_ attribute matching the selector.</p> Source code in <code>src/momapy/styling/core.py</code> <pre><code>def select(\n    self,\n    obj: LayoutElement | Builder,\n    ancestors: collections.abc.Collection[LayoutElement | Builder],\n) -&gt; bool:\n    \"\"\"Check if the object has the specified id.\n\n    Args:\n        obj: The layout element or builder to test.\n        ancestors: List of ancestor elements (unused for id matching).\n\n    Returns:\n        True if the object has an id_ attribute matching the selector.\n    \"\"\"\n    return hasattr(obj, \"id_\") and obj.id_ == self.id_\n</code></pre>"},{"location":"api_reference/styling/#momapy.styling.NotSelector","title":"NotSelector  <code>dataclass</code>","text":"<pre><code>NotSelector(selectors: tuple[Selector, ...])\n</code></pre> <p>               Bases: <code>Selector</code></p> <p>Selector that matches if none of its component selectors match (NOT logic).</p> <p>Examples:</p> <pre><code>selector = NotSelector((TypeSelector(\"Hidden\"),))\nselector.select(element, [])  # True if element is NOT of type Hidden\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>selectors</code> <code>tuple[Selector, ...]</code> <p>The tuple of negated conjunct selectors</p> required <p>Methods:</p> Name Description <code>select</code> <p>Check if none of the selectors in the tuple match.</p>"},{"location":"api_reference/styling/#momapy.styling.NotSelector.select","title":"select","text":"<pre><code>select(obj: LayoutElement | Builder, ancestors: Collection[LayoutElement | Builder]) -&gt; bool\n</code></pre> <p>Check if none of the selectors in the tuple match.</p> <p>Parameters:</p> Name Type Description Default <code>obj</code> <code>LayoutElement | Builder</code> <p>The layout element or builder to test.</p> required <code>ancestors</code> <code>Collection[LayoutElement | Builder]</code> <p>List of ancestor elements.</p> required <p>Returns:</p> Type Description <code>bool</code> <p>True if no selector matches the object.</p> Source code in <code>src/momapy/styling/core.py</code> <pre><code>def select(\n    self,\n    obj: LayoutElement | Builder,\n    ancestors: collections.abc.Collection[LayoutElement | Builder],\n) -&gt; bool:\n    \"\"\"Check if none of the selectors in the tuple match.\n\n    Args:\n        obj: The layout element or builder to test.\n        ancestors: List of ancestor elements.\n\n    Returns:\n        True if no selector matches the object.\n    \"\"\"\n    return not any([selector.select(obj, ancestors) for selector in self.selectors])\n</code></pre>"},{"location":"api_reference/styling/#momapy.styling.OrSelector","title":"OrSelector  <code>dataclass</code>","text":"<pre><code>OrSelector(selectors: tuple[Selector, ...])\n</code></pre> <p>               Bases: <code>Selector</code></p> <p>Selector that matches if any of its component selectors match (OR logic).</p> <p>Examples:</p> <pre><code>selector = OrSelector((TypeSelector(\"Rectangle\"), TypeSelector(\"Circle\")))\nselector.select(some_shape, [])  # True if shape is Rectangle OR Circle\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>selectors</code> <code>tuple[Selector, ...]</code> <p>The tuple of disjunct selectors</p> required <p>Methods:</p> Name Description <code>select</code> <p>Check if any selector in the tuple matches.</p>"},{"location":"api_reference/styling/#momapy.styling.OrSelector.select","title":"select","text":"<pre><code>select(obj: LayoutElement | Builder, ancestors: Collection[LayoutElement | Builder]) -&gt; bool\n</code></pre> <p>Check if any selector in the tuple matches.</p> <p>Parameters:</p> Name Type Description Default <code>obj</code> <code>LayoutElement | Builder</code> <p>The layout element or builder to test.</p> required <code>ancestors</code> <code>Collection[LayoutElement | Builder]</code> <p>List of ancestor elements.</p> required <p>Returns:</p> Type Description <code>bool</code> <p>True if any selector matches the object.</p> Source code in <code>src/momapy/styling/core.py</code> <pre><code>def select(\n    self,\n    obj: LayoutElement | Builder,\n    ancestors: collections.abc.Collection[LayoutElement | Builder],\n) -&gt; bool:\n    \"\"\"Check if any selector in the tuple matches.\n\n    Args:\n        obj: The layout element or builder to test.\n        ancestors: List of ancestor elements.\n\n    Returns:\n        True if any selector matches the object.\n    \"\"\"\n    return any([selector.select(obj, ancestors) for selector in self.selectors])\n</code></pre>"},{"location":"api_reference/styling/#momapy.styling.Selector","title":"Selector  <code>dataclass</code>","text":"<pre><code>Selector()\n</code></pre> <p>               Bases: <code>ABC</code></p> <p>Abstract base class for CSS-like selectors.</p> <p>Selectors determine whether a layout element matches specific criteria. All selector types inherit from this class and implement the select() method.</p> <p>Methods:</p> Name Description <code>select</code> <p>Check if the layout element matches this selector.</p>"},{"location":"api_reference/styling/#momapy.styling.Selector.select","title":"select  <code>abstractmethod</code>","text":"<pre><code>select(obj: LayoutElement | Builder, ancestors: Collection[LayoutElement | Builder]) -&gt; bool\n</code></pre> <p>Check if the layout element matches this selector.</p> <p>Parameters:</p> Name Type Description Default <code>obj</code> <code>LayoutElement | Builder</code> <p>The layout element or builder to test.</p> required <code>ancestors</code> <code>Collection[LayoutElement | Builder]</code> <p>List of ancestor elements.</p> required <p>Returns:</p> Type Description <code>bool</code> <p>True if the element matches, False otherwise.</p> Source code in <code>src/momapy/styling/core.py</code> <pre><code>@abc.abstractmethod\ndef select(\n    self,\n    obj: LayoutElement | Builder,\n    ancestors: collections.abc.Collection[LayoutElement | Builder],\n) -&gt; bool:\n    \"\"\"Check if the layout element matches this selector.\n\n    Args:\n        obj: The layout element or builder to test.\n        ancestors: List of ancestor elements.\n\n    Returns:\n        True if the element matches, False otherwise.\n    \"\"\"\n    pass\n</code></pre>"},{"location":"api_reference/styling/#momapy.styling.StyleCollection","title":"StyleCollection","text":"<p>               Bases: <code>dict</code></p> <p>A dictionary-based collection of style properties.</p> <p>StyleCollection maps attribute names to their values, similar to a CSS declaration block. It is used within StyleSheets to store styles for specific selectors.</p> <p>Examples:</p> <pre><code>styles = StyleCollection({\"fill\": \"red\", \"stroke_width\": 2})\nstyles[\"fill\"]\n</code></pre>"},{"location":"api_reference/styling/#momapy.styling.StyleSheet","title":"StyleSheet","text":"<p>               Bases: <code>dict</code></p> <p>A dictionary-based stylesheet mapping selectors to style collections.</p> <p>StyleSheet extends dict to map Selector objects to StyleCollection objects. Supports merging stylesheets using the | operator.</p> <p>Examples:</p> <pre><code>from momapy.styling import StyleSheet, TypeSelector\nss = StyleSheet({TypeSelector(\"Rectangle\"): StyleCollection({\"fill\": \"blue\"})})\n\n# Merge stylesheets\ncombined = ss | another_stylesheet\n</code></pre> <p>Methods:</p> Name Description <code>__ior__</code> <p>Merge another stylesheet into this one in place and return it.</p> <code>__or__</code> <p>Return a new stylesheet merging this one with another stylesheet.</p> <code>from_file</code> <p>Parse and return a StyleSheet from a CSS file.</p> <code>from_files</code> <p>Parse and combine multiple CSS files into a single StyleSheet.</p> <code>from_string</code> <p>Parse and return a StyleSheet from a CSS string.</p>"},{"location":"api_reference/styling/#momapy.styling.StyleSheet.__ior__","title":"__ior__","text":"<pre><code>__ior__(other: StyleSheet) -&gt; StyleSheet\n</code></pre> <p>Merge another stylesheet into this one in place and return it.</p> Source code in <code>src/momapy/styling/core.py</code> <pre><code>def __ior__(self, other: \"StyleSheet\") -&gt; \"StyleSheet\":\n    \"\"\"Merge another stylesheet into this one in place and return it.\"\"\"\n    for key, value in other.items():\n        if key in self:\n            self[key] |= value\n        else:\n            self[key] = value\n    return self\n</code></pre>"},{"location":"api_reference/styling/#momapy.styling.StyleSheet.__or__","title":"__or__","text":"<pre><code>__or__(other: StyleSheet) -&gt; StyleSheet\n</code></pre> <p>Return a new stylesheet merging this one with another stylesheet.</p> Source code in <code>src/momapy/styling/core.py</code> <pre><code>def __or__(self, other: \"StyleSheet\") -&gt; \"StyleSheet\":\n    \"\"\"Return a new stylesheet merging this one with another stylesheet.\"\"\"\n    merged = copy.deepcopy(self)\n    merged |= other\n    return merged\n</code></pre>"},{"location":"api_reference/styling/#momapy.styling.StyleSheet.from_file","title":"from_file  <code>classmethod</code>","text":"<pre><code>from_file(file_path: str | PathLike) -&gt; StyleSheet\n</code></pre> <p>Parse and return a StyleSheet from a CSS file.</p> <p>Relative <code>@import</code> paths inside the CSS file are resolved against the directory of <code>file_path</code>, not against the process working directory.</p> <p>Parameters:</p> Name Type Description Default <code>file_path</code> <code>str | PathLike</code> <p>Path to the CSS file to parse.</p> required <p>Returns:</p> Type Description <code>StyleSheet</code> <p>A StyleSheet containing the parsed selectors and style collections.</p> <p>Raises:</p> Type Description <code>ParseException</code> <p>If the CSS file is malformed.</p> Source code in <code>src/momapy/styling/core.py</code> <pre><code>@classmethod\ndef from_file(cls, file_path: str | os.PathLike) -&gt; \"StyleSheet\":\n    \"\"\"Parse and return a StyleSheet from a CSS file.\n\n    Relative ``@import`` paths inside the CSS file are resolved against\n    the directory of ``file_path``, not against the process working\n    directory.\n\n    Args:\n        file_path: Path to the CSS file to parse.\n\n    Returns:\n        A StyleSheet containing the parsed selectors and style collections.\n\n    Raises:\n        pyparsing.ParseException: If the CSS file is malformed.\n    \"\"\"\n    path = pathlib.Path(file_path).resolve()\n    doc_parser = _make_document_parser(path.parent)\n    style_sheet = doc_parser.parse_file(str(path), parse_all=True)[0]\n    return style_sheet\n</code></pre>"},{"location":"api_reference/styling/#momapy.styling.StyleSheet.from_files","title":"from_files  <code>classmethod</code>","text":"<pre><code>from_files(file_paths: Collection[str]) -&gt; StyleSheet\n</code></pre> <p>Parse and combine multiple CSS files into a single StyleSheet.</p> <p>Parameters:</p> Name Type Description Default <code>file_paths</code> <code>Collection[str]</code> <p>Collection of paths to CSS files.</p> required <p>Returns:</p> Type Description <code>StyleSheet</code> <p>A merged StyleSheet containing all parsed styles; an empty</p> <code>StyleSheet</code> <p>StyleSheet when <code>file_paths</code> is empty.</p> Source code in <code>src/momapy/styling/core.py</code> <pre><code>@classmethod\ndef from_files(cls, file_paths: collections.abc.Collection[str]) -&gt; \"StyleSheet\":\n    \"\"\"Parse and combine multiple CSS files into a single StyleSheet.\n\n    Args:\n        file_paths: Collection of paths to CSS files.\n\n    Returns:\n        A merged StyleSheet containing all parsed styles; an empty\n        StyleSheet when ``file_paths`` is empty.\n    \"\"\"\n    style_sheet = cls()\n    for file_path in file_paths:\n        style_sheet |= StyleSheet.from_file(file_path)\n    return style_sheet\n</code></pre>"},{"location":"api_reference/styling/#momapy.styling.StyleSheet.from_string","title":"from_string  <code>classmethod</code>","text":"<pre><code>from_string(s: str) -&gt; StyleSheet\n</code></pre> <p>Parse and return a StyleSheet from a CSS string.</p> <p>Relative <code>@import</code> paths inside the string are resolved against the current working directory (a string has no file location); absolute <code>@import</code> paths are resolved as-is.</p> <p>Parameters:</p> Name Type Description Default <code>s</code> <code>str</code> <p>CSS string to parse.</p> required <p>Returns:</p> Type Description <code>StyleSheet</code> <p>A StyleSheet containing the parsed selectors and style collections.</p> <p>Raises:</p> Type Description <code>ParseException</code> <p>If the CSS string is malformed.</p> <p>Examples:</p> <pre><code>css = \"Rectangle { fill: red; }\"\nss = StyleSheet.from_string(css)\n</code></pre> Source code in <code>src/momapy/styling/core.py</code> <pre><code>@classmethod\ndef from_string(cls, s: str) -&gt; \"StyleSheet\":\n    \"\"\"Parse and return a StyleSheet from a CSS string.\n\n    Relative ``@import`` paths inside the string are resolved against\n    the current working directory (a string has no file location);\n    absolute ``@import`` paths are resolved as-is.\n\n    Args:\n        s: CSS string to parse.\n\n    Returns:\n        A StyleSheet containing the parsed selectors and style collections.\n\n    Raises:\n        pyparsing.ParseException: If the CSS string is malformed.\n\n    Examples:\n        ```python\n        css = \"Rectangle { fill: red; }\"\n        ss = StyleSheet.from_string(css)\n        ```\n    \"\"\"\n    doc_parser = _make_document_parser(pathlib.Path.cwd())\n    style_sheet = doc_parser.parse_string(s, parse_all=True)[0]\n    return style_sheet\n</code></pre>"},{"location":"api_reference/styling/#momapy.styling.TypeSelector","title":"TypeSelector  <code>dataclass</code>","text":"<pre><code>TypeSelector(class_name: str)\n</code></pre> <p>               Bases: <code>Selector</code></p> <p>Selector that matches elements by their exact class name.</p> <p>Examples:</p> <pre><code>selector = TypeSelector(\"Rectangle\")\nselector.select(some_rectangle, [])  # True if type is Rectangle\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>class_name</code> <code>str</code> <p>The name of the class</p> required <p>Methods:</p> Name Description <code>select</code> <p>Check if the object's class name matches exactly.</p>"},{"location":"api_reference/styling/#momapy.styling.TypeSelector.select","title":"select","text":"<pre><code>select(obj: LayoutElement | Builder, ancestors: Collection[LayoutElement | Builder]) -&gt; bool\n</code></pre> <p>Check if the object's class name matches exactly.</p> <p>Parameters:</p> Name Type Description Default <code>obj</code> <code>LayoutElement | Builder</code> <p>The layout element or builder to test.</p> required <code>ancestors</code> <code>Collection[LayoutElement | Builder]</code> <p>List of ancestor elements (unused for type matching).</p> required <p>Returns:</p> Type Description <code>bool</code> <p>True if the object's class name or builder name matches.</p> Source code in <code>src/momapy/styling/core.py</code> <pre><code>def select(\n    self,\n    obj: LayoutElement | Builder,\n    ancestors: collections.abc.Collection[LayoutElement | Builder],\n) -&gt; bool:\n    \"\"\"Check if the object's class name matches exactly.\n\n    Args:\n        obj: The layout element or builder to test.\n        ancestors: List of ancestor elements (unused for type matching).\n\n    Returns:\n        True if the object's class name or builder name matches.\n    \"\"\"\n    obj_cls_name = type(obj).__name__\n    return (\n        obj_cls_name == self.class_name\n        or obj_cls_name == f\"{self.class_name}Builder\"\n    )\n</code></pre>"},{"location":"api_reference/styling/#momapy.styling.apply_style_collection","title":"apply_style_collection","text":"<pre><code>apply_style_collection(layout_element: LayoutElement | Builder, style_collection: StyleCollection, strict: bool = True) -&gt; LayoutElement | Builder\n</code></pre> <p>Apply a StyleCollection to a layout element.</p> <p>Parameters:</p> Name Type Description Default <code>layout_element</code> <code>LayoutElement | Builder</code> <p>The element or builder to apply styles to.</p> required <code>style_collection</code> <code>StyleCollection</code> <p>The styles to apply.</p> required <code>strict</code> <code>bool</code> <p>If True, raises AttributeError for invalid attributes. If False, silently ignores them.</p> <code>True</code> <p>Returns:</p> Type Description <code>LayoutElement | Builder</code> <p>The modified layout element or builder.</p> <p>Raises:</p> Type Description <code>AttributeError</code> <p>If strict=True and an attribute doesn't exist on the element.</p> Source code in <code>src/momapy/styling/core.py</code> <pre><code>def apply_style_collection(\n    layout_element: (LayoutElement | Builder),\n    style_collection: StyleCollection,\n    strict: bool = True,\n) -&gt; LayoutElement | Builder:\n    \"\"\"Apply a StyleCollection to a layout element.\n\n    Args:\n        layout_element: The element or builder to apply styles to.\n        style_collection: The styles to apply.\n        strict: If True, raises AttributeError for invalid attributes.\n            If False, silently ignores them.\n\n    Returns:\n        The modified layout element or builder.\n\n    Raises:\n        AttributeError: If strict=True and an attribute doesn't exist on the element.\n    \"\"\"\n    if not isinstance(layout_element, Builder):\n        layout_element = builder_from_object(layout_element)\n        is_builder = False\n    else:\n        is_builder = True\n    for attribute, value in style_collection.items():\n        if hasattr(layout_element, attribute):\n            setattr(layout_element, attribute, value)\n        else:\n            if strict:\n                raise AttributeError(\n                    f\"{type(layout_element)} object has no attribute '{attribute}'\"\n                )\n    if is_builder:\n        return layout_element\n    return object_from_builder(layout_element)\n</code></pre>"},{"location":"api_reference/styling/#momapy.styling.apply_style_sheet","title":"apply_style_sheet","text":"<pre><code>apply_style_sheet(map_or_layout_element: Map | LayoutElement | Builder, style_sheet: StyleSheet, strict: bool = True, ancestors: Collection[LayoutElement | Builder] | None = None) -&gt; Map | LayoutElement | Builder\n</code></pre> <p>Apply a StyleSheet to a layout element or map layout recursively.</p> <p>Parameters:</p> Name Type Description Default <code>map_or_layout_element</code> <code>Map | LayoutElement | Builder</code> <p>The map, element, or builder to style.</p> required <code>style_sheet</code> <code>StyleSheet</code> <p>The stylesheet to apply.</p> required <code>strict</code> <code>bool</code> <p>If True, raises errors for invalid attributes.</p> <code>True</code> <code>ancestors</code> <code>Collection[LayoutElement | Builder] | None</code> <p>Internal list of ancestor elements for selector matching.</p> <code>None</code> <p>Returns:</p> Type Description <code>Map | LayoutElement | Builder</code> <p>The modified map, layout element, or builder.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If given a map without a layout (e.g. an SBML map).</p> Source code in <code>src/momapy/styling/core.py</code> <pre><code>def apply_style_sheet(\n    map_or_layout_element: (Map | LayoutElement | Builder),\n    style_sheet: StyleSheet,\n    strict: bool = True,\n    ancestors: collections.abc.Collection[LayoutElement | Builder] | None = None,\n) -&gt; Map | LayoutElement | Builder:\n    \"\"\"Apply a StyleSheet to a layout element or map layout recursively.\n\n    Args:\n        map_or_layout_element: The map, element, or builder to style.\n        style_sheet: The stylesheet to apply.\n        strict: If True, raises errors for invalid attributes.\n        ancestors: Internal list of ancestor elements for selector matching.\n\n    Returns:\n        The modified map, layout element, or builder.\n\n    Raises:\n        ValueError: If given a map without a layout (e.g. an SBML map).\n    \"\"\"\n    if not isinstance(map_or_layout_element, Builder):\n        map_or_layout_element = builder_from_object(map_or_layout_element)\n        is_builder = False\n    else:\n        is_builder = True\n    if isinstance_or_builder(map_or_layout_element, Map):\n        layout_element = map_or_layout_element.layout\n        if layout_element is None:\n            raise ValueError(\"cannot apply a stylesheet to a map without a layout\")\n    else:\n        layout_element = map_or_layout_element\n    if style_sheet is not None:\n        if ancestors is None:\n            ancestors = []\n        for selector, style_collection in style_sheet.items():\n            if selector.select(layout_element, ancestors):\n                apply_style_collection(\n                    layout_element=layout_element,\n                    style_collection=style_collection,\n                    strict=strict,\n                )\n        ancestors = ancestors + [layout_element]\n        for child in layout_element.children():\n            apply_style_sheet(\n                map_or_layout_element=child,\n                style_sheet=style_sheet,\n                strict=strict,\n                ancestors=ancestors,\n            )\n    if is_builder:\n        return map_or_layout_element\n    return object_from_builder(map_or_layout_element)\n</code></pre>"},{"location":"api_reference/styling/#momapy.styling.combine_style_sheets","title":"combine_style_sheets","text":"<pre><code>combine_style_sheets(style_sheets: Sequence[StyleSheet]) -&gt; StyleSheet | None\n</code></pre> <p>Merge multiple StyleSheets into a single StyleSheet.</p> <p>Parameters:</p> Name Type Description Default <code>style_sheets</code> <code>Sequence[StyleSheet]</code> <p>Collection of StyleSheets to merge.</p> required <p>Returns:</p> Type Description <code>StyleSheet | None</code> <p>A combined StyleSheet, or None if the input is empty.</p> Source code in <code>src/momapy/styling/core.py</code> <pre><code>def combine_style_sheets(\n    style_sheets: collections.abc.Sequence[StyleSheet],\n) -&gt; StyleSheet | None:\n    \"\"\"Merge multiple StyleSheets into a single StyleSheet.\n\n    Args:\n        style_sheets: Collection of StyleSheets to merge.\n\n    Returns:\n        A combined StyleSheet, or None if the input is empty.\n    \"\"\"\n    if not style_sheets:\n        return None\n    output_style_sheet = copy.deepcopy(style_sheets[0])\n    for style_sheet in style_sheets[1:]:\n        output_style_sheet |= style_sheet\n    return output_style_sheet\n</code></pre>"},{"location":"api_reference/styling/#momapy.styling.get_stylable_attributes","title":"get_stylable_attributes","text":"<pre><code>get_stylable_attributes(layout_element_or_class: LayoutElement | type, presentation_only: bool = False) -&gt; list[str]\n</code></pre> <p>Return the CSS-style names of stylable attributes of a layout element.</p> <p>Returns the names of all dataclass fields that can be set via a stylesheet (i.e., via <code>apply_style_collection</code>), formatted as CSS property names (with hyphens instead of underscores).</p> <p>Parameters:</p> Name Type Description Default <code>layout_element_or_class</code> <code>LayoutElement | type</code> <p>A layout element instance or class to inspect.</p> required <code>presentation_only</code> <code>bool</code> <p>If True, only return presentation attributes (fill, stroke, font, transform, etc.). Defaults to False.</p> <code>False</code> <p>Returns:</p> Type Description <code>list[str]</code> <p>A sorted list of CSS-style attribute names.</p> <p>Raises:</p> Type Description <code>TypeError</code> <p>If the argument is not a LayoutElement class or instance.</p> Source code in <code>src/momapy/styling/core.py</code> <pre><code>def get_stylable_attributes(\n    layout_element_or_class: LayoutElement | type,\n    presentation_only: bool = False,\n) -&gt; list[str]:\n    \"\"\"Return the CSS-style names of stylable attributes of a layout element.\n\n    Returns the names of all dataclass fields that can be set via a\n    stylesheet (i.e., via ``apply_style_collection``), formatted as\n    CSS property names (with hyphens instead of underscores).\n\n    Args:\n        layout_element_or_class: A layout element instance or class to\n            inspect.\n        presentation_only: If True, only return presentation attributes\n            (fill, stroke, font, transform, etc.). Defaults to False.\n\n    Returns:\n        A sorted list of CSS-style attribute names.\n\n    Raises:\n        TypeError: If the argument is not a LayoutElement class or\n            instance.\n    \"\"\"\n    if isinstance(layout_element_or_class, type):\n        layout_element_class = layout_element_or_class\n    else:\n        layout_element_class = type(layout_element_or_class)\n    if not (\n        dataclasses.is_dataclass(layout_element_class)\n        and issubclass(layout_element_class, LayoutElement)\n    ):\n        raise TypeError(\n            f\"Expected a LayoutElement class or instance, got {layout_element_class}\"\n        )\n    stylable_attributes = []\n    for field in dataclasses.fields(layout_element_class):\n        if field.name.startswith(\"_\"):\n            continue\n        # id_ is the element's identity, not a stylable property: it is targeted\n        # with the #id selector, and the CSS name \"id\" would not round-trip back\n        # to id_ through the attribute-name parse action.\n        if field.name == \"id_\":\n            continue\n        if presentation_only and not _is_presentation_attribute(field.name):\n            continue\n        css_name = field.name.rstrip(\"_\").replace(\"_\", \"-\")\n        stylable_attributes.append(css_name)\n    stylable_attributes.sort()\n    return stylable_attributes\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/","title":"CellDesigner","text":""},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner","title":"momapy.celldesigner","text":"<p>CellDesigner format support.</p> <p>Modules:</p> Name Description <code>elements</code> <p>Base classes and mixins for CellDesigner model and layout elements.</p> <code>io</code> <p>Input/output module for CellDesigner maps.</p> <code>layout</code> <p>Layout classes for CellDesigner maps.</p> <code>map</code> <p>Map classes for CellDesigner maps.</p> <code>model</code> <p>Model classes for CellDesigner maps.</p> <code>utils</code> <p>Utility functions for CellDesigner map manipulation.</p> <p>Classes:</p> Name Description <code>AndGate</code> <p>AND gate.</p> <code>AndGateLayout</code> <p>AND gate layout.</p> <code>AntisenseRNA</code> <p>Antisense RNA.</p> <code>AntisenseRNAActiveLayout</code> <p>Antisense RNA active layout.</p> <code>AntisenseRNALayout</code> <p>Antisense RNA layout.</p> <code>AntisenseRNATemplate</code> <p>Antisense RNA template.</p> <code>BooleanLogicGate</code> <p>Abstract base class for boolean logic gates.</p> <code>BooleanLogicGateInput</code> <p>Boolean logic gate input.</p> <code>Catalysis</code> <p>Catalysis.</p> <code>CatalysisLayout</code> <p>Catalysis layout.</p> <code>Catalyzer</code> <p>Catalyzer.</p> <code>CellDesignerDoubleHeadedArc</code> <p>Abstract base class for CellDesigner double-headed arcs.</p> <code>CellDesignerLayout</code> <p>CellDesigner layout.</p> <code>CellDesignerMap</code> <p>Class for CellDesigner maps.</p> <code>CellDesignerModel</code> <p>CellDesigner model.</p> <code>CellDesignerModelElement</code> <p>Abstract base class for CellDesigner model elements.</p> <code>CellDesignerNode</code> <p>Abstract base class for CellDesigner nodes.</p> <code>CellDesignerSingleHeadedArc</code> <p>Abstract base class for CellDesigner single-headed arcs.</p> <code>CodingRegion</code> <p>Coding region.</p> <code>Compartment</code> <p>Compartment.</p> <code>CompartmentCorner</code> <p>Compartment corner.</p> <code>CompartmentSide</code> <p>Compartment side.</p> <code>Complex</code> <p>Complex.</p> <code>ComplexActiveLayout</code> <p>Complex active layout.</p> <code>ComplexLayout</code> <p>Complex layout.</p> <code>ConsumptionLayout</code> <p>Consumption layout.</p> <code>CornerCompartmentLayout</code> <p>Corner compartment layout.</p> <code>DegradedActiveLayout</code> <p>Degraded active layout.</p> <code>DegradedLayout</code> <p>Degraded layout.</p> <code>Dissociation</code> <p>Dissociation.</p> <code>DissociationLayout</code> <p>Dissociation layout.</p> <code>Drug</code> <p>Drug.</p> <code>DrugActiveLayout</code> <p>Drug active layout.</p> <code>DrugLayout</code> <p>Drug layout.</p> <code>Gene</code> <p>Gene.</p> <code>GeneActiveLayout</code> <p>Gene active layout.</p> <code>GeneLayout</code> <p>Gene layout.</p> <code>GeneTemplate</code> <p>Gene template.</p> <code>GenericProtein</code> <p>Generic protein.</p> <code>GenericProteinActiveLayout</code> <p>Generic protein active layout.</p> <code>GenericProteinLayout</code> <p>Generic protein layout.</p> <code>GenericProteinTemplate</code> <p>Generic protein template.</p> <code>HeterodimerAssociation</code> <p>Heterodimer association.</p> <code>HeterodimerAssociationLayout</code> <p>Heterodimer association layout.</p> <code>Inhibition</code> <p>Inhibition.</p> <code>InhibitionLayout</code> <p>Inhibition layout.</p> <code>Inhibitor</code> <p>Inhibitor.</p> <code>Ion</code> <p>Ion.</p> <code>IonActiveLayout</code> <p>Ion active layout.</p> <code>IonChannel</code> <p>Ion channel.</p> <code>IonChannelActiveLayout</code> <p>Ion channel active layout.</p> <code>IonChannelLayout</code> <p>Ion channel layout.</p> <code>IonChannelTemplate</code> <p>Ion channel template.</p> <code>IonLayout</code> <p>Ion layout.</p> <code>KnownOrUnknownModulation</code> <p>Abstract base class for known or unknown modulations.</p> <code>KnownOrUnknownModulator</code> <p>Abstract base class for known or unknown modulators.</p> <code>KnownTransitionOmitted</code> <p>Known transition omitted.</p> <code>KnownTransitionOmittedLayout</code> <p>Known transition omitted layout.</p> <code>LineCompartmentLayout</code> <p>Line compartment layout.</p> <code>LogicArcLayout</code> <p>Logic arc layout.</p> <code>Modification</code> <p>Modification.</p> <code>ModificationLayout</code> <p>Modification layout.</p> <code>ModificationResidue</code> <p>Modification residue.</p> <code>ModificationSite</code> <p>Modification site.</p> <code>ModificationState</code> <p>Modification state.</p> <code>Modulation</code> <p>Modulation.</p> <code>ModulationLayout</code> <p>Modulation layout.</p> <code>Modulator</code> <p>Modulator.</p> <code>NegativeInfluence</code> <p>Negative influence.</p> <code>NotGate</code> <p>NOT gate.</p> <code>NotGateLayout</code> <p>NOT gate layout.</p> <code>OrGate</code> <p>OR gate.</p> <code>OrGateLayout</code> <p>OR gate layout.</p> <code>OvalCompartmentLayout</code> <p>Oval compartment layout.</p> <code>Phenotype</code> <p>Phenotype.</p> <code>PhenotypeActiveLayout</code> <p>Phenotype active layout.</p> <code>PhenotypeLayout</code> <p>Phenotype layout.</p> <code>PhysicalStimulation</code> <p>Physical stimulation.</p> <code>PhysicalStimulationLayout</code> <p>Physical stimulation layout.</p> <code>PhysicalStimulator</code> <p>Physical stimulator.</p> <code>PositiveInfluence</code> <p>Positive influence.</p> <code>PositiveInfluenceLayout</code> <p>Positive influence layout.</p> <code>Product</code> <p>Product.</p> <code>ProductionLayout</code> <p>Production layout.</p> <code>Protein</code> <p>Abstract base class for proteins.</p> <code>ProteinBindingDomain</code> <p>Protein binding domain.</p> <code>ProteinTemplate</code> <p>Abstract base class for protein templates.</p> <code>RNA</code> <p>RNA.</p> <code>RNAActiveLayout</code> <p>RNA active layout.</p> <code>RNALayout</code> <p>RNA layout.</p> <code>RNATemplate</code> <p>RNA template.</p> <code>Reactant</code> <p>Reactant.</p> <code>Reaction</code> <p>Abstract base class for reactions.</p> <code>ReactionLayout</code> <p>Reaction layout.</p> <code>Receptor</code> <p>Receptor.</p> <code>ReceptorActiveLayout</code> <p>Receptor active layout.</p> <code>ReceptorLayout</code> <p>Receptor layout.</p> <code>ReceptorTemplate</code> <p>Receptor template.</p> <code>RectangleCompartmentLayout</code> <p>Rectangle compartment layout.</p> <code>Region</code> <p>Region.</p> <code>RegulatoryRegion</code> <p>Regulatory region.</p> <code>SimpleMolecule</code> <p>Simple molecule.</p> <code>SimpleMoleculeActiveLayout</code> <p>Simple molecule active layout.</p> <code>SimpleMoleculeLayout</code> <p>Simple molecule layout.</p> <code>Species</code> <p>Abstract base class for species.</p> <code>SpeciesTemplate</code> <p>Abstract base class for species templates.</p> <code>StateTransition</code> <p>State transition.</p> <code>StateTransitionLayout</code> <p>State transition layout.</p> <code>StructuralState</code> <p>Structural state.</p> <code>StructuralStateLayout</code> <p>Structural state layout.</p> <code>Transcription</code> <p>Transcription.</p> <code>TranscriptionLayout</code> <p>Transcription layout.</p> <code>TranscriptionStartingSiteL</code> <p>Transcription starting site L.</p> <code>TranscriptionStartingSiteR</code> <p>Transcription starting site R.</p> <code>Translation</code> <p>Translation.</p> <code>TranslationLayout</code> <p>Translation layout.</p> <code>Transport</code> <p>Transport.</p> <code>TransportLayout</code> <p>Transport layout.</p> <code>Trigger</code> <p>Trigger.</p> <code>Triggering</code> <p>Triggering.</p> <code>TriggeringLayout</code> <p>Triggering layout.</p> <code>TruncatedProtein</code> <p>Truncated protein.</p> <code>TruncatedProteinActiveLayout</code> <p>Truncated protein active layout.</p> <code>TruncatedProteinLayout</code> <p>Truncated protein layout.</p> <code>TruncatedProteinTemplate</code> <p>Truncated protein template.</p> <code>Truncation</code> <p>Truncation.</p> <code>TruncationLayout</code> <p>Truncation layout.</p> <code>Unknown</code> <p>Unknown.</p> <code>UnknownActiveLayout</code> <p>Unknown active layout.</p> <code>UnknownCatalysis</code> <p>Unknown catalysis.</p> <code>UnknownCatalysisLayout</code> <p>Unknown catalysis layout.</p> <code>UnknownCatalyzer</code> <p>Unknown catalyzer.</p> <code>UnknownGate</code> <p>Unknown gate.</p> <code>UnknownGateLayout</code> <p>Unknown gate layout.</p> <code>UnknownInhibition</code> <p>Unknown inhibition.</p> <code>UnknownInhibitionLayout</code> <p>Unknown inhibition layout.</p> <code>UnknownInhibitor</code> <p>Unknown inhibitor.</p> <code>UnknownLayout</code> <p>Unknown layout.</p> <code>UnknownModulation</code> <p>Unknown modulation.</p> <code>UnknownModulationLayout</code> <p>Unknown modulation layout.</p> <code>UnknownModulator</code> <p>Unknown modulator.</p> <code>UnknownNegativeInfluence</code> <p>Unknown negative influence.</p> <code>UnknownPhysicalStimulation</code> <p>Unknown physical stimulation.</p> <code>UnknownPhysicalStimulationLayout</code> <p>Unknown physical stimulation layout.</p> <code>UnknownPositiveInfluence</code> <p>Unknown positive influence.</p> <code>UnknownPositiveInfluenceLayout</code> <p>Unknown positive influence layout.</p> <code>UnknownTransition</code> <p>Unknown transition.</p> <code>UnknownTransitionLayout</code> <p>Unknown transition layout.</p> <code>UnknownTriggering</code> <p>Unknown triggering.</p> <code>UnknownTriggeringLayout</code> <p>Unknown triggering layout.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AndGate","title":"AndGate  <code>dataclass</code>","text":"<pre><code>AndGate(*, id_: str = make_uuid4_as_str(), inputs: frozenset[BooleanLogicGateInput] = frozenset())\n</code></pre> <p>               Bases: <code>BooleanLogicGate</code></p> <p>AND gate.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'e109fccb-7623-44d9-856a-47fd71dc881a'</code> <code>inputs</code> <code>frozenset[BooleanLogicGateInput]</code> <p>The inputs of the Boolean logic gate</p> <code>&lt;dynamic&gt;</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AndGate.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AndGateLayout","title":"AndGateLayout  <code>dataclass</code>","text":"<pre><code>AndGateLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = white, filter_: NoneValueType | Filter | None = None, height: float = 15.0, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = 1.25, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 15.0, text: str = '&amp;', font_family: str = DEFAULT_FONT_FAMILY, font_fill: Color | NoneValueType = black, font_stroke: Color | NoneValueType = NoneValue, font_style: FontStyle = NORMAL, font_weight: FontWeight | float = NORMAL)\n</code></pre> <p>               Bases: <code>_SimpleNodeMixin</code>, <code>_TextMixin</code>, <code>CellDesignerNode</code></p> <p>AND gate layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'10e3bd66-b365-437d-9dee-efe49fb5443f'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>15.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The stroke width of the node</p> <code>1.25</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>15.0</code> <code>text</code> <code>str</code> <p>The label drawn inside the gate.</p> <code>'&amp;'</code> <code>font_family</code> <code>str</code> <p>Font family for the text.</p> <code>'DejaVu Sans'</code> <code>font_fill</code> <code>Color | NoneValueType</code> <p>Fill color for the text.</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>font_stroke</code> <code>Color | NoneValueType</code> <p>Stroke color for the text outline.</p> <code>&lt;momapy.drawing.NoneValueType object at 0x7f8b17323e00&gt;</code> <code>font_style</code> <code>FontStyle</code> <p>Font style (normal, italic, etc.).</p> <code>&lt;FontStyle.NORMAL: 0&gt;</code> <code>font_weight</code> <code>FontWeight | float</code> <p>Font weight (normal, bold, etc.).</p> <code>&lt;FontWeight.NORMAL: 0&gt;</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>angle</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Return the east anchor of the node.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Return the label center anchor of the node.</p> <code>north</code> <p>Return the north anchor of the node.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Return the south anchor of the node.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Return the west anchor of the node.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AndGateLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AndGateLayout.angle","title":"angle","text":"<pre><code>angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AndGateLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AndGateLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AndGateLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AndGateLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AndGateLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AndGateLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AndGateLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AndGateLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AndGateLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Return the east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Return the east anchor of the node.\"\"\"\n    return self.own_angle(0)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AndGateLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AndGateLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AndGateLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AndGateLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AndGateLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Return the label center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Return the label center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AndGateLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Return the north anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Return the north anchor of the node.\"\"\"\n    return self.own_angle(90)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AndGateLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AndGateLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AndGateLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AndGateLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AndGateLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AndGateLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AndGateLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AndGateLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AndGateLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AndGateLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AndGateLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AndGateLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Return the south anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Return the south anchor of the node.\"\"\"\n    return self.own_angle(270)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AndGateLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AndGateLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AndGateLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AndGateLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AndGateLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AndGateLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Return the west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Return the west anchor of the node.\"\"\"\n    return self.own_angle(180)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AndGateLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AndGateLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AndGateLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AndGateLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AntisenseRNA","title":"AntisenseRNA  <code>dataclass</code>","text":"<pre><code>AntisenseRNA(*, id_: str = make_uuid4_as_str(), name: str | None = None, sbo_term: str | None = None, metaid: str | None = None, compartment: Compartment | None = None, hypothetical: bool = False, active: bool = False, homomultimer: int = 1, template: AntisenseRNATemplate, modifications: frozenset[Modification] = frozenset())\n</code></pre> <p>               Bases: <code>Species</code></p> <p>Antisense RNA.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'fe613530-a289-411f-8d3a-db58085fd1bc'</code> <code>name</code> <code>str | None</code> <p>Human-readable name of the element.</p> <code>None</code> <code>sbo_term</code> <code>str | None</code> <p>Optional SBO term identifier for semantic annotation.</p> <code>None</code> <code>metaid</code> <code>str | None</code> <p>Optional metadata identifier for RDF annotations.</p> <code>None</code> <code>compartment</code> <code>Compartment | None</code> <p>The compartment containing this species.</p> <code>None</code> <code>hypothetical</code> <code>bool</code> <p>Whether the species is hypothetical or not</p> <code>False</code> <code>active</code> <code>bool</code> <p>Whether the species is active or not</p> <code>False</code> <code>homomultimer</code> <code>int</code> <p>The number of subunits forming the species</p> <code>1</code> <code>template</code> <code>AntisenseRNATemplate</code> <p>The template of the antisense RNA</p> required <code>modifications</code> <code>frozenset[Modification]</code> <p>The modifications of the antisense RNA</p> <code>&lt;dynamic&gt;</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AntisenseRNA.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AntisenseRNAActiveLayout","title":"AntisenseRNAActiveLayout  <code>dataclass</code>","text":"<pre><code>AntisenseRNAActiveLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = NoneValue, filter_: NoneValueType | Filter | None = None, height: float = 25.0 + DEFAULT_ACTIVE_YSEP * 2, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = (4, 2), stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = 1.0, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 90.0 + DEFAULT_ACTIVE_XSEP * 2, offset: float = 3.0, subunits_stroke: tuple[NoneValueType | Color, ...] | None = None, subunits_stroke_width: tuple[NoneValueType | float, ...] | None = None, subunits_stroke_dasharray: tuple[NoneValueType | tuple[float, ...], ...] | None = None, subunits_stroke_dashoffset: tuple[float, ...] | None = None, subunits_fill: tuple[NoneValueType | Color, ...] | None = None, subunits_transform: tuple[NoneValueType | tuple[Transformation, ...], ...] | None = None, subunits_filter: tuple[NoneValueType | Filter, ...] | None = None, n: int = 1, angle: float = 45.0)\n</code></pre> <p>               Bases: <code>_MultiNodeMixin</code>, <code>CellDesignerNode</code></p> <p>Antisense RNA active layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'875e94a0-c1cf-4e0b-b6c6-7d81bf5d249c'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>&lt;momapy.drawing.NoneValueType object at 0x7f8b17323e00&gt;</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>33.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>(4, 2)</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The stroke width of the node</p> <code>1.0</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>98.0</code> <code>offset</code> <code>float</code> <p>Offset distance between stacked units.</p> <code>3.0</code> <code>subunits_stroke</code> <code>tuple[NoneValueType | Color, ...] | None</code> <p>Tuple of stroke colors for each subunit.</p> <code>None</code> <code>subunits_stroke_width</code> <code>tuple[NoneValueType | float, ...] | None</code> <p>Tuple of stroke widths for each subunit.</p> <code>None</code> <code>subunits_stroke_dasharray</code> <code>tuple[NoneValueType | tuple[float, ...], ...] | None</code> <p>Tuple of dash patterns for each subunit.</p> <code>None</code> <code>subunits_stroke_dashoffset</code> <code>tuple[float, ...] | None</code> <p>Tuple of dash offsets for each subunit.</p> <code>None</code> <code>subunits_fill</code> <code>tuple[NoneValueType | Color, ...] | None</code> <p>Tuple of fill colors for each subunit.</p> <code>None</code> <code>subunits_transform</code> <code>tuple[NoneValueType | tuple[Transformation, ...], ...] | None</code> <p>Tuple of transformations for each subunit.</p> <code>None</code> <code>subunits_filter</code> <code>tuple[NoneValueType | Filter, ...] | None</code> <p>Tuple of filters for each subunit.</p> <code>None</code> <code>n</code> <code>int</code> <p>Number of stacked node copies to draw.</p> <code>1</code> <code>angle</code> <code>float</code> <p>The slant angle of the parallelogram.</p> <code>45.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Return the east anchor of the node.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Get the center position for the node label.</p> <code>north</code> <p>Return the north anchor of the node.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Return the south anchor of the node.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Return the west anchor of the node.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>angle</code> <code>float</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AntisenseRNAActiveLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AntisenseRNAActiveLayout.angle","title":"angle  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>angle: float = dataclasses.field(default=45.0, metadata={'description': 'The slant angle of the parallelogram.'})\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AntisenseRNAActiveLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AntisenseRNAActiveLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AntisenseRNAActiveLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AntisenseRNAActiveLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AntisenseRNAActiveLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AntisenseRNAActiveLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AntisenseRNAActiveLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AntisenseRNAActiveLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AntisenseRNAActiveLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Return the east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Return the east anchor of the node.\"\"\"\n    return self.own_angle(0)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AntisenseRNAActiveLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AntisenseRNAActiveLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AntisenseRNAActiveLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AntisenseRNAActiveLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AntisenseRNAActiveLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Get the center position for the node label.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point where the label should be centered.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Get the center position for the node label.\n\n    Returns:\n        Point where the label should be centered.\n    \"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2 - (self._n - 1) * self.offset,\n        self.height / 2 - height / 2 - (self._n - 1) * self.offset,\n    )\n    return self._make_subunit_shape(\n        position=position, width=width, height=height\n    ).position\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AntisenseRNAActiveLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Return the north anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Return the north anchor of the node.\"\"\"\n    return self.own_angle(90)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AntisenseRNAActiveLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AntisenseRNAActiveLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AntisenseRNAActiveLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AntisenseRNAActiveLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AntisenseRNAActiveLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AntisenseRNAActiveLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AntisenseRNAActiveLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AntisenseRNAActiveLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AntisenseRNAActiveLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AntisenseRNAActiveLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AntisenseRNAActiveLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AntisenseRNAActiveLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Return the south anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Return the south anchor of the node.\"\"\"\n    return self.own_angle(270)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AntisenseRNAActiveLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AntisenseRNAActiveLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AntisenseRNAActiveLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AntisenseRNAActiveLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AntisenseRNAActiveLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AntisenseRNAActiveLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Return the west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Return the west anchor of the node.\"\"\"\n    return self.own_angle(180)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AntisenseRNAActiveLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AntisenseRNAActiveLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AntisenseRNAActiveLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AntisenseRNAActiveLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AntisenseRNALayout","title":"AntisenseRNALayout  <code>dataclass</code>","text":"<pre><code>AntisenseRNALayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = from_hex('#FFCCCC'), filter_: NoneValueType | Filter | None = None, height: float = 25.0, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = 1.0, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 90.0, offset: float = 3.0, subunits_stroke: tuple[NoneValueType | Color, ...] | None = None, subunits_stroke_width: tuple[NoneValueType | float, ...] | None = None, subunits_stroke_dasharray: tuple[NoneValueType | tuple[float, ...], ...] | None = None, subunits_stroke_dashoffset: tuple[float, ...] | None = None, subunits_fill: tuple[NoneValueType | Color, ...] | None = None, subunits_transform: tuple[NoneValueType | tuple[Transformation, ...], ...] | None = None, subunits_filter: tuple[NoneValueType | Filter, ...] | None = None, n: int = 1, angle: float = 45.0)\n</code></pre> <p>               Bases: <code>_MultiNodeMixin</code>, <code>CellDesignerNode</code></p> <p>Antisense RNA layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'5bad9f9d-2029-42e2-ad12-b3040fd1bfc7'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>Color(red=255, green=204, blue=204, alpha=1.0)</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>25.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The stroke width of the node</p> <code>1.0</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>90.0</code> <code>offset</code> <code>float</code> <p>Offset distance between stacked units.</p> <code>3.0</code> <code>subunits_stroke</code> <code>tuple[NoneValueType | Color, ...] | None</code> <p>Tuple of stroke colors for each subunit.</p> <code>None</code> <code>subunits_stroke_width</code> <code>tuple[NoneValueType | float, ...] | None</code> <p>Tuple of stroke widths for each subunit.</p> <code>None</code> <code>subunits_stroke_dasharray</code> <code>tuple[NoneValueType | tuple[float, ...], ...] | None</code> <p>Tuple of dash patterns for each subunit.</p> <code>None</code> <code>subunits_stroke_dashoffset</code> <code>tuple[float, ...] | None</code> <p>Tuple of dash offsets for each subunit.</p> <code>None</code> <code>subunits_fill</code> <code>tuple[NoneValueType | Color, ...] | None</code> <p>Tuple of fill colors for each subunit.</p> <code>None</code> <code>subunits_transform</code> <code>tuple[NoneValueType | tuple[Transformation, ...], ...] | None</code> <p>Tuple of transformations for each subunit.</p> <code>None</code> <code>subunits_filter</code> <code>tuple[NoneValueType | Filter, ...] | None</code> <p>Tuple of filters for each subunit.</p> <code>None</code> <code>n</code> <code>int</code> <p>Number of stacked node copies to draw.</p> <code>1</code> <code>angle</code> <code>float</code> <p>The slant angle of the parallelogram.</p> <code>45.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Return the east anchor point of the top subunit shape.</p> <code>east_north_east</code> <p>Return the east-north-east anchor point of the top subunit shape.</p> <code>east_south_east</code> <p>Return the east-south-east anchor point of the top subunit shape.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Get the center position for the node label.</p> <code>north</code> <p>Return the north anchor point of the top subunit shape.</p> <code>north_east</code> <p>Return the north-east anchor point of the top subunit shape.</p> <code>north_north_east</code> <p>Return the north-north-east anchor point of the top subunit shape.</p> <code>north_north_west</code> <p>Return the north-north-west anchor point of the top subunit shape.</p> <code>north_west</code> <p>Return the north-west anchor point of the top subunit shape.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Return the south anchor point of the top subunit shape.</p> <code>south_east</code> <p>Return the south-east anchor point of the top subunit shape.</p> <code>south_south_east</code> <p>Return the south-south-east anchor point of the top subunit shape.</p> <code>south_south_west</code> <p>Return the south-south-west anchor point of the top subunit shape.</p> <code>south_west</code> <p>Return the south-west anchor point of the top subunit shape.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Return the west anchor point of the top subunit shape.</p> <code>west_north_west</code> <p>Return the west-north-west anchor point of the top subunit shape.</p> <code>west_south_west</code> <p>Return the west-south-west anchor point of the top subunit shape.</p> <p>Attributes:</p> Name Type Description <code>angle</code> <code>float</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AntisenseRNALayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AntisenseRNALayout.angle","title":"angle  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>angle: float = dataclasses.field(default=45.0, metadata={'description': 'The slant angle of the parallelogram.'})\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AntisenseRNALayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AntisenseRNALayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AntisenseRNALayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AntisenseRNALayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AntisenseRNALayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AntisenseRNALayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AntisenseRNALayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AntisenseRNALayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AntisenseRNALayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Return the east anchor point of the top subunit shape.</p> Source code in <code>src/momapy/celldesigner/layout.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Return the east anchor point of the top subunit shape.\"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2,\n        self.height / 2 - height / 2,\n    )\n    shape = self._make_subunit_shape(position, width, height)\n    return shape.joint3() / 2 + shape.joint2() / 2\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AntisenseRNALayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east-north-east anchor point of the top subunit shape.</p> Source code in <code>src/momapy/celldesigner/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east-north-east anchor point of the top subunit shape.\"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2,\n        self.height / 2 - height / 2,\n    )\n    shape = self._make_subunit_shape(position, width, height)\n    return shape.joint3() * 0.25 + shape.joint2() * 0.75\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AntisenseRNALayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east-south-east anchor point of the top subunit shape.</p> Source code in <code>src/momapy/celldesigner/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east-south-east anchor point of the top subunit shape.\"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2,\n        self.height / 2 - height / 2,\n    )\n    shape = self._make_subunit_shape(position, width, height)\n    return shape.joint3() * 0.75 + shape.joint2() * 0.25\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AntisenseRNALayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AntisenseRNALayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AntisenseRNALayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Get the center position for the node label.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point where the label should be centered.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Get the center position for the node label.\n\n    Returns:\n        Point where the label should be centered.\n    \"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2 - (self._n - 1) * self.offset,\n        self.height / 2 - height / 2 - (self._n - 1) * self.offset,\n    )\n    return self._make_subunit_shape(\n        position=position, width=width, height=height\n    ).position\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AntisenseRNALayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Return the north anchor point of the top subunit shape.</p> Source code in <code>src/momapy/celldesigner/layout.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Return the north anchor point of the top subunit shape.\"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2,\n        self.height / 2 - height / 2,\n    )\n    shape = self._make_subunit_shape(position, width, height)\n    return shape.joint1() / 2 + shape.joint2() / 2\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AntisenseRNALayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north-east anchor point of the top subunit shape.</p> Source code in <code>src/momapy/celldesigner/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north-east anchor point of the top subunit shape.\"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2,\n        self.height / 2 - height / 2,\n    )\n    shape = self._make_subunit_shape(position, width, height)\n    return shape.joint2()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AntisenseRNALayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north-north-east anchor point of the top subunit shape.</p> Source code in <code>src/momapy/celldesigner/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north-north-east anchor point of the top subunit shape.\"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2,\n        self.height / 2 - height / 2,\n    )\n    shape = self._make_subunit_shape(position, width, height)\n    return shape.joint1() * 0.25 + shape.joint2() * 0.75\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AntisenseRNALayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north-north-west anchor point of the top subunit shape.</p> Source code in <code>src/momapy/celldesigner/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north-north-west anchor point of the top subunit shape.\"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2,\n        self.height / 2 - height / 2,\n    )\n    shape = self._make_subunit_shape(position, width, height)\n    return shape.joint1() * 0.75 + shape.joint2() * 0.25\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AntisenseRNALayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north-west anchor point of the top subunit shape.</p> Source code in <code>src/momapy/celldesigner/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north-west anchor point of the top subunit shape.\"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2,\n        self.height / 2 - height / 2,\n    )\n    shape = self._make_subunit_shape(position, width, height)\n    return shape.joint1()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AntisenseRNALayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AntisenseRNALayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AntisenseRNALayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AntisenseRNALayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AntisenseRNALayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AntisenseRNALayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AntisenseRNALayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AntisenseRNALayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Return the south anchor point of the top subunit shape.</p> Source code in <code>src/momapy/celldesigner/layout.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Return the south anchor point of the top subunit shape.\"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2,\n        self.height / 2 - height / 2,\n    )\n    shape = self._make_subunit_shape(position, width, height)\n    return shape.joint4() / 2 + shape.joint3() / 2\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AntisenseRNALayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south-east anchor point of the top subunit shape.</p> Source code in <code>src/momapy/celldesigner/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south-east anchor point of the top subunit shape.\"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2,\n        self.height / 2 - height / 2,\n    )\n    shape = self._make_subunit_shape(position, width, height)\n    return shape.joint3()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AntisenseRNALayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south-south-east anchor point of the top subunit shape.</p> Source code in <code>src/momapy/celldesigner/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south-south-east anchor point of the top subunit shape.\"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2,\n        self.height / 2 - height / 2,\n    )\n    shape = self._make_subunit_shape(position, width, height)\n    return shape.joint4() * 0.25 + shape.joint3() * 0.75\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AntisenseRNALayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south-south-west anchor point of the top subunit shape.</p> Source code in <code>src/momapy/celldesigner/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south-south-west anchor point of the top subunit shape.\"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2,\n        self.height / 2 - height / 2,\n    )\n    shape = self._make_subunit_shape(position, width, height)\n    return shape.joint4() * 0.75 + shape.joint3() * 0.25\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AntisenseRNALayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south-west anchor point of the top subunit shape.</p> Source code in <code>src/momapy/celldesigner/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south-west anchor point of the top subunit shape.\"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2,\n        self.height / 2 - height / 2,\n    )\n    shape = self._make_subunit_shape(position, width, height)\n    return shape.joint4()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AntisenseRNALayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AntisenseRNALayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Return the west anchor point of the top subunit shape.</p> Source code in <code>src/momapy/celldesigner/layout.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Return the west anchor point of the top subunit shape.\"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2,\n        self.height / 2 - height / 2,\n    )\n    shape = self._make_subunit_shape(position, width, height)\n    return shape.joint4() / 2 + shape.joint1() / 2\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AntisenseRNALayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west-north-west anchor point of the top subunit shape.</p> Source code in <code>src/momapy/celldesigner/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west-north-west anchor point of the top subunit shape.\"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2,\n        self.height / 2 - height / 2,\n    )\n    shape = self._make_subunit_shape(position, width, height)\n    return shape.joint4() * 0.25 + shape.joint1() * 0.75\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AntisenseRNALayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west-south-west anchor point of the top subunit shape.</p> Source code in <code>src/momapy/celldesigner/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west-south-west anchor point of the top subunit shape.\"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2,\n        self.height / 2 - height / 2,\n    )\n    shape = self._make_subunit_shape(position, width, height)\n    return shape.joint4() * 0.75 + shape.joint1() * 0.25\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AntisenseRNALayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AntisenseRNALayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AntisenseRNATemplate","title":"AntisenseRNATemplate  <code>dataclass</code>","text":"<pre><code>AntisenseRNATemplate(*, id_: str = make_uuid4_as_str(), name: str, regions: frozenset[ModificationSite | CodingRegion | ProteinBindingDomain] = frozenset())\n</code></pre> <p>               Bases: <code>SpeciesTemplate</code></p> <p>Antisense RNA template.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'e25c1aaa-5f0f-47fa-b9b7-815ef61da884'</code> <code>name</code> <code>str</code> <p>The name of the species template</p> required <code>regions</code> <code>frozenset[ModificationSite | CodingRegion | ProteinBindingDomain]</code> <p>The regions of the antisense RNA template</p> <code>&lt;dynamic&gt;</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.AntisenseRNATemplate.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.BooleanLogicGate","title":"BooleanLogicGate  <code>dataclass</code>","text":"<pre><code>BooleanLogicGate(*, id_: str = make_uuid4_as_str(), inputs: frozenset[BooleanLogicGateInput] = frozenset())\n</code></pre> <p>               Bases: <code>CellDesignerModelElement</code></p> <p>Abstract base class for boolean logic gates.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'8cf7b73f-94c4-4e9c-baa8-6d31ec63ec44'</code> <code>inputs</code> <code>frozenset[BooleanLogicGateInput]</code> <p>The inputs of the Boolean logic gate</p> <code>&lt;dynamic&gt;</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.BooleanLogicGate.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.BooleanLogicGateInput","title":"BooleanLogicGateInput  <code>dataclass</code>","text":"<pre><code>BooleanLogicGateInput(*, id_: str = make_uuid4_as_str(), name: str | None = None, sbo_term: str | None = None, metaid: str | None = None, referred_element: Species)\n</code></pre> <p>               Bases: <code>SimpleSpeciesReference</code>, <code>CellDesignerModelElement</code></p> <p>Boolean logic gate input.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'d15cc8ba-658b-4c2f-a92e-d266ee43d27a'</code> <code>name</code> <code>str | None</code> <p>Human-readable name of the element.</p> <code>None</code> <code>sbo_term</code> <code>str | None</code> <p>Optional SBO term identifier for semantic annotation.</p> <code>None</code> <code>metaid</code> <code>str | None</code> <p>Optional metadata identifier for RDF annotations.</p> <code>None</code> <code>referred_element</code> <code>Species</code> <p>The species being referenced.</p> required <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.BooleanLogicGateInput.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.Catalysis","title":"Catalysis  <code>dataclass</code>","text":"<pre><code>Catalysis(*, id_: str = make_uuid4_as_str(), source: Species | BooleanLogicGate, target: Species | None)\n</code></pre> <p>               Bases: <code>PhysicalStimulation</code></p> <p>Catalysis.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'ee821594-7d32-401f-bc91-22c7357c7db9'</code> <code>source</code> <code>Species | BooleanLogicGate</code> <p>The source of the modulation</p> required <code>target</code> <code>Species | None</code> <p>The target of the modulation</p> required <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.Catalysis.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CatalysisLayout","title":"CatalysisLayout  <code>dataclass</code>","text":"<pre><code>CatalysisLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, end_shorten: float = 0.0, fill: NoneValueType | Color | None = None, filter_: NoneValueType | Filter | None = None, path_fill: NoneValueType | Color | None = NoneValue, path_filter: NoneValueType | Filter | None = None, path_stroke: NoneValueType | Color | None = black, path_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, path_stroke_dashoffset: NoneValueType | float | None = None, path_stroke_width: float | None = 1.0, path_transform: NoneValueType | tuple[Transformation, ...] | None = None, stroke: NoneValueType | Color | None = None, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: NoneValueType | float | None = None, segments: tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...] = tuple(), source: LayoutElement | None = None, start_shorten: float = 0.0, target: LayoutElement | None = None, transform: NoneValueType | tuple[Transformation, ...] | None = None, arrowhead_fill: NoneValueType | Color | None = white, arrowhead_filter: NoneValueType | Filter | None = None, arrowhead_stroke: NoneValueType | Color | None = black, arrowhead_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, arrowhead_stroke_dashoffset: NoneValueType | float | None = None, arrowhead_stroke_width: float | None = 1.0, arrowhead_transform: NoneValueType | tuple[Transformation, ...] | None = None, arrowhead_height: float = 7.0, arrowhead_width: float = 7.0)\n</code></pre> <p>               Bases: <code>CellDesignerSingleHeadedArc</code></p> <p>Catalysis layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'7f72bb2c-3f97-4f2e-895e-c875263455ff'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>end_shorten</code> <code>float</code> <p>The length the end of the arc will be shorten by</p> <code>0.0</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the arc</p> <code>None</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the arc</p> <code>None</code> <code>path_fill</code> <code>NoneValueType | Color | None</code> <p>The path fill color of the arc</p> <code>&lt;momapy.drawing.NoneValueType object at 0x7f8b17323e00&gt;</code> <code>path_filter</code> <code>NoneValueType | Filter | None</code> <p>The path filter of the arc</p> <code>None</code> <code>path_stroke</code> <code>NoneValueType | Color | None</code> <p>The path stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>path_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The path stroke dasharray of the arc</p> <code>None</code> <code>path_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The path stroke dashoffset of the arc</p> <code>None</code> <code>path_stroke_width</code> <code>float | None</code> <p>The path stroke width of the arc</p> <code>1.0</code> <code>path_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The path transform of the arc</p> <code>None</code> <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the arc</p> <code>None</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the arc</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the arc</p> <code>None</code> <code>stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the arc</p> <code>None</code> <code>segments</code> <code>tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...]</code> <p>The path segments of the arc</p> <code>&lt;dynamic&gt;</code> <code>source</code> <code>LayoutElement | None</code> <p>The source of the arc</p> <code>None</code> <code>start_shorten</code> <code>float</code> <p>The length the start of the arc will be shorten by</p> <code>0.0</code> <code>target</code> <code>LayoutElement | None</code> <p>The target of the arc</p> <code>None</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the arc</p> <code>None</code> <code>arrowhead_fill</code> <code>NoneValueType | Color | None</code> <p>The arrowhead fill color of the arc</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>arrowhead_filter</code> <code>NoneValueType | Filter | None</code> <p>The arrowhead filter of the arc</p> <code>None</code> <code>arrowhead_stroke</code> <code>NoneValueType | Color | None</code> <p>The arrowhead stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>arrowhead_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The arrowhead stroke dasharray of the arc</p> <code>None</code> <code>arrowhead_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The arrowhead stroke dashoffset of the arc</p> <code>None</code> <code>arrowhead_stroke_width</code> <code>float | None</code> <p>The arrowhead stroke width of the arc</p> <code>1.0</code> <code>arrowhead_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The arrowhead transform of the arc</p> <code>None</code> <code>arrowhead_height</code> <code>float</code> <p>The height of the arrowhead.</p> <code>7.0</code> <code>arrowhead_width</code> <code>float</code> <p>The width of the arrowhead.</p> <code>7.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>arrowhead_base</code> <p>Return the arrowhead base anchor point of the single-headed arc.</p> <code>arrowhead_bbox</code> <p>Return the bounding box of the single-headed arc arrowhead.</p> <code>arrowhead_border</code> <p>Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.</p> <code>arrowhead_drawing_elements</code> <p>Return the drawing elements of the single-headed arc arrowhead.</p> <code>arrowhead_length</code> <p>Return the length of the single-headed arc arrowhead.</p> <code>arrowhead_tip</code> <p>Return the arrowhead tip anchor point of the single-headed arc.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>childless</code> <p>Return a copy of the arc with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>end_point</code> <p>Return the ending point of the arc.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>fraction</code> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <code>length</code> <p>Return the total length of the arc path.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_children</code> <p>Return the self children of the arc.</p> <code>own_drawing_elements</code> <p>Return the arc's own drawing elements, including those of its mixins.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>path_drawing_elements</code> <p>Return the drawing elements of the single-headed arc path.</p> <code>points</code> <p>Return the points of the arc path.</p> <code>start_point</code> <p>Return the starting point of the arc.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CatalysisLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CatalysisLayout.arrowhead_base","title":"arrowhead_base","text":"<pre><code>arrowhead_base() -&gt; Point\n</code></pre> <p>Return the arrowhead base anchor point of the single-headed arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_base(self) -&gt; Point:\n    \"\"\"Return the arrowhead base anchor point of the single-headed arc.\"\"\"\n    arrowhead_length = self.arrowhead_length()\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return self.arrowhead_tip() - (arrowhead_length, 0)\n    fraction = 1 - (arrowhead_length + self.end_shorten) / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CatalysisLayout.arrowhead_bbox","title":"arrowhead_bbox","text":"<pre><code>arrowhead_bbox() -&gt; Bbox\n</code></pre> <p>Return the bounding box of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_bbox(self) -&gt; Bbox:\n    \"\"\"Return the bounding box of the single-headed arc arrowhead.\"\"\"\n    return get_drawing_elements_bbox(self.arrowhead_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CatalysisLayout.arrowhead_border","title":"arrowhead_border","text":"<pre><code>arrowhead_border(point: Point) -&gt; Point\n</code></pre> <p>Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_border(self, point: Point) -&gt; Point:\n    \"\"\"Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    point = get_drawing_elements_border(self.arrowhead_drawing_elements(), point)\n    if point is None:\n        return self.arrowhead_tip()\n    return point\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CatalysisLayout.arrowhead_drawing_elements","title":"arrowhead_drawing_elements","text":"<pre><code>arrowhead_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_drawing_elements(\n    self,\n) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the single-headed arc arrowhead.\"\"\"\n    drawing_elements = self._arrowhead_border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_arrowhead\",\n        elements=tuple(drawing_elements),\n        fill=self.arrowhead_fill,\n        filter_=self.arrowhead_filter,\n        id_=f\"{self.id_}_arrowhead\",\n        stroke=self.arrowhead_stroke,\n        stroke_dasharray=self.arrowhead_stroke_dasharray,\n        stroke_dashoffset=self.arrowhead_stroke_dashoffset,\n        stroke_width=self.arrowhead_stroke_width,\n        transform=self.arrowhead_transform,\n    )\n    transformation = self._get_arrowhead_transformation()\n    group = group.transformed(transformation)\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CatalysisLayout.arrowhead_length","title":"arrowhead_length","text":"<pre><code>arrowhead_length() -&gt; float\n</code></pre> <p>Return the length of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_length(self) -&gt; float:\n    \"\"\"Return the length of the single-headed arc arrowhead.\"\"\"\n    bbox = get_drawing_elements_bbox(self._arrowhead_border_drawing_elements())\n    if math.isnan(bbox.width):\n        return 0.0\n    return bbox.east().x\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CatalysisLayout.arrowhead_tip","title":"arrowhead_tip","text":"<pre><code>arrowhead_tip() -&gt; Point\n</code></pre> <p>Return the arrowhead tip anchor point of the single-headed arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_tip(self) -&gt; Point:\n    \"\"\"Return the arrowhead tip anchor point of the single-headed arc.\"\"\"\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return segment.p2\n    fraction = 1 - self.end_shorten / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CatalysisLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CatalysisLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the arc with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the arc with no children.\"\"\"\n    return dataclasses.replace(self, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CatalysisLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CatalysisLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CatalysisLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CatalysisLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CatalysisLayout.end_point","title":"end_point","text":"<pre><code>end_point() -&gt; Point\n</code></pre> <p>Return the ending point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_point(self) -&gt; Point:\n    \"\"\"Return the ending point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[-1]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CatalysisLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CatalysisLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CatalysisLayout.fraction","title":"fraction","text":"<pre><code>fraction(fraction: float) -&gt; tuple[Point, float]\n</code></pre> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def fraction(self, fraction: float) -&gt; tuple[Point, float]:\n    \"\"\"Return the position and angle on the arc at a given fraction (of the total arc length).\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    current_length = 0\n    length_to_reach = fraction * self.length()\n    for segment in self.segments:\n        current_length += segment.length()\n        if current_length &gt;= length_to_reach:\n            break\n    segment_start_length = current_length - segment.length()\n    segment_fraction = (length_to_reach - segment_start_length) / segment.length()\n    position, angle = segment.get_position_and_angle_at_fraction(segment_fraction)\n    return position, angle\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CatalysisLayout.length","title":"length","text":"<pre><code>length() -&gt; float\n</code></pre> <p>Return the total length of the arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def length(self) -&gt; float:\n    \"\"\"Return the total length of the arc path.\"\"\"\n    return sum([segment.length() for segment in self.segments])\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CatalysisLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CatalysisLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the arc.\"\"\"\n    return []\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CatalysisLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the arc's own drawing elements, including those of its mixins.</p> Source code in <code>src/momapy/celldesigner/elements.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the arc's own drawing elements, including those of its mixins.\"\"\"\n    drawing_elements = super_or_builder(\n        CellDesignerSingleHeadedArc, self\n    ).own_drawing_elements()\n    done_bases = []\n    for base in type(self).__mro__:\n        if (\n            issubclass_or_builder(base, _SBGNMixin)\n            and base is not type(self)\n            and not any([issubclass(done_base, base) for done_base in done_bases])\n        ):\n            drawing_elements += getattr(base, \"_mixin_drawing_elements\")(self)\n            done_bases.append(base)\n    return drawing_elements\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CatalysisLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CatalysisLayout.path_drawing_elements","title":"path_drawing_elements","text":"<pre><code>path_drawing_elements() -&gt; list[Path]\n</code></pre> <p>Return the drawing elements of the single-headed arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def path_drawing_elements(self) -&gt; list[Path]:\n    \"\"\"Return the drawing elements of the single-headed arc path.\"\"\"\n    arrowhead_length = self.arrowhead_length()\n    if len(self.segments) == 1:\n        segment = (\n            self.segments[0]\n            .shortened(self.start_shorten, \"start\")\n            .shortened(self.end_shorten + arrowhead_length, \"end\")\n        )\n        actions = [\n            MoveTo(segment.p1),\n            self._make_path_action_from_segment(segment),\n        ]\n    else:\n        first_segment = self.segments[0].shortened(self.start_shorten, \"start\")\n        last_segment = self.segments[-1].shortened(\n            self.end_shorten + arrowhead_length, \"end\"\n        )\n        actions = [\n            MoveTo(first_segment.p1),\n            self._make_path_action_from_segment(first_segment),\n        ]\n        for segment in self.segments[1:-1]:\n            action = self._make_path_action_from_segment(segment)\n            actions.append(action)\n        actions.append(self._make_path_action_from_segment(last_segment))\n    path = Path(\n        actions=tuple(actions),\n        class_=f\"{type(self).__name__}_path\",\n        fill=self.path_fill,\n        filter_=self.path_filter,\n        id_=f\"{self.id_}_path\",\n        stroke=self.path_stroke,\n        stroke_dasharray=self.path_stroke_dasharray,\n        stroke_dashoffset=self.path_stroke_dashoffset,\n        stroke_width=self.path_stroke_width,\n        transform=self.path_transform,\n    )\n    return [path]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CatalysisLayout.points","title":"points","text":"<pre><code>points() -&gt; list[Point]\n</code></pre> <p>Return the points of the arc path.</p> <p>An arc with no segments has no points and returns an empty list.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def points(self) -&gt; list[Point]:\n    \"\"\"Return the points of the arc path.\n\n    An arc with no segments has no points and returns an empty list.\n    \"\"\"\n    if not self.segments:\n        return []\n    points = []\n    for segment in self.segments:\n        points.append(segment.p1)\n    points.append(segment.p2)\n    return points\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CatalysisLayout.start_point","title":"start_point","text":"<pre><code>start_point() -&gt; Point\n</code></pre> <p>Return the starting point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_point(self) -&gt; Point:\n    \"\"\"Return the starting point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[0]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CatalysisLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.Catalyzer","title":"Catalyzer  <code>dataclass</code>","text":"<pre><code>Catalyzer(*, id_: str = make_uuid4_as_str(), name: str | None = None, sbo_term: str | None = None, metaid: str | None = None, referred_element: Species | BooleanLogicGate)\n</code></pre> <p>               Bases: <code>PhysicalStimulator</code></p> <p>Catalyzer.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'1a8c94f4-40e3-4fd2-851a-fd1707928249'</code> <code>name</code> <code>str | None</code> <p>Human-readable name of the element.</p> <code>None</code> <code>sbo_term</code> <code>str | None</code> <p>Optional SBO term identifier for semantic annotation.</p> <code>None</code> <code>metaid</code> <code>str | None</code> <p>Optional metadata identifier for RDF annotations.</p> <code>None</code> <code>referred_element</code> <code>Species | BooleanLogicGate</code> <p>The species or boolean logic gate the modifier refers to</p> required <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.Catalyzer.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerDoubleHeadedArc","title":"CellDesignerDoubleHeadedArc  <code>dataclass</code>","text":"<pre><code>CellDesignerDoubleHeadedArc(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, end_shorten: float = 0.0, fill: NoneValueType | Color | None = None, filter_: NoneValueType | Filter | None = None, path_fill: NoneValueType | Color | None = NoneValue, path_filter: NoneValueType | Filter | None = None, path_stroke: NoneValueType | Color | None = black, path_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, path_stroke_dashoffset: NoneValueType | float | None = None, path_stroke_width: float | None = 1.0, path_transform: NoneValueType | tuple[Transformation, ...] | None = None, stroke: NoneValueType | Color | None = None, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: NoneValueType | float | None = None, segments: tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...] = tuple(), source: LayoutElement | None = None, start_shorten: float = 0.0, target: LayoutElement | None = None, transform: NoneValueType | tuple[Transformation, ...] | None = None, end_arrowhead_fill: NoneValueType | Color | None = None, end_arrowhead_filter: NoneValueType | Filter | None = None, end_arrowhead_stroke: NoneValueType | Color | None = None, end_arrowhead_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, end_arrowhead_stroke_dashoffset: NoneValueType | float | None = None, end_arrowhead_stroke_width: NoneValueType | float | None = None, end_arrowhead_transform: NoneValueType | tuple[Transformation, ...] | None = None, start_arrowhead_fill: NoneValueType | Color | None = None, start_arrowhead_filter: NoneValueType | Filter | None = None, start_arrowhead_stroke: NoneValueType | Color | None = None, start_arrowhead_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, start_arrowhead_stroke_dashoffset: NoneValueType | float | None = None, start_arrowhead_stroke_width: NoneValueType | float | None = None, start_arrowhead_transform: NoneValueType | tuple[Transformation, ...] | None = None)\n</code></pre> <p>               Bases: <code>DoubleHeadedArc</code></p> <p>Abstract base class for CellDesigner double-headed arcs.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'748a1c05-1d19-4bff-9005-79b2241f0d40'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>end_shorten</code> <code>float</code> <p>The length the end of the arc will be shorten by</p> <code>0.0</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the arc</p> <code>None</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the arc</p> <code>None</code> <code>path_fill</code> <code>NoneValueType | Color | None</code> <p>The path fill color of the arc</p> <code>&lt;momapy.drawing.NoneValueType object at 0x7f8b17323e00&gt;</code> <code>path_filter</code> <code>NoneValueType | Filter | None</code> <p>The path filter of the arc</p> <code>None</code> <code>path_stroke</code> <code>NoneValueType | Color | None</code> <p>The path stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>path_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The path stroke dasharray of the arc</p> <code>None</code> <code>path_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The path stroke dashoffset of the arc</p> <code>None</code> <code>path_stroke_width</code> <code>float | None</code> <p>The path stroke width of the arc</p> <code>1.0</code> <code>path_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The path transform of the arc</p> <code>None</code> <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the arc</p> <code>None</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the arc</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the arc</p> <code>None</code> <code>stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the arc</p> <code>None</code> <code>segments</code> <code>tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...]</code> <p>The path segments of the arc</p> <code>&lt;dynamic&gt;</code> <code>source</code> <code>LayoutElement | None</code> <p>The source of the arc</p> <code>None</code> <code>start_shorten</code> <code>float</code> <p>The length the start of the arc will be shorten by</p> <code>0.0</code> <code>target</code> <code>LayoutElement | None</code> <p>The target of the arc</p> <code>None</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the arc</p> <code>None</code> <code>end_arrowhead_fill</code> <code>NoneValueType | Color | None</code> <p>The end arrowhead fill color of the arc</p> <code>None</code> <code>end_arrowhead_filter</code> <code>NoneValueType | Filter | None</code> <p>The end arrowhead filter of the arc</p> <code>None</code> <code>end_arrowhead_stroke</code> <code>NoneValueType | Color | None</code> <p>The end arrowhead stroke color of the arc</p> <code>None</code> <code>end_arrowhead_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The end arrowhead stroke dasharray of the arc</p> <code>None</code> <code>end_arrowhead_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The end arrowhead stroke dashoffset of the arc</p> <code>None</code> <code>end_arrowhead_stroke_width</code> <code>NoneValueType | float | None</code> <p>The end arrowhead stroke width of the arc</p> <code>None</code> <code>end_arrowhead_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The end arrowhead transform of the arc</p> <code>None</code> <code>start_arrowhead_fill</code> <code>NoneValueType | Color | None</code> <p>The start arrowhead fill color of the arc</p> <code>None</code> <code>start_arrowhead_filter</code> <code>NoneValueType | Filter | None</code> <p>The start arrowhead filter of the arc</p> <code>None</code> <code>start_arrowhead_stroke</code> <code>NoneValueType | Color | None</code> <p>The start arrowhead stroke color of the arc</p> <code>None</code> <code>start_arrowhead_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The start arrowhead stroke dasharray of the arc</p> <code>None</code> <code>start_arrowhead_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The start arrowhead stroke dashoffset of the arc</p> <code>None</code> <code>start_arrowhead_stroke_width</code> <code>NoneValueType | float | None</code> <p>The start arrowhead stroke width of the arc</p> <code>None</code> <code>start_arrowhead_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The start arrowhead transform of the arc</p> <code>None</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>childless</code> <p>Return a copy of the arc with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>end_arrowhead_base</code> <p>Return the base anchor point of the double-headed arc end arrowhead.</p> <code>end_arrowhead_bbox</code> <p>Return the bounding box of the double-headed arc end arrowhead.</p> <code>end_arrowhead_border</code> <p>Return the point at the intersection of the drawing elements of the double-headed arc end arrowhead and the line going through the center of these drawing elements and the given point.</p> <code>end_arrowhead_drawing_elements</code> <p>Return the drawing elements of the double-headed arc end arrowhead.</p> <code>end_arrowhead_length</code> <p>Return the length of the double-headed arc end arrowhead.</p> <code>end_arrowhead_tip</code> <p>Return the tip anchor point of the double-headed arc end arrowhead.</p> <code>end_point</code> <p>Return the ending point of the arc.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>fraction</code> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <code>length</code> <p>Return the total length of the arc path.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_children</code> <p>Return the self children of the arc.</p> <code>own_drawing_elements</code> <p>Return the arc's own drawing elements, including those of its mixins.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>path_drawing_elements</code> <p>Return the drawing elements of the double-headed arc path.</p> <code>points</code> <p>Return the points of the arc path.</p> <code>start_arrowhead_base</code> <p>Return the base anchor point of the double-headed arc start arrowhead.</p> <code>start_arrowhead_bbox</code> <p>Return the bounding box of the double-headed arc start arrowhead.</p> <code>start_arrowhead_border</code> <p>Return the point at the intersection of the drawing elements of the double-headed arc start arrowhead and the line going through the center of these drawing elements and the given point.</p> <code>start_arrowhead_drawing_elements</code> <p>Return the drawing elements of the double-headed arc start arrowhead.</p> <code>start_arrowhead_length</code> <p>Return the length of the double-headed arc start arrowhead.</p> <code>start_arrowhead_tip</code> <p>Return the tip anchor point of the double-headed arc start arrowhead.</p> <code>start_point</code> <p>Return the starting point of the arc.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerDoubleHeadedArc.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerDoubleHeadedArc.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerDoubleHeadedArc.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the arc with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the arc with no children.\"\"\"\n    return dataclasses.replace(self, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerDoubleHeadedArc.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerDoubleHeadedArc.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerDoubleHeadedArc.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerDoubleHeadedArc.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerDoubleHeadedArc.end_arrowhead_base","title":"end_arrowhead_base","text":"<pre><code>end_arrowhead_base() -&gt; Point\n</code></pre> <p>Return the base anchor point of the double-headed arc end arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_arrowhead_base(self) -&gt; Point:\n    \"\"\"Return the base anchor point of the double-headed arc end arrowhead.\"\"\"\n    arrowhead_length = self.end_arrowhead_length()\n    if arrowhead_length == 0:\n        return self.end_point()\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return self.end_arrowhead_tip() - (arrowhead_length, 0)\n    fraction = 1 - (arrowhead_length + self.end_shorten) / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerDoubleHeadedArc.end_arrowhead_bbox","title":"end_arrowhead_bbox","text":"<pre><code>end_arrowhead_bbox() -&gt; Bbox\n</code></pre> <p>Return the bounding box of the double-headed arc end arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_arrowhead_bbox(self) -&gt; Bbox:\n    \"\"\"Return the bounding box of the double-headed arc end arrowhead.\"\"\"\n    return get_drawing_elements_bbox(self.end_arrowhead_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerDoubleHeadedArc.end_arrowhead_border","title":"end_arrowhead_border","text":"<pre><code>end_arrowhead_border(point: Point) -&gt; Point\n</code></pre> <p>Return the point at the intersection of the drawing elements of the double-headed arc end arrowhead and the line going through the center of these drawing elements and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_arrowhead_border(self, point: Point) -&gt; Point:\n    \"\"\"Return the point at the intersection of the drawing elements of the double-headed arc end arrowhead and the line going through the center of these drawing elements and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    point = get_drawing_elements_border(\n        self.end_arrowhead_drawing_elements(), point\n    )\n    if point is None:\n        return self.end_arrowhead_tip()\n    return point\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerDoubleHeadedArc.end_arrowhead_drawing_elements","title":"end_arrowhead_drawing_elements","text":"<pre><code>end_arrowhead_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the double-headed arc end arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_arrowhead_drawing_elements(\n    self,\n) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the double-headed arc end arrowhead.\"\"\"\n    drawing_elements = self._end_arrowhead_border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_end_arrowhead\",\n        elements=tuple(drawing_elements),\n        fill=self.end_arrowhead_fill,\n        filter_=self.end_arrowhead_filter,\n        id_=f\"{self.id_}_end_arrowhead\",\n        stroke=self.end_arrowhead_stroke,\n        stroke_width=self.end_arrowhead_stroke_width,\n        stroke_dasharray=self.end_arrowhead_stroke_dasharray,\n        stroke_dashoffset=self.end_arrowhead_stroke_dashoffset,\n        transform=self.end_arrowhead_transform,\n    )\n    transformation = self._get_end_arrowhead_transformation()\n    group = group.transformed(transformation)\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerDoubleHeadedArc.end_arrowhead_length","title":"end_arrowhead_length","text":"<pre><code>end_arrowhead_length() -&gt; float\n</code></pre> <p>Return the length of the double-headed arc end arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_arrowhead_length(self) -&gt; float:\n    \"\"\"Return the length of the double-headed arc end arrowhead.\"\"\"\n    bbox = get_drawing_elements_bbox(self._end_arrowhead_border_drawing_elements())\n    if math.isnan(bbox.width):\n        return 0.0\n    return bbox.east().x\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerDoubleHeadedArc.end_arrowhead_tip","title":"end_arrowhead_tip","text":"<pre><code>end_arrowhead_tip() -&gt; Point\n</code></pre> <p>Return the tip anchor point of the double-headed arc end arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_arrowhead_tip(self) -&gt; Point:\n    \"\"\"Return the tip anchor point of the double-headed arc end arrowhead.\"\"\"\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return segment.p2\n    fraction = 1 - self.end_shorten / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerDoubleHeadedArc.end_point","title":"end_point","text":"<pre><code>end_point() -&gt; Point\n</code></pre> <p>Return the ending point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_point(self) -&gt; Point:\n    \"\"\"Return the ending point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[-1]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerDoubleHeadedArc.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerDoubleHeadedArc.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerDoubleHeadedArc.fraction","title":"fraction","text":"<pre><code>fraction(fraction: float) -&gt; tuple[Point, float]\n</code></pre> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def fraction(self, fraction: float) -&gt; tuple[Point, float]:\n    \"\"\"Return the position and angle on the arc at a given fraction (of the total arc length).\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    current_length = 0\n    length_to_reach = fraction * self.length()\n    for segment in self.segments:\n        current_length += segment.length()\n        if current_length &gt;= length_to_reach:\n            break\n    segment_start_length = current_length - segment.length()\n    segment_fraction = (length_to_reach - segment_start_length) / segment.length()\n    position, angle = segment.get_position_and_angle_at_fraction(segment_fraction)\n    return position, angle\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerDoubleHeadedArc.length","title":"length","text":"<pre><code>length() -&gt; float\n</code></pre> <p>Return the total length of the arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def length(self) -&gt; float:\n    \"\"\"Return the total length of the arc path.\"\"\"\n    return sum([segment.length() for segment in self.segments])\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerDoubleHeadedArc.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerDoubleHeadedArc.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the arc.\"\"\"\n    return []\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerDoubleHeadedArc.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the arc's own drawing elements, including those of its mixins.</p> Source code in <code>src/momapy/celldesigner/elements.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the arc's own drawing elements, including those of its mixins.\"\"\"\n    drawing_elements = super_or_builder(\n        CellDesignerDoubleHeadedArc, self\n    ).own_drawing_elements()\n    done_bases = []\n    for base in type(self).__mro__:\n        if (\n            issubclass_or_builder(base, _SBGNMixin)\n            and base is not type(self)\n            and not any([issubclass(done_base, base) for done_base in done_bases])\n        ):\n            drawing_elements += getattr(base, \"_mixin_drawing_elements\")(self)\n            done_bases.append(base)\n    return drawing_elements\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerDoubleHeadedArc.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerDoubleHeadedArc.path_drawing_elements","title":"path_drawing_elements","text":"<pre><code>path_drawing_elements() -&gt; list[Path]\n</code></pre> <p>Return the drawing elements of the double-headed arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def path_drawing_elements(self) -&gt; list[Path]:\n    \"\"\"Return the drawing elements of the double-headed arc path.\"\"\"\n    start_arrowhead_length = self.start_arrowhead_length()\n    end_arrowhead_length = self.end_arrowhead_length()\n    if len(self.segments) == 1:\n        segment = (\n            self.segments[0]\n            .shortened(self.start_shorten + start_arrowhead_length, \"start\")\n            .shortened(self.end_shorten + end_arrowhead_length, \"end\")\n        )\n        actions = [\n            MoveTo(segment.p1),\n            self._make_path_action_from_segment(segment),\n        ]\n    else:\n        first_segment = self.segments[0].shortened(\n            self.start_shorten + start_arrowhead_length, \"start\"\n        )\n        last_segment = self.segments[-1].shortened(\n            self.end_shorten + end_arrowhead_length, \"end\"\n        )\n        actions = [\n            MoveTo(first_segment.p1),\n            self._make_path_action_from_segment(first_segment),\n        ]\n        for segment in self.segments[1:-1]:\n            action = self._make_path_action_from_segment(segment)\n            actions.append(action)\n        actions.append(self._make_path_action_from_segment(last_segment))\n    path = Path(\n        actions=tuple(actions),\n        class_=f\"{type(self).__name__}_path\",\n        fill=self.path_fill,\n        filter_=self.path_filter,\n        id_=f\"{self.id_}_path\",\n        stroke=self.path_stroke,\n        stroke_dasharray=self.path_stroke_dasharray,\n        stroke_dashoffset=self.path_stroke_dashoffset,\n        stroke_width=self.path_stroke_width,\n        transform=self.path_transform,\n    )\n    return [path]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerDoubleHeadedArc.points","title":"points","text":"<pre><code>points() -&gt; list[Point]\n</code></pre> <p>Return the points of the arc path.</p> <p>An arc with no segments has no points and returns an empty list.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def points(self) -&gt; list[Point]:\n    \"\"\"Return the points of the arc path.\n\n    An arc with no segments has no points and returns an empty list.\n    \"\"\"\n    if not self.segments:\n        return []\n    points = []\n    for segment in self.segments:\n        points.append(segment.p1)\n    points.append(segment.p2)\n    return points\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerDoubleHeadedArc.start_arrowhead_base","title":"start_arrowhead_base","text":"<pre><code>start_arrowhead_base() -&gt; Point\n</code></pre> <p>Return the base anchor point of the double-headed arc start arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_arrowhead_base(self) -&gt; Point:\n    \"\"\"Return the base anchor point of the double-headed arc start arrowhead.\"\"\"\n    arrowhead_length = self.start_arrowhead_length()\n    if arrowhead_length == 0:\n        return self.start_point()\n    segment = self.segments[0]\n    segment = Segment(segment.p2, segment.p1)\n    segment_length = segment.length()\n    if segment_length == 0:\n        return self.start_arrowhead_tip() + (arrowhead_length, 0)\n    fraction = 1 - (arrowhead_length + self.start_shorten) / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerDoubleHeadedArc.start_arrowhead_bbox","title":"start_arrowhead_bbox","text":"<pre><code>start_arrowhead_bbox() -&gt; Bbox\n</code></pre> <p>Return the bounding box of the double-headed arc start arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_arrowhead_bbox(self) -&gt; Bbox:\n    \"\"\"Return the bounding box of the double-headed arc start arrowhead.\"\"\"\n    return get_drawing_elements_bbox(self.start_arrowhead_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerDoubleHeadedArc.start_arrowhead_border","title":"start_arrowhead_border","text":"<pre><code>start_arrowhead_border(point: Point) -&gt; Point\n</code></pre> <p>Return the point at the intersection of the drawing elements of the double-headed arc start arrowhead and the line going through the center of these drawing elements and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_arrowhead_border(self, point: Point) -&gt; Point:\n    \"\"\"Return the point at the intersection of the drawing elements of the double-headed arc start arrowhead and the line going through the center of these drawing elements and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    point = get_drawing_elements_border(\n        self.start_arrowhead_drawing_elements(), point\n    )\n    if point is None:\n        return self.start_arrowhead_tip()\n    return point\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerDoubleHeadedArc.start_arrowhead_drawing_elements","title":"start_arrowhead_drawing_elements","text":"<pre><code>start_arrowhead_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the double-headed arc start arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_arrowhead_drawing_elements(\n    self,\n) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the double-headed arc start arrowhead.\"\"\"\n    drawing_elements = self._start_arrowhead_border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_start_arrowhead\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}_start_arrowhead\",\n        fill=self.start_arrowhead_fill,\n        filter_=self.start_arrowhead_filter,\n        stroke=self.start_arrowhead_stroke,\n        stroke_dasharray=self.start_arrowhead_stroke_dasharray,\n        stroke_dashoffset=self.start_arrowhead_stroke_dashoffset,\n        stroke_width=self.start_arrowhead_stroke_width,\n        transform=self.start_arrowhead_transform,\n    )\n    transformation = self._get_start_arrowhead_transformation()\n    group = group.transformed(transformation)\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerDoubleHeadedArc.start_arrowhead_length","title":"start_arrowhead_length","text":"<pre><code>start_arrowhead_length() -&gt; float\n</code></pre> <p>Return the length of the double-headed arc start arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_arrowhead_length(self) -&gt; float:\n    \"\"\"Return the length of the double-headed arc start arrowhead.\"\"\"\n    bbox = get_drawing_elements_bbox(\n        self._start_arrowhead_border_drawing_elements()\n    )\n    if math.isnan(bbox.width):\n        return 0.0\n    return abs(bbox.west().x)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerDoubleHeadedArc.start_arrowhead_tip","title":"start_arrowhead_tip","text":"<pre><code>start_arrowhead_tip() -&gt; Point\n</code></pre> <p>Return the tip anchor point of the double-headed arc start arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_arrowhead_tip(self) -&gt; Point:\n    \"\"\"Return the tip anchor point of the double-headed arc start arrowhead.\"\"\"\n    segment = self.segments[0]\n    segment = Segment(segment.p2, segment.p1)\n    segment_length = segment.length()\n    if segment_length == 0:\n        return segment.p2\n    fraction = 1 - self.start_shorten / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerDoubleHeadedArc.start_point","title":"start_point","text":"<pre><code>start_point() -&gt; Point\n</code></pre> <p>Return the starting point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_point(self) -&gt; Point:\n    \"\"\"Return the starting point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[0]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerDoubleHeadedArc.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerLayout","title":"CellDesignerLayout  <code>dataclass</code>","text":"<pre><code>CellDesignerLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = white, filter_: NoneValueType | Filter | None = None, height: float, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = None, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: NoneValueType | float | None = None, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float)\n</code></pre> <p>               Bases: <code>Layout</code></p> <p>CellDesigner layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'e24f1702-14d7-4d42-814c-e2edf9dc7e36'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> required <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>None</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the node</p> <code>None</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> required <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>angle</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Return the east anchor of the node.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>is_sublayout</code> <p>Return <code>True</code> if the layout is a sublayout of another given layout, <code>False</code> otherwise.</p> <code>label_center</code> <p>Return the label center anchor of the node.</p> <code>north</code> <p>Return the north anchor of the node.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Return the south anchor of the node.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Return the west anchor of the node.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerLayout.angle","title":"angle","text":"<pre><code>angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Return the east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Return the east anchor of the node.\"\"\"\n    return self.own_angle(0)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerLayout.is_sublayout","title":"is_sublayout","text":"<pre><code>is_sublayout(other: Layout, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout is a sublayout of another given layout, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def is_sublayout(\n    self, other: \"Layout\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout is a sublayout of another given layout, `False` otherwise.\"\"\"\n\n    def _is_sublist(\n        list1: list[LayoutElement],\n        list2: list[LayoutElement],\n        unordered: bool = False,\n    ) -&gt; bool:\n        if not unordered:\n            i = 0\n            for elem1 in list1:\n                elem2 = list2[i]\n                while elem2 != elem1 and i &lt; len(list2) - 1:\n                    i += 1\n                    elem2 = list2[i]\n                if not elem2 == elem1:\n                    return False\n                i += 1\n        else:\n            dlist1 = collections.defaultdict(int)\n            dlist2 = collections.defaultdict(int)\n            for elem1 in list1:\n                dlist1[elem1] += 1\n            for elem2 in list2:\n                dlist2[elem2] += 1\n            for elem in dlist1:\n                if dlist1[elem] &gt; dlist2[elem]:\n                    return False\n        return True\n\n    if self.childless() != other.childless():\n        return False\n    if flattened:\n        return _is_sublist(\n            self.flattened()[1:],\n            other.flattened()[1:],\n            unordered=unordered,\n        )\n    return _is_sublist(self.children(), other.children(), unordered=unordered)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Return the label center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Return the label center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Return the north anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Return the north anchor of the node.\"\"\"\n    return self.own_angle(90)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Return the south anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Return the south anchor of the node.\"\"\"\n    return self.own_angle(270)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Return the west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Return the west anchor of the node.\"\"\"\n    return self.own_angle(180)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerMap","title":"CellDesignerMap  <code>dataclass</code>","text":"<pre><code>CellDesignerMap(*, id_: str = make_uuid4_as_str(), model: CellDesignerModel | None = None, layout: CellDesignerLayout | None = None, layout_model_mapping: LayoutModelMapping | None = None)\n</code></pre> <p>               Bases: <code>Map</code></p> <p>Class for CellDesigner maps.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'da210358-c220-4b20-8b31-8fc9903b163f'</code> <code>model</code> <code>CellDesignerModel | None</code> <p>The model of the map</p> <code>None</code> <code>layout</code> <code>CellDesignerLayout | None</code> <p>The layout of the map</p> <code>None</code> <code>layout_model_mapping</code> <code>LayoutModelMapping | None</code> <p>The layout model mapping of the map</p> <code>None</code> <p>Methods:</p> Name Description <code>get_mapping</code> <p>Return the model element or layout elements mapped to <code>map_element</code>.</p> <code>is_submap</code> <p>Return <code>True</code> if the <code>Map</code> is a submap of another given map, <code>False</code> otherwise.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerMap.get_mapping","title":"get_mapping","text":"<pre><code>get_mapping(map_element: MapElement) -&gt; ModelElement | list[LayoutElement | frozenset[LayoutElement]] | None\n</code></pre> <p>Return the model element or layout elements mapped to <code>map_element</code>.</p> <p>The lookup is bidirectional: a layout key (a singleton or frozenset) resolves to its model element, and a model element resolves to the list of layout keys mapped to it. Forwards to <code>layout_model_mapping.get_mapping</code>.</p> <p>Returns <code>None</code> when the map has no <code>layout_model_mapping</code> (for example a layout-less map such as one read from SBML).</p> Source code in <code>src/momapy/core/map.py</code> <pre><code>def get_mapping(\n    self,\n    map_element: \"MapElement\",\n) -&gt; \"ModelElement | list[LayoutElement | frozenset[LayoutElement]] | None\":\n    \"\"\"Return the model element or layout elements mapped to `map_element`.\n\n    The lookup is bidirectional: a layout key (a singleton or frozenset)\n    resolves to its model element, and a model element resolves to the\n    list of layout keys mapped to it. Forwards to\n    `layout_model_mapping.get_mapping`.\n\n    Returns `None` when the map has no `layout_model_mapping` (for\n    example a layout-less map such as one read from SBML).\n    \"\"\"\n    if self.layout_model_mapping is None:\n        return None\n    return self.layout_model_mapping.get_mapping(map_element)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerMap.is_submap","title":"is_submap","text":"<pre><code>is_submap(other: Map) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the <code>Map</code> is a submap of another given map, <code>False</code> otherwise.</p> <p>A complete map is never a submap of an incomplete one: if either <code>self</code> or <code>other</code> has a <code>None</code> model, layout, or layout-model mapping, this returns <code>False</code>.</p> Source code in <code>src/momapy/core/map.py</code> <pre><code>def is_submap(self, other: \"Map\") -&gt; bool:\n    \"\"\"Return `True` if the `Map` is a submap of another given map, `False` otherwise.\n\n    A complete map is never a submap of an incomplete one: if either `self`\n    or `other` has a `None` model, layout, or layout-model mapping, this\n    returns `False`.\n    \"\"\"\n    if (\n        self.model is None\n        or self.layout is None\n        or self.layout_model_mapping is None\n        or other.model is None\n        or other.layout is None\n        or other.layout_model_mapping is None\n    ):\n        return False\n    return (\n        self.model.is_submodel(other.model)\n        and self.layout.is_sublayout(other.layout)\n        and self.layout_model_mapping.is_submapping(other.layout_model_mapping)\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerModel","title":"CellDesignerModel  <code>dataclass</code>","text":"<pre><code>CellDesignerModel(*, id_: str = make_uuid4_as_str(), name: str | None = None, sbo_term: str | None = None, metaid: str | None = None, compartments: frozenset[Compartment] = frozenset(), species: frozenset[Species] = frozenset(), reactions: frozenset[Reaction] = frozenset(), species_templates: frozenset[SpeciesTemplate] = frozenset(), boolean_logic_gates: frozenset[BooleanLogicGate] = frozenset(), modulations: frozenset[Modulation | UnknownModulation] = frozenset())\n</code></pre> <p>               Bases: <code>SBMLModel</code></p> <p>CellDesigner model.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'ad8efe1c-a1b3-4cec-8784-52c378fdb00c'</code> <code>name</code> <code>str | None</code> <p>Human-readable name of the model.</p> <code>None</code> <code>sbo_term</code> <code>str | None</code> <p>Optional SBO term identifier for semantic annotation.</p> <code>None</code> <code>metaid</code> <code>str | None</code> <p>Optional metadata identifier for RDF annotations.</p> <code>None</code> <code>compartments</code> <code>frozenset[Compartment]</code> <p>Build an immutable unordered collection of unique elements.</p> <code>&lt;dynamic&gt;</code> <code>species</code> <code>frozenset[Species]</code> <p>Build an immutable unordered collection of unique elements.</p> <code>&lt;dynamic&gt;</code> <code>reactions</code> <code>frozenset[Reaction]</code> <p>Build an immutable unordered collection of unique elements.</p> <code>&lt;dynamic&gt;</code> <code>species_templates</code> <code>frozenset[SpeciesTemplate]</code> <p>The species templates of the CellDesigner model</p> <code>&lt;dynamic&gt;</code> <code>boolean_logic_gates</code> <code>frozenset[BooleanLogicGate]</code> <p>The boolean logic gates of the CellDesigner model</p> <code>&lt;dynamic&gt;</code> <code>modulations</code> <code>frozenset[Modulation | UnknownModulation]</code> <p>The modulations of the CellDesigner model</p> <code>&lt;dynamic&gt;</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from this <code>Model</code>.</p> <code>is_submodel</code> <p>Check if this model is a submodel of another model.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerModel.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from this <code>Model</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers of the <code>Model</code>, deduplicating by object identity. The <code>Model</code> itself is not a <code>ModelElement</code> and is not included.</p> <p>This mirrors <code>ModelElement.descendants</code> and contrasts with <code>LayoutElement.descendants</code>, which walks the explicit <code>children()</code> tree (visual containment) rather than reference reachability.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order.</p> Source code in <code>src/momapy/core/model.py</code> <pre><code>def descendants(self) -&gt; list[ModelElement]:\n    \"\"\"Return every `ModelElement` reachable from this `Model`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers of the `Model`, deduplicating by\n    object identity. The `Model` itself is not a `ModelElement` and is\n    not included.\n\n    This mirrors\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and contrasts with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which walks the explicit `children()` tree (visual *containment*)\n    rather than reference reachability.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order.\n    \"\"\"\n    seen: set[int] = set()\n    result: list[ModelElement] = []\n    for field in dataclasses.fields(type(self)):\n        _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerModel.is_submodel","title":"is_submodel","text":"<pre><code>is_submodel(other: CellDesignerModel) -&gt; bool\n</code></pre> <p>Check if this model is a submodel of another model.</p> <p>Parameters:</p> Name Type Description Default <code>other</code> <code>CellDesignerModel</code> <p>Another CellDesigner model to compare against.</p> required <p>Returns:</p> Type Description <code>bool</code> <p>True if this model is a submodel of <code>other</code>, False otherwise.</p> Source code in <code>src/momapy/celldesigner/model.py</code> <pre><code>def is_submodel(self, other: \"CellDesignerModel\") -&gt; bool:\n    \"\"\"Check if this model is a submodel of another model.\n\n    Args:\n        other: Another CellDesigner model to compare against.\n\n    Returns:\n        True if this model is a submodel of `other`, False otherwise.\n    \"\"\"\n    return (\n        self.compartments.issubset(other.compartments)\n        and self.species.issubset(other.species)\n        and self.reactions.issubset(other.reactions)\n        and self.species_templates.issubset(other.species_templates)\n        and self.boolean_logic_gates.issubset(other.boolean_logic_gates)\n        and self.modulations.issubset(other.modulations)\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerModelElement","title":"CellDesignerModelElement  <code>dataclass</code>","text":"<pre><code>CellDesignerModelElement(*, id_: str = make_uuid4_as_str())\n</code></pre> <p>               Bases: <code>ModelElement</code></p> <p>Abstract base class for CellDesigner model elements.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'ca630cd8-30e0-4410-9073-8e4f93aef733'</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerModelElement.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerNode","title":"CellDesignerNode  <code>dataclass</code>","text":"<pre><code>CellDesignerNode(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = white, filter_: NoneValueType | Filter | None = None, height: float, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = 1.25, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float)\n</code></pre> <p>               Bases: <code>SBGNNode</code></p> <p>Abstract base class for CellDesigner nodes.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'1faae9bf-484c-467c-83fa-deb684836954'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> required <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The stroke width of the node</p> <code>1.25</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> required <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>angle</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Return the east anchor of the node.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Return the label center anchor of the node.</p> <code>north</code> <p>Return the north anchor of the node.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Return the south anchor of the node.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Return the west anchor of the node.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerNode.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerNode.angle","title":"angle","text":"<pre><code>angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerNode.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerNode.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerNode.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerNode.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerNode.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerNode.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerNode.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerNode.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerNode.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Return the east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Return the east anchor of the node.\"\"\"\n    return self.own_angle(0)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerNode.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerNode.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerNode.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerNode.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerNode.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Return the label center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Return the label center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerNode.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Return the north anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Return the north anchor of the node.\"\"\"\n    return self.own_angle(90)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerNode.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerNode.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerNode.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerNode.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerNode.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerNode.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerNode.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerNode.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerNode.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerNode.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerNode.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerNode.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Return the south anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Return the south anchor of the node.\"\"\"\n    return self.own_angle(270)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerNode.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerNode.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerNode.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerNode.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerNode.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerNode.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Return the west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Return the west anchor of the node.\"\"\"\n    return self.own_angle(180)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerNode.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerNode.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerNode.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerNode.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerSingleHeadedArc","title":"CellDesignerSingleHeadedArc  <code>dataclass</code>","text":"<pre><code>CellDesignerSingleHeadedArc(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, end_shorten: float = 0.0, fill: NoneValueType | Color | None = None, filter_: NoneValueType | Filter | None = None, path_fill: NoneValueType | Color | None = NoneValue, path_filter: NoneValueType | Filter | None = None, path_stroke: NoneValueType | Color | None = black, path_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, path_stroke_dashoffset: NoneValueType | float | None = None, path_stroke_width: float | None = 1.0, path_transform: NoneValueType | tuple[Transformation, ...] | None = None, stroke: NoneValueType | Color | None = None, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: NoneValueType | float | None = None, segments: tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...] = tuple(), source: LayoutElement | None = None, start_shorten: float = 0.0, target: LayoutElement | None = None, transform: NoneValueType | tuple[Transformation, ...] | None = None, arrowhead_fill: NoneValueType | Color | None = None, arrowhead_filter: NoneValueType | Filter | None = None, arrowhead_stroke: NoneValueType | Color | None = black, arrowhead_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, arrowhead_stroke_dashoffset: NoneValueType | float | None = None, arrowhead_stroke_width: float | None = 1.0, arrowhead_transform: NoneValueType | tuple[Transformation, ...] | None = None)\n</code></pre> <p>               Bases: <code>SingleHeadedArc</code></p> <p>Abstract base class for CellDesigner single-headed arcs.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'10ebe6b8-e428-40f3-80e9-edc2f1b10c11'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>end_shorten</code> <code>float</code> <p>The length the end of the arc will be shorten by</p> <code>0.0</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the arc</p> <code>None</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the arc</p> <code>None</code> <code>path_fill</code> <code>NoneValueType | Color | None</code> <p>The path fill color of the arc</p> <code>&lt;momapy.drawing.NoneValueType object at 0x7f8b17323e00&gt;</code> <code>path_filter</code> <code>NoneValueType | Filter | None</code> <p>The path filter of the arc</p> <code>None</code> <code>path_stroke</code> <code>NoneValueType | Color | None</code> <p>The path stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>path_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The path stroke dasharray of the arc</p> <code>None</code> <code>path_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The path stroke dashoffset of the arc</p> <code>None</code> <code>path_stroke_width</code> <code>float | None</code> <p>The path stroke width of the arc</p> <code>1.0</code> <code>path_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The path transform of the arc</p> <code>None</code> <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the arc</p> <code>None</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the arc</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the arc</p> <code>None</code> <code>stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the arc</p> <code>None</code> <code>segments</code> <code>tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...]</code> <p>The path segments of the arc</p> <code>&lt;dynamic&gt;</code> <code>source</code> <code>LayoutElement | None</code> <p>The source of the arc</p> <code>None</code> <code>start_shorten</code> <code>float</code> <p>The length the start of the arc will be shorten by</p> <code>0.0</code> <code>target</code> <code>LayoutElement | None</code> <p>The target of the arc</p> <code>None</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the arc</p> <code>None</code> <code>arrowhead_fill</code> <code>NoneValueType | Color | None</code> <p>The arrowhead fill color of the arc</p> <code>None</code> <code>arrowhead_filter</code> <code>NoneValueType | Filter | None</code> <p>The arrowhead filter of the arc</p> <code>None</code> <code>arrowhead_stroke</code> <code>NoneValueType | Color | None</code> <p>The arrowhead stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>arrowhead_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The arrowhead stroke dasharray of the arc</p> <code>None</code> <code>arrowhead_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The arrowhead stroke dashoffset of the arc</p> <code>None</code> <code>arrowhead_stroke_width</code> <code>float | None</code> <p>The arrowhead stroke width of the arc</p> <code>1.0</code> <code>arrowhead_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The arrowhead transform of the arc</p> <code>None</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>arrowhead_base</code> <p>Return the arrowhead base anchor point of the single-headed arc.</p> <code>arrowhead_bbox</code> <p>Return the bounding box of the single-headed arc arrowhead.</p> <code>arrowhead_border</code> <p>Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.</p> <code>arrowhead_drawing_elements</code> <p>Return the drawing elements of the single-headed arc arrowhead.</p> <code>arrowhead_length</code> <p>Return the length of the single-headed arc arrowhead.</p> <code>arrowhead_tip</code> <p>Return the arrowhead tip anchor point of the single-headed arc.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>childless</code> <p>Return a copy of the arc with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>end_point</code> <p>Return the ending point of the arc.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>fraction</code> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <code>length</code> <p>Return the total length of the arc path.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_children</code> <p>Return the self children of the arc.</p> <code>own_drawing_elements</code> <p>Return the arc's own drawing elements, including those of its mixins.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>path_drawing_elements</code> <p>Return the drawing elements of the single-headed arc path.</p> <code>points</code> <p>Return the points of the arc path.</p> <code>start_point</code> <p>Return the starting point of the arc.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerSingleHeadedArc.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerSingleHeadedArc.arrowhead_base","title":"arrowhead_base","text":"<pre><code>arrowhead_base() -&gt; Point\n</code></pre> <p>Return the arrowhead base anchor point of the single-headed arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_base(self) -&gt; Point:\n    \"\"\"Return the arrowhead base anchor point of the single-headed arc.\"\"\"\n    arrowhead_length = self.arrowhead_length()\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return self.arrowhead_tip() - (arrowhead_length, 0)\n    fraction = 1 - (arrowhead_length + self.end_shorten) / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerSingleHeadedArc.arrowhead_bbox","title":"arrowhead_bbox","text":"<pre><code>arrowhead_bbox() -&gt; Bbox\n</code></pre> <p>Return the bounding box of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_bbox(self) -&gt; Bbox:\n    \"\"\"Return the bounding box of the single-headed arc arrowhead.\"\"\"\n    return get_drawing_elements_bbox(self.arrowhead_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerSingleHeadedArc.arrowhead_border","title":"arrowhead_border","text":"<pre><code>arrowhead_border(point: Point) -&gt; Point\n</code></pre> <p>Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_border(self, point: Point) -&gt; Point:\n    \"\"\"Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    point = get_drawing_elements_border(self.arrowhead_drawing_elements(), point)\n    if point is None:\n        return self.arrowhead_tip()\n    return point\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerSingleHeadedArc.arrowhead_drawing_elements","title":"arrowhead_drawing_elements","text":"<pre><code>arrowhead_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_drawing_elements(\n    self,\n) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the single-headed arc arrowhead.\"\"\"\n    drawing_elements = self._arrowhead_border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_arrowhead\",\n        elements=tuple(drawing_elements),\n        fill=self.arrowhead_fill,\n        filter_=self.arrowhead_filter,\n        id_=f\"{self.id_}_arrowhead\",\n        stroke=self.arrowhead_stroke,\n        stroke_dasharray=self.arrowhead_stroke_dasharray,\n        stroke_dashoffset=self.arrowhead_stroke_dashoffset,\n        stroke_width=self.arrowhead_stroke_width,\n        transform=self.arrowhead_transform,\n    )\n    transformation = self._get_arrowhead_transformation()\n    group = group.transformed(transformation)\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerSingleHeadedArc.arrowhead_length","title":"arrowhead_length","text":"<pre><code>arrowhead_length() -&gt; float\n</code></pre> <p>Return the length of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_length(self) -&gt; float:\n    \"\"\"Return the length of the single-headed arc arrowhead.\"\"\"\n    bbox = get_drawing_elements_bbox(self._arrowhead_border_drawing_elements())\n    if math.isnan(bbox.width):\n        return 0.0\n    return bbox.east().x\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerSingleHeadedArc.arrowhead_tip","title":"arrowhead_tip","text":"<pre><code>arrowhead_tip() -&gt; Point\n</code></pre> <p>Return the arrowhead tip anchor point of the single-headed arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_tip(self) -&gt; Point:\n    \"\"\"Return the arrowhead tip anchor point of the single-headed arc.\"\"\"\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return segment.p2\n    fraction = 1 - self.end_shorten / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerSingleHeadedArc.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerSingleHeadedArc.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the arc with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the arc with no children.\"\"\"\n    return dataclasses.replace(self, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerSingleHeadedArc.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerSingleHeadedArc.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerSingleHeadedArc.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerSingleHeadedArc.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerSingleHeadedArc.end_point","title":"end_point","text":"<pre><code>end_point() -&gt; Point\n</code></pre> <p>Return the ending point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_point(self) -&gt; Point:\n    \"\"\"Return the ending point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[-1]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerSingleHeadedArc.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerSingleHeadedArc.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerSingleHeadedArc.fraction","title":"fraction","text":"<pre><code>fraction(fraction: float) -&gt; tuple[Point, float]\n</code></pre> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def fraction(self, fraction: float) -&gt; tuple[Point, float]:\n    \"\"\"Return the position and angle on the arc at a given fraction (of the total arc length).\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    current_length = 0\n    length_to_reach = fraction * self.length()\n    for segment in self.segments:\n        current_length += segment.length()\n        if current_length &gt;= length_to_reach:\n            break\n    segment_start_length = current_length - segment.length()\n    segment_fraction = (length_to_reach - segment_start_length) / segment.length()\n    position, angle = segment.get_position_and_angle_at_fraction(segment_fraction)\n    return position, angle\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerSingleHeadedArc.length","title":"length","text":"<pre><code>length() -&gt; float\n</code></pre> <p>Return the total length of the arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def length(self) -&gt; float:\n    \"\"\"Return the total length of the arc path.\"\"\"\n    return sum([segment.length() for segment in self.segments])\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerSingleHeadedArc.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerSingleHeadedArc.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the arc.\"\"\"\n    return []\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerSingleHeadedArc.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the arc's own drawing elements, including those of its mixins.</p> Source code in <code>src/momapy/celldesigner/elements.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the arc's own drawing elements, including those of its mixins.\"\"\"\n    drawing_elements = super_or_builder(\n        CellDesignerSingleHeadedArc, self\n    ).own_drawing_elements()\n    done_bases = []\n    for base in type(self).__mro__:\n        if (\n            issubclass_or_builder(base, _SBGNMixin)\n            and base is not type(self)\n            and not any([issubclass(done_base, base) for done_base in done_bases])\n        ):\n            drawing_elements += getattr(base, \"_mixin_drawing_elements\")(self)\n            done_bases.append(base)\n    return drawing_elements\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerSingleHeadedArc.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerSingleHeadedArc.path_drawing_elements","title":"path_drawing_elements","text":"<pre><code>path_drawing_elements() -&gt; list[Path]\n</code></pre> <p>Return the drawing elements of the single-headed arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def path_drawing_elements(self) -&gt; list[Path]:\n    \"\"\"Return the drawing elements of the single-headed arc path.\"\"\"\n    arrowhead_length = self.arrowhead_length()\n    if len(self.segments) == 1:\n        segment = (\n            self.segments[0]\n            .shortened(self.start_shorten, \"start\")\n            .shortened(self.end_shorten + arrowhead_length, \"end\")\n        )\n        actions = [\n            MoveTo(segment.p1),\n            self._make_path_action_from_segment(segment),\n        ]\n    else:\n        first_segment = self.segments[0].shortened(self.start_shorten, \"start\")\n        last_segment = self.segments[-1].shortened(\n            self.end_shorten + arrowhead_length, \"end\"\n        )\n        actions = [\n            MoveTo(first_segment.p1),\n            self._make_path_action_from_segment(first_segment),\n        ]\n        for segment in self.segments[1:-1]:\n            action = self._make_path_action_from_segment(segment)\n            actions.append(action)\n        actions.append(self._make_path_action_from_segment(last_segment))\n    path = Path(\n        actions=tuple(actions),\n        class_=f\"{type(self).__name__}_path\",\n        fill=self.path_fill,\n        filter_=self.path_filter,\n        id_=f\"{self.id_}_path\",\n        stroke=self.path_stroke,\n        stroke_dasharray=self.path_stroke_dasharray,\n        stroke_dashoffset=self.path_stroke_dashoffset,\n        stroke_width=self.path_stroke_width,\n        transform=self.path_transform,\n    )\n    return [path]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerSingleHeadedArc.points","title":"points","text":"<pre><code>points() -&gt; list[Point]\n</code></pre> <p>Return the points of the arc path.</p> <p>An arc with no segments has no points and returns an empty list.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def points(self) -&gt; list[Point]:\n    \"\"\"Return the points of the arc path.\n\n    An arc with no segments has no points and returns an empty list.\n    \"\"\"\n    if not self.segments:\n        return []\n    points = []\n    for segment in self.segments:\n        points.append(segment.p1)\n    points.append(segment.p2)\n    return points\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerSingleHeadedArc.start_point","title":"start_point","text":"<pre><code>start_point() -&gt; Point\n</code></pre> <p>Return the starting point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_point(self) -&gt; Point:\n    \"\"\"Return the starting point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[0]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CellDesignerSingleHeadedArc.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CodingRegion","title":"CodingRegion  <code>dataclass</code>","text":"<pre><code>CodingRegion(*, id_: str = make_uuid4_as_str(), name: str | None = None, active: bool = False)\n</code></pre> <p>               Bases: <code>Region</code></p> <p>Coding region.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'36aaa3a7-28b2-462c-94e3-f6edc0524599'</code> <code>name</code> <code>str | None</code> <p>The name of the region</p> <code>None</code> <code>active</code> <code>bool</code> <p>Whether the region is active or not</p> <code>False</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CodingRegion.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.Compartment","title":"Compartment  <code>dataclass</code>","text":"<pre><code>Compartment(*, id_: str = make_uuid4_as_str(), name: str | None = None, sbo_term: str | None = None, metaid: str | None = None, outside: Optional[ForwardRef(Compartment, module=model)] = None)\n</code></pre> <p>               Bases: <code>Compartment</code>, <code>CellDesignerModelElement</code></p> <p>Compartment.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'5d8e7e46-a782-499d-ace6-58fe044a4a59'</code> <code>name</code> <code>str | None</code> <p>Human-readable name of the element.</p> <code>None</code> <code>sbo_term</code> <code>str | None</code> <p>Optional SBO term identifier for semantic annotation.</p> <code>None</code> <code>metaid</code> <code>str | None</code> <p>Optional metadata identifier for RDF annotations.</p> <code>None</code> <code>outside</code> <code>ForwardRef | None</code> <p>Optional outer compartment for hierarchical nesting.</p> <code>None</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.Compartment.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CompartmentCorner","title":"CompartmentCorner","text":"<p>               Bases: <code>Enum</code></p> <p>Compartment corner.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CompartmentSide","title":"CompartmentSide","text":"<p>               Bases: <code>Enum</code></p> <p>Compartment side.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.Complex","title":"Complex  <code>dataclass</code>","text":"<pre><code>Complex(*, id_: str = make_uuid4_as_str(), name: str | None = None, sbo_term: str | None = None, metaid: str | None = None, compartment: Compartment | None = None, hypothetical: bool = False, active: bool = False, homomultimer: int = 1, structural_states: frozenset[StructuralState] = frozenset(), subunits: frozenset[Species] = frozenset())\n</code></pre> <p>               Bases: <code>Species</code></p> <p>Complex.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'ade62422-1c09-433e-aedf-01f550c96947'</code> <code>name</code> <code>str | None</code> <p>Human-readable name of the element.</p> <code>None</code> <code>sbo_term</code> <code>str | None</code> <p>Optional SBO term identifier for semantic annotation.</p> <code>None</code> <code>metaid</code> <code>str | None</code> <p>Optional metadata identifier for RDF annotations.</p> <code>None</code> <code>compartment</code> <code>Compartment | None</code> <p>The compartment containing this species.</p> <code>None</code> <code>hypothetical</code> <code>bool</code> <p>Whether the species is hypothetical or not</p> <code>False</code> <code>active</code> <code>bool</code> <p>Whether the species is active or not</p> <code>False</code> <code>homomultimer</code> <code>int</code> <p>The number of subunits forming the species</p> <code>1</code> <code>structural_states</code> <code>frozenset[StructuralState]</code> <p>The structural states of the complex</p> <code>&lt;dynamic&gt;</code> <code>subunits</code> <code>frozenset[Species]</code> <p>The subunits of the complex</p> <code>&lt;dynamic&gt;</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.Complex.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ComplexActiveLayout","title":"ComplexActiveLayout  <code>dataclass</code>","text":"<pre><code>ComplexActiveLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = NoneValue, filter_: NoneValueType | Filter | None = None, height: float = 120.0 + DEFAULT_ACTIVE_YSEP * 2, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = (4, 2), stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = 1.0, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 100.0 + DEFAULT_ACTIVE_XSEP * 2, offset: float = 3.0, subunits_stroke: tuple[NoneValueType | Color, ...] | None = None, subunits_stroke_width: tuple[NoneValueType | float, ...] | None = None, subunits_stroke_dasharray: tuple[NoneValueType | tuple[float, ...], ...] | None = None, subunits_stroke_dashoffset: tuple[float, ...] | None = None, subunits_fill: tuple[NoneValueType | Color, ...] | None = None, subunits_transform: tuple[NoneValueType | tuple[Transformation, ...], ...] | None = None, subunits_filter: tuple[NoneValueType | Filter, ...] | None = None, n: int = 1, cut_corners: float = 6.0)\n</code></pre> <p>               Bases: <code>_MultiNodeMixin</code>, <code>CellDesignerNode</code></p> <p>Complex active layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'6c138830-41ef-4f3c-a88b-83141d3e1db4'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>&lt;momapy.drawing.NoneValueType object at 0x7f8b17323e00&gt;</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>128.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>(4, 2)</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The stroke width of the node</p> <code>1.0</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>108.0</code> <code>offset</code> <code>float</code> <p>Offset distance between stacked units.</p> <code>3.0</code> <code>subunits_stroke</code> <code>tuple[NoneValueType | Color, ...] | None</code> <p>Tuple of stroke colors for each subunit.</p> <code>None</code> <code>subunits_stroke_width</code> <code>tuple[NoneValueType | float, ...] | None</code> <p>Tuple of stroke widths for each subunit.</p> <code>None</code> <code>subunits_stroke_dasharray</code> <code>tuple[NoneValueType | tuple[float, ...], ...] | None</code> <p>Tuple of dash patterns for each subunit.</p> <code>None</code> <code>subunits_stroke_dashoffset</code> <code>tuple[float, ...] | None</code> <p>Tuple of dash offsets for each subunit.</p> <code>None</code> <code>subunits_fill</code> <code>tuple[NoneValueType | Color, ...] | None</code> <p>Tuple of fill colors for each subunit.</p> <code>None</code> <code>subunits_transform</code> <code>tuple[NoneValueType | tuple[Transformation, ...], ...] | None</code> <p>Tuple of transformations for each subunit.</p> <code>None</code> <code>subunits_filter</code> <code>tuple[NoneValueType | Filter, ...] | None</code> <p>Tuple of filters for each subunit.</p> <code>None</code> <code>n</code> <code>int</code> <p>Number of stacked node copies to draw.</p> <code>1</code> <code>cut_corners</code> <code>float</code> <p>The size of the cut corners.</p> <code>6.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>angle</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Return the east anchor of the node.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Get the center position for the node label.</p> <code>north</code> <p>Return the north anchor of the node.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Return the south anchor of the node.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Return the west anchor of the node.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ComplexActiveLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ComplexActiveLayout.angle","title":"angle","text":"<pre><code>angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ComplexActiveLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ComplexActiveLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ComplexActiveLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ComplexActiveLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ComplexActiveLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ComplexActiveLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ComplexActiveLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ComplexActiveLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ComplexActiveLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Return the east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Return the east anchor of the node.\"\"\"\n    return self.own_angle(0)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ComplexActiveLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ComplexActiveLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ComplexActiveLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ComplexActiveLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ComplexActiveLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Get the center position for the node label.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point where the label should be centered.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Get the center position for the node label.\n\n    Returns:\n        Point where the label should be centered.\n    \"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2 - (self._n - 1) * self.offset,\n        self.height / 2 - height / 2 - (self._n - 1) * self.offset,\n    )\n    return self._make_subunit_shape(\n        position=position, width=width, height=height\n    ).position\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ComplexActiveLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Return the north anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Return the north anchor of the node.\"\"\"\n    return self.own_angle(90)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ComplexActiveLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ComplexActiveLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ComplexActiveLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ComplexActiveLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ComplexActiveLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ComplexActiveLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ComplexActiveLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ComplexActiveLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ComplexActiveLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ComplexActiveLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ComplexActiveLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ComplexActiveLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Return the south anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Return the south anchor of the node.\"\"\"\n    return self.own_angle(270)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ComplexActiveLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ComplexActiveLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ComplexActiveLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ComplexActiveLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ComplexActiveLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ComplexActiveLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Return the west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Return the west anchor of the node.\"\"\"\n    return self.own_angle(180)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ComplexActiveLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ComplexActiveLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ComplexActiveLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ComplexActiveLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ComplexLayout","title":"ComplexLayout  <code>dataclass</code>","text":"<pre><code>ComplexLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = from_hex('#F7F7F7'), filter_: NoneValueType | Filter | None = None, height: float = 120.0, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = 2.0, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 100.0, offset: float = 3.0, subunits_stroke: tuple[NoneValueType | Color, ...] | None = None, subunits_stroke_width: tuple[NoneValueType | float, ...] | None = None, subunits_stroke_dasharray: tuple[NoneValueType | tuple[float, ...], ...] | None = None, subunits_stroke_dashoffset: tuple[float, ...] | None = None, subunits_fill: tuple[NoneValueType | Color, ...] | None = None, subunits_transform: tuple[NoneValueType | tuple[Transformation, ...], ...] | None = None, subunits_filter: tuple[NoneValueType | Filter, ...] | None = None, n: int = 1, cut_corners: float = 6.0)\n</code></pre> <p>               Bases: <code>_MultiNodeMixin</code>, <code>CellDesignerNode</code></p> <p>Complex layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'e2303e48-357c-461d-a070-002b202dc2d4'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>Color(red=247, green=247, blue=247, alpha=1.0)</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>120.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The stroke width of the node</p> <code>2.0</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>100.0</code> <code>offset</code> <code>float</code> <p>Offset distance between stacked units.</p> <code>3.0</code> <code>subunits_stroke</code> <code>tuple[NoneValueType | Color, ...] | None</code> <p>Tuple of stroke colors for each subunit.</p> <code>None</code> <code>subunits_stroke_width</code> <code>tuple[NoneValueType | float, ...] | None</code> <p>Tuple of stroke widths for each subunit.</p> <code>None</code> <code>subunits_stroke_dasharray</code> <code>tuple[NoneValueType | tuple[float, ...], ...] | None</code> <p>Tuple of dash patterns for each subunit.</p> <code>None</code> <code>subunits_stroke_dashoffset</code> <code>tuple[float, ...] | None</code> <p>Tuple of dash offsets for each subunit.</p> <code>None</code> <code>subunits_fill</code> <code>tuple[NoneValueType | Color, ...] | None</code> <p>Tuple of fill colors for each subunit.</p> <code>None</code> <code>subunits_transform</code> <code>tuple[NoneValueType | tuple[Transformation, ...], ...] | None</code> <p>Tuple of transformations for each subunit.</p> <code>None</code> <code>subunits_filter</code> <code>tuple[NoneValueType | Filter, ...] | None</code> <p>Tuple of filters for each subunit.</p> <code>None</code> <code>n</code> <code>int</code> <p>Number of stacked node copies to draw.</p> <code>1</code> <code>cut_corners</code> <code>float</code> <p>The size of the cut corners.</p> <code>6.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>angle</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Return the east anchor of the node.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Return the center point for the node's label.</p> <code>north</code> <p>Return the north anchor of the node.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Return the south anchor of the node.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Return the west anchor of the node.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ComplexLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ComplexLayout.angle","title":"angle","text":"<pre><code>angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ComplexLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ComplexLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ComplexLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ComplexLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ComplexLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ComplexLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ComplexLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ComplexLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ComplexLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Return the east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Return the east anchor of the node.\"\"\"\n    return self.own_angle(0)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ComplexLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ComplexLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ComplexLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ComplexLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ComplexLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Return the center point for the node's label.</p> Source code in <code>src/momapy/celldesigner/layout.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Return the center point for the node's label.\"\"\"\n    return self.south() - (0, 12)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ComplexLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Return the north anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Return the north anchor of the node.\"\"\"\n    return self.own_angle(90)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ComplexLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ComplexLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ComplexLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ComplexLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ComplexLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ComplexLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ComplexLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ComplexLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ComplexLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ComplexLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ComplexLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ComplexLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Return the south anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Return the south anchor of the node.\"\"\"\n    return self.own_angle(270)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ComplexLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ComplexLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ComplexLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ComplexLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ComplexLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ComplexLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Return the west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Return the west anchor of the node.\"\"\"\n    return self.own_angle(180)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ComplexLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ComplexLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ComplexLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ComplexLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ConsumptionLayout","title":"ConsumptionLayout  <code>dataclass</code>","text":"<pre><code>ConsumptionLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, end_shorten: float = 0.0, fill: NoneValueType | Color | None = None, filter_: NoneValueType | Filter | None = None, path_fill: NoneValueType | Color | None = NoneValue, path_filter: NoneValueType | Filter | None = None, path_stroke: NoneValueType | Color | None = black, path_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, path_stroke_dashoffset: NoneValueType | float | None = None, path_stroke_width: float | None = 1.0, path_transform: NoneValueType | tuple[Transformation, ...] | None = None, stroke: NoneValueType | Color | None = None, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: NoneValueType | float | None = None, segments: tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...] = tuple(), source: LayoutElement | None = None, start_shorten: float = 0.0, target: LayoutElement | None = None, transform: NoneValueType | tuple[Transformation, ...] | None = None, arrowhead_fill: NoneValueType | Color | None = None, arrowhead_filter: NoneValueType | Filter | None = None, arrowhead_stroke: NoneValueType | Color | None = black, arrowhead_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, arrowhead_stroke_dashoffset: NoneValueType | float | None = None, arrowhead_stroke_width: float | None = 1.0, arrowhead_transform: NoneValueType | tuple[Transformation, ...] | None = None)\n</code></pre> <p>               Bases: <code>CellDesignerSingleHeadedArc</code></p> <p>Consumption layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'a49693a5-9d40-4bbf-8a24-348f4f9ccc07'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>end_shorten</code> <code>float</code> <p>The length the end of the arc will be shorten by</p> <code>0.0</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the arc</p> <code>None</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the arc</p> <code>None</code> <code>path_fill</code> <code>NoneValueType | Color | None</code> <p>The path fill color of the arc</p> <code>&lt;momapy.drawing.NoneValueType object at 0x7f8b17323e00&gt;</code> <code>path_filter</code> <code>NoneValueType | Filter | None</code> <p>The path filter of the arc</p> <code>None</code> <code>path_stroke</code> <code>NoneValueType | Color | None</code> <p>The path stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>path_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The path stroke dasharray of the arc</p> <code>None</code> <code>path_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The path stroke dashoffset of the arc</p> <code>None</code> <code>path_stroke_width</code> <code>float | None</code> <p>The path stroke width of the arc</p> <code>1.0</code> <code>path_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The path transform of the arc</p> <code>None</code> <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the arc</p> <code>None</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the arc</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the arc</p> <code>None</code> <code>stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the arc</p> <code>None</code> <code>segments</code> <code>tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...]</code> <p>The path segments of the arc</p> <code>&lt;dynamic&gt;</code> <code>source</code> <code>LayoutElement | None</code> <p>The source of the arc</p> <code>None</code> <code>start_shorten</code> <code>float</code> <p>The length the start of the arc will be shorten by</p> <code>0.0</code> <code>target</code> <code>LayoutElement | None</code> <p>The target of the arc</p> <code>None</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the arc</p> <code>None</code> <code>arrowhead_fill</code> <code>NoneValueType | Color | None</code> <p>The arrowhead fill color of the arc</p> <code>None</code> <code>arrowhead_filter</code> <code>NoneValueType | Filter | None</code> <p>The arrowhead filter of the arc</p> <code>None</code> <code>arrowhead_stroke</code> <code>NoneValueType | Color | None</code> <p>The arrowhead stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>arrowhead_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The arrowhead stroke dasharray of the arc</p> <code>None</code> <code>arrowhead_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The arrowhead stroke dashoffset of the arc</p> <code>None</code> <code>arrowhead_stroke_width</code> <code>float | None</code> <p>The arrowhead stroke width of the arc</p> <code>1.0</code> <code>arrowhead_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The arrowhead transform of the arc</p> <code>None</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>arrowhead_base</code> <p>Return the arrowhead base anchor point of the single-headed arc.</p> <code>arrowhead_bbox</code> <p>Return the bounding box of the single-headed arc arrowhead.</p> <code>arrowhead_border</code> <p>Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.</p> <code>arrowhead_drawing_elements</code> <p>Return the drawing elements of the single-headed arc arrowhead.</p> <code>arrowhead_length</code> <p>Return the length of the single-headed arc arrowhead.</p> <code>arrowhead_tip</code> <p>Return the arrowhead tip anchor point of the single-headed arc.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>childless</code> <p>Return a copy of the arc with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>end_point</code> <p>Return the ending point of the arc.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>fraction</code> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <code>length</code> <p>Return the total length of the arc path.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_children</code> <p>Return the self children of the arc.</p> <code>own_drawing_elements</code> <p>Return the arc's own drawing elements, including those of its mixins.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>path_drawing_elements</code> <p>Return the drawing elements of the single-headed arc path.</p> <code>points</code> <p>Return the points of the arc path.</p> <code>start_point</code> <p>Return the starting point of the arc.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ConsumptionLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ConsumptionLayout.arrowhead_base","title":"arrowhead_base","text":"<pre><code>arrowhead_base() -&gt; Point\n</code></pre> <p>Return the arrowhead base anchor point of the single-headed arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_base(self) -&gt; Point:\n    \"\"\"Return the arrowhead base anchor point of the single-headed arc.\"\"\"\n    arrowhead_length = self.arrowhead_length()\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return self.arrowhead_tip() - (arrowhead_length, 0)\n    fraction = 1 - (arrowhead_length + self.end_shorten) / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ConsumptionLayout.arrowhead_bbox","title":"arrowhead_bbox","text":"<pre><code>arrowhead_bbox() -&gt; Bbox\n</code></pre> <p>Return the bounding box of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_bbox(self) -&gt; Bbox:\n    \"\"\"Return the bounding box of the single-headed arc arrowhead.\"\"\"\n    return get_drawing_elements_bbox(self.arrowhead_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ConsumptionLayout.arrowhead_border","title":"arrowhead_border","text":"<pre><code>arrowhead_border(point: Point) -&gt; Point\n</code></pre> <p>Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_border(self, point: Point) -&gt; Point:\n    \"\"\"Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    point = get_drawing_elements_border(self.arrowhead_drawing_elements(), point)\n    if point is None:\n        return self.arrowhead_tip()\n    return point\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ConsumptionLayout.arrowhead_drawing_elements","title":"arrowhead_drawing_elements","text":"<pre><code>arrowhead_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_drawing_elements(\n    self,\n) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the single-headed arc arrowhead.\"\"\"\n    drawing_elements = self._arrowhead_border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_arrowhead\",\n        elements=tuple(drawing_elements),\n        fill=self.arrowhead_fill,\n        filter_=self.arrowhead_filter,\n        id_=f\"{self.id_}_arrowhead\",\n        stroke=self.arrowhead_stroke,\n        stroke_dasharray=self.arrowhead_stroke_dasharray,\n        stroke_dashoffset=self.arrowhead_stroke_dashoffset,\n        stroke_width=self.arrowhead_stroke_width,\n        transform=self.arrowhead_transform,\n    )\n    transformation = self._get_arrowhead_transformation()\n    group = group.transformed(transformation)\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ConsumptionLayout.arrowhead_length","title":"arrowhead_length","text":"<pre><code>arrowhead_length() -&gt; float\n</code></pre> <p>Return the length of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_length(self) -&gt; float:\n    \"\"\"Return the length of the single-headed arc arrowhead.\"\"\"\n    bbox = get_drawing_elements_bbox(self._arrowhead_border_drawing_elements())\n    if math.isnan(bbox.width):\n        return 0.0\n    return bbox.east().x\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ConsumptionLayout.arrowhead_tip","title":"arrowhead_tip","text":"<pre><code>arrowhead_tip() -&gt; Point\n</code></pre> <p>Return the arrowhead tip anchor point of the single-headed arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_tip(self) -&gt; Point:\n    \"\"\"Return the arrowhead tip anchor point of the single-headed arc.\"\"\"\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return segment.p2\n    fraction = 1 - self.end_shorten / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ConsumptionLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ConsumptionLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the arc with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the arc with no children.\"\"\"\n    return dataclasses.replace(self, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ConsumptionLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ConsumptionLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ConsumptionLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ConsumptionLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ConsumptionLayout.end_point","title":"end_point","text":"<pre><code>end_point() -&gt; Point\n</code></pre> <p>Return the ending point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_point(self) -&gt; Point:\n    \"\"\"Return the ending point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[-1]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ConsumptionLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ConsumptionLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ConsumptionLayout.fraction","title":"fraction","text":"<pre><code>fraction(fraction: float) -&gt; tuple[Point, float]\n</code></pre> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def fraction(self, fraction: float) -&gt; tuple[Point, float]:\n    \"\"\"Return the position and angle on the arc at a given fraction (of the total arc length).\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    current_length = 0\n    length_to_reach = fraction * self.length()\n    for segment in self.segments:\n        current_length += segment.length()\n        if current_length &gt;= length_to_reach:\n            break\n    segment_start_length = current_length - segment.length()\n    segment_fraction = (length_to_reach - segment_start_length) / segment.length()\n    position, angle = segment.get_position_and_angle_at_fraction(segment_fraction)\n    return position, angle\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ConsumptionLayout.length","title":"length","text":"<pre><code>length() -&gt; float\n</code></pre> <p>Return the total length of the arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def length(self) -&gt; float:\n    \"\"\"Return the total length of the arc path.\"\"\"\n    return sum([segment.length() for segment in self.segments])\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ConsumptionLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ConsumptionLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the arc.\"\"\"\n    return []\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ConsumptionLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the arc's own drawing elements, including those of its mixins.</p> Source code in <code>src/momapy/celldesigner/elements.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the arc's own drawing elements, including those of its mixins.\"\"\"\n    drawing_elements = super_or_builder(\n        CellDesignerSingleHeadedArc, self\n    ).own_drawing_elements()\n    done_bases = []\n    for base in type(self).__mro__:\n        if (\n            issubclass_or_builder(base, _SBGNMixin)\n            and base is not type(self)\n            and not any([issubclass(done_base, base) for done_base in done_bases])\n        ):\n            drawing_elements += getattr(base, \"_mixin_drawing_elements\")(self)\n            done_bases.append(base)\n    return drawing_elements\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ConsumptionLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ConsumptionLayout.path_drawing_elements","title":"path_drawing_elements","text":"<pre><code>path_drawing_elements() -&gt; list[Path]\n</code></pre> <p>Return the drawing elements of the single-headed arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def path_drawing_elements(self) -&gt; list[Path]:\n    \"\"\"Return the drawing elements of the single-headed arc path.\"\"\"\n    arrowhead_length = self.arrowhead_length()\n    if len(self.segments) == 1:\n        segment = (\n            self.segments[0]\n            .shortened(self.start_shorten, \"start\")\n            .shortened(self.end_shorten + arrowhead_length, \"end\")\n        )\n        actions = [\n            MoveTo(segment.p1),\n            self._make_path_action_from_segment(segment),\n        ]\n    else:\n        first_segment = self.segments[0].shortened(self.start_shorten, \"start\")\n        last_segment = self.segments[-1].shortened(\n            self.end_shorten + arrowhead_length, \"end\"\n        )\n        actions = [\n            MoveTo(first_segment.p1),\n            self._make_path_action_from_segment(first_segment),\n        ]\n        for segment in self.segments[1:-1]:\n            action = self._make_path_action_from_segment(segment)\n            actions.append(action)\n        actions.append(self._make_path_action_from_segment(last_segment))\n    path = Path(\n        actions=tuple(actions),\n        class_=f\"{type(self).__name__}_path\",\n        fill=self.path_fill,\n        filter_=self.path_filter,\n        id_=f\"{self.id_}_path\",\n        stroke=self.path_stroke,\n        stroke_dasharray=self.path_stroke_dasharray,\n        stroke_dashoffset=self.path_stroke_dashoffset,\n        stroke_width=self.path_stroke_width,\n        transform=self.path_transform,\n    )\n    return [path]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ConsumptionLayout.points","title":"points","text":"<pre><code>points() -&gt; list[Point]\n</code></pre> <p>Return the points of the arc path.</p> <p>An arc with no segments has no points and returns an empty list.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def points(self) -&gt; list[Point]:\n    \"\"\"Return the points of the arc path.\n\n    An arc with no segments has no points and returns an empty list.\n    \"\"\"\n    if not self.segments:\n        return []\n    points = []\n    for segment in self.segments:\n        points.append(segment.p1)\n    points.append(segment.p2)\n    return points\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ConsumptionLayout.start_point","title":"start_point","text":"<pre><code>start_point() -&gt; Point\n</code></pre> <p>Return the starting point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_point(self) -&gt; Point:\n    \"\"\"Return the starting point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[0]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ConsumptionLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CornerCompartmentLayout","title":"CornerCompartmentLayout  <code>dataclass</code>","text":"<pre><code>CornerCompartmentLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = white, filter_: NoneValueType | Filter | None = None, height: float = 16.0, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = 1.25, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 16.0, corner: CompartmentCorner = NORTHWEST, inner_stroke: NoneValueType | Color | None = black, inner_stroke_width: float | None = 1.0, rounded_corners: float = 40.0, sep: float = 12.0)\n</code></pre> <p>               Bases: <code>_SimpleNodeMixin</code>, <code>CellDesignerNode</code></p> <p>Corner compartment layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'4786c842-0619-460e-b68e-d8c6fc23396d'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>16.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The stroke width of the node</p> <code>1.25</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>16.0</code> <code>corner</code> <code>CompartmentCorner</code> <p>The corner where the rounded border sits.</p> <code>&lt;CompartmentCorner.NORTHWEST: 'NORTHWEST'&gt;</code> <code>inner_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the inner border.</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>inner_stroke_width</code> <code>float | None</code> <p>The stroke width of the inner border.</p> <code>1.0</code> <code>rounded_corners</code> <code>float</code> <p>The corner radius of the rounded border.</p> <code>40.0</code> <code>sep</code> <code>float</code> <p>The separation between the outer and inner borders.</p> <code>12.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>angle</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Return the east anchor of the node.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Return the label center anchor of the node.</p> <code>north</code> <p>Return the north anchor of the node.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Return the south anchor of the node.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Return the west anchor of the node.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CornerCompartmentLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CornerCompartmentLayout.angle","title":"angle","text":"<pre><code>angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CornerCompartmentLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CornerCompartmentLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CornerCompartmentLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CornerCompartmentLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CornerCompartmentLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CornerCompartmentLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CornerCompartmentLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CornerCompartmentLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CornerCompartmentLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Return the east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Return the east anchor of the node.\"\"\"\n    return self.own_angle(0)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CornerCompartmentLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CornerCompartmentLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CornerCompartmentLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CornerCompartmentLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CornerCompartmentLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Return the label center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Return the label center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CornerCompartmentLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Return the north anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Return the north anchor of the node.\"\"\"\n    return self.own_angle(90)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CornerCompartmentLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CornerCompartmentLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CornerCompartmentLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CornerCompartmentLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CornerCompartmentLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CornerCompartmentLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CornerCompartmentLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CornerCompartmentLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CornerCompartmentLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CornerCompartmentLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CornerCompartmentLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CornerCompartmentLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Return the south anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Return the south anchor of the node.\"\"\"\n    return self.own_angle(270)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CornerCompartmentLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CornerCompartmentLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CornerCompartmentLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CornerCompartmentLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CornerCompartmentLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CornerCompartmentLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Return the west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Return the west anchor of the node.\"\"\"\n    return self.own_angle(180)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CornerCompartmentLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CornerCompartmentLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CornerCompartmentLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.CornerCompartmentLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DegradedActiveLayout","title":"DegradedActiveLayout  <code>dataclass</code>","text":"<pre><code>DegradedActiveLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = NoneValue, filter_: NoneValueType | Filter | None = None, height: float = 30.0 + DEFAULT_ACTIVE_YSEP * 2, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = (4, 2), stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = 1.0, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 30.0 + DEFAULT_ACTIVE_XSEP * 2, offset: float = 3.0, subunits_stroke: tuple[NoneValueType | Color, ...] | None = None, subunits_stroke_width: tuple[NoneValueType | float, ...] | None = None, subunits_stroke_dasharray: tuple[NoneValueType | tuple[float, ...], ...] | None = None, subunits_stroke_dashoffset: tuple[float, ...] | None = None, subunits_fill: tuple[NoneValueType | Color, ...] | None = None, subunits_transform: tuple[NoneValueType | tuple[Transformation, ...], ...] | None = None, subunits_filter: tuple[NoneValueType | Filter, ...] | None = None, n: int = 1)\n</code></pre> <p>               Bases: <code>_MultiNodeMixin</code>, <code>CellDesignerNode</code></p> <p>Degraded active layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'63bf2518-13fd-4996-9e5c-3f6b6aab5566'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>&lt;momapy.drawing.NoneValueType object at 0x7f8b17323e00&gt;</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>38.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>(4, 2)</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The stroke width of the node</p> <code>1.0</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>38.0</code> <code>offset</code> <code>float</code> <p>Offset distance between stacked units.</p> <code>3.0</code> <code>subunits_stroke</code> <code>tuple[NoneValueType | Color, ...] | None</code> <p>Tuple of stroke colors for each subunit.</p> <code>None</code> <code>subunits_stroke_width</code> <code>tuple[NoneValueType | float, ...] | None</code> <p>Tuple of stroke widths for each subunit.</p> <code>None</code> <code>subunits_stroke_dasharray</code> <code>tuple[NoneValueType | tuple[float, ...], ...] | None</code> <p>Tuple of dash patterns for each subunit.</p> <code>None</code> <code>subunits_stroke_dashoffset</code> <code>tuple[float, ...] | None</code> <p>Tuple of dash offsets for each subunit.</p> <code>None</code> <code>subunits_fill</code> <code>tuple[NoneValueType | Color, ...] | None</code> <p>Tuple of fill colors for each subunit.</p> <code>None</code> <code>subunits_transform</code> <code>tuple[NoneValueType | tuple[Transformation, ...], ...] | None</code> <p>Tuple of transformations for each subunit.</p> <code>None</code> <code>subunits_filter</code> <code>tuple[NoneValueType | Filter, ...] | None</code> <p>Tuple of filters for each subunit.</p> <code>None</code> <code>n</code> <code>int</code> <p>Number of stacked node copies to draw.</p> <code>1</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>angle</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Return the east anchor of the node.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Get the center position for the node label.</p> <code>north</code> <p>Return the north anchor of the node.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Return the south anchor of the node.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Return the west anchor of the node.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DegradedActiveLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DegradedActiveLayout.angle","title":"angle","text":"<pre><code>angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DegradedActiveLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DegradedActiveLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DegradedActiveLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DegradedActiveLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DegradedActiveLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DegradedActiveLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DegradedActiveLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DegradedActiveLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DegradedActiveLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Return the east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Return the east anchor of the node.\"\"\"\n    return self.own_angle(0)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DegradedActiveLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DegradedActiveLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DegradedActiveLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DegradedActiveLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DegradedActiveLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Get the center position for the node label.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point where the label should be centered.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Get the center position for the node label.\n\n    Returns:\n        Point where the label should be centered.\n    \"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2 - (self._n - 1) * self.offset,\n        self.height / 2 - height / 2 - (self._n - 1) * self.offset,\n    )\n    return self._make_subunit_shape(\n        position=position, width=width, height=height\n    ).position\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DegradedActiveLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Return the north anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Return the north anchor of the node.\"\"\"\n    return self.own_angle(90)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DegradedActiveLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DegradedActiveLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DegradedActiveLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DegradedActiveLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DegradedActiveLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DegradedActiveLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DegradedActiveLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DegradedActiveLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DegradedActiveLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DegradedActiveLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DegradedActiveLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DegradedActiveLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Return the south anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Return the south anchor of the node.\"\"\"\n    return self.own_angle(270)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DegradedActiveLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DegradedActiveLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DegradedActiveLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DegradedActiveLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DegradedActiveLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DegradedActiveLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Return the west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Return the west anchor of the node.\"\"\"\n    return self.own_angle(180)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DegradedActiveLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DegradedActiveLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DegradedActiveLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DegradedActiveLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DegradedLayout","title":"DegradedLayout  <code>dataclass</code>","text":"<pre><code>DegradedLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = white, filter_: NoneValueType | Filter | None = None, height: float = 30.0, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = 1.25, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 30.0, offset: float = 3.0, subunits_stroke: tuple[NoneValueType | Color, ...] | None = None, subunits_stroke_width: tuple[NoneValueType | float, ...] | None = None, subunits_stroke_dasharray: tuple[NoneValueType | tuple[float, ...], ...] | None = None, subunits_stroke_dashoffset: tuple[float, ...] | None = None, subunits_fill: tuple[NoneValueType | Color, ...] | None = None, subunits_transform: tuple[NoneValueType | tuple[Transformation, ...], ...] | None = None, subunits_filter: tuple[NoneValueType | Filter, ...] | None = None, n: int = 1)\n</code></pre> <p>               Bases: <code>_MultiNodeMixin</code>, <code>CellDesignerNode</code></p> <p>Degraded layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'73ef942a-481d-4bb1-8b3b-3e0fa9ac2dcd'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>30.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The stroke width of the node</p> <code>1.25</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>30.0</code> <code>offset</code> <code>float</code> <p>Offset distance between stacked units.</p> <code>3.0</code> <code>subunits_stroke</code> <code>tuple[NoneValueType | Color, ...] | None</code> <p>Tuple of stroke colors for each subunit.</p> <code>None</code> <code>subunits_stroke_width</code> <code>tuple[NoneValueType | float, ...] | None</code> <p>Tuple of stroke widths for each subunit.</p> <code>None</code> <code>subunits_stroke_dasharray</code> <code>tuple[NoneValueType | tuple[float, ...], ...] | None</code> <p>Tuple of dash patterns for each subunit.</p> <code>None</code> <code>subunits_stroke_dashoffset</code> <code>tuple[float, ...] | None</code> <p>Tuple of dash offsets for each subunit.</p> <code>None</code> <code>subunits_fill</code> <code>tuple[NoneValueType | Color, ...] | None</code> <p>Tuple of fill colors for each subunit.</p> <code>None</code> <code>subunits_transform</code> <code>tuple[NoneValueType | tuple[Transformation, ...], ...] | None</code> <p>Tuple of transformations for each subunit.</p> <code>None</code> <code>subunits_filter</code> <code>tuple[NoneValueType | Filter, ...] | None</code> <p>Tuple of filters for each subunit.</p> <code>None</code> <code>n</code> <code>int</code> <p>Number of stacked node copies to draw.</p> <code>1</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>angle</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Return the east anchor of the node.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Get the center position for the node label.</p> <code>north</code> <p>Return the north anchor of the node.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Return the south anchor of the node.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Return the west anchor of the node.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DegradedLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DegradedLayout.angle","title":"angle","text":"<pre><code>angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DegradedLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DegradedLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DegradedLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DegradedLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DegradedLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DegradedLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DegradedLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DegradedLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DegradedLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Return the east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Return the east anchor of the node.\"\"\"\n    return self.own_angle(0)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DegradedLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DegradedLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DegradedLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DegradedLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DegradedLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Get the center position for the node label.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point where the label should be centered.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Get the center position for the node label.\n\n    Returns:\n        Point where the label should be centered.\n    \"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2 - (self._n - 1) * self.offset,\n        self.height / 2 - height / 2 - (self._n - 1) * self.offset,\n    )\n    return self._make_subunit_shape(\n        position=position, width=width, height=height\n    ).position\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DegradedLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Return the north anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Return the north anchor of the node.\"\"\"\n    return self.own_angle(90)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DegradedLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DegradedLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DegradedLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DegradedLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DegradedLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DegradedLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DegradedLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DegradedLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DegradedLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DegradedLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DegradedLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DegradedLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Return the south anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Return the south anchor of the node.\"\"\"\n    return self.own_angle(270)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DegradedLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DegradedLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DegradedLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DegradedLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DegradedLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DegradedLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Return the west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Return the west anchor of the node.\"\"\"\n    return self.own_angle(180)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DegradedLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DegradedLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DegradedLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DegradedLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.Dissociation","title":"Dissociation  <code>dataclass</code>","text":"<pre><code>Dissociation(*, id_: str = make_uuid4_as_str(), name: str | None = None, sbo_term: str | None = None, metaid: str | None = None, reversible: bool = False, compartment: Compartment | None = None, reactants: frozenset[Reactant] = frozenset(), products: frozenset[Product] = frozenset(), modifiers: frozenset[KnownOrUnknownModulator] = frozenset(), has_external_source: bool = False, has_external_sink: bool = False)\n</code></pre> <p>               Bases: <code>Reaction</code></p> <p>Dissociation.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'aceb703b-5941-4dfd-ab1c-16c5af0423f9'</code> <code>name</code> <code>str | None</code> <p>Human-readable name of the element.</p> <code>None</code> <code>sbo_term</code> <code>str | None</code> <p>Optional SBO term identifier for semantic annotation.</p> <code>None</code> <code>metaid</code> <code>str | None</code> <p>Optional metadata identifier for RDF annotations.</p> <code>None</code> <code>reversible</code> <code>bool</code> <p>Whether the reaction can proceed in both directions.</p> <code>False</code> <code>compartment</code> <code>Compartment | None</code> <p>Optional compartment where the reaction occurs.</p> <code>None</code> <code>reactants</code> <code>frozenset[Reactant]</code> <p>The reactants of the reaction</p> <code>&lt;dynamic&gt;</code> <code>products</code> <code>frozenset[Product]</code> <p>The products of the reaction</p> <code>&lt;dynamic&gt;</code> <code>modifiers</code> <code>frozenset[KnownOrUnknownModulator]</code> <p>The modifiers of the reaction</p> <code>&lt;dynamic&gt;</code> <code>has_external_source</code> <code>bool</code> <p>Whether the reaction has an unspecified external source (a Degraded reactant in CellDesigner).</p> <code>False</code> <code>has_external_sink</code> <code>bool</code> <p>Whether the reaction has an unspecified external sink (a Degraded product in CellDesigner).</p> <code>False</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.Dissociation.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DissociationLayout","title":"DissociationLayout  <code>dataclass</code>","text":"<pre><code>DissociationLayout(*, left_connector_fraction: float = 0.375, right_connector_fraction: float = 0.625, reaction_node_font_family: str = DEFAULT_FONT_FAMILY, reaction_node_font_fill: Color | NoneValueType = black, reaction_node_font_stroke: Color | NoneValueType = NoneValue, reaction_node_font_style: FontStyle = NORMAL, reaction_node_font_weight: FontWeight | float = NORMAL, reaction_node_text: str | None = None, reaction_node_height: float = 10.0, reaction_node_width: float = 10.0, reaction_node_segment: int = 1, reaction_node_stroke: NoneValueType | Color | None = black, reaction_node_stroke_width: float | None = 1.0, reaction_node_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, reaction_node_stroke_dashoffset: float | None = None, reaction_node_fill: NoneValueType | Color | None = white, reaction_node_transform: NoneValueType | tuple[Transformation, ...] | None = None, reaction_node_filter: NoneValueType | Filter | None = None, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, end_shorten: float = 2.0, fill: NoneValueType | Color | None = None, filter_: NoneValueType | Filter | None = None, path_fill: NoneValueType | Color | None = NoneValue, path_filter: NoneValueType | Filter | None = None, path_stroke: NoneValueType | Color | None = black, path_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, path_stroke_dashoffset: NoneValueType | float | None = None, path_stroke_width: float | None = 1.0, path_transform: NoneValueType | tuple[Transformation, ...] | None = None, stroke: NoneValueType | Color | None = None, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: NoneValueType | float | None = None, segments: tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...] = tuple(), source: LayoutElement | None = None, start_shorten: float = 2.0, target: LayoutElement | None = None, transform: NoneValueType | tuple[Transformation, ...] | None = None, end_arrowhead_fill: NoneValueType | Color | None = white, end_arrowhead_filter: NoneValueType | Filter | None = None, end_arrowhead_stroke: NoneValueType | Color | None = black, end_arrowhead_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, end_arrowhead_stroke_dashoffset: float | None = None, end_arrowhead_stroke_width: float | None = 1.0, end_arrowhead_transform: NoneValueType | tuple[Transformation, ...] | None = None, start_arrowhead_fill: NoneValueType | Color | None = black, start_arrowhead_filter: NoneValueType | Filter | None = None, start_arrowhead_stroke: NoneValueType | Color | None = black, start_arrowhead_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, start_arrowhead_stroke_dashoffset: float | None = None, start_arrowhead_stroke_width: float | None = 1.0, start_arrowhead_transform: NoneValueType | tuple[Transformation, ...] | None = None, reversible: bool = False, end_arrowhead_height: float = 10.0, end_arrowhead_sep: float = 2.0, end_arrowhead_width: float = 10.0, start_arrowhead_height: float = 8.0, start_arrowhead_width: float = 15.0)\n</code></pre> <p>               Bases: <code>ReactionLayout</code>, <code>_ReactionNodeMixin</code></p> <p>Dissociation layout.</p> <p>Parameters:</p> Name Type Description Default <code>left_connector_fraction</code> <code>float</code> <p>The fraction along the segment where the left connector attaches.</p> <code>0.375</code> <code>right_connector_fraction</code> <code>float</code> <p>The fraction along the segment where the right connector attaches.</p> <code>0.625</code> <code>reaction_node_font_family</code> <code>str</code> <p>The font family of the reaction node label.</p> <code>'DejaVu Sans'</code> <code>reaction_node_font_fill</code> <code>Color | NoneValueType</code> <p>The fill color of the reaction node label.</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>reaction_node_font_stroke</code> <code>Color | NoneValueType</code> <p>The stroke color of the reaction node label.</p> <code>&lt;momapy.drawing.NoneValueType object at 0x7f8b17323e00&gt;</code> <code>reaction_node_font_style</code> <code>FontStyle</code> <p>The font style of the reaction node label.</p> <code>&lt;FontStyle.NORMAL: 0&gt;</code> <code>reaction_node_font_weight</code> <code>FontWeight | float</code> <p>The font weight of the reaction node label.</p> <code>&lt;FontWeight.NORMAL: 0&gt;</code> <code>reaction_node_text</code> <code>str | None</code> <p>The text of the reaction node label.</p> <code>None</code> <code>reaction_node_height</code> <code>float</code> <p>The height of the reaction node.</p> <code>10.0</code> <code>reaction_node_width</code> <code>float</code> <p>The width of the reaction node.</p> <code>10.0</code> <code>reaction_node_segment</code> <code>int</code> <p>The index of the arc segment carrying the reaction node.</p> <code>1</code> <code>reaction_node_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the reaction node.</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>reaction_node_stroke_width</code> <code>float | None</code> <p>The stroke width of the reaction node.</p> <code>1.0</code> <code>reaction_node_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The dash pattern of the reaction node's border.</p> <code>None</code> <code>reaction_node_stroke_dashoffset</code> <code>float | None</code> <p>The dash offset of the reaction node's border.</p> <code>None</code> <code>reaction_node_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the reaction node.</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>reaction_node_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform applied to the reaction node.</p> <code>None</code> <code>reaction_node_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter applied to the reaction node.</p> <code>None</code> <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'1151d460-15cf-45d8-b4b7-6637b2846919'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>end_shorten</code> <code>float</code> <p>The length the end of the arc will be shorten by</p> <code>2.0</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the arc</p> <code>None</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the arc</p> <code>None</code> <code>path_fill</code> <code>NoneValueType | Color | None</code> <p>The path fill color of the arc</p> <code>&lt;momapy.drawing.NoneValueType object at 0x7f8b17323e00&gt;</code> <code>path_filter</code> <code>NoneValueType | Filter | None</code> <p>The path filter of the arc</p> <code>None</code> <code>path_stroke</code> <code>NoneValueType | Color | None</code> <p>The path stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>path_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The path stroke dasharray of the arc</p> <code>None</code> <code>path_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The path stroke dashoffset of the arc</p> <code>None</code> <code>path_stroke_width</code> <code>float | None</code> <p>The path stroke width of the arc</p> <code>1.0</code> <code>path_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The path transform of the arc</p> <code>None</code> <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the arc</p> <code>None</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the arc</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the arc</p> <code>None</code> <code>stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the arc</p> <code>None</code> <code>segments</code> <code>tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...]</code> <p>The path segments of the arc</p> <code>&lt;dynamic&gt;</code> <code>source</code> <code>LayoutElement | None</code> <p>The source of the arc</p> <code>None</code> <code>start_shorten</code> <code>float</code> <p>The length the start of the arc will be shorten by</p> <code>2.0</code> <code>target</code> <code>LayoutElement | None</code> <p>The target of the arc</p> <code>None</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the arc</p> <code>None</code> <code>end_arrowhead_fill</code> <code>NoneValueType | Color | None</code> <p>The end arrowhead fill color of the arc</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>end_arrowhead_filter</code> <code>NoneValueType | Filter | None</code> <p>The end arrowhead filter of the arc</p> <code>None</code> <code>end_arrowhead_stroke</code> <code>NoneValueType | Color | None</code> <p>The end arrowhead stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>end_arrowhead_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The end arrowhead stroke dasharray of the arc</p> <code>None</code> <code>end_arrowhead_stroke_dashoffset</code> <code>float | None</code> <p>The end arrowhead stroke dashoffset of the arc</p> <code>None</code> <code>end_arrowhead_stroke_width</code> <code>float | None</code> <p>The end arrowhead stroke width of the arc</p> <code>1.0</code> <code>end_arrowhead_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The end arrowhead transform of the arc</p> <code>None</code> <code>start_arrowhead_fill</code> <code>NoneValueType | Color | None</code> <p>The start arrowhead fill color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>start_arrowhead_filter</code> <code>NoneValueType | Filter | None</code> <p>The start arrowhead filter of the arc</p> <code>None</code> <code>start_arrowhead_stroke</code> <code>NoneValueType | Color | None</code> <p>The start arrowhead stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>start_arrowhead_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The start arrowhead stroke dasharray of the arc</p> <code>None</code> <code>start_arrowhead_stroke_dashoffset</code> <code>float | None</code> <p>The start arrowhead stroke dashoffset of the arc</p> <code>None</code> <code>start_arrowhead_stroke_width</code> <code>float | None</code> <p>The start arrowhead stroke width of the arc</p> <code>1.0</code> <code>start_arrowhead_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The start arrowhead transform of the arc</p> <code>None</code> <code>reversible</code> <code>bool</code> <p>Whether the reaction is reversible.</p> <code>False</code> <code>end_arrowhead_height</code> <code>float</code> <p>The height of the end arrowhead.</p> <code>10.0</code> <code>end_arrowhead_sep</code> <code>float</code> <p>The separation between the outer and inner end circles.</p> <code>2.0</code> <code>end_arrowhead_width</code> <code>float</code> <p>The width of the end arrowhead.</p> <code>10.0</code> <code>start_arrowhead_height</code> <code>float</code> <p>The height of the start arrowhead.</p> <code>8.0</code> <code>start_arrowhead_width</code> <code>float</code> <p>The width of the start arrowhead.</p> <code>15.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>childless</code> <p>Return a copy of the arc with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>end_arrowhead_base</code> <p>Return the base anchor point of the double-headed arc end arrowhead.</p> <code>end_arrowhead_bbox</code> <p>Return the bounding box of the double-headed arc end arrowhead.</p> <code>end_arrowhead_border</code> <p>Return the point at the intersection of the drawing elements of the double-headed arc end arrowhead and the line going through the center of these drawing elements and the given point.</p> <code>end_arrowhead_drawing_elements</code> <p>Return the drawing elements of the double-headed arc end arrowhead.</p> <code>end_arrowhead_length</code> <p>Return the length of the double-headed arc end arrowhead.</p> <code>end_arrowhead_tip</code> <p>Return the tip anchor point of the double-headed arc end arrowhead.</p> <code>end_point</code> <p>Return the ending point of the arc.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>fraction</code> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <code>length</code> <p>Return the total length of the arc path.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_children</code> <p>Return the self children of the arc.</p> <code>own_drawing_elements</code> <p>Return the arc's own drawing elements, including those of its mixins.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>path_drawing_elements</code> <p>Return the drawing elements of the double-headed arc path.</p> <code>points</code> <p>Return the points of the arc path.</p> <code>start_arrowhead_base</code> <p>Return the base anchor point of the double-headed arc start arrowhead.</p> <code>start_arrowhead_bbox</code> <p>Return the bounding box of the double-headed arc start arrowhead.</p> <code>start_arrowhead_border</code> <p>Return the point at the intersection of the drawing elements of the double-headed arc start arrowhead and the line going through the center of these drawing elements and the given point.</p> <code>start_arrowhead_drawing_elements</code> <p>Return the drawing elements of the double-headed arc start arrowhead.</p> <code>start_arrowhead_length</code> <p>Return the length of the double-headed arc start arrowhead.</p> <code>start_arrowhead_tip</code> <p>Return the tip anchor point of the double-headed arc start arrowhead.</p> <code>start_point</code> <p>Return the starting point of the arc.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DissociationLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DissociationLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DissociationLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the arc with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the arc with no children.\"\"\"\n    return dataclasses.replace(self, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DissociationLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DissociationLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DissociationLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DissociationLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DissociationLayout.end_arrowhead_base","title":"end_arrowhead_base","text":"<pre><code>end_arrowhead_base() -&gt; Point\n</code></pre> <p>Return the base anchor point of the double-headed arc end arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_arrowhead_base(self) -&gt; Point:\n    \"\"\"Return the base anchor point of the double-headed arc end arrowhead.\"\"\"\n    arrowhead_length = self.end_arrowhead_length()\n    if arrowhead_length == 0:\n        return self.end_point()\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return self.end_arrowhead_tip() - (arrowhead_length, 0)\n    fraction = 1 - (arrowhead_length + self.end_shorten) / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DissociationLayout.end_arrowhead_bbox","title":"end_arrowhead_bbox","text":"<pre><code>end_arrowhead_bbox() -&gt; Bbox\n</code></pre> <p>Return the bounding box of the double-headed arc end arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_arrowhead_bbox(self) -&gt; Bbox:\n    \"\"\"Return the bounding box of the double-headed arc end arrowhead.\"\"\"\n    return get_drawing_elements_bbox(self.end_arrowhead_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DissociationLayout.end_arrowhead_border","title":"end_arrowhead_border","text":"<pre><code>end_arrowhead_border(point: Point) -&gt; Point\n</code></pre> <p>Return the point at the intersection of the drawing elements of the double-headed arc end arrowhead and the line going through the center of these drawing elements and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_arrowhead_border(self, point: Point) -&gt; Point:\n    \"\"\"Return the point at the intersection of the drawing elements of the double-headed arc end arrowhead and the line going through the center of these drawing elements and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    point = get_drawing_elements_border(\n        self.end_arrowhead_drawing_elements(), point\n    )\n    if point is None:\n        return self.end_arrowhead_tip()\n    return point\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DissociationLayout.end_arrowhead_drawing_elements","title":"end_arrowhead_drawing_elements","text":"<pre><code>end_arrowhead_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the double-headed arc end arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_arrowhead_drawing_elements(\n    self,\n) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the double-headed arc end arrowhead.\"\"\"\n    drawing_elements = self._end_arrowhead_border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_end_arrowhead\",\n        elements=tuple(drawing_elements),\n        fill=self.end_arrowhead_fill,\n        filter_=self.end_arrowhead_filter,\n        id_=f\"{self.id_}_end_arrowhead\",\n        stroke=self.end_arrowhead_stroke,\n        stroke_width=self.end_arrowhead_stroke_width,\n        stroke_dasharray=self.end_arrowhead_stroke_dasharray,\n        stroke_dashoffset=self.end_arrowhead_stroke_dashoffset,\n        transform=self.end_arrowhead_transform,\n    )\n    transformation = self._get_end_arrowhead_transformation()\n    group = group.transformed(transformation)\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DissociationLayout.end_arrowhead_length","title":"end_arrowhead_length","text":"<pre><code>end_arrowhead_length() -&gt; float\n</code></pre> <p>Return the length of the double-headed arc end arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_arrowhead_length(self) -&gt; float:\n    \"\"\"Return the length of the double-headed arc end arrowhead.\"\"\"\n    bbox = get_drawing_elements_bbox(self._end_arrowhead_border_drawing_elements())\n    if math.isnan(bbox.width):\n        return 0.0\n    return bbox.east().x\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DissociationLayout.end_arrowhead_tip","title":"end_arrowhead_tip","text":"<pre><code>end_arrowhead_tip() -&gt; Point\n</code></pre> <p>Return the tip anchor point of the double-headed arc end arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_arrowhead_tip(self) -&gt; Point:\n    \"\"\"Return the tip anchor point of the double-headed arc end arrowhead.\"\"\"\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return segment.p2\n    fraction = 1 - self.end_shorten / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DissociationLayout.end_point","title":"end_point","text":"<pre><code>end_point() -&gt; Point\n</code></pre> <p>Return the ending point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_point(self) -&gt; Point:\n    \"\"\"Return the ending point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[-1]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DissociationLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DissociationLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DissociationLayout.fraction","title":"fraction","text":"<pre><code>fraction(fraction: float) -&gt; tuple[Point, float]\n</code></pre> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def fraction(self, fraction: float) -&gt; tuple[Point, float]:\n    \"\"\"Return the position and angle on the arc at a given fraction (of the total arc length).\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    current_length = 0\n    length_to_reach = fraction * self.length()\n    for segment in self.segments:\n        current_length += segment.length()\n        if current_length &gt;= length_to_reach:\n            break\n    segment_start_length = current_length - segment.length()\n    segment_fraction = (length_to_reach - segment_start_length) / segment.length()\n    position, angle = segment.get_position_and_angle_at_fraction(segment_fraction)\n    return position, angle\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DissociationLayout.length","title":"length","text":"<pre><code>length() -&gt; float\n</code></pre> <p>Return the total length of the arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def length(self) -&gt; float:\n    \"\"\"Return the total length of the arc path.\"\"\"\n    return sum([segment.length() for segment in self.segments])\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DissociationLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DissociationLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the arc.\"\"\"\n    return []\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DissociationLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the arc's own drawing elements, including those of its mixins.</p> Source code in <code>src/momapy/celldesigner/elements.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the arc's own drawing elements, including those of its mixins.\"\"\"\n    drawing_elements = super_or_builder(\n        CellDesignerDoubleHeadedArc, self\n    ).own_drawing_elements()\n    done_bases = []\n    for base in type(self).__mro__:\n        if (\n            issubclass_or_builder(base, _SBGNMixin)\n            and base is not type(self)\n            and not any([issubclass(done_base, base) for done_base in done_bases])\n        ):\n            drawing_elements += getattr(base, \"_mixin_drawing_elements\")(self)\n            done_bases.append(base)\n    return drawing_elements\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DissociationLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DissociationLayout.path_drawing_elements","title":"path_drawing_elements","text":"<pre><code>path_drawing_elements() -&gt; list[Path]\n</code></pre> <p>Return the drawing elements of the double-headed arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def path_drawing_elements(self) -&gt; list[Path]:\n    \"\"\"Return the drawing elements of the double-headed arc path.\"\"\"\n    start_arrowhead_length = self.start_arrowhead_length()\n    end_arrowhead_length = self.end_arrowhead_length()\n    if len(self.segments) == 1:\n        segment = (\n            self.segments[0]\n            .shortened(self.start_shorten + start_arrowhead_length, \"start\")\n            .shortened(self.end_shorten + end_arrowhead_length, \"end\")\n        )\n        actions = [\n            MoveTo(segment.p1),\n            self._make_path_action_from_segment(segment),\n        ]\n    else:\n        first_segment = self.segments[0].shortened(\n            self.start_shorten + start_arrowhead_length, \"start\"\n        )\n        last_segment = self.segments[-1].shortened(\n            self.end_shorten + end_arrowhead_length, \"end\"\n        )\n        actions = [\n            MoveTo(first_segment.p1),\n            self._make_path_action_from_segment(first_segment),\n        ]\n        for segment in self.segments[1:-1]:\n            action = self._make_path_action_from_segment(segment)\n            actions.append(action)\n        actions.append(self._make_path_action_from_segment(last_segment))\n    path = Path(\n        actions=tuple(actions),\n        class_=f\"{type(self).__name__}_path\",\n        fill=self.path_fill,\n        filter_=self.path_filter,\n        id_=f\"{self.id_}_path\",\n        stroke=self.path_stroke,\n        stroke_dasharray=self.path_stroke_dasharray,\n        stroke_dashoffset=self.path_stroke_dashoffset,\n        stroke_width=self.path_stroke_width,\n        transform=self.path_transform,\n    )\n    return [path]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DissociationLayout.points","title":"points","text":"<pre><code>points() -&gt; list[Point]\n</code></pre> <p>Return the points of the arc path.</p> <p>An arc with no segments has no points and returns an empty list.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def points(self) -&gt; list[Point]:\n    \"\"\"Return the points of the arc path.\n\n    An arc with no segments has no points and returns an empty list.\n    \"\"\"\n    if not self.segments:\n        return []\n    points = []\n    for segment in self.segments:\n        points.append(segment.p1)\n    points.append(segment.p2)\n    return points\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DissociationLayout.start_arrowhead_base","title":"start_arrowhead_base","text":"<pre><code>start_arrowhead_base() -&gt; Point\n</code></pre> <p>Return the base anchor point of the double-headed arc start arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_arrowhead_base(self) -&gt; Point:\n    \"\"\"Return the base anchor point of the double-headed arc start arrowhead.\"\"\"\n    arrowhead_length = self.start_arrowhead_length()\n    if arrowhead_length == 0:\n        return self.start_point()\n    segment = self.segments[0]\n    segment = Segment(segment.p2, segment.p1)\n    segment_length = segment.length()\n    if segment_length == 0:\n        return self.start_arrowhead_tip() + (arrowhead_length, 0)\n    fraction = 1 - (arrowhead_length + self.start_shorten) / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DissociationLayout.start_arrowhead_bbox","title":"start_arrowhead_bbox","text":"<pre><code>start_arrowhead_bbox() -&gt; Bbox\n</code></pre> <p>Return the bounding box of the double-headed arc start arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_arrowhead_bbox(self) -&gt; Bbox:\n    \"\"\"Return the bounding box of the double-headed arc start arrowhead.\"\"\"\n    return get_drawing_elements_bbox(self.start_arrowhead_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DissociationLayout.start_arrowhead_border","title":"start_arrowhead_border","text":"<pre><code>start_arrowhead_border(point: Point) -&gt; Point\n</code></pre> <p>Return the point at the intersection of the drawing elements of the double-headed arc start arrowhead and the line going through the center of these drawing elements and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_arrowhead_border(self, point: Point) -&gt; Point:\n    \"\"\"Return the point at the intersection of the drawing elements of the double-headed arc start arrowhead and the line going through the center of these drawing elements and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    point = get_drawing_elements_border(\n        self.start_arrowhead_drawing_elements(), point\n    )\n    if point is None:\n        return self.start_arrowhead_tip()\n    return point\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DissociationLayout.start_arrowhead_drawing_elements","title":"start_arrowhead_drawing_elements","text":"<pre><code>start_arrowhead_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the double-headed arc start arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_arrowhead_drawing_elements(\n    self,\n) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the double-headed arc start arrowhead.\"\"\"\n    drawing_elements = self._start_arrowhead_border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_start_arrowhead\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}_start_arrowhead\",\n        fill=self.start_arrowhead_fill,\n        filter_=self.start_arrowhead_filter,\n        stroke=self.start_arrowhead_stroke,\n        stroke_dasharray=self.start_arrowhead_stroke_dasharray,\n        stroke_dashoffset=self.start_arrowhead_stroke_dashoffset,\n        stroke_width=self.start_arrowhead_stroke_width,\n        transform=self.start_arrowhead_transform,\n    )\n    transformation = self._get_start_arrowhead_transformation()\n    group = group.transformed(transformation)\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DissociationLayout.start_arrowhead_length","title":"start_arrowhead_length","text":"<pre><code>start_arrowhead_length() -&gt; float\n</code></pre> <p>Return the length of the double-headed arc start arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_arrowhead_length(self) -&gt; float:\n    \"\"\"Return the length of the double-headed arc start arrowhead.\"\"\"\n    bbox = get_drawing_elements_bbox(\n        self._start_arrowhead_border_drawing_elements()\n    )\n    if math.isnan(bbox.width):\n        return 0.0\n    return abs(bbox.west().x)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DissociationLayout.start_arrowhead_tip","title":"start_arrowhead_tip","text":"<pre><code>start_arrowhead_tip() -&gt; Point\n</code></pre> <p>Return the tip anchor point of the double-headed arc start arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_arrowhead_tip(self) -&gt; Point:\n    \"\"\"Return the tip anchor point of the double-headed arc start arrowhead.\"\"\"\n    segment = self.segments[0]\n    segment = Segment(segment.p2, segment.p1)\n    segment_length = segment.length()\n    if segment_length == 0:\n        return segment.p2\n    fraction = 1 - self.start_shorten / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DissociationLayout.start_point","title":"start_point","text":"<pre><code>start_point() -&gt; Point\n</code></pre> <p>Return the starting point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_point(self) -&gt; Point:\n    \"\"\"Return the starting point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[0]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DissociationLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.Drug","title":"Drug  <code>dataclass</code>","text":"<pre><code>Drug(*, id_: str = make_uuid4_as_str(), name: str | None = None, sbo_term: str | None = None, metaid: str | None = None, compartment: Compartment | None = None, hypothetical: bool = False, active: bool = False, homomultimer: int = 1)\n</code></pre> <p>               Bases: <code>Species</code></p> <p>Drug.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'c5ed6fcd-28d2-4b9d-a568-e9e416b31352'</code> <code>name</code> <code>str | None</code> <p>Human-readable name of the element.</p> <code>None</code> <code>sbo_term</code> <code>str | None</code> <p>Optional SBO term identifier for semantic annotation.</p> <code>None</code> <code>metaid</code> <code>str | None</code> <p>Optional metadata identifier for RDF annotations.</p> <code>None</code> <code>compartment</code> <code>Compartment | None</code> <p>The compartment containing this species.</p> <code>None</code> <code>hypothetical</code> <code>bool</code> <p>Whether the species is hypothetical or not</p> <code>False</code> <code>active</code> <code>bool</code> <p>Whether the species is active or not</p> <code>False</code> <code>homomultimer</code> <code>int</code> <p>The number of subunits forming the species</p> <code>1</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.Drug.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DrugActiveLayout","title":"DrugActiveLayout  <code>dataclass</code>","text":"<pre><code>DrugActiveLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = NoneValue, filter_: NoneValueType | Filter | None = None, height: float = 30.0 + DEFAULT_ACTIVE_YSEP * 2, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = (4, 2), stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = 1.0, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 80.0 + DEFAULT_ACTIVE_XSEP * 2, offset: float = 3.0, subunits_stroke: tuple[NoneValueType | Color, ...] | None = None, subunits_stroke_width: tuple[NoneValueType | float, ...] | None = None, subunits_stroke_dasharray: tuple[NoneValueType | tuple[float, ...], ...] | None = None, subunits_stroke_dashoffset: tuple[float, ...] | None = None, subunits_fill: tuple[NoneValueType | Color, ...] | None = None, subunits_transform: tuple[NoneValueType | tuple[Transformation, ...], ...] | None = None, subunits_filter: tuple[NoneValueType | Filter, ...] | None = None, n: int = 1, horizontal_proportion: float = 0.2, sep: float = 4.0)\n</code></pre> <p>               Bases: <code>_MultiNodeMixin</code>, <code>CellDesignerNode</code></p> <p>Drug active layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'4c04361b-2fc1-4555-9198-ca047c009bef'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>&lt;momapy.drawing.NoneValueType object at 0x7f8b17323e00&gt;</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>38.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>(4, 2)</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The stroke width of the node</p> <code>1.0</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>88.0</code> <code>offset</code> <code>float</code> <p>Offset distance between stacked units.</p> <code>3.0</code> <code>subunits_stroke</code> <code>tuple[NoneValueType | Color, ...] | None</code> <p>Tuple of stroke colors for each subunit.</p> <code>None</code> <code>subunits_stroke_width</code> <code>tuple[NoneValueType | float, ...] | None</code> <p>Tuple of stroke widths for each subunit.</p> <code>None</code> <code>subunits_stroke_dasharray</code> <code>tuple[NoneValueType | tuple[float, ...], ...] | None</code> <p>Tuple of dash patterns for each subunit.</p> <code>None</code> <code>subunits_stroke_dashoffset</code> <code>tuple[float, ...] | None</code> <p>Tuple of dash offsets for each subunit.</p> <code>None</code> <code>subunits_fill</code> <code>tuple[NoneValueType | Color, ...] | None</code> <p>Tuple of fill colors for each subunit.</p> <code>None</code> <code>subunits_transform</code> <code>tuple[NoneValueType | tuple[Transformation, ...], ...] | None</code> <p>Tuple of transformations for each subunit.</p> <code>None</code> <code>subunits_filter</code> <code>tuple[NoneValueType | Filter, ...] | None</code> <p>Tuple of filters for each subunit.</p> <code>None</code> <code>n</code> <code>int</code> <p>Number of stacked node copies to draw.</p> <code>1</code> <code>horizontal_proportion</code> <code>float</code> <p>The proportion of the width taken by each rounded end.</p> <code>0.2</code> <code>sep</code> <code>float</code> <p>The separation between the outer and inner outlines.</p> <code>4.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>angle</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Return the east anchor of the node.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Get the center position for the node label.</p> <code>north</code> <p>Return the north anchor of the node.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Return the south anchor of the node.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Return the west anchor of the node.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DrugActiveLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DrugActiveLayout.angle","title":"angle","text":"<pre><code>angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DrugActiveLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DrugActiveLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DrugActiveLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DrugActiveLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DrugActiveLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DrugActiveLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DrugActiveLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DrugActiveLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DrugActiveLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Return the east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Return the east anchor of the node.\"\"\"\n    return self.own_angle(0)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DrugActiveLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DrugActiveLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DrugActiveLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DrugActiveLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DrugActiveLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Get the center position for the node label.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point where the label should be centered.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Get the center position for the node label.\n\n    Returns:\n        Point where the label should be centered.\n    \"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2 - (self._n - 1) * self.offset,\n        self.height / 2 - height / 2 - (self._n - 1) * self.offset,\n    )\n    return self._make_subunit_shape(\n        position=position, width=width, height=height\n    ).position\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DrugActiveLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Return the north anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Return the north anchor of the node.\"\"\"\n    return self.own_angle(90)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DrugActiveLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DrugActiveLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DrugActiveLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DrugActiveLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DrugActiveLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DrugActiveLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DrugActiveLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DrugActiveLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DrugActiveLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DrugActiveLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DrugActiveLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DrugActiveLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Return the south anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Return the south anchor of the node.\"\"\"\n    return self.own_angle(270)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DrugActiveLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DrugActiveLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DrugActiveLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DrugActiveLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DrugActiveLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DrugActiveLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Return the west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Return the west anchor of the node.\"\"\"\n    return self.own_angle(180)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DrugActiveLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DrugActiveLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DrugActiveLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DrugActiveLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DrugLayout","title":"DrugLayout  <code>dataclass</code>","text":"<pre><code>DrugLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = from_hex('#FF00FF'), filter_: NoneValueType | Filter | None = None, height: float = 30.0, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = 1.0, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 80.0, offset: float = 3.0, subunits_stroke: tuple[NoneValueType | Color, ...] | None = None, subunits_stroke_width: tuple[NoneValueType | float, ...] | None = None, subunits_stroke_dasharray: tuple[NoneValueType | tuple[float, ...], ...] | None = None, subunits_stroke_dashoffset: tuple[float, ...] | None = None, subunits_fill: tuple[NoneValueType | Color, ...] | None = None, subunits_transform: tuple[NoneValueType | tuple[Transformation, ...], ...] | None = None, subunits_filter: tuple[NoneValueType | Filter, ...] | None = None, n: int = 1, horizontal_proportion: float = 0.2, sep: float = 4.0)\n</code></pre> <p>               Bases: <code>_MultiNodeMixin</code>, <code>CellDesignerNode</code></p> <p>Drug layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'cb62cc59-50b8-46a5-a532-195e3d8c1eeb'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>Color(red=255, green=0, blue=255, alpha=1.0)</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>30.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The stroke width of the node</p> <code>1.0</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>80.0</code> <code>offset</code> <code>float</code> <p>Offset distance between stacked units.</p> <code>3.0</code> <code>subunits_stroke</code> <code>tuple[NoneValueType | Color, ...] | None</code> <p>Tuple of stroke colors for each subunit.</p> <code>None</code> <code>subunits_stroke_width</code> <code>tuple[NoneValueType | float, ...] | None</code> <p>Tuple of stroke widths for each subunit.</p> <code>None</code> <code>subunits_stroke_dasharray</code> <code>tuple[NoneValueType | tuple[float, ...], ...] | None</code> <p>Tuple of dash patterns for each subunit.</p> <code>None</code> <code>subunits_stroke_dashoffset</code> <code>tuple[float, ...] | None</code> <p>Tuple of dash offsets for each subunit.</p> <code>None</code> <code>subunits_fill</code> <code>tuple[NoneValueType | Color, ...] | None</code> <p>Tuple of fill colors for each subunit.</p> <code>None</code> <code>subunits_transform</code> <code>tuple[NoneValueType | tuple[Transformation, ...], ...] | None</code> <p>Tuple of transformations for each subunit.</p> <code>None</code> <code>subunits_filter</code> <code>tuple[NoneValueType | Filter, ...] | None</code> <p>Tuple of filters for each subunit.</p> <code>None</code> <code>n</code> <code>int</code> <p>Number of stacked node copies to draw.</p> <code>1</code> <code>horizontal_proportion</code> <code>float</code> <p>The proportion of the width taken by each rounded end.</p> <code>0.2</code> <code>sep</code> <code>float</code> <p>The separation between the outer and inner outlines.</p> <code>4.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>angle</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Return the east anchor of the node.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Get the center position for the node label.</p> <code>north</code> <p>Return the north anchor of the node.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Return the south anchor of the node.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Return the west anchor of the node.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DrugLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DrugLayout.angle","title":"angle","text":"<pre><code>angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DrugLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DrugLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DrugLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DrugLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DrugLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DrugLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DrugLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DrugLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DrugLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Return the east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Return the east anchor of the node.\"\"\"\n    return self.own_angle(0)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DrugLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DrugLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DrugLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DrugLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DrugLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Get the center position for the node label.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point where the label should be centered.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Get the center position for the node label.\n\n    Returns:\n        Point where the label should be centered.\n    \"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2 - (self._n - 1) * self.offset,\n        self.height / 2 - height / 2 - (self._n - 1) * self.offset,\n    )\n    return self._make_subunit_shape(\n        position=position, width=width, height=height\n    ).position\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DrugLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Return the north anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Return the north anchor of the node.\"\"\"\n    return self.own_angle(90)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DrugLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DrugLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DrugLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DrugLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DrugLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DrugLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DrugLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DrugLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DrugLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DrugLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DrugLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DrugLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Return the south anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Return the south anchor of the node.\"\"\"\n    return self.own_angle(270)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DrugLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DrugLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DrugLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DrugLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DrugLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DrugLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Return the west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Return the west anchor of the node.\"\"\"\n    return self.own_angle(180)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DrugLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DrugLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DrugLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.DrugLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.Gene","title":"Gene  <code>dataclass</code>","text":"<pre><code>Gene(*, id_: str = make_uuid4_as_str(), name: str | None = None, sbo_term: str | None = None, metaid: str | None = None, compartment: Compartment | None = None, hypothetical: bool = False, active: bool = False, homomultimer: int = 1, template: GeneTemplate, modifications: frozenset[Modification] = frozenset())\n</code></pre> <p>               Bases: <code>Species</code></p> <p>Gene.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'d2a50442-e8b5-4fda-a4db-38705ad1a762'</code> <code>name</code> <code>str | None</code> <p>Human-readable name of the element.</p> <code>None</code> <code>sbo_term</code> <code>str | None</code> <p>Optional SBO term identifier for semantic annotation.</p> <code>None</code> <code>metaid</code> <code>str | None</code> <p>Optional metadata identifier for RDF annotations.</p> <code>None</code> <code>compartment</code> <code>Compartment | None</code> <p>The compartment containing this species.</p> <code>None</code> <code>hypothetical</code> <code>bool</code> <p>Whether the species is hypothetical or not</p> <code>False</code> <code>active</code> <code>bool</code> <p>Whether the species is active or not</p> <code>False</code> <code>homomultimer</code> <code>int</code> <p>The number of subunits forming the species</p> <code>1</code> <code>template</code> <code>GeneTemplate</code> <p>The template of the gene</p> required <code>modifications</code> <code>frozenset[Modification]</code> <p>The modifications of the gene</p> <code>&lt;dynamic&gt;</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.Gene.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GeneActiveLayout","title":"GeneActiveLayout  <code>dataclass</code>","text":"<pre><code>GeneActiveLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = NoneValue, filter_: NoneValueType | Filter | None = None, height: float = 25.0 + DEFAULT_ACTIVE_YSEP * 2, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = (4, 2), stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = 1.0, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 80.0 + DEFAULT_ACTIVE_XSEP * 2, offset: float = 3.0, subunits_stroke: tuple[NoneValueType | Color, ...] | None = None, subunits_stroke_width: tuple[NoneValueType | float, ...] | None = None, subunits_stroke_dasharray: tuple[NoneValueType | tuple[float, ...], ...] | None = None, subunits_stroke_dashoffset: tuple[float, ...] | None = None, subunits_fill: tuple[NoneValueType | Color, ...] | None = None, subunits_transform: tuple[NoneValueType | tuple[Transformation, ...], ...] | None = None, subunits_filter: tuple[NoneValueType | Filter, ...] | None = None, n: int = 1)\n</code></pre> <p>               Bases: <code>_MultiNodeMixin</code>, <code>CellDesignerNode</code></p> <p>Gene active layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'b0f947d2-1f13-4afd-921c-2e90a21232cf'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>&lt;momapy.drawing.NoneValueType object at 0x7f8b17323e00&gt;</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>33.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>(4, 2)</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The stroke width of the node</p> <code>1.0</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>88.0</code> <code>offset</code> <code>float</code> <p>Offset distance between stacked units.</p> <code>3.0</code> <code>subunits_stroke</code> <code>tuple[NoneValueType | Color, ...] | None</code> <p>Tuple of stroke colors for each subunit.</p> <code>None</code> <code>subunits_stroke_width</code> <code>tuple[NoneValueType | float, ...] | None</code> <p>Tuple of stroke widths for each subunit.</p> <code>None</code> <code>subunits_stroke_dasharray</code> <code>tuple[NoneValueType | tuple[float, ...], ...] | None</code> <p>Tuple of dash patterns for each subunit.</p> <code>None</code> <code>subunits_stroke_dashoffset</code> <code>tuple[float, ...] | None</code> <p>Tuple of dash offsets for each subunit.</p> <code>None</code> <code>subunits_fill</code> <code>tuple[NoneValueType | Color, ...] | None</code> <p>Tuple of fill colors for each subunit.</p> <code>None</code> <code>subunits_transform</code> <code>tuple[NoneValueType | tuple[Transformation, ...], ...] | None</code> <p>Tuple of transformations for each subunit.</p> <code>None</code> <code>subunits_filter</code> <code>tuple[NoneValueType | Filter, ...] | None</code> <p>Tuple of filters for each subunit.</p> <code>None</code> <code>n</code> <code>int</code> <p>Number of stacked node copies to draw.</p> <code>1</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>angle</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Return the east anchor of the node.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Get the center position for the node label.</p> <code>north</code> <p>Return the north anchor of the node.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Return the south anchor of the node.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Return the west anchor of the node.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GeneActiveLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GeneActiveLayout.angle","title":"angle","text":"<pre><code>angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GeneActiveLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GeneActiveLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GeneActiveLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GeneActiveLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GeneActiveLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GeneActiveLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GeneActiveLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GeneActiveLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GeneActiveLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Return the east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Return the east anchor of the node.\"\"\"\n    return self.own_angle(0)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GeneActiveLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GeneActiveLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GeneActiveLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GeneActiveLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GeneActiveLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Get the center position for the node label.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point where the label should be centered.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Get the center position for the node label.\n\n    Returns:\n        Point where the label should be centered.\n    \"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2 - (self._n - 1) * self.offset,\n        self.height / 2 - height / 2 - (self._n - 1) * self.offset,\n    )\n    return self._make_subunit_shape(\n        position=position, width=width, height=height\n    ).position\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GeneActiveLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Return the north anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Return the north anchor of the node.\"\"\"\n    return self.own_angle(90)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GeneActiveLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GeneActiveLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GeneActiveLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GeneActiveLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GeneActiveLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GeneActiveLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GeneActiveLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GeneActiveLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GeneActiveLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GeneActiveLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GeneActiveLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GeneActiveLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Return the south anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Return the south anchor of the node.\"\"\"\n    return self.own_angle(270)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GeneActiveLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GeneActiveLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GeneActiveLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GeneActiveLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GeneActiveLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GeneActiveLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Return the west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Return the west anchor of the node.\"\"\"\n    return self.own_angle(180)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GeneActiveLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GeneActiveLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GeneActiveLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GeneActiveLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GeneLayout","title":"GeneLayout  <code>dataclass</code>","text":"<pre><code>GeneLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = from_hex('#FFFFCC'), filter_: NoneValueType | Filter | None = None, height: float = 25.0, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = 1.0, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 80.0, offset: float = 3.0, subunits_stroke: tuple[NoneValueType | Color, ...] | None = None, subunits_stroke_width: tuple[NoneValueType | float, ...] | None = None, subunits_stroke_dasharray: tuple[NoneValueType | tuple[float, ...], ...] | None = None, subunits_stroke_dashoffset: tuple[float, ...] | None = None, subunits_fill: tuple[NoneValueType | Color, ...] | None = None, subunits_transform: tuple[NoneValueType | tuple[Transformation, ...], ...] | None = None, subunits_filter: tuple[NoneValueType | Filter, ...] | None = None, n: int = 1)\n</code></pre> <p>               Bases: <code>_MultiNodeMixin</code>, <code>CellDesignerNode</code></p> <p>Gene layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'4d3e848a-609b-4c04-8a4b-dccca20db046'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>Color(red=255, green=255, blue=204, alpha=1.0)</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>25.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The stroke width of the node</p> <code>1.0</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>80.0</code> <code>offset</code> <code>float</code> <p>Offset distance between stacked units.</p> <code>3.0</code> <code>subunits_stroke</code> <code>tuple[NoneValueType | Color, ...] | None</code> <p>Tuple of stroke colors for each subunit.</p> <code>None</code> <code>subunits_stroke_width</code> <code>tuple[NoneValueType | float, ...] | None</code> <p>Tuple of stroke widths for each subunit.</p> <code>None</code> <code>subunits_stroke_dasharray</code> <code>tuple[NoneValueType | tuple[float, ...], ...] | None</code> <p>Tuple of dash patterns for each subunit.</p> <code>None</code> <code>subunits_stroke_dashoffset</code> <code>tuple[float, ...] | None</code> <p>Tuple of dash offsets for each subunit.</p> <code>None</code> <code>subunits_fill</code> <code>tuple[NoneValueType | Color, ...] | None</code> <p>Tuple of fill colors for each subunit.</p> <code>None</code> <code>subunits_transform</code> <code>tuple[NoneValueType | tuple[Transformation, ...], ...] | None</code> <p>Tuple of transformations for each subunit.</p> <code>None</code> <code>subunits_filter</code> <code>tuple[NoneValueType | Filter, ...] | None</code> <p>Tuple of filters for each subunit.</p> <code>None</code> <code>n</code> <code>int</code> <p>Number of stacked node copies to draw.</p> <code>1</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>angle</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Return the east anchor of the node.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Get the center position for the node label.</p> <code>north</code> <p>Return the north anchor of the node.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Return the south anchor of the node.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Return the west anchor of the node.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GeneLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GeneLayout.angle","title":"angle","text":"<pre><code>angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GeneLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GeneLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GeneLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GeneLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GeneLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GeneLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GeneLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GeneLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GeneLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Return the east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Return the east anchor of the node.\"\"\"\n    return self.own_angle(0)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GeneLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GeneLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GeneLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GeneLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GeneLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Get the center position for the node label.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point where the label should be centered.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Get the center position for the node label.\n\n    Returns:\n        Point where the label should be centered.\n    \"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2 - (self._n - 1) * self.offset,\n        self.height / 2 - height / 2 - (self._n - 1) * self.offset,\n    )\n    return self._make_subunit_shape(\n        position=position, width=width, height=height\n    ).position\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GeneLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Return the north anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Return the north anchor of the node.\"\"\"\n    return self.own_angle(90)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GeneLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GeneLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GeneLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GeneLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GeneLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GeneLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GeneLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GeneLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GeneLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GeneLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GeneLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GeneLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Return the south anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Return the south anchor of the node.\"\"\"\n    return self.own_angle(270)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GeneLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GeneLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GeneLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GeneLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GeneLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GeneLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Return the west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Return the west anchor of the node.\"\"\"\n    return self.own_angle(180)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GeneLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GeneLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GeneLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GeneLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GeneTemplate","title":"GeneTemplate  <code>dataclass</code>","text":"<pre><code>GeneTemplate(*, id_: str = make_uuid4_as_str(), name: str, regions: frozenset[ModificationSite | CodingRegion | RegulatoryRegion | TranscriptionStartingSiteL | TranscriptionStartingSiteR] = frozenset())\n</code></pre> <p>               Bases: <code>SpeciesTemplate</code></p> <p>Gene template.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'b977f95b-5714-4ce6-a09a-c302b0e9f99a'</code> <code>name</code> <code>str</code> <p>The name of the species template</p> required <code>regions</code> <code>frozenset[ModificationSite | CodingRegion | RegulatoryRegion | TranscriptionStartingSiteL | TranscriptionStartingSiteR]</code> <p>The regions of the gene template</p> <code>&lt;dynamic&gt;</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GeneTemplate.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GenericProtein","title":"GenericProtein  <code>dataclass</code>","text":"<pre><code>GenericProtein(*, id_: str = make_uuid4_as_str(), name: str | None = None, sbo_term: str | None = None, metaid: str | None = None, compartment: Compartment | None = None, hypothetical: bool = False, active: bool = False, homomultimer: int = 1, template: GenericProteinTemplate, modifications: frozenset[Modification] = frozenset(), structural_states: frozenset[StructuralState] = frozenset())\n</code></pre> <p>               Bases: <code>Protein</code></p> <p>Generic protein.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'1d915680-f45f-45a2-95aa-5c99fdd6afe3'</code> <code>name</code> <code>str | None</code> <p>Human-readable name of the element.</p> <code>None</code> <code>sbo_term</code> <code>str | None</code> <p>Optional SBO term identifier for semantic annotation.</p> <code>None</code> <code>metaid</code> <code>str | None</code> <p>Optional metadata identifier for RDF annotations.</p> <code>None</code> <code>compartment</code> <code>Compartment | None</code> <p>The compartment containing this species.</p> <code>None</code> <code>hypothetical</code> <code>bool</code> <p>Whether the species is hypothetical or not</p> <code>False</code> <code>active</code> <code>bool</code> <p>Whether the species is active or not</p> <code>False</code> <code>homomultimer</code> <code>int</code> <p>The number of subunits forming the species</p> <code>1</code> <code>template</code> <code>GenericProteinTemplate</code> <p>The template of the generic protein</p> required <code>modifications</code> <code>frozenset[Modification]</code> <p>The modifications of the protein</p> <code>&lt;dynamic&gt;</code> <code>structural_states</code> <code>frozenset[StructuralState]</code> <p>The structural states of the protein</p> <code>&lt;dynamic&gt;</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GenericProtein.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GenericProteinActiveLayout","title":"GenericProteinActiveLayout  <code>dataclass</code>","text":"<pre><code>GenericProteinActiveLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = NoneValue, filter_: NoneValueType | Filter | None = None, height: float = 40.0 + DEFAULT_ACTIVE_YSEP * 2, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = (4, 2), stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = 1.0, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 80.0 + DEFAULT_ACTIVE_XSEP * 2, offset: float = 3.0, subunits_stroke: tuple[NoneValueType | Color, ...] | None = None, subunits_stroke_width: tuple[NoneValueType | float, ...] | None = None, subunits_stroke_dasharray: tuple[NoneValueType | tuple[float, ...], ...] | None = None, subunits_stroke_dashoffset: tuple[float, ...] | None = None, subunits_fill: tuple[NoneValueType | Color, ...] | None = None, subunits_transform: tuple[NoneValueType | tuple[Transformation, ...], ...] | None = None, subunits_filter: tuple[NoneValueType | Filter, ...] | None = None, n: int = 1, rounded_corners: float = 5.0)\n</code></pre> <p>               Bases: <code>_MultiNodeMixin</code>, <code>CellDesignerNode</code></p> <p>Generic protein active layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'5344b247-8697-4e75-bd52-03ec024293d3'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>&lt;momapy.drawing.NoneValueType object at 0x7f8b17323e00&gt;</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>48.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>(4, 2)</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The stroke width of the node</p> <code>1.0</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>88.0</code> <code>offset</code> <code>float</code> <p>Offset distance between stacked units.</p> <code>3.0</code> <code>subunits_stroke</code> <code>tuple[NoneValueType | Color, ...] | None</code> <p>Tuple of stroke colors for each subunit.</p> <code>None</code> <code>subunits_stroke_width</code> <code>tuple[NoneValueType | float, ...] | None</code> <p>Tuple of stroke widths for each subunit.</p> <code>None</code> <code>subunits_stroke_dasharray</code> <code>tuple[NoneValueType | tuple[float, ...], ...] | None</code> <p>Tuple of dash patterns for each subunit.</p> <code>None</code> <code>subunits_stroke_dashoffset</code> <code>tuple[float, ...] | None</code> <p>Tuple of dash offsets for each subunit.</p> <code>None</code> <code>subunits_fill</code> <code>tuple[NoneValueType | Color, ...] | None</code> <p>Tuple of fill colors for each subunit.</p> <code>None</code> <code>subunits_transform</code> <code>tuple[NoneValueType | tuple[Transformation, ...], ...] | None</code> <p>Tuple of transformations for each subunit.</p> <code>None</code> <code>subunits_filter</code> <code>tuple[NoneValueType | Filter, ...] | None</code> <p>Tuple of filters for each subunit.</p> <code>None</code> <code>n</code> <code>int</code> <p>Number of stacked node copies to draw.</p> <code>1</code> <code>rounded_corners</code> <code>float</code> <p>The radius of the rounded corners.</p> <code>5.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>angle</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Return the east anchor of the node.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Get the center position for the node label.</p> <code>north</code> <p>Return the north anchor of the node.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Return the south anchor of the node.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Return the west anchor of the node.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GenericProteinActiveLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GenericProteinActiveLayout.angle","title":"angle","text":"<pre><code>angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GenericProteinActiveLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GenericProteinActiveLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GenericProteinActiveLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GenericProteinActiveLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GenericProteinActiveLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GenericProteinActiveLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GenericProteinActiveLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GenericProteinActiveLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GenericProteinActiveLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Return the east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Return the east anchor of the node.\"\"\"\n    return self.own_angle(0)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GenericProteinActiveLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GenericProteinActiveLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GenericProteinActiveLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GenericProteinActiveLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GenericProteinActiveLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Get the center position for the node label.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point where the label should be centered.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Get the center position for the node label.\n\n    Returns:\n        Point where the label should be centered.\n    \"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2 - (self._n - 1) * self.offset,\n        self.height / 2 - height / 2 - (self._n - 1) * self.offset,\n    )\n    return self._make_subunit_shape(\n        position=position, width=width, height=height\n    ).position\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GenericProteinActiveLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Return the north anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Return the north anchor of the node.\"\"\"\n    return self.own_angle(90)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GenericProteinActiveLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GenericProteinActiveLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GenericProteinActiveLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GenericProteinActiveLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GenericProteinActiveLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GenericProteinActiveLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GenericProteinActiveLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GenericProteinActiveLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GenericProteinActiveLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GenericProteinActiveLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GenericProteinActiveLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GenericProteinActiveLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Return the south anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Return the south anchor of the node.\"\"\"\n    return self.own_angle(270)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GenericProteinActiveLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GenericProteinActiveLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GenericProteinActiveLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GenericProteinActiveLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GenericProteinActiveLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GenericProteinActiveLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Return the west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Return the west anchor of the node.\"\"\"\n    return self.own_angle(180)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GenericProteinActiveLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GenericProteinActiveLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GenericProteinActiveLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GenericProteinActiveLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GenericProteinLayout","title":"GenericProteinLayout  <code>dataclass</code>","text":"<pre><code>GenericProteinLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = from_hex('#CCFFCC'), filter_: NoneValueType | Filter | None = None, height: float = 40.0, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = 1.0, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 80.0, offset: float = 3.0, subunits_stroke: tuple[NoneValueType | Color, ...] | None = None, subunits_stroke_width: tuple[NoneValueType | float, ...] | None = None, subunits_stroke_dasharray: tuple[NoneValueType | tuple[float, ...], ...] | None = None, subunits_stroke_dashoffset: tuple[float, ...] | None = None, subunits_fill: tuple[NoneValueType | Color, ...] | None = None, subunits_transform: tuple[NoneValueType | tuple[Transformation, ...], ...] | None = None, subunits_filter: tuple[NoneValueType | Filter, ...] | None = None, n: int = 1, rounded_corners: float = 5.0)\n</code></pre> <p>               Bases: <code>_MultiNodeMixin</code>, <code>CellDesignerNode</code></p> <p>Generic protein layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'234d2078-5635-4632-9118-9ac3e749ee28'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>Color(red=204, green=255, blue=204, alpha=1.0)</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>40.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The stroke width of the node</p> <code>1.0</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>80.0</code> <code>offset</code> <code>float</code> <p>Offset distance between stacked units.</p> <code>3.0</code> <code>subunits_stroke</code> <code>tuple[NoneValueType | Color, ...] | None</code> <p>Tuple of stroke colors for each subunit.</p> <code>None</code> <code>subunits_stroke_width</code> <code>tuple[NoneValueType | float, ...] | None</code> <p>Tuple of stroke widths for each subunit.</p> <code>None</code> <code>subunits_stroke_dasharray</code> <code>tuple[NoneValueType | tuple[float, ...], ...] | None</code> <p>Tuple of dash patterns for each subunit.</p> <code>None</code> <code>subunits_stroke_dashoffset</code> <code>tuple[float, ...] | None</code> <p>Tuple of dash offsets for each subunit.</p> <code>None</code> <code>subunits_fill</code> <code>tuple[NoneValueType | Color, ...] | None</code> <p>Tuple of fill colors for each subunit.</p> <code>None</code> <code>subunits_transform</code> <code>tuple[NoneValueType | tuple[Transformation, ...], ...] | None</code> <p>Tuple of transformations for each subunit.</p> <code>None</code> <code>subunits_filter</code> <code>tuple[NoneValueType | Filter, ...] | None</code> <p>Tuple of filters for each subunit.</p> <code>None</code> <code>n</code> <code>int</code> <p>Number of stacked node copies to draw.</p> <code>1</code> <code>rounded_corners</code> <code>float</code> <p>The radius of the rounded corners.</p> <code>5.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>angle</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Return the east anchor of the node.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Get the center position for the node label.</p> <code>north</code> <p>Return the north anchor of the node.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Return the south anchor of the node.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Return the west anchor of the node.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GenericProteinLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GenericProteinLayout.angle","title":"angle","text":"<pre><code>angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GenericProteinLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GenericProteinLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GenericProteinLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GenericProteinLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GenericProteinLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GenericProteinLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GenericProteinLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GenericProteinLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GenericProteinLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Return the east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Return the east anchor of the node.\"\"\"\n    return self.own_angle(0)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GenericProteinLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GenericProteinLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GenericProteinLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GenericProteinLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GenericProteinLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Get the center position for the node label.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point where the label should be centered.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Get the center position for the node label.\n\n    Returns:\n        Point where the label should be centered.\n    \"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2 - (self._n - 1) * self.offset,\n        self.height / 2 - height / 2 - (self._n - 1) * self.offset,\n    )\n    return self._make_subunit_shape(\n        position=position, width=width, height=height\n    ).position\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GenericProteinLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Return the north anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Return the north anchor of the node.\"\"\"\n    return self.own_angle(90)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GenericProteinLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GenericProteinLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GenericProteinLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GenericProteinLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GenericProteinLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GenericProteinLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GenericProteinLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GenericProteinLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GenericProteinLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GenericProteinLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GenericProteinLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GenericProteinLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Return the south anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Return the south anchor of the node.\"\"\"\n    return self.own_angle(270)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GenericProteinLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GenericProteinLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GenericProteinLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GenericProteinLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GenericProteinLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GenericProteinLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Return the west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Return the west anchor of the node.\"\"\"\n    return self.own_angle(180)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GenericProteinLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GenericProteinLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GenericProteinLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GenericProteinLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GenericProteinTemplate","title":"GenericProteinTemplate  <code>dataclass</code>","text":"<pre><code>GenericProteinTemplate(*, id_: str = make_uuid4_as_str(), name: str, modification_residues: frozenset[ModificationResidue] = frozenset())\n</code></pre> <p>               Bases: <code>ProteinTemplate</code></p> <p>Generic protein template.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'86027c20-8b9c-4146-a4ee-e3d33e343cef'</code> <code>name</code> <code>str</code> <p>The name of the species template</p> required <code>modification_residues</code> <code>frozenset[ModificationResidue]</code> <p>The modification residues of the protein template</p> <code>&lt;dynamic&gt;</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.GenericProteinTemplate.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.HeterodimerAssociation","title":"HeterodimerAssociation  <code>dataclass</code>","text":"<pre><code>HeterodimerAssociation(*, id_: str = make_uuid4_as_str(), name: str | None = None, sbo_term: str | None = None, metaid: str | None = None, reversible: bool = False, compartment: Compartment | None = None, reactants: frozenset[Reactant] = frozenset(), products: frozenset[Product] = frozenset(), modifiers: frozenset[KnownOrUnknownModulator] = frozenset(), has_external_source: bool = False, has_external_sink: bool = False)\n</code></pre> <p>               Bases: <code>Reaction</code></p> <p>Heterodimer association.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'f54defc0-e402-4da8-9814-a18e887f0a36'</code> <code>name</code> <code>str | None</code> <p>Human-readable name of the element.</p> <code>None</code> <code>sbo_term</code> <code>str | None</code> <p>Optional SBO term identifier for semantic annotation.</p> <code>None</code> <code>metaid</code> <code>str | None</code> <p>Optional metadata identifier for RDF annotations.</p> <code>None</code> <code>reversible</code> <code>bool</code> <p>Whether the reaction can proceed in both directions.</p> <code>False</code> <code>compartment</code> <code>Compartment | None</code> <p>Optional compartment where the reaction occurs.</p> <code>None</code> <code>reactants</code> <code>frozenset[Reactant]</code> <p>The reactants of the reaction</p> <code>&lt;dynamic&gt;</code> <code>products</code> <code>frozenset[Product]</code> <p>The products of the reaction</p> <code>&lt;dynamic&gt;</code> <code>modifiers</code> <code>frozenset[KnownOrUnknownModulator]</code> <p>The modifiers of the reaction</p> <code>&lt;dynamic&gt;</code> <code>has_external_source</code> <code>bool</code> <p>Whether the reaction has an unspecified external source (a Degraded reactant in CellDesigner).</p> <code>False</code> <code>has_external_sink</code> <code>bool</code> <p>Whether the reaction has an unspecified external sink (a Degraded product in CellDesigner).</p> <code>False</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.HeterodimerAssociation.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.HeterodimerAssociationLayout","title":"HeterodimerAssociationLayout  <code>dataclass</code>","text":"<pre><code>HeterodimerAssociationLayout(*, left_connector_fraction: float = 0.375, right_connector_fraction: float = 0.625, reaction_node_font_family: str = DEFAULT_FONT_FAMILY, reaction_node_font_fill: Color | NoneValueType = black, reaction_node_font_stroke: Color | NoneValueType = NoneValue, reaction_node_font_style: FontStyle = NORMAL, reaction_node_font_weight: FontWeight | float = NORMAL, reaction_node_text: str | None = None, reaction_node_height: float = 10.0, reaction_node_width: float = 10.0, reaction_node_segment: int = 1, reaction_node_stroke: NoneValueType | Color | None = black, reaction_node_stroke_width: float | None = 1.0, reaction_node_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, reaction_node_stroke_dashoffset: float | None = None, reaction_node_fill: NoneValueType | Color | None = white, reaction_node_transform: NoneValueType | tuple[Transformation, ...] | None = None, reaction_node_filter: NoneValueType | Filter | None = None, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, end_shorten: float = 2.0, fill: NoneValueType | Color | None = None, filter_: NoneValueType | Filter | None = None, path_fill: NoneValueType | Color | None = NoneValue, path_filter: NoneValueType | Filter | None = None, path_stroke: NoneValueType | Color | None = black, path_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, path_stroke_dashoffset: NoneValueType | float | None = None, path_stroke_width: float | None = 1.0, path_transform: NoneValueType | tuple[Transformation, ...] | None = None, stroke: NoneValueType | Color | None = None, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: NoneValueType | float | None = None, segments: tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...] = tuple(), source: LayoutElement | None = None, start_shorten: float = 2.0, target: LayoutElement | None = None, transform: NoneValueType | tuple[Transformation, ...] | None = None, end_arrowhead_fill: NoneValueType | Color | None = black, end_arrowhead_filter: NoneValueType | Filter | None = None, end_arrowhead_stroke: NoneValueType | Color | None = black, end_arrowhead_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, end_arrowhead_stroke_dashoffset: float | None = None, end_arrowhead_stroke_width: float | None = 1.0, end_arrowhead_transform: NoneValueType | tuple[Transformation, ...] | None = None, start_arrowhead_fill: NoneValueType | Color | None = black, start_arrowhead_filter: NoneValueType | Filter | None = None, start_arrowhead_stroke: NoneValueType | Color | None = black, start_arrowhead_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, start_arrowhead_stroke_dashoffset: float | None = None, start_arrowhead_stroke_width: float | None = 1.0, start_arrowhead_transform: NoneValueType | tuple[Transformation, ...] | None = None, reversible: bool = False, end_arrowhead_height: float = 8.0, end_arrowhead_width: float = 15.0, start_arrowhead_height: float = 6.0, start_arrowhead_width: float = 6.0)\n</code></pre> <p>               Bases: <code>ReactionLayout</code>, <code>_ReactionNodeMixin</code></p> <p>Heterodimer association layout.</p> <p>Parameters:</p> Name Type Description Default <code>left_connector_fraction</code> <code>float</code> <p>The fraction along the segment where the left connector attaches.</p> <code>0.375</code> <code>right_connector_fraction</code> <code>float</code> <p>The fraction along the segment where the right connector attaches.</p> <code>0.625</code> <code>reaction_node_font_family</code> <code>str</code> <p>The font family of the reaction node label.</p> <code>'DejaVu Sans'</code> <code>reaction_node_font_fill</code> <code>Color | NoneValueType</code> <p>The fill color of the reaction node label.</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>reaction_node_font_stroke</code> <code>Color | NoneValueType</code> <p>The stroke color of the reaction node label.</p> <code>&lt;momapy.drawing.NoneValueType object at 0x7f8b17323e00&gt;</code> <code>reaction_node_font_style</code> <code>FontStyle</code> <p>The font style of the reaction node label.</p> <code>&lt;FontStyle.NORMAL: 0&gt;</code> <code>reaction_node_font_weight</code> <code>FontWeight | float</code> <p>The font weight of the reaction node label.</p> <code>&lt;FontWeight.NORMAL: 0&gt;</code> <code>reaction_node_text</code> <code>str | None</code> <p>The text of the reaction node label.</p> <code>None</code> <code>reaction_node_height</code> <code>float</code> <p>The height of the reaction node.</p> <code>10.0</code> <code>reaction_node_width</code> <code>float</code> <p>The width of the reaction node.</p> <code>10.0</code> <code>reaction_node_segment</code> <code>int</code> <p>The index of the arc segment carrying the reaction node.</p> <code>1</code> <code>reaction_node_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the reaction node.</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>reaction_node_stroke_width</code> <code>float | None</code> <p>The stroke width of the reaction node.</p> <code>1.0</code> <code>reaction_node_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The dash pattern of the reaction node's border.</p> <code>None</code> <code>reaction_node_stroke_dashoffset</code> <code>float | None</code> <p>The dash offset of the reaction node's border.</p> <code>None</code> <code>reaction_node_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the reaction node.</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>reaction_node_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform applied to the reaction node.</p> <code>None</code> <code>reaction_node_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter applied to the reaction node.</p> <code>None</code> <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'713454ee-6b25-4ee3-98ed-3f9b7d267e64'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>end_shorten</code> <code>float</code> <p>The length the end of the arc will be shorten by</p> <code>2.0</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the arc</p> <code>None</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the arc</p> <code>None</code> <code>path_fill</code> <code>NoneValueType | Color | None</code> <p>The path fill color of the arc</p> <code>&lt;momapy.drawing.NoneValueType object at 0x7f8b17323e00&gt;</code> <code>path_filter</code> <code>NoneValueType | Filter | None</code> <p>The path filter of the arc</p> <code>None</code> <code>path_stroke</code> <code>NoneValueType | Color | None</code> <p>The path stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>path_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The path stroke dasharray of the arc</p> <code>None</code> <code>path_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The path stroke dashoffset of the arc</p> <code>None</code> <code>path_stroke_width</code> <code>float | None</code> <p>The path stroke width of the arc</p> <code>1.0</code> <code>path_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The path transform of the arc</p> <code>None</code> <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the arc</p> <code>None</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the arc</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the arc</p> <code>None</code> <code>stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the arc</p> <code>None</code> <code>segments</code> <code>tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...]</code> <p>The path segments of the arc</p> <code>&lt;dynamic&gt;</code> <code>source</code> <code>LayoutElement | None</code> <p>The source of the arc</p> <code>None</code> <code>start_shorten</code> <code>float</code> <p>The length the start of the arc will be shorten by</p> <code>2.0</code> <code>target</code> <code>LayoutElement | None</code> <p>The target of the arc</p> <code>None</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the arc</p> <code>None</code> <code>end_arrowhead_fill</code> <code>NoneValueType | Color | None</code> <p>The end arrowhead fill color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>end_arrowhead_filter</code> <code>NoneValueType | Filter | None</code> <p>The end arrowhead filter of the arc</p> <code>None</code> <code>end_arrowhead_stroke</code> <code>NoneValueType | Color | None</code> <p>The end arrowhead stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>end_arrowhead_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The end arrowhead stroke dasharray of the arc</p> <code>None</code> <code>end_arrowhead_stroke_dashoffset</code> <code>float | None</code> <p>The end arrowhead stroke dashoffset of the arc</p> <code>None</code> <code>end_arrowhead_stroke_width</code> <code>float | None</code> <p>The end arrowhead stroke width of the arc</p> <code>1.0</code> <code>end_arrowhead_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The end arrowhead transform of the arc</p> <code>None</code> <code>start_arrowhead_fill</code> <code>NoneValueType | Color | None</code> <p>The start arrowhead fill color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>start_arrowhead_filter</code> <code>NoneValueType | Filter | None</code> <p>The start arrowhead filter of the arc</p> <code>None</code> <code>start_arrowhead_stroke</code> <code>NoneValueType | Color | None</code> <p>The start arrowhead stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>start_arrowhead_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The start arrowhead stroke dasharray of the arc</p> <code>None</code> <code>start_arrowhead_stroke_dashoffset</code> <code>float | None</code> <p>The start arrowhead stroke dashoffset of the arc</p> <code>None</code> <code>start_arrowhead_stroke_width</code> <code>float | None</code> <p>The start arrowhead stroke width of the arc</p> <code>1.0</code> <code>start_arrowhead_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The start arrowhead transform of the arc</p> <code>None</code> <code>reversible</code> <code>bool</code> <p>Whether the reaction is reversible.</p> <code>False</code> <code>end_arrowhead_height</code> <code>float</code> <p>The height of the end arrowhead.</p> <code>8.0</code> <code>end_arrowhead_width</code> <code>float</code> <p>The width of the end arrowhead.</p> <code>15.0</code> <code>start_arrowhead_height</code> <code>float</code> <p>The height of the start arrowhead.</p> <code>6.0</code> <code>start_arrowhead_width</code> <code>float</code> <p>The width of the start arrowhead.</p> <code>6.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>childless</code> <p>Return a copy of the arc with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>end_arrowhead_base</code> <p>Return the base anchor point of the double-headed arc end arrowhead.</p> <code>end_arrowhead_bbox</code> <p>Return the bounding box of the double-headed arc end arrowhead.</p> <code>end_arrowhead_border</code> <p>Return the point at the intersection of the drawing elements of the double-headed arc end arrowhead and the line going through the center of these drawing elements and the given point.</p> <code>end_arrowhead_drawing_elements</code> <p>Return the drawing elements of the double-headed arc end arrowhead.</p> <code>end_arrowhead_length</code> <p>Return the length of the double-headed arc end arrowhead.</p> <code>end_arrowhead_tip</code> <p>Return the tip anchor point of the double-headed arc end arrowhead.</p> <code>end_point</code> <p>Return the ending point of the arc.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>fraction</code> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <code>length</code> <p>Return the total length of the arc path.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_children</code> <p>Return the self children of the arc.</p> <code>own_drawing_elements</code> <p>Return the arc's own drawing elements, including those of its mixins.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>path_drawing_elements</code> <p>Return the drawing elements of the double-headed arc path.</p> <code>points</code> <p>Return the points of the arc path.</p> <code>start_arrowhead_base</code> <p>Return the base anchor point of the double-headed arc start arrowhead.</p> <code>start_arrowhead_bbox</code> <p>Return the bounding box of the double-headed arc start arrowhead.</p> <code>start_arrowhead_border</code> <p>Return the point at the intersection of the drawing elements of the double-headed arc start arrowhead and the line going through the center of these drawing elements and the given point.</p> <code>start_arrowhead_drawing_elements</code> <p>Return the drawing elements of the double-headed arc start arrowhead.</p> <code>start_arrowhead_length</code> <p>Return the length of the double-headed arc start arrowhead.</p> <code>start_arrowhead_tip</code> <p>Return the tip anchor point of the double-headed arc start arrowhead.</p> <code>start_point</code> <p>Return the starting point of the arc.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.HeterodimerAssociationLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.HeterodimerAssociationLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.HeterodimerAssociationLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the arc with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the arc with no children.\"\"\"\n    return dataclasses.replace(self, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.HeterodimerAssociationLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.HeterodimerAssociationLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.HeterodimerAssociationLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.HeterodimerAssociationLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.HeterodimerAssociationLayout.end_arrowhead_base","title":"end_arrowhead_base","text":"<pre><code>end_arrowhead_base() -&gt; Point\n</code></pre> <p>Return the base anchor point of the double-headed arc end arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_arrowhead_base(self) -&gt; Point:\n    \"\"\"Return the base anchor point of the double-headed arc end arrowhead.\"\"\"\n    arrowhead_length = self.end_arrowhead_length()\n    if arrowhead_length == 0:\n        return self.end_point()\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return self.end_arrowhead_tip() - (arrowhead_length, 0)\n    fraction = 1 - (arrowhead_length + self.end_shorten) / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.HeterodimerAssociationLayout.end_arrowhead_bbox","title":"end_arrowhead_bbox","text":"<pre><code>end_arrowhead_bbox() -&gt; Bbox\n</code></pre> <p>Return the bounding box of the double-headed arc end arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_arrowhead_bbox(self) -&gt; Bbox:\n    \"\"\"Return the bounding box of the double-headed arc end arrowhead.\"\"\"\n    return get_drawing_elements_bbox(self.end_arrowhead_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.HeterodimerAssociationLayout.end_arrowhead_border","title":"end_arrowhead_border","text":"<pre><code>end_arrowhead_border(point: Point) -&gt; Point\n</code></pre> <p>Return the point at the intersection of the drawing elements of the double-headed arc end arrowhead and the line going through the center of these drawing elements and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_arrowhead_border(self, point: Point) -&gt; Point:\n    \"\"\"Return the point at the intersection of the drawing elements of the double-headed arc end arrowhead and the line going through the center of these drawing elements and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    point = get_drawing_elements_border(\n        self.end_arrowhead_drawing_elements(), point\n    )\n    if point is None:\n        return self.end_arrowhead_tip()\n    return point\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.HeterodimerAssociationLayout.end_arrowhead_drawing_elements","title":"end_arrowhead_drawing_elements","text":"<pre><code>end_arrowhead_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the double-headed arc end arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_arrowhead_drawing_elements(\n    self,\n) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the double-headed arc end arrowhead.\"\"\"\n    drawing_elements = self._end_arrowhead_border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_end_arrowhead\",\n        elements=tuple(drawing_elements),\n        fill=self.end_arrowhead_fill,\n        filter_=self.end_arrowhead_filter,\n        id_=f\"{self.id_}_end_arrowhead\",\n        stroke=self.end_arrowhead_stroke,\n        stroke_width=self.end_arrowhead_stroke_width,\n        stroke_dasharray=self.end_arrowhead_stroke_dasharray,\n        stroke_dashoffset=self.end_arrowhead_stroke_dashoffset,\n        transform=self.end_arrowhead_transform,\n    )\n    transformation = self._get_end_arrowhead_transformation()\n    group = group.transformed(transformation)\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.HeterodimerAssociationLayout.end_arrowhead_length","title":"end_arrowhead_length","text":"<pre><code>end_arrowhead_length() -&gt; float\n</code></pre> <p>Return the length of the double-headed arc end arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_arrowhead_length(self) -&gt; float:\n    \"\"\"Return the length of the double-headed arc end arrowhead.\"\"\"\n    bbox = get_drawing_elements_bbox(self._end_arrowhead_border_drawing_elements())\n    if math.isnan(bbox.width):\n        return 0.0\n    return bbox.east().x\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.HeterodimerAssociationLayout.end_arrowhead_tip","title":"end_arrowhead_tip","text":"<pre><code>end_arrowhead_tip() -&gt; Point\n</code></pre> <p>Return the tip anchor point of the double-headed arc end arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_arrowhead_tip(self) -&gt; Point:\n    \"\"\"Return the tip anchor point of the double-headed arc end arrowhead.\"\"\"\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return segment.p2\n    fraction = 1 - self.end_shorten / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.HeterodimerAssociationLayout.end_point","title":"end_point","text":"<pre><code>end_point() -&gt; Point\n</code></pre> <p>Return the ending point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_point(self) -&gt; Point:\n    \"\"\"Return the ending point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[-1]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.HeterodimerAssociationLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.HeterodimerAssociationLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.HeterodimerAssociationLayout.fraction","title":"fraction","text":"<pre><code>fraction(fraction: float) -&gt; tuple[Point, float]\n</code></pre> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def fraction(self, fraction: float) -&gt; tuple[Point, float]:\n    \"\"\"Return the position and angle on the arc at a given fraction (of the total arc length).\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    current_length = 0\n    length_to_reach = fraction * self.length()\n    for segment in self.segments:\n        current_length += segment.length()\n        if current_length &gt;= length_to_reach:\n            break\n    segment_start_length = current_length - segment.length()\n    segment_fraction = (length_to_reach - segment_start_length) / segment.length()\n    position, angle = segment.get_position_and_angle_at_fraction(segment_fraction)\n    return position, angle\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.HeterodimerAssociationLayout.length","title":"length","text":"<pre><code>length() -&gt; float\n</code></pre> <p>Return the total length of the arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def length(self) -&gt; float:\n    \"\"\"Return the total length of the arc path.\"\"\"\n    return sum([segment.length() for segment in self.segments])\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.HeterodimerAssociationLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.HeterodimerAssociationLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the arc.\"\"\"\n    return []\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.HeterodimerAssociationLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the arc's own drawing elements, including those of its mixins.</p> Source code in <code>src/momapy/celldesigner/elements.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the arc's own drawing elements, including those of its mixins.\"\"\"\n    drawing_elements = super_or_builder(\n        CellDesignerDoubleHeadedArc, self\n    ).own_drawing_elements()\n    done_bases = []\n    for base in type(self).__mro__:\n        if (\n            issubclass_or_builder(base, _SBGNMixin)\n            and base is not type(self)\n            and not any([issubclass(done_base, base) for done_base in done_bases])\n        ):\n            drawing_elements += getattr(base, \"_mixin_drawing_elements\")(self)\n            done_bases.append(base)\n    return drawing_elements\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.HeterodimerAssociationLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.HeterodimerAssociationLayout.path_drawing_elements","title":"path_drawing_elements","text":"<pre><code>path_drawing_elements() -&gt; list[Path]\n</code></pre> <p>Return the drawing elements of the double-headed arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def path_drawing_elements(self) -&gt; list[Path]:\n    \"\"\"Return the drawing elements of the double-headed arc path.\"\"\"\n    start_arrowhead_length = self.start_arrowhead_length()\n    end_arrowhead_length = self.end_arrowhead_length()\n    if len(self.segments) == 1:\n        segment = (\n            self.segments[0]\n            .shortened(self.start_shorten + start_arrowhead_length, \"start\")\n            .shortened(self.end_shorten + end_arrowhead_length, \"end\")\n        )\n        actions = [\n            MoveTo(segment.p1),\n            self._make_path_action_from_segment(segment),\n        ]\n    else:\n        first_segment = self.segments[0].shortened(\n            self.start_shorten + start_arrowhead_length, \"start\"\n        )\n        last_segment = self.segments[-1].shortened(\n            self.end_shorten + end_arrowhead_length, \"end\"\n        )\n        actions = [\n            MoveTo(first_segment.p1),\n            self._make_path_action_from_segment(first_segment),\n        ]\n        for segment in self.segments[1:-1]:\n            action = self._make_path_action_from_segment(segment)\n            actions.append(action)\n        actions.append(self._make_path_action_from_segment(last_segment))\n    path = Path(\n        actions=tuple(actions),\n        class_=f\"{type(self).__name__}_path\",\n        fill=self.path_fill,\n        filter_=self.path_filter,\n        id_=f\"{self.id_}_path\",\n        stroke=self.path_stroke,\n        stroke_dasharray=self.path_stroke_dasharray,\n        stroke_dashoffset=self.path_stroke_dashoffset,\n        stroke_width=self.path_stroke_width,\n        transform=self.path_transform,\n    )\n    return [path]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.HeterodimerAssociationLayout.points","title":"points","text":"<pre><code>points() -&gt; list[Point]\n</code></pre> <p>Return the points of the arc path.</p> <p>An arc with no segments has no points and returns an empty list.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def points(self) -&gt; list[Point]:\n    \"\"\"Return the points of the arc path.\n\n    An arc with no segments has no points and returns an empty list.\n    \"\"\"\n    if not self.segments:\n        return []\n    points = []\n    for segment in self.segments:\n        points.append(segment.p1)\n    points.append(segment.p2)\n    return points\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.HeterodimerAssociationLayout.start_arrowhead_base","title":"start_arrowhead_base","text":"<pre><code>start_arrowhead_base() -&gt; Point\n</code></pre> <p>Return the base anchor point of the double-headed arc start arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_arrowhead_base(self) -&gt; Point:\n    \"\"\"Return the base anchor point of the double-headed arc start arrowhead.\"\"\"\n    arrowhead_length = self.start_arrowhead_length()\n    if arrowhead_length == 0:\n        return self.start_point()\n    segment = self.segments[0]\n    segment = Segment(segment.p2, segment.p1)\n    segment_length = segment.length()\n    if segment_length == 0:\n        return self.start_arrowhead_tip() + (arrowhead_length, 0)\n    fraction = 1 - (arrowhead_length + self.start_shorten) / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.HeterodimerAssociationLayout.start_arrowhead_bbox","title":"start_arrowhead_bbox","text":"<pre><code>start_arrowhead_bbox() -&gt; Bbox\n</code></pre> <p>Return the bounding box of the double-headed arc start arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_arrowhead_bbox(self) -&gt; Bbox:\n    \"\"\"Return the bounding box of the double-headed arc start arrowhead.\"\"\"\n    return get_drawing_elements_bbox(self.start_arrowhead_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.HeterodimerAssociationLayout.start_arrowhead_border","title":"start_arrowhead_border","text":"<pre><code>start_arrowhead_border(point: Point) -&gt; Point\n</code></pre> <p>Return the point at the intersection of the drawing elements of the double-headed arc start arrowhead and the line going through the center of these drawing elements and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_arrowhead_border(self, point: Point) -&gt; Point:\n    \"\"\"Return the point at the intersection of the drawing elements of the double-headed arc start arrowhead and the line going through the center of these drawing elements and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    point = get_drawing_elements_border(\n        self.start_arrowhead_drawing_elements(), point\n    )\n    if point is None:\n        return self.start_arrowhead_tip()\n    return point\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.HeterodimerAssociationLayout.start_arrowhead_drawing_elements","title":"start_arrowhead_drawing_elements","text":"<pre><code>start_arrowhead_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the double-headed arc start arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_arrowhead_drawing_elements(\n    self,\n) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the double-headed arc start arrowhead.\"\"\"\n    drawing_elements = self._start_arrowhead_border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_start_arrowhead\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}_start_arrowhead\",\n        fill=self.start_arrowhead_fill,\n        filter_=self.start_arrowhead_filter,\n        stroke=self.start_arrowhead_stroke,\n        stroke_dasharray=self.start_arrowhead_stroke_dasharray,\n        stroke_dashoffset=self.start_arrowhead_stroke_dashoffset,\n        stroke_width=self.start_arrowhead_stroke_width,\n        transform=self.start_arrowhead_transform,\n    )\n    transformation = self._get_start_arrowhead_transformation()\n    group = group.transformed(transformation)\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.HeterodimerAssociationLayout.start_arrowhead_length","title":"start_arrowhead_length","text":"<pre><code>start_arrowhead_length() -&gt; float\n</code></pre> <p>Return the length of the double-headed arc start arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_arrowhead_length(self) -&gt; float:\n    \"\"\"Return the length of the double-headed arc start arrowhead.\"\"\"\n    bbox = get_drawing_elements_bbox(\n        self._start_arrowhead_border_drawing_elements()\n    )\n    if math.isnan(bbox.width):\n        return 0.0\n    return abs(bbox.west().x)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.HeterodimerAssociationLayout.start_arrowhead_tip","title":"start_arrowhead_tip","text":"<pre><code>start_arrowhead_tip() -&gt; Point\n</code></pre> <p>Return the tip anchor point of the double-headed arc start arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_arrowhead_tip(self) -&gt; Point:\n    \"\"\"Return the tip anchor point of the double-headed arc start arrowhead.\"\"\"\n    segment = self.segments[0]\n    segment = Segment(segment.p2, segment.p1)\n    segment_length = segment.length()\n    if segment_length == 0:\n        return segment.p2\n    fraction = 1 - self.start_shorten / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.HeterodimerAssociationLayout.start_point","title":"start_point","text":"<pre><code>start_point() -&gt; Point\n</code></pre> <p>Return the starting point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_point(self) -&gt; Point:\n    \"\"\"Return the starting point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[0]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.HeterodimerAssociationLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.Inhibition","title":"Inhibition  <code>dataclass</code>","text":"<pre><code>Inhibition(*, id_: str = make_uuid4_as_str(), source: Species | BooleanLogicGate, target: Species | None)\n</code></pre> <p>               Bases: <code>Modulation</code></p> <p>Inhibition.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'54f55039-78ea-424f-9506-5bdea523d148'</code> <code>source</code> <code>Species | BooleanLogicGate</code> <p>The source of the modulation</p> required <code>target</code> <code>Species | None</code> <p>The target of the modulation</p> required <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.Inhibition.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.InhibitionLayout","title":"InhibitionLayout  <code>dataclass</code>","text":"<pre><code>InhibitionLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, end_shorten: float = 3.0, fill: NoneValueType | Color | None = None, filter_: NoneValueType | Filter | None = None, path_fill: NoneValueType | Color | None = NoneValue, path_filter: NoneValueType | Filter | None = None, path_stroke: NoneValueType | Color | None = black, path_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, path_stroke_dashoffset: NoneValueType | float | None = None, path_stroke_width: float | None = 1.0, path_transform: NoneValueType | tuple[Transformation, ...] | None = None, stroke: NoneValueType | Color | None = None, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: NoneValueType | float | None = None, segments: tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...] = tuple(), source: LayoutElement | None = None, start_shorten: float = 0.0, target: LayoutElement | None = None, transform: NoneValueType | tuple[Transformation, ...] | None = None, arrowhead_fill: NoneValueType | Color | None = None, arrowhead_filter: NoneValueType | Filter | None = None, arrowhead_stroke: NoneValueType | Color | None = black, arrowhead_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, arrowhead_stroke_dashoffset: NoneValueType | float | None = None, arrowhead_stroke_width: float | None = 1.0, arrowhead_transform: NoneValueType | tuple[Transformation, ...] | None = None, arrowhead_height: float = 10.0)\n</code></pre> <p>               Bases: <code>CellDesignerSingleHeadedArc</code></p> <p>Inhibition layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'47095883-42a3-463d-957d-a1ebd8c00216'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>end_shorten</code> <code>float</code> <p>The length the end of the arc will be shorten by</p> <code>3.0</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the arc</p> <code>None</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the arc</p> <code>None</code> <code>path_fill</code> <code>NoneValueType | Color | None</code> <p>The path fill color of the arc</p> <code>&lt;momapy.drawing.NoneValueType object at 0x7f8b17323e00&gt;</code> <code>path_filter</code> <code>NoneValueType | Filter | None</code> <p>The path filter of the arc</p> <code>None</code> <code>path_stroke</code> <code>NoneValueType | Color | None</code> <p>The path stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>path_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The path stroke dasharray of the arc</p> <code>None</code> <code>path_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The path stroke dashoffset of the arc</p> <code>None</code> <code>path_stroke_width</code> <code>float | None</code> <p>The path stroke width of the arc</p> <code>1.0</code> <code>path_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The path transform of the arc</p> <code>None</code> <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the arc</p> <code>None</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the arc</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the arc</p> <code>None</code> <code>stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the arc</p> <code>None</code> <code>segments</code> <code>tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...]</code> <p>The path segments of the arc</p> <code>&lt;dynamic&gt;</code> <code>source</code> <code>LayoutElement | None</code> <p>The source of the arc</p> <code>None</code> <code>start_shorten</code> <code>float</code> <p>The length the start of the arc will be shorten by</p> <code>0.0</code> <code>target</code> <code>LayoutElement | None</code> <p>The target of the arc</p> <code>None</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the arc</p> <code>None</code> <code>arrowhead_fill</code> <code>NoneValueType | Color | None</code> <p>The arrowhead fill color of the arc</p> <code>None</code> <code>arrowhead_filter</code> <code>NoneValueType | Filter | None</code> <p>The arrowhead filter of the arc</p> <code>None</code> <code>arrowhead_stroke</code> <code>NoneValueType | Color | None</code> <p>The arrowhead stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>arrowhead_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The arrowhead stroke dasharray of the arc</p> <code>None</code> <code>arrowhead_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The arrowhead stroke dashoffset of the arc</p> <code>None</code> <code>arrowhead_stroke_width</code> <code>float | None</code> <p>The arrowhead stroke width of the arc</p> <code>1.0</code> <code>arrowhead_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The arrowhead transform of the arc</p> <code>None</code> <code>arrowhead_height</code> <code>float</code> <p>The height of the arrowhead bar.</p> <code>10.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>arrowhead_base</code> <p>Return the arrowhead base anchor point of the single-headed arc.</p> <code>arrowhead_bbox</code> <p>Return the bounding box of the single-headed arc arrowhead.</p> <code>arrowhead_border</code> <p>Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.</p> <code>arrowhead_drawing_elements</code> <p>Return the drawing elements of the single-headed arc arrowhead.</p> <code>arrowhead_length</code> <p>Return the length of the single-headed arc arrowhead.</p> <code>arrowhead_tip</code> <p>Return the arrowhead tip anchor point of the single-headed arc.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>childless</code> <p>Return a copy of the arc with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>end_point</code> <p>Return the ending point of the arc.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>fraction</code> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <code>length</code> <p>Return the total length of the arc path.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_children</code> <p>Return the self children of the arc.</p> <code>own_drawing_elements</code> <p>Return the arc's own drawing elements, including those of its mixins.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>path_drawing_elements</code> <p>Return the drawing elements of the single-headed arc path.</p> <code>points</code> <p>Return the points of the arc path.</p> <code>start_point</code> <p>Return the starting point of the arc.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.InhibitionLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.InhibitionLayout.arrowhead_base","title":"arrowhead_base","text":"<pre><code>arrowhead_base() -&gt; Point\n</code></pre> <p>Return the arrowhead base anchor point of the single-headed arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_base(self) -&gt; Point:\n    \"\"\"Return the arrowhead base anchor point of the single-headed arc.\"\"\"\n    arrowhead_length = self.arrowhead_length()\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return self.arrowhead_tip() - (arrowhead_length, 0)\n    fraction = 1 - (arrowhead_length + self.end_shorten) / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.InhibitionLayout.arrowhead_bbox","title":"arrowhead_bbox","text":"<pre><code>arrowhead_bbox() -&gt; Bbox\n</code></pre> <p>Return the bounding box of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_bbox(self) -&gt; Bbox:\n    \"\"\"Return the bounding box of the single-headed arc arrowhead.\"\"\"\n    return get_drawing_elements_bbox(self.arrowhead_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.InhibitionLayout.arrowhead_border","title":"arrowhead_border","text":"<pre><code>arrowhead_border(point: Point) -&gt; Point\n</code></pre> <p>Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_border(self, point: Point) -&gt; Point:\n    \"\"\"Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    point = get_drawing_elements_border(self.arrowhead_drawing_elements(), point)\n    if point is None:\n        return self.arrowhead_tip()\n    return point\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.InhibitionLayout.arrowhead_drawing_elements","title":"arrowhead_drawing_elements","text":"<pre><code>arrowhead_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_drawing_elements(\n    self,\n) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the single-headed arc arrowhead.\"\"\"\n    drawing_elements = self._arrowhead_border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_arrowhead\",\n        elements=tuple(drawing_elements),\n        fill=self.arrowhead_fill,\n        filter_=self.arrowhead_filter,\n        id_=f\"{self.id_}_arrowhead\",\n        stroke=self.arrowhead_stroke,\n        stroke_dasharray=self.arrowhead_stroke_dasharray,\n        stroke_dashoffset=self.arrowhead_stroke_dashoffset,\n        stroke_width=self.arrowhead_stroke_width,\n        transform=self.arrowhead_transform,\n    )\n    transformation = self._get_arrowhead_transformation()\n    group = group.transformed(transformation)\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.InhibitionLayout.arrowhead_length","title":"arrowhead_length","text":"<pre><code>arrowhead_length() -&gt; float\n</code></pre> <p>Return the length of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_length(self) -&gt; float:\n    \"\"\"Return the length of the single-headed arc arrowhead.\"\"\"\n    bbox = get_drawing_elements_bbox(self._arrowhead_border_drawing_elements())\n    if math.isnan(bbox.width):\n        return 0.0\n    return bbox.east().x\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.InhibitionLayout.arrowhead_tip","title":"arrowhead_tip","text":"<pre><code>arrowhead_tip() -&gt; Point\n</code></pre> <p>Return the arrowhead tip anchor point of the single-headed arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_tip(self) -&gt; Point:\n    \"\"\"Return the arrowhead tip anchor point of the single-headed arc.\"\"\"\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return segment.p2\n    fraction = 1 - self.end_shorten / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.InhibitionLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.InhibitionLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the arc with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the arc with no children.\"\"\"\n    return dataclasses.replace(self, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.InhibitionLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.InhibitionLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.InhibitionLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.InhibitionLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.InhibitionLayout.end_point","title":"end_point","text":"<pre><code>end_point() -&gt; Point\n</code></pre> <p>Return the ending point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_point(self) -&gt; Point:\n    \"\"\"Return the ending point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[-1]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.InhibitionLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.InhibitionLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.InhibitionLayout.fraction","title":"fraction","text":"<pre><code>fraction(fraction: float) -&gt; tuple[Point, float]\n</code></pre> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def fraction(self, fraction: float) -&gt; tuple[Point, float]:\n    \"\"\"Return the position and angle on the arc at a given fraction (of the total arc length).\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    current_length = 0\n    length_to_reach = fraction * self.length()\n    for segment in self.segments:\n        current_length += segment.length()\n        if current_length &gt;= length_to_reach:\n            break\n    segment_start_length = current_length - segment.length()\n    segment_fraction = (length_to_reach - segment_start_length) / segment.length()\n    position, angle = segment.get_position_and_angle_at_fraction(segment_fraction)\n    return position, angle\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.InhibitionLayout.length","title":"length","text":"<pre><code>length() -&gt; float\n</code></pre> <p>Return the total length of the arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def length(self) -&gt; float:\n    \"\"\"Return the total length of the arc path.\"\"\"\n    return sum([segment.length() for segment in self.segments])\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.InhibitionLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.InhibitionLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the arc.\"\"\"\n    return []\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.InhibitionLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the arc's own drawing elements, including those of its mixins.</p> Source code in <code>src/momapy/celldesigner/elements.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the arc's own drawing elements, including those of its mixins.\"\"\"\n    drawing_elements = super_or_builder(\n        CellDesignerSingleHeadedArc, self\n    ).own_drawing_elements()\n    done_bases = []\n    for base in type(self).__mro__:\n        if (\n            issubclass_or_builder(base, _SBGNMixin)\n            and base is not type(self)\n            and not any([issubclass(done_base, base) for done_base in done_bases])\n        ):\n            drawing_elements += getattr(base, \"_mixin_drawing_elements\")(self)\n            done_bases.append(base)\n    return drawing_elements\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.InhibitionLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.InhibitionLayout.path_drawing_elements","title":"path_drawing_elements","text":"<pre><code>path_drawing_elements() -&gt; list[Path]\n</code></pre> <p>Return the drawing elements of the single-headed arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def path_drawing_elements(self) -&gt; list[Path]:\n    \"\"\"Return the drawing elements of the single-headed arc path.\"\"\"\n    arrowhead_length = self.arrowhead_length()\n    if len(self.segments) == 1:\n        segment = (\n            self.segments[0]\n            .shortened(self.start_shorten, \"start\")\n            .shortened(self.end_shorten + arrowhead_length, \"end\")\n        )\n        actions = [\n            MoveTo(segment.p1),\n            self._make_path_action_from_segment(segment),\n        ]\n    else:\n        first_segment = self.segments[0].shortened(self.start_shorten, \"start\")\n        last_segment = self.segments[-1].shortened(\n            self.end_shorten + arrowhead_length, \"end\"\n        )\n        actions = [\n            MoveTo(first_segment.p1),\n            self._make_path_action_from_segment(first_segment),\n        ]\n        for segment in self.segments[1:-1]:\n            action = self._make_path_action_from_segment(segment)\n            actions.append(action)\n        actions.append(self._make_path_action_from_segment(last_segment))\n    path = Path(\n        actions=tuple(actions),\n        class_=f\"{type(self).__name__}_path\",\n        fill=self.path_fill,\n        filter_=self.path_filter,\n        id_=f\"{self.id_}_path\",\n        stroke=self.path_stroke,\n        stroke_dasharray=self.path_stroke_dasharray,\n        stroke_dashoffset=self.path_stroke_dashoffset,\n        stroke_width=self.path_stroke_width,\n        transform=self.path_transform,\n    )\n    return [path]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.InhibitionLayout.points","title":"points","text":"<pre><code>points() -&gt; list[Point]\n</code></pre> <p>Return the points of the arc path.</p> <p>An arc with no segments has no points and returns an empty list.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def points(self) -&gt; list[Point]:\n    \"\"\"Return the points of the arc path.\n\n    An arc with no segments has no points and returns an empty list.\n    \"\"\"\n    if not self.segments:\n        return []\n    points = []\n    for segment in self.segments:\n        points.append(segment.p1)\n    points.append(segment.p2)\n    return points\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.InhibitionLayout.start_point","title":"start_point","text":"<pre><code>start_point() -&gt; Point\n</code></pre> <p>Return the starting point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_point(self) -&gt; Point:\n    \"\"\"Return the starting point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[0]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.InhibitionLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.Inhibitor","title":"Inhibitor  <code>dataclass</code>","text":"<pre><code>Inhibitor(*, id_: str = make_uuid4_as_str(), name: str | None = None, sbo_term: str | None = None, metaid: str | None = None, referred_element: Species | BooleanLogicGate)\n</code></pre> <p>               Bases: <code>Modulator</code></p> <p>Inhibitor.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'da650028-6e52-4edb-a742-acf5f3e6ec09'</code> <code>name</code> <code>str | None</code> <p>Human-readable name of the element.</p> <code>None</code> <code>sbo_term</code> <code>str | None</code> <p>Optional SBO term identifier for semantic annotation.</p> <code>None</code> <code>metaid</code> <code>str | None</code> <p>Optional metadata identifier for RDF annotations.</p> <code>None</code> <code>referred_element</code> <code>Species | BooleanLogicGate</code> <p>The species or boolean logic gate the modifier refers to</p> required <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.Inhibitor.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.Ion","title":"Ion  <code>dataclass</code>","text":"<pre><code>Ion(*, id_: str = make_uuid4_as_str(), name: str | None = None, sbo_term: str | None = None, metaid: str | None = None, compartment: Compartment | None = None, hypothetical: bool = False, active: bool = False, homomultimer: int = 1)\n</code></pre> <p>               Bases: <code>Species</code></p> <p>Ion.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'fe98a6bb-f4a5-4553-a780-30c4f6d326d5'</code> <code>name</code> <code>str | None</code> <p>Human-readable name of the element.</p> <code>None</code> <code>sbo_term</code> <code>str | None</code> <p>Optional SBO term identifier for semantic annotation.</p> <code>None</code> <code>metaid</code> <code>str | None</code> <p>Optional metadata identifier for RDF annotations.</p> <code>None</code> <code>compartment</code> <code>Compartment | None</code> <p>The compartment containing this species.</p> <code>None</code> <code>hypothetical</code> <code>bool</code> <p>Whether the species is hypothetical or not</p> <code>False</code> <code>active</code> <code>bool</code> <p>Whether the species is active or not</p> <code>False</code> <code>homomultimer</code> <code>int</code> <p>The number of subunits forming the species</p> <code>1</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.Ion.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonActiveLayout","title":"IonActiveLayout  <code>dataclass</code>","text":"<pre><code>IonActiveLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = NoneValue, filter_: NoneValueType | Filter | None = None, height: float = 35.0 + DEFAULT_ACTIVE_YSEP * 2, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = (4, 2), stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = 1.0, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 35.0 + DEFAULT_ACTIVE_XSEP * 2, offset: float = 3.0, subunits_stroke: tuple[NoneValueType | Color, ...] | None = None, subunits_stroke_width: tuple[NoneValueType | float, ...] | None = None, subunits_stroke_dasharray: tuple[NoneValueType | tuple[float, ...], ...] | None = None, subunits_stroke_dashoffset: tuple[float, ...] | None = None, subunits_fill: tuple[NoneValueType | Color, ...] | None = None, subunits_transform: tuple[NoneValueType | tuple[Transformation, ...], ...] | None = None, subunits_filter: tuple[NoneValueType | Filter, ...] | None = None, n: int = 1)\n</code></pre> <p>               Bases: <code>_MultiNodeMixin</code>, <code>CellDesignerNode</code></p> <p>Ion active layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'796077fc-e76f-43b1-9ecf-dd3008fb5501'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>&lt;momapy.drawing.NoneValueType object at 0x7f8b17323e00&gt;</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>43.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>(4, 2)</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The stroke width of the node</p> <code>1.0</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>43.0</code> <code>offset</code> <code>float</code> <p>Offset distance between stacked units.</p> <code>3.0</code> <code>subunits_stroke</code> <code>tuple[NoneValueType | Color, ...] | None</code> <p>Tuple of stroke colors for each subunit.</p> <code>None</code> <code>subunits_stroke_width</code> <code>tuple[NoneValueType | float, ...] | None</code> <p>Tuple of stroke widths for each subunit.</p> <code>None</code> <code>subunits_stroke_dasharray</code> <code>tuple[NoneValueType | tuple[float, ...], ...] | None</code> <p>Tuple of dash patterns for each subunit.</p> <code>None</code> <code>subunits_stroke_dashoffset</code> <code>tuple[float, ...] | None</code> <p>Tuple of dash offsets for each subunit.</p> <code>None</code> <code>subunits_fill</code> <code>tuple[NoneValueType | Color, ...] | None</code> <p>Tuple of fill colors for each subunit.</p> <code>None</code> <code>subunits_transform</code> <code>tuple[NoneValueType | tuple[Transformation, ...], ...] | None</code> <p>Tuple of transformations for each subunit.</p> <code>None</code> <code>subunits_filter</code> <code>tuple[NoneValueType | Filter, ...] | None</code> <p>Tuple of filters for each subunit.</p> <code>None</code> <code>n</code> <code>int</code> <p>Number of stacked node copies to draw.</p> <code>1</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>angle</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Return the east anchor of the node.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Get the center position for the node label.</p> <code>north</code> <p>Return the north anchor of the node.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Return the south anchor of the node.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Return the west anchor of the node.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonActiveLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonActiveLayout.angle","title":"angle","text":"<pre><code>angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonActiveLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonActiveLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonActiveLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonActiveLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonActiveLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonActiveLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonActiveLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonActiveLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonActiveLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Return the east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Return the east anchor of the node.\"\"\"\n    return self.own_angle(0)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonActiveLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonActiveLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonActiveLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonActiveLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonActiveLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Get the center position for the node label.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point where the label should be centered.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Get the center position for the node label.\n\n    Returns:\n        Point where the label should be centered.\n    \"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2 - (self._n - 1) * self.offset,\n        self.height / 2 - height / 2 - (self._n - 1) * self.offset,\n    )\n    return self._make_subunit_shape(\n        position=position, width=width, height=height\n    ).position\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonActiveLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Return the north anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Return the north anchor of the node.\"\"\"\n    return self.own_angle(90)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonActiveLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonActiveLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonActiveLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonActiveLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonActiveLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonActiveLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonActiveLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonActiveLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonActiveLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonActiveLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonActiveLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonActiveLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Return the south anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Return the south anchor of the node.\"\"\"\n    return self.own_angle(270)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonActiveLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonActiveLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonActiveLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonActiveLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonActiveLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonActiveLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Return the west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Return the west anchor of the node.\"\"\"\n    return self.own_angle(180)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonActiveLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonActiveLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonActiveLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonActiveLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonChannel","title":"IonChannel  <code>dataclass</code>","text":"<pre><code>IonChannel(*, id_: str = make_uuid4_as_str(), name: str | None = None, sbo_term: str | None = None, metaid: str | None = None, compartment: Compartment | None = None, hypothetical: bool = False, active: bool = False, homomultimer: int = 1, template: IonChannelTemplate, modifications: frozenset[Modification] = frozenset(), structural_states: frozenset[StructuralState] = frozenset())\n</code></pre> <p>               Bases: <code>Protein</code></p> <p>Ion channel.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'a21ba827-189f-497d-b796-edf59f76c39b'</code> <code>name</code> <code>str | None</code> <p>Human-readable name of the element.</p> <code>None</code> <code>sbo_term</code> <code>str | None</code> <p>Optional SBO term identifier for semantic annotation.</p> <code>None</code> <code>metaid</code> <code>str | None</code> <p>Optional metadata identifier for RDF annotations.</p> <code>None</code> <code>compartment</code> <code>Compartment | None</code> <p>The compartment containing this species.</p> <code>None</code> <code>hypothetical</code> <code>bool</code> <p>Whether the species is hypothetical or not</p> <code>False</code> <code>active</code> <code>bool</code> <p>Whether the species is active or not</p> <code>False</code> <code>homomultimer</code> <code>int</code> <p>The number of subunits forming the species</p> <code>1</code> <code>template</code> <code>IonChannelTemplate</code> <p>The template of the ion channel</p> required <code>modifications</code> <code>frozenset[Modification]</code> <p>The modifications of the protein</p> <code>&lt;dynamic&gt;</code> <code>structural_states</code> <code>frozenset[StructuralState]</code> <p>The structural states of the protein</p> <code>&lt;dynamic&gt;</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonChannel.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonChannelActiveLayout","title":"IonChannelActiveLayout  <code>dataclass</code>","text":"<pre><code>IonChannelActiveLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = NoneValue, filter_: NoneValueType | Filter | None = None, height: float = 40.0 + DEFAULT_ACTIVE_YSEP * 2, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = (4, 2), stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = 1.0, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 80.0 + DEFAULT_ACTIVE_XSEP * 2, offset: float = 3.0, subunits_stroke: tuple[NoneValueType | Color, ...] | None = None, subunits_stroke_width: tuple[NoneValueType | float, ...] | None = None, subunits_stroke_dasharray: tuple[NoneValueType | tuple[float, ...], ...] | None = None, subunits_stroke_dashoffset: tuple[float, ...] | None = None, subunits_fill: tuple[NoneValueType | Color, ...] | None = None, subunits_transform: tuple[NoneValueType | tuple[Transformation, ...], ...] | None = None, subunits_filter: tuple[NoneValueType | Filter, ...] | None = None, n: int = 1, rounded_corners: float = 5.0, right_rectangle_width: float = 20.0)\n</code></pre> <p>               Bases: <code>_MultiNodeMixin</code>, <code>CellDesignerNode</code></p> <p>Ion channel active layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'e1e1f856-8320-412c-a5c1-3f0dec1c560d'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>&lt;momapy.drawing.NoneValueType object at 0x7f8b17323e00&gt;</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>48.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>(4, 2)</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The stroke width of the node</p> <code>1.0</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>88.0</code> <code>offset</code> <code>float</code> <p>Offset distance between stacked units.</p> <code>3.0</code> <code>subunits_stroke</code> <code>tuple[NoneValueType | Color, ...] | None</code> <p>Tuple of stroke colors for each subunit.</p> <code>None</code> <code>subunits_stroke_width</code> <code>tuple[NoneValueType | float, ...] | None</code> <p>Tuple of stroke widths for each subunit.</p> <code>None</code> <code>subunits_stroke_dasharray</code> <code>tuple[NoneValueType | tuple[float, ...], ...] | None</code> <p>Tuple of dash patterns for each subunit.</p> <code>None</code> <code>subunits_stroke_dashoffset</code> <code>tuple[float, ...] | None</code> <p>Tuple of dash offsets for each subunit.</p> <code>None</code> <code>subunits_fill</code> <code>tuple[NoneValueType | Color, ...] | None</code> <p>Tuple of fill colors for each subunit.</p> <code>None</code> <code>subunits_transform</code> <code>tuple[NoneValueType | tuple[Transformation, ...], ...] | None</code> <p>Tuple of transformations for each subunit.</p> <code>None</code> <code>subunits_filter</code> <code>tuple[NoneValueType | Filter, ...] | None</code> <p>Tuple of filters for each subunit.</p> <code>None</code> <code>n</code> <code>int</code> <p>Number of stacked node copies to draw.</p> <code>1</code> <code>rounded_corners</code> <code>float</code> <p>The radius of the rounded corners.</p> <code>5.0</code> <code>right_rectangle_width</code> <code>float</code> <p>The width of the right-hand gate rectangle.</p> <code>20.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>angle</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Return the east anchor of the node.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Get the center position for the node label.</p> <code>north</code> <p>Return the north anchor of the node.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Return the south anchor of the node.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Return the west anchor of the node.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonChannelActiveLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonChannelActiveLayout.angle","title":"angle","text":"<pre><code>angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonChannelActiveLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonChannelActiveLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonChannelActiveLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonChannelActiveLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonChannelActiveLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonChannelActiveLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonChannelActiveLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonChannelActiveLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonChannelActiveLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Return the east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Return the east anchor of the node.\"\"\"\n    return self.own_angle(0)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonChannelActiveLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonChannelActiveLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonChannelActiveLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonChannelActiveLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonChannelActiveLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Get the center position for the node label.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point where the label should be centered.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Get the center position for the node label.\n\n    Returns:\n        Point where the label should be centered.\n    \"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2 - (self._n - 1) * self.offset,\n        self.height / 2 - height / 2 - (self._n - 1) * self.offset,\n    )\n    return self._make_subunit_shape(\n        position=position, width=width, height=height\n    ).position\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonChannelActiveLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Return the north anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Return the north anchor of the node.\"\"\"\n    return self.own_angle(90)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonChannelActiveLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonChannelActiveLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonChannelActiveLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonChannelActiveLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonChannelActiveLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonChannelActiveLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonChannelActiveLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonChannelActiveLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonChannelActiveLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonChannelActiveLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonChannelActiveLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonChannelActiveLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Return the south anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Return the south anchor of the node.\"\"\"\n    return self.own_angle(270)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonChannelActiveLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonChannelActiveLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonChannelActiveLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonChannelActiveLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonChannelActiveLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonChannelActiveLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Return the west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Return the west anchor of the node.\"\"\"\n    return self.own_angle(180)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonChannelActiveLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonChannelActiveLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonChannelActiveLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonChannelActiveLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonChannelLayout","title":"IonChannelLayout  <code>dataclass</code>","text":"<pre><code>IonChannelLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = from_hex('#CCFFCC'), filter_: NoneValueType | Filter | None = None, height: float = 40.0, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = 1.0, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 80.0, offset: float = 3.0, subunits_stroke: tuple[NoneValueType | Color, ...] | None = None, subunits_stroke_width: tuple[NoneValueType | float, ...] | None = None, subunits_stroke_dasharray: tuple[NoneValueType | tuple[float, ...], ...] | None = None, subunits_stroke_dashoffset: tuple[float, ...] | None = None, subunits_fill: tuple[NoneValueType | Color, ...] | None = None, subunits_transform: tuple[NoneValueType | tuple[Transformation, ...], ...] | None = None, subunits_filter: tuple[NoneValueType | Filter, ...] | None = None, n: int = 1, rounded_corners: float = 5.0, right_rectangle_width: float = 20.0)\n</code></pre> <p>               Bases: <code>_MultiNodeMixin</code>, <code>CellDesignerNode</code></p> <p>Ion channel layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'dac322a1-8205-453a-a51f-b19ba80b70bc'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>Color(red=204, green=255, blue=204, alpha=1.0)</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>40.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The stroke width of the node</p> <code>1.0</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>80.0</code> <code>offset</code> <code>float</code> <p>Offset distance between stacked units.</p> <code>3.0</code> <code>subunits_stroke</code> <code>tuple[NoneValueType | Color, ...] | None</code> <p>Tuple of stroke colors for each subunit.</p> <code>None</code> <code>subunits_stroke_width</code> <code>tuple[NoneValueType | float, ...] | None</code> <p>Tuple of stroke widths for each subunit.</p> <code>None</code> <code>subunits_stroke_dasharray</code> <code>tuple[NoneValueType | tuple[float, ...], ...] | None</code> <p>Tuple of dash patterns for each subunit.</p> <code>None</code> <code>subunits_stroke_dashoffset</code> <code>tuple[float, ...] | None</code> <p>Tuple of dash offsets for each subunit.</p> <code>None</code> <code>subunits_fill</code> <code>tuple[NoneValueType | Color, ...] | None</code> <p>Tuple of fill colors for each subunit.</p> <code>None</code> <code>subunits_transform</code> <code>tuple[NoneValueType | tuple[Transformation, ...], ...] | None</code> <p>Tuple of transformations for each subunit.</p> <code>None</code> <code>subunits_filter</code> <code>tuple[NoneValueType | Filter, ...] | None</code> <p>Tuple of filters for each subunit.</p> <code>None</code> <code>n</code> <code>int</code> <p>Number of stacked node copies to draw.</p> <code>1</code> <code>rounded_corners</code> <code>float</code> <p>The radius of the rounded corners.</p> <code>5.0</code> <code>right_rectangle_width</code> <code>float</code> <p>The width of the right-hand gate rectangle.</p> <code>20.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>angle</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Return the east anchor of the node.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Return the center point for the node's label.</p> <code>north</code> <p>Return the north anchor of the node.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Return the south anchor of the node.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Return the west anchor of the node.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonChannelLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonChannelLayout.angle","title":"angle","text":"<pre><code>angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonChannelLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonChannelLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonChannelLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonChannelLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonChannelLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonChannelLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonChannelLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonChannelLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonChannelLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Return the east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Return the east anchor of the node.\"\"\"\n    return self.own_angle(0)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonChannelLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonChannelLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonChannelLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonChannelLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonChannelLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Return the center point for the node's label.</p> Source code in <code>src/momapy/celldesigner/layout.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Return the center point for the node's label.\"\"\"\n    return Point(self.position.x - self.right_rectangle_width / 2, self.position.y)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonChannelLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Return the north anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Return the north anchor of the node.\"\"\"\n    return self.own_angle(90)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonChannelLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonChannelLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonChannelLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonChannelLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonChannelLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonChannelLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonChannelLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonChannelLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonChannelLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonChannelLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonChannelLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonChannelLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Return the south anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Return the south anchor of the node.\"\"\"\n    return self.own_angle(270)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonChannelLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonChannelLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonChannelLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonChannelLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonChannelLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonChannelLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Return the west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Return the west anchor of the node.\"\"\"\n    return self.own_angle(180)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonChannelLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonChannelLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonChannelLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonChannelLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonChannelTemplate","title":"IonChannelTemplate  <code>dataclass</code>","text":"<pre><code>IonChannelTemplate(*, id_: str = make_uuid4_as_str(), name: str, modification_residues: frozenset[ModificationResidue] = frozenset())\n</code></pre> <p>               Bases: <code>ProteinTemplate</code></p> <p>Ion channel template.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'8c7a7a25-ceb7-44b4-9e93-2a9eba345297'</code> <code>name</code> <code>str</code> <p>The name of the species template</p> required <code>modification_residues</code> <code>frozenset[ModificationResidue]</code> <p>The modification residues of the protein template</p> <code>&lt;dynamic&gt;</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonChannelTemplate.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonLayout","title":"IonLayout  <code>dataclass</code>","text":"<pre><code>IonLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = from_hex('#9999FF'), filter_: NoneValueType | Filter | None = None, height: float = 35.0, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = 1.0, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 35.0, offset: float = 3.0, subunits_stroke: tuple[NoneValueType | Color, ...] | None = None, subunits_stroke_width: tuple[NoneValueType | float, ...] | None = None, subunits_stroke_dasharray: tuple[NoneValueType | tuple[float, ...], ...] | None = None, subunits_stroke_dashoffset: tuple[float, ...] | None = None, subunits_fill: tuple[NoneValueType | Color, ...] | None = None, subunits_transform: tuple[NoneValueType | tuple[Transformation, ...], ...] | None = None, subunits_filter: tuple[NoneValueType | Filter, ...] | None = None, n: int = 1)\n</code></pre> <p>               Bases: <code>_MultiNodeMixin</code>, <code>CellDesignerNode</code></p> <p>Ion layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'fa5a2928-6d65-4a16-8690-0f2fec199364'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>Color(red=153, green=153, blue=255, alpha=1.0)</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>35.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The stroke width of the node</p> <code>1.0</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>35.0</code> <code>offset</code> <code>float</code> <p>Offset distance between stacked units.</p> <code>3.0</code> <code>subunits_stroke</code> <code>tuple[NoneValueType | Color, ...] | None</code> <p>Tuple of stroke colors for each subunit.</p> <code>None</code> <code>subunits_stroke_width</code> <code>tuple[NoneValueType | float, ...] | None</code> <p>Tuple of stroke widths for each subunit.</p> <code>None</code> <code>subunits_stroke_dasharray</code> <code>tuple[NoneValueType | tuple[float, ...], ...] | None</code> <p>Tuple of dash patterns for each subunit.</p> <code>None</code> <code>subunits_stroke_dashoffset</code> <code>tuple[float, ...] | None</code> <p>Tuple of dash offsets for each subunit.</p> <code>None</code> <code>subunits_fill</code> <code>tuple[NoneValueType | Color, ...] | None</code> <p>Tuple of fill colors for each subunit.</p> <code>None</code> <code>subunits_transform</code> <code>tuple[NoneValueType | tuple[Transformation, ...], ...] | None</code> <p>Tuple of transformations for each subunit.</p> <code>None</code> <code>subunits_filter</code> <code>tuple[NoneValueType | Filter, ...] | None</code> <p>Tuple of filters for each subunit.</p> <code>None</code> <code>n</code> <code>int</code> <p>Number of stacked node copies to draw.</p> <code>1</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>angle</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Return the east anchor of the node.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Get the center position for the node label.</p> <code>north</code> <p>Return the north anchor of the node.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Return the south anchor of the node.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Return the west anchor of the node.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonLayout.angle","title":"angle","text":"<pre><code>angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Return the east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Return the east anchor of the node.\"\"\"\n    return self.own_angle(0)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Get the center position for the node label.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point where the label should be centered.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Get the center position for the node label.\n\n    Returns:\n        Point where the label should be centered.\n    \"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2 - (self._n - 1) * self.offset,\n        self.height / 2 - height / 2 - (self._n - 1) * self.offset,\n    )\n    return self._make_subunit_shape(\n        position=position, width=width, height=height\n    ).position\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Return the north anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Return the north anchor of the node.\"\"\"\n    return self.own_angle(90)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Return the south anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Return the south anchor of the node.\"\"\"\n    return self.own_angle(270)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Return the west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Return the west anchor of the node.\"\"\"\n    return self.own_angle(180)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.IonLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.KnownOrUnknownModulation","title":"KnownOrUnknownModulation  <code>dataclass</code>","text":"<pre><code>KnownOrUnknownModulation(*, id_: str = make_uuid4_as_str(), source: Species | BooleanLogicGate, target: Species | None)\n</code></pre> <p>               Bases: <code>CellDesignerModelElement</code></p> <p>Abstract base class for known or unknown modulations.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'97f452ce-f7e1-481f-adbb-b0a31db01f16'</code> <code>source</code> <code>Species | BooleanLogicGate</code> <p>The source of the modulation</p> required <code>target</code> <code>Species | None</code> <p>The target of the modulation</p> required <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.KnownOrUnknownModulation.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.KnownOrUnknownModulator","title":"KnownOrUnknownModulator  <code>dataclass</code>","text":"<pre><code>KnownOrUnknownModulator(*, id_: str = make_uuid4_as_str(), name: str | None = None, sbo_term: str | None = None, metaid: str | None = None, referred_element: Species | BooleanLogicGate)\n</code></pre> <p>               Bases: <code>ModifierSpeciesReference</code>, <code>CellDesignerModelElement</code></p> <p>Abstract base class for known or unknown modulators.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'202e5b6d-34ca-4ace-8e4e-f8ca4e7e8a21'</code> <code>name</code> <code>str | None</code> <p>Human-readable name of the element.</p> <code>None</code> <code>sbo_term</code> <code>str | None</code> <p>Optional SBO term identifier for semantic annotation.</p> <code>None</code> <code>metaid</code> <code>str | None</code> <p>Optional metadata identifier for RDF annotations.</p> <code>None</code> <code>referred_element</code> <code>Species | BooleanLogicGate</code> <p>The species or boolean logic gate the modifier refers to</p> required <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.KnownOrUnknownModulator.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.KnownTransitionOmitted","title":"KnownTransitionOmitted  <code>dataclass</code>","text":"<pre><code>KnownTransitionOmitted(*, id_: str = make_uuid4_as_str(), name: str | None = None, sbo_term: str | None = None, metaid: str | None = None, reversible: bool = False, compartment: Compartment | None = None, reactants: frozenset[Reactant] = frozenset(), products: frozenset[Product] = frozenset(), modifiers: frozenset[KnownOrUnknownModulator] = frozenset(), has_external_source: bool = False, has_external_sink: bool = False)\n</code></pre> <p>               Bases: <code>Reaction</code></p> <p>Known transition omitted.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'4360c88f-3bf0-4cfa-af7a-359fd51bad08'</code> <code>name</code> <code>str | None</code> <p>Human-readable name of the element.</p> <code>None</code> <code>sbo_term</code> <code>str | None</code> <p>Optional SBO term identifier for semantic annotation.</p> <code>None</code> <code>metaid</code> <code>str | None</code> <p>Optional metadata identifier for RDF annotations.</p> <code>None</code> <code>reversible</code> <code>bool</code> <p>Whether the reaction can proceed in both directions.</p> <code>False</code> <code>compartment</code> <code>Compartment | None</code> <p>Optional compartment where the reaction occurs.</p> <code>None</code> <code>reactants</code> <code>frozenset[Reactant]</code> <p>The reactants of the reaction</p> <code>&lt;dynamic&gt;</code> <code>products</code> <code>frozenset[Product]</code> <p>The products of the reaction</p> <code>&lt;dynamic&gt;</code> <code>modifiers</code> <code>frozenset[KnownOrUnknownModulator]</code> <p>The modifiers of the reaction</p> <code>&lt;dynamic&gt;</code> <code>has_external_source</code> <code>bool</code> <p>Whether the reaction has an unspecified external source (a Degraded reactant in CellDesigner).</p> <code>False</code> <code>has_external_sink</code> <code>bool</code> <p>Whether the reaction has an unspecified external sink (a Degraded product in CellDesigner).</p> <code>False</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.KnownTransitionOmitted.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.KnownTransitionOmittedLayout","title":"KnownTransitionOmittedLayout  <code>dataclass</code>","text":"<pre><code>KnownTransitionOmittedLayout(*, left_connector_fraction: float = 0.375, right_connector_fraction: float = 0.625, reaction_node_font_family: str = DEFAULT_FONT_FAMILY, reaction_node_font_fill: Color | NoneValueType = black, reaction_node_font_stroke: Color | NoneValueType = NoneValue, reaction_node_font_style: FontStyle = NORMAL, reaction_node_font_weight: FontWeight | float = BOLD, reaction_node_text: str | None = '//', reaction_node_height: float = 10.0, reaction_node_width: float = 10.0, reaction_node_segment: int = 1, reaction_node_stroke: NoneValueType | Color | None = black, reaction_node_stroke_width: float | None = 1.0, reaction_node_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, reaction_node_stroke_dashoffset: float | None = None, reaction_node_fill: NoneValueType | Color | None = white, reaction_node_transform: NoneValueType | tuple[Transformation, ...] | None = None, reaction_node_filter: NoneValueType | Filter | None = None, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, end_shorten: float = 2.0, fill: NoneValueType | Color | None = None, filter_: NoneValueType | Filter | None = None, path_fill: NoneValueType | Color | None = NoneValue, path_filter: NoneValueType | Filter | None = None, path_stroke: NoneValueType | Color | None = black, path_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, path_stroke_dashoffset: NoneValueType | float | None = None, path_stroke_width: float | None = 1.0, path_transform: NoneValueType | tuple[Transformation, ...] | None = None, stroke: NoneValueType | Color | None = None, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: NoneValueType | float | None = None, segments: tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...] = tuple(), source: LayoutElement | None = None, start_shorten: float = 2.0, target: LayoutElement | None = None, transform: NoneValueType | tuple[Transformation, ...] | None = None, end_arrowhead_fill: NoneValueType | Color | None = black, end_arrowhead_filter: NoneValueType | Filter | None = None, end_arrowhead_stroke: NoneValueType | Color | None = black, end_arrowhead_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, end_arrowhead_stroke_dashoffset: float | None = None, end_arrowhead_stroke_width: float | None = 1.0, end_arrowhead_transform: NoneValueType | tuple[Transformation, ...] | None = None, start_arrowhead_fill: NoneValueType | Color | None = black, start_arrowhead_filter: NoneValueType | Filter | None = None, start_arrowhead_stroke: NoneValueType | Color | None = black, start_arrowhead_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, start_arrowhead_stroke_dashoffset: float | None = None, start_arrowhead_stroke_width: float | None = 1.0, start_arrowhead_transform: NoneValueType | tuple[Transformation, ...] | None = None, reversible: bool = False, end_arrowhead_height: float = 8.0, end_arrowhead_width: float = 15.0, start_arrowhead_height: float = 8.0, start_arrowhead_width: float = 15.0)\n</code></pre> <p>               Bases: <code>ReactionLayout</code>, <code>_ReactionNodeMixin</code></p> <p>Known transition omitted layout.</p> <p>Parameters:</p> Name Type Description Default <code>left_connector_fraction</code> <code>float</code> <p>The fraction along the segment where the left connector attaches.</p> <code>0.375</code> <code>right_connector_fraction</code> <code>float</code> <p>The fraction along the segment where the right connector attaches.</p> <code>0.625</code> <code>reaction_node_font_family</code> <code>str</code> <p>The font family of the reaction node label.</p> <code>'DejaVu Sans'</code> <code>reaction_node_font_fill</code> <code>Color | NoneValueType</code> <p>The fill color of the reaction node label.</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>reaction_node_font_stroke</code> <code>Color | NoneValueType</code> <p>The stroke color of the reaction node label.</p> <code>&lt;momapy.drawing.NoneValueType object at 0x7f8b17323e00&gt;</code> <code>reaction_node_font_style</code> <code>FontStyle</code> <p>The font style of the reaction node label.</p> <code>&lt;FontStyle.NORMAL: 0&gt;</code> <code>reaction_node_font_weight</code> <code>FontWeight | float</code> <p>The font weight of the reaction node label.</p> <code>&lt;FontWeight.BOLD: 1&gt;</code> <code>reaction_node_text</code> <code>str | None</code> <p>The text of the reaction node label.</p> <code>'//'</code> <code>reaction_node_height</code> <code>float</code> <p>The height of the reaction node.</p> <code>10.0</code> <code>reaction_node_width</code> <code>float</code> <p>The width of the reaction node.</p> <code>10.0</code> <code>reaction_node_segment</code> <code>int</code> <p>The index of the arc segment carrying the reaction node.</p> <code>1</code> <code>reaction_node_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the reaction node.</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>reaction_node_stroke_width</code> <code>float | None</code> <p>The stroke width of the reaction node.</p> <code>1.0</code> <code>reaction_node_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The dash pattern of the reaction node's border.</p> <code>None</code> <code>reaction_node_stroke_dashoffset</code> <code>float | None</code> <p>The dash offset of the reaction node's border.</p> <code>None</code> <code>reaction_node_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the reaction node.</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>reaction_node_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform applied to the reaction node.</p> <code>None</code> <code>reaction_node_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter applied to the reaction node.</p> <code>None</code> <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'460017e7-bc2d-42c7-8b47-75b2545398a5'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>end_shorten</code> <code>float</code> <p>The length the end of the arc will be shorten by</p> <code>2.0</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the arc</p> <code>None</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the arc</p> <code>None</code> <code>path_fill</code> <code>NoneValueType | Color | None</code> <p>The path fill color of the arc</p> <code>&lt;momapy.drawing.NoneValueType object at 0x7f8b17323e00&gt;</code> <code>path_filter</code> <code>NoneValueType | Filter | None</code> <p>The path filter of the arc</p> <code>None</code> <code>path_stroke</code> <code>NoneValueType | Color | None</code> <p>The path stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>path_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The path stroke dasharray of the arc</p> <code>None</code> <code>path_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The path stroke dashoffset of the arc</p> <code>None</code> <code>path_stroke_width</code> <code>float | None</code> <p>The path stroke width of the arc</p> <code>1.0</code> <code>path_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The path transform of the arc</p> <code>None</code> <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the arc</p> <code>None</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the arc</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the arc</p> <code>None</code> <code>stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the arc</p> <code>None</code> <code>segments</code> <code>tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...]</code> <p>The path segments of the arc</p> <code>&lt;dynamic&gt;</code> <code>source</code> <code>LayoutElement | None</code> <p>The source of the arc</p> <code>None</code> <code>start_shorten</code> <code>float</code> <p>The length the start of the arc will be shorten by</p> <code>2.0</code> <code>target</code> <code>LayoutElement | None</code> <p>The target of the arc</p> <code>None</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the arc</p> <code>None</code> <code>end_arrowhead_fill</code> <code>NoneValueType | Color | None</code> <p>The end arrowhead fill color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>end_arrowhead_filter</code> <code>NoneValueType | Filter | None</code> <p>The end arrowhead filter of the arc</p> <code>None</code> <code>end_arrowhead_stroke</code> <code>NoneValueType | Color | None</code> <p>The end arrowhead stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>end_arrowhead_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The end arrowhead stroke dasharray of the arc</p> <code>None</code> <code>end_arrowhead_stroke_dashoffset</code> <code>float | None</code> <p>The end arrowhead stroke dashoffset of the arc</p> <code>None</code> <code>end_arrowhead_stroke_width</code> <code>float | None</code> <p>The end arrowhead stroke width of the arc</p> <code>1.0</code> <code>end_arrowhead_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The end arrowhead transform of the arc</p> <code>None</code> <code>start_arrowhead_fill</code> <code>NoneValueType | Color | None</code> <p>The start arrowhead fill color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>start_arrowhead_filter</code> <code>NoneValueType | Filter | None</code> <p>The start arrowhead filter of the arc</p> <code>None</code> <code>start_arrowhead_stroke</code> <code>NoneValueType | Color | None</code> <p>The start arrowhead stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>start_arrowhead_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The start arrowhead stroke dasharray of the arc</p> <code>None</code> <code>start_arrowhead_stroke_dashoffset</code> <code>float | None</code> <p>The start arrowhead stroke dashoffset of the arc</p> <code>None</code> <code>start_arrowhead_stroke_width</code> <code>float | None</code> <p>The start arrowhead stroke width of the arc</p> <code>1.0</code> <code>start_arrowhead_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The start arrowhead transform of the arc</p> <code>None</code> <code>reversible</code> <code>bool</code> <p>Whether the reaction is reversible.</p> <code>False</code> <code>end_arrowhead_height</code> <code>float</code> <p>The height of the end arrowhead.</p> <code>8.0</code> <code>end_arrowhead_width</code> <code>float</code> <p>The width of the end arrowhead.</p> <code>15.0</code> <code>start_arrowhead_height</code> <code>float</code> <p>The height of the start arrowhead.</p> <code>8.0</code> <code>start_arrowhead_width</code> <code>float</code> <p>The width of the start arrowhead.</p> <code>15.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>childless</code> <p>Return a copy of the arc with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>end_arrowhead_base</code> <p>Return the base anchor point of the double-headed arc end arrowhead.</p> <code>end_arrowhead_bbox</code> <p>Return the bounding box of the double-headed arc end arrowhead.</p> <code>end_arrowhead_border</code> <p>Return the point at the intersection of the drawing elements of the double-headed arc end arrowhead and the line going through the center of these drawing elements and the given point.</p> <code>end_arrowhead_drawing_elements</code> <p>Return the drawing elements of the double-headed arc end arrowhead.</p> <code>end_arrowhead_length</code> <p>Return the length of the double-headed arc end arrowhead.</p> <code>end_arrowhead_tip</code> <p>Return the tip anchor point of the double-headed arc end arrowhead.</p> <code>end_point</code> <p>Return the ending point of the arc.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>fraction</code> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <code>length</code> <p>Return the total length of the arc path.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_children</code> <p>Return the self children of the arc.</p> <code>own_drawing_elements</code> <p>Return the arc's own drawing elements, including those of its mixins.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>path_drawing_elements</code> <p>Return the drawing elements of the double-headed arc path.</p> <code>points</code> <p>Return the points of the arc path.</p> <code>start_arrowhead_base</code> <p>Return the base anchor point of the double-headed arc start arrowhead.</p> <code>start_arrowhead_bbox</code> <p>Return the bounding box of the double-headed arc start arrowhead.</p> <code>start_arrowhead_border</code> <p>Return the point at the intersection of the drawing elements of the double-headed arc start arrowhead and the line going through the center of these drawing elements and the given point.</p> <code>start_arrowhead_drawing_elements</code> <p>Return the drawing elements of the double-headed arc start arrowhead.</p> <code>start_arrowhead_length</code> <p>Return the length of the double-headed arc start arrowhead.</p> <code>start_arrowhead_tip</code> <p>Return the tip anchor point of the double-headed arc start arrowhead.</p> <code>start_point</code> <p>Return the starting point of the arc.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.KnownTransitionOmittedLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.KnownTransitionOmittedLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.KnownTransitionOmittedLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the arc with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the arc with no children.\"\"\"\n    return dataclasses.replace(self, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.KnownTransitionOmittedLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.KnownTransitionOmittedLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.KnownTransitionOmittedLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.KnownTransitionOmittedLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.KnownTransitionOmittedLayout.end_arrowhead_base","title":"end_arrowhead_base","text":"<pre><code>end_arrowhead_base() -&gt; Point\n</code></pre> <p>Return the base anchor point of the double-headed arc end arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_arrowhead_base(self) -&gt; Point:\n    \"\"\"Return the base anchor point of the double-headed arc end arrowhead.\"\"\"\n    arrowhead_length = self.end_arrowhead_length()\n    if arrowhead_length == 0:\n        return self.end_point()\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return self.end_arrowhead_tip() - (arrowhead_length, 0)\n    fraction = 1 - (arrowhead_length + self.end_shorten) / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.KnownTransitionOmittedLayout.end_arrowhead_bbox","title":"end_arrowhead_bbox","text":"<pre><code>end_arrowhead_bbox() -&gt; Bbox\n</code></pre> <p>Return the bounding box of the double-headed arc end arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_arrowhead_bbox(self) -&gt; Bbox:\n    \"\"\"Return the bounding box of the double-headed arc end arrowhead.\"\"\"\n    return get_drawing_elements_bbox(self.end_arrowhead_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.KnownTransitionOmittedLayout.end_arrowhead_border","title":"end_arrowhead_border","text":"<pre><code>end_arrowhead_border(point: Point) -&gt; Point\n</code></pre> <p>Return the point at the intersection of the drawing elements of the double-headed arc end arrowhead and the line going through the center of these drawing elements and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_arrowhead_border(self, point: Point) -&gt; Point:\n    \"\"\"Return the point at the intersection of the drawing elements of the double-headed arc end arrowhead and the line going through the center of these drawing elements and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    point = get_drawing_elements_border(\n        self.end_arrowhead_drawing_elements(), point\n    )\n    if point is None:\n        return self.end_arrowhead_tip()\n    return point\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.KnownTransitionOmittedLayout.end_arrowhead_drawing_elements","title":"end_arrowhead_drawing_elements","text":"<pre><code>end_arrowhead_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the double-headed arc end arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_arrowhead_drawing_elements(\n    self,\n) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the double-headed arc end arrowhead.\"\"\"\n    drawing_elements = self._end_arrowhead_border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_end_arrowhead\",\n        elements=tuple(drawing_elements),\n        fill=self.end_arrowhead_fill,\n        filter_=self.end_arrowhead_filter,\n        id_=f\"{self.id_}_end_arrowhead\",\n        stroke=self.end_arrowhead_stroke,\n        stroke_width=self.end_arrowhead_stroke_width,\n        stroke_dasharray=self.end_arrowhead_stroke_dasharray,\n        stroke_dashoffset=self.end_arrowhead_stroke_dashoffset,\n        transform=self.end_arrowhead_transform,\n    )\n    transformation = self._get_end_arrowhead_transformation()\n    group = group.transformed(transformation)\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.KnownTransitionOmittedLayout.end_arrowhead_length","title":"end_arrowhead_length","text":"<pre><code>end_arrowhead_length() -&gt; float\n</code></pre> <p>Return the length of the double-headed arc end arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_arrowhead_length(self) -&gt; float:\n    \"\"\"Return the length of the double-headed arc end arrowhead.\"\"\"\n    bbox = get_drawing_elements_bbox(self._end_arrowhead_border_drawing_elements())\n    if math.isnan(bbox.width):\n        return 0.0\n    return bbox.east().x\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.KnownTransitionOmittedLayout.end_arrowhead_tip","title":"end_arrowhead_tip","text":"<pre><code>end_arrowhead_tip() -&gt; Point\n</code></pre> <p>Return the tip anchor point of the double-headed arc end arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_arrowhead_tip(self) -&gt; Point:\n    \"\"\"Return the tip anchor point of the double-headed arc end arrowhead.\"\"\"\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return segment.p2\n    fraction = 1 - self.end_shorten / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.KnownTransitionOmittedLayout.end_point","title":"end_point","text":"<pre><code>end_point() -&gt; Point\n</code></pre> <p>Return the ending point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_point(self) -&gt; Point:\n    \"\"\"Return the ending point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[-1]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.KnownTransitionOmittedLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.KnownTransitionOmittedLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.KnownTransitionOmittedLayout.fraction","title":"fraction","text":"<pre><code>fraction(fraction: float) -&gt; tuple[Point, float]\n</code></pre> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def fraction(self, fraction: float) -&gt; tuple[Point, float]:\n    \"\"\"Return the position and angle on the arc at a given fraction (of the total arc length).\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    current_length = 0\n    length_to_reach = fraction * self.length()\n    for segment in self.segments:\n        current_length += segment.length()\n        if current_length &gt;= length_to_reach:\n            break\n    segment_start_length = current_length - segment.length()\n    segment_fraction = (length_to_reach - segment_start_length) / segment.length()\n    position, angle = segment.get_position_and_angle_at_fraction(segment_fraction)\n    return position, angle\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.KnownTransitionOmittedLayout.length","title":"length","text":"<pre><code>length() -&gt; float\n</code></pre> <p>Return the total length of the arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def length(self) -&gt; float:\n    \"\"\"Return the total length of the arc path.\"\"\"\n    return sum([segment.length() for segment in self.segments])\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.KnownTransitionOmittedLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.KnownTransitionOmittedLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the arc.\"\"\"\n    return []\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.KnownTransitionOmittedLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the arc's own drawing elements, including those of its mixins.</p> Source code in <code>src/momapy/celldesigner/elements.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the arc's own drawing elements, including those of its mixins.\"\"\"\n    drawing_elements = super_or_builder(\n        CellDesignerDoubleHeadedArc, self\n    ).own_drawing_elements()\n    done_bases = []\n    for base in type(self).__mro__:\n        if (\n            issubclass_or_builder(base, _SBGNMixin)\n            and base is not type(self)\n            and not any([issubclass(done_base, base) for done_base in done_bases])\n        ):\n            drawing_elements += getattr(base, \"_mixin_drawing_elements\")(self)\n            done_bases.append(base)\n    return drawing_elements\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.KnownTransitionOmittedLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.KnownTransitionOmittedLayout.path_drawing_elements","title":"path_drawing_elements","text":"<pre><code>path_drawing_elements() -&gt; list[Path]\n</code></pre> <p>Return the drawing elements of the double-headed arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def path_drawing_elements(self) -&gt; list[Path]:\n    \"\"\"Return the drawing elements of the double-headed arc path.\"\"\"\n    start_arrowhead_length = self.start_arrowhead_length()\n    end_arrowhead_length = self.end_arrowhead_length()\n    if len(self.segments) == 1:\n        segment = (\n            self.segments[0]\n            .shortened(self.start_shorten + start_arrowhead_length, \"start\")\n            .shortened(self.end_shorten + end_arrowhead_length, \"end\")\n        )\n        actions = [\n            MoveTo(segment.p1),\n            self._make_path_action_from_segment(segment),\n        ]\n    else:\n        first_segment = self.segments[0].shortened(\n            self.start_shorten + start_arrowhead_length, \"start\"\n        )\n        last_segment = self.segments[-1].shortened(\n            self.end_shorten + end_arrowhead_length, \"end\"\n        )\n        actions = [\n            MoveTo(first_segment.p1),\n            self._make_path_action_from_segment(first_segment),\n        ]\n        for segment in self.segments[1:-1]:\n            action = self._make_path_action_from_segment(segment)\n            actions.append(action)\n        actions.append(self._make_path_action_from_segment(last_segment))\n    path = Path(\n        actions=tuple(actions),\n        class_=f\"{type(self).__name__}_path\",\n        fill=self.path_fill,\n        filter_=self.path_filter,\n        id_=f\"{self.id_}_path\",\n        stroke=self.path_stroke,\n        stroke_dasharray=self.path_stroke_dasharray,\n        stroke_dashoffset=self.path_stroke_dashoffset,\n        stroke_width=self.path_stroke_width,\n        transform=self.path_transform,\n    )\n    return [path]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.KnownTransitionOmittedLayout.points","title":"points","text":"<pre><code>points() -&gt; list[Point]\n</code></pre> <p>Return the points of the arc path.</p> <p>An arc with no segments has no points and returns an empty list.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def points(self) -&gt; list[Point]:\n    \"\"\"Return the points of the arc path.\n\n    An arc with no segments has no points and returns an empty list.\n    \"\"\"\n    if not self.segments:\n        return []\n    points = []\n    for segment in self.segments:\n        points.append(segment.p1)\n    points.append(segment.p2)\n    return points\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.KnownTransitionOmittedLayout.start_arrowhead_base","title":"start_arrowhead_base","text":"<pre><code>start_arrowhead_base() -&gt; Point\n</code></pre> <p>Return the base anchor point of the double-headed arc start arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_arrowhead_base(self) -&gt; Point:\n    \"\"\"Return the base anchor point of the double-headed arc start arrowhead.\"\"\"\n    arrowhead_length = self.start_arrowhead_length()\n    if arrowhead_length == 0:\n        return self.start_point()\n    segment = self.segments[0]\n    segment = Segment(segment.p2, segment.p1)\n    segment_length = segment.length()\n    if segment_length == 0:\n        return self.start_arrowhead_tip() + (arrowhead_length, 0)\n    fraction = 1 - (arrowhead_length + self.start_shorten) / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.KnownTransitionOmittedLayout.start_arrowhead_bbox","title":"start_arrowhead_bbox","text":"<pre><code>start_arrowhead_bbox() -&gt; Bbox\n</code></pre> <p>Return the bounding box of the double-headed arc start arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_arrowhead_bbox(self) -&gt; Bbox:\n    \"\"\"Return the bounding box of the double-headed arc start arrowhead.\"\"\"\n    return get_drawing_elements_bbox(self.start_arrowhead_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.KnownTransitionOmittedLayout.start_arrowhead_border","title":"start_arrowhead_border","text":"<pre><code>start_arrowhead_border(point: Point) -&gt; Point\n</code></pre> <p>Return the point at the intersection of the drawing elements of the double-headed arc start arrowhead and the line going through the center of these drawing elements and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_arrowhead_border(self, point: Point) -&gt; Point:\n    \"\"\"Return the point at the intersection of the drawing elements of the double-headed arc start arrowhead and the line going through the center of these drawing elements and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    point = get_drawing_elements_border(\n        self.start_arrowhead_drawing_elements(), point\n    )\n    if point is None:\n        return self.start_arrowhead_tip()\n    return point\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.KnownTransitionOmittedLayout.start_arrowhead_drawing_elements","title":"start_arrowhead_drawing_elements","text":"<pre><code>start_arrowhead_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the double-headed arc start arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_arrowhead_drawing_elements(\n    self,\n) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the double-headed arc start arrowhead.\"\"\"\n    drawing_elements = self._start_arrowhead_border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_start_arrowhead\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}_start_arrowhead\",\n        fill=self.start_arrowhead_fill,\n        filter_=self.start_arrowhead_filter,\n        stroke=self.start_arrowhead_stroke,\n        stroke_dasharray=self.start_arrowhead_stroke_dasharray,\n        stroke_dashoffset=self.start_arrowhead_stroke_dashoffset,\n        stroke_width=self.start_arrowhead_stroke_width,\n        transform=self.start_arrowhead_transform,\n    )\n    transformation = self._get_start_arrowhead_transformation()\n    group = group.transformed(transformation)\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.KnownTransitionOmittedLayout.start_arrowhead_length","title":"start_arrowhead_length","text":"<pre><code>start_arrowhead_length() -&gt; float\n</code></pre> <p>Return the length of the double-headed arc start arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_arrowhead_length(self) -&gt; float:\n    \"\"\"Return the length of the double-headed arc start arrowhead.\"\"\"\n    bbox = get_drawing_elements_bbox(\n        self._start_arrowhead_border_drawing_elements()\n    )\n    if math.isnan(bbox.width):\n        return 0.0\n    return abs(bbox.west().x)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.KnownTransitionOmittedLayout.start_arrowhead_tip","title":"start_arrowhead_tip","text":"<pre><code>start_arrowhead_tip() -&gt; Point\n</code></pre> <p>Return the tip anchor point of the double-headed arc start arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_arrowhead_tip(self) -&gt; Point:\n    \"\"\"Return the tip anchor point of the double-headed arc start arrowhead.\"\"\"\n    segment = self.segments[0]\n    segment = Segment(segment.p2, segment.p1)\n    segment_length = segment.length()\n    if segment_length == 0:\n        return segment.p2\n    fraction = 1 - self.start_shorten / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.KnownTransitionOmittedLayout.start_point","title":"start_point","text":"<pre><code>start_point() -&gt; Point\n</code></pre> <p>Return the starting point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_point(self) -&gt; Point:\n    \"\"\"Return the starting point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[0]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.KnownTransitionOmittedLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.LineCompartmentLayout","title":"LineCompartmentLayout  <code>dataclass</code>","text":"<pre><code>LineCompartmentLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = white, filter_: NoneValueType | Filter | None = None, height: float = 16.0, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = 1.25, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 16.0, side: CompartmentSide = NORTH, inner_stroke: NoneValueType | Color | None = black, inner_stroke_width: float | None = 1.0, sep: float = 12.0)\n</code></pre> <p>               Bases: <code>_SimpleNodeMixin</code>, <code>CellDesignerNode</code></p> <p>Line compartment layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'80fa9bab-860f-4e7d-a3c8-e71c3bd3131a'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>16.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The stroke width of the node</p> <code>1.25</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>16.0</code> <code>side</code> <code>CompartmentSide</code> <p>The side bounded by the double line.</p> <code>&lt;CompartmentSide.NORTH: 'NORTH'&gt;</code> <code>inner_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the inner line.</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>inner_stroke_width</code> <code>float | None</code> <p>The stroke width of the inner line.</p> <code>1.0</code> <code>sep</code> <code>float</code> <p>The separation between the outer and inner lines.</p> <code>12.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>angle</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Return the east anchor of the node.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Return the label center anchor of the node.</p> <code>north</code> <p>Return the north anchor of the node.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Return the south anchor of the node.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Return the west anchor of the node.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.LineCompartmentLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.LineCompartmentLayout.angle","title":"angle","text":"<pre><code>angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.LineCompartmentLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.LineCompartmentLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.LineCompartmentLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.LineCompartmentLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.LineCompartmentLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.LineCompartmentLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.LineCompartmentLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.LineCompartmentLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.LineCompartmentLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Return the east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Return the east anchor of the node.\"\"\"\n    return self.own_angle(0)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.LineCompartmentLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.LineCompartmentLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.LineCompartmentLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.LineCompartmentLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.LineCompartmentLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Return the label center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Return the label center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.LineCompartmentLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Return the north anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Return the north anchor of the node.\"\"\"\n    return self.own_angle(90)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.LineCompartmentLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.LineCompartmentLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.LineCompartmentLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.LineCompartmentLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.LineCompartmentLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.LineCompartmentLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.LineCompartmentLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.LineCompartmentLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.LineCompartmentLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.LineCompartmentLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.LineCompartmentLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.LineCompartmentLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Return the south anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Return the south anchor of the node.\"\"\"\n    return self.own_angle(270)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.LineCompartmentLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.LineCompartmentLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.LineCompartmentLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.LineCompartmentLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.LineCompartmentLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.LineCompartmentLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Return the west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Return the west anchor of the node.\"\"\"\n    return self.own_angle(180)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.LineCompartmentLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.LineCompartmentLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.LineCompartmentLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.LineCompartmentLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.LogicArcLayout","title":"LogicArcLayout  <code>dataclass</code>","text":"<pre><code>LogicArcLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, end_shorten: float = 0.0, fill: NoneValueType | Color | None = None, filter_: NoneValueType | Filter | None = None, path_fill: NoneValueType | Color | None = NoneValue, path_filter: NoneValueType | Filter | None = None, path_stroke: NoneValueType | Color | None = black, path_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, path_stroke_dashoffset: NoneValueType | float | None = None, path_stroke_width: float | None = 1.0, path_transform: NoneValueType | tuple[Transformation, ...] | None = None, stroke: NoneValueType | Color | None = None, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: NoneValueType | float | None = None, segments: tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...] = tuple(), source: LayoutElement | None = None, start_shorten: float = 0.0, target: LayoutElement | None = None, transform: NoneValueType | tuple[Transformation, ...] | None = None, arrowhead_fill: NoneValueType | Color | None = None, arrowhead_filter: NoneValueType | Filter | None = None, arrowhead_stroke: NoneValueType | Color | None = black, arrowhead_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, arrowhead_stroke_dashoffset: NoneValueType | float | None = None, arrowhead_stroke_width: float | None = 1.0, arrowhead_transform: NoneValueType | tuple[Transformation, ...] | None = None)\n</code></pre> <p>               Bases: <code>CellDesignerSingleHeadedArc</code></p> <p>Logic arc layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'1899c3ce-4e98-44b8-a9e7-bb72b972ef89'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>end_shorten</code> <code>float</code> <p>The length the end of the arc will be shorten by</p> <code>0.0</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the arc</p> <code>None</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the arc</p> <code>None</code> <code>path_fill</code> <code>NoneValueType | Color | None</code> <p>The path fill color of the arc</p> <code>&lt;momapy.drawing.NoneValueType object at 0x7f8b17323e00&gt;</code> <code>path_filter</code> <code>NoneValueType | Filter | None</code> <p>The path filter of the arc</p> <code>None</code> <code>path_stroke</code> <code>NoneValueType | Color | None</code> <p>The path stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>path_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The path stroke dasharray of the arc</p> <code>None</code> <code>path_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The path stroke dashoffset of the arc</p> <code>None</code> <code>path_stroke_width</code> <code>float | None</code> <p>The path stroke width of the arc</p> <code>1.0</code> <code>path_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The path transform of the arc</p> <code>None</code> <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the arc</p> <code>None</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the arc</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the arc</p> <code>None</code> <code>stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the arc</p> <code>None</code> <code>segments</code> <code>tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...]</code> <p>The path segments of the arc</p> <code>&lt;dynamic&gt;</code> <code>source</code> <code>LayoutElement | None</code> <p>The source of the arc</p> <code>None</code> <code>start_shorten</code> <code>float</code> <p>The length the start of the arc will be shorten by</p> <code>0.0</code> <code>target</code> <code>LayoutElement | None</code> <p>The target of the arc</p> <code>None</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the arc</p> <code>None</code> <code>arrowhead_fill</code> <code>NoneValueType | Color | None</code> <p>The arrowhead fill color of the arc</p> <code>None</code> <code>arrowhead_filter</code> <code>NoneValueType | Filter | None</code> <p>The arrowhead filter of the arc</p> <code>None</code> <code>arrowhead_stroke</code> <code>NoneValueType | Color | None</code> <p>The arrowhead stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>arrowhead_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The arrowhead stroke dasharray of the arc</p> <code>None</code> <code>arrowhead_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The arrowhead stroke dashoffset of the arc</p> <code>None</code> <code>arrowhead_stroke_width</code> <code>float | None</code> <p>The arrowhead stroke width of the arc</p> <code>1.0</code> <code>arrowhead_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The arrowhead transform of the arc</p> <code>None</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>arrowhead_base</code> <p>Return the arrowhead base anchor point of the single-headed arc.</p> <code>arrowhead_bbox</code> <p>Return the bounding box of the single-headed arc arrowhead.</p> <code>arrowhead_border</code> <p>Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.</p> <code>arrowhead_drawing_elements</code> <p>Return the drawing elements of the single-headed arc arrowhead.</p> <code>arrowhead_length</code> <p>Return the length of the single-headed arc arrowhead.</p> <code>arrowhead_tip</code> <p>Return the arrowhead tip anchor point of the single-headed arc.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>childless</code> <p>Return a copy of the arc with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>end_point</code> <p>Return the ending point of the arc.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>fraction</code> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <code>length</code> <p>Return the total length of the arc path.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_children</code> <p>Return the self children of the arc.</p> <code>own_drawing_elements</code> <p>Return the arc's own drawing elements, including those of its mixins.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>path_drawing_elements</code> <p>Return the drawing elements of the single-headed arc path.</p> <code>points</code> <p>Return the points of the arc path.</p> <code>start_point</code> <p>Return the starting point of the arc.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.LogicArcLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.LogicArcLayout.arrowhead_base","title":"arrowhead_base","text":"<pre><code>arrowhead_base() -&gt; Point\n</code></pre> <p>Return the arrowhead base anchor point of the single-headed arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_base(self) -&gt; Point:\n    \"\"\"Return the arrowhead base anchor point of the single-headed arc.\"\"\"\n    arrowhead_length = self.arrowhead_length()\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return self.arrowhead_tip() - (arrowhead_length, 0)\n    fraction = 1 - (arrowhead_length + self.end_shorten) / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.LogicArcLayout.arrowhead_bbox","title":"arrowhead_bbox","text":"<pre><code>arrowhead_bbox() -&gt; Bbox\n</code></pre> <p>Return the bounding box of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_bbox(self) -&gt; Bbox:\n    \"\"\"Return the bounding box of the single-headed arc arrowhead.\"\"\"\n    return get_drawing_elements_bbox(self.arrowhead_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.LogicArcLayout.arrowhead_border","title":"arrowhead_border","text":"<pre><code>arrowhead_border(point: Point) -&gt; Point\n</code></pre> <p>Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_border(self, point: Point) -&gt; Point:\n    \"\"\"Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    point = get_drawing_elements_border(self.arrowhead_drawing_elements(), point)\n    if point is None:\n        return self.arrowhead_tip()\n    return point\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.LogicArcLayout.arrowhead_drawing_elements","title":"arrowhead_drawing_elements","text":"<pre><code>arrowhead_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_drawing_elements(\n    self,\n) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the single-headed arc arrowhead.\"\"\"\n    drawing_elements = self._arrowhead_border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_arrowhead\",\n        elements=tuple(drawing_elements),\n        fill=self.arrowhead_fill,\n        filter_=self.arrowhead_filter,\n        id_=f\"{self.id_}_arrowhead\",\n        stroke=self.arrowhead_stroke,\n        stroke_dasharray=self.arrowhead_stroke_dasharray,\n        stroke_dashoffset=self.arrowhead_stroke_dashoffset,\n        stroke_width=self.arrowhead_stroke_width,\n        transform=self.arrowhead_transform,\n    )\n    transformation = self._get_arrowhead_transformation()\n    group = group.transformed(transformation)\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.LogicArcLayout.arrowhead_length","title":"arrowhead_length","text":"<pre><code>arrowhead_length() -&gt; float\n</code></pre> <p>Return the length of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_length(self) -&gt; float:\n    \"\"\"Return the length of the single-headed arc arrowhead.\"\"\"\n    bbox = get_drawing_elements_bbox(self._arrowhead_border_drawing_elements())\n    if math.isnan(bbox.width):\n        return 0.0\n    return bbox.east().x\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.LogicArcLayout.arrowhead_tip","title":"arrowhead_tip","text":"<pre><code>arrowhead_tip() -&gt; Point\n</code></pre> <p>Return the arrowhead tip anchor point of the single-headed arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_tip(self) -&gt; Point:\n    \"\"\"Return the arrowhead tip anchor point of the single-headed arc.\"\"\"\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return segment.p2\n    fraction = 1 - self.end_shorten / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.LogicArcLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.LogicArcLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the arc with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the arc with no children.\"\"\"\n    return dataclasses.replace(self, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.LogicArcLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.LogicArcLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.LogicArcLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.LogicArcLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.LogicArcLayout.end_point","title":"end_point","text":"<pre><code>end_point() -&gt; Point\n</code></pre> <p>Return the ending point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_point(self) -&gt; Point:\n    \"\"\"Return the ending point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[-1]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.LogicArcLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.LogicArcLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.LogicArcLayout.fraction","title":"fraction","text":"<pre><code>fraction(fraction: float) -&gt; tuple[Point, float]\n</code></pre> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def fraction(self, fraction: float) -&gt; tuple[Point, float]:\n    \"\"\"Return the position and angle on the arc at a given fraction (of the total arc length).\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    current_length = 0\n    length_to_reach = fraction * self.length()\n    for segment in self.segments:\n        current_length += segment.length()\n        if current_length &gt;= length_to_reach:\n            break\n    segment_start_length = current_length - segment.length()\n    segment_fraction = (length_to_reach - segment_start_length) / segment.length()\n    position, angle = segment.get_position_and_angle_at_fraction(segment_fraction)\n    return position, angle\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.LogicArcLayout.length","title":"length","text":"<pre><code>length() -&gt; float\n</code></pre> <p>Return the total length of the arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def length(self) -&gt; float:\n    \"\"\"Return the total length of the arc path.\"\"\"\n    return sum([segment.length() for segment in self.segments])\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.LogicArcLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.LogicArcLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the arc.\"\"\"\n    return []\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.LogicArcLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the arc's own drawing elements, including those of its mixins.</p> Source code in <code>src/momapy/celldesigner/elements.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the arc's own drawing elements, including those of its mixins.\"\"\"\n    drawing_elements = super_or_builder(\n        CellDesignerSingleHeadedArc, self\n    ).own_drawing_elements()\n    done_bases = []\n    for base in type(self).__mro__:\n        if (\n            issubclass_or_builder(base, _SBGNMixin)\n            and base is not type(self)\n            and not any([issubclass(done_base, base) for done_base in done_bases])\n        ):\n            drawing_elements += getattr(base, \"_mixin_drawing_elements\")(self)\n            done_bases.append(base)\n    return drawing_elements\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.LogicArcLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.LogicArcLayout.path_drawing_elements","title":"path_drawing_elements","text":"<pre><code>path_drawing_elements() -&gt; list[Path]\n</code></pre> <p>Return the drawing elements of the single-headed arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def path_drawing_elements(self) -&gt; list[Path]:\n    \"\"\"Return the drawing elements of the single-headed arc path.\"\"\"\n    arrowhead_length = self.arrowhead_length()\n    if len(self.segments) == 1:\n        segment = (\n            self.segments[0]\n            .shortened(self.start_shorten, \"start\")\n            .shortened(self.end_shorten + arrowhead_length, \"end\")\n        )\n        actions = [\n            MoveTo(segment.p1),\n            self._make_path_action_from_segment(segment),\n        ]\n    else:\n        first_segment = self.segments[0].shortened(self.start_shorten, \"start\")\n        last_segment = self.segments[-1].shortened(\n            self.end_shorten + arrowhead_length, \"end\"\n        )\n        actions = [\n            MoveTo(first_segment.p1),\n            self._make_path_action_from_segment(first_segment),\n        ]\n        for segment in self.segments[1:-1]:\n            action = self._make_path_action_from_segment(segment)\n            actions.append(action)\n        actions.append(self._make_path_action_from_segment(last_segment))\n    path = Path(\n        actions=tuple(actions),\n        class_=f\"{type(self).__name__}_path\",\n        fill=self.path_fill,\n        filter_=self.path_filter,\n        id_=f\"{self.id_}_path\",\n        stroke=self.path_stroke,\n        stroke_dasharray=self.path_stroke_dasharray,\n        stroke_dashoffset=self.path_stroke_dashoffset,\n        stroke_width=self.path_stroke_width,\n        transform=self.path_transform,\n    )\n    return [path]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.LogicArcLayout.points","title":"points","text":"<pre><code>points() -&gt; list[Point]\n</code></pre> <p>Return the points of the arc path.</p> <p>An arc with no segments has no points and returns an empty list.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def points(self) -&gt; list[Point]:\n    \"\"\"Return the points of the arc path.\n\n    An arc with no segments has no points and returns an empty list.\n    \"\"\"\n    if not self.segments:\n        return []\n    points = []\n    for segment in self.segments:\n        points.append(segment.p1)\n    points.append(segment.p2)\n    return points\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.LogicArcLayout.start_point","title":"start_point","text":"<pre><code>start_point() -&gt; Point\n</code></pre> <p>Return the starting point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_point(self) -&gt; Point:\n    \"\"\"Return the starting point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[0]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.LogicArcLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.Modification","title":"Modification  <code>dataclass</code>","text":"<pre><code>Modification(*, id_: str = make_uuid4_as_str(), residue: ModificationResidue | ModificationSite | None = None, state: ModificationState | None = None)\n</code></pre> <p>               Bases: <code>CellDesignerModelElement</code></p> <p>Modification.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'46c93bff-32b3-49d9-b618-0409fc4138bd'</code> <code>residue</code> <code>ModificationResidue | ModificationSite | None</code> <p>The residue of the modification</p> <code>None</code> <code>state</code> <code>ModificationState | None</code> <p>The state of the modification</p> <code>None</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.Modification.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ModificationLayout","title":"ModificationLayout  <code>dataclass</code>","text":"<pre><code>ModificationLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = white, filter_: NoneValueType | Filter | None = None, height: float = 16.0, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = 1.25, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 16.0)\n</code></pre> <p>               Bases: <code>_SimpleNodeMixin</code>, <code>CellDesignerNode</code></p> <p>Modification layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'02e8d5a2-e3b4-4dcd-aa40-9dc2cbe47a15'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>16.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The stroke width of the node</p> <code>1.25</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>16.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>angle</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Return the east anchor of the node.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Return the label center anchor of the node.</p> <code>north</code> <p>Return the north anchor of the node.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Return the south anchor of the node.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Return the west anchor of the node.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ModificationLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ModificationLayout.angle","title":"angle","text":"<pre><code>angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ModificationLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ModificationLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ModificationLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ModificationLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ModificationLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ModificationLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ModificationLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ModificationLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ModificationLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Return the east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Return the east anchor of the node.\"\"\"\n    return self.own_angle(0)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ModificationLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ModificationLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ModificationLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ModificationLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ModificationLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Return the label center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Return the label center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ModificationLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Return the north anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Return the north anchor of the node.\"\"\"\n    return self.own_angle(90)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ModificationLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ModificationLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ModificationLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ModificationLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ModificationLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ModificationLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ModificationLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ModificationLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ModificationLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ModificationLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ModificationLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ModificationLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Return the south anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Return the south anchor of the node.\"\"\"\n    return self.own_angle(270)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ModificationLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ModificationLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ModificationLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ModificationLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ModificationLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ModificationLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Return the west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Return the west anchor of the node.\"\"\"\n    return self.own_angle(180)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ModificationLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ModificationLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ModificationLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ModificationLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ModificationResidue","title":"ModificationResidue  <code>dataclass</code>","text":"<pre><code>ModificationResidue(*, id_: str = make_uuid4_as_str(), name: str | None = None, order: int | None = None)\n</code></pre> <p>               Bases: <code>CellDesignerModelElement</code></p> <p>Modification residue.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'305bff33-5434-4d35-8714-606863c00610'</code> <code>name</code> <code>str | None</code> <p>The name of the residue</p> <code>None</code> <code>order</code> <code>int | None</code> <p>The sequential order of the residue</p> <code>None</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ModificationResidue.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ModificationSite","title":"ModificationSite  <code>dataclass</code>","text":"<pre><code>ModificationSite(*, id_: str = make_uuid4_as_str(), name: str | None = None, active: bool = False)\n</code></pre> <p>               Bases: <code>Region</code></p> <p>Modification site.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'fe59a8f5-f61f-4fb4-86d7-ded07c1eafb3'</code> <code>name</code> <code>str | None</code> <p>The name of the region</p> <code>None</code> <code>active</code> <code>bool</code> <p>Whether the region is active or not</p> <code>False</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ModificationSite.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ModificationState","title":"ModificationState","text":"<p>               Bases: <code>Enum</code></p> <p>Modification state.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.Modulation","title":"Modulation  <code>dataclass</code>","text":"<pre><code>Modulation(*, id_: str = make_uuid4_as_str(), source: Species | BooleanLogicGate, target: Species | None)\n</code></pre> <p>               Bases: <code>KnownOrUnknownModulation</code></p> <p>Modulation.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'0718df9d-72e0-4ec6-a0b5-3a33529082db'</code> <code>source</code> <code>Species | BooleanLogicGate</code> <p>The source of the modulation</p> required <code>target</code> <code>Species | None</code> <p>The target of the modulation</p> required <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.Modulation.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ModulationLayout","title":"ModulationLayout  <code>dataclass</code>","text":"<pre><code>ModulationLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, end_shorten: float = 0.0, fill: NoneValueType | Color | None = None, filter_: NoneValueType | Filter | None = None, path_fill: NoneValueType | Color | None = NoneValue, path_filter: NoneValueType | Filter | None = None, path_stroke: NoneValueType | Color | None = black, path_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, path_stroke_dashoffset: NoneValueType | float | None = None, path_stroke_width: float | None = 1.0, path_transform: NoneValueType | tuple[Transformation, ...] | None = None, stroke: NoneValueType | Color | None = None, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: NoneValueType | float | None = None, segments: tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...] = tuple(), source: LayoutElement | None = None, start_shorten: float = 0.0, target: LayoutElement | None = None, transform: NoneValueType | tuple[Transformation, ...] | None = None, arrowhead_fill: NoneValueType | Color | None = white, arrowhead_filter: NoneValueType | Filter | None = None, arrowhead_stroke: NoneValueType | Color | None = black, arrowhead_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, arrowhead_stroke_dashoffset: NoneValueType | float | None = None, arrowhead_stroke_width: float | None = 1.0, arrowhead_transform: NoneValueType | tuple[Transformation, ...] | None = None, arrowhead_height: float = 8.0, arrowhead_width: float = 15.0)\n</code></pre> <p>               Bases: <code>CellDesignerSingleHeadedArc</code></p> <p>Modulation layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'cc477d59-217b-4ca2-9ae3-67b0258c803a'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>end_shorten</code> <code>float</code> <p>The length the end of the arc will be shorten by</p> <code>0.0</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the arc</p> <code>None</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the arc</p> <code>None</code> <code>path_fill</code> <code>NoneValueType | Color | None</code> <p>The path fill color of the arc</p> <code>&lt;momapy.drawing.NoneValueType object at 0x7f8b17323e00&gt;</code> <code>path_filter</code> <code>NoneValueType | Filter | None</code> <p>The path filter of the arc</p> <code>None</code> <code>path_stroke</code> <code>NoneValueType | Color | None</code> <p>The path stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>path_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The path stroke dasharray of the arc</p> <code>None</code> <code>path_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The path stroke dashoffset of the arc</p> <code>None</code> <code>path_stroke_width</code> <code>float | None</code> <p>The path stroke width of the arc</p> <code>1.0</code> <code>path_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The path transform of the arc</p> <code>None</code> <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the arc</p> <code>None</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the arc</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the arc</p> <code>None</code> <code>stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the arc</p> <code>None</code> <code>segments</code> <code>tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...]</code> <p>The path segments of the arc</p> <code>&lt;dynamic&gt;</code> <code>source</code> <code>LayoutElement | None</code> <p>The source of the arc</p> <code>None</code> <code>start_shorten</code> <code>float</code> <p>The length the start of the arc will be shorten by</p> <code>0.0</code> <code>target</code> <code>LayoutElement | None</code> <p>The target of the arc</p> <code>None</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the arc</p> <code>None</code> <code>arrowhead_fill</code> <code>NoneValueType | Color | None</code> <p>The arrowhead fill color of the arc</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>arrowhead_filter</code> <code>NoneValueType | Filter | None</code> <p>The arrowhead filter of the arc</p> <code>None</code> <code>arrowhead_stroke</code> <code>NoneValueType | Color | None</code> <p>The arrowhead stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>arrowhead_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The arrowhead stroke dasharray of the arc</p> <code>None</code> <code>arrowhead_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The arrowhead stroke dashoffset of the arc</p> <code>None</code> <code>arrowhead_stroke_width</code> <code>float | None</code> <p>The arrowhead stroke width of the arc</p> <code>1.0</code> <code>arrowhead_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The arrowhead transform of the arc</p> <code>None</code> <code>arrowhead_height</code> <code>float</code> <p>The height of the arrowhead.</p> <code>8.0</code> <code>arrowhead_width</code> <code>float</code> <p>The width of the arrowhead.</p> <code>15.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>arrowhead_base</code> <p>Return the arrowhead base anchor point of the single-headed arc.</p> <code>arrowhead_bbox</code> <p>Return the bounding box of the single-headed arc arrowhead.</p> <code>arrowhead_border</code> <p>Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.</p> <code>arrowhead_drawing_elements</code> <p>Return the drawing elements of the single-headed arc arrowhead.</p> <code>arrowhead_length</code> <p>Return the length of the single-headed arc arrowhead.</p> <code>arrowhead_tip</code> <p>Return the arrowhead tip anchor point of the single-headed arc.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>childless</code> <p>Return a copy of the arc with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>end_point</code> <p>Return the ending point of the arc.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>fraction</code> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <code>length</code> <p>Return the total length of the arc path.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_children</code> <p>Return the self children of the arc.</p> <code>own_drawing_elements</code> <p>Return the arc's own drawing elements, including those of its mixins.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>path_drawing_elements</code> <p>Return the drawing elements of the single-headed arc path.</p> <code>points</code> <p>Return the points of the arc path.</p> <code>start_point</code> <p>Return the starting point of the arc.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ModulationLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ModulationLayout.arrowhead_base","title":"arrowhead_base","text":"<pre><code>arrowhead_base() -&gt; Point\n</code></pre> <p>Return the arrowhead base anchor point of the single-headed arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_base(self) -&gt; Point:\n    \"\"\"Return the arrowhead base anchor point of the single-headed arc.\"\"\"\n    arrowhead_length = self.arrowhead_length()\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return self.arrowhead_tip() - (arrowhead_length, 0)\n    fraction = 1 - (arrowhead_length + self.end_shorten) / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ModulationLayout.arrowhead_bbox","title":"arrowhead_bbox","text":"<pre><code>arrowhead_bbox() -&gt; Bbox\n</code></pre> <p>Return the bounding box of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_bbox(self) -&gt; Bbox:\n    \"\"\"Return the bounding box of the single-headed arc arrowhead.\"\"\"\n    return get_drawing_elements_bbox(self.arrowhead_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ModulationLayout.arrowhead_border","title":"arrowhead_border","text":"<pre><code>arrowhead_border(point: Point) -&gt; Point\n</code></pre> <p>Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_border(self, point: Point) -&gt; Point:\n    \"\"\"Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    point = get_drawing_elements_border(self.arrowhead_drawing_elements(), point)\n    if point is None:\n        return self.arrowhead_tip()\n    return point\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ModulationLayout.arrowhead_drawing_elements","title":"arrowhead_drawing_elements","text":"<pre><code>arrowhead_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_drawing_elements(\n    self,\n) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the single-headed arc arrowhead.\"\"\"\n    drawing_elements = self._arrowhead_border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_arrowhead\",\n        elements=tuple(drawing_elements),\n        fill=self.arrowhead_fill,\n        filter_=self.arrowhead_filter,\n        id_=f\"{self.id_}_arrowhead\",\n        stroke=self.arrowhead_stroke,\n        stroke_dasharray=self.arrowhead_stroke_dasharray,\n        stroke_dashoffset=self.arrowhead_stroke_dashoffset,\n        stroke_width=self.arrowhead_stroke_width,\n        transform=self.arrowhead_transform,\n    )\n    transformation = self._get_arrowhead_transformation()\n    group = group.transformed(transformation)\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ModulationLayout.arrowhead_length","title":"arrowhead_length","text":"<pre><code>arrowhead_length() -&gt; float\n</code></pre> <p>Return the length of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_length(self) -&gt; float:\n    \"\"\"Return the length of the single-headed arc arrowhead.\"\"\"\n    bbox = get_drawing_elements_bbox(self._arrowhead_border_drawing_elements())\n    if math.isnan(bbox.width):\n        return 0.0\n    return bbox.east().x\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ModulationLayout.arrowhead_tip","title":"arrowhead_tip","text":"<pre><code>arrowhead_tip() -&gt; Point\n</code></pre> <p>Return the arrowhead tip anchor point of the single-headed arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_tip(self) -&gt; Point:\n    \"\"\"Return the arrowhead tip anchor point of the single-headed arc.\"\"\"\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return segment.p2\n    fraction = 1 - self.end_shorten / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ModulationLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ModulationLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the arc with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the arc with no children.\"\"\"\n    return dataclasses.replace(self, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ModulationLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ModulationLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ModulationLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ModulationLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ModulationLayout.end_point","title":"end_point","text":"<pre><code>end_point() -&gt; Point\n</code></pre> <p>Return the ending point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_point(self) -&gt; Point:\n    \"\"\"Return the ending point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[-1]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ModulationLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ModulationLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ModulationLayout.fraction","title":"fraction","text":"<pre><code>fraction(fraction: float) -&gt; tuple[Point, float]\n</code></pre> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def fraction(self, fraction: float) -&gt; tuple[Point, float]:\n    \"\"\"Return the position and angle on the arc at a given fraction (of the total arc length).\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    current_length = 0\n    length_to_reach = fraction * self.length()\n    for segment in self.segments:\n        current_length += segment.length()\n        if current_length &gt;= length_to_reach:\n            break\n    segment_start_length = current_length - segment.length()\n    segment_fraction = (length_to_reach - segment_start_length) / segment.length()\n    position, angle = segment.get_position_and_angle_at_fraction(segment_fraction)\n    return position, angle\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ModulationLayout.length","title":"length","text":"<pre><code>length() -&gt; float\n</code></pre> <p>Return the total length of the arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def length(self) -&gt; float:\n    \"\"\"Return the total length of the arc path.\"\"\"\n    return sum([segment.length() for segment in self.segments])\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ModulationLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ModulationLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the arc.\"\"\"\n    return []\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ModulationLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the arc's own drawing elements, including those of its mixins.</p> Source code in <code>src/momapy/celldesigner/elements.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the arc's own drawing elements, including those of its mixins.\"\"\"\n    drawing_elements = super_or_builder(\n        CellDesignerSingleHeadedArc, self\n    ).own_drawing_elements()\n    done_bases = []\n    for base in type(self).__mro__:\n        if (\n            issubclass_or_builder(base, _SBGNMixin)\n            and base is not type(self)\n            and not any([issubclass(done_base, base) for done_base in done_bases])\n        ):\n            drawing_elements += getattr(base, \"_mixin_drawing_elements\")(self)\n            done_bases.append(base)\n    return drawing_elements\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ModulationLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ModulationLayout.path_drawing_elements","title":"path_drawing_elements","text":"<pre><code>path_drawing_elements() -&gt; list[Path]\n</code></pre> <p>Return the drawing elements of the single-headed arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def path_drawing_elements(self) -&gt; list[Path]:\n    \"\"\"Return the drawing elements of the single-headed arc path.\"\"\"\n    arrowhead_length = self.arrowhead_length()\n    if len(self.segments) == 1:\n        segment = (\n            self.segments[0]\n            .shortened(self.start_shorten, \"start\")\n            .shortened(self.end_shorten + arrowhead_length, \"end\")\n        )\n        actions = [\n            MoveTo(segment.p1),\n            self._make_path_action_from_segment(segment),\n        ]\n    else:\n        first_segment = self.segments[0].shortened(self.start_shorten, \"start\")\n        last_segment = self.segments[-1].shortened(\n            self.end_shorten + arrowhead_length, \"end\"\n        )\n        actions = [\n            MoveTo(first_segment.p1),\n            self._make_path_action_from_segment(first_segment),\n        ]\n        for segment in self.segments[1:-1]:\n            action = self._make_path_action_from_segment(segment)\n            actions.append(action)\n        actions.append(self._make_path_action_from_segment(last_segment))\n    path = Path(\n        actions=tuple(actions),\n        class_=f\"{type(self).__name__}_path\",\n        fill=self.path_fill,\n        filter_=self.path_filter,\n        id_=f\"{self.id_}_path\",\n        stroke=self.path_stroke,\n        stroke_dasharray=self.path_stroke_dasharray,\n        stroke_dashoffset=self.path_stroke_dashoffset,\n        stroke_width=self.path_stroke_width,\n        transform=self.path_transform,\n    )\n    return [path]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ModulationLayout.points","title":"points","text":"<pre><code>points() -&gt; list[Point]\n</code></pre> <p>Return the points of the arc path.</p> <p>An arc with no segments has no points and returns an empty list.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def points(self) -&gt; list[Point]:\n    \"\"\"Return the points of the arc path.\n\n    An arc with no segments has no points and returns an empty list.\n    \"\"\"\n    if not self.segments:\n        return []\n    points = []\n    for segment in self.segments:\n        points.append(segment.p1)\n    points.append(segment.p2)\n    return points\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ModulationLayout.start_point","title":"start_point","text":"<pre><code>start_point() -&gt; Point\n</code></pre> <p>Return the starting point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_point(self) -&gt; Point:\n    \"\"\"Return the starting point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[0]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ModulationLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.Modulator","title":"Modulator  <code>dataclass</code>","text":"<pre><code>Modulator(*, id_: str = make_uuid4_as_str(), name: str | None = None, sbo_term: str | None = None, metaid: str | None = None, referred_element: Species | BooleanLogicGate)\n</code></pre> <p>               Bases: <code>KnownOrUnknownModulator</code></p> <p>Modulator.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'da77edbc-00ab-4858-b948-852b3ed3100b'</code> <code>name</code> <code>str | None</code> <p>Human-readable name of the element.</p> <code>None</code> <code>sbo_term</code> <code>str | None</code> <p>Optional SBO term identifier for semantic annotation.</p> <code>None</code> <code>metaid</code> <code>str | None</code> <p>Optional metadata identifier for RDF annotations.</p> <code>None</code> <code>referred_element</code> <code>Species | BooleanLogicGate</code> <p>The species or boolean logic gate the modifier refers to</p> required <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.Modulator.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.NegativeInfluence","title":"NegativeInfluence  <code>dataclass</code>","text":"<pre><code>NegativeInfluence(*, id_: str = make_uuid4_as_str(), source: Species | BooleanLogicGate, target: Species | None)\n</code></pre> <p>               Bases: <code>Modulation</code></p> <p>Negative influence.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'9a828e41-c69a-4e56-81c3-8a2e72bda089'</code> <code>source</code> <code>Species | BooleanLogicGate</code> <p>The source of the modulation</p> required <code>target</code> <code>Species | None</code> <p>The target of the modulation</p> required <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.NegativeInfluence.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.NotGate","title":"NotGate  <code>dataclass</code>","text":"<pre><code>NotGate(*, id_: str = make_uuid4_as_str(), inputs: frozenset[BooleanLogicGateInput] = frozenset())\n</code></pre> <p>               Bases: <code>BooleanLogicGate</code></p> <p>NOT gate.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'1e116dd9-3986-44b1-9093-f52815f046f9'</code> <code>inputs</code> <code>frozenset[BooleanLogicGateInput]</code> <p>The inputs of the Boolean logic gate</p> <code>&lt;dynamic&gt;</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.NotGate.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.NotGateLayout","title":"NotGateLayout  <code>dataclass</code>","text":"<pre><code>NotGateLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = white, filter_: NoneValueType | Filter | None = None, height: float = 15.0, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = 1.25, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 15.0, text: str = '!', font_family: str = DEFAULT_FONT_FAMILY, font_fill: Color | NoneValueType = black, font_stroke: Color | NoneValueType = NoneValue, font_style: FontStyle = NORMAL, font_weight: FontWeight | float = NORMAL)\n</code></pre> <p>               Bases: <code>_SimpleNodeMixin</code>, <code>_TextMixin</code>, <code>CellDesignerNode</code></p> <p>NOT gate layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'eec1e4b5-1a76-4371-8996-fbf05ce27c3e'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>15.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The stroke width of the node</p> <code>1.25</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>15.0</code> <code>text</code> <code>str</code> <p>The label drawn inside the gate.</p> <code>'!'</code> <code>font_family</code> <code>str</code> <p>Font family for the text.</p> <code>'DejaVu Sans'</code> <code>font_fill</code> <code>Color | NoneValueType</code> <p>Fill color for the text.</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>font_stroke</code> <code>Color | NoneValueType</code> <p>Stroke color for the text outline.</p> <code>&lt;momapy.drawing.NoneValueType object at 0x7f8b17323e00&gt;</code> <code>font_style</code> <code>FontStyle</code> <p>Font style (normal, italic, etc.).</p> <code>&lt;FontStyle.NORMAL: 0&gt;</code> <code>font_weight</code> <code>FontWeight | float</code> <p>Font weight (normal, bold, etc.).</p> <code>&lt;FontWeight.NORMAL: 0&gt;</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>angle</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Return the east anchor of the node.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Return the label center anchor of the node.</p> <code>north</code> <p>Return the north anchor of the node.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Return the south anchor of the node.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Return the west anchor of the node.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.NotGateLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.NotGateLayout.angle","title":"angle","text":"<pre><code>angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.NotGateLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.NotGateLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.NotGateLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.NotGateLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.NotGateLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.NotGateLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.NotGateLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.NotGateLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.NotGateLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Return the east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Return the east anchor of the node.\"\"\"\n    return self.own_angle(0)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.NotGateLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.NotGateLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.NotGateLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.NotGateLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.NotGateLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Return the label center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Return the label center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.NotGateLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Return the north anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Return the north anchor of the node.\"\"\"\n    return self.own_angle(90)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.NotGateLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.NotGateLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.NotGateLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.NotGateLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.NotGateLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.NotGateLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.NotGateLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.NotGateLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.NotGateLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.NotGateLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.NotGateLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.NotGateLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Return the south anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Return the south anchor of the node.\"\"\"\n    return self.own_angle(270)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.NotGateLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.NotGateLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.NotGateLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.NotGateLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.NotGateLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.NotGateLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Return the west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Return the west anchor of the node.\"\"\"\n    return self.own_angle(180)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.NotGateLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.NotGateLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.NotGateLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.NotGateLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.OrGate","title":"OrGate  <code>dataclass</code>","text":"<pre><code>OrGate(*, id_: str = make_uuid4_as_str(), inputs: frozenset[BooleanLogicGateInput] = frozenset())\n</code></pre> <p>               Bases: <code>BooleanLogicGate</code></p> <p>OR gate.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'db3e3a79-e93a-425c-bbcb-1c5ebaad1c7b'</code> <code>inputs</code> <code>frozenset[BooleanLogicGateInput]</code> <p>The inputs of the Boolean logic gate</p> <code>&lt;dynamic&gt;</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.OrGate.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.OrGateLayout","title":"OrGateLayout  <code>dataclass</code>","text":"<pre><code>OrGateLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = white, filter_: NoneValueType | Filter | None = None, height: float = 15.0, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = 1.25, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 15.0, text: str = '|', font_family: str = DEFAULT_FONT_FAMILY, font_fill: Color | NoneValueType = black, font_stroke: Color | NoneValueType = NoneValue, font_style: FontStyle = NORMAL, font_weight: FontWeight | float = NORMAL)\n</code></pre> <p>               Bases: <code>_SimpleNodeMixin</code>, <code>_TextMixin</code>, <code>CellDesignerNode</code></p> <p>OR gate layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'7722dc93-d9e4-4878-8325-fe9c03e16a38'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>15.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The stroke width of the node</p> <code>1.25</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>15.0</code> <code>text</code> <code>str</code> <p>The label drawn inside the gate.</p> <code>'|'</code> <code>font_family</code> <code>str</code> <p>Font family for the text.</p> <code>'DejaVu Sans'</code> <code>font_fill</code> <code>Color | NoneValueType</code> <p>Fill color for the text.</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>font_stroke</code> <code>Color | NoneValueType</code> <p>Stroke color for the text outline.</p> <code>&lt;momapy.drawing.NoneValueType object at 0x7f8b17323e00&gt;</code> <code>font_style</code> <code>FontStyle</code> <p>Font style (normal, italic, etc.).</p> <code>&lt;FontStyle.NORMAL: 0&gt;</code> <code>font_weight</code> <code>FontWeight | float</code> <p>Font weight (normal, bold, etc.).</p> <code>&lt;FontWeight.NORMAL: 0&gt;</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>angle</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Return the east anchor of the node.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Return the label center anchor of the node.</p> <code>north</code> <p>Return the north anchor of the node.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Return the south anchor of the node.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Return the west anchor of the node.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.OrGateLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.OrGateLayout.angle","title":"angle","text":"<pre><code>angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.OrGateLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.OrGateLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.OrGateLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.OrGateLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.OrGateLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.OrGateLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.OrGateLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.OrGateLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.OrGateLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Return the east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Return the east anchor of the node.\"\"\"\n    return self.own_angle(0)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.OrGateLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.OrGateLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.OrGateLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.OrGateLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.OrGateLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Return the label center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Return the label center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.OrGateLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Return the north anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Return the north anchor of the node.\"\"\"\n    return self.own_angle(90)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.OrGateLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.OrGateLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.OrGateLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.OrGateLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.OrGateLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.OrGateLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.OrGateLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.OrGateLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.OrGateLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.OrGateLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.OrGateLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.OrGateLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Return the south anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Return the south anchor of the node.\"\"\"\n    return self.own_angle(270)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.OrGateLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.OrGateLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.OrGateLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.OrGateLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.OrGateLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.OrGateLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Return the west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Return the west anchor of the node.\"\"\"\n    return self.own_angle(180)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.OrGateLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.OrGateLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.OrGateLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.OrGateLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.OvalCompartmentLayout","title":"OvalCompartmentLayout  <code>dataclass</code>","text":"<pre><code>OvalCompartmentLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = white, filter_: NoneValueType | Filter | None = None, height: float = 16.0, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = 1.25, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 16.0, inner_fill: NoneValueType | Color | None = white, inner_stroke: NoneValueType | Color | None = black, inner_stroke_width: float | None = 1.0, sep: float = 12.0)\n</code></pre> <p>               Bases: <code>_SimpleNodeMixin</code>, <code>CellDesignerNode</code></p> <p>Oval compartment layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'608cc384-e2f6-46a7-a044-2db37922c026'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>16.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The stroke width of the node</p> <code>1.25</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>16.0</code> <code>inner_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the inner ellipse.</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>inner_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the inner ellipse.</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>inner_stroke_width</code> <code>float | None</code> <p>The stroke width of the inner ellipse.</p> <code>1.0</code> <code>sep</code> <code>float</code> <p>The separation between the outer and inner ellipses.</p> <code>12.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>angle</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Return the east anchor of the node.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Return the label center anchor of the node.</p> <code>north</code> <p>Return the north anchor of the node.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Return the south anchor of the node.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Return the west anchor of the node.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.OvalCompartmentLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.OvalCompartmentLayout.angle","title":"angle","text":"<pre><code>angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.OvalCompartmentLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.OvalCompartmentLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.OvalCompartmentLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.OvalCompartmentLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.OvalCompartmentLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.OvalCompartmentLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.OvalCompartmentLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.OvalCompartmentLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.OvalCompartmentLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Return the east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Return the east anchor of the node.\"\"\"\n    return self.own_angle(0)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.OvalCompartmentLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.OvalCompartmentLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.OvalCompartmentLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.OvalCompartmentLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.OvalCompartmentLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Return the label center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Return the label center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.OvalCompartmentLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Return the north anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Return the north anchor of the node.\"\"\"\n    return self.own_angle(90)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.OvalCompartmentLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.OvalCompartmentLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.OvalCompartmentLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.OvalCompartmentLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.OvalCompartmentLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.OvalCompartmentLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.OvalCompartmentLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.OvalCompartmentLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.OvalCompartmentLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.OvalCompartmentLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.OvalCompartmentLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.OvalCompartmentLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Return the south anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Return the south anchor of the node.\"\"\"\n    return self.own_angle(270)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.OvalCompartmentLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.OvalCompartmentLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.OvalCompartmentLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.OvalCompartmentLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.OvalCompartmentLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.OvalCompartmentLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Return the west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Return the west anchor of the node.\"\"\"\n    return self.own_angle(180)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.OvalCompartmentLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.OvalCompartmentLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.OvalCompartmentLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.OvalCompartmentLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.Phenotype","title":"Phenotype  <code>dataclass</code>","text":"<pre><code>Phenotype(*, id_: str = make_uuid4_as_str(), name: str | None = None, sbo_term: str | None = None, metaid: str | None = None, compartment: Compartment | None = None, hypothetical: bool = False, active: bool = False, homomultimer: int = 1)\n</code></pre> <p>               Bases: <code>Species</code></p> <p>Phenotype.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'cc186c71-c701-4436-b765-526c9edcde39'</code> <code>name</code> <code>str | None</code> <p>Human-readable name of the element.</p> <code>None</code> <code>sbo_term</code> <code>str | None</code> <p>Optional SBO term identifier for semantic annotation.</p> <code>None</code> <code>metaid</code> <code>str | None</code> <p>Optional metadata identifier for RDF annotations.</p> <code>None</code> <code>compartment</code> <code>Compartment | None</code> <p>The compartment containing this species.</p> <code>None</code> <code>hypothetical</code> <code>bool</code> <p>Whether the species is hypothetical or not</p> <code>False</code> <code>active</code> <code>bool</code> <p>Whether the species is active or not</p> <code>False</code> <code>homomultimer</code> <code>int</code> <p>The number of subunits forming the species</p> <code>1</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.Phenotype.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhenotypeActiveLayout","title":"PhenotypeActiveLayout  <code>dataclass</code>","text":"<pre><code>PhenotypeActiveLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = NoneValue, filter_: NoneValueType | Filter | None = None, height: float = 30.0 + DEFAULT_ACTIVE_YSEP * 2, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = (4, 2), stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = 1.0, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 80.0 + DEFAULT_ACTIVE_XSEP * 2, offset: float = 3.0, subunits_stroke: tuple[NoneValueType | Color, ...] | None = None, subunits_stroke_width: tuple[NoneValueType | float, ...] | None = None, subunits_stroke_dasharray: tuple[NoneValueType | tuple[float, ...], ...] | None = None, subunits_stroke_dashoffset: tuple[float, ...] | None = None, subunits_fill: tuple[NoneValueType | Color, ...] | None = None, subunits_transform: tuple[NoneValueType | tuple[Transformation, ...], ...] | None = None, subunits_filter: tuple[NoneValueType | Filter, ...] | None = None, n: int = 1, angle: float = 60.0)\n</code></pre> <p>               Bases: <code>_MultiNodeMixin</code>, <code>CellDesignerNode</code></p> <p>Phenotype active layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'f0db3921-1f40-4a65-b685-b0e1c5ab92b1'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>&lt;momapy.drawing.NoneValueType object at 0x7f8b17323e00&gt;</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>38.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>(4, 2)</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The stroke width of the node</p> <code>1.0</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>88.0</code> <code>offset</code> <code>float</code> <p>Offset distance between stacked units.</p> <code>3.0</code> <code>subunits_stroke</code> <code>tuple[NoneValueType | Color, ...] | None</code> <p>Tuple of stroke colors for each subunit.</p> <code>None</code> <code>subunits_stroke_width</code> <code>tuple[NoneValueType | float, ...] | None</code> <p>Tuple of stroke widths for each subunit.</p> <code>None</code> <code>subunits_stroke_dasharray</code> <code>tuple[NoneValueType | tuple[float, ...], ...] | None</code> <p>Tuple of dash patterns for each subunit.</p> <code>None</code> <code>subunits_stroke_dashoffset</code> <code>tuple[float, ...] | None</code> <p>Tuple of dash offsets for each subunit.</p> <code>None</code> <code>subunits_fill</code> <code>tuple[NoneValueType | Color, ...] | None</code> <p>Tuple of fill colors for each subunit.</p> <code>None</code> <code>subunits_transform</code> <code>tuple[NoneValueType | tuple[Transformation, ...], ...] | None</code> <p>Tuple of transformations for each subunit.</p> <code>None</code> <code>subunits_filter</code> <code>tuple[NoneValueType | Filter, ...] | None</code> <p>Tuple of filters for each subunit.</p> <code>None</code> <code>n</code> <code>int</code> <p>Number of stacked node copies to draw.</p> <code>1</code> <code>angle</code> <code>float</code> <p>The angle of the left and right points of the hexagon.</p> <code>60.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Return the east anchor of the node.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Get the center position for the node label.</p> <code>north</code> <p>Return the north anchor of the node.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Return the south anchor of the node.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Return the west anchor of the node.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>angle</code> <code>float</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhenotypeActiveLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhenotypeActiveLayout.angle","title":"angle  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>angle: float = dataclasses.field(default=60.0, metadata={'description': 'The angle of the left and right points of the hexagon.'})\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhenotypeActiveLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhenotypeActiveLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhenotypeActiveLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhenotypeActiveLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhenotypeActiveLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhenotypeActiveLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhenotypeActiveLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhenotypeActiveLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhenotypeActiveLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Return the east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Return the east anchor of the node.\"\"\"\n    return self.own_angle(0)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhenotypeActiveLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhenotypeActiveLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhenotypeActiveLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhenotypeActiveLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhenotypeActiveLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Get the center position for the node label.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point where the label should be centered.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Get the center position for the node label.\n\n    Returns:\n        Point where the label should be centered.\n    \"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2 - (self._n - 1) * self.offset,\n        self.height / 2 - height / 2 - (self._n - 1) * self.offset,\n    )\n    return self._make_subunit_shape(\n        position=position, width=width, height=height\n    ).position\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhenotypeActiveLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Return the north anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Return the north anchor of the node.\"\"\"\n    return self.own_angle(90)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhenotypeActiveLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhenotypeActiveLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhenotypeActiveLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhenotypeActiveLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhenotypeActiveLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhenotypeActiveLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhenotypeActiveLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhenotypeActiveLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhenotypeActiveLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhenotypeActiveLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhenotypeActiveLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhenotypeActiveLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Return the south anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Return the south anchor of the node.\"\"\"\n    return self.own_angle(270)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhenotypeActiveLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhenotypeActiveLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhenotypeActiveLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhenotypeActiveLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhenotypeActiveLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhenotypeActiveLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Return the west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Return the west anchor of the node.\"\"\"\n    return self.own_angle(180)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhenotypeActiveLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhenotypeActiveLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhenotypeActiveLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhenotypeActiveLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhenotypeLayout","title":"PhenotypeLayout  <code>dataclass</code>","text":"<pre><code>PhenotypeLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = from_hex('#CC99FF'), filter_: NoneValueType | Filter | None = None, height: float = 30.0, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = 1.0, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 80.0, offset: float = 3.0, subunits_stroke: tuple[NoneValueType | Color, ...] | None = None, subunits_stroke_width: tuple[NoneValueType | float, ...] | None = None, subunits_stroke_dasharray: tuple[NoneValueType | tuple[float, ...], ...] | None = None, subunits_stroke_dashoffset: tuple[float, ...] | None = None, subunits_fill: tuple[NoneValueType | Color, ...] | None = None, subunits_transform: tuple[NoneValueType | tuple[Transformation, ...], ...] | None = None, subunits_filter: tuple[NoneValueType | Filter, ...] | None = None, n: int = 1, angle: float = 60.0)\n</code></pre> <p>               Bases: <code>_MultiNodeMixin</code>, <code>CellDesignerNode</code></p> <p>Phenotype layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'c0a26972-7825-4b34-b422-a606761a2945'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>Color(red=204, green=153, blue=255, alpha=1.0)</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>30.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The stroke width of the node</p> <code>1.0</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>80.0</code> <code>offset</code> <code>float</code> <p>Offset distance between stacked units.</p> <code>3.0</code> <code>subunits_stroke</code> <code>tuple[NoneValueType | Color, ...] | None</code> <p>Tuple of stroke colors for each subunit.</p> <code>None</code> <code>subunits_stroke_width</code> <code>tuple[NoneValueType | float, ...] | None</code> <p>Tuple of stroke widths for each subunit.</p> <code>None</code> <code>subunits_stroke_dasharray</code> <code>tuple[NoneValueType | tuple[float, ...], ...] | None</code> <p>Tuple of dash patterns for each subunit.</p> <code>None</code> <code>subunits_stroke_dashoffset</code> <code>tuple[float, ...] | None</code> <p>Tuple of dash offsets for each subunit.</p> <code>None</code> <code>subunits_fill</code> <code>tuple[NoneValueType | Color, ...] | None</code> <p>Tuple of fill colors for each subunit.</p> <code>None</code> <code>subunits_transform</code> <code>tuple[NoneValueType | tuple[Transformation, ...], ...] | None</code> <p>Tuple of transformations for each subunit.</p> <code>None</code> <code>subunits_filter</code> <code>tuple[NoneValueType | Filter, ...] | None</code> <p>Tuple of filters for each subunit.</p> <code>None</code> <code>n</code> <code>int</code> <p>Number of stacked node copies to draw.</p> <code>1</code> <code>angle</code> <code>float</code> <p>The angle of the left and right points of the hexagon.</p> <code>60.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Return the east anchor point of the top subunit shape.</p> <code>east_north_east</code> <p>Return the east-north-east anchor point of the top subunit shape.</p> <code>east_south_east</code> <p>Return the east-south-east anchor point of the top subunit shape.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Get the center position for the node label.</p> <code>north</code> <p>Return the north anchor point of the top subunit shape.</p> <code>north_east</code> <p>Return the north-east anchor point of the top subunit shape.</p> <code>north_north_east</code> <p>Return the north-north-east anchor point of the top subunit shape.</p> <code>north_north_west</code> <p>Return the north-north-west anchor point of the top subunit shape.</p> <code>north_west</code> <p>Return the north-west anchor point of the top subunit shape.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Return the south anchor point of the top subunit shape.</p> <code>south_east</code> <p>Return the south-east anchor point of the top subunit shape.</p> <code>south_south_east</code> <p>Return the south-south-east anchor point of the top subunit shape.</p> <code>south_south_west</code> <p>Return the south-south-west anchor point of the top subunit shape.</p> <code>south_west</code> <p>Return the south-west anchor point of the top subunit shape.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Return the west anchor point of the top subunit shape.</p> <code>west_north_west</code> <p>Return the west-north-west anchor point of the top subunit shape.</p> <code>west_south_west</code> <p>Return the west-south-west anchor point of the top subunit shape.</p> <p>Attributes:</p> Name Type Description <code>angle</code> <code>float</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhenotypeLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhenotypeLayout.angle","title":"angle  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>angle: float = dataclasses.field(default=60.0, metadata={'description': 'The angle of the left and right points of the hexagon.'})\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhenotypeLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhenotypeLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhenotypeLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhenotypeLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhenotypeLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhenotypeLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhenotypeLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhenotypeLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhenotypeLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Return the east anchor point of the top subunit shape.</p> Source code in <code>src/momapy/celldesigner/layout.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Return the east anchor point of the top subunit shape.\"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2,\n        self.height / 2 - height / 2,\n    )\n    shape = self._make_subunit_shape(position, width, height)\n    return shape.joint3()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhenotypeLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east-north-east anchor point of the top subunit shape.</p> Source code in <code>src/momapy/celldesigner/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east-north-east anchor point of the top subunit shape.\"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2,\n        self.height / 2 - height / 2,\n    )\n    shape = self._make_subunit_shape(position, width, height)\n    return shape.joint3() / 2 + shape.joint2() / 2\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhenotypeLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east-south-east anchor point of the top subunit shape.</p> Source code in <code>src/momapy/celldesigner/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east-south-east anchor point of the top subunit shape.\"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2,\n        self.height / 2 - height / 2,\n    )\n    shape = self._make_subunit_shape(position, width, height)\n    return shape.joint4() / 2 + shape.joint3() / 2\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhenotypeLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhenotypeLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhenotypeLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Get the center position for the node label.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point where the label should be centered.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Get the center position for the node label.\n\n    Returns:\n        Point where the label should be centered.\n    \"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2 - (self._n - 1) * self.offset,\n        self.height / 2 - height / 2 - (self._n - 1) * self.offset,\n    )\n    return self._make_subunit_shape(\n        position=position, width=width, height=height\n    ).position\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhenotypeLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Return the north anchor point of the top subunit shape.</p> Source code in <code>src/momapy/celldesigner/layout.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Return the north anchor point of the top subunit shape.\"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2,\n        self.height / 2 - height / 2,\n    )\n    shape = self._make_subunit_shape(position, width, height)\n    return shape.joint1() / 2 + shape.joint2() / 2\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhenotypeLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north-east anchor point of the top subunit shape.</p> Source code in <code>src/momapy/celldesigner/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north-east anchor point of the top subunit shape.\"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2,\n        self.height / 2 - height / 2,\n    )\n    shape = self._make_subunit_shape(position, width, height)\n    return shape.joint2()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhenotypeLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north-north-east anchor point of the top subunit shape.</p> Source code in <code>src/momapy/celldesigner/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north-north-east anchor point of the top subunit shape.\"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2,\n        self.height / 2 - height / 2,\n    )\n    shape = self._make_subunit_shape(position, width, height)\n    return shape.joint1() * 0.25 + shape.joint2() * 0.75\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhenotypeLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north-north-west anchor point of the top subunit shape.</p> Source code in <code>src/momapy/celldesigner/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north-north-west anchor point of the top subunit shape.\"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2,\n        self.height / 2 - height / 2,\n    )\n    shape = self._make_subunit_shape(position, width, height)\n    return shape.joint1() * 0.75 + shape.joint2() * 0.25\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhenotypeLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north-west anchor point of the top subunit shape.</p> Source code in <code>src/momapy/celldesigner/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north-west anchor point of the top subunit shape.\"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2,\n        self.height / 2 - height / 2,\n    )\n    shape = self._make_subunit_shape(position, width, height)\n    return shape.joint1()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhenotypeLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhenotypeLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhenotypeLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhenotypeLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhenotypeLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhenotypeLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhenotypeLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhenotypeLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Return the south anchor point of the top subunit shape.</p> Source code in <code>src/momapy/celldesigner/layout.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Return the south anchor point of the top subunit shape.\"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2,\n        self.height / 2 - height / 2,\n    )\n    shape = self._make_subunit_shape(position, width, height)\n    return shape.joint5() / 2 + shape.joint4() / 2\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhenotypeLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south-east anchor point of the top subunit shape.</p> Source code in <code>src/momapy/celldesigner/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south-east anchor point of the top subunit shape.\"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2,\n        self.height / 2 - height / 2,\n    )\n    shape = self._make_subunit_shape(position, width, height)\n    return shape.joint4()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhenotypeLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south-south-east anchor point of the top subunit shape.</p> Source code in <code>src/momapy/celldesigner/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south-south-east anchor point of the top subunit shape.\"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2,\n        self.height / 2 - height / 2,\n    )\n    shape = self._make_subunit_shape(position, width, height)\n    return shape.joint5() * 0.25 + shape.joint4() * 0.75\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhenotypeLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south-south-west anchor point of the top subunit shape.</p> Source code in <code>src/momapy/celldesigner/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south-south-west anchor point of the top subunit shape.\"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2,\n        self.height / 2 - height / 2,\n    )\n    shape = self._make_subunit_shape(position, width, height)\n    return shape.joint5() * 0.75 + shape.joint4() * 0.25\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhenotypeLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south-west anchor point of the top subunit shape.</p> Source code in <code>src/momapy/celldesigner/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south-west anchor point of the top subunit shape.\"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2,\n        self.height / 2 - height / 2,\n    )\n    shape = self._make_subunit_shape(position, width, height)\n    return shape.joint5()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhenotypeLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhenotypeLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Return the west anchor point of the top subunit shape.</p> Source code in <code>src/momapy/celldesigner/layout.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Return the west anchor point of the top subunit shape.\"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2,\n        self.height / 2 - height / 2,\n    )\n    shape = self._make_subunit_shape(position, width, height)\n    return shape.joint6()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhenotypeLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west-north-west anchor point of the top subunit shape.</p> Source code in <code>src/momapy/celldesigner/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west-north-west anchor point of the top subunit shape.\"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2,\n        self.height / 2 - height / 2,\n    )\n    shape = self._make_subunit_shape(position, width, height)\n    return shape.joint6() / 2 + shape.joint1() / 2\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhenotypeLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west-south-west anchor point of the top subunit shape.</p> Source code in <code>src/momapy/celldesigner/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west-south-west anchor point of the top subunit shape.\"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2,\n        self.height / 2 - height / 2,\n    )\n    shape = self._make_subunit_shape(position, width, height)\n    return shape.joint6() / 2 + shape.joint5() / 2\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhenotypeLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhenotypeLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhysicalStimulation","title":"PhysicalStimulation  <code>dataclass</code>","text":"<pre><code>PhysicalStimulation(*, id_: str = make_uuid4_as_str(), source: Species | BooleanLogicGate, target: Species | None)\n</code></pre> <p>               Bases: <code>Modulation</code></p> <p>Physical stimulation.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'df111eb6-19e5-4e16-92b0-12da58142461'</code> <code>source</code> <code>Species | BooleanLogicGate</code> <p>The source of the modulation</p> required <code>target</code> <code>Species | None</code> <p>The target of the modulation</p> required <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhysicalStimulation.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhysicalStimulationLayout","title":"PhysicalStimulationLayout  <code>dataclass</code>","text":"<pre><code>PhysicalStimulationLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, end_shorten: float = 0.0, fill: NoneValueType | Color | None = None, filter_: NoneValueType | Filter | None = None, path_fill: NoneValueType | Color | None = NoneValue, path_filter: NoneValueType | Filter | None = None, path_stroke: NoneValueType | Color | None = black, path_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, path_stroke_dashoffset: NoneValueType | float | None = None, path_stroke_width: float | None = 1.0, path_transform: NoneValueType | tuple[Transformation, ...] | None = None, stroke: NoneValueType | Color | None = None, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: NoneValueType | float | None = None, segments: tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...] = tuple(), source: LayoutElement | None = None, start_shorten: float = 0.0, target: LayoutElement | None = None, transform: NoneValueType | tuple[Transformation, ...] | None = None, arrowhead_fill: NoneValueType | Color | None = white, arrowhead_filter: NoneValueType | Filter | None = None, arrowhead_stroke: NoneValueType | Color | None = black, arrowhead_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, arrowhead_stroke_dashoffset: NoneValueType | float | None = None, arrowhead_stroke_width: float | None = 1.0, arrowhead_transform: NoneValueType | tuple[Transformation, ...] | None = None, arrowhead_height: float = 10.0, arrowhead_width: float = 10.0)\n</code></pre> <p>               Bases: <code>CellDesignerSingleHeadedArc</code></p> <p>Physical stimulation layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'535f457f-2e64-494f-af75-d1803a4e8356'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>end_shorten</code> <code>float</code> <p>The length the end of the arc will be shorten by</p> <code>0.0</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the arc</p> <code>None</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the arc</p> <code>None</code> <code>path_fill</code> <code>NoneValueType | Color | None</code> <p>The path fill color of the arc</p> <code>&lt;momapy.drawing.NoneValueType object at 0x7f8b17323e00&gt;</code> <code>path_filter</code> <code>NoneValueType | Filter | None</code> <p>The path filter of the arc</p> <code>None</code> <code>path_stroke</code> <code>NoneValueType | Color | None</code> <p>The path stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>path_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The path stroke dasharray of the arc</p> <code>None</code> <code>path_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The path stroke dashoffset of the arc</p> <code>None</code> <code>path_stroke_width</code> <code>float | None</code> <p>The path stroke width of the arc</p> <code>1.0</code> <code>path_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The path transform of the arc</p> <code>None</code> <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the arc</p> <code>None</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the arc</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the arc</p> <code>None</code> <code>stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the arc</p> <code>None</code> <code>segments</code> <code>tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...]</code> <p>The path segments of the arc</p> <code>&lt;dynamic&gt;</code> <code>source</code> <code>LayoutElement | None</code> <p>The source of the arc</p> <code>None</code> <code>start_shorten</code> <code>float</code> <p>The length the start of the arc will be shorten by</p> <code>0.0</code> <code>target</code> <code>LayoutElement | None</code> <p>The target of the arc</p> <code>None</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the arc</p> <code>None</code> <code>arrowhead_fill</code> <code>NoneValueType | Color | None</code> <p>The arrowhead fill color of the arc</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>arrowhead_filter</code> <code>NoneValueType | Filter | None</code> <p>The arrowhead filter of the arc</p> <code>None</code> <code>arrowhead_stroke</code> <code>NoneValueType | Color | None</code> <p>The arrowhead stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>arrowhead_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The arrowhead stroke dasharray of the arc</p> <code>None</code> <code>arrowhead_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The arrowhead stroke dashoffset of the arc</p> <code>None</code> <code>arrowhead_stroke_width</code> <code>float | None</code> <p>The arrowhead stroke width of the arc</p> <code>1.0</code> <code>arrowhead_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The arrowhead transform of the arc</p> <code>None</code> <code>arrowhead_height</code> <code>float</code> <p>The height of the arrowhead.</p> <code>10.0</code> <code>arrowhead_width</code> <code>float</code> <p>The width of the arrowhead.</p> <code>10.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>arrowhead_base</code> <p>Return the arrowhead base anchor point of the single-headed arc.</p> <code>arrowhead_bbox</code> <p>Return the bounding box of the single-headed arc arrowhead.</p> <code>arrowhead_border</code> <p>Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.</p> <code>arrowhead_drawing_elements</code> <p>Return the drawing elements of the single-headed arc arrowhead.</p> <code>arrowhead_length</code> <p>Return the length of the single-headed arc arrowhead.</p> <code>arrowhead_tip</code> <p>Return the arrowhead tip anchor point of the single-headed arc.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>childless</code> <p>Return a copy of the arc with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>end_point</code> <p>Return the ending point of the arc.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>fraction</code> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <code>length</code> <p>Return the total length of the arc path.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_children</code> <p>Return the self children of the arc.</p> <code>own_drawing_elements</code> <p>Return the arc's own drawing elements, including those of its mixins.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>path_drawing_elements</code> <p>Return the drawing elements of the single-headed arc path.</p> <code>points</code> <p>Return the points of the arc path.</p> <code>start_point</code> <p>Return the starting point of the arc.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhysicalStimulationLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhysicalStimulationLayout.arrowhead_base","title":"arrowhead_base","text":"<pre><code>arrowhead_base() -&gt; Point\n</code></pre> <p>Return the arrowhead base anchor point of the single-headed arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_base(self) -&gt; Point:\n    \"\"\"Return the arrowhead base anchor point of the single-headed arc.\"\"\"\n    arrowhead_length = self.arrowhead_length()\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return self.arrowhead_tip() - (arrowhead_length, 0)\n    fraction = 1 - (arrowhead_length + self.end_shorten) / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhysicalStimulationLayout.arrowhead_bbox","title":"arrowhead_bbox","text":"<pre><code>arrowhead_bbox() -&gt; Bbox\n</code></pre> <p>Return the bounding box of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_bbox(self) -&gt; Bbox:\n    \"\"\"Return the bounding box of the single-headed arc arrowhead.\"\"\"\n    return get_drawing_elements_bbox(self.arrowhead_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhysicalStimulationLayout.arrowhead_border","title":"arrowhead_border","text":"<pre><code>arrowhead_border(point: Point) -&gt; Point\n</code></pre> <p>Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_border(self, point: Point) -&gt; Point:\n    \"\"\"Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    point = get_drawing_elements_border(self.arrowhead_drawing_elements(), point)\n    if point is None:\n        return self.arrowhead_tip()\n    return point\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhysicalStimulationLayout.arrowhead_drawing_elements","title":"arrowhead_drawing_elements","text":"<pre><code>arrowhead_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_drawing_elements(\n    self,\n) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the single-headed arc arrowhead.\"\"\"\n    drawing_elements = self._arrowhead_border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_arrowhead\",\n        elements=tuple(drawing_elements),\n        fill=self.arrowhead_fill,\n        filter_=self.arrowhead_filter,\n        id_=f\"{self.id_}_arrowhead\",\n        stroke=self.arrowhead_stroke,\n        stroke_dasharray=self.arrowhead_stroke_dasharray,\n        stroke_dashoffset=self.arrowhead_stroke_dashoffset,\n        stroke_width=self.arrowhead_stroke_width,\n        transform=self.arrowhead_transform,\n    )\n    transformation = self._get_arrowhead_transformation()\n    group = group.transformed(transformation)\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhysicalStimulationLayout.arrowhead_length","title":"arrowhead_length","text":"<pre><code>arrowhead_length() -&gt; float\n</code></pre> <p>Return the length of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_length(self) -&gt; float:\n    \"\"\"Return the length of the single-headed arc arrowhead.\"\"\"\n    bbox = get_drawing_elements_bbox(self._arrowhead_border_drawing_elements())\n    if math.isnan(bbox.width):\n        return 0.0\n    return bbox.east().x\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhysicalStimulationLayout.arrowhead_tip","title":"arrowhead_tip","text":"<pre><code>arrowhead_tip() -&gt; Point\n</code></pre> <p>Return the arrowhead tip anchor point of the single-headed arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_tip(self) -&gt; Point:\n    \"\"\"Return the arrowhead tip anchor point of the single-headed arc.\"\"\"\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return segment.p2\n    fraction = 1 - self.end_shorten / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhysicalStimulationLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhysicalStimulationLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the arc with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the arc with no children.\"\"\"\n    return dataclasses.replace(self, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhysicalStimulationLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhysicalStimulationLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhysicalStimulationLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhysicalStimulationLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhysicalStimulationLayout.end_point","title":"end_point","text":"<pre><code>end_point() -&gt; Point\n</code></pre> <p>Return the ending point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_point(self) -&gt; Point:\n    \"\"\"Return the ending point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[-1]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhysicalStimulationLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhysicalStimulationLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhysicalStimulationLayout.fraction","title":"fraction","text":"<pre><code>fraction(fraction: float) -&gt; tuple[Point, float]\n</code></pre> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def fraction(self, fraction: float) -&gt; tuple[Point, float]:\n    \"\"\"Return the position and angle on the arc at a given fraction (of the total arc length).\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    current_length = 0\n    length_to_reach = fraction * self.length()\n    for segment in self.segments:\n        current_length += segment.length()\n        if current_length &gt;= length_to_reach:\n            break\n    segment_start_length = current_length - segment.length()\n    segment_fraction = (length_to_reach - segment_start_length) / segment.length()\n    position, angle = segment.get_position_and_angle_at_fraction(segment_fraction)\n    return position, angle\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhysicalStimulationLayout.length","title":"length","text":"<pre><code>length() -&gt; float\n</code></pre> <p>Return the total length of the arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def length(self) -&gt; float:\n    \"\"\"Return the total length of the arc path.\"\"\"\n    return sum([segment.length() for segment in self.segments])\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhysicalStimulationLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhysicalStimulationLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the arc.\"\"\"\n    return []\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhysicalStimulationLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the arc's own drawing elements, including those of its mixins.</p> Source code in <code>src/momapy/celldesigner/elements.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the arc's own drawing elements, including those of its mixins.\"\"\"\n    drawing_elements = super_or_builder(\n        CellDesignerSingleHeadedArc, self\n    ).own_drawing_elements()\n    done_bases = []\n    for base in type(self).__mro__:\n        if (\n            issubclass_or_builder(base, _SBGNMixin)\n            and base is not type(self)\n            and not any([issubclass(done_base, base) for done_base in done_bases])\n        ):\n            drawing_elements += getattr(base, \"_mixin_drawing_elements\")(self)\n            done_bases.append(base)\n    return drawing_elements\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhysicalStimulationLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhysicalStimulationLayout.path_drawing_elements","title":"path_drawing_elements","text":"<pre><code>path_drawing_elements() -&gt; list[Path]\n</code></pre> <p>Return the drawing elements of the single-headed arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def path_drawing_elements(self) -&gt; list[Path]:\n    \"\"\"Return the drawing elements of the single-headed arc path.\"\"\"\n    arrowhead_length = self.arrowhead_length()\n    if len(self.segments) == 1:\n        segment = (\n            self.segments[0]\n            .shortened(self.start_shorten, \"start\")\n            .shortened(self.end_shorten + arrowhead_length, \"end\")\n        )\n        actions = [\n            MoveTo(segment.p1),\n            self._make_path_action_from_segment(segment),\n        ]\n    else:\n        first_segment = self.segments[0].shortened(self.start_shorten, \"start\")\n        last_segment = self.segments[-1].shortened(\n            self.end_shorten + arrowhead_length, \"end\"\n        )\n        actions = [\n            MoveTo(first_segment.p1),\n            self._make_path_action_from_segment(first_segment),\n        ]\n        for segment in self.segments[1:-1]:\n            action = self._make_path_action_from_segment(segment)\n            actions.append(action)\n        actions.append(self._make_path_action_from_segment(last_segment))\n    path = Path(\n        actions=tuple(actions),\n        class_=f\"{type(self).__name__}_path\",\n        fill=self.path_fill,\n        filter_=self.path_filter,\n        id_=f\"{self.id_}_path\",\n        stroke=self.path_stroke,\n        stroke_dasharray=self.path_stroke_dasharray,\n        stroke_dashoffset=self.path_stroke_dashoffset,\n        stroke_width=self.path_stroke_width,\n        transform=self.path_transform,\n    )\n    return [path]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhysicalStimulationLayout.points","title":"points","text":"<pre><code>points() -&gt; list[Point]\n</code></pre> <p>Return the points of the arc path.</p> <p>An arc with no segments has no points and returns an empty list.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def points(self) -&gt; list[Point]:\n    \"\"\"Return the points of the arc path.\n\n    An arc with no segments has no points and returns an empty list.\n    \"\"\"\n    if not self.segments:\n        return []\n    points = []\n    for segment in self.segments:\n        points.append(segment.p1)\n    points.append(segment.p2)\n    return points\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhysicalStimulationLayout.start_point","title":"start_point","text":"<pre><code>start_point() -&gt; Point\n</code></pre> <p>Return the starting point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_point(self) -&gt; Point:\n    \"\"\"Return the starting point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[0]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhysicalStimulationLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhysicalStimulator","title":"PhysicalStimulator  <code>dataclass</code>","text":"<pre><code>PhysicalStimulator(*, id_: str = make_uuid4_as_str(), name: str | None = None, sbo_term: str | None = None, metaid: str | None = None, referred_element: Species | BooleanLogicGate)\n</code></pre> <p>               Bases: <code>Modulator</code></p> <p>Physical stimulator.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'ffaaa484-9db7-4806-9430-4e934cbd129c'</code> <code>name</code> <code>str | None</code> <p>Human-readable name of the element.</p> <code>None</code> <code>sbo_term</code> <code>str | None</code> <p>Optional SBO term identifier for semantic annotation.</p> <code>None</code> <code>metaid</code> <code>str | None</code> <p>Optional metadata identifier for RDF annotations.</p> <code>None</code> <code>referred_element</code> <code>Species | BooleanLogicGate</code> <p>The species or boolean logic gate the modifier refers to</p> required <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PhysicalStimulator.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PositiveInfluence","title":"PositiveInfluence  <code>dataclass</code>","text":"<pre><code>PositiveInfluence(*, id_: str = make_uuid4_as_str(), source: Species | BooleanLogicGate, target: Species | None)\n</code></pre> <p>               Bases: <code>Modulation</code></p> <p>Positive influence.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'ff845811-4cc8-40f0-9d53-dbb2e6b01c34'</code> <code>source</code> <code>Species | BooleanLogicGate</code> <p>The source of the modulation</p> required <code>target</code> <code>Species | None</code> <p>The target of the modulation</p> required <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PositiveInfluence.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PositiveInfluenceLayout","title":"PositiveInfluenceLayout  <code>dataclass</code>","text":"<pre><code>PositiveInfluenceLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, end_shorten: float = 0.0, fill: NoneValueType | Color | None = None, filter_: NoneValueType | Filter | None = None, path_fill: NoneValueType | Color | None = NoneValue, path_filter: NoneValueType | Filter | None = None, path_stroke: NoneValueType | Color | None = black, path_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, path_stroke_dashoffset: NoneValueType | float | None = None, path_stroke_width: float | None = 1.0, path_transform: NoneValueType | tuple[Transformation, ...] | None = None, stroke: NoneValueType | Color | None = None, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: NoneValueType | float | None = None, segments: tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...] = tuple(), source: LayoutElement | None = None, start_shorten: float = 0.0, target: LayoutElement | None = None, transform: NoneValueType | tuple[Transformation, ...] | None = None, arrowhead_fill: NoneValueType | Color | None = NoneValue, arrowhead_filter: NoneValueType | Filter | None = None, arrowhead_stroke: NoneValueType | Color | None = black, arrowhead_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, arrowhead_stroke_dashoffset: NoneValueType | float | None = None, arrowhead_stroke_width: float | None = 2.0, arrowhead_transform: NoneValueType | tuple[Transformation, ...] | None = None, arrowhead_height: float = 10.0, arrowhead_width: float = 10.0)\n</code></pre> <p>               Bases: <code>CellDesignerSingleHeadedArc</code></p> <p>Positive influence layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'36999f41-9225-4e31-b2dc-80246d3565d2'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>end_shorten</code> <code>float</code> <p>The length the end of the arc will be shorten by</p> <code>0.0</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the arc</p> <code>None</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the arc</p> <code>None</code> <code>path_fill</code> <code>NoneValueType | Color | None</code> <p>The path fill color of the arc</p> <code>&lt;momapy.drawing.NoneValueType object at 0x7f8b17323e00&gt;</code> <code>path_filter</code> <code>NoneValueType | Filter | None</code> <p>The path filter of the arc</p> <code>None</code> <code>path_stroke</code> <code>NoneValueType | Color | None</code> <p>The path stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>path_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The path stroke dasharray of the arc</p> <code>None</code> <code>path_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The path stroke dashoffset of the arc</p> <code>None</code> <code>path_stroke_width</code> <code>float | None</code> <p>The path stroke width of the arc</p> <code>1.0</code> <code>path_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The path transform of the arc</p> <code>None</code> <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the arc</p> <code>None</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the arc</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the arc</p> <code>None</code> <code>stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the arc</p> <code>None</code> <code>segments</code> <code>tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...]</code> <p>The path segments of the arc</p> <code>&lt;dynamic&gt;</code> <code>source</code> <code>LayoutElement | None</code> <p>The source of the arc</p> <code>None</code> <code>start_shorten</code> <code>float</code> <p>The length the start of the arc will be shorten by</p> <code>0.0</code> <code>target</code> <code>LayoutElement | None</code> <p>The target of the arc</p> <code>None</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the arc</p> <code>None</code> <code>arrowhead_fill</code> <code>NoneValueType | Color | None</code> <p>The arrowhead fill color of the arc</p> <code>&lt;momapy.drawing.NoneValueType object at 0x7f8b17323e00&gt;</code> <code>arrowhead_filter</code> <code>NoneValueType | Filter | None</code> <p>The arrowhead filter of the arc</p> <code>None</code> <code>arrowhead_stroke</code> <code>NoneValueType | Color | None</code> <p>The arrowhead stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>arrowhead_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The arrowhead stroke dasharray of the arc</p> <code>None</code> <code>arrowhead_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The arrowhead stroke dashoffset of the arc</p> <code>None</code> <code>arrowhead_stroke_width</code> <code>float | None</code> <p>The arrowhead stroke width of the arc</p> <code>2.0</code> <code>arrowhead_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The arrowhead transform of the arc</p> <code>None</code> <code>arrowhead_height</code> <code>float</code> <p>The height of the arrowhead.</p> <code>10.0</code> <code>arrowhead_width</code> <code>float</code> <p>The width of the arrowhead.</p> <code>10.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>arrowhead_base</code> <p>Return the arrowhead base anchor point of the single-headed arc.</p> <code>arrowhead_bbox</code> <p>Return the bounding box of the single-headed arc arrowhead.</p> <code>arrowhead_border</code> <p>Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.</p> <code>arrowhead_drawing_elements</code> <p>Return the drawing elements of the single-headed arc arrowhead.</p> <code>arrowhead_length</code> <p>Return the length of the single-headed arc arrowhead.</p> <code>arrowhead_tip</code> <p>Return the arrowhead tip anchor point of the single-headed arc.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>childless</code> <p>Return a copy of the arc with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>end_point</code> <p>Return the ending point of the arc.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>fraction</code> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <code>length</code> <p>Return the total length of the arc path.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_children</code> <p>Return the self children of the arc.</p> <code>own_drawing_elements</code> <p>Return the arc's own drawing elements, including those of its mixins.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>path_drawing_elements</code> <p>Return the drawing elements of the single-headed arc path.</p> <code>points</code> <p>Return the points of the arc path.</p> <code>start_point</code> <p>Return the starting point of the arc.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PositiveInfluenceLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PositiveInfluenceLayout.arrowhead_base","title":"arrowhead_base","text":"<pre><code>arrowhead_base() -&gt; Point\n</code></pre> <p>Return the arrowhead base anchor point of the single-headed arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_base(self) -&gt; Point:\n    \"\"\"Return the arrowhead base anchor point of the single-headed arc.\"\"\"\n    arrowhead_length = self.arrowhead_length()\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return self.arrowhead_tip() - (arrowhead_length, 0)\n    fraction = 1 - (arrowhead_length + self.end_shorten) / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PositiveInfluenceLayout.arrowhead_bbox","title":"arrowhead_bbox","text":"<pre><code>arrowhead_bbox() -&gt; Bbox\n</code></pre> <p>Return the bounding box of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_bbox(self) -&gt; Bbox:\n    \"\"\"Return the bounding box of the single-headed arc arrowhead.\"\"\"\n    return get_drawing_elements_bbox(self.arrowhead_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PositiveInfluenceLayout.arrowhead_border","title":"arrowhead_border","text":"<pre><code>arrowhead_border(point: Point) -&gt; Point\n</code></pre> <p>Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_border(self, point: Point) -&gt; Point:\n    \"\"\"Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    point = get_drawing_elements_border(self.arrowhead_drawing_elements(), point)\n    if point is None:\n        return self.arrowhead_tip()\n    return point\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PositiveInfluenceLayout.arrowhead_drawing_elements","title":"arrowhead_drawing_elements","text":"<pre><code>arrowhead_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_drawing_elements(\n    self,\n) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the single-headed arc arrowhead.\"\"\"\n    drawing_elements = self._arrowhead_border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_arrowhead\",\n        elements=tuple(drawing_elements),\n        fill=self.arrowhead_fill,\n        filter_=self.arrowhead_filter,\n        id_=f\"{self.id_}_arrowhead\",\n        stroke=self.arrowhead_stroke,\n        stroke_dasharray=self.arrowhead_stroke_dasharray,\n        stroke_dashoffset=self.arrowhead_stroke_dashoffset,\n        stroke_width=self.arrowhead_stroke_width,\n        transform=self.arrowhead_transform,\n    )\n    transformation = self._get_arrowhead_transformation()\n    group = group.transformed(transformation)\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PositiveInfluenceLayout.arrowhead_length","title":"arrowhead_length","text":"<pre><code>arrowhead_length() -&gt; float\n</code></pre> <p>Return the length of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_length(self) -&gt; float:\n    \"\"\"Return the length of the single-headed arc arrowhead.\"\"\"\n    bbox = get_drawing_elements_bbox(self._arrowhead_border_drawing_elements())\n    if math.isnan(bbox.width):\n        return 0.0\n    return bbox.east().x\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PositiveInfluenceLayout.arrowhead_tip","title":"arrowhead_tip","text":"<pre><code>arrowhead_tip() -&gt; Point\n</code></pre> <p>Return the arrowhead tip anchor point of the single-headed arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_tip(self) -&gt; Point:\n    \"\"\"Return the arrowhead tip anchor point of the single-headed arc.\"\"\"\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return segment.p2\n    fraction = 1 - self.end_shorten / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PositiveInfluenceLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PositiveInfluenceLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the arc with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the arc with no children.\"\"\"\n    return dataclasses.replace(self, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PositiveInfluenceLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PositiveInfluenceLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PositiveInfluenceLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PositiveInfluenceLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PositiveInfluenceLayout.end_point","title":"end_point","text":"<pre><code>end_point() -&gt; Point\n</code></pre> <p>Return the ending point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_point(self) -&gt; Point:\n    \"\"\"Return the ending point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[-1]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PositiveInfluenceLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PositiveInfluenceLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PositiveInfluenceLayout.fraction","title":"fraction","text":"<pre><code>fraction(fraction: float) -&gt; tuple[Point, float]\n</code></pre> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def fraction(self, fraction: float) -&gt; tuple[Point, float]:\n    \"\"\"Return the position and angle on the arc at a given fraction (of the total arc length).\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    current_length = 0\n    length_to_reach = fraction * self.length()\n    for segment in self.segments:\n        current_length += segment.length()\n        if current_length &gt;= length_to_reach:\n            break\n    segment_start_length = current_length - segment.length()\n    segment_fraction = (length_to_reach - segment_start_length) / segment.length()\n    position, angle = segment.get_position_and_angle_at_fraction(segment_fraction)\n    return position, angle\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PositiveInfluenceLayout.length","title":"length","text":"<pre><code>length() -&gt; float\n</code></pre> <p>Return the total length of the arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def length(self) -&gt; float:\n    \"\"\"Return the total length of the arc path.\"\"\"\n    return sum([segment.length() for segment in self.segments])\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PositiveInfluenceLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PositiveInfluenceLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the arc.\"\"\"\n    return []\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PositiveInfluenceLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the arc's own drawing elements, including those of its mixins.</p> Source code in <code>src/momapy/celldesigner/elements.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the arc's own drawing elements, including those of its mixins.\"\"\"\n    drawing_elements = super_or_builder(\n        CellDesignerSingleHeadedArc, self\n    ).own_drawing_elements()\n    done_bases = []\n    for base in type(self).__mro__:\n        if (\n            issubclass_or_builder(base, _SBGNMixin)\n            and base is not type(self)\n            and not any([issubclass(done_base, base) for done_base in done_bases])\n        ):\n            drawing_elements += getattr(base, \"_mixin_drawing_elements\")(self)\n            done_bases.append(base)\n    return drawing_elements\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PositiveInfluenceLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PositiveInfluenceLayout.path_drawing_elements","title":"path_drawing_elements","text":"<pre><code>path_drawing_elements() -&gt; list[Path]\n</code></pre> <p>Return the drawing elements of the single-headed arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def path_drawing_elements(self) -&gt; list[Path]:\n    \"\"\"Return the drawing elements of the single-headed arc path.\"\"\"\n    arrowhead_length = self.arrowhead_length()\n    if len(self.segments) == 1:\n        segment = (\n            self.segments[0]\n            .shortened(self.start_shorten, \"start\")\n            .shortened(self.end_shorten + arrowhead_length, \"end\")\n        )\n        actions = [\n            MoveTo(segment.p1),\n            self._make_path_action_from_segment(segment),\n        ]\n    else:\n        first_segment = self.segments[0].shortened(self.start_shorten, \"start\")\n        last_segment = self.segments[-1].shortened(\n            self.end_shorten + arrowhead_length, \"end\"\n        )\n        actions = [\n            MoveTo(first_segment.p1),\n            self._make_path_action_from_segment(first_segment),\n        ]\n        for segment in self.segments[1:-1]:\n            action = self._make_path_action_from_segment(segment)\n            actions.append(action)\n        actions.append(self._make_path_action_from_segment(last_segment))\n    path = Path(\n        actions=tuple(actions),\n        class_=f\"{type(self).__name__}_path\",\n        fill=self.path_fill,\n        filter_=self.path_filter,\n        id_=f\"{self.id_}_path\",\n        stroke=self.path_stroke,\n        stroke_dasharray=self.path_stroke_dasharray,\n        stroke_dashoffset=self.path_stroke_dashoffset,\n        stroke_width=self.path_stroke_width,\n        transform=self.path_transform,\n    )\n    return [path]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PositiveInfluenceLayout.points","title":"points","text":"<pre><code>points() -&gt; list[Point]\n</code></pre> <p>Return the points of the arc path.</p> <p>An arc with no segments has no points and returns an empty list.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def points(self) -&gt; list[Point]:\n    \"\"\"Return the points of the arc path.\n\n    An arc with no segments has no points and returns an empty list.\n    \"\"\"\n    if not self.segments:\n        return []\n    points = []\n    for segment in self.segments:\n        points.append(segment.p1)\n    points.append(segment.p2)\n    return points\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PositiveInfluenceLayout.start_point","title":"start_point","text":"<pre><code>start_point() -&gt; Point\n</code></pre> <p>Return the starting point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_point(self) -&gt; Point:\n    \"\"\"Return the starting point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[0]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.PositiveInfluenceLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.Product","title":"Product  <code>dataclass</code>","text":"<pre><code>Product(*, id_: str = make_uuid4_as_str(), name: str | None = None, sbo_term: str | None = None, metaid: str | None = None, referred_element: Species, stoichiometry: float | None = None, base: bool = False)\n</code></pre> <p>               Bases: <code>SpeciesReference</code>, <code>CellDesignerModelElement</code></p> <p>Product.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'6db1ce61-1025-4af9-92a4-a2e35fcf4968'</code> <code>name</code> <code>str | None</code> <p>Human-readable name of the element.</p> <code>None</code> <code>sbo_term</code> <code>str | None</code> <p>Optional SBO term identifier for semantic annotation.</p> <code>None</code> <code>metaid</code> <code>str | None</code> <p>Optional metadata identifier for RDF annotations.</p> <code>None</code> <code>referred_element</code> <code>Species</code> <p>The species being referenced.</p> required <code>stoichiometry</code> <code>float | None</code> <p>Optional stoichiometric coefficient.</p> <code>None</code> <code>base</code> <code>bool</code> <p>Whether the product is a base product or not</p> <code>False</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.Product.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ProductionLayout","title":"ProductionLayout  <code>dataclass</code>","text":"<pre><code>ProductionLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, end_shorten: float = 2.0, fill: NoneValueType | Color | None = None, filter_: NoneValueType | Filter | None = None, path_fill: NoneValueType | Color | None = NoneValue, path_filter: NoneValueType | Filter | None = None, path_stroke: NoneValueType | Color | None = black, path_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, path_stroke_dashoffset: NoneValueType | float | None = None, path_stroke_width: float | None = 1.0, path_transform: NoneValueType | tuple[Transformation, ...] | None = None, stroke: NoneValueType | Color | None = None, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: NoneValueType | float | None = None, segments: tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...] = tuple(), source: LayoutElement | None = None, start_shorten: float = 0.0, target: LayoutElement | None = None, transform: NoneValueType | tuple[Transformation, ...] | None = None, arrowhead_fill: NoneValueType | Color | None = black, arrowhead_filter: NoneValueType | Filter | None = None, arrowhead_stroke: NoneValueType | Color | None = black, arrowhead_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, arrowhead_stroke_dashoffset: NoneValueType | float | None = None, arrowhead_stroke_width: float | None = 1.0, arrowhead_transform: NoneValueType | tuple[Transformation, ...] | None = None, arrowhead_height: float = 8.0, arrowhead_width: float = 15.0)\n</code></pre> <p>               Bases: <code>CellDesignerSingleHeadedArc</code></p> <p>Production layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'9e79240a-7cc1-4e5a-b99e-ab40ab669007'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>end_shorten</code> <code>float</code> <p>The length the end of the arc will be shorten by</p> <code>2.0</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the arc</p> <code>None</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the arc</p> <code>None</code> <code>path_fill</code> <code>NoneValueType | Color | None</code> <p>The path fill color of the arc</p> <code>&lt;momapy.drawing.NoneValueType object at 0x7f8b17323e00&gt;</code> <code>path_filter</code> <code>NoneValueType | Filter | None</code> <p>The path filter of the arc</p> <code>None</code> <code>path_stroke</code> <code>NoneValueType | Color | None</code> <p>The path stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>path_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The path stroke dasharray of the arc</p> <code>None</code> <code>path_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The path stroke dashoffset of the arc</p> <code>None</code> <code>path_stroke_width</code> <code>float | None</code> <p>The path stroke width of the arc</p> <code>1.0</code> <code>path_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The path transform of the arc</p> <code>None</code> <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the arc</p> <code>None</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the arc</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the arc</p> <code>None</code> <code>stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the arc</p> <code>None</code> <code>segments</code> <code>tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...]</code> <p>The path segments of the arc</p> <code>&lt;dynamic&gt;</code> <code>source</code> <code>LayoutElement | None</code> <p>The source of the arc</p> <code>None</code> <code>start_shorten</code> <code>float</code> <p>The length the start of the arc will be shorten by</p> <code>0.0</code> <code>target</code> <code>LayoutElement | None</code> <p>The target of the arc</p> <code>None</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the arc</p> <code>None</code> <code>arrowhead_fill</code> <code>NoneValueType | Color | None</code> <p>The arrowhead fill color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>arrowhead_filter</code> <code>NoneValueType | Filter | None</code> <p>The arrowhead filter of the arc</p> <code>None</code> <code>arrowhead_stroke</code> <code>NoneValueType | Color | None</code> <p>The arrowhead stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>arrowhead_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The arrowhead stroke dasharray of the arc</p> <code>None</code> <code>arrowhead_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The arrowhead stroke dashoffset of the arc</p> <code>None</code> <code>arrowhead_stroke_width</code> <code>float | None</code> <p>The arrowhead stroke width of the arc</p> <code>1.0</code> <code>arrowhead_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The arrowhead transform of the arc</p> <code>None</code> <code>arrowhead_height</code> <code>float</code> <p>The height of the arrowhead.</p> <code>8.0</code> <code>arrowhead_width</code> <code>float</code> <p>The width of the arrowhead.</p> <code>15.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>arrowhead_base</code> <p>Return the arrowhead base anchor point of the single-headed arc.</p> <code>arrowhead_bbox</code> <p>Return the bounding box of the single-headed arc arrowhead.</p> <code>arrowhead_border</code> <p>Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.</p> <code>arrowhead_drawing_elements</code> <p>Return the drawing elements of the single-headed arc arrowhead.</p> <code>arrowhead_length</code> <p>Return the length of the single-headed arc arrowhead.</p> <code>arrowhead_tip</code> <p>Return the arrowhead tip anchor point of the single-headed arc.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>childless</code> <p>Return a copy of the arc with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>end_point</code> <p>Return the ending point of the arc.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>fraction</code> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <code>length</code> <p>Return the total length of the arc path.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_children</code> <p>Return the self children of the arc.</p> <code>own_drawing_elements</code> <p>Return the arc's own drawing elements, including those of its mixins.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>path_drawing_elements</code> <p>Return the drawing elements of the single-headed arc path.</p> <code>points</code> <p>Return the points of the arc path.</p> <code>start_point</code> <p>Return the starting point of the arc.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ProductionLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ProductionLayout.arrowhead_base","title":"arrowhead_base","text":"<pre><code>arrowhead_base() -&gt; Point\n</code></pre> <p>Return the arrowhead base anchor point of the single-headed arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_base(self) -&gt; Point:\n    \"\"\"Return the arrowhead base anchor point of the single-headed arc.\"\"\"\n    arrowhead_length = self.arrowhead_length()\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return self.arrowhead_tip() - (arrowhead_length, 0)\n    fraction = 1 - (arrowhead_length + self.end_shorten) / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ProductionLayout.arrowhead_bbox","title":"arrowhead_bbox","text":"<pre><code>arrowhead_bbox() -&gt; Bbox\n</code></pre> <p>Return the bounding box of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_bbox(self) -&gt; Bbox:\n    \"\"\"Return the bounding box of the single-headed arc arrowhead.\"\"\"\n    return get_drawing_elements_bbox(self.arrowhead_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ProductionLayout.arrowhead_border","title":"arrowhead_border","text":"<pre><code>arrowhead_border(point: Point) -&gt; Point\n</code></pre> <p>Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_border(self, point: Point) -&gt; Point:\n    \"\"\"Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    point = get_drawing_elements_border(self.arrowhead_drawing_elements(), point)\n    if point is None:\n        return self.arrowhead_tip()\n    return point\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ProductionLayout.arrowhead_drawing_elements","title":"arrowhead_drawing_elements","text":"<pre><code>arrowhead_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_drawing_elements(\n    self,\n) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the single-headed arc arrowhead.\"\"\"\n    drawing_elements = self._arrowhead_border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_arrowhead\",\n        elements=tuple(drawing_elements),\n        fill=self.arrowhead_fill,\n        filter_=self.arrowhead_filter,\n        id_=f\"{self.id_}_arrowhead\",\n        stroke=self.arrowhead_stroke,\n        stroke_dasharray=self.arrowhead_stroke_dasharray,\n        stroke_dashoffset=self.arrowhead_stroke_dashoffset,\n        stroke_width=self.arrowhead_stroke_width,\n        transform=self.arrowhead_transform,\n    )\n    transformation = self._get_arrowhead_transformation()\n    group = group.transformed(transformation)\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ProductionLayout.arrowhead_length","title":"arrowhead_length","text":"<pre><code>arrowhead_length() -&gt; float\n</code></pre> <p>Return the length of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_length(self) -&gt; float:\n    \"\"\"Return the length of the single-headed arc arrowhead.\"\"\"\n    bbox = get_drawing_elements_bbox(self._arrowhead_border_drawing_elements())\n    if math.isnan(bbox.width):\n        return 0.0\n    return bbox.east().x\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ProductionLayout.arrowhead_tip","title":"arrowhead_tip","text":"<pre><code>arrowhead_tip() -&gt; Point\n</code></pre> <p>Return the arrowhead tip anchor point of the single-headed arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_tip(self) -&gt; Point:\n    \"\"\"Return the arrowhead tip anchor point of the single-headed arc.\"\"\"\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return segment.p2\n    fraction = 1 - self.end_shorten / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ProductionLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ProductionLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the arc with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the arc with no children.\"\"\"\n    return dataclasses.replace(self, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ProductionLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ProductionLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ProductionLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ProductionLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ProductionLayout.end_point","title":"end_point","text":"<pre><code>end_point() -&gt; Point\n</code></pre> <p>Return the ending point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_point(self) -&gt; Point:\n    \"\"\"Return the ending point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[-1]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ProductionLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ProductionLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ProductionLayout.fraction","title":"fraction","text":"<pre><code>fraction(fraction: float) -&gt; tuple[Point, float]\n</code></pre> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def fraction(self, fraction: float) -&gt; tuple[Point, float]:\n    \"\"\"Return the position and angle on the arc at a given fraction (of the total arc length).\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    current_length = 0\n    length_to_reach = fraction * self.length()\n    for segment in self.segments:\n        current_length += segment.length()\n        if current_length &gt;= length_to_reach:\n            break\n    segment_start_length = current_length - segment.length()\n    segment_fraction = (length_to_reach - segment_start_length) / segment.length()\n    position, angle = segment.get_position_and_angle_at_fraction(segment_fraction)\n    return position, angle\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ProductionLayout.length","title":"length","text":"<pre><code>length() -&gt; float\n</code></pre> <p>Return the total length of the arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def length(self) -&gt; float:\n    \"\"\"Return the total length of the arc path.\"\"\"\n    return sum([segment.length() for segment in self.segments])\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ProductionLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ProductionLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the arc.\"\"\"\n    return []\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ProductionLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the arc's own drawing elements, including those of its mixins.</p> Source code in <code>src/momapy/celldesigner/elements.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the arc's own drawing elements, including those of its mixins.\"\"\"\n    drawing_elements = super_or_builder(\n        CellDesignerSingleHeadedArc, self\n    ).own_drawing_elements()\n    done_bases = []\n    for base in type(self).__mro__:\n        if (\n            issubclass_or_builder(base, _SBGNMixin)\n            and base is not type(self)\n            and not any([issubclass(done_base, base) for done_base in done_bases])\n        ):\n            drawing_elements += getattr(base, \"_mixin_drawing_elements\")(self)\n            done_bases.append(base)\n    return drawing_elements\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ProductionLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ProductionLayout.path_drawing_elements","title":"path_drawing_elements","text":"<pre><code>path_drawing_elements() -&gt; list[Path]\n</code></pre> <p>Return the drawing elements of the single-headed arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def path_drawing_elements(self) -&gt; list[Path]:\n    \"\"\"Return the drawing elements of the single-headed arc path.\"\"\"\n    arrowhead_length = self.arrowhead_length()\n    if len(self.segments) == 1:\n        segment = (\n            self.segments[0]\n            .shortened(self.start_shorten, \"start\")\n            .shortened(self.end_shorten + arrowhead_length, \"end\")\n        )\n        actions = [\n            MoveTo(segment.p1),\n            self._make_path_action_from_segment(segment),\n        ]\n    else:\n        first_segment = self.segments[0].shortened(self.start_shorten, \"start\")\n        last_segment = self.segments[-1].shortened(\n            self.end_shorten + arrowhead_length, \"end\"\n        )\n        actions = [\n            MoveTo(first_segment.p1),\n            self._make_path_action_from_segment(first_segment),\n        ]\n        for segment in self.segments[1:-1]:\n            action = self._make_path_action_from_segment(segment)\n            actions.append(action)\n        actions.append(self._make_path_action_from_segment(last_segment))\n    path = Path(\n        actions=tuple(actions),\n        class_=f\"{type(self).__name__}_path\",\n        fill=self.path_fill,\n        filter_=self.path_filter,\n        id_=f\"{self.id_}_path\",\n        stroke=self.path_stroke,\n        stroke_dasharray=self.path_stroke_dasharray,\n        stroke_dashoffset=self.path_stroke_dashoffset,\n        stroke_width=self.path_stroke_width,\n        transform=self.path_transform,\n    )\n    return [path]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ProductionLayout.points","title":"points","text":"<pre><code>points() -&gt; list[Point]\n</code></pre> <p>Return the points of the arc path.</p> <p>An arc with no segments has no points and returns an empty list.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def points(self) -&gt; list[Point]:\n    \"\"\"Return the points of the arc path.\n\n    An arc with no segments has no points and returns an empty list.\n    \"\"\"\n    if not self.segments:\n        return []\n    points = []\n    for segment in self.segments:\n        points.append(segment.p1)\n    points.append(segment.p2)\n    return points\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ProductionLayout.start_point","title":"start_point","text":"<pre><code>start_point() -&gt; Point\n</code></pre> <p>Return the starting point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_point(self) -&gt; Point:\n    \"\"\"Return the starting point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[0]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ProductionLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.Protein","title":"Protein  <code>dataclass</code>","text":"<pre><code>Protein(*, id_: str = make_uuid4_as_str(), name: str | None = None, sbo_term: str | None = None, metaid: str | None = None, compartment: Compartment | None = None, hypothetical: bool = False, active: bool = False, homomultimer: int = 1, template: ProteinTemplate, modifications: frozenset[Modification] = frozenset(), structural_states: frozenset[StructuralState] = frozenset())\n</code></pre> <p>               Bases: <code>Species</code></p> <p>Abstract base class for proteins.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'ba533a51-06e8-49d3-854d-c5c2be085690'</code> <code>name</code> <code>str | None</code> <p>Human-readable name of the element.</p> <code>None</code> <code>sbo_term</code> <code>str | None</code> <p>Optional SBO term identifier for semantic annotation.</p> <code>None</code> <code>metaid</code> <code>str | None</code> <p>Optional metadata identifier for RDF annotations.</p> <code>None</code> <code>compartment</code> <code>Compartment | None</code> <p>The compartment containing this species.</p> <code>None</code> <code>hypothetical</code> <code>bool</code> <p>Whether the species is hypothetical or not</p> <code>False</code> <code>active</code> <code>bool</code> <p>Whether the species is active or not</p> <code>False</code> <code>homomultimer</code> <code>int</code> <p>The number of subunits forming the species</p> <code>1</code> <code>template</code> <code>ProteinTemplate</code> <p>The template of the species</p> required <code>modifications</code> <code>frozenset[Modification]</code> <p>The modifications of the protein</p> <code>&lt;dynamic&gt;</code> <code>structural_states</code> <code>frozenset[StructuralState]</code> <p>The structural states of the protein</p> <code>&lt;dynamic&gt;</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.Protein.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ProteinBindingDomain","title":"ProteinBindingDomain  <code>dataclass</code>","text":"<pre><code>ProteinBindingDomain(*, id_: str = make_uuid4_as_str(), name: str | None = None, active: bool = False)\n</code></pre> <p>               Bases: <code>Region</code></p> <p>Protein binding domain.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'2f0b7edb-5937-478d-8972-0d554ca080f9'</code> <code>name</code> <code>str | None</code> <p>The name of the region</p> <code>None</code> <code>active</code> <code>bool</code> <p>Whether the region is active or not</p> <code>False</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ProteinBindingDomain.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ProteinTemplate","title":"ProteinTemplate  <code>dataclass</code>","text":"<pre><code>ProteinTemplate(*, id_: str = make_uuid4_as_str(), name: str, modification_residues: frozenset[ModificationResidue] = frozenset())\n</code></pre> <p>               Bases: <code>SpeciesTemplate</code></p> <p>Abstract base class for protein templates.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'af4f1d93-ddb7-4bd1-acdb-b4be9a0296e3'</code> <code>name</code> <code>str</code> <p>The name of the species template</p> required <code>modification_residues</code> <code>frozenset[ModificationResidue]</code> <p>The modification residues of the protein template</p> <code>&lt;dynamic&gt;</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ProteinTemplate.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RNA","title":"RNA  <code>dataclass</code>","text":"<pre><code>RNA(*, id_: str = make_uuid4_as_str(), name: str | None = None, sbo_term: str | None = None, metaid: str | None = None, compartment: Compartment | None = None, hypothetical: bool = False, active: bool = False, homomultimer: int = 1, template: RNATemplate, modifications: frozenset[Modification] = frozenset())\n</code></pre> <p>               Bases: <code>Species</code></p> <p>RNA.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'f2cd73e3-9c71-4297-b3e5-78cd497e1d1f'</code> <code>name</code> <code>str | None</code> <p>Human-readable name of the element.</p> <code>None</code> <code>sbo_term</code> <code>str | None</code> <p>Optional SBO term identifier for semantic annotation.</p> <code>None</code> <code>metaid</code> <code>str | None</code> <p>Optional metadata identifier for RDF annotations.</p> <code>None</code> <code>compartment</code> <code>Compartment | None</code> <p>The compartment containing this species.</p> <code>None</code> <code>hypothetical</code> <code>bool</code> <p>Whether the species is hypothetical or not</p> <code>False</code> <code>active</code> <code>bool</code> <p>Whether the species is active or not</p> <code>False</code> <code>homomultimer</code> <code>int</code> <p>The number of subunits forming the species</p> <code>1</code> <code>template</code> <code>RNATemplate</code> <p>The template of the RNA</p> required <code>modifications</code> <code>frozenset[Modification]</code> <p>The modifications of the RNA</p> <code>&lt;dynamic&gt;</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RNA.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RNAActiveLayout","title":"RNAActiveLayout  <code>dataclass</code>","text":"<pre><code>RNAActiveLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = NoneValue, filter_: NoneValueType | Filter | None = None, height: float = 25.0 + DEFAULT_ACTIVE_YSEP * 2, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = (4, 2), stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = 1.0, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 90.0 + DEFAULT_ACTIVE_XSEP * 2, offset: float = 3.0, subunits_stroke: tuple[NoneValueType | Color, ...] | None = None, subunits_stroke_width: tuple[NoneValueType | float, ...] | None = None, subunits_stroke_dasharray: tuple[NoneValueType | tuple[float, ...], ...] | None = None, subunits_stroke_dashoffset: tuple[float, ...] | None = None, subunits_fill: tuple[NoneValueType | Color, ...] | None = None, subunits_transform: tuple[NoneValueType | tuple[Transformation, ...], ...] | None = None, subunits_filter: tuple[NoneValueType | Filter, ...] | None = None, n: int = 1, angle: float = 45.0)\n</code></pre> <p>               Bases: <code>_MultiNodeMixin</code>, <code>CellDesignerNode</code></p> <p>RNA active layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'e9b1f64a-d9a8-4d84-92d2-23f2280cc81c'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>&lt;momapy.drawing.NoneValueType object at 0x7f8b17323e00&gt;</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>33.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>(4, 2)</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The stroke width of the node</p> <code>1.0</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>98.0</code> <code>offset</code> <code>float</code> <p>Offset distance between stacked units.</p> <code>3.0</code> <code>subunits_stroke</code> <code>tuple[NoneValueType | Color, ...] | None</code> <p>Tuple of stroke colors for each subunit.</p> <code>None</code> <code>subunits_stroke_width</code> <code>tuple[NoneValueType | float, ...] | None</code> <p>Tuple of stroke widths for each subunit.</p> <code>None</code> <code>subunits_stroke_dasharray</code> <code>tuple[NoneValueType | tuple[float, ...], ...] | None</code> <p>Tuple of dash patterns for each subunit.</p> <code>None</code> <code>subunits_stroke_dashoffset</code> <code>tuple[float, ...] | None</code> <p>Tuple of dash offsets for each subunit.</p> <code>None</code> <code>subunits_fill</code> <code>tuple[NoneValueType | Color, ...] | None</code> <p>Tuple of fill colors for each subunit.</p> <code>None</code> <code>subunits_transform</code> <code>tuple[NoneValueType | tuple[Transformation, ...], ...] | None</code> <p>Tuple of transformations for each subunit.</p> <code>None</code> <code>subunits_filter</code> <code>tuple[NoneValueType | Filter, ...] | None</code> <p>Tuple of filters for each subunit.</p> <code>None</code> <code>n</code> <code>int</code> <p>Number of stacked node copies to draw.</p> <code>1</code> <code>angle</code> <code>float</code> <p>The slant angle of the parallelogram.</p> <code>45.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Return the east anchor of the node.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Get the center position for the node label.</p> <code>north</code> <p>Return the north anchor of the node.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Return the south anchor of the node.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Return the west anchor of the node.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>angle</code> <code>float</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RNAActiveLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RNAActiveLayout.angle","title":"angle  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>angle: float = dataclasses.field(default=45.0, metadata={'description': 'The slant angle of the parallelogram.'})\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RNAActiveLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RNAActiveLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RNAActiveLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RNAActiveLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RNAActiveLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RNAActiveLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RNAActiveLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RNAActiveLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RNAActiveLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Return the east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Return the east anchor of the node.\"\"\"\n    return self.own_angle(0)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RNAActiveLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RNAActiveLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RNAActiveLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RNAActiveLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RNAActiveLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Get the center position for the node label.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point where the label should be centered.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Get the center position for the node label.\n\n    Returns:\n        Point where the label should be centered.\n    \"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2 - (self._n - 1) * self.offset,\n        self.height / 2 - height / 2 - (self._n - 1) * self.offset,\n    )\n    return self._make_subunit_shape(\n        position=position, width=width, height=height\n    ).position\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RNAActiveLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Return the north anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Return the north anchor of the node.\"\"\"\n    return self.own_angle(90)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RNAActiveLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RNAActiveLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RNAActiveLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RNAActiveLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RNAActiveLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RNAActiveLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RNAActiveLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RNAActiveLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RNAActiveLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RNAActiveLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RNAActiveLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RNAActiveLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Return the south anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Return the south anchor of the node.\"\"\"\n    return self.own_angle(270)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RNAActiveLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RNAActiveLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RNAActiveLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RNAActiveLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RNAActiveLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RNAActiveLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Return the west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Return the west anchor of the node.\"\"\"\n    return self.own_angle(180)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RNAActiveLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RNAActiveLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RNAActiveLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RNAActiveLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RNALayout","title":"RNALayout  <code>dataclass</code>","text":"<pre><code>RNALayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = from_hex('#66FF66'), filter_: NoneValueType | Filter | None = None, height: float = 25.0, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = 1.0, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 90.0, offset: float = 3.0, subunits_stroke: tuple[NoneValueType | Color, ...] | None = None, subunits_stroke_width: tuple[NoneValueType | float, ...] | None = None, subunits_stroke_dasharray: tuple[NoneValueType | tuple[float, ...], ...] | None = None, subunits_stroke_dashoffset: tuple[float, ...] | None = None, subunits_fill: tuple[NoneValueType | Color, ...] | None = None, subunits_transform: tuple[NoneValueType | tuple[Transformation, ...], ...] | None = None, subunits_filter: tuple[NoneValueType | Filter, ...] | None = None, n: int = 1, angle: float = 45.0)\n</code></pre> <p>               Bases: <code>_MultiNodeMixin</code>, <code>CellDesignerNode</code></p> <p>RNA layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'a963add9-6e9f-41ad-9248-60ea306e4c03'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>Color(red=102, green=255, blue=102, alpha=1.0)</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>25.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The stroke width of the node</p> <code>1.0</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>90.0</code> <code>offset</code> <code>float</code> <p>Offset distance between stacked units.</p> <code>3.0</code> <code>subunits_stroke</code> <code>tuple[NoneValueType | Color, ...] | None</code> <p>Tuple of stroke colors for each subunit.</p> <code>None</code> <code>subunits_stroke_width</code> <code>tuple[NoneValueType | float, ...] | None</code> <p>Tuple of stroke widths for each subunit.</p> <code>None</code> <code>subunits_stroke_dasharray</code> <code>tuple[NoneValueType | tuple[float, ...], ...] | None</code> <p>Tuple of dash patterns for each subunit.</p> <code>None</code> <code>subunits_stroke_dashoffset</code> <code>tuple[float, ...] | None</code> <p>Tuple of dash offsets for each subunit.</p> <code>None</code> <code>subunits_fill</code> <code>tuple[NoneValueType | Color, ...] | None</code> <p>Tuple of fill colors for each subunit.</p> <code>None</code> <code>subunits_transform</code> <code>tuple[NoneValueType | tuple[Transformation, ...], ...] | None</code> <p>Tuple of transformations for each subunit.</p> <code>None</code> <code>subunits_filter</code> <code>tuple[NoneValueType | Filter, ...] | None</code> <p>Tuple of filters for each subunit.</p> <code>None</code> <code>n</code> <code>int</code> <p>Number of stacked node copies to draw.</p> <code>1</code> <code>angle</code> <code>float</code> <p>The slant angle of the parallelogram.</p> <code>45.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Return the east anchor point of the top subunit shape.</p> <code>east_north_east</code> <p>Return the east-north-east anchor point of the top subunit shape.</p> <code>east_south_east</code> <p>Return the east-south-east anchor point of the top subunit shape.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Get the center position for the node label.</p> <code>north</code> <p>Return the north anchor point of the top subunit shape.</p> <code>north_east</code> <p>Return the north-east anchor point of the top subunit shape.</p> <code>north_north_east</code> <p>Return the north-north-east anchor point of the top subunit shape.</p> <code>north_north_west</code> <p>Return the north-north-west anchor point of the top subunit shape.</p> <code>north_west</code> <p>Return the north-west anchor point of the top subunit shape.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Return the south anchor point of the top subunit shape.</p> <code>south_east</code> <p>Return the south-east anchor point of the top subunit shape.</p> <code>south_south_east</code> <p>Return the south-south-east anchor point of the top subunit shape.</p> <code>south_south_west</code> <p>Return the south-south-west anchor point of the top subunit shape.</p> <code>south_west</code> <p>Return the south-west anchor point of the top subunit shape.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Return the west anchor point of the top subunit shape.</p> <code>west_north_west</code> <p>Return the west-north-west anchor point of the top subunit shape.</p> <code>west_south_west</code> <p>Return the west-south-west anchor point of the top subunit shape.</p> <p>Attributes:</p> Name Type Description <code>angle</code> <code>float</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RNALayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RNALayout.angle","title":"angle  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>angle: float = dataclasses.field(default=45.0, metadata={'description': 'The slant angle of the parallelogram.'})\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RNALayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RNALayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RNALayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RNALayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RNALayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RNALayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RNALayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RNALayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RNALayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Return the east anchor point of the top subunit shape.</p> Source code in <code>src/momapy/celldesigner/layout.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Return the east anchor point of the top subunit shape.\"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2,\n        self.height / 2 - height / 2,\n    )\n    shape = self._make_subunit_shape(position, width, height)\n    return shape.joint3() / 2 + shape.joint2() / 2\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RNALayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east-north-east anchor point of the top subunit shape.</p> Source code in <code>src/momapy/celldesigner/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east-north-east anchor point of the top subunit shape.\"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2,\n        self.height / 2 - height / 2,\n    )\n    shape = self._make_subunit_shape(position, width, height)\n    return shape.joint3() * 0.25 + shape.joint2() * 0.75\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RNALayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east-south-east anchor point of the top subunit shape.</p> Source code in <code>src/momapy/celldesigner/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east-south-east anchor point of the top subunit shape.\"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2,\n        self.height / 2 - height / 2,\n    )\n    shape = self._make_subunit_shape(position, width, height)\n    return shape.joint3() * 0.75 + shape.joint2() * 0.25\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RNALayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RNALayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RNALayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Get the center position for the node label.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point where the label should be centered.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Get the center position for the node label.\n\n    Returns:\n        Point where the label should be centered.\n    \"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2 - (self._n - 1) * self.offset,\n        self.height / 2 - height / 2 - (self._n - 1) * self.offset,\n    )\n    return self._make_subunit_shape(\n        position=position, width=width, height=height\n    ).position\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RNALayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Return the north anchor point of the top subunit shape.</p> Source code in <code>src/momapy/celldesigner/layout.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Return the north anchor point of the top subunit shape.\"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2,\n        self.height / 2 - height / 2,\n    )\n    shape = self._make_subunit_shape(position, width, height)\n    return shape.joint1() / 2 + shape.joint2() / 2\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RNALayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north-east anchor point of the top subunit shape.</p> Source code in <code>src/momapy/celldesigner/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north-east anchor point of the top subunit shape.\"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2,\n        self.height / 2 - height / 2,\n    )\n    shape = self._make_subunit_shape(position, width, height)\n    return shape.joint2()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RNALayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north-north-east anchor point of the top subunit shape.</p> Source code in <code>src/momapy/celldesigner/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north-north-east anchor point of the top subunit shape.\"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2,\n        self.height / 2 - height / 2,\n    )\n    shape = self._make_subunit_shape(position, width, height)\n    return shape.joint1() * 0.25 + shape.joint2() * 0.75\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RNALayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north-north-west anchor point of the top subunit shape.</p> Source code in <code>src/momapy/celldesigner/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north-north-west anchor point of the top subunit shape.\"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2,\n        self.height / 2 - height / 2,\n    )\n    shape = self._make_subunit_shape(position, width, height)\n    return shape.joint1() * 0.75 + shape.joint2() * 0.25\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RNALayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north-west anchor point of the top subunit shape.</p> Source code in <code>src/momapy/celldesigner/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north-west anchor point of the top subunit shape.\"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2,\n        self.height / 2 - height / 2,\n    )\n    shape = self._make_subunit_shape(position, width, height)\n    return shape.joint1()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RNALayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RNALayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RNALayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RNALayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RNALayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RNALayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RNALayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RNALayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Return the south anchor point of the top subunit shape.</p> Source code in <code>src/momapy/celldesigner/layout.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Return the south anchor point of the top subunit shape.\"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2,\n        self.height / 2 - height / 2,\n    )\n    shape = self._make_subunit_shape(position, width, height)\n    return shape.joint4() / 2 + shape.joint3() / 2\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RNALayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south-east anchor point of the top subunit shape.</p> Source code in <code>src/momapy/celldesigner/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south-east anchor point of the top subunit shape.\"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2,\n        self.height / 2 - height / 2,\n    )\n    shape = self._make_subunit_shape(position, width, height)\n    return shape.joint3()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RNALayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south-south-east anchor point of the top subunit shape.</p> Source code in <code>src/momapy/celldesigner/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south-south-east anchor point of the top subunit shape.\"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2,\n        self.height / 2 - height / 2,\n    )\n    shape = self._make_subunit_shape(position, width, height)\n    return shape.joint4() * 0.25 + shape.joint3() * 0.75\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RNALayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south-south-west anchor point of the top subunit shape.</p> Source code in <code>src/momapy/celldesigner/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south-south-west anchor point of the top subunit shape.\"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2,\n        self.height / 2 - height / 2,\n    )\n    shape = self._make_subunit_shape(position, width, height)\n    return shape.joint4() * 0.75 + shape.joint3() * 0.25\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RNALayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south-west anchor point of the top subunit shape.</p> Source code in <code>src/momapy/celldesigner/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south-west anchor point of the top subunit shape.\"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2,\n        self.height / 2 - height / 2,\n    )\n    shape = self._make_subunit_shape(position, width, height)\n    return shape.joint4()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RNALayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RNALayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Return the west anchor point of the top subunit shape.</p> Source code in <code>src/momapy/celldesigner/layout.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Return the west anchor point of the top subunit shape.\"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2,\n        self.height / 2 - height / 2,\n    )\n    shape = self._make_subunit_shape(position, width, height)\n    return shape.joint4() / 2 + shape.joint1() / 2\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RNALayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west-north-west anchor point of the top subunit shape.</p> Source code in <code>src/momapy/celldesigner/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west-north-west anchor point of the top subunit shape.\"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2,\n        self.height / 2 - height / 2,\n    )\n    shape = self._make_subunit_shape(position, width, height)\n    return shape.joint4() * 0.25 + shape.joint1() * 0.75\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RNALayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west-south-west anchor point of the top subunit shape.</p> Source code in <code>src/momapy/celldesigner/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west-south-west anchor point of the top subunit shape.\"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2,\n        self.height / 2 - height / 2,\n    )\n    shape = self._make_subunit_shape(position, width, height)\n    return shape.joint4() * 0.75 + shape.joint1() * 0.25\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RNALayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RNALayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RNATemplate","title":"RNATemplate  <code>dataclass</code>","text":"<pre><code>RNATemplate(*, id_: str = make_uuid4_as_str(), name: str, regions: frozenset[ModificationSite | CodingRegion | ProteinBindingDomain] = frozenset())\n</code></pre> <p>               Bases: <code>SpeciesTemplate</code></p> <p>RNA template.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'33503606-3696-4e4a-a13c-a18e958312c6'</code> <code>name</code> <code>str</code> <p>The name of the species template</p> required <code>regions</code> <code>frozenset[ModificationSite | CodingRegion | ProteinBindingDomain]</code> <p>The regions of the RNA template</p> <code>&lt;dynamic&gt;</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RNATemplate.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.Reactant","title":"Reactant  <code>dataclass</code>","text":"<pre><code>Reactant(*, id_: str = make_uuid4_as_str(), name: str | None = None, sbo_term: str | None = None, metaid: str | None = None, referred_element: Species, stoichiometry: float | None = None, base: bool = False)\n</code></pre> <p>               Bases: <code>SpeciesReference</code>, <code>CellDesignerModelElement</code></p> <p>Reactant.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'eba66b72-cf45-495a-b748-1e5c018f30a0'</code> <code>name</code> <code>str | None</code> <p>Human-readable name of the element.</p> <code>None</code> <code>sbo_term</code> <code>str | None</code> <p>Optional SBO term identifier for semantic annotation.</p> <code>None</code> <code>metaid</code> <code>str | None</code> <p>Optional metadata identifier for RDF annotations.</p> <code>None</code> <code>referred_element</code> <code>Species</code> <p>The species being referenced.</p> required <code>stoichiometry</code> <code>float | None</code> <p>Optional stoichiometric coefficient.</p> <code>None</code> <code>base</code> <code>bool</code> <p>Whether the reactant is a base reactant or not</p> <code>False</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.Reactant.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.Reaction","title":"Reaction  <code>dataclass</code>","text":"<pre><code>Reaction(*, id_: str = make_uuid4_as_str(), name: str | None = None, sbo_term: str | None = None, metaid: str | None = None, reversible: bool = False, compartment: Compartment | None = None, reactants: frozenset[Reactant] = frozenset(), products: frozenset[Product] = frozenset(), modifiers: frozenset[KnownOrUnknownModulator] = frozenset(), has_external_source: bool = False, has_external_sink: bool = False)\n</code></pre> <p>               Bases: <code>Reaction</code>, <code>CellDesignerModelElement</code></p> <p>Abstract base class for reactions.</p> <p>CellDesigner's degraded glyph (a <code>&lt;species class=\"DEGRADED\"&gt;</code> used as a source-and-sink for unspecified external flux) is not represented as a member of <code>reactants</code> or <code>products</code>. Instead, it is encoded as the boolean flags <code>has_external_source</code> (a degraded reactant) and <code>has_external_sink</code> (a degraded product).</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'94f89dba-f35c-4a80-81aa-3b858ce37db2'</code> <code>name</code> <code>str | None</code> <p>Human-readable name of the element.</p> <code>None</code> <code>sbo_term</code> <code>str | None</code> <p>Optional SBO term identifier for semantic annotation.</p> <code>None</code> <code>metaid</code> <code>str | None</code> <p>Optional metadata identifier for RDF annotations.</p> <code>None</code> <code>reversible</code> <code>bool</code> <p>Whether the reaction can proceed in both directions.</p> <code>False</code> <code>compartment</code> <code>Compartment | None</code> <p>Optional compartment where the reaction occurs.</p> <code>None</code> <code>reactants</code> <code>frozenset[Reactant]</code> <p>The reactants of the reaction</p> <code>&lt;dynamic&gt;</code> <code>products</code> <code>frozenset[Product]</code> <p>The products of the reaction</p> <code>&lt;dynamic&gt;</code> <code>modifiers</code> <code>frozenset[KnownOrUnknownModulator]</code> <p>The modifiers of the reaction</p> <code>&lt;dynamic&gt;</code> <code>has_external_source</code> <code>bool</code> <p>Whether the reaction has an unspecified external source (a Degraded reactant in CellDesigner).</p> <code>False</code> <code>has_external_sink</code> <code>bool</code> <p>Whether the reaction has an unspecified external sink (a Degraded product in CellDesigner).</p> <code>False</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.Reaction.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReactionLayout","title":"ReactionLayout  <code>dataclass</code>","text":"<pre><code>ReactionLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, end_shorten: float = 0.0, fill: NoneValueType | Color | None = None, filter_: NoneValueType | Filter | None = None, path_fill: NoneValueType | Color | None = NoneValue, path_filter: NoneValueType | Filter | None = None, path_stroke: NoneValueType | Color | None = black, path_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, path_stroke_dashoffset: NoneValueType | float | None = None, path_stroke_width: float | None = 1.0, path_transform: NoneValueType | tuple[Transformation, ...] | None = None, stroke: NoneValueType | Color | None = None, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: NoneValueType | float | None = None, segments: tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...] = tuple(), source: LayoutElement | None = None, start_shorten: float = 0.0, target: LayoutElement | None = None, transform: NoneValueType | tuple[Transformation, ...] | None = None, end_arrowhead_fill: NoneValueType | Color | None = None, end_arrowhead_filter: NoneValueType | Filter | None = None, end_arrowhead_stroke: NoneValueType | Color | None = None, end_arrowhead_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, end_arrowhead_stroke_dashoffset: NoneValueType | float | None = None, end_arrowhead_stroke_width: NoneValueType | float | None = None, end_arrowhead_transform: NoneValueType | tuple[Transformation, ...] | None = None, start_arrowhead_fill: NoneValueType | Color | None = None, start_arrowhead_filter: NoneValueType | Filter | None = None, start_arrowhead_stroke: NoneValueType | Color | None = None, start_arrowhead_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, start_arrowhead_stroke_dashoffset: NoneValueType | float | None = None, start_arrowhead_stroke_width: NoneValueType | float | None = None, start_arrowhead_transform: NoneValueType | tuple[Transformation, ...] | None = None, reversible: bool = False)\n</code></pre> <p>               Bases: <code>CellDesignerDoubleHeadedArc</code></p> <p>Reaction layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'57cb4609-112e-4ee0-abec-652e4a8049d3'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>end_shorten</code> <code>float</code> <p>The length the end of the arc will be shorten by</p> <code>0.0</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the arc</p> <code>None</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the arc</p> <code>None</code> <code>path_fill</code> <code>NoneValueType | Color | None</code> <p>The path fill color of the arc</p> <code>&lt;momapy.drawing.NoneValueType object at 0x7f8b17323e00&gt;</code> <code>path_filter</code> <code>NoneValueType | Filter | None</code> <p>The path filter of the arc</p> <code>None</code> <code>path_stroke</code> <code>NoneValueType | Color | None</code> <p>The path stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>path_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The path stroke dasharray of the arc</p> <code>None</code> <code>path_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The path stroke dashoffset of the arc</p> <code>None</code> <code>path_stroke_width</code> <code>float | None</code> <p>The path stroke width of the arc</p> <code>1.0</code> <code>path_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The path transform of the arc</p> <code>None</code> <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the arc</p> <code>None</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the arc</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the arc</p> <code>None</code> <code>stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the arc</p> <code>None</code> <code>segments</code> <code>tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...]</code> <p>The path segments of the arc</p> <code>&lt;dynamic&gt;</code> <code>source</code> <code>LayoutElement | None</code> <p>The source of the arc</p> <code>None</code> <code>start_shorten</code> <code>float</code> <p>The length the start of the arc will be shorten by</p> <code>0.0</code> <code>target</code> <code>LayoutElement | None</code> <p>The target of the arc</p> <code>None</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the arc</p> <code>None</code> <code>end_arrowhead_fill</code> <code>NoneValueType | Color | None</code> <p>The end arrowhead fill color of the arc</p> <code>None</code> <code>end_arrowhead_filter</code> <code>NoneValueType | Filter | None</code> <p>The end arrowhead filter of the arc</p> <code>None</code> <code>end_arrowhead_stroke</code> <code>NoneValueType | Color | None</code> <p>The end arrowhead stroke color of the arc</p> <code>None</code> <code>end_arrowhead_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The end arrowhead stroke dasharray of the arc</p> <code>None</code> <code>end_arrowhead_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The end arrowhead stroke dashoffset of the arc</p> <code>None</code> <code>end_arrowhead_stroke_width</code> <code>NoneValueType | float | None</code> <p>The end arrowhead stroke width of the arc</p> <code>None</code> <code>end_arrowhead_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The end arrowhead transform of the arc</p> <code>None</code> <code>start_arrowhead_fill</code> <code>NoneValueType | Color | None</code> <p>The start arrowhead fill color of the arc</p> <code>None</code> <code>start_arrowhead_filter</code> <code>NoneValueType | Filter | None</code> <p>The start arrowhead filter of the arc</p> <code>None</code> <code>start_arrowhead_stroke</code> <code>NoneValueType | Color | None</code> <p>The start arrowhead stroke color of the arc</p> <code>None</code> <code>start_arrowhead_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The start arrowhead stroke dasharray of the arc</p> <code>None</code> <code>start_arrowhead_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The start arrowhead stroke dashoffset of the arc</p> <code>None</code> <code>start_arrowhead_stroke_width</code> <code>NoneValueType | float | None</code> <p>The start arrowhead stroke width of the arc</p> <code>None</code> <code>start_arrowhead_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The start arrowhead transform of the arc</p> <code>None</code> <code>reversible</code> <code>bool</code> <p>Whether the reaction is reversible.</p> <code>False</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>childless</code> <p>Return a copy of the arc with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>end_arrowhead_base</code> <p>Return the base anchor point of the double-headed arc end arrowhead.</p> <code>end_arrowhead_bbox</code> <p>Return the bounding box of the double-headed arc end arrowhead.</p> <code>end_arrowhead_border</code> <p>Return the point at the intersection of the drawing elements of the double-headed arc end arrowhead and the line going through the center of these drawing elements and the given point.</p> <code>end_arrowhead_drawing_elements</code> <p>Return the drawing elements of the double-headed arc end arrowhead.</p> <code>end_arrowhead_length</code> <p>Return the length of the double-headed arc end arrowhead.</p> <code>end_arrowhead_tip</code> <p>Return the tip anchor point of the double-headed arc end arrowhead.</p> <code>end_point</code> <p>Return the ending point of the arc.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>fraction</code> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <code>length</code> <p>Return the total length of the arc path.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_children</code> <p>Return the self children of the arc.</p> <code>own_drawing_elements</code> <p>Return the arc's own drawing elements, including those of its mixins.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>path_drawing_elements</code> <p>Return the drawing elements of the double-headed arc path.</p> <code>points</code> <p>Return the points of the arc path.</p> <code>start_arrowhead_base</code> <p>Return the base anchor point of the double-headed arc start arrowhead.</p> <code>start_arrowhead_bbox</code> <p>Return the bounding box of the double-headed arc start arrowhead.</p> <code>start_arrowhead_border</code> <p>Return the point at the intersection of the drawing elements of the double-headed arc start arrowhead and the line going through the center of these drawing elements and the given point.</p> <code>start_arrowhead_drawing_elements</code> <p>Return the drawing elements of the double-headed arc start arrowhead.</p> <code>start_arrowhead_length</code> <p>Return the length of the double-headed arc start arrowhead.</p> <code>start_arrowhead_tip</code> <p>Return the tip anchor point of the double-headed arc start arrowhead.</p> <code>start_point</code> <p>Return the starting point of the arc.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReactionLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReactionLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReactionLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the arc with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the arc with no children.\"\"\"\n    return dataclasses.replace(self, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReactionLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReactionLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReactionLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReactionLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReactionLayout.end_arrowhead_base","title":"end_arrowhead_base","text":"<pre><code>end_arrowhead_base() -&gt; Point\n</code></pre> <p>Return the base anchor point of the double-headed arc end arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_arrowhead_base(self) -&gt; Point:\n    \"\"\"Return the base anchor point of the double-headed arc end arrowhead.\"\"\"\n    arrowhead_length = self.end_arrowhead_length()\n    if arrowhead_length == 0:\n        return self.end_point()\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return self.end_arrowhead_tip() - (arrowhead_length, 0)\n    fraction = 1 - (arrowhead_length + self.end_shorten) / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReactionLayout.end_arrowhead_bbox","title":"end_arrowhead_bbox","text":"<pre><code>end_arrowhead_bbox() -&gt; Bbox\n</code></pre> <p>Return the bounding box of the double-headed arc end arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_arrowhead_bbox(self) -&gt; Bbox:\n    \"\"\"Return the bounding box of the double-headed arc end arrowhead.\"\"\"\n    return get_drawing_elements_bbox(self.end_arrowhead_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReactionLayout.end_arrowhead_border","title":"end_arrowhead_border","text":"<pre><code>end_arrowhead_border(point: Point) -&gt; Point\n</code></pre> <p>Return the point at the intersection of the drawing elements of the double-headed arc end arrowhead and the line going through the center of these drawing elements and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_arrowhead_border(self, point: Point) -&gt; Point:\n    \"\"\"Return the point at the intersection of the drawing elements of the double-headed arc end arrowhead and the line going through the center of these drawing elements and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    point = get_drawing_elements_border(\n        self.end_arrowhead_drawing_elements(), point\n    )\n    if point is None:\n        return self.end_arrowhead_tip()\n    return point\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReactionLayout.end_arrowhead_drawing_elements","title":"end_arrowhead_drawing_elements","text":"<pre><code>end_arrowhead_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the double-headed arc end arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_arrowhead_drawing_elements(\n    self,\n) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the double-headed arc end arrowhead.\"\"\"\n    drawing_elements = self._end_arrowhead_border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_end_arrowhead\",\n        elements=tuple(drawing_elements),\n        fill=self.end_arrowhead_fill,\n        filter_=self.end_arrowhead_filter,\n        id_=f\"{self.id_}_end_arrowhead\",\n        stroke=self.end_arrowhead_stroke,\n        stroke_width=self.end_arrowhead_stroke_width,\n        stroke_dasharray=self.end_arrowhead_stroke_dasharray,\n        stroke_dashoffset=self.end_arrowhead_stroke_dashoffset,\n        transform=self.end_arrowhead_transform,\n    )\n    transformation = self._get_end_arrowhead_transformation()\n    group = group.transformed(transformation)\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReactionLayout.end_arrowhead_length","title":"end_arrowhead_length","text":"<pre><code>end_arrowhead_length() -&gt; float\n</code></pre> <p>Return the length of the double-headed arc end arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_arrowhead_length(self) -&gt; float:\n    \"\"\"Return the length of the double-headed arc end arrowhead.\"\"\"\n    bbox = get_drawing_elements_bbox(self._end_arrowhead_border_drawing_elements())\n    if math.isnan(bbox.width):\n        return 0.0\n    return bbox.east().x\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReactionLayout.end_arrowhead_tip","title":"end_arrowhead_tip","text":"<pre><code>end_arrowhead_tip() -&gt; Point\n</code></pre> <p>Return the tip anchor point of the double-headed arc end arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_arrowhead_tip(self) -&gt; Point:\n    \"\"\"Return the tip anchor point of the double-headed arc end arrowhead.\"\"\"\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return segment.p2\n    fraction = 1 - self.end_shorten / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReactionLayout.end_point","title":"end_point","text":"<pre><code>end_point() -&gt; Point\n</code></pre> <p>Return the ending point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_point(self) -&gt; Point:\n    \"\"\"Return the ending point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[-1]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReactionLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReactionLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReactionLayout.fraction","title":"fraction","text":"<pre><code>fraction(fraction: float) -&gt; tuple[Point, float]\n</code></pre> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def fraction(self, fraction: float) -&gt; tuple[Point, float]:\n    \"\"\"Return the position and angle on the arc at a given fraction (of the total arc length).\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    current_length = 0\n    length_to_reach = fraction * self.length()\n    for segment in self.segments:\n        current_length += segment.length()\n        if current_length &gt;= length_to_reach:\n            break\n    segment_start_length = current_length - segment.length()\n    segment_fraction = (length_to_reach - segment_start_length) / segment.length()\n    position, angle = segment.get_position_and_angle_at_fraction(segment_fraction)\n    return position, angle\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReactionLayout.length","title":"length","text":"<pre><code>length() -&gt; float\n</code></pre> <p>Return the total length of the arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def length(self) -&gt; float:\n    \"\"\"Return the total length of the arc path.\"\"\"\n    return sum([segment.length() for segment in self.segments])\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReactionLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReactionLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the arc.\"\"\"\n    return []\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReactionLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the arc's own drawing elements, including those of its mixins.</p> Source code in <code>src/momapy/celldesigner/elements.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the arc's own drawing elements, including those of its mixins.\"\"\"\n    drawing_elements = super_or_builder(\n        CellDesignerDoubleHeadedArc, self\n    ).own_drawing_elements()\n    done_bases = []\n    for base in type(self).__mro__:\n        if (\n            issubclass_or_builder(base, _SBGNMixin)\n            and base is not type(self)\n            and not any([issubclass(done_base, base) for done_base in done_bases])\n        ):\n            drawing_elements += getattr(base, \"_mixin_drawing_elements\")(self)\n            done_bases.append(base)\n    return drawing_elements\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReactionLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReactionLayout.path_drawing_elements","title":"path_drawing_elements","text":"<pre><code>path_drawing_elements() -&gt; list[Path]\n</code></pre> <p>Return the drawing elements of the double-headed arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def path_drawing_elements(self) -&gt; list[Path]:\n    \"\"\"Return the drawing elements of the double-headed arc path.\"\"\"\n    start_arrowhead_length = self.start_arrowhead_length()\n    end_arrowhead_length = self.end_arrowhead_length()\n    if len(self.segments) == 1:\n        segment = (\n            self.segments[0]\n            .shortened(self.start_shorten + start_arrowhead_length, \"start\")\n            .shortened(self.end_shorten + end_arrowhead_length, \"end\")\n        )\n        actions = [\n            MoveTo(segment.p1),\n            self._make_path_action_from_segment(segment),\n        ]\n    else:\n        first_segment = self.segments[0].shortened(\n            self.start_shorten + start_arrowhead_length, \"start\"\n        )\n        last_segment = self.segments[-1].shortened(\n            self.end_shorten + end_arrowhead_length, \"end\"\n        )\n        actions = [\n            MoveTo(first_segment.p1),\n            self._make_path_action_from_segment(first_segment),\n        ]\n        for segment in self.segments[1:-1]:\n            action = self._make_path_action_from_segment(segment)\n            actions.append(action)\n        actions.append(self._make_path_action_from_segment(last_segment))\n    path = Path(\n        actions=tuple(actions),\n        class_=f\"{type(self).__name__}_path\",\n        fill=self.path_fill,\n        filter_=self.path_filter,\n        id_=f\"{self.id_}_path\",\n        stroke=self.path_stroke,\n        stroke_dasharray=self.path_stroke_dasharray,\n        stroke_dashoffset=self.path_stroke_dashoffset,\n        stroke_width=self.path_stroke_width,\n        transform=self.path_transform,\n    )\n    return [path]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReactionLayout.points","title":"points","text":"<pre><code>points() -&gt; list[Point]\n</code></pre> <p>Return the points of the arc path.</p> <p>An arc with no segments has no points and returns an empty list.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def points(self) -&gt; list[Point]:\n    \"\"\"Return the points of the arc path.\n\n    An arc with no segments has no points and returns an empty list.\n    \"\"\"\n    if not self.segments:\n        return []\n    points = []\n    for segment in self.segments:\n        points.append(segment.p1)\n    points.append(segment.p2)\n    return points\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReactionLayout.start_arrowhead_base","title":"start_arrowhead_base","text":"<pre><code>start_arrowhead_base() -&gt; Point\n</code></pre> <p>Return the base anchor point of the double-headed arc start arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_arrowhead_base(self) -&gt; Point:\n    \"\"\"Return the base anchor point of the double-headed arc start arrowhead.\"\"\"\n    arrowhead_length = self.start_arrowhead_length()\n    if arrowhead_length == 0:\n        return self.start_point()\n    segment = self.segments[0]\n    segment = Segment(segment.p2, segment.p1)\n    segment_length = segment.length()\n    if segment_length == 0:\n        return self.start_arrowhead_tip() + (arrowhead_length, 0)\n    fraction = 1 - (arrowhead_length + self.start_shorten) / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReactionLayout.start_arrowhead_bbox","title":"start_arrowhead_bbox","text":"<pre><code>start_arrowhead_bbox() -&gt; Bbox\n</code></pre> <p>Return the bounding box of the double-headed arc start arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_arrowhead_bbox(self) -&gt; Bbox:\n    \"\"\"Return the bounding box of the double-headed arc start arrowhead.\"\"\"\n    return get_drawing_elements_bbox(self.start_arrowhead_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReactionLayout.start_arrowhead_border","title":"start_arrowhead_border","text":"<pre><code>start_arrowhead_border(point: Point) -&gt; Point\n</code></pre> <p>Return the point at the intersection of the drawing elements of the double-headed arc start arrowhead and the line going through the center of these drawing elements and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_arrowhead_border(self, point: Point) -&gt; Point:\n    \"\"\"Return the point at the intersection of the drawing elements of the double-headed arc start arrowhead and the line going through the center of these drawing elements and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    point = get_drawing_elements_border(\n        self.start_arrowhead_drawing_elements(), point\n    )\n    if point is None:\n        return self.start_arrowhead_tip()\n    return point\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReactionLayout.start_arrowhead_drawing_elements","title":"start_arrowhead_drawing_elements","text":"<pre><code>start_arrowhead_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the double-headed arc start arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_arrowhead_drawing_elements(\n    self,\n) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the double-headed arc start arrowhead.\"\"\"\n    drawing_elements = self._start_arrowhead_border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_start_arrowhead\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}_start_arrowhead\",\n        fill=self.start_arrowhead_fill,\n        filter_=self.start_arrowhead_filter,\n        stroke=self.start_arrowhead_stroke,\n        stroke_dasharray=self.start_arrowhead_stroke_dasharray,\n        stroke_dashoffset=self.start_arrowhead_stroke_dashoffset,\n        stroke_width=self.start_arrowhead_stroke_width,\n        transform=self.start_arrowhead_transform,\n    )\n    transformation = self._get_start_arrowhead_transformation()\n    group = group.transformed(transformation)\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReactionLayout.start_arrowhead_length","title":"start_arrowhead_length","text":"<pre><code>start_arrowhead_length() -&gt; float\n</code></pre> <p>Return the length of the double-headed arc start arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_arrowhead_length(self) -&gt; float:\n    \"\"\"Return the length of the double-headed arc start arrowhead.\"\"\"\n    bbox = get_drawing_elements_bbox(\n        self._start_arrowhead_border_drawing_elements()\n    )\n    if math.isnan(bbox.width):\n        return 0.0\n    return abs(bbox.west().x)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReactionLayout.start_arrowhead_tip","title":"start_arrowhead_tip","text":"<pre><code>start_arrowhead_tip() -&gt; Point\n</code></pre> <p>Return the tip anchor point of the double-headed arc start arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_arrowhead_tip(self) -&gt; Point:\n    \"\"\"Return the tip anchor point of the double-headed arc start arrowhead.\"\"\"\n    segment = self.segments[0]\n    segment = Segment(segment.p2, segment.p1)\n    segment_length = segment.length()\n    if segment_length == 0:\n        return segment.p2\n    fraction = 1 - self.start_shorten / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReactionLayout.start_point","title":"start_point","text":"<pre><code>start_point() -&gt; Point\n</code></pre> <p>Return the starting point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_point(self) -&gt; Point:\n    \"\"\"Return the starting point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[0]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReactionLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.Receptor","title":"Receptor  <code>dataclass</code>","text":"<pre><code>Receptor(*, id_: str = make_uuid4_as_str(), name: str | None = None, sbo_term: str | None = None, metaid: str | None = None, compartment: Compartment | None = None, hypothetical: bool = False, active: bool = False, homomultimer: int = 1, template: ReceptorTemplate, modifications: frozenset[Modification] = frozenset(), structural_states: frozenset[StructuralState] = frozenset())\n</code></pre> <p>               Bases: <code>Protein</code></p> <p>Receptor.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'25dd16f2-13dc-4586-a766-680be9b65ac2'</code> <code>name</code> <code>str | None</code> <p>Human-readable name of the element.</p> <code>None</code> <code>sbo_term</code> <code>str | None</code> <p>Optional SBO term identifier for semantic annotation.</p> <code>None</code> <code>metaid</code> <code>str | None</code> <p>Optional metadata identifier for RDF annotations.</p> <code>None</code> <code>compartment</code> <code>Compartment | None</code> <p>The compartment containing this species.</p> <code>None</code> <code>hypothetical</code> <code>bool</code> <p>Whether the species is hypothetical or not</p> <code>False</code> <code>active</code> <code>bool</code> <p>Whether the species is active or not</p> <code>False</code> <code>homomultimer</code> <code>int</code> <p>The number of subunits forming the species</p> <code>1</code> <code>template</code> <code>ReceptorTemplate</code> <p>The template of the receptor</p> required <code>modifications</code> <code>frozenset[Modification]</code> <p>The modifications of the protein</p> <code>&lt;dynamic&gt;</code> <code>structural_states</code> <code>frozenset[StructuralState]</code> <p>The structural states of the protein</p> <code>&lt;dynamic&gt;</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.Receptor.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReceptorActiveLayout","title":"ReceptorActiveLayout  <code>dataclass</code>","text":"<pre><code>ReceptorActiveLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = NoneValue, filter_: NoneValueType | Filter | None = None, height: float = 40.0 + DEFAULT_ACTIVE_YSEP * 2, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = (4, 2), stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = 1.0, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 80.0 + DEFAULT_ACTIVE_XSEP * 2, offset: float = 3.0, subunits_stroke: tuple[NoneValueType | Color, ...] | None = None, subunits_stroke_width: tuple[NoneValueType | float, ...] | None = None, subunits_stroke_dasharray: tuple[NoneValueType | tuple[float, ...], ...] | None = None, subunits_stroke_dashoffset: tuple[float, ...] | None = None, subunits_fill: tuple[NoneValueType | Color, ...] | None = None, subunits_transform: tuple[NoneValueType | tuple[Transformation, ...], ...] | None = None, subunits_filter: tuple[NoneValueType | Filter, ...] | None = None, n: int = 1, vertical_truncation: float = 0.1)\n</code></pre> <p>               Bases: <code>_MultiNodeMixin</code>, <code>CellDesignerNode</code></p> <p>Receptor active layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'c5fb4d9d-e382-4ef1-a78f-ac5a27acfbee'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>&lt;momapy.drawing.NoneValueType object at 0x7f8b17323e00&gt;</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>48.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>(4, 2)</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The stroke width of the node</p> <code>1.0</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>88.0</code> <code>offset</code> <code>float</code> <p>Offset distance between stacked units.</p> <code>3.0</code> <code>subunits_stroke</code> <code>tuple[NoneValueType | Color, ...] | None</code> <p>Tuple of stroke colors for each subunit.</p> <code>None</code> <code>subunits_stroke_width</code> <code>tuple[NoneValueType | float, ...] | None</code> <p>Tuple of stroke widths for each subunit.</p> <code>None</code> <code>subunits_stroke_dasharray</code> <code>tuple[NoneValueType | tuple[float, ...], ...] | None</code> <p>Tuple of dash patterns for each subunit.</p> <code>None</code> <code>subunits_stroke_dashoffset</code> <code>tuple[float, ...] | None</code> <p>Tuple of dash offsets for each subunit.</p> <code>None</code> <code>subunits_fill</code> <code>tuple[NoneValueType | Color, ...] | None</code> <p>Tuple of fill colors for each subunit.</p> <code>None</code> <code>subunits_transform</code> <code>tuple[NoneValueType | tuple[Transformation, ...], ...] | None</code> <p>Tuple of transformations for each subunit.</p> <code>None</code> <code>subunits_filter</code> <code>tuple[NoneValueType | Filter, ...] | None</code> <p>Tuple of filters for each subunit.</p> <code>None</code> <code>n</code> <code>int</code> <p>Number of stacked node copies to draw.</p> <code>1</code> <code>vertical_truncation</code> <code>float</code> <p>The proportion of the height taken by the notch.</p> <code>0.1</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>angle</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Return the east anchor of the node.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Get the center position for the node label.</p> <code>north</code> <p>Return the north anchor of the node.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Return the south anchor of the node.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Return the west anchor of the node.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReceptorActiveLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReceptorActiveLayout.angle","title":"angle","text":"<pre><code>angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReceptorActiveLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReceptorActiveLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReceptorActiveLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReceptorActiveLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReceptorActiveLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReceptorActiveLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReceptorActiveLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReceptorActiveLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReceptorActiveLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Return the east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Return the east anchor of the node.\"\"\"\n    return self.own_angle(0)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReceptorActiveLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReceptorActiveLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReceptorActiveLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReceptorActiveLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReceptorActiveLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Get the center position for the node label.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point where the label should be centered.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Get the center position for the node label.\n\n    Returns:\n        Point where the label should be centered.\n    \"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2 - (self._n - 1) * self.offset,\n        self.height / 2 - height / 2 - (self._n - 1) * self.offset,\n    )\n    return self._make_subunit_shape(\n        position=position, width=width, height=height\n    ).position\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReceptorActiveLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Return the north anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Return the north anchor of the node.\"\"\"\n    return self.own_angle(90)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReceptorActiveLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReceptorActiveLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReceptorActiveLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReceptorActiveLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReceptorActiveLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReceptorActiveLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReceptorActiveLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReceptorActiveLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReceptorActiveLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReceptorActiveLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReceptorActiveLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReceptorActiveLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Return the south anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Return the south anchor of the node.\"\"\"\n    return self.own_angle(270)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReceptorActiveLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReceptorActiveLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReceptorActiveLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReceptorActiveLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReceptorActiveLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReceptorActiveLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Return the west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Return the west anchor of the node.\"\"\"\n    return self.own_angle(180)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReceptorActiveLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReceptorActiveLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReceptorActiveLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReceptorActiveLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReceptorLayout","title":"ReceptorLayout  <code>dataclass</code>","text":"<pre><code>ReceptorLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = from_hex('#CCFFCC'), filter_: NoneValueType | Filter | None = None, height: float = 40.0, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = 1.0, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 80.0, offset: float = 3.0, subunits_stroke: tuple[NoneValueType | Color, ...] | None = None, subunits_stroke_width: tuple[NoneValueType | float, ...] | None = None, subunits_stroke_dasharray: tuple[NoneValueType | tuple[float, ...], ...] | None = None, subunits_stroke_dashoffset: tuple[float, ...] | None = None, subunits_fill: tuple[NoneValueType | Color, ...] | None = None, subunits_transform: tuple[NoneValueType | tuple[Transformation, ...], ...] | None = None, subunits_filter: tuple[NoneValueType | Filter, ...] | None = None, n: int = 1, vertical_truncation: float = 0.1)\n</code></pre> <p>               Bases: <code>_MultiNodeMixin</code>, <code>CellDesignerNode</code></p> <p>Receptor layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'fa772e07-c208-4df7-aacd-b5f10733b81c'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>Color(red=204, green=255, blue=204, alpha=1.0)</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>40.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The stroke width of the node</p> <code>1.0</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>80.0</code> <code>offset</code> <code>float</code> <p>Offset distance between stacked units.</p> <code>3.0</code> <code>subunits_stroke</code> <code>tuple[NoneValueType | Color, ...] | None</code> <p>Tuple of stroke colors for each subunit.</p> <code>None</code> <code>subunits_stroke_width</code> <code>tuple[NoneValueType | float, ...] | None</code> <p>Tuple of stroke widths for each subunit.</p> <code>None</code> <code>subunits_stroke_dasharray</code> <code>tuple[NoneValueType | tuple[float, ...], ...] | None</code> <p>Tuple of dash patterns for each subunit.</p> <code>None</code> <code>subunits_stroke_dashoffset</code> <code>tuple[float, ...] | None</code> <p>Tuple of dash offsets for each subunit.</p> <code>None</code> <code>subunits_fill</code> <code>tuple[NoneValueType | Color, ...] | None</code> <p>Tuple of fill colors for each subunit.</p> <code>None</code> <code>subunits_transform</code> <code>tuple[NoneValueType | tuple[Transformation, ...], ...] | None</code> <p>Tuple of transformations for each subunit.</p> <code>None</code> <code>subunits_filter</code> <code>tuple[NoneValueType | Filter, ...] | None</code> <p>Tuple of filters for each subunit.</p> <code>None</code> <code>n</code> <code>int</code> <p>Number of stacked node copies to draw.</p> <code>1</code> <code>vertical_truncation</code> <code>float</code> <p>The proportion of the height taken by the notch.</p> <code>0.1</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>angle</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Return the east anchor point of the top subunit shape.</p> <code>east_north_east</code> <p>Return the east-north-east anchor point of the top subunit shape.</p> <code>east_south_east</code> <p>Return the east-south-east anchor point of the top subunit shape.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Get the center position for the node label.</p> <code>north</code> <p>Return the north anchor point of the top subunit shape.</p> <code>north_east</code> <p>Return the north-east anchor point of the top subunit shape.</p> <code>north_north_east</code> <p>Return the north-north-east anchor point of the top subunit shape.</p> <code>north_north_west</code> <p>Return the north-north-west anchor point of the top subunit shape.</p> <code>north_west</code> <p>Return the north-west anchor point of the top subunit shape.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Return the south anchor point of the top subunit shape.</p> <code>south_east</code> <p>Return the south-east anchor point of the top subunit shape.</p> <code>south_south_east</code> <p>Return the south-south-east anchor point of the top subunit shape.</p> <code>south_south_west</code> <p>Return the south-south-west anchor point of the top subunit shape.</p> <code>south_west</code> <p>Return the south-west anchor point of the top subunit shape.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Return the west anchor point of the top subunit shape.</p> <code>west_north_west</code> <p>Return the west-north-west anchor point of the top subunit shape.</p> <code>west_south_west</code> <p>Return the west-south-west anchor point of the top subunit shape.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReceptorLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReceptorLayout.angle","title":"angle","text":"<pre><code>angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReceptorLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReceptorLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReceptorLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReceptorLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReceptorLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReceptorLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReceptorLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReceptorLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReceptorLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Return the east anchor point of the top subunit shape.</p> Source code in <code>src/momapy/celldesigner/layout.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Return the east anchor point of the top subunit shape.\"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2,\n        self.height / 2 - height / 2,\n    )\n    shape = self._make_subunit_shape(position, width, height)\n    return shape.joint4() / 2 + shape.joint3() / 2\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReceptorLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east-north-east anchor point of the top subunit shape.</p> Source code in <code>src/momapy/celldesigner/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east-north-east anchor point of the top subunit shape.\"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2,\n        self.height / 2 - height / 2,\n    )\n    shape = self._make_subunit_shape(position, width, height)\n    return shape.joint4() * 0.25 + shape.joint3() * 0.75\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReceptorLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east-south-east anchor point of the top subunit shape.</p> Source code in <code>src/momapy/celldesigner/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east-south-east anchor point of the top subunit shape.\"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2,\n        self.height / 2 - height / 2,\n    )\n    shape = self._make_subunit_shape(position, width, height)\n    return shape.joint4() * 0.75 + shape.joint3() * 0.25\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReceptorLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReceptorLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReceptorLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Get the center position for the node label.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point where the label should be centered.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Get the center position for the node label.\n\n    Returns:\n        Point where the label should be centered.\n    \"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2 - (self._n - 1) * self.offset,\n        self.height / 2 - height / 2 - (self._n - 1) * self.offset,\n    )\n    return self._make_subunit_shape(\n        position=position, width=width, height=height\n    ).position\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReceptorLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Return the north anchor point of the top subunit shape.</p> Source code in <code>src/momapy/celldesigner/layout.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Return the north anchor point of the top subunit shape.\"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2,\n        self.height / 2 - height / 2,\n    )\n    shape = self._make_subunit_shape(position, width, height)\n    return shape.joint2()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReceptorLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north-east anchor point of the top subunit shape.</p> Source code in <code>src/momapy/celldesigner/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north-east anchor point of the top subunit shape.\"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2,\n        self.height / 2 - height / 2,\n    )\n    shape = self._make_subunit_shape(position, width, height)\n    return shape.joint3()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReceptorLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north-north-east anchor point of the top subunit shape.</p> Source code in <code>src/momapy/celldesigner/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north-north-east anchor point of the top subunit shape.\"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2,\n        self.height / 2 - height / 2,\n    )\n    shape = self._make_subunit_shape(position, width, height)\n    return shape.joint2() * 0.25 + shape.joint3() * 0.75\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReceptorLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north-north-west anchor point of the top subunit shape.</p> Source code in <code>src/momapy/celldesigner/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north-north-west anchor point of the top subunit shape.\"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2,\n        self.height / 2 - height / 2,\n    )\n    shape = self._make_subunit_shape(position, width, height)\n    return shape.joint1() / 2 + shape.joint2() / 2\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReceptorLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north-west anchor point of the top subunit shape.</p> Source code in <code>src/momapy/celldesigner/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north-west anchor point of the top subunit shape.\"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2,\n        self.height / 2 - height / 2,\n    )\n    shape = self._make_subunit_shape(position, width, height)\n    return shape.joint1()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReceptorLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReceptorLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReceptorLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReceptorLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReceptorLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReceptorLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReceptorLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReceptorLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Return the south anchor point of the top subunit shape.</p> Source code in <code>src/momapy/celldesigner/layout.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Return the south anchor point of the top subunit shape.\"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2,\n        self.height / 2 - height / 2,\n    )\n    shape = self._make_subunit_shape(position, width, height)\n    return shape.joint5()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReceptorLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south-east anchor point of the top subunit shape.</p> Source code in <code>src/momapy/celldesigner/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south-east anchor point of the top subunit shape.\"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2,\n        self.height / 2 - height / 2,\n    )\n    shape = self._make_subunit_shape(position, width, height)\n    return shape.joint4()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReceptorLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south-south-east anchor point of the top subunit shape.</p> Source code in <code>src/momapy/celldesigner/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south-south-east anchor point of the top subunit shape.\"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2,\n        self.height / 2 - height / 2,\n    )\n    shape = self._make_subunit_shape(position, width, height)\n    return shape.joint5() / 2 + shape.joint4() / 2\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReceptorLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south-south-west anchor point of the top subunit shape.</p> Source code in <code>src/momapy/celldesigner/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south-south-west anchor point of the top subunit shape.\"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2,\n        self.height / 2 - height / 2,\n    )\n    shape = self._make_subunit_shape(position, width, height)\n    return shape.joint6() / 2 + shape.joint5() / 2\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReceptorLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south-west anchor point of the top subunit shape.</p> Source code in <code>src/momapy/celldesigner/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south-west anchor point of the top subunit shape.\"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2,\n        self.height / 2 - height / 2,\n    )\n    shape = self._make_subunit_shape(position, width, height)\n    return shape.joint6()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReceptorLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReceptorLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Return the west anchor point of the top subunit shape.</p> Source code in <code>src/momapy/celldesigner/layout.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Return the west anchor point of the top subunit shape.\"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2,\n        self.height / 2 - height / 2,\n    )\n    shape = self._make_subunit_shape(position, width, height)\n    return shape.joint6() / 2 + shape.joint1() / 2\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReceptorLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west-north-west anchor point of the top subunit shape.</p> Source code in <code>src/momapy/celldesigner/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west-north-west anchor point of the top subunit shape.\"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2,\n        self.height / 2 - height / 2,\n    )\n    shape = self._make_subunit_shape(position, width, height)\n    return shape.joint6() * 0.25 + shape.joint1() * 0.75\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReceptorLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west-south-west anchor point of the top subunit shape.</p> Source code in <code>src/momapy/celldesigner/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west-south-west anchor point of the top subunit shape.\"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2,\n        self.height / 2 - height / 2,\n    )\n    shape = self._make_subunit_shape(position, width, height)\n    return shape.joint6() * 0.75 + shape.joint1() * 0.25\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReceptorLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReceptorLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReceptorTemplate","title":"ReceptorTemplate  <code>dataclass</code>","text":"<pre><code>ReceptorTemplate(*, id_: str = make_uuid4_as_str(), name: str, modification_residues: frozenset[ModificationResidue] = frozenset())\n</code></pre> <p>               Bases: <code>ProteinTemplate</code></p> <p>Receptor template.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'d8ec4c59-70dc-4851-8129-46ee63a0691a'</code> <code>name</code> <code>str</code> <p>The name of the species template</p> required <code>modification_residues</code> <code>frozenset[ModificationResidue]</code> <p>The modification residues of the protein template</p> <code>&lt;dynamic&gt;</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.ReceptorTemplate.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RectangleCompartmentLayout","title":"RectangleCompartmentLayout  <code>dataclass</code>","text":"<pre><code>RectangleCompartmentLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = white, filter_: NoneValueType | Filter | None = None, height: float = 16.0, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = 1.25, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 16.0, inner_fill: NoneValueType | Color | None = white, inner_rounded_corners: float = 10.0, inner_stroke: NoneValueType | Color | None = black, inner_stroke_width: float | None = 1.0, rounded_corners: float = 10.0, sep: float = 12.0)\n</code></pre> <p>               Bases: <code>_SimpleNodeMixin</code>, <code>CellDesignerNode</code></p> <p>Rectangle compartment layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'3ca04733-3e40-4818-a08b-8915c1dab577'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>16.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The stroke width of the node</p> <code>1.25</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>16.0</code> <code>inner_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the inner rectangle.</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>inner_rounded_corners</code> <code>float</code> <p>The corner radius of the inner rectangle.</p> <code>10.0</code> <code>inner_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the inner rectangle.</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>inner_stroke_width</code> <code>float | None</code> <p>The stroke width of the inner rectangle.</p> <code>1.0</code> <code>rounded_corners</code> <code>float</code> <p>The corner radius of the outer rectangle.</p> <code>10.0</code> <code>sep</code> <code>float</code> <p>The separation between the outer and inner rectangles.</p> <code>12.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>angle</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Return the east anchor of the node.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Return the label center anchor of the node.</p> <code>north</code> <p>Return the north anchor of the node.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Return the south anchor of the node.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Return the west anchor of the node.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RectangleCompartmentLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RectangleCompartmentLayout.angle","title":"angle","text":"<pre><code>angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RectangleCompartmentLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RectangleCompartmentLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RectangleCompartmentLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RectangleCompartmentLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RectangleCompartmentLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RectangleCompartmentLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RectangleCompartmentLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RectangleCompartmentLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RectangleCompartmentLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Return the east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Return the east anchor of the node.\"\"\"\n    return self.own_angle(0)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RectangleCompartmentLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RectangleCompartmentLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RectangleCompartmentLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RectangleCompartmentLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RectangleCompartmentLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Return the label center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Return the label center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RectangleCompartmentLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Return the north anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Return the north anchor of the node.\"\"\"\n    return self.own_angle(90)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RectangleCompartmentLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RectangleCompartmentLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RectangleCompartmentLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RectangleCompartmentLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RectangleCompartmentLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RectangleCompartmentLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RectangleCompartmentLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RectangleCompartmentLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RectangleCompartmentLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RectangleCompartmentLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RectangleCompartmentLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RectangleCompartmentLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Return the south anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Return the south anchor of the node.\"\"\"\n    return self.own_angle(270)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RectangleCompartmentLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RectangleCompartmentLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RectangleCompartmentLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RectangleCompartmentLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RectangleCompartmentLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RectangleCompartmentLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Return the west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Return the west anchor of the node.\"\"\"\n    return self.own_angle(180)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RectangleCompartmentLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RectangleCompartmentLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RectangleCompartmentLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RectangleCompartmentLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.Region","title":"Region  <code>dataclass</code>","text":"<pre><code>Region(*, id_: str = make_uuid4_as_str(), name: str | None = None, active: bool = False)\n</code></pre> <p>               Bases: <code>CellDesignerModelElement</code></p> <p>Region.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'a00ddd09-b81d-4b26-a930-3dd40c351b91'</code> <code>name</code> <code>str | None</code> <p>The name of the region</p> <code>None</code> <code>active</code> <code>bool</code> <p>Whether the region is active or not</p> <code>False</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.Region.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RegulatoryRegion","title":"RegulatoryRegion  <code>dataclass</code>","text":"<pre><code>RegulatoryRegion(*, id_: str = make_uuid4_as_str(), name: str | None = None, active: bool = False)\n</code></pre> <p>               Bases: <code>Region</code></p> <p>Regulatory region.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'7d4d09ec-dfb8-4c81-bbbe-09253958110b'</code> <code>name</code> <code>str | None</code> <p>The name of the region</p> <code>None</code> <code>active</code> <code>bool</code> <p>Whether the region is active or not</p> <code>False</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.RegulatoryRegion.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.SimpleMolecule","title":"SimpleMolecule  <code>dataclass</code>","text":"<pre><code>SimpleMolecule(*, id_: str = make_uuid4_as_str(), name: str | None = None, sbo_term: str | None = None, metaid: str | None = None, compartment: Compartment | None = None, hypothetical: bool = False, active: bool = False, homomultimer: int = 1)\n</code></pre> <p>               Bases: <code>Species</code></p> <p>Simple molecule.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'5d9ccecb-fc1b-45fd-956c-549f886cb4e0'</code> <code>name</code> <code>str | None</code> <p>Human-readable name of the element.</p> <code>None</code> <code>sbo_term</code> <code>str | None</code> <p>Optional SBO term identifier for semantic annotation.</p> <code>None</code> <code>metaid</code> <code>str | None</code> <p>Optional metadata identifier for RDF annotations.</p> <code>None</code> <code>compartment</code> <code>Compartment | None</code> <p>The compartment containing this species.</p> <code>None</code> <code>hypothetical</code> <code>bool</code> <p>Whether the species is hypothetical or not</p> <code>False</code> <code>active</code> <code>bool</code> <p>Whether the species is active or not</p> <code>False</code> <code>homomultimer</code> <code>int</code> <p>The number of subunits forming the species</p> <code>1</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.SimpleMolecule.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.SimpleMoleculeActiveLayout","title":"SimpleMoleculeActiveLayout  <code>dataclass</code>","text":"<pre><code>SimpleMoleculeActiveLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = NoneValue, filter_: NoneValueType | Filter | None = None, height: float = 25.0 + DEFAULT_ACTIVE_YSEP * 2, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = (4, 2), stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = 1.0, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 70.0 + DEFAULT_ACTIVE_XSEP * 2, offset: float = 3.0, subunits_stroke: tuple[NoneValueType | Color, ...] | None = None, subunits_stroke_width: tuple[NoneValueType | float, ...] | None = None, subunits_stroke_dasharray: tuple[NoneValueType | tuple[float, ...], ...] | None = None, subunits_stroke_dashoffset: tuple[float, ...] | None = None, subunits_fill: tuple[NoneValueType | Color, ...] | None = None, subunits_transform: tuple[NoneValueType | tuple[Transformation, ...], ...] | None = None, subunits_filter: tuple[NoneValueType | Filter, ...] | None = None, n: int = 1)\n</code></pre> <p>               Bases: <code>_MultiNodeMixin</code>, <code>CellDesignerNode</code></p> <p>Simple molecule active layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'f187d87b-efc4-47f8-accf-ff33ebbece97'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>&lt;momapy.drawing.NoneValueType object at 0x7f8b17323e00&gt;</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>33.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>(4, 2)</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The stroke width of the node</p> <code>1.0</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>78.0</code> <code>offset</code> <code>float</code> <p>Offset distance between stacked units.</p> <code>3.0</code> <code>subunits_stroke</code> <code>tuple[NoneValueType | Color, ...] | None</code> <p>Tuple of stroke colors for each subunit.</p> <code>None</code> <code>subunits_stroke_width</code> <code>tuple[NoneValueType | float, ...] | None</code> <p>Tuple of stroke widths for each subunit.</p> <code>None</code> <code>subunits_stroke_dasharray</code> <code>tuple[NoneValueType | tuple[float, ...], ...] | None</code> <p>Tuple of dash patterns for each subunit.</p> <code>None</code> <code>subunits_stroke_dashoffset</code> <code>tuple[float, ...] | None</code> <p>Tuple of dash offsets for each subunit.</p> <code>None</code> <code>subunits_fill</code> <code>tuple[NoneValueType | Color, ...] | None</code> <p>Tuple of fill colors for each subunit.</p> <code>None</code> <code>subunits_transform</code> <code>tuple[NoneValueType | tuple[Transformation, ...], ...] | None</code> <p>Tuple of transformations for each subunit.</p> <code>None</code> <code>subunits_filter</code> <code>tuple[NoneValueType | Filter, ...] | None</code> <p>Tuple of filters for each subunit.</p> <code>None</code> <code>n</code> <code>int</code> <p>Number of stacked node copies to draw.</p> <code>1</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>angle</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Return the east anchor of the node.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Get the center position for the node label.</p> <code>north</code> <p>Return the north anchor of the node.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Return the south anchor of the node.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Return the west anchor of the node.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.SimpleMoleculeActiveLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.SimpleMoleculeActiveLayout.angle","title":"angle","text":"<pre><code>angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.SimpleMoleculeActiveLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.SimpleMoleculeActiveLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.SimpleMoleculeActiveLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.SimpleMoleculeActiveLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.SimpleMoleculeActiveLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.SimpleMoleculeActiveLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.SimpleMoleculeActiveLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.SimpleMoleculeActiveLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.SimpleMoleculeActiveLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Return the east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Return the east anchor of the node.\"\"\"\n    return self.own_angle(0)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.SimpleMoleculeActiveLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.SimpleMoleculeActiveLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.SimpleMoleculeActiveLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.SimpleMoleculeActiveLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.SimpleMoleculeActiveLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Get the center position for the node label.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point where the label should be centered.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Get the center position for the node label.\n\n    Returns:\n        Point where the label should be centered.\n    \"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2 - (self._n - 1) * self.offset,\n        self.height / 2 - height / 2 - (self._n - 1) * self.offset,\n    )\n    return self._make_subunit_shape(\n        position=position, width=width, height=height\n    ).position\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.SimpleMoleculeActiveLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Return the north anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Return the north anchor of the node.\"\"\"\n    return self.own_angle(90)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.SimpleMoleculeActiveLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.SimpleMoleculeActiveLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.SimpleMoleculeActiveLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.SimpleMoleculeActiveLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.SimpleMoleculeActiveLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.SimpleMoleculeActiveLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.SimpleMoleculeActiveLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.SimpleMoleculeActiveLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.SimpleMoleculeActiveLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.SimpleMoleculeActiveLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.SimpleMoleculeActiveLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.SimpleMoleculeActiveLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Return the south anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Return the south anchor of the node.\"\"\"\n    return self.own_angle(270)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.SimpleMoleculeActiveLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.SimpleMoleculeActiveLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.SimpleMoleculeActiveLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.SimpleMoleculeActiveLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.SimpleMoleculeActiveLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.SimpleMoleculeActiveLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Return the west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Return the west anchor of the node.\"\"\"\n    return self.own_angle(180)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.SimpleMoleculeActiveLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.SimpleMoleculeActiveLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.SimpleMoleculeActiveLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.SimpleMoleculeActiveLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.SimpleMoleculeLayout","title":"SimpleMoleculeLayout  <code>dataclass</code>","text":"<pre><code>SimpleMoleculeLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = from_hex('#CCFF66'), filter_: NoneValueType | Filter | None = None, height: float = 25.0, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = 1.0, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 70.0, offset: float = 3.0, subunits_stroke: tuple[NoneValueType | Color, ...] | None = None, subunits_stroke_width: tuple[NoneValueType | float, ...] | None = None, subunits_stroke_dasharray: tuple[NoneValueType | tuple[float, ...], ...] | None = None, subunits_stroke_dashoffset: tuple[float, ...] | None = None, subunits_fill: tuple[NoneValueType | Color, ...] | None = None, subunits_transform: tuple[NoneValueType | tuple[Transformation, ...], ...] | None = None, subunits_filter: tuple[NoneValueType | Filter, ...] | None = None, n: int = 1)\n</code></pre> <p>               Bases: <code>_MultiNodeMixin</code>, <code>CellDesignerNode</code></p> <p>Simple molecule layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'c70206f1-6f9c-45d6-9a27-c9783518b560'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>Color(red=204, green=255, blue=102, alpha=1.0)</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>25.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The stroke width of the node</p> <code>1.0</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>70.0</code> <code>offset</code> <code>float</code> <p>Offset distance between stacked units.</p> <code>3.0</code> <code>subunits_stroke</code> <code>tuple[NoneValueType | Color, ...] | None</code> <p>Tuple of stroke colors for each subunit.</p> <code>None</code> <code>subunits_stroke_width</code> <code>tuple[NoneValueType | float, ...] | None</code> <p>Tuple of stroke widths for each subunit.</p> <code>None</code> <code>subunits_stroke_dasharray</code> <code>tuple[NoneValueType | tuple[float, ...], ...] | None</code> <p>Tuple of dash patterns for each subunit.</p> <code>None</code> <code>subunits_stroke_dashoffset</code> <code>tuple[float, ...] | None</code> <p>Tuple of dash offsets for each subunit.</p> <code>None</code> <code>subunits_fill</code> <code>tuple[NoneValueType | Color, ...] | None</code> <p>Tuple of fill colors for each subunit.</p> <code>None</code> <code>subunits_transform</code> <code>tuple[NoneValueType | tuple[Transformation, ...], ...] | None</code> <p>Tuple of transformations for each subunit.</p> <code>None</code> <code>subunits_filter</code> <code>tuple[NoneValueType | Filter, ...] | None</code> <p>Tuple of filters for each subunit.</p> <code>None</code> <code>n</code> <code>int</code> <p>Number of stacked node copies to draw.</p> <code>1</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>angle</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Return the east anchor of the node.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Get the center position for the node label.</p> <code>north</code> <p>Return the north anchor of the node.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Return the south anchor of the node.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Return the west anchor of the node.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.SimpleMoleculeLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.SimpleMoleculeLayout.angle","title":"angle","text":"<pre><code>angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.SimpleMoleculeLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.SimpleMoleculeLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.SimpleMoleculeLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.SimpleMoleculeLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.SimpleMoleculeLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.SimpleMoleculeLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.SimpleMoleculeLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.SimpleMoleculeLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.SimpleMoleculeLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Return the east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Return the east anchor of the node.\"\"\"\n    return self.own_angle(0)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.SimpleMoleculeLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.SimpleMoleculeLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.SimpleMoleculeLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.SimpleMoleculeLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.SimpleMoleculeLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Get the center position for the node label.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point where the label should be centered.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Get the center position for the node label.\n\n    Returns:\n        Point where the label should be centered.\n    \"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2 - (self._n - 1) * self.offset,\n        self.height / 2 - height / 2 - (self._n - 1) * self.offset,\n    )\n    return self._make_subunit_shape(\n        position=position, width=width, height=height\n    ).position\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.SimpleMoleculeLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Return the north anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Return the north anchor of the node.\"\"\"\n    return self.own_angle(90)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.SimpleMoleculeLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.SimpleMoleculeLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.SimpleMoleculeLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.SimpleMoleculeLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.SimpleMoleculeLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.SimpleMoleculeLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.SimpleMoleculeLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.SimpleMoleculeLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.SimpleMoleculeLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.SimpleMoleculeLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.SimpleMoleculeLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.SimpleMoleculeLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Return the south anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Return the south anchor of the node.\"\"\"\n    return self.own_angle(270)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.SimpleMoleculeLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.SimpleMoleculeLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.SimpleMoleculeLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.SimpleMoleculeLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.SimpleMoleculeLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.SimpleMoleculeLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Return the west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Return the west anchor of the node.\"\"\"\n    return self.own_angle(180)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.SimpleMoleculeLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.SimpleMoleculeLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.SimpleMoleculeLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.SimpleMoleculeLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.Species","title":"Species  <code>dataclass</code>","text":"<pre><code>Species(*, id_: str = make_uuid4_as_str(), name: str | None = None, sbo_term: str | None = None, metaid: str | None = None, compartment: Compartment | None = None, hypothetical: bool = False, active: bool = False, homomultimer: int = 1)\n</code></pre> <p>               Bases: <code>Species</code>, <code>CellDesignerModelElement</code></p> <p>Abstract base class for species.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'b030c20c-abbb-4ae6-ba95-d377f2639657'</code> <code>name</code> <code>str | None</code> <p>Human-readable name of the element.</p> <code>None</code> <code>sbo_term</code> <code>str | None</code> <p>Optional SBO term identifier for semantic annotation.</p> <code>None</code> <code>metaid</code> <code>str | None</code> <p>Optional metadata identifier for RDF annotations.</p> <code>None</code> <code>compartment</code> <code>Compartment | None</code> <p>The compartment containing this species.</p> <code>None</code> <code>hypothetical</code> <code>bool</code> <p>Whether the species is hypothetical or not</p> <code>False</code> <code>active</code> <code>bool</code> <p>Whether the species is active or not</p> <code>False</code> <code>homomultimer</code> <code>int</code> <p>The number of subunits forming the species</p> <code>1</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.Species.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.SpeciesTemplate","title":"SpeciesTemplate  <code>dataclass</code>","text":"<pre><code>SpeciesTemplate(*, id_: str = make_uuid4_as_str(), name: str)\n</code></pre> <p>               Bases: <code>CellDesignerModelElement</code></p> <p>Abstract base class for species templates.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'de3721bb-0f12-4dbf-8102-042e344d499c'</code> <code>name</code> <code>str</code> <p>The name of the species template</p> required <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.SpeciesTemplate.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.StateTransition","title":"StateTransition  <code>dataclass</code>","text":"<pre><code>StateTransition(*, id_: str = make_uuid4_as_str(), name: str | None = None, sbo_term: str | None = None, metaid: str | None = None, reversible: bool = False, compartment: Compartment | None = None, reactants: frozenset[Reactant] = frozenset(), products: frozenset[Product] = frozenset(), modifiers: frozenset[KnownOrUnknownModulator] = frozenset(), has_external_source: bool = False, has_external_sink: bool = False)\n</code></pre> <p>               Bases: <code>Reaction</code></p> <p>State transition.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'c143257a-1598-48b7-a26d-345d48785d14'</code> <code>name</code> <code>str | None</code> <p>Human-readable name of the element.</p> <code>None</code> <code>sbo_term</code> <code>str | None</code> <p>Optional SBO term identifier for semantic annotation.</p> <code>None</code> <code>metaid</code> <code>str | None</code> <p>Optional metadata identifier for RDF annotations.</p> <code>None</code> <code>reversible</code> <code>bool</code> <p>Whether the reaction can proceed in both directions.</p> <code>False</code> <code>compartment</code> <code>Compartment | None</code> <p>Optional compartment where the reaction occurs.</p> <code>None</code> <code>reactants</code> <code>frozenset[Reactant]</code> <p>The reactants of the reaction</p> <code>&lt;dynamic&gt;</code> <code>products</code> <code>frozenset[Product]</code> <p>The products of the reaction</p> <code>&lt;dynamic&gt;</code> <code>modifiers</code> <code>frozenset[KnownOrUnknownModulator]</code> <p>The modifiers of the reaction</p> <code>&lt;dynamic&gt;</code> <code>has_external_source</code> <code>bool</code> <p>Whether the reaction has an unspecified external source (a Degraded reactant in CellDesigner).</p> <code>False</code> <code>has_external_sink</code> <code>bool</code> <p>Whether the reaction has an unspecified external sink (a Degraded product in CellDesigner).</p> <code>False</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.StateTransition.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.StateTransitionLayout","title":"StateTransitionLayout  <code>dataclass</code>","text":"<pre><code>StateTransitionLayout(*, left_connector_fraction: float = 0.375, right_connector_fraction: float = 0.625, reaction_node_font_family: str = DEFAULT_FONT_FAMILY, reaction_node_font_fill: Color | NoneValueType = black, reaction_node_font_stroke: Color | NoneValueType = NoneValue, reaction_node_font_style: FontStyle = NORMAL, reaction_node_font_weight: FontWeight | float = NORMAL, reaction_node_text: str | None = None, reaction_node_height: float = 10.0, reaction_node_width: float = 10.0, reaction_node_segment: int = 1, reaction_node_stroke: NoneValueType | Color | None = black, reaction_node_stroke_width: float | None = 1.0, reaction_node_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, reaction_node_stroke_dashoffset: float | None = None, reaction_node_fill: NoneValueType | Color | None = white, reaction_node_transform: NoneValueType | tuple[Transformation, ...] | None = None, reaction_node_filter: NoneValueType | Filter | None = None, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, end_shorten: float = 2.0, fill: NoneValueType | Color | None = None, filter_: NoneValueType | Filter | None = None, path_fill: NoneValueType | Color | None = NoneValue, path_filter: NoneValueType | Filter | None = None, path_stroke: NoneValueType | Color | None = black, path_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, path_stroke_dashoffset: NoneValueType | float | None = None, path_stroke_width: float | None = 1.0, path_transform: NoneValueType | tuple[Transformation, ...] | None = None, stroke: NoneValueType | Color | None = None, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: NoneValueType | float | None = None, segments: tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...] = tuple(), source: LayoutElement | None = None, start_shorten: float = 2.0, target: LayoutElement | None = None, transform: NoneValueType | tuple[Transformation, ...] | None = None, end_arrowhead_fill: NoneValueType | Color | None = black, end_arrowhead_filter: NoneValueType | Filter | None = None, end_arrowhead_stroke: NoneValueType | Color | None = black, end_arrowhead_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, end_arrowhead_stroke_dashoffset: float | None = None, end_arrowhead_stroke_width: float | None = 1.0, end_arrowhead_transform: NoneValueType | tuple[Transformation, ...] | None = None, start_arrowhead_fill: NoneValueType | Color | None = black, start_arrowhead_filter: NoneValueType | Filter | None = None, start_arrowhead_stroke: NoneValueType | Color | None = black, start_arrowhead_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, start_arrowhead_stroke_dashoffset: float | None = None, start_arrowhead_stroke_width: float | None = 1.0, start_arrowhead_transform: NoneValueType | tuple[Transformation, ...] | None = None, reversible: bool = False, end_arrowhead_height: float = 8.0, end_arrowhead_width: float = 15.0, start_arrowhead_height: float = 8.0, start_arrowhead_width: float = 15.0)\n</code></pre> <p>               Bases: <code>ReactionLayout</code>, <code>_ReactionNodeMixin</code></p> <p>State transition layout.</p> <p>Parameters:</p> Name Type Description Default <code>left_connector_fraction</code> <code>float</code> <p>The fraction along the segment where the left connector attaches.</p> <code>0.375</code> <code>right_connector_fraction</code> <code>float</code> <p>The fraction along the segment where the right connector attaches.</p> <code>0.625</code> <code>reaction_node_font_family</code> <code>str</code> <p>The font family of the reaction node label.</p> <code>'DejaVu Sans'</code> <code>reaction_node_font_fill</code> <code>Color | NoneValueType</code> <p>The fill color of the reaction node label.</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>reaction_node_font_stroke</code> <code>Color | NoneValueType</code> <p>The stroke color of the reaction node label.</p> <code>&lt;momapy.drawing.NoneValueType object at 0x7f8b17323e00&gt;</code> <code>reaction_node_font_style</code> <code>FontStyle</code> <p>The font style of the reaction node label.</p> <code>&lt;FontStyle.NORMAL: 0&gt;</code> <code>reaction_node_font_weight</code> <code>FontWeight | float</code> <p>The font weight of the reaction node label.</p> <code>&lt;FontWeight.NORMAL: 0&gt;</code> <code>reaction_node_text</code> <code>str | None</code> <p>The text of the reaction node label.</p> <code>None</code> <code>reaction_node_height</code> <code>float</code> <p>The height of the reaction node.</p> <code>10.0</code> <code>reaction_node_width</code> <code>float</code> <p>The width of the reaction node.</p> <code>10.0</code> <code>reaction_node_segment</code> <code>int</code> <p>The index of the arc segment carrying the reaction node.</p> <code>1</code> <code>reaction_node_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the reaction node.</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>reaction_node_stroke_width</code> <code>float | None</code> <p>The stroke width of the reaction node.</p> <code>1.0</code> <code>reaction_node_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The dash pattern of the reaction node's border.</p> <code>None</code> <code>reaction_node_stroke_dashoffset</code> <code>float | None</code> <p>The dash offset of the reaction node's border.</p> <code>None</code> <code>reaction_node_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the reaction node.</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>reaction_node_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform applied to the reaction node.</p> <code>None</code> <code>reaction_node_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter applied to the reaction node.</p> <code>None</code> <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'792cbc66-4ec7-412c-bc82-b4d700e7f858'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>end_shorten</code> <code>float</code> <p>The length the end of the arc will be shorten by</p> <code>2.0</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the arc</p> <code>None</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the arc</p> <code>None</code> <code>path_fill</code> <code>NoneValueType | Color | None</code> <p>The path fill color of the arc</p> <code>&lt;momapy.drawing.NoneValueType object at 0x7f8b17323e00&gt;</code> <code>path_filter</code> <code>NoneValueType | Filter | None</code> <p>The path filter of the arc</p> <code>None</code> <code>path_stroke</code> <code>NoneValueType | Color | None</code> <p>The path stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>path_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The path stroke dasharray of the arc</p> <code>None</code> <code>path_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The path stroke dashoffset of the arc</p> <code>None</code> <code>path_stroke_width</code> <code>float | None</code> <p>The path stroke width of the arc</p> <code>1.0</code> <code>path_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The path transform of the arc</p> <code>None</code> <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the arc</p> <code>None</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the arc</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the arc</p> <code>None</code> <code>stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the arc</p> <code>None</code> <code>segments</code> <code>tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...]</code> <p>The path segments of the arc</p> <code>&lt;dynamic&gt;</code> <code>source</code> <code>LayoutElement | None</code> <p>The source of the arc</p> <code>None</code> <code>start_shorten</code> <code>float</code> <p>The length the start of the arc will be shorten by</p> <code>2.0</code> <code>target</code> <code>LayoutElement | None</code> <p>The target of the arc</p> <code>None</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the arc</p> <code>None</code> <code>end_arrowhead_fill</code> <code>NoneValueType | Color | None</code> <p>The end arrowhead fill color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>end_arrowhead_filter</code> <code>NoneValueType | Filter | None</code> <p>The end arrowhead filter of the arc</p> <code>None</code> <code>end_arrowhead_stroke</code> <code>NoneValueType | Color | None</code> <p>The end arrowhead stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>end_arrowhead_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The end arrowhead stroke dasharray of the arc</p> <code>None</code> <code>end_arrowhead_stroke_dashoffset</code> <code>float | None</code> <p>The end arrowhead stroke dashoffset of the arc</p> <code>None</code> <code>end_arrowhead_stroke_width</code> <code>float | None</code> <p>The end arrowhead stroke width of the arc</p> <code>1.0</code> <code>end_arrowhead_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The end arrowhead transform of the arc</p> <code>None</code> <code>start_arrowhead_fill</code> <code>NoneValueType | Color | None</code> <p>The start arrowhead fill color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>start_arrowhead_filter</code> <code>NoneValueType | Filter | None</code> <p>The start arrowhead filter of the arc</p> <code>None</code> <code>start_arrowhead_stroke</code> <code>NoneValueType | Color | None</code> <p>The start arrowhead stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>start_arrowhead_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The start arrowhead stroke dasharray of the arc</p> <code>None</code> <code>start_arrowhead_stroke_dashoffset</code> <code>float | None</code> <p>The start arrowhead stroke dashoffset of the arc</p> <code>None</code> <code>start_arrowhead_stroke_width</code> <code>float | None</code> <p>The start arrowhead stroke width of the arc</p> <code>1.0</code> <code>start_arrowhead_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The start arrowhead transform of the arc</p> <code>None</code> <code>reversible</code> <code>bool</code> <p>Whether the reaction is reversible.</p> <code>False</code> <code>end_arrowhead_height</code> <code>float</code> <p>The height of the end arrowhead.</p> <code>8.0</code> <code>end_arrowhead_width</code> <code>float</code> <p>The width of the end arrowhead.</p> <code>15.0</code> <code>start_arrowhead_height</code> <code>float</code> <p>The height of the start arrowhead.</p> <code>8.0</code> <code>start_arrowhead_width</code> <code>float</code> <p>The width of the start arrowhead.</p> <code>15.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>childless</code> <p>Return a copy of the arc with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>end_arrowhead_base</code> <p>Return the base anchor point of the double-headed arc end arrowhead.</p> <code>end_arrowhead_bbox</code> <p>Return the bounding box of the double-headed arc end arrowhead.</p> <code>end_arrowhead_border</code> <p>Return the point at the intersection of the drawing elements of the double-headed arc end arrowhead and the line going through the center of these drawing elements and the given point.</p> <code>end_arrowhead_drawing_elements</code> <p>Return the drawing elements of the double-headed arc end arrowhead.</p> <code>end_arrowhead_length</code> <p>Return the length of the double-headed arc end arrowhead.</p> <code>end_arrowhead_tip</code> <p>Return the tip anchor point of the double-headed arc end arrowhead.</p> <code>end_point</code> <p>Return the ending point of the arc.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>fraction</code> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <code>length</code> <p>Return the total length of the arc path.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_children</code> <p>Return the self children of the arc.</p> <code>own_drawing_elements</code> <p>Return the arc's own drawing elements, including those of its mixins.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>path_drawing_elements</code> <p>Return the drawing elements of the double-headed arc path.</p> <code>points</code> <p>Return the points of the arc path.</p> <code>start_arrowhead_base</code> <p>Return the base anchor point of the double-headed arc start arrowhead.</p> <code>start_arrowhead_bbox</code> <p>Return the bounding box of the double-headed arc start arrowhead.</p> <code>start_arrowhead_border</code> <p>Return the point at the intersection of the drawing elements of the double-headed arc start arrowhead and the line going through the center of these drawing elements and the given point.</p> <code>start_arrowhead_drawing_elements</code> <p>Return the drawing elements of the double-headed arc start arrowhead.</p> <code>start_arrowhead_length</code> <p>Return the length of the double-headed arc start arrowhead.</p> <code>start_arrowhead_tip</code> <p>Return the tip anchor point of the double-headed arc start arrowhead.</p> <code>start_point</code> <p>Return the starting point of the arc.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.StateTransitionLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.StateTransitionLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.StateTransitionLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the arc with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the arc with no children.\"\"\"\n    return dataclasses.replace(self, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.StateTransitionLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.StateTransitionLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.StateTransitionLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.StateTransitionLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.StateTransitionLayout.end_arrowhead_base","title":"end_arrowhead_base","text":"<pre><code>end_arrowhead_base() -&gt; Point\n</code></pre> <p>Return the base anchor point of the double-headed arc end arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_arrowhead_base(self) -&gt; Point:\n    \"\"\"Return the base anchor point of the double-headed arc end arrowhead.\"\"\"\n    arrowhead_length = self.end_arrowhead_length()\n    if arrowhead_length == 0:\n        return self.end_point()\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return self.end_arrowhead_tip() - (arrowhead_length, 0)\n    fraction = 1 - (arrowhead_length + self.end_shorten) / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.StateTransitionLayout.end_arrowhead_bbox","title":"end_arrowhead_bbox","text":"<pre><code>end_arrowhead_bbox() -&gt; Bbox\n</code></pre> <p>Return the bounding box of the double-headed arc end arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_arrowhead_bbox(self) -&gt; Bbox:\n    \"\"\"Return the bounding box of the double-headed arc end arrowhead.\"\"\"\n    return get_drawing_elements_bbox(self.end_arrowhead_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.StateTransitionLayout.end_arrowhead_border","title":"end_arrowhead_border","text":"<pre><code>end_arrowhead_border(point: Point) -&gt; Point\n</code></pre> <p>Return the point at the intersection of the drawing elements of the double-headed arc end arrowhead and the line going through the center of these drawing elements and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_arrowhead_border(self, point: Point) -&gt; Point:\n    \"\"\"Return the point at the intersection of the drawing elements of the double-headed arc end arrowhead and the line going through the center of these drawing elements and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    point = get_drawing_elements_border(\n        self.end_arrowhead_drawing_elements(), point\n    )\n    if point is None:\n        return self.end_arrowhead_tip()\n    return point\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.StateTransitionLayout.end_arrowhead_drawing_elements","title":"end_arrowhead_drawing_elements","text":"<pre><code>end_arrowhead_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the double-headed arc end arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_arrowhead_drawing_elements(\n    self,\n) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the double-headed arc end arrowhead.\"\"\"\n    drawing_elements = self._end_arrowhead_border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_end_arrowhead\",\n        elements=tuple(drawing_elements),\n        fill=self.end_arrowhead_fill,\n        filter_=self.end_arrowhead_filter,\n        id_=f\"{self.id_}_end_arrowhead\",\n        stroke=self.end_arrowhead_stroke,\n        stroke_width=self.end_arrowhead_stroke_width,\n        stroke_dasharray=self.end_arrowhead_stroke_dasharray,\n        stroke_dashoffset=self.end_arrowhead_stroke_dashoffset,\n        transform=self.end_arrowhead_transform,\n    )\n    transformation = self._get_end_arrowhead_transformation()\n    group = group.transformed(transformation)\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.StateTransitionLayout.end_arrowhead_length","title":"end_arrowhead_length","text":"<pre><code>end_arrowhead_length() -&gt; float\n</code></pre> <p>Return the length of the double-headed arc end arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_arrowhead_length(self) -&gt; float:\n    \"\"\"Return the length of the double-headed arc end arrowhead.\"\"\"\n    bbox = get_drawing_elements_bbox(self._end_arrowhead_border_drawing_elements())\n    if math.isnan(bbox.width):\n        return 0.0\n    return bbox.east().x\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.StateTransitionLayout.end_arrowhead_tip","title":"end_arrowhead_tip","text":"<pre><code>end_arrowhead_tip() -&gt; Point\n</code></pre> <p>Return the tip anchor point of the double-headed arc end arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_arrowhead_tip(self) -&gt; Point:\n    \"\"\"Return the tip anchor point of the double-headed arc end arrowhead.\"\"\"\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return segment.p2\n    fraction = 1 - self.end_shorten / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.StateTransitionLayout.end_point","title":"end_point","text":"<pre><code>end_point() -&gt; Point\n</code></pre> <p>Return the ending point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_point(self) -&gt; Point:\n    \"\"\"Return the ending point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[-1]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.StateTransitionLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.StateTransitionLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.StateTransitionLayout.fraction","title":"fraction","text":"<pre><code>fraction(fraction: float) -&gt; tuple[Point, float]\n</code></pre> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def fraction(self, fraction: float) -&gt; tuple[Point, float]:\n    \"\"\"Return the position and angle on the arc at a given fraction (of the total arc length).\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    current_length = 0\n    length_to_reach = fraction * self.length()\n    for segment in self.segments:\n        current_length += segment.length()\n        if current_length &gt;= length_to_reach:\n            break\n    segment_start_length = current_length - segment.length()\n    segment_fraction = (length_to_reach - segment_start_length) / segment.length()\n    position, angle = segment.get_position_and_angle_at_fraction(segment_fraction)\n    return position, angle\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.StateTransitionLayout.length","title":"length","text":"<pre><code>length() -&gt; float\n</code></pre> <p>Return the total length of the arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def length(self) -&gt; float:\n    \"\"\"Return the total length of the arc path.\"\"\"\n    return sum([segment.length() for segment in self.segments])\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.StateTransitionLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.StateTransitionLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the arc.\"\"\"\n    return []\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.StateTransitionLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the arc's own drawing elements, including those of its mixins.</p> Source code in <code>src/momapy/celldesigner/elements.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the arc's own drawing elements, including those of its mixins.\"\"\"\n    drawing_elements = super_or_builder(\n        CellDesignerDoubleHeadedArc, self\n    ).own_drawing_elements()\n    done_bases = []\n    for base in type(self).__mro__:\n        if (\n            issubclass_or_builder(base, _SBGNMixin)\n            and base is not type(self)\n            and not any([issubclass(done_base, base) for done_base in done_bases])\n        ):\n            drawing_elements += getattr(base, \"_mixin_drawing_elements\")(self)\n            done_bases.append(base)\n    return drawing_elements\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.StateTransitionLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.StateTransitionLayout.path_drawing_elements","title":"path_drawing_elements","text":"<pre><code>path_drawing_elements() -&gt; list[Path]\n</code></pre> <p>Return the drawing elements of the double-headed arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def path_drawing_elements(self) -&gt; list[Path]:\n    \"\"\"Return the drawing elements of the double-headed arc path.\"\"\"\n    start_arrowhead_length = self.start_arrowhead_length()\n    end_arrowhead_length = self.end_arrowhead_length()\n    if len(self.segments) == 1:\n        segment = (\n            self.segments[0]\n            .shortened(self.start_shorten + start_arrowhead_length, \"start\")\n            .shortened(self.end_shorten + end_arrowhead_length, \"end\")\n        )\n        actions = [\n            MoveTo(segment.p1),\n            self._make_path_action_from_segment(segment),\n        ]\n    else:\n        first_segment = self.segments[0].shortened(\n            self.start_shorten + start_arrowhead_length, \"start\"\n        )\n        last_segment = self.segments[-1].shortened(\n            self.end_shorten + end_arrowhead_length, \"end\"\n        )\n        actions = [\n            MoveTo(first_segment.p1),\n            self._make_path_action_from_segment(first_segment),\n        ]\n        for segment in self.segments[1:-1]:\n            action = self._make_path_action_from_segment(segment)\n            actions.append(action)\n        actions.append(self._make_path_action_from_segment(last_segment))\n    path = Path(\n        actions=tuple(actions),\n        class_=f\"{type(self).__name__}_path\",\n        fill=self.path_fill,\n        filter_=self.path_filter,\n        id_=f\"{self.id_}_path\",\n        stroke=self.path_stroke,\n        stroke_dasharray=self.path_stroke_dasharray,\n        stroke_dashoffset=self.path_stroke_dashoffset,\n        stroke_width=self.path_stroke_width,\n        transform=self.path_transform,\n    )\n    return [path]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.StateTransitionLayout.points","title":"points","text":"<pre><code>points() -&gt; list[Point]\n</code></pre> <p>Return the points of the arc path.</p> <p>An arc with no segments has no points and returns an empty list.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def points(self) -&gt; list[Point]:\n    \"\"\"Return the points of the arc path.\n\n    An arc with no segments has no points and returns an empty list.\n    \"\"\"\n    if not self.segments:\n        return []\n    points = []\n    for segment in self.segments:\n        points.append(segment.p1)\n    points.append(segment.p2)\n    return points\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.StateTransitionLayout.start_arrowhead_base","title":"start_arrowhead_base","text":"<pre><code>start_arrowhead_base() -&gt; Point\n</code></pre> <p>Return the base anchor point of the double-headed arc start arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_arrowhead_base(self) -&gt; Point:\n    \"\"\"Return the base anchor point of the double-headed arc start arrowhead.\"\"\"\n    arrowhead_length = self.start_arrowhead_length()\n    if arrowhead_length == 0:\n        return self.start_point()\n    segment = self.segments[0]\n    segment = Segment(segment.p2, segment.p1)\n    segment_length = segment.length()\n    if segment_length == 0:\n        return self.start_arrowhead_tip() + (arrowhead_length, 0)\n    fraction = 1 - (arrowhead_length + self.start_shorten) / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.StateTransitionLayout.start_arrowhead_bbox","title":"start_arrowhead_bbox","text":"<pre><code>start_arrowhead_bbox() -&gt; Bbox\n</code></pre> <p>Return the bounding box of the double-headed arc start arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_arrowhead_bbox(self) -&gt; Bbox:\n    \"\"\"Return the bounding box of the double-headed arc start arrowhead.\"\"\"\n    return get_drawing_elements_bbox(self.start_arrowhead_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.StateTransitionLayout.start_arrowhead_border","title":"start_arrowhead_border","text":"<pre><code>start_arrowhead_border(point: Point) -&gt; Point\n</code></pre> <p>Return the point at the intersection of the drawing elements of the double-headed arc start arrowhead and the line going through the center of these drawing elements and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_arrowhead_border(self, point: Point) -&gt; Point:\n    \"\"\"Return the point at the intersection of the drawing elements of the double-headed arc start arrowhead and the line going through the center of these drawing elements and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    point = get_drawing_elements_border(\n        self.start_arrowhead_drawing_elements(), point\n    )\n    if point is None:\n        return self.start_arrowhead_tip()\n    return point\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.StateTransitionLayout.start_arrowhead_drawing_elements","title":"start_arrowhead_drawing_elements","text":"<pre><code>start_arrowhead_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the double-headed arc start arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_arrowhead_drawing_elements(\n    self,\n) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the double-headed arc start arrowhead.\"\"\"\n    drawing_elements = self._start_arrowhead_border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_start_arrowhead\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}_start_arrowhead\",\n        fill=self.start_arrowhead_fill,\n        filter_=self.start_arrowhead_filter,\n        stroke=self.start_arrowhead_stroke,\n        stroke_dasharray=self.start_arrowhead_stroke_dasharray,\n        stroke_dashoffset=self.start_arrowhead_stroke_dashoffset,\n        stroke_width=self.start_arrowhead_stroke_width,\n        transform=self.start_arrowhead_transform,\n    )\n    transformation = self._get_start_arrowhead_transformation()\n    group = group.transformed(transformation)\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.StateTransitionLayout.start_arrowhead_length","title":"start_arrowhead_length","text":"<pre><code>start_arrowhead_length() -&gt; float\n</code></pre> <p>Return the length of the double-headed arc start arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_arrowhead_length(self) -&gt; float:\n    \"\"\"Return the length of the double-headed arc start arrowhead.\"\"\"\n    bbox = get_drawing_elements_bbox(\n        self._start_arrowhead_border_drawing_elements()\n    )\n    if math.isnan(bbox.width):\n        return 0.0\n    return abs(bbox.west().x)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.StateTransitionLayout.start_arrowhead_tip","title":"start_arrowhead_tip","text":"<pre><code>start_arrowhead_tip() -&gt; Point\n</code></pre> <p>Return the tip anchor point of the double-headed arc start arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_arrowhead_tip(self) -&gt; Point:\n    \"\"\"Return the tip anchor point of the double-headed arc start arrowhead.\"\"\"\n    segment = self.segments[0]\n    segment = Segment(segment.p2, segment.p1)\n    segment_length = segment.length()\n    if segment_length == 0:\n        return segment.p2\n    fraction = 1 - self.start_shorten / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.StateTransitionLayout.start_point","title":"start_point","text":"<pre><code>start_point() -&gt; Point\n</code></pre> <p>Return the starting point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_point(self) -&gt; Point:\n    \"\"\"Return the starting point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[0]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.StateTransitionLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.StructuralState","title":"StructuralState  <code>dataclass</code>","text":"<pre><code>StructuralState(*, id_: str = make_uuid4_as_str(), value: str | None = None)\n</code></pre> <p>               Bases: <code>CellDesignerModelElement</code></p> <p>Structural state.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'e5774b47-9e9e-4b4a-82da-b4a2f56f573f'</code> <code>value</code> <code>str | None</code> <p>The value of the structural state</p> <code>None</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.StructuralState.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.StructuralStateLayout","title":"StructuralStateLayout  <code>dataclass</code>","text":"<pre><code>StructuralStateLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = white, filter_: NoneValueType | Filter | None = None, height: float = 16.0, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = 1.25, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 50.0)\n</code></pre> <p>               Bases: <code>_SimpleNodeMixin</code>, <code>CellDesignerNode</code></p> <p>Structural state layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'068362cf-a02c-4828-8b55-eb56cb940550'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>16.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The stroke width of the node</p> <code>1.25</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>50.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>angle</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Return the east anchor of the node.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Return the label center anchor of the node.</p> <code>north</code> <p>Return the north anchor of the node.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Return the south anchor of the node.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Return the west anchor of the node.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.StructuralStateLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.StructuralStateLayout.angle","title":"angle","text":"<pre><code>angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.StructuralStateLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.StructuralStateLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.StructuralStateLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.StructuralStateLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.StructuralStateLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.StructuralStateLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.StructuralStateLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.StructuralStateLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.StructuralStateLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Return the east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Return the east anchor of the node.\"\"\"\n    return self.own_angle(0)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.StructuralStateLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.StructuralStateLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.StructuralStateLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.StructuralStateLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.StructuralStateLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Return the label center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Return the label center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.StructuralStateLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Return the north anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Return the north anchor of the node.\"\"\"\n    return self.own_angle(90)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.StructuralStateLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.StructuralStateLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.StructuralStateLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.StructuralStateLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.StructuralStateLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.StructuralStateLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.StructuralStateLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.StructuralStateLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.StructuralStateLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.StructuralStateLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.StructuralStateLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.StructuralStateLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Return the south anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Return the south anchor of the node.\"\"\"\n    return self.own_angle(270)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.StructuralStateLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.StructuralStateLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.StructuralStateLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.StructuralStateLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.StructuralStateLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.StructuralStateLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Return the west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Return the west anchor of the node.\"\"\"\n    return self.own_angle(180)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.StructuralStateLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.StructuralStateLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.StructuralStateLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.StructuralStateLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.Transcription","title":"Transcription  <code>dataclass</code>","text":"<pre><code>Transcription(*, id_: str = make_uuid4_as_str(), name: str | None = None, sbo_term: str | None = None, metaid: str | None = None, reversible: bool = False, compartment: Compartment | None = None, reactants: frozenset[Reactant] = frozenset(), products: frozenset[Product] = frozenset(), modifiers: frozenset[KnownOrUnknownModulator] = frozenset(), has_external_source: bool = False, has_external_sink: bool = False)\n</code></pre> <p>               Bases: <code>Reaction</code></p> <p>Transcription.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'f0b148d0-5aca-42fd-9599-42036ab34c95'</code> <code>name</code> <code>str | None</code> <p>Human-readable name of the element.</p> <code>None</code> <code>sbo_term</code> <code>str | None</code> <p>Optional SBO term identifier for semantic annotation.</p> <code>None</code> <code>metaid</code> <code>str | None</code> <p>Optional metadata identifier for RDF annotations.</p> <code>None</code> <code>reversible</code> <code>bool</code> <p>Whether the reaction can proceed in both directions.</p> <code>False</code> <code>compartment</code> <code>Compartment | None</code> <p>Optional compartment where the reaction occurs.</p> <code>None</code> <code>reactants</code> <code>frozenset[Reactant]</code> <p>The reactants of the reaction</p> <code>&lt;dynamic&gt;</code> <code>products</code> <code>frozenset[Product]</code> <p>The products of the reaction</p> <code>&lt;dynamic&gt;</code> <code>modifiers</code> <code>frozenset[KnownOrUnknownModulator]</code> <p>The modifiers of the reaction</p> <code>&lt;dynamic&gt;</code> <code>has_external_source</code> <code>bool</code> <p>Whether the reaction has an unspecified external source (a Degraded reactant in CellDesigner).</p> <code>False</code> <code>has_external_sink</code> <code>bool</code> <p>Whether the reaction has an unspecified external sink (a Degraded product in CellDesigner).</p> <code>False</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.Transcription.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TranscriptionLayout","title":"TranscriptionLayout  <code>dataclass</code>","text":"<pre><code>TranscriptionLayout(*, left_connector_fraction: float = 0.375, right_connector_fraction: float = 0.625, reaction_node_font_family: str = DEFAULT_FONT_FAMILY, reaction_node_font_fill: Color | NoneValueType = black, reaction_node_font_stroke: Color | NoneValueType = NoneValue, reaction_node_font_style: FontStyle = NORMAL, reaction_node_font_weight: FontWeight | float = NORMAL, reaction_node_text: str | None = None, reaction_node_height: float = 10.0, reaction_node_width: float = 10.0, reaction_node_segment: int = 1, reaction_node_stroke: NoneValueType | Color | None = black, reaction_node_stroke_width: float | None = 1.0, reaction_node_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, reaction_node_stroke_dashoffset: float | None = None, reaction_node_fill: NoneValueType | Color | None = white, reaction_node_transform: NoneValueType | tuple[Transformation, ...] | None = None, reaction_node_filter: NoneValueType | Filter | None = None, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, end_shorten: float = 2.0, fill: NoneValueType | Color | None = None, filter_: NoneValueType | Filter | None = None, path_fill: NoneValueType | Color | None = NoneValue, path_filter: NoneValueType | Filter | None = None, path_stroke: NoneValueType | Color | None = black, path_stroke_dasharray: NoneValueType | tuple[float, ...] | None = (12, 4, 2, 4, 2, 4), path_stroke_dashoffset: NoneValueType | float | None = None, path_stroke_width: float | None = 1.0, path_transform: NoneValueType | tuple[Transformation, ...] | None = None, stroke: NoneValueType | Color | None = None, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: NoneValueType | float | None = None, segments: tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...] = tuple(), source: LayoutElement | None = None, start_shorten: float = 2.0, target: LayoutElement | None = None, transform: NoneValueType | tuple[Transformation, ...] | None = None, end_arrowhead_fill: NoneValueType | Color | None = black, end_arrowhead_filter: NoneValueType | Filter | None = None, end_arrowhead_stroke: NoneValueType | Color | None = black, end_arrowhead_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, end_arrowhead_stroke_dashoffset: float | None = None, end_arrowhead_stroke_width: float | None = 1.0, end_arrowhead_transform: NoneValueType | tuple[Transformation, ...] | None = None, start_arrowhead_fill: NoneValueType | Color | None = black, start_arrowhead_filter: NoneValueType | Filter | None = None, start_arrowhead_stroke: NoneValueType | Color | None = black, start_arrowhead_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, start_arrowhead_stroke_dashoffset: float | None = None, start_arrowhead_stroke_width: float | None = 1.0, start_arrowhead_transform: NoneValueType | tuple[Transformation, ...] | None = None, reversible: bool = False, end_arrowhead_height: float = 8.0, end_arrowhead_width: float = 15.0, start_arrowhead_height: float = 8.0, start_arrowhead_width: float = 15.0)\n</code></pre> <p>               Bases: <code>ReactionLayout</code>, <code>_ReactionNodeMixin</code></p> <p>Transcription layout.</p> <p>Parameters:</p> Name Type Description Default <code>left_connector_fraction</code> <code>float</code> <p>The fraction along the segment where the left connector attaches.</p> <code>0.375</code> <code>right_connector_fraction</code> <code>float</code> <p>The fraction along the segment where the right connector attaches.</p> <code>0.625</code> <code>reaction_node_font_family</code> <code>str</code> <p>The font family of the reaction node label.</p> <code>'DejaVu Sans'</code> <code>reaction_node_font_fill</code> <code>Color | NoneValueType</code> <p>The fill color of the reaction node label.</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>reaction_node_font_stroke</code> <code>Color | NoneValueType</code> <p>The stroke color of the reaction node label.</p> <code>&lt;momapy.drawing.NoneValueType object at 0x7f8b17323e00&gt;</code> <code>reaction_node_font_style</code> <code>FontStyle</code> <p>The font style of the reaction node label.</p> <code>&lt;FontStyle.NORMAL: 0&gt;</code> <code>reaction_node_font_weight</code> <code>FontWeight | float</code> <p>The font weight of the reaction node label.</p> <code>&lt;FontWeight.NORMAL: 0&gt;</code> <code>reaction_node_text</code> <code>str | None</code> <p>The text of the reaction node label.</p> <code>None</code> <code>reaction_node_height</code> <code>float</code> <p>The height of the reaction node.</p> <code>10.0</code> <code>reaction_node_width</code> <code>float</code> <p>The width of the reaction node.</p> <code>10.0</code> <code>reaction_node_segment</code> <code>int</code> <p>The index of the arc segment carrying the reaction node.</p> <code>1</code> <code>reaction_node_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the reaction node.</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>reaction_node_stroke_width</code> <code>float | None</code> <p>The stroke width of the reaction node.</p> <code>1.0</code> <code>reaction_node_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The dash pattern of the reaction node's border.</p> <code>None</code> <code>reaction_node_stroke_dashoffset</code> <code>float | None</code> <p>The dash offset of the reaction node's border.</p> <code>None</code> <code>reaction_node_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the reaction node.</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>reaction_node_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform applied to the reaction node.</p> <code>None</code> <code>reaction_node_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter applied to the reaction node.</p> <code>None</code> <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'79d08fb8-76c1-4df7-a443-80aeeb1035c4'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>end_shorten</code> <code>float</code> <p>The length the end of the arc will be shorten by</p> <code>2.0</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the arc</p> <code>None</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the arc</p> <code>None</code> <code>path_fill</code> <code>NoneValueType | Color | None</code> <p>The path fill color of the arc</p> <code>&lt;momapy.drawing.NoneValueType object at 0x7f8b17323e00&gt;</code> <code>path_filter</code> <code>NoneValueType | Filter | None</code> <p>The path filter of the arc</p> <code>None</code> <code>path_stroke</code> <code>NoneValueType | Color | None</code> <p>The path stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>path_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The path stroke dasharray of the arc</p> <code>(12, 4, 2, 4, 2, 4)</code> <code>path_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The path stroke dashoffset of the arc</p> <code>None</code> <code>path_stroke_width</code> <code>float | None</code> <p>The path stroke width of the arc</p> <code>1.0</code> <code>path_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The path transform of the arc</p> <code>None</code> <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the arc</p> <code>None</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the arc</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the arc</p> <code>None</code> <code>stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the arc</p> <code>None</code> <code>segments</code> <code>tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...]</code> <p>The path segments of the arc</p> <code>&lt;dynamic&gt;</code> <code>source</code> <code>LayoutElement | None</code> <p>The source of the arc</p> <code>None</code> <code>start_shorten</code> <code>float</code> <p>The length the start of the arc will be shorten by</p> <code>2.0</code> <code>target</code> <code>LayoutElement | None</code> <p>The target of the arc</p> <code>None</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the arc</p> <code>None</code> <code>end_arrowhead_fill</code> <code>NoneValueType | Color | None</code> <p>The end arrowhead fill color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>end_arrowhead_filter</code> <code>NoneValueType | Filter | None</code> <p>The end arrowhead filter of the arc</p> <code>None</code> <code>end_arrowhead_stroke</code> <code>NoneValueType | Color | None</code> <p>The end arrowhead stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>end_arrowhead_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The end arrowhead stroke dasharray of the arc</p> <code>None</code> <code>end_arrowhead_stroke_dashoffset</code> <code>float | None</code> <p>The end arrowhead stroke dashoffset of the arc</p> <code>None</code> <code>end_arrowhead_stroke_width</code> <code>float | None</code> <p>The end arrowhead stroke width of the arc</p> <code>1.0</code> <code>end_arrowhead_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The end arrowhead transform of the arc</p> <code>None</code> <code>start_arrowhead_fill</code> <code>NoneValueType | Color | None</code> <p>The start arrowhead fill color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>start_arrowhead_filter</code> <code>NoneValueType | Filter | None</code> <p>The start arrowhead filter of the arc</p> <code>None</code> <code>start_arrowhead_stroke</code> <code>NoneValueType | Color | None</code> <p>The start arrowhead stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>start_arrowhead_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The start arrowhead stroke dasharray of the arc</p> <code>None</code> <code>start_arrowhead_stroke_dashoffset</code> <code>float | None</code> <p>The start arrowhead stroke dashoffset of the arc</p> <code>None</code> <code>start_arrowhead_stroke_width</code> <code>float | None</code> <p>The start arrowhead stroke width of the arc</p> <code>1.0</code> <code>start_arrowhead_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The start arrowhead transform of the arc</p> <code>None</code> <code>reversible</code> <code>bool</code> <p>Whether the reaction is reversible.</p> <code>False</code> <code>end_arrowhead_height</code> <code>float</code> <p>The height of the end arrowhead.</p> <code>8.0</code> <code>end_arrowhead_width</code> <code>float</code> <p>The width of the end arrowhead.</p> <code>15.0</code> <code>start_arrowhead_height</code> <code>float</code> <p>The height of the start arrowhead.</p> <code>8.0</code> <code>start_arrowhead_width</code> <code>float</code> <p>The width of the start arrowhead.</p> <code>15.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>childless</code> <p>Return a copy of the arc with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>end_arrowhead_base</code> <p>Return the base anchor point of the double-headed arc end arrowhead.</p> <code>end_arrowhead_bbox</code> <p>Return the bounding box of the double-headed arc end arrowhead.</p> <code>end_arrowhead_border</code> <p>Return the point at the intersection of the drawing elements of the double-headed arc end arrowhead and the line going through the center of these drawing elements and the given point.</p> <code>end_arrowhead_drawing_elements</code> <p>Return the drawing elements of the double-headed arc end arrowhead.</p> <code>end_arrowhead_length</code> <p>Return the length of the double-headed arc end arrowhead.</p> <code>end_arrowhead_tip</code> <p>Return the tip anchor point of the double-headed arc end arrowhead.</p> <code>end_point</code> <p>Return the ending point of the arc.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>fraction</code> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <code>length</code> <p>Return the total length of the arc path.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_children</code> <p>Return the self children of the arc.</p> <code>own_drawing_elements</code> <p>Return the arc's own drawing elements, including those of its mixins.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>path_drawing_elements</code> <p>Return the drawing elements of the double-headed arc path.</p> <code>points</code> <p>Return the points of the arc path.</p> <code>start_arrowhead_base</code> <p>Return the base anchor point of the double-headed arc start arrowhead.</p> <code>start_arrowhead_bbox</code> <p>Return the bounding box of the double-headed arc start arrowhead.</p> <code>start_arrowhead_border</code> <p>Return the point at the intersection of the drawing elements of the double-headed arc start arrowhead and the line going through the center of these drawing elements and the given point.</p> <code>start_arrowhead_drawing_elements</code> <p>Return the drawing elements of the double-headed arc start arrowhead.</p> <code>start_arrowhead_length</code> <p>Return the length of the double-headed arc start arrowhead.</p> <code>start_arrowhead_tip</code> <p>Return the tip anchor point of the double-headed arc start arrowhead.</p> <code>start_point</code> <p>Return the starting point of the arc.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TranscriptionLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TranscriptionLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TranscriptionLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the arc with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the arc with no children.\"\"\"\n    return dataclasses.replace(self, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TranscriptionLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TranscriptionLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TranscriptionLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TranscriptionLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TranscriptionLayout.end_arrowhead_base","title":"end_arrowhead_base","text":"<pre><code>end_arrowhead_base() -&gt; Point\n</code></pre> <p>Return the base anchor point of the double-headed arc end arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_arrowhead_base(self) -&gt; Point:\n    \"\"\"Return the base anchor point of the double-headed arc end arrowhead.\"\"\"\n    arrowhead_length = self.end_arrowhead_length()\n    if arrowhead_length == 0:\n        return self.end_point()\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return self.end_arrowhead_tip() - (arrowhead_length, 0)\n    fraction = 1 - (arrowhead_length + self.end_shorten) / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TranscriptionLayout.end_arrowhead_bbox","title":"end_arrowhead_bbox","text":"<pre><code>end_arrowhead_bbox() -&gt; Bbox\n</code></pre> <p>Return the bounding box of the double-headed arc end arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_arrowhead_bbox(self) -&gt; Bbox:\n    \"\"\"Return the bounding box of the double-headed arc end arrowhead.\"\"\"\n    return get_drawing_elements_bbox(self.end_arrowhead_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TranscriptionLayout.end_arrowhead_border","title":"end_arrowhead_border","text":"<pre><code>end_arrowhead_border(point: Point) -&gt; Point\n</code></pre> <p>Return the point at the intersection of the drawing elements of the double-headed arc end arrowhead and the line going through the center of these drawing elements and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_arrowhead_border(self, point: Point) -&gt; Point:\n    \"\"\"Return the point at the intersection of the drawing elements of the double-headed arc end arrowhead and the line going through the center of these drawing elements and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    point = get_drawing_elements_border(\n        self.end_arrowhead_drawing_elements(), point\n    )\n    if point is None:\n        return self.end_arrowhead_tip()\n    return point\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TranscriptionLayout.end_arrowhead_drawing_elements","title":"end_arrowhead_drawing_elements","text":"<pre><code>end_arrowhead_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the double-headed arc end arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_arrowhead_drawing_elements(\n    self,\n) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the double-headed arc end arrowhead.\"\"\"\n    drawing_elements = self._end_arrowhead_border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_end_arrowhead\",\n        elements=tuple(drawing_elements),\n        fill=self.end_arrowhead_fill,\n        filter_=self.end_arrowhead_filter,\n        id_=f\"{self.id_}_end_arrowhead\",\n        stroke=self.end_arrowhead_stroke,\n        stroke_width=self.end_arrowhead_stroke_width,\n        stroke_dasharray=self.end_arrowhead_stroke_dasharray,\n        stroke_dashoffset=self.end_arrowhead_stroke_dashoffset,\n        transform=self.end_arrowhead_transform,\n    )\n    transformation = self._get_end_arrowhead_transformation()\n    group = group.transformed(transformation)\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TranscriptionLayout.end_arrowhead_length","title":"end_arrowhead_length","text":"<pre><code>end_arrowhead_length() -&gt; float\n</code></pre> <p>Return the length of the double-headed arc end arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_arrowhead_length(self) -&gt; float:\n    \"\"\"Return the length of the double-headed arc end arrowhead.\"\"\"\n    bbox = get_drawing_elements_bbox(self._end_arrowhead_border_drawing_elements())\n    if math.isnan(bbox.width):\n        return 0.0\n    return bbox.east().x\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TranscriptionLayout.end_arrowhead_tip","title":"end_arrowhead_tip","text":"<pre><code>end_arrowhead_tip() -&gt; Point\n</code></pre> <p>Return the tip anchor point of the double-headed arc end arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_arrowhead_tip(self) -&gt; Point:\n    \"\"\"Return the tip anchor point of the double-headed arc end arrowhead.\"\"\"\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return segment.p2\n    fraction = 1 - self.end_shorten / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TranscriptionLayout.end_point","title":"end_point","text":"<pre><code>end_point() -&gt; Point\n</code></pre> <p>Return the ending point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_point(self) -&gt; Point:\n    \"\"\"Return the ending point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[-1]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TranscriptionLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TranscriptionLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TranscriptionLayout.fraction","title":"fraction","text":"<pre><code>fraction(fraction: float) -&gt; tuple[Point, float]\n</code></pre> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def fraction(self, fraction: float) -&gt; tuple[Point, float]:\n    \"\"\"Return the position and angle on the arc at a given fraction (of the total arc length).\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    current_length = 0\n    length_to_reach = fraction * self.length()\n    for segment in self.segments:\n        current_length += segment.length()\n        if current_length &gt;= length_to_reach:\n            break\n    segment_start_length = current_length - segment.length()\n    segment_fraction = (length_to_reach - segment_start_length) / segment.length()\n    position, angle = segment.get_position_and_angle_at_fraction(segment_fraction)\n    return position, angle\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TranscriptionLayout.length","title":"length","text":"<pre><code>length() -&gt; float\n</code></pre> <p>Return the total length of the arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def length(self) -&gt; float:\n    \"\"\"Return the total length of the arc path.\"\"\"\n    return sum([segment.length() for segment in self.segments])\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TranscriptionLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TranscriptionLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the arc.\"\"\"\n    return []\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TranscriptionLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the arc's own drawing elements, including those of its mixins.</p> Source code in <code>src/momapy/celldesigner/elements.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the arc's own drawing elements, including those of its mixins.\"\"\"\n    drawing_elements = super_or_builder(\n        CellDesignerDoubleHeadedArc, self\n    ).own_drawing_elements()\n    done_bases = []\n    for base in type(self).__mro__:\n        if (\n            issubclass_or_builder(base, _SBGNMixin)\n            and base is not type(self)\n            and not any([issubclass(done_base, base) for done_base in done_bases])\n        ):\n            drawing_elements += getattr(base, \"_mixin_drawing_elements\")(self)\n            done_bases.append(base)\n    return drawing_elements\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TranscriptionLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TranscriptionLayout.path_drawing_elements","title":"path_drawing_elements","text":"<pre><code>path_drawing_elements() -&gt; list[Path]\n</code></pre> <p>Return the drawing elements of the double-headed arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def path_drawing_elements(self) -&gt; list[Path]:\n    \"\"\"Return the drawing elements of the double-headed arc path.\"\"\"\n    start_arrowhead_length = self.start_arrowhead_length()\n    end_arrowhead_length = self.end_arrowhead_length()\n    if len(self.segments) == 1:\n        segment = (\n            self.segments[0]\n            .shortened(self.start_shorten + start_arrowhead_length, \"start\")\n            .shortened(self.end_shorten + end_arrowhead_length, \"end\")\n        )\n        actions = [\n            MoveTo(segment.p1),\n            self._make_path_action_from_segment(segment),\n        ]\n    else:\n        first_segment = self.segments[0].shortened(\n            self.start_shorten + start_arrowhead_length, \"start\"\n        )\n        last_segment = self.segments[-1].shortened(\n            self.end_shorten + end_arrowhead_length, \"end\"\n        )\n        actions = [\n            MoveTo(first_segment.p1),\n            self._make_path_action_from_segment(first_segment),\n        ]\n        for segment in self.segments[1:-1]:\n            action = self._make_path_action_from_segment(segment)\n            actions.append(action)\n        actions.append(self._make_path_action_from_segment(last_segment))\n    path = Path(\n        actions=tuple(actions),\n        class_=f\"{type(self).__name__}_path\",\n        fill=self.path_fill,\n        filter_=self.path_filter,\n        id_=f\"{self.id_}_path\",\n        stroke=self.path_stroke,\n        stroke_dasharray=self.path_stroke_dasharray,\n        stroke_dashoffset=self.path_stroke_dashoffset,\n        stroke_width=self.path_stroke_width,\n        transform=self.path_transform,\n    )\n    return [path]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TranscriptionLayout.points","title":"points","text":"<pre><code>points() -&gt; list[Point]\n</code></pre> <p>Return the points of the arc path.</p> <p>An arc with no segments has no points and returns an empty list.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def points(self) -&gt; list[Point]:\n    \"\"\"Return the points of the arc path.\n\n    An arc with no segments has no points and returns an empty list.\n    \"\"\"\n    if not self.segments:\n        return []\n    points = []\n    for segment in self.segments:\n        points.append(segment.p1)\n    points.append(segment.p2)\n    return points\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TranscriptionLayout.start_arrowhead_base","title":"start_arrowhead_base","text":"<pre><code>start_arrowhead_base() -&gt; Point\n</code></pre> <p>Return the base anchor point of the double-headed arc start arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_arrowhead_base(self) -&gt; Point:\n    \"\"\"Return the base anchor point of the double-headed arc start arrowhead.\"\"\"\n    arrowhead_length = self.start_arrowhead_length()\n    if arrowhead_length == 0:\n        return self.start_point()\n    segment = self.segments[0]\n    segment = Segment(segment.p2, segment.p1)\n    segment_length = segment.length()\n    if segment_length == 0:\n        return self.start_arrowhead_tip() + (arrowhead_length, 0)\n    fraction = 1 - (arrowhead_length + self.start_shorten) / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TranscriptionLayout.start_arrowhead_bbox","title":"start_arrowhead_bbox","text":"<pre><code>start_arrowhead_bbox() -&gt; Bbox\n</code></pre> <p>Return the bounding box of the double-headed arc start arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_arrowhead_bbox(self) -&gt; Bbox:\n    \"\"\"Return the bounding box of the double-headed arc start arrowhead.\"\"\"\n    return get_drawing_elements_bbox(self.start_arrowhead_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TranscriptionLayout.start_arrowhead_border","title":"start_arrowhead_border","text":"<pre><code>start_arrowhead_border(point: Point) -&gt; Point\n</code></pre> <p>Return the point at the intersection of the drawing elements of the double-headed arc start arrowhead and the line going through the center of these drawing elements and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_arrowhead_border(self, point: Point) -&gt; Point:\n    \"\"\"Return the point at the intersection of the drawing elements of the double-headed arc start arrowhead and the line going through the center of these drawing elements and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    point = get_drawing_elements_border(\n        self.start_arrowhead_drawing_elements(), point\n    )\n    if point is None:\n        return self.start_arrowhead_tip()\n    return point\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TranscriptionLayout.start_arrowhead_drawing_elements","title":"start_arrowhead_drawing_elements","text":"<pre><code>start_arrowhead_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the double-headed arc start arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_arrowhead_drawing_elements(\n    self,\n) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the double-headed arc start arrowhead.\"\"\"\n    drawing_elements = self._start_arrowhead_border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_start_arrowhead\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}_start_arrowhead\",\n        fill=self.start_arrowhead_fill,\n        filter_=self.start_arrowhead_filter,\n        stroke=self.start_arrowhead_stroke,\n        stroke_dasharray=self.start_arrowhead_stroke_dasharray,\n        stroke_dashoffset=self.start_arrowhead_stroke_dashoffset,\n        stroke_width=self.start_arrowhead_stroke_width,\n        transform=self.start_arrowhead_transform,\n    )\n    transformation = self._get_start_arrowhead_transformation()\n    group = group.transformed(transformation)\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TranscriptionLayout.start_arrowhead_length","title":"start_arrowhead_length","text":"<pre><code>start_arrowhead_length() -&gt; float\n</code></pre> <p>Return the length of the double-headed arc start arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_arrowhead_length(self) -&gt; float:\n    \"\"\"Return the length of the double-headed arc start arrowhead.\"\"\"\n    bbox = get_drawing_elements_bbox(\n        self._start_arrowhead_border_drawing_elements()\n    )\n    if math.isnan(bbox.width):\n        return 0.0\n    return abs(bbox.west().x)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TranscriptionLayout.start_arrowhead_tip","title":"start_arrowhead_tip","text":"<pre><code>start_arrowhead_tip() -&gt; Point\n</code></pre> <p>Return the tip anchor point of the double-headed arc start arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_arrowhead_tip(self) -&gt; Point:\n    \"\"\"Return the tip anchor point of the double-headed arc start arrowhead.\"\"\"\n    segment = self.segments[0]\n    segment = Segment(segment.p2, segment.p1)\n    segment_length = segment.length()\n    if segment_length == 0:\n        return segment.p2\n    fraction = 1 - self.start_shorten / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TranscriptionLayout.start_point","title":"start_point","text":"<pre><code>start_point() -&gt; Point\n</code></pre> <p>Return the starting point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_point(self) -&gt; Point:\n    \"\"\"Return the starting point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[0]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TranscriptionLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TranscriptionStartingSiteL","title":"TranscriptionStartingSiteL  <code>dataclass</code>","text":"<pre><code>TranscriptionStartingSiteL(*, id_: str = make_uuid4_as_str(), name: str | None = None, active: bool = False)\n</code></pre> <p>               Bases: <code>Region</code></p> <p>Transcription starting site L.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'9cbabc6e-06a1-44fc-a361-9a84b572be6d'</code> <code>name</code> <code>str | None</code> <p>The name of the region</p> <code>None</code> <code>active</code> <code>bool</code> <p>Whether the region is active or not</p> <code>False</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TranscriptionStartingSiteL.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TranscriptionStartingSiteR","title":"TranscriptionStartingSiteR  <code>dataclass</code>","text":"<pre><code>TranscriptionStartingSiteR(*, id_: str = make_uuid4_as_str(), name: str | None = None, active: bool = False)\n</code></pre> <p>               Bases: <code>Region</code></p> <p>Transcription starting site R.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'eef1cb8e-52ea-410d-95f2-fb4e80cb7838'</code> <code>name</code> <code>str | None</code> <p>The name of the region</p> <code>None</code> <code>active</code> <code>bool</code> <p>Whether the region is active or not</p> <code>False</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TranscriptionStartingSiteR.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.Translation","title":"Translation  <code>dataclass</code>","text":"<pre><code>Translation(*, id_: str = make_uuid4_as_str(), name: str | None = None, sbo_term: str | None = None, metaid: str | None = None, reversible: bool = False, compartment: Compartment | None = None, reactants: frozenset[Reactant] = frozenset(), products: frozenset[Product] = frozenset(), modifiers: frozenset[KnownOrUnknownModulator] = frozenset(), has_external_source: bool = False, has_external_sink: bool = False)\n</code></pre> <p>               Bases: <code>Reaction</code></p> <p>Translation.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'0c386d07-34bf-4247-87a0-f83ac4dc0e12'</code> <code>name</code> <code>str | None</code> <p>Human-readable name of the element.</p> <code>None</code> <code>sbo_term</code> <code>str | None</code> <p>Optional SBO term identifier for semantic annotation.</p> <code>None</code> <code>metaid</code> <code>str | None</code> <p>Optional metadata identifier for RDF annotations.</p> <code>None</code> <code>reversible</code> <code>bool</code> <p>Whether the reaction can proceed in both directions.</p> <code>False</code> <code>compartment</code> <code>Compartment | None</code> <p>Optional compartment where the reaction occurs.</p> <code>None</code> <code>reactants</code> <code>frozenset[Reactant]</code> <p>The reactants of the reaction</p> <code>&lt;dynamic&gt;</code> <code>products</code> <code>frozenset[Product]</code> <p>The products of the reaction</p> <code>&lt;dynamic&gt;</code> <code>modifiers</code> <code>frozenset[KnownOrUnknownModulator]</code> <p>The modifiers of the reaction</p> <code>&lt;dynamic&gt;</code> <code>has_external_source</code> <code>bool</code> <p>Whether the reaction has an unspecified external source (a Degraded reactant in CellDesigner).</p> <code>False</code> <code>has_external_sink</code> <code>bool</code> <p>Whether the reaction has an unspecified external sink (a Degraded product in CellDesigner).</p> <code>False</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.Translation.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TranslationLayout","title":"TranslationLayout  <code>dataclass</code>","text":"<pre><code>TranslationLayout(*, left_connector_fraction: float = 0.375, right_connector_fraction: float = 0.625, reaction_node_font_family: str = DEFAULT_FONT_FAMILY, reaction_node_font_fill: Color | NoneValueType = black, reaction_node_font_stroke: Color | NoneValueType = NoneValue, reaction_node_font_style: FontStyle = NORMAL, reaction_node_font_weight: FontWeight | float = NORMAL, reaction_node_text: str | None = None, reaction_node_height: float = 10.0, reaction_node_width: float = 10.0, reaction_node_segment: int = 1, reaction_node_stroke: NoneValueType | Color | None = black, reaction_node_stroke_width: float | None = 1.0, reaction_node_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, reaction_node_stroke_dashoffset: float | None = None, reaction_node_fill: NoneValueType | Color | None = white, reaction_node_transform: NoneValueType | tuple[Transformation, ...] | None = None, reaction_node_filter: NoneValueType | Filter | None = None, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, end_shorten: float = 2.0, fill: NoneValueType | Color | None = None, filter_: NoneValueType | Filter | None = None, path_fill: NoneValueType | Color | None = NoneValue, path_filter: NoneValueType | Filter | None = None, path_stroke: NoneValueType | Color | None = black, path_stroke_dasharray: NoneValueType | tuple[float, ...] | None = (12, 4, 2, 4), path_stroke_dashoffset: NoneValueType | float | None = None, path_stroke_width: float | None = 1.0, path_transform: NoneValueType | tuple[Transformation, ...] | None = None, stroke: NoneValueType | Color | None = None, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: NoneValueType | float | None = None, segments: tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...] = tuple(), source: LayoutElement | None = None, start_shorten: float = 2.0, target: LayoutElement | None = None, transform: NoneValueType | tuple[Transformation, ...] | None = None, end_arrowhead_fill: NoneValueType | Color | None = black, end_arrowhead_filter: NoneValueType | Filter | None = None, end_arrowhead_stroke: NoneValueType | Color | None = black, end_arrowhead_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, end_arrowhead_stroke_dashoffset: float | None = None, end_arrowhead_stroke_width: float | None = 1.0, end_arrowhead_transform: NoneValueType | tuple[Transformation, ...] | None = None, start_arrowhead_fill: NoneValueType | Color | None = black, start_arrowhead_filter: NoneValueType | Filter | None = None, start_arrowhead_stroke: NoneValueType | Color | None = black, start_arrowhead_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, start_arrowhead_stroke_dashoffset: float | None = None, start_arrowhead_stroke_width: float | None = 1.0, start_arrowhead_transform: NoneValueType | tuple[Transformation, ...] | None = None, reversible: bool = False, end_arrowhead_height: float = 8.0, end_arrowhead_width: float = 15.0, start_arrowhead_height: float = 8.0, start_arrowhead_width: float = 15.0)\n</code></pre> <p>               Bases: <code>ReactionLayout</code>, <code>_ReactionNodeMixin</code></p> <p>Translation layout.</p> <p>Parameters:</p> Name Type Description Default <code>left_connector_fraction</code> <code>float</code> <p>The fraction along the segment where the left connector attaches.</p> <code>0.375</code> <code>right_connector_fraction</code> <code>float</code> <p>The fraction along the segment where the right connector attaches.</p> <code>0.625</code> <code>reaction_node_font_family</code> <code>str</code> <p>The font family of the reaction node label.</p> <code>'DejaVu Sans'</code> <code>reaction_node_font_fill</code> <code>Color | NoneValueType</code> <p>The fill color of the reaction node label.</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>reaction_node_font_stroke</code> <code>Color | NoneValueType</code> <p>The stroke color of the reaction node label.</p> <code>&lt;momapy.drawing.NoneValueType object at 0x7f8b17323e00&gt;</code> <code>reaction_node_font_style</code> <code>FontStyle</code> <p>The font style of the reaction node label.</p> <code>&lt;FontStyle.NORMAL: 0&gt;</code> <code>reaction_node_font_weight</code> <code>FontWeight | float</code> <p>The font weight of the reaction node label.</p> <code>&lt;FontWeight.NORMAL: 0&gt;</code> <code>reaction_node_text</code> <code>str | None</code> <p>The text of the reaction node label.</p> <code>None</code> <code>reaction_node_height</code> <code>float</code> <p>The height of the reaction node.</p> <code>10.0</code> <code>reaction_node_width</code> <code>float</code> <p>The width of the reaction node.</p> <code>10.0</code> <code>reaction_node_segment</code> <code>int</code> <p>The index of the arc segment carrying the reaction node.</p> <code>1</code> <code>reaction_node_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the reaction node.</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>reaction_node_stroke_width</code> <code>float | None</code> <p>The stroke width of the reaction node.</p> <code>1.0</code> <code>reaction_node_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The dash pattern of the reaction node's border.</p> <code>None</code> <code>reaction_node_stroke_dashoffset</code> <code>float | None</code> <p>The dash offset of the reaction node's border.</p> <code>None</code> <code>reaction_node_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the reaction node.</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>reaction_node_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform applied to the reaction node.</p> <code>None</code> <code>reaction_node_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter applied to the reaction node.</p> <code>None</code> <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'911e532c-c886-4d52-a6be-81fa2933a719'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>end_shorten</code> <code>float</code> <p>The length the end of the arc will be shorten by</p> <code>2.0</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the arc</p> <code>None</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the arc</p> <code>None</code> <code>path_fill</code> <code>NoneValueType | Color | None</code> <p>The path fill color of the arc</p> <code>&lt;momapy.drawing.NoneValueType object at 0x7f8b17323e00&gt;</code> <code>path_filter</code> <code>NoneValueType | Filter | None</code> <p>The path filter of the arc</p> <code>None</code> <code>path_stroke</code> <code>NoneValueType | Color | None</code> <p>The path stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>path_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The path stroke dasharray of the arc</p> <code>(12, 4, 2, 4)</code> <code>path_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The path stroke dashoffset of the arc</p> <code>None</code> <code>path_stroke_width</code> <code>float | None</code> <p>The path stroke width of the arc</p> <code>1.0</code> <code>path_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The path transform of the arc</p> <code>None</code> <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the arc</p> <code>None</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the arc</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the arc</p> <code>None</code> <code>stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the arc</p> <code>None</code> <code>segments</code> <code>tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...]</code> <p>The path segments of the arc</p> <code>&lt;dynamic&gt;</code> <code>source</code> <code>LayoutElement | None</code> <p>The source of the arc</p> <code>None</code> <code>start_shorten</code> <code>float</code> <p>The length the start of the arc will be shorten by</p> <code>2.0</code> <code>target</code> <code>LayoutElement | None</code> <p>The target of the arc</p> <code>None</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the arc</p> <code>None</code> <code>end_arrowhead_fill</code> <code>NoneValueType | Color | None</code> <p>The end arrowhead fill color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>end_arrowhead_filter</code> <code>NoneValueType | Filter | None</code> <p>The end arrowhead filter of the arc</p> <code>None</code> <code>end_arrowhead_stroke</code> <code>NoneValueType | Color | None</code> <p>The end arrowhead stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>end_arrowhead_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The end arrowhead stroke dasharray of the arc</p> <code>None</code> <code>end_arrowhead_stroke_dashoffset</code> <code>float | None</code> <p>The end arrowhead stroke dashoffset of the arc</p> <code>None</code> <code>end_arrowhead_stroke_width</code> <code>float | None</code> <p>The end arrowhead stroke width of the arc</p> <code>1.0</code> <code>end_arrowhead_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The end arrowhead transform of the arc</p> <code>None</code> <code>start_arrowhead_fill</code> <code>NoneValueType | Color | None</code> <p>The start arrowhead fill color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>start_arrowhead_filter</code> <code>NoneValueType | Filter | None</code> <p>The start arrowhead filter of the arc</p> <code>None</code> <code>start_arrowhead_stroke</code> <code>NoneValueType | Color | None</code> <p>The start arrowhead stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>start_arrowhead_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The start arrowhead stroke dasharray of the arc</p> <code>None</code> <code>start_arrowhead_stroke_dashoffset</code> <code>float | None</code> <p>The start arrowhead stroke dashoffset of the arc</p> <code>None</code> <code>start_arrowhead_stroke_width</code> <code>float | None</code> <p>The start arrowhead stroke width of the arc</p> <code>1.0</code> <code>start_arrowhead_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The start arrowhead transform of the arc</p> <code>None</code> <code>reversible</code> <code>bool</code> <p>Whether the reaction is reversible.</p> <code>False</code> <code>end_arrowhead_height</code> <code>float</code> <p>The height of the end arrowhead.</p> <code>8.0</code> <code>end_arrowhead_width</code> <code>float</code> <p>The width of the end arrowhead.</p> <code>15.0</code> <code>start_arrowhead_height</code> <code>float</code> <p>The height of the start arrowhead.</p> <code>8.0</code> <code>start_arrowhead_width</code> <code>float</code> <p>The width of the start arrowhead.</p> <code>15.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>childless</code> <p>Return a copy of the arc with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>end_arrowhead_base</code> <p>Return the base anchor point of the double-headed arc end arrowhead.</p> <code>end_arrowhead_bbox</code> <p>Return the bounding box of the double-headed arc end arrowhead.</p> <code>end_arrowhead_border</code> <p>Return the point at the intersection of the drawing elements of the double-headed arc end arrowhead and the line going through the center of these drawing elements and the given point.</p> <code>end_arrowhead_drawing_elements</code> <p>Return the drawing elements of the double-headed arc end arrowhead.</p> <code>end_arrowhead_length</code> <p>Return the length of the double-headed arc end arrowhead.</p> <code>end_arrowhead_tip</code> <p>Return the tip anchor point of the double-headed arc end arrowhead.</p> <code>end_point</code> <p>Return the ending point of the arc.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>fraction</code> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <code>length</code> <p>Return the total length of the arc path.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_children</code> <p>Return the self children of the arc.</p> <code>own_drawing_elements</code> <p>Return the arc's own drawing elements, including those of its mixins.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>path_drawing_elements</code> <p>Return the drawing elements of the double-headed arc path.</p> <code>points</code> <p>Return the points of the arc path.</p> <code>start_arrowhead_base</code> <p>Return the base anchor point of the double-headed arc start arrowhead.</p> <code>start_arrowhead_bbox</code> <p>Return the bounding box of the double-headed arc start arrowhead.</p> <code>start_arrowhead_border</code> <p>Return the point at the intersection of the drawing elements of the double-headed arc start arrowhead and the line going through the center of these drawing elements and the given point.</p> <code>start_arrowhead_drawing_elements</code> <p>Return the drawing elements of the double-headed arc start arrowhead.</p> <code>start_arrowhead_length</code> <p>Return the length of the double-headed arc start arrowhead.</p> <code>start_arrowhead_tip</code> <p>Return the tip anchor point of the double-headed arc start arrowhead.</p> <code>start_point</code> <p>Return the starting point of the arc.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TranslationLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TranslationLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TranslationLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the arc with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the arc with no children.\"\"\"\n    return dataclasses.replace(self, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TranslationLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TranslationLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TranslationLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TranslationLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TranslationLayout.end_arrowhead_base","title":"end_arrowhead_base","text":"<pre><code>end_arrowhead_base() -&gt; Point\n</code></pre> <p>Return the base anchor point of the double-headed arc end arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_arrowhead_base(self) -&gt; Point:\n    \"\"\"Return the base anchor point of the double-headed arc end arrowhead.\"\"\"\n    arrowhead_length = self.end_arrowhead_length()\n    if arrowhead_length == 0:\n        return self.end_point()\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return self.end_arrowhead_tip() - (arrowhead_length, 0)\n    fraction = 1 - (arrowhead_length + self.end_shorten) / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TranslationLayout.end_arrowhead_bbox","title":"end_arrowhead_bbox","text":"<pre><code>end_arrowhead_bbox() -&gt; Bbox\n</code></pre> <p>Return the bounding box of the double-headed arc end arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_arrowhead_bbox(self) -&gt; Bbox:\n    \"\"\"Return the bounding box of the double-headed arc end arrowhead.\"\"\"\n    return get_drawing_elements_bbox(self.end_arrowhead_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TranslationLayout.end_arrowhead_border","title":"end_arrowhead_border","text":"<pre><code>end_arrowhead_border(point: Point) -&gt; Point\n</code></pre> <p>Return the point at the intersection of the drawing elements of the double-headed arc end arrowhead and the line going through the center of these drawing elements and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_arrowhead_border(self, point: Point) -&gt; Point:\n    \"\"\"Return the point at the intersection of the drawing elements of the double-headed arc end arrowhead and the line going through the center of these drawing elements and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    point = get_drawing_elements_border(\n        self.end_arrowhead_drawing_elements(), point\n    )\n    if point is None:\n        return self.end_arrowhead_tip()\n    return point\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TranslationLayout.end_arrowhead_drawing_elements","title":"end_arrowhead_drawing_elements","text":"<pre><code>end_arrowhead_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the double-headed arc end arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_arrowhead_drawing_elements(\n    self,\n) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the double-headed arc end arrowhead.\"\"\"\n    drawing_elements = self._end_arrowhead_border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_end_arrowhead\",\n        elements=tuple(drawing_elements),\n        fill=self.end_arrowhead_fill,\n        filter_=self.end_arrowhead_filter,\n        id_=f\"{self.id_}_end_arrowhead\",\n        stroke=self.end_arrowhead_stroke,\n        stroke_width=self.end_arrowhead_stroke_width,\n        stroke_dasharray=self.end_arrowhead_stroke_dasharray,\n        stroke_dashoffset=self.end_arrowhead_stroke_dashoffset,\n        transform=self.end_arrowhead_transform,\n    )\n    transformation = self._get_end_arrowhead_transformation()\n    group = group.transformed(transformation)\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TranslationLayout.end_arrowhead_length","title":"end_arrowhead_length","text":"<pre><code>end_arrowhead_length() -&gt; float\n</code></pre> <p>Return the length of the double-headed arc end arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_arrowhead_length(self) -&gt; float:\n    \"\"\"Return the length of the double-headed arc end arrowhead.\"\"\"\n    bbox = get_drawing_elements_bbox(self._end_arrowhead_border_drawing_elements())\n    if math.isnan(bbox.width):\n        return 0.0\n    return bbox.east().x\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TranslationLayout.end_arrowhead_tip","title":"end_arrowhead_tip","text":"<pre><code>end_arrowhead_tip() -&gt; Point\n</code></pre> <p>Return the tip anchor point of the double-headed arc end arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_arrowhead_tip(self) -&gt; Point:\n    \"\"\"Return the tip anchor point of the double-headed arc end arrowhead.\"\"\"\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return segment.p2\n    fraction = 1 - self.end_shorten / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TranslationLayout.end_point","title":"end_point","text":"<pre><code>end_point() -&gt; Point\n</code></pre> <p>Return the ending point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_point(self) -&gt; Point:\n    \"\"\"Return the ending point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[-1]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TranslationLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TranslationLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TranslationLayout.fraction","title":"fraction","text":"<pre><code>fraction(fraction: float) -&gt; tuple[Point, float]\n</code></pre> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def fraction(self, fraction: float) -&gt; tuple[Point, float]:\n    \"\"\"Return the position and angle on the arc at a given fraction (of the total arc length).\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    current_length = 0\n    length_to_reach = fraction * self.length()\n    for segment in self.segments:\n        current_length += segment.length()\n        if current_length &gt;= length_to_reach:\n            break\n    segment_start_length = current_length - segment.length()\n    segment_fraction = (length_to_reach - segment_start_length) / segment.length()\n    position, angle = segment.get_position_and_angle_at_fraction(segment_fraction)\n    return position, angle\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TranslationLayout.length","title":"length","text":"<pre><code>length() -&gt; float\n</code></pre> <p>Return the total length of the arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def length(self) -&gt; float:\n    \"\"\"Return the total length of the arc path.\"\"\"\n    return sum([segment.length() for segment in self.segments])\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TranslationLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TranslationLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the arc.\"\"\"\n    return []\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TranslationLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the arc's own drawing elements, including those of its mixins.</p> Source code in <code>src/momapy/celldesigner/elements.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the arc's own drawing elements, including those of its mixins.\"\"\"\n    drawing_elements = super_or_builder(\n        CellDesignerDoubleHeadedArc, self\n    ).own_drawing_elements()\n    done_bases = []\n    for base in type(self).__mro__:\n        if (\n            issubclass_or_builder(base, _SBGNMixin)\n            and base is not type(self)\n            and not any([issubclass(done_base, base) for done_base in done_bases])\n        ):\n            drawing_elements += getattr(base, \"_mixin_drawing_elements\")(self)\n            done_bases.append(base)\n    return drawing_elements\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TranslationLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TranslationLayout.path_drawing_elements","title":"path_drawing_elements","text":"<pre><code>path_drawing_elements() -&gt; list[Path]\n</code></pre> <p>Return the drawing elements of the double-headed arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def path_drawing_elements(self) -&gt; list[Path]:\n    \"\"\"Return the drawing elements of the double-headed arc path.\"\"\"\n    start_arrowhead_length = self.start_arrowhead_length()\n    end_arrowhead_length = self.end_arrowhead_length()\n    if len(self.segments) == 1:\n        segment = (\n            self.segments[0]\n            .shortened(self.start_shorten + start_arrowhead_length, \"start\")\n            .shortened(self.end_shorten + end_arrowhead_length, \"end\")\n        )\n        actions = [\n            MoveTo(segment.p1),\n            self._make_path_action_from_segment(segment),\n        ]\n    else:\n        first_segment = self.segments[0].shortened(\n            self.start_shorten + start_arrowhead_length, \"start\"\n        )\n        last_segment = self.segments[-1].shortened(\n            self.end_shorten + end_arrowhead_length, \"end\"\n        )\n        actions = [\n            MoveTo(first_segment.p1),\n            self._make_path_action_from_segment(first_segment),\n        ]\n        for segment in self.segments[1:-1]:\n            action = self._make_path_action_from_segment(segment)\n            actions.append(action)\n        actions.append(self._make_path_action_from_segment(last_segment))\n    path = Path(\n        actions=tuple(actions),\n        class_=f\"{type(self).__name__}_path\",\n        fill=self.path_fill,\n        filter_=self.path_filter,\n        id_=f\"{self.id_}_path\",\n        stroke=self.path_stroke,\n        stroke_dasharray=self.path_stroke_dasharray,\n        stroke_dashoffset=self.path_stroke_dashoffset,\n        stroke_width=self.path_stroke_width,\n        transform=self.path_transform,\n    )\n    return [path]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TranslationLayout.points","title":"points","text":"<pre><code>points() -&gt; list[Point]\n</code></pre> <p>Return the points of the arc path.</p> <p>An arc with no segments has no points and returns an empty list.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def points(self) -&gt; list[Point]:\n    \"\"\"Return the points of the arc path.\n\n    An arc with no segments has no points and returns an empty list.\n    \"\"\"\n    if not self.segments:\n        return []\n    points = []\n    for segment in self.segments:\n        points.append(segment.p1)\n    points.append(segment.p2)\n    return points\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TranslationLayout.start_arrowhead_base","title":"start_arrowhead_base","text":"<pre><code>start_arrowhead_base() -&gt; Point\n</code></pre> <p>Return the base anchor point of the double-headed arc start arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_arrowhead_base(self) -&gt; Point:\n    \"\"\"Return the base anchor point of the double-headed arc start arrowhead.\"\"\"\n    arrowhead_length = self.start_arrowhead_length()\n    if arrowhead_length == 0:\n        return self.start_point()\n    segment = self.segments[0]\n    segment = Segment(segment.p2, segment.p1)\n    segment_length = segment.length()\n    if segment_length == 0:\n        return self.start_arrowhead_tip() + (arrowhead_length, 0)\n    fraction = 1 - (arrowhead_length + self.start_shorten) / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TranslationLayout.start_arrowhead_bbox","title":"start_arrowhead_bbox","text":"<pre><code>start_arrowhead_bbox() -&gt; Bbox\n</code></pre> <p>Return the bounding box of the double-headed arc start arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_arrowhead_bbox(self) -&gt; Bbox:\n    \"\"\"Return the bounding box of the double-headed arc start arrowhead.\"\"\"\n    return get_drawing_elements_bbox(self.start_arrowhead_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TranslationLayout.start_arrowhead_border","title":"start_arrowhead_border","text":"<pre><code>start_arrowhead_border(point: Point) -&gt; Point\n</code></pre> <p>Return the point at the intersection of the drawing elements of the double-headed arc start arrowhead and the line going through the center of these drawing elements and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_arrowhead_border(self, point: Point) -&gt; Point:\n    \"\"\"Return the point at the intersection of the drawing elements of the double-headed arc start arrowhead and the line going through the center of these drawing elements and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    point = get_drawing_elements_border(\n        self.start_arrowhead_drawing_elements(), point\n    )\n    if point is None:\n        return self.start_arrowhead_tip()\n    return point\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TranslationLayout.start_arrowhead_drawing_elements","title":"start_arrowhead_drawing_elements","text":"<pre><code>start_arrowhead_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the double-headed arc start arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_arrowhead_drawing_elements(\n    self,\n) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the double-headed arc start arrowhead.\"\"\"\n    drawing_elements = self._start_arrowhead_border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_start_arrowhead\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}_start_arrowhead\",\n        fill=self.start_arrowhead_fill,\n        filter_=self.start_arrowhead_filter,\n        stroke=self.start_arrowhead_stroke,\n        stroke_dasharray=self.start_arrowhead_stroke_dasharray,\n        stroke_dashoffset=self.start_arrowhead_stroke_dashoffset,\n        stroke_width=self.start_arrowhead_stroke_width,\n        transform=self.start_arrowhead_transform,\n    )\n    transformation = self._get_start_arrowhead_transformation()\n    group = group.transformed(transformation)\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TranslationLayout.start_arrowhead_length","title":"start_arrowhead_length","text":"<pre><code>start_arrowhead_length() -&gt; float\n</code></pre> <p>Return the length of the double-headed arc start arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_arrowhead_length(self) -&gt; float:\n    \"\"\"Return the length of the double-headed arc start arrowhead.\"\"\"\n    bbox = get_drawing_elements_bbox(\n        self._start_arrowhead_border_drawing_elements()\n    )\n    if math.isnan(bbox.width):\n        return 0.0\n    return abs(bbox.west().x)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TranslationLayout.start_arrowhead_tip","title":"start_arrowhead_tip","text":"<pre><code>start_arrowhead_tip() -&gt; Point\n</code></pre> <p>Return the tip anchor point of the double-headed arc start arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_arrowhead_tip(self) -&gt; Point:\n    \"\"\"Return the tip anchor point of the double-headed arc start arrowhead.\"\"\"\n    segment = self.segments[0]\n    segment = Segment(segment.p2, segment.p1)\n    segment_length = segment.length()\n    if segment_length == 0:\n        return segment.p2\n    fraction = 1 - self.start_shorten / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TranslationLayout.start_point","title":"start_point","text":"<pre><code>start_point() -&gt; Point\n</code></pre> <p>Return the starting point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_point(self) -&gt; Point:\n    \"\"\"Return the starting point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[0]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TranslationLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.Transport","title":"Transport  <code>dataclass</code>","text":"<pre><code>Transport(*, id_: str = make_uuid4_as_str(), name: str | None = None, sbo_term: str | None = None, metaid: str | None = None, reversible: bool = False, compartment: Compartment | None = None, reactants: frozenset[Reactant] = frozenset(), products: frozenset[Product] = frozenset(), modifiers: frozenset[KnownOrUnknownModulator] = frozenset(), has_external_source: bool = False, has_external_sink: bool = False)\n</code></pre> <p>               Bases: <code>Reaction</code></p> <p>Transport.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'90b28585-3cf7-4cf2-82de-2d7748882945'</code> <code>name</code> <code>str | None</code> <p>Human-readable name of the element.</p> <code>None</code> <code>sbo_term</code> <code>str | None</code> <p>Optional SBO term identifier for semantic annotation.</p> <code>None</code> <code>metaid</code> <code>str | None</code> <p>Optional metadata identifier for RDF annotations.</p> <code>None</code> <code>reversible</code> <code>bool</code> <p>Whether the reaction can proceed in both directions.</p> <code>False</code> <code>compartment</code> <code>Compartment | None</code> <p>Optional compartment where the reaction occurs.</p> <code>None</code> <code>reactants</code> <code>frozenset[Reactant]</code> <p>The reactants of the reaction</p> <code>&lt;dynamic&gt;</code> <code>products</code> <code>frozenset[Product]</code> <p>The products of the reaction</p> <code>&lt;dynamic&gt;</code> <code>modifiers</code> <code>frozenset[KnownOrUnknownModulator]</code> <p>The modifiers of the reaction</p> <code>&lt;dynamic&gt;</code> <code>has_external_source</code> <code>bool</code> <p>Whether the reaction has an unspecified external source (a Degraded reactant in CellDesigner).</p> <code>False</code> <code>has_external_sink</code> <code>bool</code> <p>Whether the reaction has an unspecified external sink (a Degraded product in CellDesigner).</p> <code>False</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.Transport.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TransportLayout","title":"TransportLayout  <code>dataclass</code>","text":"<pre><code>TransportLayout(*, left_connector_fraction: float = 0.375, right_connector_fraction: float = 0.625, reaction_node_font_family: str = DEFAULT_FONT_FAMILY, reaction_node_font_fill: Color | NoneValueType = black, reaction_node_font_stroke: Color | NoneValueType = NoneValue, reaction_node_font_style: FontStyle = NORMAL, reaction_node_font_weight: FontWeight | float = NORMAL, reaction_node_text: str | None = None, reaction_node_height: float = 10.0, reaction_node_width: float = 10.0, reaction_node_segment: int = 1, reaction_node_stroke: NoneValueType | Color | None = black, reaction_node_stroke_width: float | None = 1.0, reaction_node_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, reaction_node_stroke_dashoffset: float | None = None, reaction_node_fill: NoneValueType | Color | None = white, reaction_node_transform: NoneValueType | tuple[Transformation, ...] | None = None, reaction_node_filter: NoneValueType | Filter | None = None, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, end_shorten: float = 2.0, fill: NoneValueType | Color | None = None, filter_: NoneValueType | Filter | None = None, path_fill: NoneValueType | Color | None = NoneValue, path_filter: NoneValueType | Filter | None = None, path_stroke: NoneValueType | Color | None = black, path_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, path_stroke_dashoffset: NoneValueType | float | None = None, path_stroke_width: float | None = 1.0, path_transform: NoneValueType | tuple[Transformation, ...] | None = None, stroke: NoneValueType | Color | None = None, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: NoneValueType | float | None = None, segments: tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...] = tuple(), source: LayoutElement | None = None, start_shorten: float = 2.0, target: LayoutElement | None = None, transform: NoneValueType | tuple[Transformation, ...] | None = None, end_arrowhead_fill: NoneValueType | Color | None = black, end_arrowhead_filter: NoneValueType | Filter | None = None, end_arrowhead_stroke: NoneValueType | Color | None = black, end_arrowhead_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, end_arrowhead_stroke_dashoffset: float | None = None, end_arrowhead_stroke_width: float | None = 1.0, end_arrowhead_transform: NoneValueType | tuple[Transformation, ...] | None = None, start_arrowhead_fill: NoneValueType | Color | None = black, start_arrowhead_filter: NoneValueType | Filter | None = None, start_arrowhead_stroke: NoneValueType | Color | None = black, start_arrowhead_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, start_arrowhead_stroke_dashoffset: float | None = None, start_arrowhead_stroke_width: float | None = 1.0, start_arrowhead_transform: NoneValueType | tuple[Transformation, ...] | None = None, reversible: bool = False, end_arrowhead_bar_height: float = 8.0, end_arrowhead_sep: float = 5.0, end_arrowhead_triangle_height: float = 8.0, end_arrowhead_triangle_width: float = 15.0, start_arrowhead_bar_height: float = 8.0, start_arrowhead_sep: float = 4.0, start_arrowhead_triangle_height: float = 8.0, start_arrowhead_triangle_width: float = 15.0)\n</code></pre> <p>               Bases: <code>ReactionLayout</code>, <code>_ReactionNodeMixin</code></p> <p>Transport layout.</p> <p>Parameters:</p> Name Type Description Default <code>left_connector_fraction</code> <code>float</code> <p>The fraction along the segment where the left connector attaches.</p> <code>0.375</code> <code>right_connector_fraction</code> <code>float</code> <p>The fraction along the segment where the right connector attaches.</p> <code>0.625</code> <code>reaction_node_font_family</code> <code>str</code> <p>The font family of the reaction node label.</p> <code>'DejaVu Sans'</code> <code>reaction_node_font_fill</code> <code>Color | NoneValueType</code> <p>The fill color of the reaction node label.</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>reaction_node_font_stroke</code> <code>Color | NoneValueType</code> <p>The stroke color of the reaction node label.</p> <code>&lt;momapy.drawing.NoneValueType object at 0x7f8b17323e00&gt;</code> <code>reaction_node_font_style</code> <code>FontStyle</code> <p>The font style of the reaction node label.</p> <code>&lt;FontStyle.NORMAL: 0&gt;</code> <code>reaction_node_font_weight</code> <code>FontWeight | float</code> <p>The font weight of the reaction node label.</p> <code>&lt;FontWeight.NORMAL: 0&gt;</code> <code>reaction_node_text</code> <code>str | None</code> <p>The text of the reaction node label.</p> <code>None</code> <code>reaction_node_height</code> <code>float</code> <p>The height of the reaction node.</p> <code>10.0</code> <code>reaction_node_width</code> <code>float</code> <p>The width of the reaction node.</p> <code>10.0</code> <code>reaction_node_segment</code> <code>int</code> <p>The index of the arc segment carrying the reaction node.</p> <code>1</code> <code>reaction_node_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the reaction node.</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>reaction_node_stroke_width</code> <code>float | None</code> <p>The stroke width of the reaction node.</p> <code>1.0</code> <code>reaction_node_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The dash pattern of the reaction node's border.</p> <code>None</code> <code>reaction_node_stroke_dashoffset</code> <code>float | None</code> <p>The dash offset of the reaction node's border.</p> <code>None</code> <code>reaction_node_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the reaction node.</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>reaction_node_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform applied to the reaction node.</p> <code>None</code> <code>reaction_node_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter applied to the reaction node.</p> <code>None</code> <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'3bcc7f74-0ba1-4b28-ae80-a070628e9b6e'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>end_shorten</code> <code>float</code> <p>The length the end of the arc will be shorten by</p> <code>2.0</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the arc</p> <code>None</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the arc</p> <code>None</code> <code>path_fill</code> <code>NoneValueType | Color | None</code> <p>The path fill color of the arc</p> <code>&lt;momapy.drawing.NoneValueType object at 0x7f8b17323e00&gt;</code> <code>path_filter</code> <code>NoneValueType | Filter | None</code> <p>The path filter of the arc</p> <code>None</code> <code>path_stroke</code> <code>NoneValueType | Color | None</code> <p>The path stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>path_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The path stroke dasharray of the arc</p> <code>None</code> <code>path_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The path stroke dashoffset of the arc</p> <code>None</code> <code>path_stroke_width</code> <code>float | None</code> <p>The path stroke width of the arc</p> <code>1.0</code> <code>path_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The path transform of the arc</p> <code>None</code> <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the arc</p> <code>None</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the arc</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the arc</p> <code>None</code> <code>stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the arc</p> <code>None</code> <code>segments</code> <code>tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...]</code> <p>The path segments of the arc</p> <code>&lt;dynamic&gt;</code> <code>source</code> <code>LayoutElement | None</code> <p>The source of the arc</p> <code>None</code> <code>start_shorten</code> <code>float</code> <p>The length the start of the arc will be shorten by</p> <code>2.0</code> <code>target</code> <code>LayoutElement | None</code> <p>The target of the arc</p> <code>None</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the arc</p> <code>None</code> <code>end_arrowhead_fill</code> <code>NoneValueType | Color | None</code> <p>The end arrowhead fill color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>end_arrowhead_filter</code> <code>NoneValueType | Filter | None</code> <p>The end arrowhead filter of the arc</p> <code>None</code> <code>end_arrowhead_stroke</code> <code>NoneValueType | Color | None</code> <p>The end arrowhead stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>end_arrowhead_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The end arrowhead stroke dasharray of the arc</p> <code>None</code> <code>end_arrowhead_stroke_dashoffset</code> <code>float | None</code> <p>The end arrowhead stroke dashoffset of the arc</p> <code>None</code> <code>end_arrowhead_stroke_width</code> <code>float | None</code> <p>The end arrowhead stroke width of the arc</p> <code>1.0</code> <code>end_arrowhead_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The end arrowhead transform of the arc</p> <code>None</code> <code>start_arrowhead_fill</code> <code>NoneValueType | Color | None</code> <p>The start arrowhead fill color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>start_arrowhead_filter</code> <code>NoneValueType | Filter | None</code> <p>The start arrowhead filter of the arc</p> <code>None</code> <code>start_arrowhead_stroke</code> <code>NoneValueType | Color | None</code> <p>The start arrowhead stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>start_arrowhead_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The start arrowhead stroke dasharray of the arc</p> <code>None</code> <code>start_arrowhead_stroke_dashoffset</code> <code>float | None</code> <p>The start arrowhead stroke dashoffset of the arc</p> <code>None</code> <code>start_arrowhead_stroke_width</code> <code>float | None</code> <p>The start arrowhead stroke width of the arc</p> <code>1.0</code> <code>start_arrowhead_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The start arrowhead transform of the arc</p> <code>None</code> <code>reversible</code> <code>bool</code> <p>Whether the reaction is reversible.</p> <code>False</code> <code>end_arrowhead_bar_height</code> <code>float</code> <p>The height of the end arrowhead bar.</p> <code>8.0</code> <code>end_arrowhead_sep</code> <code>float</code> <p>The separation between the end bar and triangle.</p> <code>5.0</code> <code>end_arrowhead_triangle_height</code> <code>float</code> <p>The height of the end arrowhead triangle.</p> <code>8.0</code> <code>end_arrowhead_triangle_width</code> <code>float</code> <p>The width of the end arrowhead triangle.</p> <code>15.0</code> <code>start_arrowhead_bar_height</code> <code>float</code> <p>The height of the start arrowhead bar.</p> <code>8.0</code> <code>start_arrowhead_sep</code> <code>float</code> <p>The separation between the start bar and triangle.</p> <code>4.0</code> <code>start_arrowhead_triangle_height</code> <code>float</code> <p>The height of the start arrowhead triangle.</p> <code>8.0</code> <code>start_arrowhead_triangle_width</code> <code>float</code> <p>The width of the start arrowhead triangle.</p> <code>15.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>childless</code> <p>Return a copy of the arc with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>end_arrowhead_base</code> <p>Return the base anchor point of the double-headed arc end arrowhead.</p> <code>end_arrowhead_bbox</code> <p>Return the bounding box of the double-headed arc end arrowhead.</p> <code>end_arrowhead_border</code> <p>Return the point at the intersection of the drawing elements of the double-headed arc end arrowhead and the line going through the center of these drawing elements and the given point.</p> <code>end_arrowhead_drawing_elements</code> <p>Return the drawing elements of the double-headed arc end arrowhead.</p> <code>end_arrowhead_length</code> <p>Return the length of the double-headed arc end arrowhead.</p> <code>end_arrowhead_tip</code> <p>Return the tip anchor point of the double-headed arc end arrowhead.</p> <code>end_point</code> <p>Return the ending point of the arc.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>fraction</code> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <code>length</code> <p>Return the total length of the arc path.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_children</code> <p>Return the self children of the arc.</p> <code>own_drawing_elements</code> <p>Return the arc's own drawing elements, including those of its mixins.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>path_drawing_elements</code> <p>Return the drawing elements of the double-headed arc path.</p> <code>points</code> <p>Return the points of the arc path.</p> <code>start_arrowhead_base</code> <p>Return the base anchor point of the double-headed arc start arrowhead.</p> <code>start_arrowhead_bbox</code> <p>Return the bounding box of the double-headed arc start arrowhead.</p> <code>start_arrowhead_border</code> <p>Return the point at the intersection of the drawing elements of the double-headed arc start arrowhead and the line going through the center of these drawing elements and the given point.</p> <code>start_arrowhead_drawing_elements</code> <p>Return the drawing elements of the double-headed arc start arrowhead.</p> <code>start_arrowhead_length</code> <p>Return the length of the double-headed arc start arrowhead.</p> <code>start_arrowhead_tip</code> <p>Return the tip anchor point of the double-headed arc start arrowhead.</p> <code>start_point</code> <p>Return the starting point of the arc.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TransportLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TransportLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TransportLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the arc with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the arc with no children.\"\"\"\n    return dataclasses.replace(self, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TransportLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TransportLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TransportLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TransportLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TransportLayout.end_arrowhead_base","title":"end_arrowhead_base","text":"<pre><code>end_arrowhead_base() -&gt; Point\n</code></pre> <p>Return the base anchor point of the double-headed arc end arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_arrowhead_base(self) -&gt; Point:\n    \"\"\"Return the base anchor point of the double-headed arc end arrowhead.\"\"\"\n    arrowhead_length = self.end_arrowhead_length()\n    if arrowhead_length == 0:\n        return self.end_point()\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return self.end_arrowhead_tip() - (arrowhead_length, 0)\n    fraction = 1 - (arrowhead_length + self.end_shorten) / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TransportLayout.end_arrowhead_bbox","title":"end_arrowhead_bbox","text":"<pre><code>end_arrowhead_bbox() -&gt; Bbox\n</code></pre> <p>Return the bounding box of the double-headed arc end arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_arrowhead_bbox(self) -&gt; Bbox:\n    \"\"\"Return the bounding box of the double-headed arc end arrowhead.\"\"\"\n    return get_drawing_elements_bbox(self.end_arrowhead_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TransportLayout.end_arrowhead_border","title":"end_arrowhead_border","text":"<pre><code>end_arrowhead_border(point: Point) -&gt; Point\n</code></pre> <p>Return the point at the intersection of the drawing elements of the double-headed arc end arrowhead and the line going through the center of these drawing elements and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_arrowhead_border(self, point: Point) -&gt; Point:\n    \"\"\"Return the point at the intersection of the drawing elements of the double-headed arc end arrowhead and the line going through the center of these drawing elements and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    point = get_drawing_elements_border(\n        self.end_arrowhead_drawing_elements(), point\n    )\n    if point is None:\n        return self.end_arrowhead_tip()\n    return point\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TransportLayout.end_arrowhead_drawing_elements","title":"end_arrowhead_drawing_elements","text":"<pre><code>end_arrowhead_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the double-headed arc end arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_arrowhead_drawing_elements(\n    self,\n) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the double-headed arc end arrowhead.\"\"\"\n    drawing_elements = self._end_arrowhead_border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_end_arrowhead\",\n        elements=tuple(drawing_elements),\n        fill=self.end_arrowhead_fill,\n        filter_=self.end_arrowhead_filter,\n        id_=f\"{self.id_}_end_arrowhead\",\n        stroke=self.end_arrowhead_stroke,\n        stroke_width=self.end_arrowhead_stroke_width,\n        stroke_dasharray=self.end_arrowhead_stroke_dasharray,\n        stroke_dashoffset=self.end_arrowhead_stroke_dashoffset,\n        transform=self.end_arrowhead_transform,\n    )\n    transformation = self._get_end_arrowhead_transformation()\n    group = group.transformed(transformation)\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TransportLayout.end_arrowhead_length","title":"end_arrowhead_length","text":"<pre><code>end_arrowhead_length() -&gt; float\n</code></pre> <p>Return the length of the double-headed arc end arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_arrowhead_length(self) -&gt; float:\n    \"\"\"Return the length of the double-headed arc end arrowhead.\"\"\"\n    bbox = get_drawing_elements_bbox(self._end_arrowhead_border_drawing_elements())\n    if math.isnan(bbox.width):\n        return 0.0\n    return bbox.east().x\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TransportLayout.end_arrowhead_tip","title":"end_arrowhead_tip","text":"<pre><code>end_arrowhead_tip() -&gt; Point\n</code></pre> <p>Return the tip anchor point of the double-headed arc end arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_arrowhead_tip(self) -&gt; Point:\n    \"\"\"Return the tip anchor point of the double-headed arc end arrowhead.\"\"\"\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return segment.p2\n    fraction = 1 - self.end_shorten / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TransportLayout.end_point","title":"end_point","text":"<pre><code>end_point() -&gt; Point\n</code></pre> <p>Return the ending point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_point(self) -&gt; Point:\n    \"\"\"Return the ending point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[-1]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TransportLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TransportLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TransportLayout.fraction","title":"fraction","text":"<pre><code>fraction(fraction: float) -&gt; tuple[Point, float]\n</code></pre> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def fraction(self, fraction: float) -&gt; tuple[Point, float]:\n    \"\"\"Return the position and angle on the arc at a given fraction (of the total arc length).\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    current_length = 0\n    length_to_reach = fraction * self.length()\n    for segment in self.segments:\n        current_length += segment.length()\n        if current_length &gt;= length_to_reach:\n            break\n    segment_start_length = current_length - segment.length()\n    segment_fraction = (length_to_reach - segment_start_length) / segment.length()\n    position, angle = segment.get_position_and_angle_at_fraction(segment_fraction)\n    return position, angle\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TransportLayout.length","title":"length","text":"<pre><code>length() -&gt; float\n</code></pre> <p>Return the total length of the arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def length(self) -&gt; float:\n    \"\"\"Return the total length of the arc path.\"\"\"\n    return sum([segment.length() for segment in self.segments])\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TransportLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TransportLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the arc.\"\"\"\n    return []\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TransportLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the arc's own drawing elements, including those of its mixins.</p> Source code in <code>src/momapy/celldesigner/elements.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the arc's own drawing elements, including those of its mixins.\"\"\"\n    drawing_elements = super_or_builder(\n        CellDesignerDoubleHeadedArc, self\n    ).own_drawing_elements()\n    done_bases = []\n    for base in type(self).__mro__:\n        if (\n            issubclass_or_builder(base, _SBGNMixin)\n            and base is not type(self)\n            and not any([issubclass(done_base, base) for done_base in done_bases])\n        ):\n            drawing_elements += getattr(base, \"_mixin_drawing_elements\")(self)\n            done_bases.append(base)\n    return drawing_elements\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TransportLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TransportLayout.path_drawing_elements","title":"path_drawing_elements","text":"<pre><code>path_drawing_elements() -&gt; list[Path]\n</code></pre> <p>Return the drawing elements of the double-headed arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def path_drawing_elements(self) -&gt; list[Path]:\n    \"\"\"Return the drawing elements of the double-headed arc path.\"\"\"\n    start_arrowhead_length = self.start_arrowhead_length()\n    end_arrowhead_length = self.end_arrowhead_length()\n    if len(self.segments) == 1:\n        segment = (\n            self.segments[0]\n            .shortened(self.start_shorten + start_arrowhead_length, \"start\")\n            .shortened(self.end_shorten + end_arrowhead_length, \"end\")\n        )\n        actions = [\n            MoveTo(segment.p1),\n            self._make_path_action_from_segment(segment),\n        ]\n    else:\n        first_segment = self.segments[0].shortened(\n            self.start_shorten + start_arrowhead_length, \"start\"\n        )\n        last_segment = self.segments[-1].shortened(\n            self.end_shorten + end_arrowhead_length, \"end\"\n        )\n        actions = [\n            MoveTo(first_segment.p1),\n            self._make_path_action_from_segment(first_segment),\n        ]\n        for segment in self.segments[1:-1]:\n            action = self._make_path_action_from_segment(segment)\n            actions.append(action)\n        actions.append(self._make_path_action_from_segment(last_segment))\n    path = Path(\n        actions=tuple(actions),\n        class_=f\"{type(self).__name__}_path\",\n        fill=self.path_fill,\n        filter_=self.path_filter,\n        id_=f\"{self.id_}_path\",\n        stroke=self.path_stroke,\n        stroke_dasharray=self.path_stroke_dasharray,\n        stroke_dashoffset=self.path_stroke_dashoffset,\n        stroke_width=self.path_stroke_width,\n        transform=self.path_transform,\n    )\n    return [path]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TransportLayout.points","title":"points","text":"<pre><code>points() -&gt; list[Point]\n</code></pre> <p>Return the points of the arc path.</p> <p>An arc with no segments has no points and returns an empty list.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def points(self) -&gt; list[Point]:\n    \"\"\"Return the points of the arc path.\n\n    An arc with no segments has no points and returns an empty list.\n    \"\"\"\n    if not self.segments:\n        return []\n    points = []\n    for segment in self.segments:\n        points.append(segment.p1)\n    points.append(segment.p2)\n    return points\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TransportLayout.start_arrowhead_base","title":"start_arrowhead_base","text":"<pre><code>start_arrowhead_base() -&gt; Point\n</code></pre> <p>Return the base anchor point of the double-headed arc start arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_arrowhead_base(self) -&gt; Point:\n    \"\"\"Return the base anchor point of the double-headed arc start arrowhead.\"\"\"\n    arrowhead_length = self.start_arrowhead_length()\n    if arrowhead_length == 0:\n        return self.start_point()\n    segment = self.segments[0]\n    segment = Segment(segment.p2, segment.p1)\n    segment_length = segment.length()\n    if segment_length == 0:\n        return self.start_arrowhead_tip() + (arrowhead_length, 0)\n    fraction = 1 - (arrowhead_length + self.start_shorten) / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TransportLayout.start_arrowhead_bbox","title":"start_arrowhead_bbox","text":"<pre><code>start_arrowhead_bbox() -&gt; Bbox\n</code></pre> <p>Return the bounding box of the double-headed arc start arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_arrowhead_bbox(self) -&gt; Bbox:\n    \"\"\"Return the bounding box of the double-headed arc start arrowhead.\"\"\"\n    return get_drawing_elements_bbox(self.start_arrowhead_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TransportLayout.start_arrowhead_border","title":"start_arrowhead_border","text":"<pre><code>start_arrowhead_border(point: Point) -&gt; Point\n</code></pre> <p>Return the point at the intersection of the drawing elements of the double-headed arc start arrowhead and the line going through the center of these drawing elements and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_arrowhead_border(self, point: Point) -&gt; Point:\n    \"\"\"Return the point at the intersection of the drawing elements of the double-headed arc start arrowhead and the line going through the center of these drawing elements and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    point = get_drawing_elements_border(\n        self.start_arrowhead_drawing_elements(), point\n    )\n    if point is None:\n        return self.start_arrowhead_tip()\n    return point\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TransportLayout.start_arrowhead_drawing_elements","title":"start_arrowhead_drawing_elements","text":"<pre><code>start_arrowhead_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the double-headed arc start arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_arrowhead_drawing_elements(\n    self,\n) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the double-headed arc start arrowhead.\"\"\"\n    drawing_elements = self._start_arrowhead_border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_start_arrowhead\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}_start_arrowhead\",\n        fill=self.start_arrowhead_fill,\n        filter_=self.start_arrowhead_filter,\n        stroke=self.start_arrowhead_stroke,\n        stroke_dasharray=self.start_arrowhead_stroke_dasharray,\n        stroke_dashoffset=self.start_arrowhead_stroke_dashoffset,\n        stroke_width=self.start_arrowhead_stroke_width,\n        transform=self.start_arrowhead_transform,\n    )\n    transformation = self._get_start_arrowhead_transformation()\n    group = group.transformed(transformation)\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TransportLayout.start_arrowhead_length","title":"start_arrowhead_length","text":"<pre><code>start_arrowhead_length() -&gt; float\n</code></pre> <p>Return the length of the double-headed arc start arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_arrowhead_length(self) -&gt; float:\n    \"\"\"Return the length of the double-headed arc start arrowhead.\"\"\"\n    bbox = get_drawing_elements_bbox(\n        self._start_arrowhead_border_drawing_elements()\n    )\n    if math.isnan(bbox.width):\n        return 0.0\n    return abs(bbox.west().x)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TransportLayout.start_arrowhead_tip","title":"start_arrowhead_tip","text":"<pre><code>start_arrowhead_tip() -&gt; Point\n</code></pre> <p>Return the tip anchor point of the double-headed arc start arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_arrowhead_tip(self) -&gt; Point:\n    \"\"\"Return the tip anchor point of the double-headed arc start arrowhead.\"\"\"\n    segment = self.segments[0]\n    segment = Segment(segment.p2, segment.p1)\n    segment_length = segment.length()\n    if segment_length == 0:\n        return segment.p2\n    fraction = 1 - self.start_shorten / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TransportLayout.start_point","title":"start_point","text":"<pre><code>start_point() -&gt; Point\n</code></pre> <p>Return the starting point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_point(self) -&gt; Point:\n    \"\"\"Return the starting point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[0]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TransportLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.Trigger","title":"Trigger  <code>dataclass</code>","text":"<pre><code>Trigger(*, id_: str = make_uuid4_as_str(), name: str | None = None, sbo_term: str | None = None, metaid: str | None = None, referred_element: Species | BooleanLogicGate)\n</code></pre> <p>               Bases: <code>Modulator</code></p> <p>Trigger.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'88ac768d-5ce6-45dc-a4ec-2af69c7fe1dc'</code> <code>name</code> <code>str | None</code> <p>Human-readable name of the element.</p> <code>None</code> <code>sbo_term</code> <code>str | None</code> <p>Optional SBO term identifier for semantic annotation.</p> <code>None</code> <code>metaid</code> <code>str | None</code> <p>Optional metadata identifier for RDF annotations.</p> <code>None</code> <code>referred_element</code> <code>Species | BooleanLogicGate</code> <p>The species or boolean logic gate the modifier refers to</p> required <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.Trigger.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.Triggering","title":"Triggering  <code>dataclass</code>","text":"<pre><code>Triggering(*, id_: str = make_uuid4_as_str(), source: Species | BooleanLogicGate, target: Species | None)\n</code></pre> <p>               Bases: <code>Modulation</code></p> <p>Triggering.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'e31c85de-9394-4fef-a140-8a313e4eaa50'</code> <code>source</code> <code>Species | BooleanLogicGate</code> <p>The source of the modulation</p> required <code>target</code> <code>Species | None</code> <p>The target of the modulation</p> required <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.Triggering.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TriggeringLayout","title":"TriggeringLayout  <code>dataclass</code>","text":"<pre><code>TriggeringLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, end_shorten: float = 0.0, fill: NoneValueType | Color | None = None, filter_: NoneValueType | Filter | None = None, path_fill: NoneValueType | Color | None = NoneValue, path_filter: NoneValueType | Filter | None = None, path_stroke: NoneValueType | Color | None = black, path_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, path_stroke_dashoffset: NoneValueType | float | None = None, path_stroke_width: float | None = 1.0, path_transform: NoneValueType | tuple[Transformation, ...] | None = None, stroke: NoneValueType | Color | None = None, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: NoneValueType | float | None = None, segments: tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...] = tuple(), source: LayoutElement | None = None, start_shorten: float = 0.0, target: LayoutElement | None = None, transform: NoneValueType | tuple[Transformation, ...] | None = None, arrowhead_fill: NoneValueType | Color | None = white, arrowhead_filter: NoneValueType | Filter | None = None, arrowhead_stroke: NoneValueType | Color | None = black, arrowhead_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, arrowhead_stroke_dashoffset: NoneValueType | float | None = None, arrowhead_stroke_width: float | None = 1.0, arrowhead_transform: NoneValueType | tuple[Transformation, ...] | None = None, arrowhead_bar_height: float = 8.0, arrowhead_sep: float = 5.0, arrowhead_triangle_height: float = 10.0, arrowhead_triangle_width: float = 15.0)\n</code></pre> <p>               Bases: <code>CellDesignerSingleHeadedArc</code></p> <p>Triggering layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'dbfa69b7-ad8a-4151-8915-4673a5c92adb'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>end_shorten</code> <code>float</code> <p>The length the end of the arc will be shorten by</p> <code>0.0</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the arc</p> <code>None</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the arc</p> <code>None</code> <code>path_fill</code> <code>NoneValueType | Color | None</code> <p>The path fill color of the arc</p> <code>&lt;momapy.drawing.NoneValueType object at 0x7f8b17323e00&gt;</code> <code>path_filter</code> <code>NoneValueType | Filter | None</code> <p>The path filter of the arc</p> <code>None</code> <code>path_stroke</code> <code>NoneValueType | Color | None</code> <p>The path stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>path_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The path stroke dasharray of the arc</p> <code>None</code> <code>path_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The path stroke dashoffset of the arc</p> <code>None</code> <code>path_stroke_width</code> <code>float | None</code> <p>The path stroke width of the arc</p> <code>1.0</code> <code>path_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The path transform of the arc</p> <code>None</code> <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the arc</p> <code>None</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the arc</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the arc</p> <code>None</code> <code>stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the arc</p> <code>None</code> <code>segments</code> <code>tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...]</code> <p>The path segments of the arc</p> <code>&lt;dynamic&gt;</code> <code>source</code> <code>LayoutElement | None</code> <p>The source of the arc</p> <code>None</code> <code>start_shorten</code> <code>float</code> <p>The length the start of the arc will be shorten by</p> <code>0.0</code> <code>target</code> <code>LayoutElement | None</code> <p>The target of the arc</p> <code>None</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the arc</p> <code>None</code> <code>arrowhead_fill</code> <code>NoneValueType | Color | None</code> <p>The arrowhead fill color of the arc</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>arrowhead_filter</code> <code>NoneValueType | Filter | None</code> <p>The arrowhead filter of the arc</p> <code>None</code> <code>arrowhead_stroke</code> <code>NoneValueType | Color | None</code> <p>The arrowhead stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>arrowhead_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The arrowhead stroke dasharray of the arc</p> <code>None</code> <code>arrowhead_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The arrowhead stroke dashoffset of the arc</p> <code>None</code> <code>arrowhead_stroke_width</code> <code>float | None</code> <p>The arrowhead stroke width of the arc</p> <code>1.0</code> <code>arrowhead_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The arrowhead transform of the arc</p> <code>None</code> <code>arrowhead_bar_height</code> <code>float</code> <p>The height of the arrowhead bar.</p> <code>8.0</code> <code>arrowhead_sep</code> <code>float</code> <p>The separation between the bar and the triangle.</p> <code>5.0</code> <code>arrowhead_triangle_height</code> <code>float</code> <p>The height of the arrowhead triangle.</p> <code>10.0</code> <code>arrowhead_triangle_width</code> <code>float</code> <p>The width of the arrowhead triangle.</p> <code>15.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>arrowhead_base</code> <p>Return the arrowhead base anchor point of the single-headed arc.</p> <code>arrowhead_bbox</code> <p>Return the bounding box of the single-headed arc arrowhead.</p> <code>arrowhead_border</code> <p>Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.</p> <code>arrowhead_drawing_elements</code> <p>Return the drawing elements of the single-headed arc arrowhead.</p> <code>arrowhead_length</code> <p>Return the length of the single-headed arc arrowhead.</p> <code>arrowhead_tip</code> <p>Return the arrowhead tip anchor point of the single-headed arc.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>childless</code> <p>Return a copy of the arc with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>end_point</code> <p>Return the ending point of the arc.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>fraction</code> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <code>length</code> <p>Return the total length of the arc path.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_children</code> <p>Return the self children of the arc.</p> <code>own_drawing_elements</code> <p>Return the arc's own drawing elements, including those of its mixins.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>path_drawing_elements</code> <p>Return the drawing elements of the single-headed arc path.</p> <code>points</code> <p>Return the points of the arc path.</p> <code>start_point</code> <p>Return the starting point of the arc.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TriggeringLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TriggeringLayout.arrowhead_base","title":"arrowhead_base","text":"<pre><code>arrowhead_base() -&gt; Point\n</code></pre> <p>Return the arrowhead base anchor point of the single-headed arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_base(self) -&gt; Point:\n    \"\"\"Return the arrowhead base anchor point of the single-headed arc.\"\"\"\n    arrowhead_length = self.arrowhead_length()\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return self.arrowhead_tip() - (arrowhead_length, 0)\n    fraction = 1 - (arrowhead_length + self.end_shorten) / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TriggeringLayout.arrowhead_bbox","title":"arrowhead_bbox","text":"<pre><code>arrowhead_bbox() -&gt; Bbox\n</code></pre> <p>Return the bounding box of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_bbox(self) -&gt; Bbox:\n    \"\"\"Return the bounding box of the single-headed arc arrowhead.\"\"\"\n    return get_drawing_elements_bbox(self.arrowhead_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TriggeringLayout.arrowhead_border","title":"arrowhead_border","text":"<pre><code>arrowhead_border(point: Point) -&gt; Point\n</code></pre> <p>Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_border(self, point: Point) -&gt; Point:\n    \"\"\"Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    point = get_drawing_elements_border(self.arrowhead_drawing_elements(), point)\n    if point is None:\n        return self.arrowhead_tip()\n    return point\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TriggeringLayout.arrowhead_drawing_elements","title":"arrowhead_drawing_elements","text":"<pre><code>arrowhead_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_drawing_elements(\n    self,\n) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the single-headed arc arrowhead.\"\"\"\n    drawing_elements = self._arrowhead_border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_arrowhead\",\n        elements=tuple(drawing_elements),\n        fill=self.arrowhead_fill,\n        filter_=self.arrowhead_filter,\n        id_=f\"{self.id_}_arrowhead\",\n        stroke=self.arrowhead_stroke,\n        stroke_dasharray=self.arrowhead_stroke_dasharray,\n        stroke_dashoffset=self.arrowhead_stroke_dashoffset,\n        stroke_width=self.arrowhead_stroke_width,\n        transform=self.arrowhead_transform,\n    )\n    transformation = self._get_arrowhead_transformation()\n    group = group.transformed(transformation)\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TriggeringLayout.arrowhead_length","title":"arrowhead_length","text":"<pre><code>arrowhead_length() -&gt; float\n</code></pre> <p>Return the length of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_length(self) -&gt; float:\n    \"\"\"Return the length of the single-headed arc arrowhead.\"\"\"\n    bbox = get_drawing_elements_bbox(self._arrowhead_border_drawing_elements())\n    if math.isnan(bbox.width):\n        return 0.0\n    return bbox.east().x\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TriggeringLayout.arrowhead_tip","title":"arrowhead_tip","text":"<pre><code>arrowhead_tip() -&gt; Point\n</code></pre> <p>Return the arrowhead tip anchor point of the single-headed arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_tip(self) -&gt; Point:\n    \"\"\"Return the arrowhead tip anchor point of the single-headed arc.\"\"\"\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return segment.p2\n    fraction = 1 - self.end_shorten / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TriggeringLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TriggeringLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the arc with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the arc with no children.\"\"\"\n    return dataclasses.replace(self, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TriggeringLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TriggeringLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TriggeringLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TriggeringLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TriggeringLayout.end_point","title":"end_point","text":"<pre><code>end_point() -&gt; Point\n</code></pre> <p>Return the ending point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_point(self) -&gt; Point:\n    \"\"\"Return the ending point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[-1]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TriggeringLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TriggeringLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TriggeringLayout.fraction","title":"fraction","text":"<pre><code>fraction(fraction: float) -&gt; tuple[Point, float]\n</code></pre> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def fraction(self, fraction: float) -&gt; tuple[Point, float]:\n    \"\"\"Return the position and angle on the arc at a given fraction (of the total arc length).\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    current_length = 0\n    length_to_reach = fraction * self.length()\n    for segment in self.segments:\n        current_length += segment.length()\n        if current_length &gt;= length_to_reach:\n            break\n    segment_start_length = current_length - segment.length()\n    segment_fraction = (length_to_reach - segment_start_length) / segment.length()\n    position, angle = segment.get_position_and_angle_at_fraction(segment_fraction)\n    return position, angle\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TriggeringLayout.length","title":"length","text":"<pre><code>length() -&gt; float\n</code></pre> <p>Return the total length of the arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def length(self) -&gt; float:\n    \"\"\"Return the total length of the arc path.\"\"\"\n    return sum([segment.length() for segment in self.segments])\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TriggeringLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TriggeringLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the arc.\"\"\"\n    return []\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TriggeringLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the arc's own drawing elements, including those of its mixins.</p> Source code in <code>src/momapy/celldesigner/elements.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the arc's own drawing elements, including those of its mixins.\"\"\"\n    drawing_elements = super_or_builder(\n        CellDesignerSingleHeadedArc, self\n    ).own_drawing_elements()\n    done_bases = []\n    for base in type(self).__mro__:\n        if (\n            issubclass_or_builder(base, _SBGNMixin)\n            and base is not type(self)\n            and not any([issubclass(done_base, base) for done_base in done_bases])\n        ):\n            drawing_elements += getattr(base, \"_mixin_drawing_elements\")(self)\n            done_bases.append(base)\n    return drawing_elements\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TriggeringLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TriggeringLayout.path_drawing_elements","title":"path_drawing_elements","text":"<pre><code>path_drawing_elements() -&gt; list[Path]\n</code></pre> <p>Return the drawing elements of the single-headed arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def path_drawing_elements(self) -&gt; list[Path]:\n    \"\"\"Return the drawing elements of the single-headed arc path.\"\"\"\n    arrowhead_length = self.arrowhead_length()\n    if len(self.segments) == 1:\n        segment = (\n            self.segments[0]\n            .shortened(self.start_shorten, \"start\")\n            .shortened(self.end_shorten + arrowhead_length, \"end\")\n        )\n        actions = [\n            MoveTo(segment.p1),\n            self._make_path_action_from_segment(segment),\n        ]\n    else:\n        first_segment = self.segments[0].shortened(self.start_shorten, \"start\")\n        last_segment = self.segments[-1].shortened(\n            self.end_shorten + arrowhead_length, \"end\"\n        )\n        actions = [\n            MoveTo(first_segment.p1),\n            self._make_path_action_from_segment(first_segment),\n        ]\n        for segment in self.segments[1:-1]:\n            action = self._make_path_action_from_segment(segment)\n            actions.append(action)\n        actions.append(self._make_path_action_from_segment(last_segment))\n    path = Path(\n        actions=tuple(actions),\n        class_=f\"{type(self).__name__}_path\",\n        fill=self.path_fill,\n        filter_=self.path_filter,\n        id_=f\"{self.id_}_path\",\n        stroke=self.path_stroke,\n        stroke_dasharray=self.path_stroke_dasharray,\n        stroke_dashoffset=self.path_stroke_dashoffset,\n        stroke_width=self.path_stroke_width,\n        transform=self.path_transform,\n    )\n    return [path]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TriggeringLayout.points","title":"points","text":"<pre><code>points() -&gt; list[Point]\n</code></pre> <p>Return the points of the arc path.</p> <p>An arc with no segments has no points and returns an empty list.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def points(self) -&gt; list[Point]:\n    \"\"\"Return the points of the arc path.\n\n    An arc with no segments has no points and returns an empty list.\n    \"\"\"\n    if not self.segments:\n        return []\n    points = []\n    for segment in self.segments:\n        points.append(segment.p1)\n    points.append(segment.p2)\n    return points\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TriggeringLayout.start_point","title":"start_point","text":"<pre><code>start_point() -&gt; Point\n</code></pre> <p>Return the starting point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_point(self) -&gt; Point:\n    \"\"\"Return the starting point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[0]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TriggeringLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncatedProtein","title":"TruncatedProtein  <code>dataclass</code>","text":"<pre><code>TruncatedProtein(*, id_: str = make_uuid4_as_str(), name: str | None = None, sbo_term: str | None = None, metaid: str | None = None, compartment: Compartment | None = None, hypothetical: bool = False, active: bool = False, homomultimer: int = 1, template: TruncatedProteinTemplate, modifications: frozenset[Modification] = frozenset(), structural_states: frozenset[StructuralState] = frozenset())\n</code></pre> <p>               Bases: <code>Protein</code></p> <p>Truncated protein.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'50cd4a9e-b226-4081-a792-d35b8608d190'</code> <code>name</code> <code>str | None</code> <p>Human-readable name of the element.</p> <code>None</code> <code>sbo_term</code> <code>str | None</code> <p>Optional SBO term identifier for semantic annotation.</p> <code>None</code> <code>metaid</code> <code>str | None</code> <p>Optional metadata identifier for RDF annotations.</p> <code>None</code> <code>compartment</code> <code>Compartment | None</code> <p>The compartment containing this species.</p> <code>None</code> <code>hypothetical</code> <code>bool</code> <p>Whether the species is hypothetical or not</p> <code>False</code> <code>active</code> <code>bool</code> <p>Whether the species is active or not</p> <code>False</code> <code>homomultimer</code> <code>int</code> <p>The number of subunits forming the species</p> <code>1</code> <code>template</code> <code>TruncatedProteinTemplate</code> <p>The template of the truncated protein</p> required <code>modifications</code> <code>frozenset[Modification]</code> <p>The modifications of the protein</p> <code>&lt;dynamic&gt;</code> <code>structural_states</code> <code>frozenset[StructuralState]</code> <p>The structural states of the protein</p> <code>&lt;dynamic&gt;</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncatedProtein.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncatedProteinActiveLayout","title":"TruncatedProteinActiveLayout  <code>dataclass</code>","text":"<pre><code>TruncatedProteinActiveLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = NoneValue, filter_: NoneValueType | Filter | None = None, height: float = 50.0 + DEFAULT_ACTIVE_YSEP * 2, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = (4, 2), stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = 1.0, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 80.0 + DEFAULT_ACTIVE_XSEP * 2, offset: float = 3.0, subunits_stroke: tuple[NoneValueType | Color, ...] | None = None, subunits_stroke_width: tuple[NoneValueType | float, ...] | None = None, subunits_stroke_dasharray: tuple[NoneValueType | tuple[float, ...], ...] | None = None, subunits_stroke_dashoffset: tuple[float, ...] | None = None, subunits_fill: tuple[NoneValueType | Color, ...] | None = None, subunits_transform: tuple[NoneValueType | tuple[Transformation, ...], ...] | None = None, subunits_filter: tuple[NoneValueType | Filter, ...] | None = None, n: int = 1, rounded_corners: float = 15.0, vertical_truncation: float = 0.4, horizontal_truncation: float = 0.2)\n</code></pre> <p>               Bases: <code>_MultiNodeMixin</code>, <code>CellDesignerNode</code></p> <p>Truncated protein active layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'a614ba52-b2da-4fb1-868a-0b5e575ddaad'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>&lt;momapy.drawing.NoneValueType object at 0x7f8b17323e00&gt;</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>58.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>(4, 2)</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The stroke width of the node</p> <code>1.0</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>88.0</code> <code>offset</code> <code>float</code> <p>Offset distance between stacked units.</p> <code>3.0</code> <code>subunits_stroke</code> <code>tuple[NoneValueType | Color, ...] | None</code> <p>Tuple of stroke colors for each subunit.</p> <code>None</code> <code>subunits_stroke_width</code> <code>tuple[NoneValueType | float, ...] | None</code> <p>Tuple of stroke widths for each subunit.</p> <code>None</code> <code>subunits_stroke_dasharray</code> <code>tuple[NoneValueType | tuple[float, ...], ...] | None</code> <p>Tuple of dash patterns for each subunit.</p> <code>None</code> <code>subunits_stroke_dashoffset</code> <code>tuple[float, ...] | None</code> <p>Tuple of dash offsets for each subunit.</p> <code>None</code> <code>subunits_fill</code> <code>tuple[NoneValueType | Color, ...] | None</code> <p>Tuple of fill colors for each subunit.</p> <code>None</code> <code>subunits_transform</code> <code>tuple[NoneValueType | tuple[Transformation, ...], ...] | None</code> <p>Tuple of transformations for each subunit.</p> <code>None</code> <code>subunits_filter</code> <code>tuple[NoneValueType | Filter, ...] | None</code> <p>Tuple of filters for each subunit.</p> <code>None</code> <code>n</code> <code>int</code> <p>Number of stacked node copies to draw.</p> <code>1</code> <code>rounded_corners</code> <code>float</code> <p>The radius of the rounded corners.</p> <code>15.0</code> <code>vertical_truncation</code> <code>float</code> <p>The proportion of the height removed by the truncation.</p> <code>0.4</code> <code>horizontal_truncation</code> <code>float</code> <p>The proportion of the width removed by the truncation.</p> <code>0.2</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>angle</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Return the east anchor of the node.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Get the center position for the node label.</p> <code>north</code> <p>Return the north anchor of the node.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Return the south anchor of the node.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Return the west anchor of the node.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncatedProteinActiveLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncatedProteinActiveLayout.angle","title":"angle","text":"<pre><code>angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncatedProteinActiveLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncatedProteinActiveLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncatedProteinActiveLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncatedProteinActiveLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncatedProteinActiveLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncatedProteinActiveLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncatedProteinActiveLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncatedProteinActiveLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncatedProteinActiveLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Return the east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Return the east anchor of the node.\"\"\"\n    return self.own_angle(0)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncatedProteinActiveLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncatedProteinActiveLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncatedProteinActiveLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncatedProteinActiveLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncatedProteinActiveLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Get the center position for the node label.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point where the label should be centered.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Get the center position for the node label.\n\n    Returns:\n        Point where the label should be centered.\n    \"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2 - (self._n - 1) * self.offset,\n        self.height / 2 - height / 2 - (self._n - 1) * self.offset,\n    )\n    return self._make_subunit_shape(\n        position=position, width=width, height=height\n    ).position\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncatedProteinActiveLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Return the north anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Return the north anchor of the node.\"\"\"\n    return self.own_angle(90)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncatedProteinActiveLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncatedProteinActiveLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncatedProteinActiveLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncatedProteinActiveLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncatedProteinActiveLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncatedProteinActiveLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncatedProteinActiveLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncatedProteinActiveLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncatedProteinActiveLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncatedProteinActiveLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncatedProteinActiveLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncatedProteinActiveLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Return the south anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Return the south anchor of the node.\"\"\"\n    return self.own_angle(270)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncatedProteinActiveLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncatedProteinActiveLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncatedProteinActiveLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncatedProteinActiveLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncatedProteinActiveLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncatedProteinActiveLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Return the west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Return the west anchor of the node.\"\"\"\n    return self.own_angle(180)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncatedProteinActiveLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncatedProteinActiveLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncatedProteinActiveLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncatedProteinActiveLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncatedProteinLayout","title":"TruncatedProteinLayout  <code>dataclass</code>","text":"<pre><code>TruncatedProteinLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = from_hex('#CCFFCC'), filter_: NoneValueType | Filter | None = None, height: float = 50.0, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = 1.0, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 80.0, offset: float = 3.0, subunits_stroke: tuple[NoneValueType | Color, ...] | None = None, subunits_stroke_width: tuple[NoneValueType | float, ...] | None = None, subunits_stroke_dasharray: tuple[NoneValueType | tuple[float, ...], ...] | None = None, subunits_stroke_dashoffset: tuple[float, ...] | None = None, subunits_fill: tuple[NoneValueType | Color, ...] | None = None, subunits_transform: tuple[NoneValueType | tuple[Transformation, ...], ...] | None = None, subunits_filter: tuple[NoneValueType | Filter, ...] | None = None, n: int = 1, rounded_corners: float = 15.0, vertical_truncation: float = 0.4, horizontal_truncation: float = 0.2)\n</code></pre> <p>               Bases: <code>_MultiNodeMixin</code>, <code>CellDesignerNode</code></p> <p>Truncated protein layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'a5e3c6ca-3cc6-4fd3-b6f7-1dd5fb240d3a'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>Color(red=204, green=255, blue=204, alpha=1.0)</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>50.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The stroke width of the node</p> <code>1.0</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>80.0</code> <code>offset</code> <code>float</code> <p>Offset distance between stacked units.</p> <code>3.0</code> <code>subunits_stroke</code> <code>tuple[NoneValueType | Color, ...] | None</code> <p>Tuple of stroke colors for each subunit.</p> <code>None</code> <code>subunits_stroke_width</code> <code>tuple[NoneValueType | float, ...] | None</code> <p>Tuple of stroke widths for each subunit.</p> <code>None</code> <code>subunits_stroke_dasharray</code> <code>tuple[NoneValueType | tuple[float, ...], ...] | None</code> <p>Tuple of dash patterns for each subunit.</p> <code>None</code> <code>subunits_stroke_dashoffset</code> <code>tuple[float, ...] | None</code> <p>Tuple of dash offsets for each subunit.</p> <code>None</code> <code>subunits_fill</code> <code>tuple[NoneValueType | Color, ...] | None</code> <p>Tuple of fill colors for each subunit.</p> <code>None</code> <code>subunits_transform</code> <code>tuple[NoneValueType | tuple[Transformation, ...], ...] | None</code> <p>Tuple of transformations for each subunit.</p> <code>None</code> <code>subunits_filter</code> <code>tuple[NoneValueType | Filter, ...] | None</code> <p>Tuple of filters for each subunit.</p> <code>None</code> <code>n</code> <code>int</code> <p>Number of stacked node copies to draw.</p> <code>1</code> <code>rounded_corners</code> <code>float</code> <p>The radius of the rounded corners.</p> <code>15.0</code> <code>vertical_truncation</code> <code>float</code> <p>The proportion of the height removed by the truncation.</p> <code>0.4</code> <code>horizontal_truncation</code> <code>float</code> <p>The proportion of the width removed by the truncation.</p> <code>0.2</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>angle</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Return the east anchor of the node.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Get the center position for the node label.</p> <code>north</code> <p>Return the north anchor of the node.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Return the south anchor of the node.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Return the west anchor of the node.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncatedProteinLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncatedProteinLayout.angle","title":"angle","text":"<pre><code>angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncatedProteinLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncatedProteinLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncatedProteinLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncatedProteinLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncatedProteinLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncatedProteinLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncatedProteinLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncatedProteinLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncatedProteinLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Return the east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Return the east anchor of the node.\"\"\"\n    return self.own_angle(0)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncatedProteinLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncatedProteinLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncatedProteinLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncatedProteinLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncatedProteinLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Get the center position for the node label.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point where the label should be centered.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Get the center position for the node label.\n\n    Returns:\n        Point where the label should be centered.\n    \"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2 - (self._n - 1) * self.offset,\n        self.height / 2 - height / 2 - (self._n - 1) * self.offset,\n    )\n    return self._make_subunit_shape(\n        position=position, width=width, height=height\n    ).position\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncatedProteinLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Return the north anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Return the north anchor of the node.\"\"\"\n    return self.own_angle(90)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncatedProteinLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncatedProteinLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncatedProteinLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncatedProteinLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncatedProteinLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncatedProteinLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncatedProteinLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncatedProteinLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncatedProteinLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncatedProteinLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncatedProteinLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncatedProteinLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Return the south anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Return the south anchor of the node.\"\"\"\n    return self.own_angle(270)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncatedProteinLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncatedProteinLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncatedProteinLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncatedProteinLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncatedProteinLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncatedProteinLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Return the west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Return the west anchor of the node.\"\"\"\n    return self.own_angle(180)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncatedProteinLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncatedProteinLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncatedProteinLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncatedProteinLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncatedProteinTemplate","title":"TruncatedProteinTemplate  <code>dataclass</code>","text":"<pre><code>TruncatedProteinTemplate(*, id_: str = make_uuid4_as_str(), name: str, modification_residues: frozenset[ModificationResidue] = frozenset())\n</code></pre> <p>               Bases: <code>ProteinTemplate</code></p> <p>Truncated protein template.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'b449abbf-720f-41a3-9e6f-84ced5716ec4'</code> <code>name</code> <code>str</code> <p>The name of the species template</p> required <code>modification_residues</code> <code>frozenset[ModificationResidue]</code> <p>The modification residues of the protein template</p> <code>&lt;dynamic&gt;</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncatedProteinTemplate.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.Truncation","title":"Truncation  <code>dataclass</code>","text":"<pre><code>Truncation(*, id_: str = make_uuid4_as_str(), name: str | None = None, sbo_term: str | None = None, metaid: str | None = None, reversible: bool = False, compartment: Compartment | None = None, reactants: frozenset[Reactant] = frozenset(), products: frozenset[Product] = frozenset(), modifiers: frozenset[KnownOrUnknownModulator] = frozenset(), has_external_source: bool = False, has_external_sink: bool = False)\n</code></pre> <p>               Bases: <code>Reaction</code></p> <p>Truncation.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'5c79ef15-7e66-4a9d-b993-9f39ca7d6834'</code> <code>name</code> <code>str | None</code> <p>Human-readable name of the element.</p> <code>None</code> <code>sbo_term</code> <code>str | None</code> <p>Optional SBO term identifier for semantic annotation.</p> <code>None</code> <code>metaid</code> <code>str | None</code> <p>Optional metadata identifier for RDF annotations.</p> <code>None</code> <code>reversible</code> <code>bool</code> <p>Whether the reaction can proceed in both directions.</p> <code>False</code> <code>compartment</code> <code>Compartment | None</code> <p>Optional compartment where the reaction occurs.</p> <code>None</code> <code>reactants</code> <code>frozenset[Reactant]</code> <p>The reactants of the reaction</p> <code>&lt;dynamic&gt;</code> <code>products</code> <code>frozenset[Product]</code> <p>The products of the reaction</p> <code>&lt;dynamic&gt;</code> <code>modifiers</code> <code>frozenset[KnownOrUnknownModulator]</code> <p>The modifiers of the reaction</p> <code>&lt;dynamic&gt;</code> <code>has_external_source</code> <code>bool</code> <p>Whether the reaction has an unspecified external source (a Degraded reactant in CellDesigner).</p> <code>False</code> <code>has_external_sink</code> <code>bool</code> <p>Whether the reaction has an unspecified external sink (a Degraded product in CellDesigner).</p> <code>False</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.Truncation.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncationLayout","title":"TruncationLayout  <code>dataclass</code>","text":"<pre><code>TruncationLayout(*, left_connector_fraction: float = 0.375, right_connector_fraction: float = 0.625, reaction_node_font_family: str = DEFAULT_FONT_FAMILY, reaction_node_font_fill: Color | NoneValueType = black, reaction_node_font_stroke: Color | NoneValueType = NoneValue, reaction_node_font_style: FontStyle = NORMAL, reaction_node_font_weight: FontWeight | float = BOLD, reaction_node_text: str | None = 'N', reaction_node_height: float = 10.0, reaction_node_width: float = 10.0, reaction_node_segment: int = 1, reaction_node_stroke: NoneValueType | Color | None = black, reaction_node_stroke_width: float | None = 1.0, reaction_node_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, reaction_node_stroke_dashoffset: float | None = None, reaction_node_fill: NoneValueType | Color | None = white, reaction_node_transform: NoneValueType | tuple[Transformation, ...] | None = None, reaction_node_filter: NoneValueType | Filter | None = None, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, end_shorten: float = 2.0, fill: NoneValueType | Color | None = None, filter_: NoneValueType | Filter | None = None, path_fill: NoneValueType | Color | None = NoneValue, path_filter: NoneValueType | Filter | None = None, path_stroke: NoneValueType | Color | None = black, path_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, path_stroke_dashoffset: NoneValueType | float | None = None, path_stroke_width: float | None = 1.0, path_transform: NoneValueType | tuple[Transformation, ...] | None = None, stroke: NoneValueType | Color | None = None, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: NoneValueType | float | None = None, segments: tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...] = tuple(), source: LayoutElement | None = None, start_shorten: float = 2.0, target: LayoutElement | None = None, transform: NoneValueType | tuple[Transformation, ...] | None = None, end_arrowhead_fill: NoneValueType | Color | None = white, end_arrowhead_filter: NoneValueType | Filter | None = None, end_arrowhead_stroke: NoneValueType | Color | None = black, end_arrowhead_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, end_arrowhead_stroke_dashoffset: float | None = None, end_arrowhead_stroke_width: float | None = 1.0, end_arrowhead_transform: NoneValueType | tuple[Transformation, ...] | None = None, start_arrowhead_fill: NoneValueType | Color | None = black, start_arrowhead_filter: NoneValueType | Filter | None = None, start_arrowhead_stroke: NoneValueType | Color | None = black, start_arrowhead_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, start_arrowhead_stroke_dashoffset: float | None = None, start_arrowhead_stroke_width: float | None = 1.0, start_arrowhead_transform: NoneValueType | tuple[Transformation, ...] | None = None, reversible: bool = False, end_arrowhead_height: float = 10.0, end_arrowhead_sep: float = 2.0, end_arrowhead_width: float = 10.0, start_arrowhead_height: float = 8.0, start_arrowhead_width: float = 15.0)\n</code></pre> <p>               Bases: <code>ReactionLayout</code>, <code>_ReactionNodeMixin</code></p> <p>Truncation layout.</p> <p>Parameters:</p> Name Type Description Default <code>left_connector_fraction</code> <code>float</code> <p>The fraction along the segment where the left connector attaches.</p> <code>0.375</code> <code>right_connector_fraction</code> <code>float</code> <p>The fraction along the segment where the right connector attaches.</p> <code>0.625</code> <code>reaction_node_font_family</code> <code>str</code> <p>The font family of the reaction node label.</p> <code>'DejaVu Sans'</code> <code>reaction_node_font_fill</code> <code>Color | NoneValueType</code> <p>The fill color of the reaction node label.</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>reaction_node_font_stroke</code> <code>Color | NoneValueType</code> <p>The stroke color of the reaction node label.</p> <code>&lt;momapy.drawing.NoneValueType object at 0x7f8b17323e00&gt;</code> <code>reaction_node_font_style</code> <code>FontStyle</code> <p>The font style of the reaction node label.</p> <code>&lt;FontStyle.NORMAL: 0&gt;</code> <code>reaction_node_font_weight</code> <code>FontWeight | float</code> <p>The font weight of the reaction node label.</p> <code>&lt;FontWeight.BOLD: 1&gt;</code> <code>reaction_node_text</code> <code>str | None</code> <p>The text of the reaction node label.</p> <code>'N'</code> <code>reaction_node_height</code> <code>float</code> <p>The height of the reaction node.</p> <code>10.0</code> <code>reaction_node_width</code> <code>float</code> <p>The width of the reaction node.</p> <code>10.0</code> <code>reaction_node_segment</code> <code>int</code> <p>The index of the arc segment carrying the reaction node.</p> <code>1</code> <code>reaction_node_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the reaction node.</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>reaction_node_stroke_width</code> <code>float | None</code> <p>The stroke width of the reaction node.</p> <code>1.0</code> <code>reaction_node_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The dash pattern of the reaction node's border.</p> <code>None</code> <code>reaction_node_stroke_dashoffset</code> <code>float | None</code> <p>The dash offset of the reaction node's border.</p> <code>None</code> <code>reaction_node_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the reaction node.</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>reaction_node_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform applied to the reaction node.</p> <code>None</code> <code>reaction_node_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter applied to the reaction node.</p> <code>None</code> <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'84b77846-cc17-4baf-909e-0461c3df1ce9'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>end_shorten</code> <code>float</code> <p>The length the end of the arc will be shorten by</p> <code>2.0</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the arc</p> <code>None</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the arc</p> <code>None</code> <code>path_fill</code> <code>NoneValueType | Color | None</code> <p>The path fill color of the arc</p> <code>&lt;momapy.drawing.NoneValueType object at 0x7f8b17323e00&gt;</code> <code>path_filter</code> <code>NoneValueType | Filter | None</code> <p>The path filter of the arc</p> <code>None</code> <code>path_stroke</code> <code>NoneValueType | Color | None</code> <p>The path stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>path_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The path stroke dasharray of the arc</p> <code>None</code> <code>path_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The path stroke dashoffset of the arc</p> <code>None</code> <code>path_stroke_width</code> <code>float | None</code> <p>The path stroke width of the arc</p> <code>1.0</code> <code>path_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The path transform of the arc</p> <code>None</code> <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the arc</p> <code>None</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the arc</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the arc</p> <code>None</code> <code>stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the arc</p> <code>None</code> <code>segments</code> <code>tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...]</code> <p>The path segments of the arc</p> <code>&lt;dynamic&gt;</code> <code>source</code> <code>LayoutElement | None</code> <p>The source of the arc</p> <code>None</code> <code>start_shorten</code> <code>float</code> <p>The length the start of the arc will be shorten by</p> <code>2.0</code> <code>target</code> <code>LayoutElement | None</code> <p>The target of the arc</p> <code>None</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the arc</p> <code>None</code> <code>end_arrowhead_fill</code> <code>NoneValueType | Color | None</code> <p>The end arrowhead fill color of the arc</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>end_arrowhead_filter</code> <code>NoneValueType | Filter | None</code> <p>The end arrowhead filter of the arc</p> <code>None</code> <code>end_arrowhead_stroke</code> <code>NoneValueType | Color | None</code> <p>The end arrowhead stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>end_arrowhead_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The end arrowhead stroke dasharray of the arc</p> <code>None</code> <code>end_arrowhead_stroke_dashoffset</code> <code>float | None</code> <p>The end arrowhead stroke dashoffset of the arc</p> <code>None</code> <code>end_arrowhead_stroke_width</code> <code>float | None</code> <p>The end arrowhead stroke width of the arc</p> <code>1.0</code> <code>end_arrowhead_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The end arrowhead transform of the arc</p> <code>None</code> <code>start_arrowhead_fill</code> <code>NoneValueType | Color | None</code> <p>The start arrowhead fill color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>start_arrowhead_filter</code> <code>NoneValueType | Filter | None</code> <p>The start arrowhead filter of the arc</p> <code>None</code> <code>start_arrowhead_stroke</code> <code>NoneValueType | Color | None</code> <p>The start arrowhead stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>start_arrowhead_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The start arrowhead stroke dasharray of the arc</p> <code>None</code> <code>start_arrowhead_stroke_dashoffset</code> <code>float | None</code> <p>The start arrowhead stroke dashoffset of the arc</p> <code>None</code> <code>start_arrowhead_stroke_width</code> <code>float | None</code> <p>The start arrowhead stroke width of the arc</p> <code>1.0</code> <code>start_arrowhead_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The start arrowhead transform of the arc</p> <code>None</code> <code>reversible</code> <code>bool</code> <p>Whether the reaction is reversible.</p> <code>False</code> <code>end_arrowhead_height</code> <code>float</code> <p>The height of the end arrowhead.</p> <code>10.0</code> <code>end_arrowhead_sep</code> <code>float</code> <p>The separation between the outer and inner end circles.</p> <code>2.0</code> <code>end_arrowhead_width</code> <code>float</code> <p>The width of the end arrowhead.</p> <code>10.0</code> <code>start_arrowhead_height</code> <code>float</code> <p>The height of the start arrowhead.</p> <code>8.0</code> <code>start_arrowhead_width</code> <code>float</code> <p>The width of the start arrowhead.</p> <code>15.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>childless</code> <p>Return a copy of the arc with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>end_arrowhead_base</code> <p>Return the base anchor point of the double-headed arc end arrowhead.</p> <code>end_arrowhead_bbox</code> <p>Return the bounding box of the double-headed arc end arrowhead.</p> <code>end_arrowhead_border</code> <p>Return the point at the intersection of the drawing elements of the double-headed arc end arrowhead and the line going through the center of these drawing elements and the given point.</p> <code>end_arrowhead_drawing_elements</code> <p>Return the drawing elements of the double-headed arc end arrowhead.</p> <code>end_arrowhead_length</code> <p>Return the length of the double-headed arc end arrowhead.</p> <code>end_arrowhead_tip</code> <p>Return the tip anchor point of the double-headed arc end arrowhead.</p> <code>end_point</code> <p>Return the ending point of the arc.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>fraction</code> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <code>length</code> <p>Return the total length of the arc path.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_children</code> <p>Return the self children of the arc.</p> <code>own_drawing_elements</code> <p>Return the arc's own drawing elements, including those of its mixins.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>path_drawing_elements</code> <p>Return the drawing elements of the double-headed arc path.</p> <code>points</code> <p>Return the points of the arc path.</p> <code>start_arrowhead_base</code> <p>Return the base anchor point of the double-headed arc start arrowhead.</p> <code>start_arrowhead_bbox</code> <p>Return the bounding box of the double-headed arc start arrowhead.</p> <code>start_arrowhead_border</code> <p>Return the point at the intersection of the drawing elements of the double-headed arc start arrowhead and the line going through the center of these drawing elements and the given point.</p> <code>start_arrowhead_drawing_elements</code> <p>Return the drawing elements of the double-headed arc start arrowhead.</p> <code>start_arrowhead_length</code> <p>Return the length of the double-headed arc start arrowhead.</p> <code>start_arrowhead_tip</code> <p>Return the tip anchor point of the double-headed arc start arrowhead.</p> <code>start_point</code> <p>Return the starting point of the arc.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncationLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncationLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncationLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the arc with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the arc with no children.\"\"\"\n    return dataclasses.replace(self, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncationLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncationLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncationLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncationLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncationLayout.end_arrowhead_base","title":"end_arrowhead_base","text":"<pre><code>end_arrowhead_base() -&gt; Point\n</code></pre> <p>Return the base anchor point of the double-headed arc end arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_arrowhead_base(self) -&gt; Point:\n    \"\"\"Return the base anchor point of the double-headed arc end arrowhead.\"\"\"\n    arrowhead_length = self.end_arrowhead_length()\n    if arrowhead_length == 0:\n        return self.end_point()\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return self.end_arrowhead_tip() - (arrowhead_length, 0)\n    fraction = 1 - (arrowhead_length + self.end_shorten) / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncationLayout.end_arrowhead_bbox","title":"end_arrowhead_bbox","text":"<pre><code>end_arrowhead_bbox() -&gt; Bbox\n</code></pre> <p>Return the bounding box of the double-headed arc end arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_arrowhead_bbox(self) -&gt; Bbox:\n    \"\"\"Return the bounding box of the double-headed arc end arrowhead.\"\"\"\n    return get_drawing_elements_bbox(self.end_arrowhead_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncationLayout.end_arrowhead_border","title":"end_arrowhead_border","text":"<pre><code>end_arrowhead_border(point: Point) -&gt; Point\n</code></pre> <p>Return the point at the intersection of the drawing elements of the double-headed arc end arrowhead and the line going through the center of these drawing elements and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_arrowhead_border(self, point: Point) -&gt; Point:\n    \"\"\"Return the point at the intersection of the drawing elements of the double-headed arc end arrowhead and the line going through the center of these drawing elements and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    point = get_drawing_elements_border(\n        self.end_arrowhead_drawing_elements(), point\n    )\n    if point is None:\n        return self.end_arrowhead_tip()\n    return point\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncationLayout.end_arrowhead_drawing_elements","title":"end_arrowhead_drawing_elements","text":"<pre><code>end_arrowhead_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the double-headed arc end arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_arrowhead_drawing_elements(\n    self,\n) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the double-headed arc end arrowhead.\"\"\"\n    drawing_elements = self._end_arrowhead_border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_end_arrowhead\",\n        elements=tuple(drawing_elements),\n        fill=self.end_arrowhead_fill,\n        filter_=self.end_arrowhead_filter,\n        id_=f\"{self.id_}_end_arrowhead\",\n        stroke=self.end_arrowhead_stroke,\n        stroke_width=self.end_arrowhead_stroke_width,\n        stroke_dasharray=self.end_arrowhead_stroke_dasharray,\n        stroke_dashoffset=self.end_arrowhead_stroke_dashoffset,\n        transform=self.end_arrowhead_transform,\n    )\n    transformation = self._get_end_arrowhead_transformation()\n    group = group.transformed(transformation)\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncationLayout.end_arrowhead_length","title":"end_arrowhead_length","text":"<pre><code>end_arrowhead_length() -&gt; float\n</code></pre> <p>Return the length of the double-headed arc end arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_arrowhead_length(self) -&gt; float:\n    \"\"\"Return the length of the double-headed arc end arrowhead.\"\"\"\n    bbox = get_drawing_elements_bbox(self._end_arrowhead_border_drawing_elements())\n    if math.isnan(bbox.width):\n        return 0.0\n    return bbox.east().x\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncationLayout.end_arrowhead_tip","title":"end_arrowhead_tip","text":"<pre><code>end_arrowhead_tip() -&gt; Point\n</code></pre> <p>Return the tip anchor point of the double-headed arc end arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_arrowhead_tip(self) -&gt; Point:\n    \"\"\"Return the tip anchor point of the double-headed arc end arrowhead.\"\"\"\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return segment.p2\n    fraction = 1 - self.end_shorten / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncationLayout.end_point","title":"end_point","text":"<pre><code>end_point() -&gt; Point\n</code></pre> <p>Return the ending point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_point(self) -&gt; Point:\n    \"\"\"Return the ending point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[-1]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncationLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncationLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncationLayout.fraction","title":"fraction","text":"<pre><code>fraction(fraction: float) -&gt; tuple[Point, float]\n</code></pre> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def fraction(self, fraction: float) -&gt; tuple[Point, float]:\n    \"\"\"Return the position and angle on the arc at a given fraction (of the total arc length).\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    current_length = 0\n    length_to_reach = fraction * self.length()\n    for segment in self.segments:\n        current_length += segment.length()\n        if current_length &gt;= length_to_reach:\n            break\n    segment_start_length = current_length - segment.length()\n    segment_fraction = (length_to_reach - segment_start_length) / segment.length()\n    position, angle = segment.get_position_and_angle_at_fraction(segment_fraction)\n    return position, angle\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncationLayout.length","title":"length","text":"<pre><code>length() -&gt; float\n</code></pre> <p>Return the total length of the arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def length(self) -&gt; float:\n    \"\"\"Return the total length of the arc path.\"\"\"\n    return sum([segment.length() for segment in self.segments])\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncationLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncationLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the arc.\"\"\"\n    return []\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncationLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the arc's own drawing elements, including those of its mixins.</p> Source code in <code>src/momapy/celldesigner/elements.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the arc's own drawing elements, including those of its mixins.\"\"\"\n    drawing_elements = super_or_builder(\n        CellDesignerDoubleHeadedArc, self\n    ).own_drawing_elements()\n    done_bases = []\n    for base in type(self).__mro__:\n        if (\n            issubclass_or_builder(base, _SBGNMixin)\n            and base is not type(self)\n            and not any([issubclass(done_base, base) for done_base in done_bases])\n        ):\n            drawing_elements += getattr(base, \"_mixin_drawing_elements\")(self)\n            done_bases.append(base)\n    return drawing_elements\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncationLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncationLayout.path_drawing_elements","title":"path_drawing_elements","text":"<pre><code>path_drawing_elements() -&gt; list[Path]\n</code></pre> <p>Return the drawing elements of the double-headed arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def path_drawing_elements(self) -&gt; list[Path]:\n    \"\"\"Return the drawing elements of the double-headed arc path.\"\"\"\n    start_arrowhead_length = self.start_arrowhead_length()\n    end_arrowhead_length = self.end_arrowhead_length()\n    if len(self.segments) == 1:\n        segment = (\n            self.segments[0]\n            .shortened(self.start_shorten + start_arrowhead_length, \"start\")\n            .shortened(self.end_shorten + end_arrowhead_length, \"end\")\n        )\n        actions = [\n            MoveTo(segment.p1),\n            self._make_path_action_from_segment(segment),\n        ]\n    else:\n        first_segment = self.segments[0].shortened(\n            self.start_shorten + start_arrowhead_length, \"start\"\n        )\n        last_segment = self.segments[-1].shortened(\n            self.end_shorten + end_arrowhead_length, \"end\"\n        )\n        actions = [\n            MoveTo(first_segment.p1),\n            self._make_path_action_from_segment(first_segment),\n        ]\n        for segment in self.segments[1:-1]:\n            action = self._make_path_action_from_segment(segment)\n            actions.append(action)\n        actions.append(self._make_path_action_from_segment(last_segment))\n    path = Path(\n        actions=tuple(actions),\n        class_=f\"{type(self).__name__}_path\",\n        fill=self.path_fill,\n        filter_=self.path_filter,\n        id_=f\"{self.id_}_path\",\n        stroke=self.path_stroke,\n        stroke_dasharray=self.path_stroke_dasharray,\n        stroke_dashoffset=self.path_stroke_dashoffset,\n        stroke_width=self.path_stroke_width,\n        transform=self.path_transform,\n    )\n    return [path]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncationLayout.points","title":"points","text":"<pre><code>points() -&gt; list[Point]\n</code></pre> <p>Return the points of the arc path.</p> <p>An arc with no segments has no points and returns an empty list.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def points(self) -&gt; list[Point]:\n    \"\"\"Return the points of the arc path.\n\n    An arc with no segments has no points and returns an empty list.\n    \"\"\"\n    if not self.segments:\n        return []\n    points = []\n    for segment in self.segments:\n        points.append(segment.p1)\n    points.append(segment.p2)\n    return points\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncationLayout.start_arrowhead_base","title":"start_arrowhead_base","text":"<pre><code>start_arrowhead_base() -&gt; Point\n</code></pre> <p>Return the base anchor point of the double-headed arc start arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_arrowhead_base(self) -&gt; Point:\n    \"\"\"Return the base anchor point of the double-headed arc start arrowhead.\"\"\"\n    arrowhead_length = self.start_arrowhead_length()\n    if arrowhead_length == 0:\n        return self.start_point()\n    segment = self.segments[0]\n    segment = Segment(segment.p2, segment.p1)\n    segment_length = segment.length()\n    if segment_length == 0:\n        return self.start_arrowhead_tip() + (arrowhead_length, 0)\n    fraction = 1 - (arrowhead_length + self.start_shorten) / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncationLayout.start_arrowhead_bbox","title":"start_arrowhead_bbox","text":"<pre><code>start_arrowhead_bbox() -&gt; Bbox\n</code></pre> <p>Return the bounding box of the double-headed arc start arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_arrowhead_bbox(self) -&gt; Bbox:\n    \"\"\"Return the bounding box of the double-headed arc start arrowhead.\"\"\"\n    return get_drawing_elements_bbox(self.start_arrowhead_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncationLayout.start_arrowhead_border","title":"start_arrowhead_border","text":"<pre><code>start_arrowhead_border(point: Point) -&gt; Point\n</code></pre> <p>Return the point at the intersection of the drawing elements of the double-headed arc start arrowhead and the line going through the center of these drawing elements and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_arrowhead_border(self, point: Point) -&gt; Point:\n    \"\"\"Return the point at the intersection of the drawing elements of the double-headed arc start arrowhead and the line going through the center of these drawing elements and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    point = get_drawing_elements_border(\n        self.start_arrowhead_drawing_elements(), point\n    )\n    if point is None:\n        return self.start_arrowhead_tip()\n    return point\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncationLayout.start_arrowhead_drawing_elements","title":"start_arrowhead_drawing_elements","text":"<pre><code>start_arrowhead_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the double-headed arc start arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_arrowhead_drawing_elements(\n    self,\n) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the double-headed arc start arrowhead.\"\"\"\n    drawing_elements = self._start_arrowhead_border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_start_arrowhead\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}_start_arrowhead\",\n        fill=self.start_arrowhead_fill,\n        filter_=self.start_arrowhead_filter,\n        stroke=self.start_arrowhead_stroke,\n        stroke_dasharray=self.start_arrowhead_stroke_dasharray,\n        stroke_dashoffset=self.start_arrowhead_stroke_dashoffset,\n        stroke_width=self.start_arrowhead_stroke_width,\n        transform=self.start_arrowhead_transform,\n    )\n    transformation = self._get_start_arrowhead_transformation()\n    group = group.transformed(transformation)\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncationLayout.start_arrowhead_length","title":"start_arrowhead_length","text":"<pre><code>start_arrowhead_length() -&gt; float\n</code></pre> <p>Return the length of the double-headed arc start arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_arrowhead_length(self) -&gt; float:\n    \"\"\"Return the length of the double-headed arc start arrowhead.\"\"\"\n    bbox = get_drawing_elements_bbox(\n        self._start_arrowhead_border_drawing_elements()\n    )\n    if math.isnan(bbox.width):\n        return 0.0\n    return abs(bbox.west().x)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncationLayout.start_arrowhead_tip","title":"start_arrowhead_tip","text":"<pre><code>start_arrowhead_tip() -&gt; Point\n</code></pre> <p>Return the tip anchor point of the double-headed arc start arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_arrowhead_tip(self) -&gt; Point:\n    \"\"\"Return the tip anchor point of the double-headed arc start arrowhead.\"\"\"\n    segment = self.segments[0]\n    segment = Segment(segment.p2, segment.p1)\n    segment_length = segment.length()\n    if segment_length == 0:\n        return segment.p2\n    fraction = 1 - self.start_shorten / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncationLayout.start_point","title":"start_point","text":"<pre><code>start_point() -&gt; Point\n</code></pre> <p>Return the starting point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_point(self) -&gt; Point:\n    \"\"\"Return the starting point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[0]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.TruncationLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.Unknown","title":"Unknown  <code>dataclass</code>","text":"<pre><code>Unknown(*, id_: str = make_uuid4_as_str(), name: str | None = None, sbo_term: str | None = None, metaid: str | None = None, compartment: Compartment | None = None, hypothetical: bool = False, active: bool = False, homomultimer: int = 1)\n</code></pre> <p>               Bases: <code>Species</code></p> <p>Unknown.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'cde46a39-d3aa-4782-bdb4-5ca0cc1b61a0'</code> <code>name</code> <code>str | None</code> <p>Human-readable name of the element.</p> <code>None</code> <code>sbo_term</code> <code>str | None</code> <p>Optional SBO term identifier for semantic annotation.</p> <code>None</code> <code>metaid</code> <code>str | None</code> <p>Optional metadata identifier for RDF annotations.</p> <code>None</code> <code>compartment</code> <code>Compartment | None</code> <p>The compartment containing this species.</p> <code>None</code> <code>hypothetical</code> <code>bool</code> <p>Whether the species is hypothetical or not</p> <code>False</code> <code>active</code> <code>bool</code> <p>Whether the species is active or not</p> <code>False</code> <code>homomultimer</code> <code>int</code> <p>The number of subunits forming the species</p> <code>1</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.Unknown.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownActiveLayout","title":"UnknownActiveLayout  <code>dataclass</code>","text":"<pre><code>UnknownActiveLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = NoneValue, filter_: NoneValueType | Filter | None = None, height: float = 30.0 + DEFAULT_ACTIVE_YSEP * 2, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = (4, 2), stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = 1.0, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 60.0 + DEFAULT_ACTIVE_XSEP * 2, offset: float = 3.0, subunits_stroke: tuple[NoneValueType | Color, ...] | None = None, subunits_stroke_width: tuple[NoneValueType | float, ...] | None = None, subunits_stroke_dasharray: tuple[NoneValueType | tuple[float, ...], ...] | None = None, subunits_stroke_dashoffset: tuple[float, ...] | None = None, subunits_fill: tuple[NoneValueType | Color, ...] | None = None, subunits_transform: tuple[NoneValueType | tuple[Transformation, ...], ...] | None = None, subunits_filter: tuple[NoneValueType | Filter, ...] | None = None, n: int = 1)\n</code></pre> <p>               Bases: <code>_MultiNodeMixin</code>, <code>CellDesignerNode</code></p> <p>Unknown active layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'5ca4a097-84d0-4510-a6ad-4c2672abf22b'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>&lt;momapy.drawing.NoneValueType object at 0x7f8b17323e00&gt;</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>38.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>(4, 2)</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The stroke width of the node</p> <code>1.0</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>68.0</code> <code>offset</code> <code>float</code> <p>Offset distance between stacked units.</p> <code>3.0</code> <code>subunits_stroke</code> <code>tuple[NoneValueType | Color, ...] | None</code> <p>Tuple of stroke colors for each subunit.</p> <code>None</code> <code>subunits_stroke_width</code> <code>tuple[NoneValueType | float, ...] | None</code> <p>Tuple of stroke widths for each subunit.</p> <code>None</code> <code>subunits_stroke_dasharray</code> <code>tuple[NoneValueType | tuple[float, ...], ...] | None</code> <p>Tuple of dash patterns for each subunit.</p> <code>None</code> <code>subunits_stroke_dashoffset</code> <code>tuple[float, ...] | None</code> <p>Tuple of dash offsets for each subunit.</p> <code>None</code> <code>subunits_fill</code> <code>tuple[NoneValueType | Color, ...] | None</code> <p>Tuple of fill colors for each subunit.</p> <code>None</code> <code>subunits_transform</code> <code>tuple[NoneValueType | tuple[Transformation, ...], ...] | None</code> <p>Tuple of transformations for each subunit.</p> <code>None</code> <code>subunits_filter</code> <code>tuple[NoneValueType | Filter, ...] | None</code> <p>Tuple of filters for each subunit.</p> <code>None</code> <code>n</code> <code>int</code> <p>Number of stacked node copies to draw.</p> <code>1</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>angle</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Return the east anchor of the node.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Get the center position for the node label.</p> <code>north</code> <p>Return the north anchor of the node.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Return the south anchor of the node.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Return the west anchor of the node.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownActiveLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownActiveLayout.angle","title":"angle","text":"<pre><code>angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownActiveLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownActiveLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownActiveLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownActiveLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownActiveLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownActiveLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownActiveLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownActiveLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownActiveLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Return the east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Return the east anchor of the node.\"\"\"\n    return self.own_angle(0)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownActiveLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownActiveLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownActiveLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownActiveLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownActiveLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Get the center position for the node label.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point where the label should be centered.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Get the center position for the node label.\n\n    Returns:\n        Point where the label should be centered.\n    \"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2 - (self._n - 1) * self.offset,\n        self.height / 2 - height / 2 - (self._n - 1) * self.offset,\n    )\n    return self._make_subunit_shape(\n        position=position, width=width, height=height\n    ).position\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownActiveLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Return the north anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Return the north anchor of the node.\"\"\"\n    return self.own_angle(90)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownActiveLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownActiveLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownActiveLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownActiveLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownActiveLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownActiveLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownActiveLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownActiveLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownActiveLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownActiveLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownActiveLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownActiveLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Return the south anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Return the south anchor of the node.\"\"\"\n    return self.own_angle(270)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownActiveLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownActiveLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownActiveLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownActiveLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownActiveLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownActiveLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Return the west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Return the west anchor of the node.\"\"\"\n    return self.own_angle(180)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownActiveLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownActiveLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownActiveLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownActiveLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownCatalysis","title":"UnknownCatalysis  <code>dataclass</code>","text":"<pre><code>UnknownCatalysis(*, id_: str = make_uuid4_as_str(), source: Species | BooleanLogicGate, target: Species | None)\n</code></pre> <p>               Bases: <code>UnknownPhysicalStimulation</code></p> <p>Unknown catalysis.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'1623f8e4-4e79-4e11-a8a4-aab116e954f4'</code> <code>source</code> <code>Species | BooleanLogicGate</code> <p>The source of the modulation</p> required <code>target</code> <code>Species | None</code> <p>The target of the modulation</p> required <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownCatalysis.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownCatalysisLayout","title":"UnknownCatalysisLayout  <code>dataclass</code>","text":"<pre><code>UnknownCatalysisLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, end_shorten: float = 0.0, fill: NoneValueType | Color | None = None, filter_: NoneValueType | Filter | None = None, path_fill: NoneValueType | Color | None = NoneValue, path_filter: NoneValueType | Filter | None = None, path_stroke: NoneValueType | Color | None = black, path_stroke_dasharray: NoneValueType | tuple[float, ...] | None = (12, 4), path_stroke_dashoffset: NoneValueType | float | None = None, path_stroke_width: float | None = 1.0, path_transform: NoneValueType | tuple[Transformation, ...] | None = None, stroke: NoneValueType | Color | None = None, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: NoneValueType | float | None = None, segments: tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...] = tuple(), source: LayoutElement | None = None, start_shorten: float = 0.0, target: LayoutElement | None = None, transform: NoneValueType | tuple[Transformation, ...] | None = None, arrowhead_fill: NoneValueType | Color | None = white, arrowhead_filter: NoneValueType | Filter | None = None, arrowhead_stroke: NoneValueType | Color | None = black, arrowhead_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, arrowhead_stroke_dashoffset: NoneValueType | float | None = None, arrowhead_stroke_width: float | None = 1.0, arrowhead_transform: NoneValueType | tuple[Transformation, ...] | None = None, arrowhead_height: float = 7.0, arrowhead_width: float = 7.0)\n</code></pre> <p>               Bases: <code>CellDesignerSingleHeadedArc</code></p> <p>Unknown catalysis layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'cd1e342c-3b7f-4ec4-adca-7143f609f61a'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>end_shorten</code> <code>float</code> <p>The length the end of the arc will be shorten by</p> <code>0.0</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the arc</p> <code>None</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the arc</p> <code>None</code> <code>path_fill</code> <code>NoneValueType | Color | None</code> <p>The path fill color of the arc</p> <code>&lt;momapy.drawing.NoneValueType object at 0x7f8b17323e00&gt;</code> <code>path_filter</code> <code>NoneValueType | Filter | None</code> <p>The path filter of the arc</p> <code>None</code> <code>path_stroke</code> <code>NoneValueType | Color | None</code> <p>The path stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>path_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The path stroke dasharray of the arc</p> <code>(12, 4)</code> <code>path_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The path stroke dashoffset of the arc</p> <code>None</code> <code>path_stroke_width</code> <code>float | None</code> <p>The path stroke width of the arc</p> <code>1.0</code> <code>path_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The path transform of the arc</p> <code>None</code> <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the arc</p> <code>None</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the arc</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the arc</p> <code>None</code> <code>stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the arc</p> <code>None</code> <code>segments</code> <code>tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...]</code> <p>The path segments of the arc</p> <code>&lt;dynamic&gt;</code> <code>source</code> <code>LayoutElement | None</code> <p>The source of the arc</p> <code>None</code> <code>start_shorten</code> <code>float</code> <p>The length the start of the arc will be shorten by</p> <code>0.0</code> <code>target</code> <code>LayoutElement | None</code> <p>The target of the arc</p> <code>None</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the arc</p> <code>None</code> <code>arrowhead_fill</code> <code>NoneValueType | Color | None</code> <p>The arrowhead fill color of the arc</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>arrowhead_filter</code> <code>NoneValueType | Filter | None</code> <p>The arrowhead filter of the arc</p> <code>None</code> <code>arrowhead_stroke</code> <code>NoneValueType | Color | None</code> <p>The arrowhead stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>arrowhead_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The arrowhead stroke dasharray of the arc</p> <code>None</code> <code>arrowhead_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The arrowhead stroke dashoffset of the arc</p> <code>None</code> <code>arrowhead_stroke_width</code> <code>float | None</code> <p>The arrowhead stroke width of the arc</p> <code>1.0</code> <code>arrowhead_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The arrowhead transform of the arc</p> <code>None</code> <code>arrowhead_height</code> <code>float</code> <p>The height of the arrowhead.</p> <code>7.0</code> <code>arrowhead_width</code> <code>float</code> <p>The width of the arrowhead.</p> <code>7.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>arrowhead_base</code> <p>Return the arrowhead base anchor point of the single-headed arc.</p> <code>arrowhead_bbox</code> <p>Return the bounding box of the single-headed arc arrowhead.</p> <code>arrowhead_border</code> <p>Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.</p> <code>arrowhead_drawing_elements</code> <p>Return the drawing elements of the single-headed arc arrowhead.</p> <code>arrowhead_length</code> <p>Return the length of the single-headed arc arrowhead.</p> <code>arrowhead_tip</code> <p>Return the arrowhead tip anchor point of the single-headed arc.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>childless</code> <p>Return a copy of the arc with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>end_point</code> <p>Return the ending point of the arc.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>fraction</code> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <code>length</code> <p>Return the total length of the arc path.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_children</code> <p>Return the self children of the arc.</p> <code>own_drawing_elements</code> <p>Return the arc's own drawing elements, including those of its mixins.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>path_drawing_elements</code> <p>Return the drawing elements of the single-headed arc path.</p> <code>points</code> <p>Return the points of the arc path.</p> <code>start_point</code> <p>Return the starting point of the arc.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownCatalysisLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownCatalysisLayout.arrowhead_base","title":"arrowhead_base","text":"<pre><code>arrowhead_base() -&gt; Point\n</code></pre> <p>Return the arrowhead base anchor point of the single-headed arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_base(self) -&gt; Point:\n    \"\"\"Return the arrowhead base anchor point of the single-headed arc.\"\"\"\n    arrowhead_length = self.arrowhead_length()\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return self.arrowhead_tip() - (arrowhead_length, 0)\n    fraction = 1 - (arrowhead_length + self.end_shorten) / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownCatalysisLayout.arrowhead_bbox","title":"arrowhead_bbox","text":"<pre><code>arrowhead_bbox() -&gt; Bbox\n</code></pre> <p>Return the bounding box of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_bbox(self) -&gt; Bbox:\n    \"\"\"Return the bounding box of the single-headed arc arrowhead.\"\"\"\n    return get_drawing_elements_bbox(self.arrowhead_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownCatalysisLayout.arrowhead_border","title":"arrowhead_border","text":"<pre><code>arrowhead_border(point: Point) -&gt; Point\n</code></pre> <p>Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_border(self, point: Point) -&gt; Point:\n    \"\"\"Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    point = get_drawing_elements_border(self.arrowhead_drawing_elements(), point)\n    if point is None:\n        return self.arrowhead_tip()\n    return point\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownCatalysisLayout.arrowhead_drawing_elements","title":"arrowhead_drawing_elements","text":"<pre><code>arrowhead_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_drawing_elements(\n    self,\n) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the single-headed arc arrowhead.\"\"\"\n    drawing_elements = self._arrowhead_border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_arrowhead\",\n        elements=tuple(drawing_elements),\n        fill=self.arrowhead_fill,\n        filter_=self.arrowhead_filter,\n        id_=f\"{self.id_}_arrowhead\",\n        stroke=self.arrowhead_stroke,\n        stroke_dasharray=self.arrowhead_stroke_dasharray,\n        stroke_dashoffset=self.arrowhead_stroke_dashoffset,\n        stroke_width=self.arrowhead_stroke_width,\n        transform=self.arrowhead_transform,\n    )\n    transformation = self._get_arrowhead_transformation()\n    group = group.transformed(transformation)\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownCatalysisLayout.arrowhead_length","title":"arrowhead_length","text":"<pre><code>arrowhead_length() -&gt; float\n</code></pre> <p>Return the length of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_length(self) -&gt; float:\n    \"\"\"Return the length of the single-headed arc arrowhead.\"\"\"\n    bbox = get_drawing_elements_bbox(self._arrowhead_border_drawing_elements())\n    if math.isnan(bbox.width):\n        return 0.0\n    return bbox.east().x\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownCatalysisLayout.arrowhead_tip","title":"arrowhead_tip","text":"<pre><code>arrowhead_tip() -&gt; Point\n</code></pre> <p>Return the arrowhead tip anchor point of the single-headed arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_tip(self) -&gt; Point:\n    \"\"\"Return the arrowhead tip anchor point of the single-headed arc.\"\"\"\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return segment.p2\n    fraction = 1 - self.end_shorten / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownCatalysisLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownCatalysisLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the arc with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the arc with no children.\"\"\"\n    return dataclasses.replace(self, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownCatalysisLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownCatalysisLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownCatalysisLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownCatalysisLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownCatalysisLayout.end_point","title":"end_point","text":"<pre><code>end_point() -&gt; Point\n</code></pre> <p>Return the ending point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_point(self) -&gt; Point:\n    \"\"\"Return the ending point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[-1]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownCatalysisLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownCatalysisLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownCatalysisLayout.fraction","title":"fraction","text":"<pre><code>fraction(fraction: float) -&gt; tuple[Point, float]\n</code></pre> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def fraction(self, fraction: float) -&gt; tuple[Point, float]:\n    \"\"\"Return the position and angle on the arc at a given fraction (of the total arc length).\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    current_length = 0\n    length_to_reach = fraction * self.length()\n    for segment in self.segments:\n        current_length += segment.length()\n        if current_length &gt;= length_to_reach:\n            break\n    segment_start_length = current_length - segment.length()\n    segment_fraction = (length_to_reach - segment_start_length) / segment.length()\n    position, angle = segment.get_position_and_angle_at_fraction(segment_fraction)\n    return position, angle\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownCatalysisLayout.length","title":"length","text":"<pre><code>length() -&gt; float\n</code></pre> <p>Return the total length of the arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def length(self) -&gt; float:\n    \"\"\"Return the total length of the arc path.\"\"\"\n    return sum([segment.length() for segment in self.segments])\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownCatalysisLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownCatalysisLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the arc.\"\"\"\n    return []\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownCatalysisLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the arc's own drawing elements, including those of its mixins.</p> Source code in <code>src/momapy/celldesigner/elements.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the arc's own drawing elements, including those of its mixins.\"\"\"\n    drawing_elements = super_or_builder(\n        CellDesignerSingleHeadedArc, self\n    ).own_drawing_elements()\n    done_bases = []\n    for base in type(self).__mro__:\n        if (\n            issubclass_or_builder(base, _SBGNMixin)\n            and base is not type(self)\n            and not any([issubclass(done_base, base) for done_base in done_bases])\n        ):\n            drawing_elements += getattr(base, \"_mixin_drawing_elements\")(self)\n            done_bases.append(base)\n    return drawing_elements\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownCatalysisLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownCatalysisLayout.path_drawing_elements","title":"path_drawing_elements","text":"<pre><code>path_drawing_elements() -&gt; list[Path]\n</code></pre> <p>Return the drawing elements of the single-headed arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def path_drawing_elements(self) -&gt; list[Path]:\n    \"\"\"Return the drawing elements of the single-headed arc path.\"\"\"\n    arrowhead_length = self.arrowhead_length()\n    if len(self.segments) == 1:\n        segment = (\n            self.segments[0]\n            .shortened(self.start_shorten, \"start\")\n            .shortened(self.end_shorten + arrowhead_length, \"end\")\n        )\n        actions = [\n            MoveTo(segment.p1),\n            self._make_path_action_from_segment(segment),\n        ]\n    else:\n        first_segment = self.segments[0].shortened(self.start_shorten, \"start\")\n        last_segment = self.segments[-1].shortened(\n            self.end_shorten + arrowhead_length, \"end\"\n        )\n        actions = [\n            MoveTo(first_segment.p1),\n            self._make_path_action_from_segment(first_segment),\n        ]\n        for segment in self.segments[1:-1]:\n            action = self._make_path_action_from_segment(segment)\n            actions.append(action)\n        actions.append(self._make_path_action_from_segment(last_segment))\n    path = Path(\n        actions=tuple(actions),\n        class_=f\"{type(self).__name__}_path\",\n        fill=self.path_fill,\n        filter_=self.path_filter,\n        id_=f\"{self.id_}_path\",\n        stroke=self.path_stroke,\n        stroke_dasharray=self.path_stroke_dasharray,\n        stroke_dashoffset=self.path_stroke_dashoffset,\n        stroke_width=self.path_stroke_width,\n        transform=self.path_transform,\n    )\n    return [path]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownCatalysisLayout.points","title":"points","text":"<pre><code>points() -&gt; list[Point]\n</code></pre> <p>Return the points of the arc path.</p> <p>An arc with no segments has no points and returns an empty list.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def points(self) -&gt; list[Point]:\n    \"\"\"Return the points of the arc path.\n\n    An arc with no segments has no points and returns an empty list.\n    \"\"\"\n    if not self.segments:\n        return []\n    points = []\n    for segment in self.segments:\n        points.append(segment.p1)\n    points.append(segment.p2)\n    return points\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownCatalysisLayout.start_point","title":"start_point","text":"<pre><code>start_point() -&gt; Point\n</code></pre> <p>Return the starting point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_point(self) -&gt; Point:\n    \"\"\"Return the starting point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[0]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownCatalysisLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownCatalyzer","title":"UnknownCatalyzer  <code>dataclass</code>","text":"<pre><code>UnknownCatalyzer(*, id_: str = make_uuid4_as_str(), name: str | None = None, sbo_term: str | None = None, metaid: str | None = None, referred_element: Species | BooleanLogicGate)\n</code></pre> <p>               Bases: <code>UnknownModulator</code></p> <p>Unknown catalyzer.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'42dd46b1-feb4-4306-a731-6da862b2a5e1'</code> <code>name</code> <code>str | None</code> <p>Human-readable name of the element.</p> <code>None</code> <code>sbo_term</code> <code>str | None</code> <p>Optional SBO term identifier for semantic annotation.</p> <code>None</code> <code>metaid</code> <code>str | None</code> <p>Optional metadata identifier for RDF annotations.</p> <code>None</code> <code>referred_element</code> <code>Species | BooleanLogicGate</code> <p>The species or boolean logic gate the modifier refers to</p> required <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownCatalyzer.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownGate","title":"UnknownGate  <code>dataclass</code>","text":"<pre><code>UnknownGate(*, id_: str = make_uuid4_as_str(), inputs: frozenset[BooleanLogicGateInput] = frozenset())\n</code></pre> <p>               Bases: <code>BooleanLogicGate</code></p> <p>Unknown gate.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'d36ef20b-ef5f-4dd5-9d17-00ae553f5bbe'</code> <code>inputs</code> <code>frozenset[BooleanLogicGateInput]</code> <p>The inputs of the Boolean logic gate</p> <code>&lt;dynamic&gt;</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownGate.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownGateLayout","title":"UnknownGateLayout  <code>dataclass</code>","text":"<pre><code>UnknownGateLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = white, filter_: NoneValueType | Filter | None = None, height: float = 15.0, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = 1.25, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 15.0, text: str = '?', font_family: str = DEFAULT_FONT_FAMILY, font_fill: Color | NoneValueType = black, font_stroke: Color | NoneValueType = NoneValue, font_style: FontStyle = NORMAL, font_weight: FontWeight | float = NORMAL)\n</code></pre> <p>               Bases: <code>_SimpleNodeMixin</code>, <code>_TextMixin</code>, <code>CellDesignerNode</code></p> <p>Unknown gate layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'1fd540d0-f017-4a4b-8c95-535b32963f66'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>15.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The stroke width of the node</p> <code>1.25</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>15.0</code> <code>text</code> <code>str</code> <p>The label drawn inside the gate.</p> <code>'?'</code> <code>font_family</code> <code>str</code> <p>Font family for the text.</p> <code>'DejaVu Sans'</code> <code>font_fill</code> <code>Color | NoneValueType</code> <p>Fill color for the text.</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>font_stroke</code> <code>Color | NoneValueType</code> <p>Stroke color for the text outline.</p> <code>&lt;momapy.drawing.NoneValueType object at 0x7f8b17323e00&gt;</code> <code>font_style</code> <code>FontStyle</code> <p>Font style (normal, italic, etc.).</p> <code>&lt;FontStyle.NORMAL: 0&gt;</code> <code>font_weight</code> <code>FontWeight | float</code> <p>Font weight (normal, bold, etc.).</p> <code>&lt;FontWeight.NORMAL: 0&gt;</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>angle</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Return the east anchor of the node.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Return the label center anchor of the node.</p> <code>north</code> <p>Return the north anchor of the node.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Return the south anchor of the node.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Return the west anchor of the node.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownGateLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownGateLayout.angle","title":"angle","text":"<pre><code>angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownGateLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownGateLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownGateLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownGateLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownGateLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownGateLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownGateLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownGateLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownGateLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Return the east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Return the east anchor of the node.\"\"\"\n    return self.own_angle(0)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownGateLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownGateLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownGateLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownGateLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownGateLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Return the label center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Return the label center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownGateLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Return the north anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Return the north anchor of the node.\"\"\"\n    return self.own_angle(90)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownGateLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownGateLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownGateLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownGateLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownGateLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownGateLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownGateLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownGateLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownGateLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownGateLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownGateLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownGateLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Return the south anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Return the south anchor of the node.\"\"\"\n    return self.own_angle(270)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownGateLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownGateLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownGateLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownGateLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownGateLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownGateLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Return the west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Return the west anchor of the node.\"\"\"\n    return self.own_angle(180)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownGateLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownGateLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownGateLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownGateLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownInhibition","title":"UnknownInhibition  <code>dataclass</code>","text":"<pre><code>UnknownInhibition(*, id_: str = make_uuid4_as_str(), source: Species | BooleanLogicGate, target: Species | None)\n</code></pre> <p>               Bases: <code>UnknownModulation</code></p> <p>Unknown inhibition.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'92ff8828-3cc6-4eb1-b91b-e4d83b2d20b6'</code> <code>source</code> <code>Species | BooleanLogicGate</code> <p>The source of the modulation</p> required <code>target</code> <code>Species | None</code> <p>The target of the modulation</p> required <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownInhibition.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownInhibitionLayout","title":"UnknownInhibitionLayout  <code>dataclass</code>","text":"<pre><code>UnknownInhibitionLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, end_shorten: float = 3.0, fill: NoneValueType | Color | None = None, filter_: NoneValueType | Filter | None = None, path_fill: NoneValueType | Color | None = NoneValue, path_filter: NoneValueType | Filter | None = None, path_stroke: NoneValueType | Color | None = black, path_stroke_dasharray: NoneValueType | tuple[float, ...] | None = (12, 4), path_stroke_dashoffset: NoneValueType | float | None = None, path_stroke_width: float | None = 1.0, path_transform: NoneValueType | tuple[Transformation, ...] | None = None, stroke: NoneValueType | Color | None = None, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: NoneValueType | float | None = None, segments: tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...] = tuple(), source: LayoutElement | None = None, start_shorten: float = 0.0, target: LayoutElement | None = None, transform: NoneValueType | tuple[Transformation, ...] | None = None, arrowhead_fill: NoneValueType | Color | None = None, arrowhead_filter: NoneValueType | Filter | None = None, arrowhead_stroke: NoneValueType | Color | None = black, arrowhead_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, arrowhead_stroke_dashoffset: NoneValueType | float | None = None, arrowhead_stroke_width: float | None = 1.0, arrowhead_transform: NoneValueType | tuple[Transformation, ...] | None = None, arrowhead_height: float = 10.0)\n</code></pre> <p>               Bases: <code>CellDesignerSingleHeadedArc</code></p> <p>Unknown inhibition layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'76d10209-057d-4915-8723-8dca294e38fe'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>end_shorten</code> <code>float</code> <p>The length the end of the arc will be shorten by</p> <code>3.0</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the arc</p> <code>None</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the arc</p> <code>None</code> <code>path_fill</code> <code>NoneValueType | Color | None</code> <p>The path fill color of the arc</p> <code>&lt;momapy.drawing.NoneValueType object at 0x7f8b17323e00&gt;</code> <code>path_filter</code> <code>NoneValueType | Filter | None</code> <p>The path filter of the arc</p> <code>None</code> <code>path_stroke</code> <code>NoneValueType | Color | None</code> <p>The path stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>path_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The path stroke dasharray of the arc</p> <code>(12, 4)</code> <code>path_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The path stroke dashoffset of the arc</p> <code>None</code> <code>path_stroke_width</code> <code>float | None</code> <p>The path stroke width of the arc</p> <code>1.0</code> <code>path_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The path transform of the arc</p> <code>None</code> <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the arc</p> <code>None</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the arc</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the arc</p> <code>None</code> <code>stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the arc</p> <code>None</code> <code>segments</code> <code>tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...]</code> <p>The path segments of the arc</p> <code>&lt;dynamic&gt;</code> <code>source</code> <code>LayoutElement | None</code> <p>The source of the arc</p> <code>None</code> <code>start_shorten</code> <code>float</code> <p>The length the start of the arc will be shorten by</p> <code>0.0</code> <code>target</code> <code>LayoutElement | None</code> <p>The target of the arc</p> <code>None</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the arc</p> <code>None</code> <code>arrowhead_fill</code> <code>NoneValueType | Color | None</code> <p>The arrowhead fill color of the arc</p> <code>None</code> <code>arrowhead_filter</code> <code>NoneValueType | Filter | None</code> <p>The arrowhead filter of the arc</p> <code>None</code> <code>arrowhead_stroke</code> <code>NoneValueType | Color | None</code> <p>The arrowhead stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>arrowhead_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The arrowhead stroke dasharray of the arc</p> <code>None</code> <code>arrowhead_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The arrowhead stroke dashoffset of the arc</p> <code>None</code> <code>arrowhead_stroke_width</code> <code>float | None</code> <p>The arrowhead stroke width of the arc</p> <code>1.0</code> <code>arrowhead_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The arrowhead transform of the arc</p> <code>None</code> <code>arrowhead_height</code> <code>float</code> <p>The height of the arrowhead bar.</p> <code>10.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>arrowhead_base</code> <p>Return the arrowhead base anchor point of the single-headed arc.</p> <code>arrowhead_bbox</code> <p>Return the bounding box of the single-headed arc arrowhead.</p> <code>arrowhead_border</code> <p>Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.</p> <code>arrowhead_drawing_elements</code> <p>Return the drawing elements of the single-headed arc arrowhead.</p> <code>arrowhead_length</code> <p>Return the length of the single-headed arc arrowhead.</p> <code>arrowhead_tip</code> <p>Return the arrowhead tip anchor point of the single-headed arc.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>childless</code> <p>Return a copy of the arc with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>end_point</code> <p>Return the ending point of the arc.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>fraction</code> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <code>length</code> <p>Return the total length of the arc path.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_children</code> <p>Return the self children of the arc.</p> <code>own_drawing_elements</code> <p>Return the arc's own drawing elements, including those of its mixins.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>path_drawing_elements</code> <p>Return the drawing elements of the single-headed arc path.</p> <code>points</code> <p>Return the points of the arc path.</p> <code>start_point</code> <p>Return the starting point of the arc.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownInhibitionLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownInhibitionLayout.arrowhead_base","title":"arrowhead_base","text":"<pre><code>arrowhead_base() -&gt; Point\n</code></pre> <p>Return the arrowhead base anchor point of the single-headed arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_base(self) -&gt; Point:\n    \"\"\"Return the arrowhead base anchor point of the single-headed arc.\"\"\"\n    arrowhead_length = self.arrowhead_length()\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return self.arrowhead_tip() - (arrowhead_length, 0)\n    fraction = 1 - (arrowhead_length + self.end_shorten) / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownInhibitionLayout.arrowhead_bbox","title":"arrowhead_bbox","text":"<pre><code>arrowhead_bbox() -&gt; Bbox\n</code></pre> <p>Return the bounding box of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_bbox(self) -&gt; Bbox:\n    \"\"\"Return the bounding box of the single-headed arc arrowhead.\"\"\"\n    return get_drawing_elements_bbox(self.arrowhead_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownInhibitionLayout.arrowhead_border","title":"arrowhead_border","text":"<pre><code>arrowhead_border(point: Point) -&gt; Point\n</code></pre> <p>Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_border(self, point: Point) -&gt; Point:\n    \"\"\"Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    point = get_drawing_elements_border(self.arrowhead_drawing_elements(), point)\n    if point is None:\n        return self.arrowhead_tip()\n    return point\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownInhibitionLayout.arrowhead_drawing_elements","title":"arrowhead_drawing_elements","text":"<pre><code>arrowhead_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_drawing_elements(\n    self,\n) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the single-headed arc arrowhead.\"\"\"\n    drawing_elements = self._arrowhead_border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_arrowhead\",\n        elements=tuple(drawing_elements),\n        fill=self.arrowhead_fill,\n        filter_=self.arrowhead_filter,\n        id_=f\"{self.id_}_arrowhead\",\n        stroke=self.arrowhead_stroke,\n        stroke_dasharray=self.arrowhead_stroke_dasharray,\n        stroke_dashoffset=self.arrowhead_stroke_dashoffset,\n        stroke_width=self.arrowhead_stroke_width,\n        transform=self.arrowhead_transform,\n    )\n    transformation = self._get_arrowhead_transformation()\n    group = group.transformed(transformation)\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownInhibitionLayout.arrowhead_length","title":"arrowhead_length","text":"<pre><code>arrowhead_length() -&gt; float\n</code></pre> <p>Return the length of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_length(self) -&gt; float:\n    \"\"\"Return the length of the single-headed arc arrowhead.\"\"\"\n    bbox = get_drawing_elements_bbox(self._arrowhead_border_drawing_elements())\n    if math.isnan(bbox.width):\n        return 0.0\n    return bbox.east().x\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownInhibitionLayout.arrowhead_tip","title":"arrowhead_tip","text":"<pre><code>arrowhead_tip() -&gt; Point\n</code></pre> <p>Return the arrowhead tip anchor point of the single-headed arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_tip(self) -&gt; Point:\n    \"\"\"Return the arrowhead tip anchor point of the single-headed arc.\"\"\"\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return segment.p2\n    fraction = 1 - self.end_shorten / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownInhibitionLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownInhibitionLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the arc with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the arc with no children.\"\"\"\n    return dataclasses.replace(self, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownInhibitionLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownInhibitionLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownInhibitionLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownInhibitionLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownInhibitionLayout.end_point","title":"end_point","text":"<pre><code>end_point() -&gt; Point\n</code></pre> <p>Return the ending point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_point(self) -&gt; Point:\n    \"\"\"Return the ending point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[-1]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownInhibitionLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownInhibitionLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownInhibitionLayout.fraction","title":"fraction","text":"<pre><code>fraction(fraction: float) -&gt; tuple[Point, float]\n</code></pre> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def fraction(self, fraction: float) -&gt; tuple[Point, float]:\n    \"\"\"Return the position and angle on the arc at a given fraction (of the total arc length).\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    current_length = 0\n    length_to_reach = fraction * self.length()\n    for segment in self.segments:\n        current_length += segment.length()\n        if current_length &gt;= length_to_reach:\n            break\n    segment_start_length = current_length - segment.length()\n    segment_fraction = (length_to_reach - segment_start_length) / segment.length()\n    position, angle = segment.get_position_and_angle_at_fraction(segment_fraction)\n    return position, angle\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownInhibitionLayout.length","title":"length","text":"<pre><code>length() -&gt; float\n</code></pre> <p>Return the total length of the arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def length(self) -&gt; float:\n    \"\"\"Return the total length of the arc path.\"\"\"\n    return sum([segment.length() for segment in self.segments])\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownInhibitionLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownInhibitionLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the arc.\"\"\"\n    return []\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownInhibitionLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the arc's own drawing elements, including those of its mixins.</p> Source code in <code>src/momapy/celldesigner/elements.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the arc's own drawing elements, including those of its mixins.\"\"\"\n    drawing_elements = super_or_builder(\n        CellDesignerSingleHeadedArc, self\n    ).own_drawing_elements()\n    done_bases = []\n    for base in type(self).__mro__:\n        if (\n            issubclass_or_builder(base, _SBGNMixin)\n            and base is not type(self)\n            and not any([issubclass(done_base, base) for done_base in done_bases])\n        ):\n            drawing_elements += getattr(base, \"_mixin_drawing_elements\")(self)\n            done_bases.append(base)\n    return drawing_elements\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownInhibitionLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownInhibitionLayout.path_drawing_elements","title":"path_drawing_elements","text":"<pre><code>path_drawing_elements() -&gt; list[Path]\n</code></pre> <p>Return the drawing elements of the single-headed arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def path_drawing_elements(self) -&gt; list[Path]:\n    \"\"\"Return the drawing elements of the single-headed arc path.\"\"\"\n    arrowhead_length = self.arrowhead_length()\n    if len(self.segments) == 1:\n        segment = (\n            self.segments[0]\n            .shortened(self.start_shorten, \"start\")\n            .shortened(self.end_shorten + arrowhead_length, \"end\")\n        )\n        actions = [\n            MoveTo(segment.p1),\n            self._make_path_action_from_segment(segment),\n        ]\n    else:\n        first_segment = self.segments[0].shortened(self.start_shorten, \"start\")\n        last_segment = self.segments[-1].shortened(\n            self.end_shorten + arrowhead_length, \"end\"\n        )\n        actions = [\n            MoveTo(first_segment.p1),\n            self._make_path_action_from_segment(first_segment),\n        ]\n        for segment in self.segments[1:-1]:\n            action = self._make_path_action_from_segment(segment)\n            actions.append(action)\n        actions.append(self._make_path_action_from_segment(last_segment))\n    path = Path(\n        actions=tuple(actions),\n        class_=f\"{type(self).__name__}_path\",\n        fill=self.path_fill,\n        filter_=self.path_filter,\n        id_=f\"{self.id_}_path\",\n        stroke=self.path_stroke,\n        stroke_dasharray=self.path_stroke_dasharray,\n        stroke_dashoffset=self.path_stroke_dashoffset,\n        stroke_width=self.path_stroke_width,\n        transform=self.path_transform,\n    )\n    return [path]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownInhibitionLayout.points","title":"points","text":"<pre><code>points() -&gt; list[Point]\n</code></pre> <p>Return the points of the arc path.</p> <p>An arc with no segments has no points and returns an empty list.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def points(self) -&gt; list[Point]:\n    \"\"\"Return the points of the arc path.\n\n    An arc with no segments has no points and returns an empty list.\n    \"\"\"\n    if not self.segments:\n        return []\n    points = []\n    for segment in self.segments:\n        points.append(segment.p1)\n    points.append(segment.p2)\n    return points\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownInhibitionLayout.start_point","title":"start_point","text":"<pre><code>start_point() -&gt; Point\n</code></pre> <p>Return the starting point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_point(self) -&gt; Point:\n    \"\"\"Return the starting point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[0]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownInhibitionLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownInhibitor","title":"UnknownInhibitor  <code>dataclass</code>","text":"<pre><code>UnknownInhibitor(*, id_: str = make_uuid4_as_str(), name: str | None = None, sbo_term: str | None = None, metaid: str | None = None, referred_element: Species | BooleanLogicGate)\n</code></pre> <p>               Bases: <code>UnknownModulator</code></p> <p>Unknown inhibitor.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'3a8d1179-756b-4557-b7f6-bfd03c6701c7'</code> <code>name</code> <code>str | None</code> <p>Human-readable name of the element.</p> <code>None</code> <code>sbo_term</code> <code>str | None</code> <p>Optional SBO term identifier for semantic annotation.</p> <code>None</code> <code>metaid</code> <code>str | None</code> <p>Optional metadata identifier for RDF annotations.</p> <code>None</code> <code>referred_element</code> <code>Species | BooleanLogicGate</code> <p>The species or boolean logic gate the modifier refers to</p> required <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownInhibitor.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownLayout","title":"UnknownLayout  <code>dataclass</code>","text":"<pre><code>UnknownLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = from_hex('#CCCCCC'), filter_: NoneValueType | Filter | None = None, height: float = 30.0, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = NoneValue, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = 1.0, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 60.0, offset: float = 3.0, subunits_stroke: tuple[NoneValueType | Color, ...] | None = None, subunits_stroke_width: tuple[NoneValueType | float, ...] | None = None, subunits_stroke_dasharray: tuple[NoneValueType | tuple[float, ...], ...] | None = None, subunits_stroke_dashoffset: tuple[float, ...] | None = None, subunits_fill: tuple[NoneValueType | Color, ...] | None = None, subunits_transform: tuple[NoneValueType | tuple[Transformation, ...], ...] | None = None, subunits_filter: tuple[NoneValueType | Filter, ...] | None = None, n: int = 1)\n</code></pre> <p>               Bases: <code>_MultiNodeMixin</code>, <code>CellDesignerNode</code></p> <p>Unknown layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'6851b760-b642-42d9-af70-541c2fa47fb3'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>Color(red=204, green=204, blue=204, alpha=1.0)</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>30.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>&lt;momapy.drawing.NoneValueType object at 0x7f8b17323e00&gt;</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The stroke width of the node</p> <code>1.0</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>60.0</code> <code>offset</code> <code>float</code> <p>Offset distance between stacked units.</p> <code>3.0</code> <code>subunits_stroke</code> <code>tuple[NoneValueType | Color, ...] | None</code> <p>Tuple of stroke colors for each subunit.</p> <code>None</code> <code>subunits_stroke_width</code> <code>tuple[NoneValueType | float, ...] | None</code> <p>Tuple of stroke widths for each subunit.</p> <code>None</code> <code>subunits_stroke_dasharray</code> <code>tuple[NoneValueType | tuple[float, ...], ...] | None</code> <p>Tuple of dash patterns for each subunit.</p> <code>None</code> <code>subunits_stroke_dashoffset</code> <code>tuple[float, ...] | None</code> <p>Tuple of dash offsets for each subunit.</p> <code>None</code> <code>subunits_fill</code> <code>tuple[NoneValueType | Color, ...] | None</code> <p>Tuple of fill colors for each subunit.</p> <code>None</code> <code>subunits_transform</code> <code>tuple[NoneValueType | tuple[Transformation, ...], ...] | None</code> <p>Tuple of transformations for each subunit.</p> <code>None</code> <code>subunits_filter</code> <code>tuple[NoneValueType | Filter, ...] | None</code> <p>Tuple of filters for each subunit.</p> <code>None</code> <code>n</code> <code>int</code> <p>Number of stacked node copies to draw.</p> <code>1</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>angle</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Return the east anchor of the node.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Get the center position for the node label.</p> <code>north</code> <p>Return the north anchor of the node.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Return the south anchor of the node.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Return the west anchor of the node.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownLayout.angle","title":"angle","text":"<pre><code>angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Return the east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Return the east anchor of the node.\"\"\"\n    return self.own_angle(0)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Get the center position for the node label.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point where the label should be centered.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Get the center position for the node label.\n\n    Returns:\n        Point where the label should be centered.\n    \"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2 - (self._n - 1) * self.offset,\n        self.height / 2 - height / 2 - (self._n - 1) * self.offset,\n    )\n    return self._make_subunit_shape(\n        position=position, width=width, height=height\n    ).position\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Return the north anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Return the north anchor of the node.\"\"\"\n    return self.own_angle(90)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Return the south anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Return the south anchor of the node.\"\"\"\n    return self.own_angle(270)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Return the west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Return the west anchor of the node.\"\"\"\n    return self.own_angle(180)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownModulation","title":"UnknownModulation  <code>dataclass</code>","text":"<pre><code>UnknownModulation(*, id_: str = make_uuid4_as_str(), source: Species | BooleanLogicGate, target: Species | None)\n</code></pre> <p>               Bases: <code>KnownOrUnknownModulation</code></p> <p>Unknown modulation.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'15f80489-899d-415b-bc02-a8d72dc6a19f'</code> <code>source</code> <code>Species | BooleanLogicGate</code> <p>The source of the modulation</p> required <code>target</code> <code>Species | None</code> <p>The target of the modulation</p> required <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownModulation.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownModulationLayout","title":"UnknownModulationLayout  <code>dataclass</code>","text":"<pre><code>UnknownModulationLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, end_shorten: float = 0.0, fill: NoneValueType | Color | None = None, filter_: NoneValueType | Filter | None = None, path_fill: NoneValueType | Color | None = NoneValue, path_filter: NoneValueType | Filter | None = None, path_stroke: NoneValueType | Color | None = black, path_stroke_dasharray: NoneValueType | tuple[float, ...] | None = (12, 4), path_stroke_dashoffset: NoneValueType | float | None = None, path_stroke_width: float | None = 1.0, path_transform: NoneValueType | tuple[Transformation, ...] | None = None, stroke: NoneValueType | Color | None = None, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: NoneValueType | float | None = None, segments: tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...] = tuple(), source: LayoutElement | None = None, start_shorten: float = 0.0, target: LayoutElement | None = None, transform: NoneValueType | tuple[Transformation, ...] | None = None, arrowhead_fill: NoneValueType | Color | None = white, arrowhead_filter: NoneValueType | Filter | None = None, arrowhead_stroke: NoneValueType | Color | None = black, arrowhead_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, arrowhead_stroke_dashoffset: NoneValueType | float | None = None, arrowhead_stroke_width: float | None = 1.0, arrowhead_transform: NoneValueType | tuple[Transformation, ...] | None = None, arrowhead_height: float = 8.0, arrowhead_width: float = 15.0)\n</code></pre> <p>               Bases: <code>CellDesignerSingleHeadedArc</code></p> <p>Unknown modulation layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'8f3e5da7-4933-4a02-ad72-ab0df686818b'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>end_shorten</code> <code>float</code> <p>The length the end of the arc will be shorten by</p> <code>0.0</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the arc</p> <code>None</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the arc</p> <code>None</code> <code>path_fill</code> <code>NoneValueType | Color | None</code> <p>The path fill color of the arc</p> <code>&lt;momapy.drawing.NoneValueType object at 0x7f8b17323e00&gt;</code> <code>path_filter</code> <code>NoneValueType | Filter | None</code> <p>The path filter of the arc</p> <code>None</code> <code>path_stroke</code> <code>NoneValueType | Color | None</code> <p>The path stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>path_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The path stroke dasharray of the arc</p> <code>(12, 4)</code> <code>path_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The path stroke dashoffset of the arc</p> <code>None</code> <code>path_stroke_width</code> <code>float | None</code> <p>The path stroke width of the arc</p> <code>1.0</code> <code>path_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The path transform of the arc</p> <code>None</code> <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the arc</p> <code>None</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the arc</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the arc</p> <code>None</code> <code>stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the arc</p> <code>None</code> <code>segments</code> <code>tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...]</code> <p>The path segments of the arc</p> <code>&lt;dynamic&gt;</code> <code>source</code> <code>LayoutElement | None</code> <p>The source of the arc</p> <code>None</code> <code>start_shorten</code> <code>float</code> <p>The length the start of the arc will be shorten by</p> <code>0.0</code> <code>target</code> <code>LayoutElement | None</code> <p>The target of the arc</p> <code>None</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the arc</p> <code>None</code> <code>arrowhead_fill</code> <code>NoneValueType | Color | None</code> <p>The arrowhead fill color of the arc</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>arrowhead_filter</code> <code>NoneValueType | Filter | None</code> <p>The arrowhead filter of the arc</p> <code>None</code> <code>arrowhead_stroke</code> <code>NoneValueType | Color | None</code> <p>The arrowhead stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>arrowhead_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The arrowhead stroke dasharray of the arc</p> <code>None</code> <code>arrowhead_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The arrowhead stroke dashoffset of the arc</p> <code>None</code> <code>arrowhead_stroke_width</code> <code>float | None</code> <p>The arrowhead stroke width of the arc</p> <code>1.0</code> <code>arrowhead_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The arrowhead transform of the arc</p> <code>None</code> <code>arrowhead_height</code> <code>float</code> <p>The height of the arrowhead.</p> <code>8.0</code> <code>arrowhead_width</code> <code>float</code> <p>The width of the arrowhead.</p> <code>15.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>arrowhead_base</code> <p>Return the arrowhead base anchor point of the single-headed arc.</p> <code>arrowhead_bbox</code> <p>Return the bounding box of the single-headed arc arrowhead.</p> <code>arrowhead_border</code> <p>Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.</p> <code>arrowhead_drawing_elements</code> <p>Return the drawing elements of the single-headed arc arrowhead.</p> <code>arrowhead_length</code> <p>Return the length of the single-headed arc arrowhead.</p> <code>arrowhead_tip</code> <p>Return the arrowhead tip anchor point of the single-headed arc.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>childless</code> <p>Return a copy of the arc with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>end_point</code> <p>Return the ending point of the arc.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>fraction</code> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <code>length</code> <p>Return the total length of the arc path.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_children</code> <p>Return the self children of the arc.</p> <code>own_drawing_elements</code> <p>Return the arc's own drawing elements, including those of its mixins.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>path_drawing_elements</code> <p>Return the drawing elements of the single-headed arc path.</p> <code>points</code> <p>Return the points of the arc path.</p> <code>start_point</code> <p>Return the starting point of the arc.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownModulationLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownModulationLayout.arrowhead_base","title":"arrowhead_base","text":"<pre><code>arrowhead_base() -&gt; Point\n</code></pre> <p>Return the arrowhead base anchor point of the single-headed arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_base(self) -&gt; Point:\n    \"\"\"Return the arrowhead base anchor point of the single-headed arc.\"\"\"\n    arrowhead_length = self.arrowhead_length()\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return self.arrowhead_tip() - (arrowhead_length, 0)\n    fraction = 1 - (arrowhead_length + self.end_shorten) / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownModulationLayout.arrowhead_bbox","title":"arrowhead_bbox","text":"<pre><code>arrowhead_bbox() -&gt; Bbox\n</code></pre> <p>Return the bounding box of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_bbox(self) -&gt; Bbox:\n    \"\"\"Return the bounding box of the single-headed arc arrowhead.\"\"\"\n    return get_drawing_elements_bbox(self.arrowhead_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownModulationLayout.arrowhead_border","title":"arrowhead_border","text":"<pre><code>arrowhead_border(point: Point) -&gt; Point\n</code></pre> <p>Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_border(self, point: Point) -&gt; Point:\n    \"\"\"Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    point = get_drawing_elements_border(self.arrowhead_drawing_elements(), point)\n    if point is None:\n        return self.arrowhead_tip()\n    return point\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownModulationLayout.arrowhead_drawing_elements","title":"arrowhead_drawing_elements","text":"<pre><code>arrowhead_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_drawing_elements(\n    self,\n) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the single-headed arc arrowhead.\"\"\"\n    drawing_elements = self._arrowhead_border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_arrowhead\",\n        elements=tuple(drawing_elements),\n        fill=self.arrowhead_fill,\n        filter_=self.arrowhead_filter,\n        id_=f\"{self.id_}_arrowhead\",\n        stroke=self.arrowhead_stroke,\n        stroke_dasharray=self.arrowhead_stroke_dasharray,\n        stroke_dashoffset=self.arrowhead_stroke_dashoffset,\n        stroke_width=self.arrowhead_stroke_width,\n        transform=self.arrowhead_transform,\n    )\n    transformation = self._get_arrowhead_transformation()\n    group = group.transformed(transformation)\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownModulationLayout.arrowhead_length","title":"arrowhead_length","text":"<pre><code>arrowhead_length() -&gt; float\n</code></pre> <p>Return the length of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_length(self) -&gt; float:\n    \"\"\"Return the length of the single-headed arc arrowhead.\"\"\"\n    bbox = get_drawing_elements_bbox(self._arrowhead_border_drawing_elements())\n    if math.isnan(bbox.width):\n        return 0.0\n    return bbox.east().x\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownModulationLayout.arrowhead_tip","title":"arrowhead_tip","text":"<pre><code>arrowhead_tip() -&gt; Point\n</code></pre> <p>Return the arrowhead tip anchor point of the single-headed arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_tip(self) -&gt; Point:\n    \"\"\"Return the arrowhead tip anchor point of the single-headed arc.\"\"\"\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return segment.p2\n    fraction = 1 - self.end_shorten / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownModulationLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownModulationLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the arc with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the arc with no children.\"\"\"\n    return dataclasses.replace(self, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownModulationLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownModulationLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownModulationLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownModulationLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownModulationLayout.end_point","title":"end_point","text":"<pre><code>end_point() -&gt; Point\n</code></pre> <p>Return the ending point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_point(self) -&gt; Point:\n    \"\"\"Return the ending point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[-1]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownModulationLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownModulationLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownModulationLayout.fraction","title":"fraction","text":"<pre><code>fraction(fraction: float) -&gt; tuple[Point, float]\n</code></pre> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def fraction(self, fraction: float) -&gt; tuple[Point, float]:\n    \"\"\"Return the position and angle on the arc at a given fraction (of the total arc length).\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    current_length = 0\n    length_to_reach = fraction * self.length()\n    for segment in self.segments:\n        current_length += segment.length()\n        if current_length &gt;= length_to_reach:\n            break\n    segment_start_length = current_length - segment.length()\n    segment_fraction = (length_to_reach - segment_start_length) / segment.length()\n    position, angle = segment.get_position_and_angle_at_fraction(segment_fraction)\n    return position, angle\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownModulationLayout.length","title":"length","text":"<pre><code>length() -&gt; float\n</code></pre> <p>Return the total length of the arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def length(self) -&gt; float:\n    \"\"\"Return the total length of the arc path.\"\"\"\n    return sum([segment.length() for segment in self.segments])\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownModulationLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownModulationLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the arc.\"\"\"\n    return []\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownModulationLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the arc's own drawing elements, including those of its mixins.</p> Source code in <code>src/momapy/celldesigner/elements.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the arc's own drawing elements, including those of its mixins.\"\"\"\n    drawing_elements = super_or_builder(\n        CellDesignerSingleHeadedArc, self\n    ).own_drawing_elements()\n    done_bases = []\n    for base in type(self).__mro__:\n        if (\n            issubclass_or_builder(base, _SBGNMixin)\n            and base is not type(self)\n            and not any([issubclass(done_base, base) for done_base in done_bases])\n        ):\n            drawing_elements += getattr(base, \"_mixin_drawing_elements\")(self)\n            done_bases.append(base)\n    return drawing_elements\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownModulationLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownModulationLayout.path_drawing_elements","title":"path_drawing_elements","text":"<pre><code>path_drawing_elements() -&gt; list[Path]\n</code></pre> <p>Return the drawing elements of the single-headed arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def path_drawing_elements(self) -&gt; list[Path]:\n    \"\"\"Return the drawing elements of the single-headed arc path.\"\"\"\n    arrowhead_length = self.arrowhead_length()\n    if len(self.segments) == 1:\n        segment = (\n            self.segments[0]\n            .shortened(self.start_shorten, \"start\")\n            .shortened(self.end_shorten + arrowhead_length, \"end\")\n        )\n        actions = [\n            MoveTo(segment.p1),\n            self._make_path_action_from_segment(segment),\n        ]\n    else:\n        first_segment = self.segments[0].shortened(self.start_shorten, \"start\")\n        last_segment = self.segments[-1].shortened(\n            self.end_shorten + arrowhead_length, \"end\"\n        )\n        actions = [\n            MoveTo(first_segment.p1),\n            self._make_path_action_from_segment(first_segment),\n        ]\n        for segment in self.segments[1:-1]:\n            action = self._make_path_action_from_segment(segment)\n            actions.append(action)\n        actions.append(self._make_path_action_from_segment(last_segment))\n    path = Path(\n        actions=tuple(actions),\n        class_=f\"{type(self).__name__}_path\",\n        fill=self.path_fill,\n        filter_=self.path_filter,\n        id_=f\"{self.id_}_path\",\n        stroke=self.path_stroke,\n        stroke_dasharray=self.path_stroke_dasharray,\n        stroke_dashoffset=self.path_stroke_dashoffset,\n        stroke_width=self.path_stroke_width,\n        transform=self.path_transform,\n    )\n    return [path]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownModulationLayout.points","title":"points","text":"<pre><code>points() -&gt; list[Point]\n</code></pre> <p>Return the points of the arc path.</p> <p>An arc with no segments has no points and returns an empty list.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def points(self) -&gt; list[Point]:\n    \"\"\"Return the points of the arc path.\n\n    An arc with no segments has no points and returns an empty list.\n    \"\"\"\n    if not self.segments:\n        return []\n    points = []\n    for segment in self.segments:\n        points.append(segment.p1)\n    points.append(segment.p2)\n    return points\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownModulationLayout.start_point","title":"start_point","text":"<pre><code>start_point() -&gt; Point\n</code></pre> <p>Return the starting point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_point(self) -&gt; Point:\n    \"\"\"Return the starting point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[0]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownModulationLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownModulator","title":"UnknownModulator  <code>dataclass</code>","text":"<pre><code>UnknownModulator(*, id_: str = make_uuid4_as_str(), name: str | None = None, sbo_term: str | None = None, metaid: str | None = None, referred_element: Species | BooleanLogicGate)\n</code></pre> <p>               Bases: <code>KnownOrUnknownModulator</code></p> <p>Unknown modulator.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'1e644ed6-7e40-4783-bdca-8883729e9056'</code> <code>name</code> <code>str | None</code> <p>Human-readable name of the element.</p> <code>None</code> <code>sbo_term</code> <code>str | None</code> <p>Optional SBO term identifier for semantic annotation.</p> <code>None</code> <code>metaid</code> <code>str | None</code> <p>Optional metadata identifier for RDF annotations.</p> <code>None</code> <code>referred_element</code> <code>Species | BooleanLogicGate</code> <p>The species or boolean logic gate the modifier refers to</p> required <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownModulator.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownNegativeInfluence","title":"UnknownNegativeInfluence  <code>dataclass</code>","text":"<pre><code>UnknownNegativeInfluence(*, id_: str = make_uuid4_as_str(), source: Species | BooleanLogicGate, target: Species | None)\n</code></pre> <p>               Bases: <code>UnknownModulation</code></p> <p>Unknown negative influence.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'2f48d4f0-2009-4cc3-a988-a206d2a1b382'</code> <code>source</code> <code>Species | BooleanLogicGate</code> <p>The source of the modulation</p> required <code>target</code> <code>Species | None</code> <p>The target of the modulation</p> required <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownNegativeInfluence.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownPhysicalStimulation","title":"UnknownPhysicalStimulation  <code>dataclass</code>","text":"<pre><code>UnknownPhysicalStimulation(*, id_: str = make_uuid4_as_str(), source: Species | BooleanLogicGate, target: Species | None)\n</code></pre> <p>               Bases: <code>UnknownModulation</code></p> <p>Unknown physical stimulation.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'51a45fe0-2b65-4736-9326-583371111fb2'</code> <code>source</code> <code>Species | BooleanLogicGate</code> <p>The source of the modulation</p> required <code>target</code> <code>Species | None</code> <p>The target of the modulation</p> required <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownPhysicalStimulation.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownPhysicalStimulationLayout","title":"UnknownPhysicalStimulationLayout  <code>dataclass</code>","text":"<pre><code>UnknownPhysicalStimulationLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, end_shorten: float = 0.0, fill: NoneValueType | Color | None = None, filter_: NoneValueType | Filter | None = None, path_fill: NoneValueType | Color | None = NoneValue, path_filter: NoneValueType | Filter | None = None, path_stroke: NoneValueType | Color | None = black, path_stroke_dasharray: NoneValueType | tuple[float, ...] | None = (12, 4), path_stroke_dashoffset: NoneValueType | float | None = None, path_stroke_width: float | None = 1.0, path_transform: NoneValueType | tuple[Transformation, ...] | None = None, stroke: NoneValueType | Color | None = None, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: NoneValueType | float | None = None, segments: tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...] = tuple(), source: LayoutElement | None = None, start_shorten: float = 0.0, target: LayoutElement | None = None, transform: NoneValueType | tuple[Transformation, ...] | None = None, arrowhead_fill: NoneValueType | Color | None = white, arrowhead_filter: NoneValueType | Filter | None = None, arrowhead_stroke: NoneValueType | Color | None = black, arrowhead_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, arrowhead_stroke_dashoffset: NoneValueType | float | None = None, arrowhead_stroke_width: float | None = 1.0, arrowhead_transform: NoneValueType | tuple[Transformation, ...] | None = None, arrowhead_height: float = 10.0, arrowhead_width: float = 10.0)\n</code></pre> <p>               Bases: <code>CellDesignerSingleHeadedArc</code></p> <p>Unknown physical stimulation layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'c70ed4a0-83a0-47b9-a047-9f77f8892835'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>end_shorten</code> <code>float</code> <p>The length the end of the arc will be shorten by</p> <code>0.0</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the arc</p> <code>None</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the arc</p> <code>None</code> <code>path_fill</code> <code>NoneValueType | Color | None</code> <p>The path fill color of the arc</p> <code>&lt;momapy.drawing.NoneValueType object at 0x7f8b17323e00&gt;</code> <code>path_filter</code> <code>NoneValueType | Filter | None</code> <p>The path filter of the arc</p> <code>None</code> <code>path_stroke</code> <code>NoneValueType | Color | None</code> <p>The path stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>path_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The path stroke dasharray of the arc</p> <code>(12, 4)</code> <code>path_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The path stroke dashoffset of the arc</p> <code>None</code> <code>path_stroke_width</code> <code>float | None</code> <p>The path stroke width of the arc</p> <code>1.0</code> <code>path_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The path transform of the arc</p> <code>None</code> <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the arc</p> <code>None</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the arc</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the arc</p> <code>None</code> <code>stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the arc</p> <code>None</code> <code>segments</code> <code>tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...]</code> <p>The path segments of the arc</p> <code>&lt;dynamic&gt;</code> <code>source</code> <code>LayoutElement | None</code> <p>The source of the arc</p> <code>None</code> <code>start_shorten</code> <code>float</code> <p>The length the start of the arc will be shorten by</p> <code>0.0</code> <code>target</code> <code>LayoutElement | None</code> <p>The target of the arc</p> <code>None</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the arc</p> <code>None</code> <code>arrowhead_fill</code> <code>NoneValueType | Color | None</code> <p>The arrowhead fill color of the arc</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>arrowhead_filter</code> <code>NoneValueType | Filter | None</code> <p>The arrowhead filter of the arc</p> <code>None</code> <code>arrowhead_stroke</code> <code>NoneValueType | Color | None</code> <p>The arrowhead stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>arrowhead_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The arrowhead stroke dasharray of the arc</p> <code>None</code> <code>arrowhead_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The arrowhead stroke dashoffset of the arc</p> <code>None</code> <code>arrowhead_stroke_width</code> <code>float | None</code> <p>The arrowhead stroke width of the arc</p> <code>1.0</code> <code>arrowhead_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The arrowhead transform of the arc</p> <code>None</code> <code>arrowhead_height</code> <code>float</code> <p>The height of the arrowhead.</p> <code>10.0</code> <code>arrowhead_width</code> <code>float</code> <p>The width of the arrowhead.</p> <code>10.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>arrowhead_base</code> <p>Return the arrowhead base anchor point of the single-headed arc.</p> <code>arrowhead_bbox</code> <p>Return the bounding box of the single-headed arc arrowhead.</p> <code>arrowhead_border</code> <p>Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.</p> <code>arrowhead_drawing_elements</code> <p>Return the drawing elements of the single-headed arc arrowhead.</p> <code>arrowhead_length</code> <p>Return the length of the single-headed arc arrowhead.</p> <code>arrowhead_tip</code> <p>Return the arrowhead tip anchor point of the single-headed arc.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>childless</code> <p>Return a copy of the arc with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>end_point</code> <p>Return the ending point of the arc.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>fraction</code> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <code>length</code> <p>Return the total length of the arc path.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_children</code> <p>Return the self children of the arc.</p> <code>own_drawing_elements</code> <p>Return the arc's own drawing elements, including those of its mixins.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>path_drawing_elements</code> <p>Return the drawing elements of the single-headed arc path.</p> <code>points</code> <p>Return the points of the arc path.</p> <code>start_point</code> <p>Return the starting point of the arc.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownPhysicalStimulationLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownPhysicalStimulationLayout.arrowhead_base","title":"arrowhead_base","text":"<pre><code>arrowhead_base() -&gt; Point\n</code></pre> <p>Return the arrowhead base anchor point of the single-headed arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_base(self) -&gt; Point:\n    \"\"\"Return the arrowhead base anchor point of the single-headed arc.\"\"\"\n    arrowhead_length = self.arrowhead_length()\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return self.arrowhead_tip() - (arrowhead_length, 0)\n    fraction = 1 - (arrowhead_length + self.end_shorten) / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownPhysicalStimulationLayout.arrowhead_bbox","title":"arrowhead_bbox","text":"<pre><code>arrowhead_bbox() -&gt; Bbox\n</code></pre> <p>Return the bounding box of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_bbox(self) -&gt; Bbox:\n    \"\"\"Return the bounding box of the single-headed arc arrowhead.\"\"\"\n    return get_drawing_elements_bbox(self.arrowhead_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownPhysicalStimulationLayout.arrowhead_border","title":"arrowhead_border","text":"<pre><code>arrowhead_border(point: Point) -&gt; Point\n</code></pre> <p>Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_border(self, point: Point) -&gt; Point:\n    \"\"\"Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    point = get_drawing_elements_border(self.arrowhead_drawing_elements(), point)\n    if point is None:\n        return self.arrowhead_tip()\n    return point\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownPhysicalStimulationLayout.arrowhead_drawing_elements","title":"arrowhead_drawing_elements","text":"<pre><code>arrowhead_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_drawing_elements(\n    self,\n) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the single-headed arc arrowhead.\"\"\"\n    drawing_elements = self._arrowhead_border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_arrowhead\",\n        elements=tuple(drawing_elements),\n        fill=self.arrowhead_fill,\n        filter_=self.arrowhead_filter,\n        id_=f\"{self.id_}_arrowhead\",\n        stroke=self.arrowhead_stroke,\n        stroke_dasharray=self.arrowhead_stroke_dasharray,\n        stroke_dashoffset=self.arrowhead_stroke_dashoffset,\n        stroke_width=self.arrowhead_stroke_width,\n        transform=self.arrowhead_transform,\n    )\n    transformation = self._get_arrowhead_transformation()\n    group = group.transformed(transformation)\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownPhysicalStimulationLayout.arrowhead_length","title":"arrowhead_length","text":"<pre><code>arrowhead_length() -&gt; float\n</code></pre> <p>Return the length of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_length(self) -&gt; float:\n    \"\"\"Return the length of the single-headed arc arrowhead.\"\"\"\n    bbox = get_drawing_elements_bbox(self._arrowhead_border_drawing_elements())\n    if math.isnan(bbox.width):\n        return 0.0\n    return bbox.east().x\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownPhysicalStimulationLayout.arrowhead_tip","title":"arrowhead_tip","text":"<pre><code>arrowhead_tip() -&gt; Point\n</code></pre> <p>Return the arrowhead tip anchor point of the single-headed arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_tip(self) -&gt; Point:\n    \"\"\"Return the arrowhead tip anchor point of the single-headed arc.\"\"\"\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return segment.p2\n    fraction = 1 - self.end_shorten / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownPhysicalStimulationLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownPhysicalStimulationLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the arc with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the arc with no children.\"\"\"\n    return dataclasses.replace(self, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownPhysicalStimulationLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownPhysicalStimulationLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownPhysicalStimulationLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownPhysicalStimulationLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownPhysicalStimulationLayout.end_point","title":"end_point","text":"<pre><code>end_point() -&gt; Point\n</code></pre> <p>Return the ending point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_point(self) -&gt; Point:\n    \"\"\"Return the ending point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[-1]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownPhysicalStimulationLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownPhysicalStimulationLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownPhysicalStimulationLayout.fraction","title":"fraction","text":"<pre><code>fraction(fraction: float) -&gt; tuple[Point, float]\n</code></pre> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def fraction(self, fraction: float) -&gt; tuple[Point, float]:\n    \"\"\"Return the position and angle on the arc at a given fraction (of the total arc length).\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    current_length = 0\n    length_to_reach = fraction * self.length()\n    for segment in self.segments:\n        current_length += segment.length()\n        if current_length &gt;= length_to_reach:\n            break\n    segment_start_length = current_length - segment.length()\n    segment_fraction = (length_to_reach - segment_start_length) / segment.length()\n    position, angle = segment.get_position_and_angle_at_fraction(segment_fraction)\n    return position, angle\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownPhysicalStimulationLayout.length","title":"length","text":"<pre><code>length() -&gt; float\n</code></pre> <p>Return the total length of the arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def length(self) -&gt; float:\n    \"\"\"Return the total length of the arc path.\"\"\"\n    return sum([segment.length() for segment in self.segments])\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownPhysicalStimulationLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownPhysicalStimulationLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the arc.\"\"\"\n    return []\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownPhysicalStimulationLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the arc's own drawing elements, including those of its mixins.</p> Source code in <code>src/momapy/celldesigner/elements.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the arc's own drawing elements, including those of its mixins.\"\"\"\n    drawing_elements = super_or_builder(\n        CellDesignerSingleHeadedArc, self\n    ).own_drawing_elements()\n    done_bases = []\n    for base in type(self).__mro__:\n        if (\n            issubclass_or_builder(base, _SBGNMixin)\n            and base is not type(self)\n            and not any([issubclass(done_base, base) for done_base in done_bases])\n        ):\n            drawing_elements += getattr(base, \"_mixin_drawing_elements\")(self)\n            done_bases.append(base)\n    return drawing_elements\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownPhysicalStimulationLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownPhysicalStimulationLayout.path_drawing_elements","title":"path_drawing_elements","text":"<pre><code>path_drawing_elements() -&gt; list[Path]\n</code></pre> <p>Return the drawing elements of the single-headed arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def path_drawing_elements(self) -&gt; list[Path]:\n    \"\"\"Return the drawing elements of the single-headed arc path.\"\"\"\n    arrowhead_length = self.arrowhead_length()\n    if len(self.segments) == 1:\n        segment = (\n            self.segments[0]\n            .shortened(self.start_shorten, \"start\")\n            .shortened(self.end_shorten + arrowhead_length, \"end\")\n        )\n        actions = [\n            MoveTo(segment.p1),\n            self._make_path_action_from_segment(segment),\n        ]\n    else:\n        first_segment = self.segments[0].shortened(self.start_shorten, \"start\")\n        last_segment = self.segments[-1].shortened(\n            self.end_shorten + arrowhead_length, \"end\"\n        )\n        actions = [\n            MoveTo(first_segment.p1),\n            self._make_path_action_from_segment(first_segment),\n        ]\n        for segment in self.segments[1:-1]:\n            action = self._make_path_action_from_segment(segment)\n            actions.append(action)\n        actions.append(self._make_path_action_from_segment(last_segment))\n    path = Path(\n        actions=tuple(actions),\n        class_=f\"{type(self).__name__}_path\",\n        fill=self.path_fill,\n        filter_=self.path_filter,\n        id_=f\"{self.id_}_path\",\n        stroke=self.path_stroke,\n        stroke_dasharray=self.path_stroke_dasharray,\n        stroke_dashoffset=self.path_stroke_dashoffset,\n        stroke_width=self.path_stroke_width,\n        transform=self.path_transform,\n    )\n    return [path]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownPhysicalStimulationLayout.points","title":"points","text":"<pre><code>points() -&gt; list[Point]\n</code></pre> <p>Return the points of the arc path.</p> <p>An arc with no segments has no points and returns an empty list.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def points(self) -&gt; list[Point]:\n    \"\"\"Return the points of the arc path.\n\n    An arc with no segments has no points and returns an empty list.\n    \"\"\"\n    if not self.segments:\n        return []\n    points = []\n    for segment in self.segments:\n        points.append(segment.p1)\n    points.append(segment.p2)\n    return points\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownPhysicalStimulationLayout.start_point","title":"start_point","text":"<pre><code>start_point() -&gt; Point\n</code></pre> <p>Return the starting point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_point(self) -&gt; Point:\n    \"\"\"Return the starting point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[0]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownPhysicalStimulationLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownPositiveInfluence","title":"UnknownPositiveInfluence  <code>dataclass</code>","text":"<pre><code>UnknownPositiveInfluence(*, id_: str = make_uuid4_as_str(), source: Species | BooleanLogicGate, target: Species | None)\n</code></pre> <p>               Bases: <code>UnknownModulation</code></p> <p>Unknown positive influence.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'9a5a72a3-a789-493a-8f00-87c5172de534'</code> <code>source</code> <code>Species | BooleanLogicGate</code> <p>The source of the modulation</p> required <code>target</code> <code>Species | None</code> <p>The target of the modulation</p> required <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownPositiveInfluence.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownPositiveInfluenceLayout","title":"UnknownPositiveInfluenceLayout  <code>dataclass</code>","text":"<pre><code>UnknownPositiveInfluenceLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, end_shorten: float = 0.0, fill: NoneValueType | Color | None = None, filter_: NoneValueType | Filter | None = None, path_fill: NoneValueType | Color | None = NoneValue, path_filter: NoneValueType | Filter | None = None, path_stroke: NoneValueType | Color | None = black, path_stroke_dasharray: NoneValueType | tuple[float, ...] | None = (12, 4), path_stroke_dashoffset: NoneValueType | float | None = None, path_stroke_width: float | None = 1.0, path_transform: NoneValueType | tuple[Transformation, ...] | None = None, stroke: NoneValueType | Color | None = None, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: NoneValueType | float | None = None, segments: tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...] = tuple(), source: LayoutElement | None = None, start_shorten: float = 0.0, target: LayoutElement | None = None, transform: NoneValueType | tuple[Transformation, ...] | None = None, arrowhead_fill: NoneValueType | Color | None = NoneValue, arrowhead_filter: NoneValueType | Filter | None = None, arrowhead_stroke: NoneValueType | Color | None = black, arrowhead_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, arrowhead_stroke_dashoffset: NoneValueType | float | None = None, arrowhead_stroke_width: float | None = 2.0, arrowhead_transform: NoneValueType | tuple[Transformation, ...] | None = None, arrowhead_height: float = 10.0, arrowhead_width: float = 10.0)\n</code></pre> <p>               Bases: <code>CellDesignerSingleHeadedArc</code></p> <p>Unknown positive influence layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'554cf864-da70-4ee9-9390-09eb08a338ef'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>end_shorten</code> <code>float</code> <p>The length the end of the arc will be shorten by</p> <code>0.0</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the arc</p> <code>None</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the arc</p> <code>None</code> <code>path_fill</code> <code>NoneValueType | Color | None</code> <p>The path fill color of the arc</p> <code>&lt;momapy.drawing.NoneValueType object at 0x7f8b17323e00&gt;</code> <code>path_filter</code> <code>NoneValueType | Filter | None</code> <p>The path filter of the arc</p> <code>None</code> <code>path_stroke</code> <code>NoneValueType | Color | None</code> <p>The path stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>path_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The path stroke dasharray of the arc</p> <code>(12, 4)</code> <code>path_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The path stroke dashoffset of the arc</p> <code>None</code> <code>path_stroke_width</code> <code>float | None</code> <p>The path stroke width of the arc</p> <code>1.0</code> <code>path_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The path transform of the arc</p> <code>None</code> <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the arc</p> <code>None</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the arc</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the arc</p> <code>None</code> <code>stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the arc</p> <code>None</code> <code>segments</code> <code>tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...]</code> <p>The path segments of the arc</p> <code>&lt;dynamic&gt;</code> <code>source</code> <code>LayoutElement | None</code> <p>The source of the arc</p> <code>None</code> <code>start_shorten</code> <code>float</code> <p>The length the start of the arc will be shorten by</p> <code>0.0</code> <code>target</code> <code>LayoutElement | None</code> <p>The target of the arc</p> <code>None</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the arc</p> <code>None</code> <code>arrowhead_fill</code> <code>NoneValueType | Color | None</code> <p>The arrowhead fill color of the arc</p> <code>&lt;momapy.drawing.NoneValueType object at 0x7f8b17323e00&gt;</code> <code>arrowhead_filter</code> <code>NoneValueType | Filter | None</code> <p>The arrowhead filter of the arc</p> <code>None</code> <code>arrowhead_stroke</code> <code>NoneValueType | Color | None</code> <p>The arrowhead stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>arrowhead_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The arrowhead stroke dasharray of the arc</p> <code>None</code> <code>arrowhead_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The arrowhead stroke dashoffset of the arc</p> <code>None</code> <code>arrowhead_stroke_width</code> <code>float | None</code> <p>The arrowhead stroke width of the arc</p> <code>2.0</code> <code>arrowhead_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The arrowhead transform of the arc</p> <code>None</code> <code>arrowhead_height</code> <code>float</code> <p>The height of the arrowhead.</p> <code>10.0</code> <code>arrowhead_width</code> <code>float</code> <p>The width of the arrowhead.</p> <code>10.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>arrowhead_base</code> <p>Return the arrowhead base anchor point of the single-headed arc.</p> <code>arrowhead_bbox</code> <p>Return the bounding box of the single-headed arc arrowhead.</p> <code>arrowhead_border</code> <p>Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.</p> <code>arrowhead_drawing_elements</code> <p>Return the drawing elements of the single-headed arc arrowhead.</p> <code>arrowhead_length</code> <p>Return the length of the single-headed arc arrowhead.</p> <code>arrowhead_tip</code> <p>Return the arrowhead tip anchor point of the single-headed arc.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>childless</code> <p>Return a copy of the arc with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>end_point</code> <p>Return the ending point of the arc.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>fraction</code> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <code>length</code> <p>Return the total length of the arc path.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_children</code> <p>Return the self children of the arc.</p> <code>own_drawing_elements</code> <p>Return the arc's own drawing elements, including those of its mixins.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>path_drawing_elements</code> <p>Return the drawing elements of the single-headed arc path.</p> <code>points</code> <p>Return the points of the arc path.</p> <code>start_point</code> <p>Return the starting point of the arc.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownPositiveInfluenceLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownPositiveInfluenceLayout.arrowhead_base","title":"arrowhead_base","text":"<pre><code>arrowhead_base() -&gt; Point\n</code></pre> <p>Return the arrowhead base anchor point of the single-headed arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_base(self) -&gt; Point:\n    \"\"\"Return the arrowhead base anchor point of the single-headed arc.\"\"\"\n    arrowhead_length = self.arrowhead_length()\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return self.arrowhead_tip() - (arrowhead_length, 0)\n    fraction = 1 - (arrowhead_length + self.end_shorten) / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownPositiveInfluenceLayout.arrowhead_bbox","title":"arrowhead_bbox","text":"<pre><code>arrowhead_bbox() -&gt; Bbox\n</code></pre> <p>Return the bounding box of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_bbox(self) -&gt; Bbox:\n    \"\"\"Return the bounding box of the single-headed arc arrowhead.\"\"\"\n    return get_drawing_elements_bbox(self.arrowhead_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownPositiveInfluenceLayout.arrowhead_border","title":"arrowhead_border","text":"<pre><code>arrowhead_border(point: Point) -&gt; Point\n</code></pre> <p>Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_border(self, point: Point) -&gt; Point:\n    \"\"\"Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    point = get_drawing_elements_border(self.arrowhead_drawing_elements(), point)\n    if point is None:\n        return self.arrowhead_tip()\n    return point\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownPositiveInfluenceLayout.arrowhead_drawing_elements","title":"arrowhead_drawing_elements","text":"<pre><code>arrowhead_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_drawing_elements(\n    self,\n) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the single-headed arc arrowhead.\"\"\"\n    drawing_elements = self._arrowhead_border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_arrowhead\",\n        elements=tuple(drawing_elements),\n        fill=self.arrowhead_fill,\n        filter_=self.arrowhead_filter,\n        id_=f\"{self.id_}_arrowhead\",\n        stroke=self.arrowhead_stroke,\n        stroke_dasharray=self.arrowhead_stroke_dasharray,\n        stroke_dashoffset=self.arrowhead_stroke_dashoffset,\n        stroke_width=self.arrowhead_stroke_width,\n        transform=self.arrowhead_transform,\n    )\n    transformation = self._get_arrowhead_transformation()\n    group = group.transformed(transformation)\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownPositiveInfluenceLayout.arrowhead_length","title":"arrowhead_length","text":"<pre><code>arrowhead_length() -&gt; float\n</code></pre> <p>Return the length of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_length(self) -&gt; float:\n    \"\"\"Return the length of the single-headed arc arrowhead.\"\"\"\n    bbox = get_drawing_elements_bbox(self._arrowhead_border_drawing_elements())\n    if math.isnan(bbox.width):\n        return 0.0\n    return bbox.east().x\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownPositiveInfluenceLayout.arrowhead_tip","title":"arrowhead_tip","text":"<pre><code>arrowhead_tip() -&gt; Point\n</code></pre> <p>Return the arrowhead tip anchor point of the single-headed arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_tip(self) -&gt; Point:\n    \"\"\"Return the arrowhead tip anchor point of the single-headed arc.\"\"\"\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return segment.p2\n    fraction = 1 - self.end_shorten / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownPositiveInfluenceLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownPositiveInfluenceLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the arc with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the arc with no children.\"\"\"\n    return dataclasses.replace(self, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownPositiveInfluenceLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownPositiveInfluenceLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownPositiveInfluenceLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownPositiveInfluenceLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownPositiveInfluenceLayout.end_point","title":"end_point","text":"<pre><code>end_point() -&gt; Point\n</code></pre> <p>Return the ending point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_point(self) -&gt; Point:\n    \"\"\"Return the ending point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[-1]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownPositiveInfluenceLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownPositiveInfluenceLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownPositiveInfluenceLayout.fraction","title":"fraction","text":"<pre><code>fraction(fraction: float) -&gt; tuple[Point, float]\n</code></pre> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def fraction(self, fraction: float) -&gt; tuple[Point, float]:\n    \"\"\"Return the position and angle on the arc at a given fraction (of the total arc length).\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    current_length = 0\n    length_to_reach = fraction * self.length()\n    for segment in self.segments:\n        current_length += segment.length()\n        if current_length &gt;= length_to_reach:\n            break\n    segment_start_length = current_length - segment.length()\n    segment_fraction = (length_to_reach - segment_start_length) / segment.length()\n    position, angle = segment.get_position_and_angle_at_fraction(segment_fraction)\n    return position, angle\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownPositiveInfluenceLayout.length","title":"length","text":"<pre><code>length() -&gt; float\n</code></pre> <p>Return the total length of the arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def length(self) -&gt; float:\n    \"\"\"Return the total length of the arc path.\"\"\"\n    return sum([segment.length() for segment in self.segments])\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownPositiveInfluenceLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownPositiveInfluenceLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the arc.\"\"\"\n    return []\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownPositiveInfluenceLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the arc's own drawing elements, including those of its mixins.</p> Source code in <code>src/momapy/celldesigner/elements.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the arc's own drawing elements, including those of its mixins.\"\"\"\n    drawing_elements = super_or_builder(\n        CellDesignerSingleHeadedArc, self\n    ).own_drawing_elements()\n    done_bases = []\n    for base in type(self).__mro__:\n        if (\n            issubclass_or_builder(base, _SBGNMixin)\n            and base is not type(self)\n            and not any([issubclass(done_base, base) for done_base in done_bases])\n        ):\n            drawing_elements += getattr(base, \"_mixin_drawing_elements\")(self)\n            done_bases.append(base)\n    return drawing_elements\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownPositiveInfluenceLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownPositiveInfluenceLayout.path_drawing_elements","title":"path_drawing_elements","text":"<pre><code>path_drawing_elements() -&gt; list[Path]\n</code></pre> <p>Return the drawing elements of the single-headed arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def path_drawing_elements(self) -&gt; list[Path]:\n    \"\"\"Return the drawing elements of the single-headed arc path.\"\"\"\n    arrowhead_length = self.arrowhead_length()\n    if len(self.segments) == 1:\n        segment = (\n            self.segments[0]\n            .shortened(self.start_shorten, \"start\")\n            .shortened(self.end_shorten + arrowhead_length, \"end\")\n        )\n        actions = [\n            MoveTo(segment.p1),\n            self._make_path_action_from_segment(segment),\n        ]\n    else:\n        first_segment = self.segments[0].shortened(self.start_shorten, \"start\")\n        last_segment = self.segments[-1].shortened(\n            self.end_shorten + arrowhead_length, \"end\"\n        )\n        actions = [\n            MoveTo(first_segment.p1),\n            self._make_path_action_from_segment(first_segment),\n        ]\n        for segment in self.segments[1:-1]:\n            action = self._make_path_action_from_segment(segment)\n            actions.append(action)\n        actions.append(self._make_path_action_from_segment(last_segment))\n    path = Path(\n        actions=tuple(actions),\n        class_=f\"{type(self).__name__}_path\",\n        fill=self.path_fill,\n        filter_=self.path_filter,\n        id_=f\"{self.id_}_path\",\n        stroke=self.path_stroke,\n        stroke_dasharray=self.path_stroke_dasharray,\n        stroke_dashoffset=self.path_stroke_dashoffset,\n        stroke_width=self.path_stroke_width,\n        transform=self.path_transform,\n    )\n    return [path]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownPositiveInfluenceLayout.points","title":"points","text":"<pre><code>points() -&gt; list[Point]\n</code></pre> <p>Return the points of the arc path.</p> <p>An arc with no segments has no points and returns an empty list.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def points(self) -&gt; list[Point]:\n    \"\"\"Return the points of the arc path.\n\n    An arc with no segments has no points and returns an empty list.\n    \"\"\"\n    if not self.segments:\n        return []\n    points = []\n    for segment in self.segments:\n        points.append(segment.p1)\n    points.append(segment.p2)\n    return points\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownPositiveInfluenceLayout.start_point","title":"start_point","text":"<pre><code>start_point() -&gt; Point\n</code></pre> <p>Return the starting point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_point(self) -&gt; Point:\n    \"\"\"Return the starting point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[0]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownPositiveInfluenceLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownTransition","title":"UnknownTransition  <code>dataclass</code>","text":"<pre><code>UnknownTransition(*, id_: str = make_uuid4_as_str(), name: str | None = None, sbo_term: str | None = None, metaid: str | None = None, reversible: bool = False, compartment: Compartment | None = None, reactants: frozenset[Reactant] = frozenset(), products: frozenset[Product] = frozenset(), modifiers: frozenset[KnownOrUnknownModulator] = frozenset(), has_external_source: bool = False, has_external_sink: bool = False)\n</code></pre> <p>               Bases: <code>Reaction</code></p> <p>Unknown transition.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'db47214c-2ca8-41bd-8fe2-18b696ce4649'</code> <code>name</code> <code>str | None</code> <p>Human-readable name of the element.</p> <code>None</code> <code>sbo_term</code> <code>str | None</code> <p>Optional SBO term identifier for semantic annotation.</p> <code>None</code> <code>metaid</code> <code>str | None</code> <p>Optional metadata identifier for RDF annotations.</p> <code>None</code> <code>reversible</code> <code>bool</code> <p>Whether the reaction can proceed in both directions.</p> <code>False</code> <code>compartment</code> <code>Compartment | None</code> <p>Optional compartment where the reaction occurs.</p> <code>None</code> <code>reactants</code> <code>frozenset[Reactant]</code> <p>The reactants of the reaction</p> <code>&lt;dynamic&gt;</code> <code>products</code> <code>frozenset[Product]</code> <p>The products of the reaction</p> <code>&lt;dynamic&gt;</code> <code>modifiers</code> <code>frozenset[KnownOrUnknownModulator]</code> <p>The modifiers of the reaction</p> <code>&lt;dynamic&gt;</code> <code>has_external_source</code> <code>bool</code> <p>Whether the reaction has an unspecified external source (a Degraded reactant in CellDesigner).</p> <code>False</code> <code>has_external_sink</code> <code>bool</code> <p>Whether the reaction has an unspecified external sink (a Degraded product in CellDesigner).</p> <code>False</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownTransition.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownTransitionLayout","title":"UnknownTransitionLayout  <code>dataclass</code>","text":"<pre><code>UnknownTransitionLayout(*, left_connector_fraction: float = 0.375, right_connector_fraction: float = 0.625, reaction_node_font_family: str = DEFAULT_FONT_FAMILY, reaction_node_font_fill: Color | NoneValueType = black, reaction_node_font_stroke: Color | NoneValueType = NoneValue, reaction_node_font_style: FontStyle = NORMAL, reaction_node_font_weight: FontWeight | float = BOLD, reaction_node_text: str | None = '?', reaction_node_height: float = 10.0, reaction_node_width: float = 10.0, reaction_node_segment: int = 1, reaction_node_stroke: NoneValueType | Color | None = black, reaction_node_stroke_width: float | None = 1.0, reaction_node_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, reaction_node_stroke_dashoffset: float | None = None, reaction_node_fill: NoneValueType | Color | None = white, reaction_node_transform: NoneValueType | tuple[Transformation, ...] | None = None, reaction_node_filter: NoneValueType | Filter | None = None, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, end_shorten: float = 2.0, fill: NoneValueType | Color | None = None, filter_: NoneValueType | Filter | None = None, path_fill: NoneValueType | Color | None = NoneValue, path_filter: NoneValueType | Filter | None = None, path_stroke: NoneValueType | Color | None = black, path_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, path_stroke_dashoffset: NoneValueType | float | None = None, path_stroke_width: float | None = 1.0, path_transform: NoneValueType | tuple[Transformation, ...] | None = None, stroke: NoneValueType | Color | None = None, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: NoneValueType | float | None = None, segments: tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...] = tuple(), source: LayoutElement | None = None, start_shorten: float = 2.0, target: LayoutElement | None = None, transform: NoneValueType | tuple[Transformation, ...] | None = None, end_arrowhead_fill: NoneValueType | Color | None = black, end_arrowhead_filter: NoneValueType | Filter | None = None, end_arrowhead_stroke: NoneValueType | Color | None = black, end_arrowhead_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, end_arrowhead_stroke_dashoffset: float | None = None, end_arrowhead_stroke_width: float | None = 1.0, end_arrowhead_transform: NoneValueType | tuple[Transformation, ...] | None = None, start_arrowhead_fill: NoneValueType | Color | None = black, start_arrowhead_filter: NoneValueType | Filter | None = None, start_arrowhead_stroke: NoneValueType | Color | None = black, start_arrowhead_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, start_arrowhead_stroke_dashoffset: float | None = None, start_arrowhead_stroke_width: float | None = 1.0, start_arrowhead_transform: NoneValueType | tuple[Transformation, ...] | None = None, reversible: bool = False, end_arrowhead_height: float = 8.0, end_arrowhead_width: float = 15.0, start_arrowhead_height: float = 8.0, start_arrowhead_width: float = 15.0)\n</code></pre> <p>               Bases: <code>ReactionLayout</code>, <code>_ReactionNodeMixin</code></p> <p>Unknown transition layout.</p> <p>Parameters:</p> Name Type Description Default <code>left_connector_fraction</code> <code>float</code> <p>The fraction along the segment where the left connector attaches.</p> <code>0.375</code> <code>right_connector_fraction</code> <code>float</code> <p>The fraction along the segment where the right connector attaches.</p> <code>0.625</code> <code>reaction_node_font_family</code> <code>str</code> <p>The font family of the reaction node label.</p> <code>'DejaVu Sans'</code> <code>reaction_node_font_fill</code> <code>Color | NoneValueType</code> <p>The fill color of the reaction node label.</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>reaction_node_font_stroke</code> <code>Color | NoneValueType</code> <p>The stroke color of the reaction node label.</p> <code>&lt;momapy.drawing.NoneValueType object at 0x7f8b17323e00&gt;</code> <code>reaction_node_font_style</code> <code>FontStyle</code> <p>The font style of the reaction node label.</p> <code>&lt;FontStyle.NORMAL: 0&gt;</code> <code>reaction_node_font_weight</code> <code>FontWeight | float</code> <p>The font weight of the reaction node label.</p> <code>&lt;FontWeight.BOLD: 1&gt;</code> <code>reaction_node_text</code> <code>str | None</code> <p>The text of the reaction node label.</p> <code>'?'</code> <code>reaction_node_height</code> <code>float</code> <p>The height of the reaction node.</p> <code>10.0</code> <code>reaction_node_width</code> <code>float</code> <p>The width of the reaction node.</p> <code>10.0</code> <code>reaction_node_segment</code> <code>int</code> <p>The index of the arc segment carrying the reaction node.</p> <code>1</code> <code>reaction_node_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the reaction node.</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>reaction_node_stroke_width</code> <code>float | None</code> <p>The stroke width of the reaction node.</p> <code>1.0</code> <code>reaction_node_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The dash pattern of the reaction node's border.</p> <code>None</code> <code>reaction_node_stroke_dashoffset</code> <code>float | None</code> <p>The dash offset of the reaction node's border.</p> <code>None</code> <code>reaction_node_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the reaction node.</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>reaction_node_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform applied to the reaction node.</p> <code>None</code> <code>reaction_node_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter applied to the reaction node.</p> <code>None</code> <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'8a643a9d-f8fb-4941-aef1-5580283e93c0'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>end_shorten</code> <code>float</code> <p>The length the end of the arc will be shorten by</p> <code>2.0</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the arc</p> <code>None</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the arc</p> <code>None</code> <code>path_fill</code> <code>NoneValueType | Color | None</code> <p>The path fill color of the arc</p> <code>&lt;momapy.drawing.NoneValueType object at 0x7f8b17323e00&gt;</code> <code>path_filter</code> <code>NoneValueType | Filter | None</code> <p>The path filter of the arc</p> <code>None</code> <code>path_stroke</code> <code>NoneValueType | Color | None</code> <p>The path stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>path_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The path stroke dasharray of the arc</p> <code>None</code> <code>path_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The path stroke dashoffset of the arc</p> <code>None</code> <code>path_stroke_width</code> <code>float | None</code> <p>The path stroke width of the arc</p> <code>1.0</code> <code>path_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The path transform of the arc</p> <code>None</code> <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the arc</p> <code>None</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the arc</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the arc</p> <code>None</code> <code>stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the arc</p> <code>None</code> <code>segments</code> <code>tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...]</code> <p>The path segments of the arc</p> <code>&lt;dynamic&gt;</code> <code>source</code> <code>LayoutElement | None</code> <p>The source of the arc</p> <code>None</code> <code>start_shorten</code> <code>float</code> <p>The length the start of the arc will be shorten by</p> <code>2.0</code> <code>target</code> <code>LayoutElement | None</code> <p>The target of the arc</p> <code>None</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the arc</p> <code>None</code> <code>end_arrowhead_fill</code> <code>NoneValueType | Color | None</code> <p>The end arrowhead fill color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>end_arrowhead_filter</code> <code>NoneValueType | Filter | None</code> <p>The end arrowhead filter of the arc</p> <code>None</code> <code>end_arrowhead_stroke</code> <code>NoneValueType | Color | None</code> <p>The end arrowhead stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>end_arrowhead_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The end arrowhead stroke dasharray of the arc</p> <code>None</code> <code>end_arrowhead_stroke_dashoffset</code> <code>float | None</code> <p>The end arrowhead stroke dashoffset of the arc</p> <code>None</code> <code>end_arrowhead_stroke_width</code> <code>float | None</code> <p>The end arrowhead stroke width of the arc</p> <code>1.0</code> <code>end_arrowhead_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The end arrowhead transform of the arc</p> <code>None</code> <code>start_arrowhead_fill</code> <code>NoneValueType | Color | None</code> <p>The start arrowhead fill color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>start_arrowhead_filter</code> <code>NoneValueType | Filter | None</code> <p>The start arrowhead filter of the arc</p> <code>None</code> <code>start_arrowhead_stroke</code> <code>NoneValueType | Color | None</code> <p>The start arrowhead stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>start_arrowhead_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The start arrowhead stroke dasharray of the arc</p> <code>None</code> <code>start_arrowhead_stroke_dashoffset</code> <code>float | None</code> <p>The start arrowhead stroke dashoffset of the arc</p> <code>None</code> <code>start_arrowhead_stroke_width</code> <code>float | None</code> <p>The start arrowhead stroke width of the arc</p> <code>1.0</code> <code>start_arrowhead_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The start arrowhead transform of the arc</p> <code>None</code> <code>reversible</code> <code>bool</code> <p>Whether the reaction is reversible.</p> <code>False</code> <code>end_arrowhead_height</code> <code>float</code> <p>The height of the end arrowhead.</p> <code>8.0</code> <code>end_arrowhead_width</code> <code>float</code> <p>The width of the end arrowhead.</p> <code>15.0</code> <code>start_arrowhead_height</code> <code>float</code> <p>The height of the start arrowhead.</p> <code>8.0</code> <code>start_arrowhead_width</code> <code>float</code> <p>The width of the start arrowhead.</p> <code>15.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>childless</code> <p>Return a copy of the arc with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>end_arrowhead_base</code> <p>Return the base anchor point of the double-headed arc end arrowhead.</p> <code>end_arrowhead_bbox</code> <p>Return the bounding box of the double-headed arc end arrowhead.</p> <code>end_arrowhead_border</code> <p>Return the point at the intersection of the drawing elements of the double-headed arc end arrowhead and the line going through the center of these drawing elements and the given point.</p> <code>end_arrowhead_drawing_elements</code> <p>Return the drawing elements of the double-headed arc end arrowhead.</p> <code>end_arrowhead_length</code> <p>Return the length of the double-headed arc end arrowhead.</p> <code>end_arrowhead_tip</code> <p>Return the tip anchor point of the double-headed arc end arrowhead.</p> <code>end_point</code> <p>Return the ending point of the arc.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>fraction</code> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <code>length</code> <p>Return the total length of the arc path.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_children</code> <p>Return the self children of the arc.</p> <code>own_drawing_elements</code> <p>Return the arc's own drawing elements, including those of its mixins.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>path_drawing_elements</code> <p>Return the drawing elements of the double-headed arc path.</p> <code>points</code> <p>Return the points of the arc path.</p> <code>start_arrowhead_base</code> <p>Return the base anchor point of the double-headed arc start arrowhead.</p> <code>start_arrowhead_bbox</code> <p>Return the bounding box of the double-headed arc start arrowhead.</p> <code>start_arrowhead_border</code> <p>Return the point at the intersection of the drawing elements of the double-headed arc start arrowhead and the line going through the center of these drawing elements and the given point.</p> <code>start_arrowhead_drawing_elements</code> <p>Return the drawing elements of the double-headed arc start arrowhead.</p> <code>start_arrowhead_length</code> <p>Return the length of the double-headed arc start arrowhead.</p> <code>start_arrowhead_tip</code> <p>Return the tip anchor point of the double-headed arc start arrowhead.</p> <code>start_point</code> <p>Return the starting point of the arc.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownTransitionLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownTransitionLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownTransitionLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the arc with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the arc with no children.\"\"\"\n    return dataclasses.replace(self, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownTransitionLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownTransitionLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownTransitionLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownTransitionLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownTransitionLayout.end_arrowhead_base","title":"end_arrowhead_base","text":"<pre><code>end_arrowhead_base() -&gt; Point\n</code></pre> <p>Return the base anchor point of the double-headed arc end arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_arrowhead_base(self) -&gt; Point:\n    \"\"\"Return the base anchor point of the double-headed arc end arrowhead.\"\"\"\n    arrowhead_length = self.end_arrowhead_length()\n    if arrowhead_length == 0:\n        return self.end_point()\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return self.end_arrowhead_tip() - (arrowhead_length, 0)\n    fraction = 1 - (arrowhead_length + self.end_shorten) / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownTransitionLayout.end_arrowhead_bbox","title":"end_arrowhead_bbox","text":"<pre><code>end_arrowhead_bbox() -&gt; Bbox\n</code></pre> <p>Return the bounding box of the double-headed arc end arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_arrowhead_bbox(self) -&gt; Bbox:\n    \"\"\"Return the bounding box of the double-headed arc end arrowhead.\"\"\"\n    return get_drawing_elements_bbox(self.end_arrowhead_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownTransitionLayout.end_arrowhead_border","title":"end_arrowhead_border","text":"<pre><code>end_arrowhead_border(point: Point) -&gt; Point\n</code></pre> <p>Return the point at the intersection of the drawing elements of the double-headed arc end arrowhead and the line going through the center of these drawing elements and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_arrowhead_border(self, point: Point) -&gt; Point:\n    \"\"\"Return the point at the intersection of the drawing elements of the double-headed arc end arrowhead and the line going through the center of these drawing elements and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    point = get_drawing_elements_border(\n        self.end_arrowhead_drawing_elements(), point\n    )\n    if point is None:\n        return self.end_arrowhead_tip()\n    return point\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownTransitionLayout.end_arrowhead_drawing_elements","title":"end_arrowhead_drawing_elements","text":"<pre><code>end_arrowhead_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the double-headed arc end arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_arrowhead_drawing_elements(\n    self,\n) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the double-headed arc end arrowhead.\"\"\"\n    drawing_elements = self._end_arrowhead_border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_end_arrowhead\",\n        elements=tuple(drawing_elements),\n        fill=self.end_arrowhead_fill,\n        filter_=self.end_arrowhead_filter,\n        id_=f\"{self.id_}_end_arrowhead\",\n        stroke=self.end_arrowhead_stroke,\n        stroke_width=self.end_arrowhead_stroke_width,\n        stroke_dasharray=self.end_arrowhead_stroke_dasharray,\n        stroke_dashoffset=self.end_arrowhead_stroke_dashoffset,\n        transform=self.end_arrowhead_transform,\n    )\n    transformation = self._get_end_arrowhead_transformation()\n    group = group.transformed(transformation)\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownTransitionLayout.end_arrowhead_length","title":"end_arrowhead_length","text":"<pre><code>end_arrowhead_length() -&gt; float\n</code></pre> <p>Return the length of the double-headed arc end arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_arrowhead_length(self) -&gt; float:\n    \"\"\"Return the length of the double-headed arc end arrowhead.\"\"\"\n    bbox = get_drawing_elements_bbox(self._end_arrowhead_border_drawing_elements())\n    if math.isnan(bbox.width):\n        return 0.0\n    return bbox.east().x\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownTransitionLayout.end_arrowhead_tip","title":"end_arrowhead_tip","text":"<pre><code>end_arrowhead_tip() -&gt; Point\n</code></pre> <p>Return the tip anchor point of the double-headed arc end arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_arrowhead_tip(self) -&gt; Point:\n    \"\"\"Return the tip anchor point of the double-headed arc end arrowhead.\"\"\"\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return segment.p2\n    fraction = 1 - self.end_shorten / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownTransitionLayout.end_point","title":"end_point","text":"<pre><code>end_point() -&gt; Point\n</code></pre> <p>Return the ending point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_point(self) -&gt; Point:\n    \"\"\"Return the ending point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[-1]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownTransitionLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownTransitionLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownTransitionLayout.fraction","title":"fraction","text":"<pre><code>fraction(fraction: float) -&gt; tuple[Point, float]\n</code></pre> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def fraction(self, fraction: float) -&gt; tuple[Point, float]:\n    \"\"\"Return the position and angle on the arc at a given fraction (of the total arc length).\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    current_length = 0\n    length_to_reach = fraction * self.length()\n    for segment in self.segments:\n        current_length += segment.length()\n        if current_length &gt;= length_to_reach:\n            break\n    segment_start_length = current_length - segment.length()\n    segment_fraction = (length_to_reach - segment_start_length) / segment.length()\n    position, angle = segment.get_position_and_angle_at_fraction(segment_fraction)\n    return position, angle\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownTransitionLayout.length","title":"length","text":"<pre><code>length() -&gt; float\n</code></pre> <p>Return the total length of the arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def length(self) -&gt; float:\n    \"\"\"Return the total length of the arc path.\"\"\"\n    return sum([segment.length() for segment in self.segments])\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownTransitionLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownTransitionLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the arc.\"\"\"\n    return []\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownTransitionLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the arc's own drawing elements, including those of its mixins.</p> Source code in <code>src/momapy/celldesigner/elements.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the arc's own drawing elements, including those of its mixins.\"\"\"\n    drawing_elements = super_or_builder(\n        CellDesignerDoubleHeadedArc, self\n    ).own_drawing_elements()\n    done_bases = []\n    for base in type(self).__mro__:\n        if (\n            issubclass_or_builder(base, _SBGNMixin)\n            and base is not type(self)\n            and not any([issubclass(done_base, base) for done_base in done_bases])\n        ):\n            drawing_elements += getattr(base, \"_mixin_drawing_elements\")(self)\n            done_bases.append(base)\n    return drawing_elements\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownTransitionLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownTransitionLayout.path_drawing_elements","title":"path_drawing_elements","text":"<pre><code>path_drawing_elements() -&gt; list[Path]\n</code></pre> <p>Return the drawing elements of the double-headed arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def path_drawing_elements(self) -&gt; list[Path]:\n    \"\"\"Return the drawing elements of the double-headed arc path.\"\"\"\n    start_arrowhead_length = self.start_arrowhead_length()\n    end_arrowhead_length = self.end_arrowhead_length()\n    if len(self.segments) == 1:\n        segment = (\n            self.segments[0]\n            .shortened(self.start_shorten + start_arrowhead_length, \"start\")\n            .shortened(self.end_shorten + end_arrowhead_length, \"end\")\n        )\n        actions = [\n            MoveTo(segment.p1),\n            self._make_path_action_from_segment(segment),\n        ]\n    else:\n        first_segment = self.segments[0].shortened(\n            self.start_shorten + start_arrowhead_length, \"start\"\n        )\n        last_segment = self.segments[-1].shortened(\n            self.end_shorten + end_arrowhead_length, \"end\"\n        )\n        actions = [\n            MoveTo(first_segment.p1),\n            self._make_path_action_from_segment(first_segment),\n        ]\n        for segment in self.segments[1:-1]:\n            action = self._make_path_action_from_segment(segment)\n            actions.append(action)\n        actions.append(self._make_path_action_from_segment(last_segment))\n    path = Path(\n        actions=tuple(actions),\n        class_=f\"{type(self).__name__}_path\",\n        fill=self.path_fill,\n        filter_=self.path_filter,\n        id_=f\"{self.id_}_path\",\n        stroke=self.path_stroke,\n        stroke_dasharray=self.path_stroke_dasharray,\n        stroke_dashoffset=self.path_stroke_dashoffset,\n        stroke_width=self.path_stroke_width,\n        transform=self.path_transform,\n    )\n    return [path]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownTransitionLayout.points","title":"points","text":"<pre><code>points() -&gt; list[Point]\n</code></pre> <p>Return the points of the arc path.</p> <p>An arc with no segments has no points and returns an empty list.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def points(self) -&gt; list[Point]:\n    \"\"\"Return the points of the arc path.\n\n    An arc with no segments has no points and returns an empty list.\n    \"\"\"\n    if not self.segments:\n        return []\n    points = []\n    for segment in self.segments:\n        points.append(segment.p1)\n    points.append(segment.p2)\n    return points\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownTransitionLayout.start_arrowhead_base","title":"start_arrowhead_base","text":"<pre><code>start_arrowhead_base() -&gt; Point\n</code></pre> <p>Return the base anchor point of the double-headed arc start arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_arrowhead_base(self) -&gt; Point:\n    \"\"\"Return the base anchor point of the double-headed arc start arrowhead.\"\"\"\n    arrowhead_length = self.start_arrowhead_length()\n    if arrowhead_length == 0:\n        return self.start_point()\n    segment = self.segments[0]\n    segment = Segment(segment.p2, segment.p1)\n    segment_length = segment.length()\n    if segment_length == 0:\n        return self.start_arrowhead_tip() + (arrowhead_length, 0)\n    fraction = 1 - (arrowhead_length + self.start_shorten) / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownTransitionLayout.start_arrowhead_bbox","title":"start_arrowhead_bbox","text":"<pre><code>start_arrowhead_bbox() -&gt; Bbox\n</code></pre> <p>Return the bounding box of the double-headed arc start arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_arrowhead_bbox(self) -&gt; Bbox:\n    \"\"\"Return the bounding box of the double-headed arc start arrowhead.\"\"\"\n    return get_drawing_elements_bbox(self.start_arrowhead_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownTransitionLayout.start_arrowhead_border","title":"start_arrowhead_border","text":"<pre><code>start_arrowhead_border(point: Point) -&gt; Point\n</code></pre> <p>Return the point at the intersection of the drawing elements of the double-headed arc start arrowhead and the line going through the center of these drawing elements and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_arrowhead_border(self, point: Point) -&gt; Point:\n    \"\"\"Return the point at the intersection of the drawing elements of the double-headed arc start arrowhead and the line going through the center of these drawing elements and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    point = get_drawing_elements_border(\n        self.start_arrowhead_drawing_elements(), point\n    )\n    if point is None:\n        return self.start_arrowhead_tip()\n    return point\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownTransitionLayout.start_arrowhead_drawing_elements","title":"start_arrowhead_drawing_elements","text":"<pre><code>start_arrowhead_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the double-headed arc start arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_arrowhead_drawing_elements(\n    self,\n) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the double-headed arc start arrowhead.\"\"\"\n    drawing_elements = self._start_arrowhead_border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_start_arrowhead\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}_start_arrowhead\",\n        fill=self.start_arrowhead_fill,\n        filter_=self.start_arrowhead_filter,\n        stroke=self.start_arrowhead_stroke,\n        stroke_dasharray=self.start_arrowhead_stroke_dasharray,\n        stroke_dashoffset=self.start_arrowhead_stroke_dashoffset,\n        stroke_width=self.start_arrowhead_stroke_width,\n        transform=self.start_arrowhead_transform,\n    )\n    transformation = self._get_start_arrowhead_transformation()\n    group = group.transformed(transformation)\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownTransitionLayout.start_arrowhead_length","title":"start_arrowhead_length","text":"<pre><code>start_arrowhead_length() -&gt; float\n</code></pre> <p>Return the length of the double-headed arc start arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_arrowhead_length(self) -&gt; float:\n    \"\"\"Return the length of the double-headed arc start arrowhead.\"\"\"\n    bbox = get_drawing_elements_bbox(\n        self._start_arrowhead_border_drawing_elements()\n    )\n    if math.isnan(bbox.width):\n        return 0.0\n    return abs(bbox.west().x)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownTransitionLayout.start_arrowhead_tip","title":"start_arrowhead_tip","text":"<pre><code>start_arrowhead_tip() -&gt; Point\n</code></pre> <p>Return the tip anchor point of the double-headed arc start arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_arrowhead_tip(self) -&gt; Point:\n    \"\"\"Return the tip anchor point of the double-headed arc start arrowhead.\"\"\"\n    segment = self.segments[0]\n    segment = Segment(segment.p2, segment.p1)\n    segment_length = segment.length()\n    if segment_length == 0:\n        return segment.p2\n    fraction = 1 - self.start_shorten / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownTransitionLayout.start_point","title":"start_point","text":"<pre><code>start_point() -&gt; Point\n</code></pre> <p>Return the starting point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_point(self) -&gt; Point:\n    \"\"\"Return the starting point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[0]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownTransitionLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownTriggering","title":"UnknownTriggering  <code>dataclass</code>","text":"<pre><code>UnknownTriggering(*, id_: str = make_uuid4_as_str(), source: Species | BooleanLogicGate, target: Species | None)\n</code></pre> <p>               Bases: <code>UnknownModulation</code></p> <p>Unknown triggering.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'90cfdcb6-b0da-4ba5-95c4-e058ad8febc9'</code> <code>source</code> <code>Species | BooleanLogicGate</code> <p>The source of the modulation</p> required <code>target</code> <code>Species | None</code> <p>The target of the modulation</p> required <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownTriggering.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownTriggeringLayout","title":"UnknownTriggeringLayout  <code>dataclass</code>","text":"<pre><code>UnknownTriggeringLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, end_shorten: float = 0.0, fill: NoneValueType | Color | None = None, filter_: NoneValueType | Filter | None = None, path_fill: NoneValueType | Color | None = NoneValue, path_filter: NoneValueType | Filter | None = None, path_stroke: NoneValueType | Color | None = black, path_stroke_dasharray: NoneValueType | tuple[float, ...] | None = (12, 4), path_stroke_dashoffset: NoneValueType | float | None = None, path_stroke_width: float | None = 1.0, path_transform: NoneValueType | tuple[Transformation, ...] | None = None, stroke: NoneValueType | Color | None = None, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: NoneValueType | float | None = None, segments: tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...] = tuple(), source: LayoutElement | None = None, start_shorten: float = 0.0, target: LayoutElement | None = None, transform: NoneValueType | tuple[Transformation, ...] | None = None, arrowhead_fill: NoneValueType | Color | None = white, arrowhead_filter: NoneValueType | Filter | None = None, arrowhead_stroke: NoneValueType | Color | None = black, arrowhead_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, arrowhead_stroke_dashoffset: NoneValueType | float | None = None, arrowhead_stroke_width: float | None = 1.0, arrowhead_transform: NoneValueType | tuple[Transformation, ...] | None = None, arrowhead_bar_height: float = 8.0, arrowhead_sep: float = 5.0, arrowhead_triangle_height: float = 10.0, arrowhead_triangle_width: float = 15.0)\n</code></pre> <p>               Bases: <code>CellDesignerSingleHeadedArc</code></p> <p>Unknown triggering layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'847c732f-9976-4a31-932b-04265aa2a25f'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>end_shorten</code> <code>float</code> <p>The length the end of the arc will be shorten by</p> <code>0.0</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the arc</p> <code>None</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the arc</p> <code>None</code> <code>path_fill</code> <code>NoneValueType | Color | None</code> <p>The path fill color of the arc</p> <code>&lt;momapy.drawing.NoneValueType object at 0x7f8b17323e00&gt;</code> <code>path_filter</code> <code>NoneValueType | Filter | None</code> <p>The path filter of the arc</p> <code>None</code> <code>path_stroke</code> <code>NoneValueType | Color | None</code> <p>The path stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>path_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The path stroke dasharray of the arc</p> <code>(12, 4)</code> <code>path_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The path stroke dashoffset of the arc</p> <code>None</code> <code>path_stroke_width</code> <code>float | None</code> <p>The path stroke width of the arc</p> <code>1.0</code> <code>path_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The path transform of the arc</p> <code>None</code> <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the arc</p> <code>None</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the arc</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the arc</p> <code>None</code> <code>stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the arc</p> <code>None</code> <code>segments</code> <code>tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...]</code> <p>The path segments of the arc</p> <code>&lt;dynamic&gt;</code> <code>source</code> <code>LayoutElement | None</code> <p>The source of the arc</p> <code>None</code> <code>start_shorten</code> <code>float</code> <p>The length the start of the arc will be shorten by</p> <code>0.0</code> <code>target</code> <code>LayoutElement | None</code> <p>The target of the arc</p> <code>None</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the arc</p> <code>None</code> <code>arrowhead_fill</code> <code>NoneValueType | Color | None</code> <p>The arrowhead fill color of the arc</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>arrowhead_filter</code> <code>NoneValueType | Filter | None</code> <p>The arrowhead filter of the arc</p> <code>None</code> <code>arrowhead_stroke</code> <code>NoneValueType | Color | None</code> <p>The arrowhead stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>arrowhead_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The arrowhead stroke dasharray of the arc</p> <code>None</code> <code>arrowhead_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The arrowhead stroke dashoffset of the arc</p> <code>None</code> <code>arrowhead_stroke_width</code> <code>float | None</code> <p>The arrowhead stroke width of the arc</p> <code>1.0</code> <code>arrowhead_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The arrowhead transform of the arc</p> <code>None</code> <code>arrowhead_bar_height</code> <code>float</code> <p>The height of the arrowhead bar.</p> <code>8.0</code> <code>arrowhead_sep</code> <code>float</code> <p>The separation between the bar and the triangle.</p> <code>5.0</code> <code>arrowhead_triangle_height</code> <code>float</code> <p>The height of the arrowhead triangle.</p> <code>10.0</code> <code>arrowhead_triangle_width</code> <code>float</code> <p>The width of the arrowhead triangle.</p> <code>15.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>arrowhead_base</code> <p>Return the arrowhead base anchor point of the single-headed arc.</p> <code>arrowhead_bbox</code> <p>Return the bounding box of the single-headed arc arrowhead.</p> <code>arrowhead_border</code> <p>Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.</p> <code>arrowhead_drawing_elements</code> <p>Return the drawing elements of the single-headed arc arrowhead.</p> <code>arrowhead_length</code> <p>Return the length of the single-headed arc arrowhead.</p> <code>arrowhead_tip</code> <p>Return the arrowhead tip anchor point of the single-headed arc.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>childless</code> <p>Return a copy of the arc with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>end_point</code> <p>Return the ending point of the arc.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>fraction</code> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <code>length</code> <p>Return the total length of the arc path.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_children</code> <p>Return the self children of the arc.</p> <code>own_drawing_elements</code> <p>Return the arc's own drawing elements, including those of its mixins.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>path_drawing_elements</code> <p>Return the drawing elements of the single-headed arc path.</p> <code>points</code> <p>Return the points of the arc path.</p> <code>start_point</code> <p>Return the starting point of the arc.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownTriggeringLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownTriggeringLayout.arrowhead_base","title":"arrowhead_base","text":"<pre><code>arrowhead_base() -&gt; Point\n</code></pre> <p>Return the arrowhead base anchor point of the single-headed arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_base(self) -&gt; Point:\n    \"\"\"Return the arrowhead base anchor point of the single-headed arc.\"\"\"\n    arrowhead_length = self.arrowhead_length()\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return self.arrowhead_tip() - (arrowhead_length, 0)\n    fraction = 1 - (arrowhead_length + self.end_shorten) / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownTriggeringLayout.arrowhead_bbox","title":"arrowhead_bbox","text":"<pre><code>arrowhead_bbox() -&gt; Bbox\n</code></pre> <p>Return the bounding box of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_bbox(self) -&gt; Bbox:\n    \"\"\"Return the bounding box of the single-headed arc arrowhead.\"\"\"\n    return get_drawing_elements_bbox(self.arrowhead_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownTriggeringLayout.arrowhead_border","title":"arrowhead_border","text":"<pre><code>arrowhead_border(point: Point) -&gt; Point\n</code></pre> <p>Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_border(self, point: Point) -&gt; Point:\n    \"\"\"Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    point = get_drawing_elements_border(self.arrowhead_drawing_elements(), point)\n    if point is None:\n        return self.arrowhead_tip()\n    return point\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownTriggeringLayout.arrowhead_drawing_elements","title":"arrowhead_drawing_elements","text":"<pre><code>arrowhead_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_drawing_elements(\n    self,\n) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the single-headed arc arrowhead.\"\"\"\n    drawing_elements = self._arrowhead_border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_arrowhead\",\n        elements=tuple(drawing_elements),\n        fill=self.arrowhead_fill,\n        filter_=self.arrowhead_filter,\n        id_=f\"{self.id_}_arrowhead\",\n        stroke=self.arrowhead_stroke,\n        stroke_dasharray=self.arrowhead_stroke_dasharray,\n        stroke_dashoffset=self.arrowhead_stroke_dashoffset,\n        stroke_width=self.arrowhead_stroke_width,\n        transform=self.arrowhead_transform,\n    )\n    transformation = self._get_arrowhead_transformation()\n    group = group.transformed(transformation)\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownTriggeringLayout.arrowhead_length","title":"arrowhead_length","text":"<pre><code>arrowhead_length() -&gt; float\n</code></pre> <p>Return the length of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_length(self) -&gt; float:\n    \"\"\"Return the length of the single-headed arc arrowhead.\"\"\"\n    bbox = get_drawing_elements_bbox(self._arrowhead_border_drawing_elements())\n    if math.isnan(bbox.width):\n        return 0.0\n    return bbox.east().x\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownTriggeringLayout.arrowhead_tip","title":"arrowhead_tip","text":"<pre><code>arrowhead_tip() -&gt; Point\n</code></pre> <p>Return the arrowhead tip anchor point of the single-headed arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_tip(self) -&gt; Point:\n    \"\"\"Return the arrowhead tip anchor point of the single-headed arc.\"\"\"\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return segment.p2\n    fraction = 1 - self.end_shorten / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownTriggeringLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownTriggeringLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the arc with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the arc with no children.\"\"\"\n    return dataclasses.replace(self, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownTriggeringLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownTriggeringLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownTriggeringLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownTriggeringLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownTriggeringLayout.end_point","title":"end_point","text":"<pre><code>end_point() -&gt; Point\n</code></pre> <p>Return the ending point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_point(self) -&gt; Point:\n    \"\"\"Return the ending point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[-1]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownTriggeringLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownTriggeringLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownTriggeringLayout.fraction","title":"fraction","text":"<pre><code>fraction(fraction: float) -&gt; tuple[Point, float]\n</code></pre> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def fraction(self, fraction: float) -&gt; tuple[Point, float]:\n    \"\"\"Return the position and angle on the arc at a given fraction (of the total arc length).\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    current_length = 0\n    length_to_reach = fraction * self.length()\n    for segment in self.segments:\n        current_length += segment.length()\n        if current_length &gt;= length_to_reach:\n            break\n    segment_start_length = current_length - segment.length()\n    segment_fraction = (length_to_reach - segment_start_length) / segment.length()\n    position, angle = segment.get_position_and_angle_at_fraction(segment_fraction)\n    return position, angle\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownTriggeringLayout.length","title":"length","text":"<pre><code>length() -&gt; float\n</code></pre> <p>Return the total length of the arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def length(self) -&gt; float:\n    \"\"\"Return the total length of the arc path.\"\"\"\n    return sum([segment.length() for segment in self.segments])\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownTriggeringLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownTriggeringLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the arc.\"\"\"\n    return []\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownTriggeringLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the arc's own drawing elements, including those of its mixins.</p> Source code in <code>src/momapy/celldesigner/elements.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the arc's own drawing elements, including those of its mixins.\"\"\"\n    drawing_elements = super_or_builder(\n        CellDesignerSingleHeadedArc, self\n    ).own_drawing_elements()\n    done_bases = []\n    for base in type(self).__mro__:\n        if (\n            issubclass_or_builder(base, _SBGNMixin)\n            and base is not type(self)\n            and not any([issubclass(done_base, base) for done_base in done_bases])\n        ):\n            drawing_elements += getattr(base, \"_mixin_drawing_elements\")(self)\n            done_bases.append(base)\n    return drawing_elements\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownTriggeringLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownTriggeringLayout.path_drawing_elements","title":"path_drawing_elements","text":"<pre><code>path_drawing_elements() -&gt; list[Path]\n</code></pre> <p>Return the drawing elements of the single-headed arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def path_drawing_elements(self) -&gt; list[Path]:\n    \"\"\"Return the drawing elements of the single-headed arc path.\"\"\"\n    arrowhead_length = self.arrowhead_length()\n    if len(self.segments) == 1:\n        segment = (\n            self.segments[0]\n            .shortened(self.start_shorten, \"start\")\n            .shortened(self.end_shorten + arrowhead_length, \"end\")\n        )\n        actions = [\n            MoveTo(segment.p1),\n            self._make_path_action_from_segment(segment),\n        ]\n    else:\n        first_segment = self.segments[0].shortened(self.start_shorten, \"start\")\n        last_segment = self.segments[-1].shortened(\n            self.end_shorten + arrowhead_length, \"end\"\n        )\n        actions = [\n            MoveTo(first_segment.p1),\n            self._make_path_action_from_segment(first_segment),\n        ]\n        for segment in self.segments[1:-1]:\n            action = self._make_path_action_from_segment(segment)\n            actions.append(action)\n        actions.append(self._make_path_action_from_segment(last_segment))\n    path = Path(\n        actions=tuple(actions),\n        class_=f\"{type(self).__name__}_path\",\n        fill=self.path_fill,\n        filter_=self.path_filter,\n        id_=f\"{self.id_}_path\",\n        stroke=self.path_stroke,\n        stroke_dasharray=self.path_stroke_dasharray,\n        stroke_dashoffset=self.path_stroke_dashoffset,\n        stroke_width=self.path_stroke_width,\n        transform=self.path_transform,\n    )\n    return [path]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownTriggeringLayout.points","title":"points","text":"<pre><code>points() -&gt; list[Point]\n</code></pre> <p>Return the points of the arc path.</p> <p>An arc with no segments has no points and returns an empty list.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def points(self) -&gt; list[Point]:\n    \"\"\"Return the points of the arc path.\n\n    An arc with no segments has no points and returns an empty list.\n    \"\"\"\n    if not self.segments:\n        return []\n    points = []\n    for segment in self.segments:\n        points.append(segment.p1)\n    points.append(segment.p2)\n    return points\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownTriggeringLayout.start_point","title":"start_point","text":"<pre><code>start_point() -&gt; Point\n</code></pre> <p>Return the starting point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_point(self) -&gt; Point:\n    \"\"\"Return the starting point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[0]\n</code></pre>"},{"location":"api_reference/celldesigner/celldesigner/#momapy.celldesigner.UnknownTriggeringLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/celldesigner/io/celldesigner/","title":"ID assignment","text":"<p>When reading a CellDesigner file, ID assignment is more complex than SBGN-ML: model and layout often use different XML ID sources (e.g., <code>&lt;species id&gt;</code> for the model vs. <code>&lt;speciesAlias id&gt;</code> for the layout). Some elements have no XML ID and rely on SBML <code>metaid</code>, composite IDs, or auto-generated UUIDs.</p> <p>ID provenance key:</p> <ul> <li><code>sbml:X</code> \u2014 from an SBML element attribute (e.g., <code>&lt;species id=\"s1\"&gt;</code>, <code>&lt;modifierSpeciesReference metaid=\"MSR0\"&gt;</code>)</li> <li><code>cd:X</code> \u2014 from a CellDesigner extension element attribute (e.g., <code>&lt;speciesAlias id=\"sa1\"&gt;</code>)</li> <li><code>composite</code> \u2014 synthetic ID built from parent + child XML IDs</li> <li><code>auto</code> \u2014 auto-generated UUID from the builder</li> </ul> Element type Model <code>id_</code> Source Layout <code>id_</code> Source Registered Notes Compartment <code>compartment_id</code> sbml:<code>compartment/@id</code> <code>alias_id</code> cd:<code>compartmentAlias/@id</code> yes <code>metaid</code> also stored Species Template <code>template_id</code> cd:<code>proteinReference/@id</code> etc. no layout \u2014 yes Species <code>species_id</code> or <code>f\"{species_id}_active\"</code> sbml:<code>species/@id</code> <code>alias_id</code> cd:<code>speciesAlias/@id</code> yes Dual model registration; active species get <code>_active</code> suffix ModificationResidue <code>f\"{template_id}_{residue_id}\"</code> composite no layout \u2014 yes Global uniqueness via parent Region <code>f\"{template_id}_{region_id}\"</code> composite no layout \u2014 yes Global uniqueness via parent Modification <code>f\"{species_id}_{residue_id}\"</code> composite <code>f\"{species_id}_{residue_id}_layout\"</code> composite no Deterministic from species + residue StructuralState <code>f\"{species_id}_{value}\"</code> composite <code>f\"{species_id}_{value}_layout\"</code> composite no Deterministic from species + value Reactant (base/link) <code>metaid</code> or <code>f\"{reaction_id}_{species_id}\"</code> sbml:<code>speciesReference/@metaid</code> or composite <code>f\"{model_id}_layout\"</code> derived from model id yes <code>metaid</code> preferred; layout only for link reactants and some base Product (base/link) <code>metaid</code> or <code>f\"{reaction_id}_{species_id}\"</code> sbml:<code>speciesReference/@metaid</code> or composite <code>f\"{model_id}_layout\"</code> derived from model id yes <code>metaid</code> preferred; layout only for link products and some base Modulator <code>metaid</code> sbml:<code>modifierSpeciesReference/@metaid</code> <code>f\"{metaid}_layout\"</code> derived from metaid yes <code>metaid</code> always present BooleanGate <code>f\"{reaction_id}_gate_{sorted_aliases}\"</code> composite <code>f\"{...}_layout\"</code> composite model only Deterministic from reaction + sorted aliases BooleanGateInput / LogicArcLayout <code>f\"{gate_id}_input_{input_alias}\"</code> composite <code>f\"{gate_id}_arc_{input_alias}\"</code> composite no Frozen child of gate Reaction <code>reaction_id</code> sbml:<code>reaction/@id</code> <code>f\"{reaction_id}_layout\"</code> derived from sbml yes No reaction alias in CellDesigner Modulation <code>reaction_id</code> sbml:<code>reaction/@id</code> <code>f\"{reaction_id}_layout\"</code> derived from sbml yes Encoded as fake reactions <p>Notes:</p> <ul> <li>Dual registration: Species model is registered in <code>xml_id_to_model_element</code> under both <code>species_id</code> and <code>species_alias_id</code> (CellDesigner reactions reference species by alias ID for cross-reference resolution).</li> <li><code>_layout</code> suffix: Used when model and layout share the same XML ID source (reactions, modulations, modulators with <code>metaid</code>). No suffix is needed when they come from different XML elements (species vs. alias, compartment vs. alias).</li> <li><code>metaid</code>: The SBML <code>metaid</code> attribute is preferred for Reactants, Products, and Modulators. Falls back to a composite ID or UUID when absent.</li> <li>Composite IDs: <code>f\"{parent_id}_{child_id}\"</code> for ModificationResidue and Region, since child IDs are only unique within a parent template.</li> </ul>"},{"location":"api_reference/celldesigner/io/celldesigner/#api","title":"API","text":""},{"location":"api_reference/celldesigner/io/celldesigner/#momapy.celldesigner.io.celldesigner","title":"momapy.celldesigner.io.celldesigner","text":"<p>Classes for reading and writing CellDesigner files.</p> <p>Modules:</p> Name Description <code>reader</code> <p>CellDesigner reader class.</p> <code>writer</code> <p>CellDesigner XML writer class.</p> <p>Classes:</p> Name Description <code>CellDesignerReader</code> <p>Class for CellDesigner reader objects.</p> <code>CellDesignerWriter</code> <p>CellDesigner XML writer (model-first approach).</p>"},{"location":"api_reference/celldesigner/io/celldesigner/#momapy.celldesigner.io.celldesigner.CellDesignerReader","title":"CellDesignerReader","text":"<p>               Bases: <code>Reader</code></p> <p>Class for CellDesigner reader objects.</p> <p>Methods:</p> Name Description <code>check_file</code> <p>Return <code>True</code> if the file is a CellDesigner file.</p> <code>read</code> <p>Read a CellDesigner file and return a reader result object.</p>"},{"location":"api_reference/celldesigner/io/celldesigner/#momapy.celldesigner.io.celldesigner.CellDesignerReader.check_file","title":"check_file  <code>classmethod</code>","text":"<pre><code>check_file(file_path: str | PathLike) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the file is a CellDesigner file.</p> <p>Parameters:</p> Name Type Description Default <code>file_path</code> <code>str | PathLike</code> <p>Path of the file to check.</p> required <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> if the file references the CellDesigner namespace, <code>False</code></p> <code>bool</code> <p>otherwise.</p> Source code in <code>src/momapy/celldesigner/io/celldesigner/reader.py</code> <pre><code>@classmethod\ndef check_file(cls, file_path: str | os.PathLike) -&gt; bool:\n    \"\"\"Return `True` if the file is a CellDesigner file.\n\n    Args:\n        file_path: Path of the file to check.\n\n    Returns:\n        `True` if the file references the CellDesigner namespace, `False`\n        otherwise.\n    \"\"\"\n    try:\n        with open(file_path, \"rb\") as f:\n            for line in f:\n                if b\"http://www.sbml.org/2001/ns/celldesigner\" in line:\n                    return True\n        return False\n    except Exception:\n        return False\n</code></pre>"},{"location":"api_reference/celldesigner/io/celldesigner/#momapy.celldesigner.io.celldesigner.CellDesignerReader.read","title":"read  <code>classmethod</code>","text":"<pre><code>read(file_path: str | PathLike, return_type: Literal['map', 'model', 'layout'] = 'map', with_model: bool = True, with_layout: bool = True, with_annotations: bool = True, with_notes: bool = True, **options: Any) -&gt; ReaderResult\n</code></pre> <p>Read a CellDesigner file and return a reader result object.</p> <p>Parameters:</p> Name Type Description Default <code>file_path</code> <code>str | PathLike</code> <p>Path of the CellDesigner file to read.</p> required <code>return_type</code> <code>Literal['map', 'model', 'layout']</code> <p>Shape of <code>result.obj</code>: <code>\"map\"</code> (default) returns a CellDesigner map, <code>\"model\"</code> returns the bare model, <code>\"layout\"</code> returns the bare layout.</p> <code>'map'</code> <code>with_model</code> <code>bool</code> <p>Whether to build the model. When <code>False</code> and <code>return_type=\"map\"</code>, the map's <code>model</code> is <code>None</code>. Defaults to <code>True</code>.</p> <code>True</code> <code>with_layout</code> <code>bool</code> <p>Whether to build the layout. When <code>False</code> and <code>return_type=\"map\"</code>, the map's <code>layout</code> is <code>None</code>. Defaults to <code>True</code>.</p> <code>True</code> <code>with_annotations</code> <code>bool</code> <p>Whether to read annotations. Defaults to <code>True</code>.</p> <code>True</code> <code>with_notes</code> <code>bool</code> <p>Whether to read notes. Defaults to <code>True</code>.</p> <code>True</code> <code>options</code> <code>Any</code> <p>Additional reader-specific options (ignored).</p> <code>{}</code> <p>Returns:</p> Type Description <code>ReaderResult</code> <p>A <code>ReaderResult</code> whose <code>obj</code> is a map, model, or layout depending</p> <code>ReaderResult</code> <p>on <code>return_type</code>.</p> Source code in <code>src/momapy/celldesigner/io/celldesigner/reader.py</code> <pre><code>@classmethod\ndef read(\n    cls,\n    file_path: str | os.PathLike,\n    return_type: typing.Literal[\"map\", \"model\", \"layout\"] = \"map\",\n    with_model: bool = True,\n    with_layout: bool = True,\n    with_annotations: bool = True,\n    with_notes: bool = True,\n    **options: typing.Any,\n) -&gt; ReaderResult:\n    \"\"\"Read a CellDesigner file and return a reader result object.\n\n    Args:\n        file_path: Path of the CellDesigner file to read.\n        return_type: Shape of `result.obj`: `\"map\"` (default) returns a\n            CellDesigner map, `\"model\"` returns the bare model, `\"layout\"`\n            returns the bare layout.\n        with_model: Whether to build the model. When `False` and\n            `return_type=\"map\"`, the map's `model` is `None`. Defaults to\n            `True`.\n        with_layout: Whether to build the layout. When `False` and\n            `return_type=\"map\"`, the map's `layout` is `None`. Defaults to\n            `True`.\n        with_annotations: Whether to read annotations. Defaults to `True`.\n        with_notes: Whether to read notes. Defaults to `True`.\n        options: Additional reader-specific options (ignored).\n\n    Returns:\n        A `ReaderResult` whose `obj` is a map, model, or layout depending\n        on `return_type`.\n    \"\"\"\n    check_file_exists(file_path)\n    cd_document = lxml.objectify.parse(file_path)\n    cd_sbml = cd_document.getroot()\n    (\n        obj,\n        annotations,\n        notes,\n        id_to_element,\n        source_id_to_model_element,\n        source_id_to_layout_element,\n        source_id_to_annotations,\n        source_id_to_notes,\n    ) = cls._make_main_obj(\n        cd_model=cd_sbml.model,\n        return_type=return_type,\n        with_model=with_model,\n        with_layout=with_layout,\n        with_annotations=with_annotations,\n        with_notes=with_notes,\n    )\n    result = ReaderResult(\n        obj=obj,\n        element_to_notes=notes,\n        element_to_annotations=annotations,\n        id_to_element=id_to_element,\n        source_id_to_model_element=source_id_to_model_element,\n        source_id_to_layout_element=source_id_to_layout_element,\n        source_id_to_annotations=source_id_to_annotations,\n        source_id_to_notes=source_id_to_notes,\n        file_path=file_path,\n    )\n    return result\n</code></pre>"},{"location":"api_reference/celldesigner/io/celldesigner/#momapy.celldesigner.io.celldesigner.CellDesignerWriter","title":"CellDesignerWriter","text":"<p>               Bases: <code>Writer</code></p> <p>CellDesigner XML writer (model-first approach).</p> <p>Round-trip caveat. CellDesigner encodes link geometry as anchor ids, edit points, angles, and line directions, whereas momapy stores resolved coordinates. On write this encoding is re-derived from those coordinates (see <code>compute_cd_angle</code> and the anchor/edit-point helpers), so a read/write round-trip produces equivalent link geometry rather than the exact encoding found in the source file.</p> <p>Methods:</p> Name Description <code>write</code> <p>Write a CellDesigner map to a file.</p>"},{"location":"api_reference/celldesigner/io/celldesigner/#momapy.celldesigner.io.celldesigner.CellDesignerWriter.write","title":"write  <code>classmethod</code>","text":"<pre><code>write(obj: CellDesignerMap, file_path: str | PathLike, element_to_annotations: dict | None = None, element_to_notes: dict | None = None, source_id_to_model_element: dict | None = None, source_id_to_layout_element: dict | None = None, source_id_to_annotations: dict | None = None, source_id_to_notes: dict | None = None, with_annotations: bool = True, with_notes: bool = True, **options: Any) -&gt; WriterResult\n</code></pre> <p>Write a CellDesigner map to a file.</p> <p>Parameters:</p> Name Type Description Default <code>obj</code> <code>CellDesignerMap</code> <p>A CellDesignerMap instance.</p> required <code>file_path</code> <code>str | PathLike</code> <p>Output file path.</p> required <code>element_to_annotations</code> <code>dict | None</code> <p>Annotations dict from reader result.</p> <code>None</code> <code>element_to_notes</code> <code>dict | None</code> <p>Notes dict from reader result.</p> <code>None</code> <code>source_id_to_model_element</code> <code>dict | None</code> <p>Optional source ID to model element mapping from ReaderResult.</p> <code>None</code> <code>source_id_to_layout_element</code> <code>dict | None</code> <p>Optional source ID to layout element mapping from ReaderResult.</p> <code>None</code> <code>source_id_to_annotations</code> <code>dict | None</code> <p>Optional per-source-id annotations from ReaderResult.</p> <code>None</code> <code>source_id_to_notes</code> <code>dict | None</code> <p>Optional per-source-id notes from ReaderResult.</p> <code>None</code> <code>with_annotations</code> <code>bool</code> <p>Whether to write annotations.</p> <code>True</code> <code>with_notes</code> <code>bool</code> <p>Whether to write notes.</p> <code>True</code> <code>options</code> <code>Any</code> <p>Additional options (accepted and ignored).</p> <code>{}</code> <p>Returns:</p> Type Description <code>WriterResult</code> <p>WriterResult.</p> Source code in <code>src/momapy/celldesigner/io/celldesigner/writer.py</code> <pre><code>@classmethod\ndef write(\n    cls,\n    obj: CellDesignerMap,\n    file_path: str | os.PathLike,\n    element_to_annotations: dict | None = None,\n    element_to_notes: dict | None = None,\n    source_id_to_model_element: dict | None = None,\n    source_id_to_layout_element: dict | None = None,\n    source_id_to_annotations: dict | None = None,\n    source_id_to_notes: dict | None = None,\n    with_annotations: bool = True,\n    with_notes: bool = True,\n    **options: typing.Any,\n) -&gt; WriterResult:\n    \"\"\"Write a CellDesigner map to a file.\n\n    Args:\n        obj: A CellDesignerMap instance.\n        file_path: Output file path.\n        element_to_annotations: Annotations dict from reader result.\n        element_to_notes: Notes dict from reader result.\n        source_id_to_model_element: Optional source ID to model\n            element mapping from ReaderResult.\n        source_id_to_layout_element: Optional source ID to layout\n            element mapping from ReaderResult.\n        source_id_to_annotations: Optional per-source-id annotations\n            from ReaderResult.\n        source_id_to_notes: Optional per-source-id notes from\n            ReaderResult.\n        with_annotations: Whether to write annotations.\n        with_notes: Whether to write notes.\n        options: Additional options (accepted and ignored).\n\n    Returns:\n        WriterResult.\n    \"\"\"\n    if obj.model is None or obj.layout is None:\n        raise ValueError(\"cannot write a map without both a model and a layout\")\n    check_parent_dir_exists(file_path)\n    if element_to_annotations is None:\n        element_to_annotations = {}\n    if element_to_notes is None:\n        element_to_notes = {}\n\n    subunit_to_complex = build_subunit_to_complex(obj)\n\n    writing_context = CellDesignerWritingContext(\n        map_=obj,\n        element_to_annotations=element_to_annotations,\n        element_to_notes=element_to_notes,\n        source_id_to_model_element=source_id_to_model_element,\n        source_id_to_layout_element=source_id_to_layout_element,\n        source_id_to_annotations=source_id_to_annotations,\n        source_id_to_notes=source_id_to_notes,\n        with_annotations=with_annotations,\n        with_notes=with_notes,\n        subunit_to_complex=subunit_to_complex,\n    )\n    # Phase 1: reserve all grammar-valid source ids before any\n    # emission, so a from-scratch id can never steal a source id's\n    # name and round-tripped ids are preserved verbatim.\n    reserve_source_xml_ids(writing_context)\n    writing_context.degraded_entries = collect_degraded_entries(writing_context)\n\n    sbml = make_sbml_document(writing_context)\n    tree = lxml.etree.ElementTree(sbml)\n    tree.write(\n        file_path,\n        pretty_print=True,\n        xml_declaration=True,\n        encoding=\"UTF-8\",\n    )\n    return WriterResult(obj=obj, file_path=file_path)\n</code></pre>"},{"location":"api_reference/rendering/cairo/","title":"Cairo","text":""},{"location":"api_reference/rendering/cairo/#momapy.rendering.cairo","title":"momapy.rendering.cairo","text":"<p>Class for rendering with Cairo.</p> Note <p>SVG filter effects (e.g. drop shadow, Gaussian blur) are not supported by this backend. Cairo has no native filter primitives, so any <code>filter</code> attribute on drawing elements is ignored. Use the skia or svg-native backend if you need filter effects.</p> <p>Classes:</p> Name Description <code>CairoRenderer</code> <p>Renderer implementation using the Cairo graphics library.</p>"},{"location":"api_reference/rendering/cairo/#momapy.rendering.cairo.CairoRenderer","title":"CairoRenderer  <code>dataclass</code>","text":"<pre><code>CairoRenderer(_current_state: dict[str, Any] = dict(), _states: list[dict[str, Any]] = list(), *, context: Context, _config: dict[str, Any] = dict(), _pango_font_descriptions: dict[tuple, Any] = dict())\n</code></pre> <p>               Bases: <code>StatefulRenderer</code>, <code>SupportsFileOutput</code></p> <p>Renderer implementation using the Cairo graphics library.</p> <p>This renderer supports multiple output formats including PDF, SVG, PNG, and PostScript. It uses Pango for text rendering.</p> <p>SVG filter effects (drop shadow, Gaussian blur, etc.) are not supported: Cairo has no native filter primitives, so the <code>filter</code> attribute on drawing elements is ignored.</p> <p>Attributes:</p> Name Type Description <code>context</code> <code>Context</code> <p>The Cairo context used for rendering</p> <p>Examples:</p> <pre><code>from momapy.meta.nodes import Rectangle\nfrom momapy.geometry import Point\n\n# Create a layout element to render\nnode = Rectangle(\n    position=Point(100.0, 100.0),\n    width=200.0,\n    height=100.0\n)\n\n# Create renderer and render the element\nrenderer = CairoRenderer.from_file(\"output.pdf\", 800, 600, \"pdf\")\nrenderer.begin_session()\nrenderer.render_layout_element(node)\nrenderer.end_session()\n</code></pre> <p>Methods:</p> Name Description <code>__post_init__</code> <p>Initialize the renderer's current state after initialization.</p> <code>begin_session</code> <p>Begin a rendering session.</p> <code>end_session</code> <p>End the rendering session and save the output.</p> <code>from_file</code> <p>Create a CairoRenderer instance from a file path.</p> <code>get_bolder_font_weight</code> <p>Return the lightest font weight bolder than the given font weight.</p> <code>get_current_state</code> <p>Return the current state.</p> <code>get_current_value</code> <p>Return the current value for an attribute.</p> <code>get_initial_value</code> <p>Return the initial value for an attribute.</p> <code>get_lighter_font_weight</code> <p>Return the boldest font weight lighter than the given font weight.</p> <code>new_page</code> <p>Create a new page in the output document.</p> <code>render_drawing_element</code> <p>Render a drawing element to the output.</p> <code>render_layout_element</code> <p>Render a layout element to the output.</p> <code>render_map</code> <p>Render a map.</p> <code>restore</code> <p>Set the current state to the last saved state.</p> <code>save</code> <p>Save the current state.</p> <code>self_restore</code> <p>Restore the Cairo context state.</p> <code>self_save</code> <p>Save the Cairo context state.</p> <code>set_current_state</code> <p>Set the current state to the given state.</p> <code>set_current_state_from_drawing_element</code> <p>Set the current state to a state given by a drawing element.</p> <code>set_current_value</code> <p>Set the current value for an attribute.</p>"},{"location":"api_reference/rendering/cairo/#momapy.rendering.cairo.CairoRenderer.__post_init__","title":"__post_init__","text":"<pre><code>__post_init__() -&gt; None\n</code></pre> <p>Initialize the renderer's current state after initialization.</p> Source code in <code>src/momapy/rendering/core.py</code> <pre><code>def __post_init__(self) -&gt; None:\n    \"\"\"Initialize the renderer's current state after initialization.\"\"\"\n    self._initialize_current_state()\n</code></pre>"},{"location":"api_reference/rendering/cairo/#momapy.rendering.cairo.CairoRenderer.begin_session","title":"begin_session","text":"<pre><code>begin_session() -&gt; None\n</code></pre> <p>Begin a rendering session.</p> <p>This method initializes the rendering context. For CairoRenderer, no explicit initialization is needed beyond the context setup.</p> Source code in <code>src/momapy/rendering/cairo.py</code> <pre><code>def begin_session(self) -&gt; None:\n    \"\"\"Begin a rendering session.\n\n    This method initializes the rendering context. For CairoRenderer,\n    no explicit initialization is needed beyond the context setup.\n    \"\"\"\n    pass\n</code></pre>"},{"location":"api_reference/rendering/cairo/#momapy.rendering.cairo.CairoRenderer.default_format","title":"default_format  <code>class-attribute</code>","text":"<pre><code>default_format: str | None = 'pdf'\n</code></pre> <p>The format used when <code>from_file</code> is called with <code>format_=None</code>.</p> <p>Subclasses set this to one of their :attr:<code>supported_formats</code>.</p>"},{"location":"api_reference/rendering/cairo/#momapy.rendering.cairo.CairoRenderer.end_session","title":"end_session","text":"<pre><code>end_session() -&gt; None\n</code></pre> <p>End the rendering session and save the output.</p> <p>This method finalizes the rendering and saves the output to the file. For PNG format, it writes the image data. For other formats, it finishes and flushes the surface.</p> Source code in <code>src/momapy/rendering/cairo.py</code> <pre><code>def end_session(self) -&gt; None:\n    \"\"\"End the rendering session and save the output.\n\n    This method finalizes the rendering and saves the output to the file.\n    For PNG format, it writes the image data. For other formats, it\n    finishes and flushes the surface.\n    \"\"\"\n    surface = self.context.get_target()\n    format_ = self._config.get(\"format\")\n    if format_ == \"png\":\n        surface.write_to_png(self._config[\"file_path\"])\n    surface.finish()\n    surface.flush()\n</code></pre>"},{"location":"api_reference/rendering/cairo/#momapy.rendering.cairo.CairoRenderer.from_file","title":"from_file  <code>classmethod</code>","text":"<pre><code>from_file(file_path: str | PathLike, width: float, height: float, format_: str | None = None) -&gt; Self\n</code></pre> <p>Create a CairoRenderer instance from a file path.</p> <p>Parameters:</p> Name Type Description Default <code>file_path</code> <code>str | PathLike</code> <p>The output file path</p> required <code>width</code> <code>float</code> <p>The width of the canvas</p> required <code>height</code> <code>float</code> <p>The height of the canvas</p> required <code>format_</code> <code>str | None</code> <p>The output format (pdf, svg, png, or ps). <code>None</code> selects the backend's :attr:<code>default_format</code> (\"pdf\").</p> <code>None</code> <p>Returns:</p> Type Description <code>Self</code> <p>A new CairoRenderer instance</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the format is not supported</p> <p>Examples:</p> <pre><code>renderer = CairoRenderer.from_file(\"output.pdf\", 800, 600, \"pdf\")\n</code></pre> Source code in <code>src/momapy/rendering/cairo.py</code> <pre><code>@classmethod\ndef from_file(\n    cls,\n    file_path: str | os.PathLike,\n    width: float,\n    height: float,\n    format_: str | None = None,\n) -&gt; typing_extensions.Self:\n    \"\"\"Create a CairoRenderer instance from a file path.\n\n    Args:\n        file_path: The output file path\n        width: The width of the canvas\n        height: The height of the canvas\n        format_: The output format (pdf, svg, png, or ps). ``None`` selects\n            the backend's :attr:`default_format` (\"pdf\").\n\n    Returns:\n        A new CairoRenderer instance\n\n    Raises:\n        ValueError: If the format is not supported\n\n    Examples:\n        ```python\n        renderer = CairoRenderer.from_file(\"output.pdf\", 800, 600, \"pdf\")\n        ```\n    \"\"\"\n    if format_ is None:\n        format_ = cls.default_format\n    if format_ not in cls.supported_formats:\n        raise ValueError(\n            f\"unsupported format {format_!r}: expected one of \"\n            f\"{', '.join(cls.supported_formats)}\"\n        )\n    check_parent_dir_exists(file_path)\n    config = {}\n    if format_ == \"pdf\":\n        surface = cairo.PDFSurface(file_path, width, height)\n    elif format_ == \"ps\":\n        surface = cairo.PSSurface(file_path, width, height)\n    elif format_ == \"svg\":\n        surface = cairo.SVGSurface(file_path, width, height)\n    elif format_ == \"png\":\n        surface = cairo.ImageSurface(cairo.FORMAT_ARGB32, int(width), int(height))\n    config[\"surface\"] = surface\n    config[\"file_path\"] = file_path\n    config[\"width\"] = width\n    config[\"height\"] = height\n    config[\"format\"] = format_\n    context = cairo.Context(surface)\n    return cls(context=context, _config=config)\n</code></pre>"},{"location":"api_reference/rendering/cairo/#momapy.rendering.cairo.CairoRenderer.get_bolder_font_weight","title":"get_bolder_font_weight  <code>classmethod</code>","text":"<pre><code>get_bolder_font_weight(font_weight: FontWeight | float) -&gt; float\n</code></pre> <p>Return the lightest font weight bolder than the given font weight.</p> Source code in <code>src/momapy/rendering/core.py</code> <pre><code>@classmethod\ndef get_bolder_font_weight(cls, font_weight: FontWeight | float) -&gt; float:\n    \"\"\"Return the lightest font weight bolder than the given font weight.\"\"\"\n    if isinstance(font_weight, FontWeight):\n        font_weight = cls.font_weight_value_mapping.get(font_weight)\n        if font_weight is None:\n            raise ValueError(\n                f\"font weight must be a float, {FontWeight.NORMAL}, or {FontWeight.BOLD}\"\n            )\n    if font_weight &lt; 400:\n        new_font_weight = 400.0\n    elif font_weight &lt; 600:\n        new_font_weight = 700.0\n    else:\n        new_font_weight = 900.0\n    return new_font_weight\n</code></pre>"},{"location":"api_reference/rendering/cairo/#momapy.rendering.cairo.CairoRenderer.get_current_state","title":"get_current_state","text":"<pre><code>get_current_state() -&gt; dict[str, Any]\n</code></pre> <p>Return the current state.</p> Source code in <code>src/momapy/rendering/core.py</code> <pre><code>def get_current_state(self) -&gt; dict[str, typing.Any]:\n    \"\"\"Return the current state.\"\"\"\n    return self._current_state\n</code></pre>"},{"location":"api_reference/rendering/cairo/#momapy.rendering.cairo.CairoRenderer.get_current_value","title":"get_current_value","text":"<pre><code>get_current_value(attr_name: str) -&gt; Any\n</code></pre> <p>Return the current value for an attribute.</p> Source code in <code>src/momapy/rendering/core.py</code> <pre><code>def get_current_value(self, attr_name: str) -&gt; typing.Any:\n    \"\"\"Return the current value for an attribute.\"\"\"\n    return self.get_current_state()[attr_name]\n</code></pre>"},{"location":"api_reference/rendering/cairo/#momapy.rendering.cairo.CairoRenderer.get_initial_value","title":"get_initial_value","text":"<pre><code>get_initial_value(attr_name: str) -&gt; Any\n</code></pre> <p>Return the initial value for an attribute.</p> Source code in <code>src/momapy/rendering/core.py</code> <pre><code>def get_initial_value(self, attr_name: str) -&gt; typing.Any:\n    \"\"\"Return the initial value for an attribute.\"\"\"\n    attr_value = self.initial_values.get(attr_name)\n    if attr_value is None:\n        attr_d = PRESENTATION_ATTRIBUTES[attr_name]\n        attr_value = attr_d[\"initial\"]\n        if attr_value is None:\n            attr_value = INITIAL_VALUES[attr_name]\n    return attr_value\n</code></pre>"},{"location":"api_reference/rendering/cairo/#momapy.rendering.cairo.CairoRenderer.get_lighter_font_weight","title":"get_lighter_font_weight  <code>classmethod</code>","text":"<pre><code>get_lighter_font_weight(font_weight: FontWeight | float) -&gt; float\n</code></pre> <p>Return the boldest font weight lighter than the given font weight.</p> Source code in <code>src/momapy/rendering/core.py</code> <pre><code>@classmethod\ndef get_lighter_font_weight(cls, font_weight: FontWeight | float) -&gt; float:\n    \"\"\"Return the boldest font weight lighter than the given font weight.\"\"\"\n    if isinstance(font_weight, FontWeight):\n        font_weight = cls.font_weight_value_mapping.get(font_weight)\n        if font_weight is None:\n            raise ValueError(\n                f\"font weight must be a float, {FontWeight.NORMAL}, or {FontWeight.BOLD}\"\n            )\n    if font_weight &gt; 700:\n        new_font_weight = 700.0\n    elif font_weight &gt; 500:\n        new_font_weight = 400.0\n    else:\n        new_font_weight = 100.0\n    return new_font_weight\n</code></pre>"},{"location":"api_reference/rendering/cairo/#momapy.rendering.cairo.CairoRenderer.new_page","title":"new_page","text":"<pre><code>new_page(width: float, height: float) -&gt; None\n</code></pre> <p>Create a new page in the output document.</p> <p>Parameters:</p> Name Type Description Default <code>width</code> <code>float</code> <p>The width of the new page</p> required <code>height</code> <code>float</code> <p>The height of the new page</p> required Note <p>Only PDF and PostScript formats support multiple pages. Other formats will ignore this call.</p> Source code in <code>src/momapy/rendering/cairo.py</code> <pre><code>def new_page(self, width: float, height: float) -&gt; None:\n    \"\"\"Create a new page in the output document.\n\n    Args:\n        width: The width of the new page\n        height: The height of the new page\n\n    Note:\n        Only PDF and PostScript formats support multiple pages.\n        Other formats will ignore this call.\n    \"\"\"\n    format_ = self._config.get(\"format\")\n    if format_ == \"pdf\" or format_ == \"ps\":\n        self.context.show_page()\n        surface = self.context.get_target()\n        surface.set_size(width, height)\n</code></pre>"},{"location":"api_reference/rendering/cairo/#momapy.rendering.cairo.CairoRenderer.render_drawing_element","title":"render_drawing_element","text":"<pre><code>render_drawing_element(drawing_element: DrawingElement) -&gt; None\n</code></pre> <p>Render a drawing element to the output.</p> <p>Parameters:</p> Name Type Description Default <code>drawing_element</code> <code>DrawingElement</code> <p>The drawing element to render</p> required <p>This method handles transformations and delegates to the appropriate rendering method based on the drawing element type.</p> Source code in <code>src/momapy/rendering/cairo.py</code> <pre><code>def render_drawing_element(self, drawing_element: DrawingElement) -&gt; None:\n    \"\"\"Render a drawing element to the output.\n\n    Args:\n        drawing_element: The drawing element to render\n\n    This method handles transformations and delegates to\n    the appropriate rendering method based on the drawing element type.\n    \"\"\"\n    self.save()\n    self.set_current_state_from_drawing_element(drawing_element)\n    self._add_transform_from_drawing_element(drawing_element)\n    class_ = type(drawing_element)\n    if issubclass(class_, Builder):\n        class_ = class_._cls_to_build\n    de_func = getattr(self, self._de_class_func_mapping[class_])\n    de_func(drawing_element)\n    self.restore()\n</code></pre>"},{"location":"api_reference/rendering/cairo/#momapy.rendering.cairo.CairoRenderer.render_layout_element","title":"render_layout_element","text":"<pre><code>render_layout_element(layout_element: LayoutElement) -&gt; None\n</code></pre> <p>Render a layout element to the output.</p> <p>Parameters:</p> Name Type Description Default <code>layout_element</code> <code>LayoutElement</code> <p>The layout element to render</p> required Source code in <code>src/momapy/rendering/cairo.py</code> <pre><code>def render_layout_element(self, layout_element: LayoutElement) -&gt; None:\n    \"\"\"Render a layout element to the output.\n\n    Args:\n        layout_element: The layout element to render\n    \"\"\"\n    drawing_elements = layout_element.drawing_elements()\n    for drawing_element in drawing_elements:\n        self.render_drawing_element(drawing_element)\n</code></pre>"},{"location":"api_reference/rendering/cairo/#momapy.rendering.cairo.CairoRenderer.render_map","title":"render_map","text":"<pre><code>render_map(map_: Map) -&gt; None\n</code></pre> <p>Render a map.</p> <p>This is a convenience method, not part of the abstract contract: the default implementation renders the map's layout via :meth:<code>render_layout_element</code>, which is what every built-in backend needs. Subclasses may override it if a backend requires map-specific handling, but they are not obliged to. The file pipeline (:func:<code>render_map</code>/:func:<code>render_maps</code>) does not call this method; it renders each page through :meth:<code>render_layout_element</code>.</p> <p>Parameters:</p> Name Type Description Default <code>map_</code> <code>Map</code> <p>The map to render.</p> required <p>Raises:</p> Type Description <code>ValueError</code> <p>If the map has no layout to render.</p> Source code in <code>src/momapy/rendering/core.py</code> <pre><code>def render_map(self, map_: Map) -&gt; None:\n    \"\"\"Render a map.\n\n    This is a convenience method, **not** part of the abstract\n    contract: the default implementation renders the map's layout via\n    :meth:`render_layout_element`, which is what every built-in backend\n    needs. Subclasses may override it if a backend requires\n    map-specific handling, but they are not obliged to. The file\n    pipeline (:func:`render_map`/:func:`render_maps`) does not call this\n    method; it renders each page through :meth:`render_layout_element`.\n\n    Args:\n        map_: The map to render.\n\n    Raises:\n        ValueError: If the map has no layout to render.\n    \"\"\"\n    if map_.layout is None:\n        raise ValueError(\n            \"map has no layout to render (its layout is None); \"\n            \"a layout-less map (e.g. an SBML map) cannot be rendered\"\n        )\n    self.render_layout_element(map_.layout)\n</code></pre>"},{"location":"api_reference/rendering/cairo/#momapy.rendering.cairo.CairoRenderer.restore","title":"restore","text":"<pre><code>restore() -&gt; None\n</code></pre> <p>Set the current state to the last saved state.</p> Source code in <code>src/momapy/rendering/core.py</code> <pre><code>def restore(self) -&gt; None:\n    \"\"\"Set the current state to the last saved state.\"\"\"\n    if len(self._states) &gt; 0:\n        state = self._states.pop()\n        self.set_current_state(state)\n        self.self_restore()\n    else:\n        raise RuntimeError(\n            \"restore() called with an empty state stack: no matching \"\n            \"save() to restore from\"\n        )\n</code></pre>"},{"location":"api_reference/rendering/cairo/#momapy.rendering.cairo.CairoRenderer.save","title":"save","text":"<pre><code>save() -&gt; None\n</code></pre> <p>Save the current state.</p> Source code in <code>src/momapy/rendering/core.py</code> <pre><code>def save(self) -&gt; None:\n    \"\"\"Save the current state.\"\"\"\n    self._states.append(copy.deepcopy(self.get_current_state()))\n    self.self_save()\n</code></pre>"},{"location":"api_reference/rendering/cairo/#momapy.rendering.cairo.CairoRenderer.self_restore","title":"self_restore","text":"<pre><code>self_restore() -&gt; None\n</code></pre> <p>Restore the Cairo context state.</p> <p>This method restores the Cairo context to the state saved by the most recent call to self_save().</p> Source code in <code>src/momapy/rendering/cairo.py</code> <pre><code>def self_restore(self) -&gt; None:\n    \"\"\"Restore the Cairo context state.\n\n    This method restores the Cairo context to the state saved by the\n    most recent call to self_save().\n    \"\"\"\n    self.context.restore()\n    self.context.new_path()\n</code></pre>"},{"location":"api_reference/rendering/cairo/#momapy.rendering.cairo.CairoRenderer.self_save","title":"self_save","text":"<pre><code>self_save() -&gt; None\n</code></pre> <p>Save the Cairo context state.</p> <p>This method saves the current state of the Cairo context, including transformations, clipping regions, and drawing parameters.</p> Source code in <code>src/momapy/rendering/cairo.py</code> <pre><code>def self_save(self) -&gt; None:\n    \"\"\"Save the Cairo context state.\n\n    This method saves the current state of the Cairo context, including\n    transformations, clipping regions, and drawing parameters.\n    \"\"\"\n    self.context.save()\n</code></pre>"},{"location":"api_reference/rendering/cairo/#momapy.rendering.cairo.CairoRenderer.set_current_state","title":"set_current_state","text":"<pre><code>set_current_state(state: dict[str, Any]) -&gt; None\n</code></pre> <p>Set the current state to the given state.</p> Source code in <code>src/momapy/rendering/core.py</code> <pre><code>def set_current_state(self, state: dict[str, typing.Any]) -&gt; None:\n    \"\"\"Set the current state to the given state.\"\"\"\n    for attr_name, attr_value in state.items():\n        self.set_current_value(attr_name, attr_value)\n</code></pre>"},{"location":"api_reference/rendering/cairo/#momapy.rendering.cairo.CairoRenderer.set_current_state_from_drawing_element","title":"set_current_state_from_drawing_element","text":"<pre><code>set_current_state_from_drawing_element(drawing_element: DrawingElement) -&gt; None\n</code></pre> <p>Set the current state to a state given by a drawing element.</p> Source code in <code>src/momapy/rendering/core.py</code> <pre><code>def set_current_state_from_drawing_element(\n    self, drawing_element: DrawingElement\n) -&gt; None:\n    \"\"\"Set the current state to a state given by a drawing element.\"\"\"\n    state = self._get_state_from_drawing_element(drawing_element)\n    self.set_current_state(state)\n</code></pre>"},{"location":"api_reference/rendering/cairo/#momapy.rendering.cairo.CairoRenderer.set_current_value","title":"set_current_value","text":"<pre><code>set_current_value(attr_name: str, attr_value: Any) -&gt; None\n</code></pre> <p>Set the current value for an attribute.</p> Source code in <code>src/momapy/rendering/core.py</code> <pre><code>def set_current_value(self, attr_name: str, attr_value: typing.Any) -&gt; None:\n    \"\"\"Set the current value for an attribute.\"\"\"\n    if attr_value is None:\n        attr_d = PRESENTATION_ATTRIBUTES[attr_name]\n        if not attr_d[\"inherited\"]:\n            attr_value = self.initial_values.get(attr_name)\n            if attr_value is None:\n                attr_value = attr_d[\"initial\"]\n            if attr_value is None:\n                attr_value = INITIAL_VALUES[attr_name]\n    if attr_name == \"font_weight\":\n        if isinstance(attr_value, FontWeight):\n            if attr_value == FontWeight.NORMAL or attr_value == FontWeight.BOLD:\n                attr_value = self.font_weight_value_mapping[attr_value]\n            elif attr_value == FontWeight.BOLDER:\n                attr_value = self.get_bolder_font_weight(\n                    self.get_current_value(\"font_weight\")\n                )\n            elif attr_value == FontWeight.LIGHTER:\n                attr_value = self.get_lighter_font_weight(\n                    self.get_current_value(\"font_weight\")\n                )\n    if attr_value is not None:\n        self._current_state[attr_name] = attr_value\n</code></pre>"},{"location":"api_reference/rendering/core/","title":"Core","text":""},{"location":"api_reference/rendering/core/#momapy.rendering.core","title":"momapy.rendering.core","text":"<p>Base classes and functions for rendering maps or layout elements.</p> <p>Classes:</p> Name Description <code>Renderer</code> <p>Base class for renderers.</p> <code>StatefulRenderer</code> <p>Base class for stateful renderers.</p> <code>SupportsFileOutput</code> <p>Mixin declaring the file-output capability of a renderer.</p> <p>Functions:</p> Name Description <code>get_renderer</code> <p>Get a renderer class by name.</p> <code>list_renderers</code> <p>List all available renderer names.</p> <code>register_lazy_renderer</code> <p>Register a renderer for lazy loading.</p> <code>register_renderer</code> <p>Register a renderer class.</p> <code>render_layout_element</code> <p>Render a layout element to a file in the given format with the given registered renderer.</p> <code>render_layout_elements</code> <p>Render a collection of layout elements to a file in the given format with the given registered renderer.</p> <code>render_map</code> <p>Render a map to a file in the given format with the given registered renderer.</p> <code>render_maps</code> <p>Render a collection of maps to a file in the given format with the given registered renderer.</p>"},{"location":"api_reference/rendering/core/#momapy.rendering.core.Renderer","title":"Renderer  <code>dataclass</code>","text":"<pre><code>Renderer()\n</code></pre> <p>               Bases: <code>ABC</code></p> <p>Base class for renderers.</p> <p>The abstract contract is the five render-session methods only (<code>begin_session</code>, <code>end_session</code>, <code>new_page</code>, <code>render_layout_element</code>, <code>render_drawing_element</code>). <code>render_map</code> is a concrete convenience method (it renders <code>map_.layout</code> via <code>render_layout_element</code>), so backends need not implement it. File output is a separate capability provided by the :class:<code>SupportsFileOutput</code> mixin, not a base-class obligation: a renderer that does not target a file (in-memory surface, interactive canvas, null/test renderer) subclasses <code>Renderer</code> directly and has neither <code>from_file</code> nor <code>supported_formats</code>.</p> <p>Methods:</p> Name Description <code>begin_session</code> <p>Begin a rendering session.</p> <code>end_session</code> <p>End the current rendering session.</p> <code>get_bolder_font_weight</code> <p>Return the lightest font weight bolder than the given font weight.</p> <code>get_lighter_font_weight</code> <p>Return the boldest font weight lighter than the given font weight.</p> <code>new_page</code> <p>Start a new page.</p> <code>render_drawing_element</code> <p>Render a drawing element.</p> <code>render_layout_element</code> <p>Render a layout element.</p> <code>render_map</code> <p>Render a map.</p>"},{"location":"api_reference/rendering/core/#momapy.rendering.core.Renderer.begin_session","title":"begin_session  <code>abstractmethod</code>","text":"<pre><code>begin_session() -&gt; None\n</code></pre> <p>Begin a rendering session.</p> Source code in <code>src/momapy/rendering/core.py</code> <pre><code>@abc.abstractmethod\ndef begin_session(self) -&gt; None:\n    \"\"\"Begin a rendering session.\"\"\"\n    pass\n</code></pre>"},{"location":"api_reference/rendering/core/#momapy.rendering.core.Renderer.end_session","title":"end_session  <code>abstractmethod</code>","text":"<pre><code>end_session() -&gt; None\n</code></pre> <p>End the current rendering session.</p> Source code in <code>src/momapy/rendering/core.py</code> <pre><code>@abc.abstractmethod\ndef end_session(self) -&gt; None:\n    \"\"\"End the current rendering session.\"\"\"\n    pass\n</code></pre>"},{"location":"api_reference/rendering/core/#momapy.rendering.core.Renderer.get_bolder_font_weight","title":"get_bolder_font_weight  <code>classmethod</code>","text":"<pre><code>get_bolder_font_weight(font_weight: FontWeight | float) -&gt; float\n</code></pre> <p>Return the lightest font weight bolder than the given font weight.</p> Source code in <code>src/momapy/rendering/core.py</code> <pre><code>@classmethod\ndef get_bolder_font_weight(cls, font_weight: FontWeight | float) -&gt; float:\n    \"\"\"Return the lightest font weight bolder than the given font weight.\"\"\"\n    if isinstance(font_weight, FontWeight):\n        font_weight = cls.font_weight_value_mapping.get(font_weight)\n        if font_weight is None:\n            raise ValueError(\n                f\"font weight must be a float, {FontWeight.NORMAL}, or {FontWeight.BOLD}\"\n            )\n    if font_weight &lt; 400:\n        new_font_weight = 400.0\n    elif font_weight &lt; 600:\n        new_font_weight = 700.0\n    else:\n        new_font_weight = 900.0\n    return new_font_weight\n</code></pre>"},{"location":"api_reference/rendering/core/#momapy.rendering.core.Renderer.get_lighter_font_weight","title":"get_lighter_font_weight  <code>classmethod</code>","text":"<pre><code>get_lighter_font_weight(font_weight: FontWeight | float) -&gt; float\n</code></pre> <p>Return the boldest font weight lighter than the given font weight.</p> Source code in <code>src/momapy/rendering/core.py</code> <pre><code>@classmethod\ndef get_lighter_font_weight(cls, font_weight: FontWeight | float) -&gt; float:\n    \"\"\"Return the boldest font weight lighter than the given font weight.\"\"\"\n    if isinstance(font_weight, FontWeight):\n        font_weight = cls.font_weight_value_mapping.get(font_weight)\n        if font_weight is None:\n            raise ValueError(\n                f\"font weight must be a float, {FontWeight.NORMAL}, or {FontWeight.BOLD}\"\n            )\n    if font_weight &gt; 700:\n        new_font_weight = 700.0\n    elif font_weight &gt; 500:\n        new_font_weight = 400.0\n    else:\n        new_font_weight = 100.0\n    return new_font_weight\n</code></pre>"},{"location":"api_reference/rendering/core/#momapy.rendering.core.Renderer.new_page","title":"new_page  <code>abstractmethod</code>","text":"<pre><code>new_page(width: float, height: float) -&gt; None\n</code></pre> <p>Start a new page.</p> <p>Parameters:</p> Name Type Description Default <code>width</code> <code>float</code> <p>Width of the page.</p> required <code>height</code> <code>float</code> <p>Height of the page.</p> required Source code in <code>src/momapy/rendering/core.py</code> <pre><code>@abc.abstractmethod\ndef new_page(self, width: float, height: float) -&gt; None:\n    \"\"\"Start a new page.\n\n    Args:\n        width: Width of the page.\n        height: Height of the page.\n    \"\"\"\n    pass\n</code></pre>"},{"location":"api_reference/rendering/core/#momapy.rendering.core.Renderer.render_drawing_element","title":"render_drawing_element  <code>abstractmethod</code>","text":"<pre><code>render_drawing_element(drawing_element: DrawingElement) -&gt; None\n</code></pre> <p>Render a drawing element.</p> <p>Parameters:</p> Name Type Description Default <code>drawing_element</code> <code>DrawingElement</code> <p>The drawing element to render.</p> required Source code in <code>src/momapy/rendering/core.py</code> <pre><code>@abc.abstractmethod\ndef render_drawing_element(self, drawing_element: DrawingElement) -&gt; None:\n    \"\"\"Render a drawing element.\n\n    Args:\n        drawing_element: The drawing element to render.\n    \"\"\"\n    pass\n</code></pre>"},{"location":"api_reference/rendering/core/#momapy.rendering.core.Renderer.render_layout_element","title":"render_layout_element  <code>abstractmethod</code>","text":"<pre><code>render_layout_element(layout_element: LayoutElement) -&gt; None\n</code></pre> <p>Render a layout element.</p> <p>Parameters:</p> Name Type Description Default <code>layout_element</code> <code>LayoutElement</code> <p>The layout element to render.</p> required Source code in <code>src/momapy/rendering/core.py</code> <pre><code>@abc.abstractmethod\ndef render_layout_element(self, layout_element: LayoutElement) -&gt; None:\n    \"\"\"Render a layout element.\n\n    Args:\n        layout_element: The layout element to render.\n    \"\"\"\n    pass\n</code></pre>"},{"location":"api_reference/rendering/core/#momapy.rendering.core.Renderer.render_map","title":"render_map","text":"<pre><code>render_map(map_: Map) -&gt; None\n</code></pre> <p>Render a map.</p> <p>This is a convenience method, not part of the abstract contract: the default implementation renders the map's layout via :meth:<code>render_layout_element</code>, which is what every built-in backend needs. Subclasses may override it if a backend requires map-specific handling, but they are not obliged to. The file pipeline (:func:<code>render_map</code>/:func:<code>render_maps</code>) does not call this method; it renders each page through :meth:<code>render_layout_element</code>.</p> <p>Parameters:</p> Name Type Description Default <code>map_</code> <code>Map</code> <p>The map to render.</p> required <p>Raises:</p> Type Description <code>ValueError</code> <p>If the map has no layout to render.</p> Source code in <code>src/momapy/rendering/core.py</code> <pre><code>def render_map(self, map_: Map) -&gt; None:\n    \"\"\"Render a map.\n\n    This is a convenience method, **not** part of the abstract\n    contract: the default implementation renders the map's layout via\n    :meth:`render_layout_element`, which is what every built-in backend\n    needs. Subclasses may override it if a backend requires\n    map-specific handling, but they are not obliged to. The file\n    pipeline (:func:`render_map`/:func:`render_maps`) does not call this\n    method; it renders each page through :meth:`render_layout_element`.\n\n    Args:\n        map_: The map to render.\n\n    Raises:\n        ValueError: If the map has no layout to render.\n    \"\"\"\n    if map_.layout is None:\n        raise ValueError(\n            \"map has no layout to render (its layout is None); \"\n            \"a layout-less map (e.g. an SBML map) cannot be rendered\"\n        )\n    self.render_layout_element(map_.layout)\n</code></pre>"},{"location":"api_reference/rendering/core/#momapy.rendering.core.StatefulRenderer","title":"StatefulRenderer  <code>dataclass</code>","text":"<pre><code>StatefulRenderer(_current_state: dict[str, Any] = dict(), _states: list[dict[str, Any]] = list())\n</code></pre> <p>               Bases: <code>Renderer</code></p> <p>Base class for stateful renderers.</p> <p>Methods:</p> Name Description <code>__post_init__</code> <p>Initialize the renderer's current state after initialization.</p> <code>begin_session</code> <p>Begin a rendering session.</p> <code>end_session</code> <p>End the current rendering session.</p> <code>get_bolder_font_weight</code> <p>Return the lightest font weight bolder than the given font weight.</p> <code>get_current_state</code> <p>Return the current state.</p> <code>get_current_value</code> <p>Return the current value for an attribute.</p> <code>get_initial_value</code> <p>Return the initial value for an attribute.</p> <code>get_lighter_font_weight</code> <p>Return the boldest font weight lighter than the given font weight.</p> <code>new_page</code> <p>Start a new page.</p> <code>render_drawing_element</code> <p>Render a drawing element.</p> <code>render_layout_element</code> <p>Render a layout element.</p> <code>render_map</code> <p>Render a map.</p> <code>restore</code> <p>Set the current state to the last saved state.</p> <code>save</code> <p>Save the current state.</p> <code>self_restore</code> <p>Restore the internal state of the renderer.</p> <code>self_save</code> <p>Save the internal state of the renderer.</p> <code>set_current_state</code> <p>Set the current state to the given state.</p> <code>set_current_state_from_drawing_element</code> <p>Set the current state to a state given by a drawing element.</p> <code>set_current_value</code> <p>Set the current value for an attribute.</p>"},{"location":"api_reference/rendering/core/#momapy.rendering.core.StatefulRenderer.__post_init__","title":"__post_init__","text":"<pre><code>__post_init__() -&gt; None\n</code></pre> <p>Initialize the renderer's current state after initialization.</p> Source code in <code>src/momapy/rendering/core.py</code> <pre><code>def __post_init__(self) -&gt; None:\n    \"\"\"Initialize the renderer's current state after initialization.\"\"\"\n    self._initialize_current_state()\n</code></pre>"},{"location":"api_reference/rendering/core/#momapy.rendering.core.StatefulRenderer.begin_session","title":"begin_session  <code>abstractmethod</code>","text":"<pre><code>begin_session() -&gt; None\n</code></pre> <p>Begin a rendering session.</p> Source code in <code>src/momapy/rendering/core.py</code> <pre><code>@abc.abstractmethod\ndef begin_session(self) -&gt; None:\n    \"\"\"Begin a rendering session.\"\"\"\n    pass\n</code></pre>"},{"location":"api_reference/rendering/core/#momapy.rendering.core.StatefulRenderer.end_session","title":"end_session  <code>abstractmethod</code>","text":"<pre><code>end_session() -&gt; None\n</code></pre> <p>End the current rendering session.</p> Source code in <code>src/momapy/rendering/core.py</code> <pre><code>@abc.abstractmethod\ndef end_session(self) -&gt; None:\n    \"\"\"End the current rendering session.\"\"\"\n    pass\n</code></pre>"},{"location":"api_reference/rendering/core/#momapy.rendering.core.StatefulRenderer.get_bolder_font_weight","title":"get_bolder_font_weight  <code>classmethod</code>","text":"<pre><code>get_bolder_font_weight(font_weight: FontWeight | float) -&gt; float\n</code></pre> <p>Return the lightest font weight bolder than the given font weight.</p> Source code in <code>src/momapy/rendering/core.py</code> <pre><code>@classmethod\ndef get_bolder_font_weight(cls, font_weight: FontWeight | float) -&gt; float:\n    \"\"\"Return the lightest font weight bolder than the given font weight.\"\"\"\n    if isinstance(font_weight, FontWeight):\n        font_weight = cls.font_weight_value_mapping.get(font_weight)\n        if font_weight is None:\n            raise ValueError(\n                f\"font weight must be a float, {FontWeight.NORMAL}, or {FontWeight.BOLD}\"\n            )\n    if font_weight &lt; 400:\n        new_font_weight = 400.0\n    elif font_weight &lt; 600:\n        new_font_weight = 700.0\n    else:\n        new_font_weight = 900.0\n    return new_font_weight\n</code></pre>"},{"location":"api_reference/rendering/core/#momapy.rendering.core.StatefulRenderer.get_current_state","title":"get_current_state","text":"<pre><code>get_current_state() -&gt; dict[str, Any]\n</code></pre> <p>Return the current state.</p> Source code in <code>src/momapy/rendering/core.py</code> <pre><code>def get_current_state(self) -&gt; dict[str, typing.Any]:\n    \"\"\"Return the current state.\"\"\"\n    return self._current_state\n</code></pre>"},{"location":"api_reference/rendering/core/#momapy.rendering.core.StatefulRenderer.get_current_value","title":"get_current_value","text":"<pre><code>get_current_value(attr_name: str) -&gt; Any\n</code></pre> <p>Return the current value for an attribute.</p> Source code in <code>src/momapy/rendering/core.py</code> <pre><code>def get_current_value(self, attr_name: str) -&gt; typing.Any:\n    \"\"\"Return the current value for an attribute.\"\"\"\n    return self.get_current_state()[attr_name]\n</code></pre>"},{"location":"api_reference/rendering/core/#momapy.rendering.core.StatefulRenderer.get_initial_value","title":"get_initial_value","text":"<pre><code>get_initial_value(attr_name: str) -&gt; Any\n</code></pre> <p>Return the initial value for an attribute.</p> Source code in <code>src/momapy/rendering/core.py</code> <pre><code>def get_initial_value(self, attr_name: str) -&gt; typing.Any:\n    \"\"\"Return the initial value for an attribute.\"\"\"\n    attr_value = self.initial_values.get(attr_name)\n    if attr_value is None:\n        attr_d = PRESENTATION_ATTRIBUTES[attr_name]\n        attr_value = attr_d[\"initial\"]\n        if attr_value is None:\n            attr_value = INITIAL_VALUES[attr_name]\n    return attr_value\n</code></pre>"},{"location":"api_reference/rendering/core/#momapy.rendering.core.StatefulRenderer.get_lighter_font_weight","title":"get_lighter_font_weight  <code>classmethod</code>","text":"<pre><code>get_lighter_font_weight(font_weight: FontWeight | float) -&gt; float\n</code></pre> <p>Return the boldest font weight lighter than the given font weight.</p> Source code in <code>src/momapy/rendering/core.py</code> <pre><code>@classmethod\ndef get_lighter_font_weight(cls, font_weight: FontWeight | float) -&gt; float:\n    \"\"\"Return the boldest font weight lighter than the given font weight.\"\"\"\n    if isinstance(font_weight, FontWeight):\n        font_weight = cls.font_weight_value_mapping.get(font_weight)\n        if font_weight is None:\n            raise ValueError(\n                f\"font weight must be a float, {FontWeight.NORMAL}, or {FontWeight.BOLD}\"\n            )\n    if font_weight &gt; 700:\n        new_font_weight = 700.0\n    elif font_weight &gt; 500:\n        new_font_weight = 400.0\n    else:\n        new_font_weight = 100.0\n    return new_font_weight\n</code></pre>"},{"location":"api_reference/rendering/core/#momapy.rendering.core.StatefulRenderer.new_page","title":"new_page  <code>abstractmethod</code>","text":"<pre><code>new_page(width: float, height: float) -&gt; None\n</code></pre> <p>Start a new page.</p> <p>Parameters:</p> Name Type Description Default <code>width</code> <code>float</code> <p>Width of the page.</p> required <code>height</code> <code>float</code> <p>Height of the page.</p> required Source code in <code>src/momapy/rendering/core.py</code> <pre><code>@abc.abstractmethod\ndef new_page(self, width: float, height: float) -&gt; None:\n    \"\"\"Start a new page.\n\n    Args:\n        width: Width of the page.\n        height: Height of the page.\n    \"\"\"\n    pass\n</code></pre>"},{"location":"api_reference/rendering/core/#momapy.rendering.core.StatefulRenderer.render_drawing_element","title":"render_drawing_element  <code>abstractmethod</code>","text":"<pre><code>render_drawing_element(drawing_element: DrawingElement) -&gt; None\n</code></pre> <p>Render a drawing element.</p> <p>Parameters:</p> Name Type Description Default <code>drawing_element</code> <code>DrawingElement</code> <p>The drawing element to render.</p> required Source code in <code>src/momapy/rendering/core.py</code> <pre><code>@abc.abstractmethod\ndef render_drawing_element(self, drawing_element: DrawingElement) -&gt; None:\n    \"\"\"Render a drawing element.\n\n    Args:\n        drawing_element: The drawing element to render.\n    \"\"\"\n    pass\n</code></pre>"},{"location":"api_reference/rendering/core/#momapy.rendering.core.StatefulRenderer.render_layout_element","title":"render_layout_element  <code>abstractmethod</code>","text":"<pre><code>render_layout_element(layout_element: LayoutElement) -&gt; None\n</code></pre> <p>Render a layout element.</p> <p>Parameters:</p> Name Type Description Default <code>layout_element</code> <code>LayoutElement</code> <p>The layout element to render.</p> required Source code in <code>src/momapy/rendering/core.py</code> <pre><code>@abc.abstractmethod\ndef render_layout_element(self, layout_element: LayoutElement) -&gt; None:\n    \"\"\"Render a layout element.\n\n    Args:\n        layout_element: The layout element to render.\n    \"\"\"\n    pass\n</code></pre>"},{"location":"api_reference/rendering/core/#momapy.rendering.core.StatefulRenderer.render_map","title":"render_map","text":"<pre><code>render_map(map_: Map) -&gt; None\n</code></pre> <p>Render a map.</p> <p>This is a convenience method, not part of the abstract contract: the default implementation renders the map's layout via :meth:<code>render_layout_element</code>, which is what every built-in backend needs. Subclasses may override it if a backend requires map-specific handling, but they are not obliged to. The file pipeline (:func:<code>render_map</code>/:func:<code>render_maps</code>) does not call this method; it renders each page through :meth:<code>render_layout_element</code>.</p> <p>Parameters:</p> Name Type Description Default <code>map_</code> <code>Map</code> <p>The map to render.</p> required <p>Raises:</p> Type Description <code>ValueError</code> <p>If the map has no layout to render.</p> Source code in <code>src/momapy/rendering/core.py</code> <pre><code>def render_map(self, map_: Map) -&gt; None:\n    \"\"\"Render a map.\n\n    This is a convenience method, **not** part of the abstract\n    contract: the default implementation renders the map's layout via\n    :meth:`render_layout_element`, which is what every built-in backend\n    needs. Subclasses may override it if a backend requires\n    map-specific handling, but they are not obliged to. The file\n    pipeline (:func:`render_map`/:func:`render_maps`) does not call this\n    method; it renders each page through :meth:`render_layout_element`.\n\n    Args:\n        map_: The map to render.\n\n    Raises:\n        ValueError: If the map has no layout to render.\n    \"\"\"\n    if map_.layout is None:\n        raise ValueError(\n            \"map has no layout to render (its layout is None); \"\n            \"a layout-less map (e.g. an SBML map) cannot be rendered\"\n        )\n    self.render_layout_element(map_.layout)\n</code></pre>"},{"location":"api_reference/rendering/core/#momapy.rendering.core.StatefulRenderer.restore","title":"restore","text":"<pre><code>restore() -&gt; None\n</code></pre> <p>Set the current state to the last saved state.</p> Source code in <code>src/momapy/rendering/core.py</code> <pre><code>def restore(self) -&gt; None:\n    \"\"\"Set the current state to the last saved state.\"\"\"\n    if len(self._states) &gt; 0:\n        state = self._states.pop()\n        self.set_current_state(state)\n        self.self_restore()\n    else:\n        raise RuntimeError(\n            \"restore() called with an empty state stack: no matching \"\n            \"save() to restore from\"\n        )\n</code></pre>"},{"location":"api_reference/rendering/core/#momapy.rendering.core.StatefulRenderer.save","title":"save","text":"<pre><code>save() -&gt; None\n</code></pre> <p>Save the current state.</p> Source code in <code>src/momapy/rendering/core.py</code> <pre><code>def save(self) -&gt; None:\n    \"\"\"Save the current state.\"\"\"\n    self._states.append(copy.deepcopy(self.get_current_state()))\n    self.self_save()\n</code></pre>"},{"location":"api_reference/rendering/core/#momapy.rendering.core.StatefulRenderer.self_restore","title":"self_restore  <code>abstractmethod</code>","text":"<pre><code>self_restore() -&gt; None\n</code></pre> <p>Restore the internal state of the renderer.</p> <p>This method must be implemented by subclasses to restore any internal state that is not part of the current state dictionary.</p> Source code in <code>src/momapy/rendering/core.py</code> <pre><code>@abc.abstractmethod\ndef self_restore(self) -&gt; None:\n    \"\"\"Restore the internal state of the renderer.\n\n    This method must be implemented by subclasses to restore any internal\n    state that is not part of the current state dictionary.\n    \"\"\"\n    pass\n</code></pre>"},{"location":"api_reference/rendering/core/#momapy.rendering.core.StatefulRenderer.self_save","title":"self_save  <code>abstractmethod</code>","text":"<pre><code>self_save() -&gt; None\n</code></pre> <p>Save the internal state of the renderer.</p> <p>This method must be implemented by subclasses to save any internal state that is not part of the current state dictionary.</p> Source code in <code>src/momapy/rendering/core.py</code> <pre><code>@abc.abstractmethod\ndef self_save(self) -&gt; None:\n    \"\"\"Save the internal state of the renderer.\n\n    This method must be implemented by subclasses to save any internal\n    state that is not part of the current state dictionary.\n    \"\"\"\n    pass\n</code></pre>"},{"location":"api_reference/rendering/core/#momapy.rendering.core.StatefulRenderer.set_current_state","title":"set_current_state","text":"<pre><code>set_current_state(state: dict[str, Any]) -&gt; None\n</code></pre> <p>Set the current state to the given state.</p> Source code in <code>src/momapy/rendering/core.py</code> <pre><code>def set_current_state(self, state: dict[str, typing.Any]) -&gt; None:\n    \"\"\"Set the current state to the given state.\"\"\"\n    for attr_name, attr_value in state.items():\n        self.set_current_value(attr_name, attr_value)\n</code></pre>"},{"location":"api_reference/rendering/core/#momapy.rendering.core.StatefulRenderer.set_current_state_from_drawing_element","title":"set_current_state_from_drawing_element","text":"<pre><code>set_current_state_from_drawing_element(drawing_element: DrawingElement) -&gt; None\n</code></pre> <p>Set the current state to a state given by a drawing element.</p> Source code in <code>src/momapy/rendering/core.py</code> <pre><code>def set_current_state_from_drawing_element(\n    self, drawing_element: DrawingElement\n) -&gt; None:\n    \"\"\"Set the current state to a state given by a drawing element.\"\"\"\n    state = self._get_state_from_drawing_element(drawing_element)\n    self.set_current_state(state)\n</code></pre>"},{"location":"api_reference/rendering/core/#momapy.rendering.core.StatefulRenderer.set_current_value","title":"set_current_value","text":"<pre><code>set_current_value(attr_name: str, attr_value: Any) -&gt; None\n</code></pre> <p>Set the current value for an attribute.</p> Source code in <code>src/momapy/rendering/core.py</code> <pre><code>def set_current_value(self, attr_name: str, attr_value: typing.Any) -&gt; None:\n    \"\"\"Set the current value for an attribute.\"\"\"\n    if attr_value is None:\n        attr_d = PRESENTATION_ATTRIBUTES[attr_name]\n        if not attr_d[\"inherited\"]:\n            attr_value = self.initial_values.get(attr_name)\n            if attr_value is None:\n                attr_value = attr_d[\"initial\"]\n            if attr_value is None:\n                attr_value = INITIAL_VALUES[attr_name]\n    if attr_name == \"font_weight\":\n        if isinstance(attr_value, FontWeight):\n            if attr_value == FontWeight.NORMAL or attr_value == FontWeight.BOLD:\n                attr_value = self.font_weight_value_mapping[attr_value]\n            elif attr_value == FontWeight.BOLDER:\n                attr_value = self.get_bolder_font_weight(\n                    self.get_current_value(\"font_weight\")\n                )\n            elif attr_value == FontWeight.LIGHTER:\n                attr_value = self.get_lighter_font_weight(\n                    self.get_current_value(\"font_weight\")\n                )\n    if attr_value is not None:\n        self._current_state[attr_name] = attr_value\n</code></pre>"},{"location":"api_reference/rendering/core/#momapy.rendering.core.SupportsFileOutput","title":"SupportsFileOutput","text":"<p>               Bases: <code>ABC</code></p> <p>Mixin declaring the file-output capability of a renderer.</p> <p>Mix into a :class:<code>Renderer</code> subclass to declare that it can build from and write to a file. This is the capability required by the file-output entry points (:func:<code>render_map</code>, :func:<code>render_maps</code>, :func:<code>render_layout_element</code>, :func:<code>render_layout_elements</code>); it is a capability, not an identity \u2014 a renderer mixing it in may still support other output targets (a live canvas, an in-memory surface) through its own constructors. Renderers with no file output simply do not mix it in.</p> <p>Implementers must declare :attr:<code>supported_formats</code> and implement :meth:<code>from_file</code>.</p> <p>Methods:</p> Name Description <code>from_file</code> <p>Build a renderer that writes its output to <code>file_path</code>.</p> <p>Attributes:</p> Name Type Description <code>default_format</code> <code>str | None</code> <p>The format used when <code>from_file</code> is called with <code>format_=None</code>.</p>"},{"location":"api_reference/rendering/core/#momapy.rendering.core.SupportsFileOutput.default_format","title":"default_format  <code>class-attribute</code>","text":"<pre><code>default_format: str | None = None\n</code></pre> <p>The format used when <code>from_file</code> is called with <code>format_=None</code>.</p> <p>Subclasses set this to one of their :attr:<code>supported_formats</code>.</p>"},{"location":"api_reference/rendering/core/#momapy.rendering.core.SupportsFileOutput.from_file","title":"from_file  <code>abstractmethod</code> <code>classmethod</code>","text":"<pre><code>from_file(file_path: str | PathLike, width: float, height: float, format_: str | None = None) -&gt; Self\n</code></pre> <p>Build a renderer that writes its output to <code>file_path</code>.</p> <p>Parameters:</p> Name Type Description Default <code>file_path</code> <code>str | PathLike</code> <p>The output file path.</p> required <code>width</code> <code>float</code> <p>The width of the canvas.</p> required <code>height</code> <code>float</code> <p>The height of the canvas.</p> required <code>format_</code> <code>str | None</code> <p>The output format. <code>None</code> selects the backend's :attr:<code>default_format</code>.</p> <code>None</code> <p>Returns:</p> Type Description <code>Self</code> <p>A new renderer instance writing to <code>file_path</code>.</p> Source code in <code>src/momapy/rendering/core.py</code> <pre><code>@classmethod\n@abc.abstractmethod\ndef from_file(\n    cls,\n    file_path: str | os.PathLike,\n    width: float,\n    height: float,\n    format_: str | None = None,\n) -&gt; typing_extensions.Self:\n    \"\"\"Build a renderer that writes its output to ``file_path``.\n\n    Args:\n        file_path: The output file path.\n        width: The width of the canvas.\n        height: The height of the canvas.\n        format_: The output format. ``None`` selects the backend's\n            :attr:`default_format`.\n\n    Returns:\n        A new renderer instance writing to ``file_path``.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"api_reference/rendering/core/#momapy.rendering.core.get_renderer","title":"get_renderer","text":"<pre><code>get_renderer(name: str) -&gt; type[Renderer]\n</code></pre> <p>Get a renderer class by name.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Renderer name (e.g., \"skia\", \"cairo\", \"svg-native\").</p> required <p>Returns:</p> Type Description <code>type[Renderer]</code> <p>Renderer class for the specified backend.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If no renderer with that name exists.</p> <code>ImportError</code> <p>If the renderer is registered but its backend module cannot be imported (e.g. an optional dependency such as skia-python or pycairo is not installed).</p> Source code in <code>src/momapy/rendering/core.py</code> <pre><code>def get_renderer(name: str) -&gt; type[\"Renderer\"]:\n    \"\"\"Get a renderer class by name.\n\n    Args:\n        name: Renderer name (e.g., \"skia\", \"cairo\", \"svg-native\").\n\n    Returns:\n        Renderer class for the specified backend.\n\n    Raises:\n        ValueError: If no renderer with that name exists.\n        ImportError: If the renderer is registered but its backend module\n            cannot be imported (e.g. an optional dependency such as\n            skia-python or pycairo is not installed).\n    \"\"\"\n    renderer = renderer_registry.get(name)\n    if renderer is None:\n        available = renderer_registry.list_available()\n        raise ValueError(\n            f\"No renderer named '{name}'. Available renderers: {', '.join(available)}\"\n        )\n    return renderer\n</code></pre>"},{"location":"api_reference/rendering/core/#momapy.rendering.core.list_renderers","title":"list_renderers","text":"<pre><code>list_renderers() -&gt; list[str]\n</code></pre> <p>List all available renderer names.</p> <p>Returns:</p> Type Description <code>list[str]</code> <p>Sorted list of available renderer names.</p> Source code in <code>src/momapy/rendering/core.py</code> <pre><code>def list_renderers() -&gt; list[str]:\n    \"\"\"List all available renderer names.\n\n    Returns:\n        Sorted list of available renderer names.\n    \"\"\"\n    return renderer_registry.list_available()\n</code></pre>"},{"location":"api_reference/rendering/core/#momapy.rendering.core.register_lazy_renderer","title":"register_lazy_renderer","text":"<pre><code>register_lazy_renderer(name: str, import_path: str) -&gt; None\n</code></pre> <p>Register a renderer for lazy loading.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Name to register the renderer under.</p> required <code>import_path</code> <code>str</code> <p>Import path in format \"module.path:ClassName\".</p> required Source code in <code>src/momapy/rendering/core.py</code> <pre><code>def register_lazy_renderer(name: str, import_path: str) -&gt; None:\n    \"\"\"Register a renderer for lazy loading.\n\n    Args:\n        name: Name to register the renderer under.\n        import_path: Import path in format \"module.path:ClassName\".\n    \"\"\"\n    renderer_registry.register_lazy(name, import_path)\n</code></pre>"},{"location":"api_reference/rendering/core/#momapy.rendering.core.register_renderer","title":"register_renderer","text":"<pre><code>register_renderer(name: str, cls: type[Renderer]) -&gt; None\n</code></pre> <p>Register a renderer class.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Name to register the renderer under.</p> required <code>cls</code> <code>type[Renderer]</code> <p>Renderer class (must inherit from Renderer).</p> required Source code in <code>src/momapy/rendering/core.py</code> <pre><code>def register_renderer(name: str, cls: type[\"Renderer\"]) -&gt; None:\n    \"\"\"Register a renderer class.\n\n    Args:\n        name: Name to register the renderer under.\n        cls: Renderer class (must inherit from Renderer).\n    \"\"\"\n    renderer_registry.register(name, cls)\n</code></pre>"},{"location":"api_reference/rendering/core/#momapy.rendering.core.render_layout_element","title":"render_layout_element","text":"<pre><code>render_layout_element(layout_element: LayoutElement, file_path: str | PathLike, format_: str | None = None, renderer: str | None = None, style_sheet: StyleSheet | None = None, to_top_left: bool = False) -&gt; None\n</code></pre> <p>Render a layout element to a file in the given format with the given registered renderer.</p> <p>Parameters:</p> Name Type Description Default <code>layout_element</code> <code>LayoutElement</code> <p>The layout element to render</p> required <code>file_path</code> <code>str | PathLike</code> <p>The output file path</p> required <code>format_</code> <code>str | None</code> <p>The output format. If None, inferred from file extension.</p> <code>None</code> <code>renderer</code> <code>str | None</code> <p>The registered renderer to use. If None, auto-detected based on format.</p> <code>None</code> <code>style_sheet</code> <code>StyleSheet | None</code> <p>An optional style sheet to apply before rendering</p> <code>None</code> <code>to_top_left</code> <code>bool</code> <p>Whether to move the layout element to the top left or not before rendering</p> <code>False</code> Source code in <code>src/momapy/rendering/core.py</code> <pre><code>def render_layout_element(\n    layout_element: LayoutElement,\n    file_path: str | os.PathLike,\n    format_: str | None = None,\n    renderer: str | None = None,\n    style_sheet: StyleSheet | None = None,\n    to_top_left: bool = False,\n) -&gt; None:\n    \"\"\"Render a layout element to a file in the given format with the given registered renderer.\n\n    Args:\n        layout_element: The layout element to render\n        file_path: The output file path\n        format_: The output format. If None, inferred from file extension.\n        renderer: The registered renderer to use. If None, auto-detected based on format.\n        style_sheet: An optional style sheet to apply before rendering\n        to_top_left: Whether to move the layout element to the top left or not before rendering\n    \"\"\"\n    render_layout_elements(\n        layout_elements=[layout_element],\n        file_path=file_path,\n        format_=format_,\n        renderer=renderer,\n        style_sheet=style_sheet,\n        to_top_left=to_top_left,\n    )\n</code></pre>"},{"location":"api_reference/rendering/core/#momapy.rendering.core.render_layout_elements","title":"render_layout_elements","text":"<pre><code>render_layout_elements(layout_elements: Sequence[LayoutElement], file_path: str | PathLike, format_: str | None = None, renderer: str | None = None, style_sheet: StyleSheet | None = None, to_top_left: bool = False, multi_pages: bool = False) -&gt; None\n</code></pre> <p>Render a collection of layout elements to a file in the given format with the given registered renderer.</p> <p>Parameters:</p> Name Type Description Default <code>layout_elements</code> <code>Sequence[LayoutElement]</code> <p>The layout elements to render</p> required <code>file_path</code> <code>str | PathLike</code> <p>The output file path</p> required <code>format_</code> <code>str | None</code> <p>The output format. If None, inferred from file extension.</p> <code>None</code> <code>renderer</code> <code>str | None</code> <p>The registered renderer to use. If None, auto-detected based on format.</p> <code>None</code> <code>style_sheet</code> <code>StyleSheet | None</code> <p>An optional style sheet to apply before rendering</p> <code>None</code> <code>to_top_left</code> <code>bool</code> <p>Whether to move the layout elements to the top left before rendering</p> <code>False</code> <code>multi_pages</code> <code>bool</code> <p>Whether to render each layout element on a separate page. Defaults to <code>False</code> (all elements on a single page).</p> <code>False</code> Source code in <code>src/momapy/rendering/core.py</code> <pre><code>def render_layout_elements(\n    layout_elements: collections.abc.Sequence[LayoutElement],\n    file_path: str | os.PathLike,\n    format_: str | None = None,\n    renderer: str | None = None,\n    style_sheet: StyleSheet | None = None,\n    to_top_left: bool = False,\n    multi_pages: bool = False,\n) -&gt; None:\n    \"\"\"Render a collection of layout elements to a file in the given format with the given registered renderer.\n\n    Args:\n        layout_elements: The layout elements to render\n        file_path: The output file path\n        format_: The output format. If None, inferred from file extension.\n        renderer: The registered renderer to use. If None, auto-detected based on format.\n        style_sheet: An optional style sheet to apply before rendering\n        to_top_left: Whether to move the layout elements to the top left before rendering\n        multi_pages: Whether to render each layout element on a separate page.\n            Defaults to `False` (all elements on a single page).\n    \"\"\"\n    if format_ is None:\n        file_path_obj = pathlib.Path(file_path)\n        format_ = file_path_obj.suffix[1:]\n        if not format_:\n            raise ValueError(\n                \"Cannot determine format from file path. Please specify format_ parameter.\"\n            )\n    if renderer is None:\n        renderer = _detect_renderer(format_)\n    if not layout_elements:\n        raise ValueError(\"no layout elements to render\")\n\n    def _prepare_layout_elements(\n        layout_elements: list[LayoutElement],\n        style_sheet: typing.Any = None,\n        to_top_left: bool = False,\n    ) -&gt; tuple[list[LayoutElement], float, float]:\n        bboxes = [layout_element.bbox() for layout_element in layout_elements]\n        bbox = fit(bboxes)\n        max_x = bbox.x + bbox.width / 2\n        max_y = bbox.y + bbox.height / 2\n        if style_sheet is not None or to_top_left:\n            new_layout_elements = []\n            for layout_element in layout_elements:\n                if isinstance(layout_element, LayoutElement):\n                    new_layout_elements.append(builder_from_object(layout_element))\n                elif isinstance(layout_element, Builder):\n                    new_layout_elements.append(copy.deepcopy(layout_element))\n            layout_elements = new_layout_elements\n        if style_sheet is not None:\n            if (\n                not isinstance(style_sheet, collections.abc.Collection)\n                or isinstance(style_sheet, str)\n                or isinstance(style_sheet, StyleSheet)\n            ):\n                style_sheets = [style_sheet]\n            else:\n                style_sheets = style_sheet\n            style_sheets = [\n                (\n                    StyleSheet.from_file(style_sheet)\n                    if not isinstance(style_sheet, StyleSheet)\n                    else style_sheet\n                )\n                for style_sheet in style_sheets\n            ]\n            style_sheet = combine_style_sheets(style_sheets)\n            for layout_element in layout_elements:\n                apply_style_sheet(layout_element, style_sheet)\n        if to_top_left:\n            min_x = bbox.x - bbox.width / 2\n            min_y = bbox.y - bbox.height / 2\n            max_x -= min_x\n            max_y -= min_y\n            translation = Translation(-min_x, -min_y)\n            for layout_element in layout_elements:\n                for attr_name in [\"group_transform\", \"transform\"]:\n                    if hasattr(layout_element, attr_name):\n                        if getattr(layout_element, attr_name) is None:\n                            setattr(layout_element, attr_name, [])\n                        getattr(layout_element, attr_name).append(translation)\n                        break\n        return layout_elements, max_x, max_y\n\n    renderer_cls = get_renderer(renderer)\n    if not issubclass(renderer_cls, SupportsFileOutput):\n        raise ValueError(\n            f\"Renderer '{renderer}' cannot render to a file: it does not \"\n            \"support file output (no from_file capability).\"\n        )\n    if not multi_pages:\n        prepared_layout_elements, max_x, max_y = _prepare_layout_elements(\n            layout_elements, style_sheet, to_top_left\n        )\n        renderer_instance = renderer_cls.from_file(file_path, max_x, max_y, format_)\n        renderer_instance.begin_session()\n        for prepared_layout_element in prepared_layout_elements:\n            renderer_instance.render_layout_element(prepared_layout_element)\n        renderer_instance.end_session()\n    else:\n        layout_element = layout_elements[0]\n        prepared_layout_elements, max_x, max_y = _prepare_layout_elements(\n            [layout_element], style_sheet, to_top_left\n        )\n        renderer_instance = renderer_cls.from_file(file_path, max_x, max_y, format_)\n        renderer_instance.begin_session()\n        renderer_instance.render_layout_element(prepared_layout_elements[0])\n        for layout_element in layout_elements[1:]:\n            prepared_layout_elements, max_x, max_y = _prepare_layout_elements(\n                [layout_element], style_sheet, to_top_left\n            )\n            renderer_instance.new_page(max_x, max_y)\n            renderer_instance.render_layout_element(prepared_layout_elements[0])\n        renderer_instance.end_session()\n</code></pre>"},{"location":"api_reference/rendering/core/#momapy.rendering.core.render_map","title":"render_map","text":"<pre><code>render_map(map_: Map, file_path: str | PathLike, format_: str | None = None, renderer: str | None = None, style_sheet: StyleSheet | None = None, to_top_left: bool = False) -&gt; None\n</code></pre> <p>Render a map to a file in the given format with the given registered renderer.</p> <p>Parameters:</p> Name Type Description Default <code>map_</code> <code>Map</code> <p>The map to render</p> required <code>file_path</code> <code>str | PathLike</code> <p>The output file path</p> required <code>format_</code> <code>str | None</code> <p>The output format. If None, inferred from file extension.</p> <code>None</code> <code>renderer</code> <code>str | None</code> <p>The registered renderer to use. If None, auto-detected based on format.</p> <code>None</code> <code>style_sheet</code> <code>StyleSheet | None</code> <p>An optional style sheet to apply before rendering</p> <code>None</code> <code>to_top_left</code> <code>bool</code> <p>Whether to move the map to the top left before rendering</p> <code>False</code> <p>Examples:</p> <pre><code>from momapy.io import read\nfrom momapy.rendering import render_map\n\n# Read a map from file\nresult = read(\"path/to/map.sbgn\")\nsbgn_map = result.obj\n\n# Render the map to SVG\nrender_map(sbgn_map, \"output.svg\")\n</code></pre> Source code in <code>src/momapy/rendering/core.py</code> <pre><code>def render_map(\n    map_: Map,\n    file_path: str | os.PathLike,\n    format_: str | None = None,\n    renderer: str | None = None,\n    style_sheet: StyleSheet | None = None,\n    to_top_left: bool = False,\n) -&gt; None:\n    \"\"\"Render a map to a file in the given format with the given registered renderer.\n\n    Args:\n        map_: The map to render\n        file_path: The output file path\n        format_: The output format. If None, inferred from file extension.\n        renderer: The registered renderer to use. If None, auto-detected based on format.\n        style_sheet: An optional style sheet to apply before rendering\n        to_top_left: Whether to move the map to the top left before rendering\n\n    Examples:\n        ```python\n        from momapy.io import read\n        from momapy.rendering import render_map\n\n        # Read a map from file\n        result = read(\"path/to/map.sbgn\")\n        sbgn_map = result.obj\n\n        # Render the map to SVG\n        render_map(sbgn_map, \"output.svg\")\n        ```\n    \"\"\"\n    render_maps([map_], file_path, format_, renderer, style_sheet, to_top_left)\n</code></pre>"},{"location":"api_reference/rendering/core/#momapy.rendering.core.render_maps","title":"render_maps","text":"<pre><code>render_maps(maps: Collection[Map], file_path: str | PathLike, format_: str | None = None, renderer: str | None = None, style_sheet: StyleSheet | None = None, to_top_left: bool = False, multi_pages: bool = False) -&gt; None\n</code></pre> <p>Render a collection of maps to a file in the given format with the given registered renderer.</p> <p>Parameters:</p> Name Type Description Default <code>maps</code> <code>Collection[Map]</code> <p>The maps to render</p> required <code>file_path</code> <code>str | PathLike</code> <p>The output file path</p> required <code>format_</code> <code>str | None</code> <p>The output format. If None, inferred from file extension.</p> <code>None</code> <code>renderer</code> <code>str | None</code> <p>The registered renderer to use. If None, auto-detected based on format.</p> <code>None</code> <code>style_sheet</code> <code>StyleSheet | None</code> <p>An optional style sheet to apply before rendering</p> <code>None</code> <code>to_top_left</code> <code>bool</code> <p>Whether to move the maps to the top left before rendering</p> <code>False</code> <code>multi_pages</code> <code>bool</code> <p>Whether to render each map on a separate page. Defaults to <code>False</code> (all maps on a single page).</p> <code>False</code> <p>Examples:</p> <pre><code>from momapy.io import read\nfrom momapy.rendering import render_maps\n\n# Read multiple maps from files\nresult1 = read(\"path/to/map1.sbgn\")\nfirst_map = result1.obj\nresult2 = read(\"path/to/map2.sbgn\")\nsecond_map = result2.obj\n\n# Render both maps to a multi-page PDF\nrender_maps([first_map, second_map], \"output.pdf\", multi_pages=True)\n</code></pre> Source code in <code>src/momapy/rendering/core.py</code> <pre><code>def render_maps(\n    maps: collections.abc.Collection[Map],\n    file_path: str | os.PathLike,\n    format_: str | None = None,\n    renderer: str | None = None,\n    style_sheet: StyleSheet | None = None,\n    to_top_left: bool = False,\n    multi_pages: bool = False,\n) -&gt; None:\n    \"\"\"Render a collection of maps to a file in the given format with the given registered renderer.\n\n    Args:\n        maps: The maps to render\n        file_path: The output file path\n        format_: The output format. If None, inferred from file extension.\n        renderer: The registered renderer to use. If None, auto-detected based on format.\n        style_sheet: An optional style sheet to apply before rendering\n        to_top_left: Whether to move the maps to the top left before rendering\n        multi_pages: Whether to render each map on a separate page.\n            Defaults to `False` (all maps on a single page).\n\n    Examples:\n        ```python\n        from momapy.io import read\n        from momapy.rendering import render_maps\n\n        # Read multiple maps from files\n        result1 = read(\"path/to/map1.sbgn\")\n        first_map = result1.obj\n        result2 = read(\"path/to/map2.sbgn\")\n        second_map = result2.obj\n\n        # Render both maps to a multi-page PDF\n        render_maps([first_map, second_map], \"output.pdf\", multi_pages=True)\n        ```\n    \"\"\"\n    if not maps:\n        raise ValueError(\"no maps to render\")\n    layout_elements = []\n    for map_ in maps:\n        if map_.layout is None:\n            raise ValueError(\n                \"map has no layout to render (its layout is None); \"\n                \"a layout-less map (e.g. an SBML map) cannot be rendered\"\n            )\n        layout_elements.append(map_.layout)\n    render_layout_elements(\n        layout_elements=layout_elements,\n        file_path=file_path,\n        format_=format_,\n        renderer=renderer,\n        style_sheet=style_sheet,\n        to_top_left=to_top_left,\n        multi_pages=multi_pages,\n    )\n</code></pre>"},{"location":"api_reference/rendering/skia/","title":"Skia","text":""},{"location":"api_reference/rendering/skia/#momapy.rendering.skia","title":"momapy.rendering.skia","text":"<p>Class for rendering with Skia.</p> <p>Classes:</p> Name Description <code>SkiaRenderer</code> <p>Renderer implementation using the Skia graphics library.</p>"},{"location":"api_reference/rendering/skia/#momapy.rendering.skia.SkiaRenderer","title":"SkiaRenderer  <code>dataclass</code>","text":"<pre><code>SkiaRenderer(_current_state: dict[str, Any] = dict(), _states: list[dict[str, Any]] = list(), *, canvas: Canvas, _config: dict[str, Any] = dict(), _skia_typefaces: dict[tuple, Any] = dict(), _skia_fonts: dict[tuple, Any] = dict())\n</code></pre> <p>               Bases: <code>StatefulRenderer</code>, <code>SupportsFileOutput</code></p> <p>Renderer implementation using the Skia graphics library.</p> <p>This renderer supports multiple output formats including PDF, SVG, PNG, JPEG, and WebP. It provides hardware-accelerated rendering capabilities and advanced features like filters and effects.</p> <p>Attributes:</p> Name Type Description <code>canvas</code> <code>Canvas</code> <p>The Skia canvas used for rendering</p> <p>Examples:</p> <pre><code>from momapy.meta.nodes import Rectangle\nfrom momapy.geometry import Point\n\n# Create a layout element to render\nnode = Rectangle(\n    position=Point(100.0, 100.0),\n    width=200.0,\n    height=100.0\n)\n\n# Create renderer and render the element\nrenderer = SkiaRenderer.from_file(\"output.pdf\", 800, 600, \"pdf\")\nrenderer.begin_session()\nrenderer.render_layout_element(node)\nrenderer.end_session()\n</code></pre> <p>Methods:</p> Name Description <code>__post_init__</code> <p>Initialize the renderer's current state after initialization.</p> <code>begin_session</code> <p>Begin a rendering session.</p> <code>end_session</code> <p>End the rendering session and save the output.</p> <code>from_file</code> <p>Create a SkiaRenderer instance from a file path.</p> <code>get_bolder_font_weight</code> <p>Return the lightest font weight bolder than the given font weight.</p> <code>get_current_state</code> <p>Return the current state.</p> <code>get_current_value</code> <p>Return the current value for an attribute.</p> <code>get_initial_value</code> <p>Return the initial value for an attribute.</p> <code>get_lighter_font_weight</code> <p>Return the boldest font weight lighter than the given font weight.</p> <code>new_page</code> <p>Create a new page in the output document.</p> <code>render_drawing_element</code> <p>Render a drawing element to the output.</p> <code>render_layout_element</code> <p>Render a layout element to the output.</p> <code>render_map</code> <p>Render a map.</p> <code>restore</code> <p>Set the current state to the last saved state.</p> <code>save</code> <p>Save the current state.</p> <code>self_restore</code> <p>Restore the Skia canvas state.</p> <code>self_save</code> <p>Save the Skia canvas state.</p> <code>set_current_state</code> <p>Set the current state to the given state.</p> <code>set_current_state_from_drawing_element</code> <p>Set the current state to a state given by a drawing element.</p> <code>set_current_value</code> <p>Set the current value for an attribute.</p>"},{"location":"api_reference/rendering/skia/#momapy.rendering.skia.SkiaRenderer.__post_init__","title":"__post_init__","text":"<pre><code>__post_init__() -&gt; None\n</code></pre> <p>Initialize the renderer's current state after initialization.</p> Source code in <code>src/momapy/rendering/core.py</code> <pre><code>def __post_init__(self) -&gt; None:\n    \"\"\"Initialize the renderer's current state after initialization.\"\"\"\n    self._initialize_current_state()\n</code></pre>"},{"location":"api_reference/rendering/skia/#momapy.rendering.skia.SkiaRenderer.begin_session","title":"begin_session","text":"<pre><code>begin_session() -&gt; None\n</code></pre> <p>Begin a rendering session.</p> <p>This method initializes the rendering context. For SkiaRenderer, no explicit initialization is needed beyond the canvas setup.</p> Source code in <code>src/momapy/rendering/skia.py</code> <pre><code>def begin_session(self) -&gt; None:\n    \"\"\"Begin a rendering session.\n\n    This method initializes the rendering context. For SkiaRenderer,\n    no explicit initialization is needed beyond the canvas setup.\n    \"\"\"\n    pass\n</code></pre>"},{"location":"api_reference/rendering/skia/#momapy.rendering.skia.SkiaRenderer.default_format","title":"default_format  <code>class-attribute</code>","text":"<pre><code>default_format: str | None = 'pdf'\n</code></pre> <p>The format used when <code>from_file</code> is called with <code>format_=None</code>.</p> <p>Subclasses set this to one of their :attr:<code>supported_formats</code>.</p>"},{"location":"api_reference/rendering/skia/#momapy.rendering.skia.SkiaRenderer.end_session","title":"end_session","text":"<pre><code>end_session() -&gt; None\n</code></pre> <p>End the rendering session and save the output.</p> <p>This method finalizes the rendering, flushes the canvas, and saves the output to the file. The specific actions depend on the output format: - PDF: Ends the page and closes the document - PNG/JPEG/WebP: Takes a snapshot and saves the image - SVG: Flushes the stream</p> Source code in <code>src/momapy/rendering/skia.py</code> <pre><code>def end_session(self) -&gt; None:\n    \"\"\"End the rendering session and save the output.\n\n    This method finalizes the rendering, flushes the canvas, and saves\n    the output to the file. The specific actions depend on the output format:\n    - PDF: Ends the page and closes the document\n    - PNG/JPEG/WebP: Takes a snapshot and saves the image\n    - SVG: Flushes the stream\n    \"\"\"\n    self.canvas.flush()\n    format_ = self._config.get(\"format\")\n    if format_ == \"pdf\":\n        self._config[\"document\"].endPage()\n        self._config[\"document\"].close()\n    elif format_ == \"png\":\n        image = self._config[\"surface\"].makeImageSnapshot()\n        image.save(self._config[\"file_path\"], skia.kPNG)\n    elif format_ == \"jpeg\":\n        image = self._config[\"surface\"].makeImageSnapshot()\n        image.save(self._config[\"file_path\"], skia.kJPEG)\n    elif format_ == \"webp\":\n        image = self._config[\"surface\"].makeImageSnapshot()\n        image.save(self._config[\"file_path\"], skia.kWEBP)\n    elif format_ == \"svg\":\n        del self.canvas\n        self._config[\"stream\"].flush()\n</code></pre>"},{"location":"api_reference/rendering/skia/#momapy.rendering.skia.SkiaRenderer.from_file","title":"from_file  <code>classmethod</code>","text":"<pre><code>from_file(file_path: str | PathLike, width: float, height: float, format_: str | None = None) -&gt; Self\n</code></pre> <p>Create a SkiaRenderer instance from a file path.</p> <p>Parameters:</p> Name Type Description Default <code>file_path</code> <code>str | PathLike</code> <p>The output file path</p> required <code>width</code> <code>float</code> <p>The width of the canvas</p> required <code>height</code> <code>float</code> <p>The height of the canvas</p> required <code>format_</code> <code>str | None</code> <p>The output format (pdf, svg, png, jpeg, or webp). <code>None</code> selects the backend's :attr:<code>default_format</code> (\"pdf\").</p> <code>None</code> <p>Returns:</p> Type Description <code>Self</code> <p>A new SkiaRenderer instance</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the format is not supported</p> <p>Examples:</p> <pre><code>renderer = SkiaRenderer.from_file(\"output.pdf\", 800, 600, \"pdf\")\n</code></pre> Source code in <code>src/momapy/rendering/skia.py</code> <pre><code>@classmethod\ndef from_file(\n    cls,\n    file_path: str | os.PathLike,\n    width: float,\n    height: float,\n    format_: str | None = None,\n) -&gt; typing_extensions.Self:\n    \"\"\"Create a SkiaRenderer instance from a file path.\n\n    Args:\n        file_path: The output file path\n        width: The width of the canvas\n        height: The height of the canvas\n        format_: The output format (pdf, svg, png, jpeg, or webp). ``None``\n            selects the backend's :attr:`default_format` (\"pdf\").\n\n    Returns:\n        A new SkiaRenderer instance\n\n    Raises:\n        ValueError: If the format is not supported\n\n    Examples:\n        ```python\n        renderer = SkiaRenderer.from_file(\"output.pdf\", 800, 600, \"pdf\")\n        ```\n    \"\"\"\n    if format_ is None:\n        format_ = cls.default_format\n    if format_ not in cls.supported_formats:\n        raise ValueError(\n            f\"unsupported format {format_!r}: expected one of \"\n            f\"{', '.join(cls.supported_formats)}\"\n        )\n    check_parent_dir_exists(file_path)\n    config = {}\n    canvas = None\n    if format_ == \"pdf\":\n        stream = skia.FILEWStream(file_path)\n        document = skia.PDF.MakeDocument(stream)\n        canvas = document.beginPage(width, height)\n        config[\"stream\"] = stream\n        config[\"document\"] = document\n    elif format_ in [\"png\", \"jpeg\", \"webp\"]:\n        surface = skia.Surface(width=int(width), height=int(height))\n        canvas = surface.getCanvas()\n        config[\"surface\"] = surface\n        config[\"file_path\"] = file_path\n    elif format_ == \"svg\":\n        stream = skia.FILEWStream(file_path)\n        canvas = skia.SVGCanvas.Make((width, height), stream)\n        config[\"stream\"] = stream\n    config[\"file_path\"] = file_path\n    config[\"width\"] = width\n    config[\"height\"] = height\n    config[\"format\"] = format_\n    return cls(canvas=canvas, _config=config)\n</code></pre>"},{"location":"api_reference/rendering/skia/#momapy.rendering.skia.SkiaRenderer.get_bolder_font_weight","title":"get_bolder_font_weight  <code>classmethod</code>","text":"<pre><code>get_bolder_font_weight(font_weight: FontWeight | float) -&gt; float\n</code></pre> <p>Return the lightest font weight bolder than the given font weight.</p> Source code in <code>src/momapy/rendering/core.py</code> <pre><code>@classmethod\ndef get_bolder_font_weight(cls, font_weight: FontWeight | float) -&gt; float:\n    \"\"\"Return the lightest font weight bolder than the given font weight.\"\"\"\n    if isinstance(font_weight, FontWeight):\n        font_weight = cls.font_weight_value_mapping.get(font_weight)\n        if font_weight is None:\n            raise ValueError(\n                f\"font weight must be a float, {FontWeight.NORMAL}, or {FontWeight.BOLD}\"\n            )\n    if font_weight &lt; 400:\n        new_font_weight = 400.0\n    elif font_weight &lt; 600:\n        new_font_weight = 700.0\n    else:\n        new_font_weight = 900.0\n    return new_font_weight\n</code></pre>"},{"location":"api_reference/rendering/skia/#momapy.rendering.skia.SkiaRenderer.get_current_state","title":"get_current_state","text":"<pre><code>get_current_state() -&gt; dict[str, Any]\n</code></pre> <p>Return the current state.</p> Source code in <code>src/momapy/rendering/core.py</code> <pre><code>def get_current_state(self) -&gt; dict[str, typing.Any]:\n    \"\"\"Return the current state.\"\"\"\n    return self._current_state\n</code></pre>"},{"location":"api_reference/rendering/skia/#momapy.rendering.skia.SkiaRenderer.get_current_value","title":"get_current_value","text":"<pre><code>get_current_value(attr_name: str) -&gt; Any\n</code></pre> <p>Return the current value for an attribute.</p> Source code in <code>src/momapy/rendering/core.py</code> <pre><code>def get_current_value(self, attr_name: str) -&gt; typing.Any:\n    \"\"\"Return the current value for an attribute.\"\"\"\n    return self.get_current_state()[attr_name]\n</code></pre>"},{"location":"api_reference/rendering/skia/#momapy.rendering.skia.SkiaRenderer.get_initial_value","title":"get_initial_value","text":"<pre><code>get_initial_value(attr_name: str) -&gt; Any\n</code></pre> <p>Return the initial value for an attribute.</p> Source code in <code>src/momapy/rendering/core.py</code> <pre><code>def get_initial_value(self, attr_name: str) -&gt; typing.Any:\n    \"\"\"Return the initial value for an attribute.\"\"\"\n    attr_value = self.initial_values.get(attr_name)\n    if attr_value is None:\n        attr_d = PRESENTATION_ATTRIBUTES[attr_name]\n        attr_value = attr_d[\"initial\"]\n        if attr_value is None:\n            attr_value = INITIAL_VALUES[attr_name]\n    return attr_value\n</code></pre>"},{"location":"api_reference/rendering/skia/#momapy.rendering.skia.SkiaRenderer.get_lighter_font_weight","title":"get_lighter_font_weight  <code>classmethod</code>","text":"<pre><code>get_lighter_font_weight(font_weight: FontWeight | float) -&gt; float\n</code></pre> <p>Return the boldest font weight lighter than the given font weight.</p> Source code in <code>src/momapy/rendering/core.py</code> <pre><code>@classmethod\ndef get_lighter_font_weight(cls, font_weight: FontWeight | float) -&gt; float:\n    \"\"\"Return the boldest font weight lighter than the given font weight.\"\"\"\n    if isinstance(font_weight, FontWeight):\n        font_weight = cls.font_weight_value_mapping.get(font_weight)\n        if font_weight is None:\n            raise ValueError(\n                f\"font weight must be a float, {FontWeight.NORMAL}, or {FontWeight.BOLD}\"\n            )\n    if font_weight &gt; 700:\n        new_font_weight = 700.0\n    elif font_weight &gt; 500:\n        new_font_weight = 400.0\n    else:\n        new_font_weight = 100.0\n    return new_font_weight\n</code></pre>"},{"location":"api_reference/rendering/skia/#momapy.rendering.skia.SkiaRenderer.new_page","title":"new_page","text":"<pre><code>new_page(width: float, height: float) -&gt; None\n</code></pre> <p>Create a new page in the output document.</p> <p>Parameters:</p> Name Type Description Default <code>width</code> <code>float</code> <p>The width of the new page</p> required <code>height</code> <code>float</code> <p>The height of the new page</p> required Note <p>Only PDF format supports multiple pages. Other formats will ignore this call.</p> Source code in <code>src/momapy/rendering/skia.py</code> <pre><code>def new_page(self, width: float, height: float) -&gt; None:\n    \"\"\"Create a new page in the output document.\n\n    Args:\n        width: The width of the new page\n        height: The height of the new page\n\n    Note:\n        Only PDF format supports multiple pages. Other formats will ignore this call.\n    \"\"\"\n    format_ = self._config.get(\"format\")\n    if format_ == \"pdf\":\n        self._config[\"document\"].endPage()\n        canvas = self._config[\"document\"].beginPage(width, height)\n        self.canvas = canvas\n</code></pre>"},{"location":"api_reference/rendering/skia/#momapy.rendering.skia.SkiaRenderer.render_drawing_element","title":"render_drawing_element","text":"<pre><code>render_drawing_element(drawing_element: DrawingElement) -&gt; None\n</code></pre> <p>Render a drawing element to the output.</p> <p>Parameters:</p> Name Type Description Default <code>drawing_element</code> <code>DrawingElement</code> <p>The drawing element to render</p> required <p>This method handles filters, transformations, and delegates to the appropriate rendering method based on the drawing element type.</p> Source code in <code>src/momapy/rendering/skia.py</code> <pre><code>def render_drawing_element(self, drawing_element: DrawingElement) -&gt; None:\n    \"\"\"Render a drawing element to the output.\n\n    Args:\n        drawing_element: The drawing element to render\n\n    This method handles filters, transformations, and delegates to\n    the appropriate rendering method based on the drawing element type.\n    \"\"\"\n    self.save()\n    self.set_current_state_from_drawing_element(drawing_element)\n    self._add_transform_from_drawing_element(drawing_element)\n    class_ = type(drawing_element)\n    if issubclass(class_, Builder):\n        class_ = class_._cls_to_build\n    de_func = getattr(self, self._de_class_func_mapping[class_])\n    filter_ = self.get_current_value(\"filter_\")\n    if filter_ is not NoneValue:\n        bbox = drawing_element.bbox()\n        saved_canvas = self.canvas\n        recorder = skia.PictureRecorder()\n        canvas = recorder.beginRecording(\n            skia.Rect.MakeXYWH(\n                bbox.north_west().x,\n                bbox.north_west().y,\n                bbox.width,\n                bbox.height,\n            )\n        )\n        self.canvas = canvas\n        de_func(drawing_element)\n        picture = recorder.finishRecordingAsPicture()\n        skia_paint = self._make_filter_paint(\n            filter_, drawing_element.get_filter_region()\n        )\n        self.canvas = saved_canvas\n        self.canvas.drawPicture(picture, paint=skia_paint)\n    else:\n        de_func(drawing_element)\n    self.restore()\n</code></pre>"},{"location":"api_reference/rendering/skia/#momapy.rendering.skia.SkiaRenderer.render_layout_element","title":"render_layout_element","text":"<pre><code>render_layout_element(layout_element: LayoutElement) -&gt; None\n</code></pre> <p>Render a layout element to the output.</p> <p>Parameters:</p> Name Type Description Default <code>layout_element</code> <code>LayoutElement</code> <p>The layout element to render</p> required Source code in <code>src/momapy/rendering/skia.py</code> <pre><code>def render_layout_element(self, layout_element: LayoutElement) -&gt; None:\n    \"\"\"Render a layout element to the output.\n\n    Args:\n        layout_element: The layout element to render\n    \"\"\"\n    drawing_elements = layout_element.drawing_elements()\n    for drawing_element in drawing_elements:\n        self.render_drawing_element(drawing_element)\n</code></pre>"},{"location":"api_reference/rendering/skia/#momapy.rendering.skia.SkiaRenderer.render_map","title":"render_map","text":"<pre><code>render_map(map_: Map) -&gt; None\n</code></pre> <p>Render a map.</p> <p>This is a convenience method, not part of the abstract contract: the default implementation renders the map's layout via :meth:<code>render_layout_element</code>, which is what every built-in backend needs. Subclasses may override it if a backend requires map-specific handling, but they are not obliged to. The file pipeline (:func:<code>render_map</code>/:func:<code>render_maps</code>) does not call this method; it renders each page through :meth:<code>render_layout_element</code>.</p> <p>Parameters:</p> Name Type Description Default <code>map_</code> <code>Map</code> <p>The map to render.</p> required <p>Raises:</p> Type Description <code>ValueError</code> <p>If the map has no layout to render.</p> Source code in <code>src/momapy/rendering/core.py</code> <pre><code>def render_map(self, map_: Map) -&gt; None:\n    \"\"\"Render a map.\n\n    This is a convenience method, **not** part of the abstract\n    contract: the default implementation renders the map's layout via\n    :meth:`render_layout_element`, which is what every built-in backend\n    needs. Subclasses may override it if a backend requires\n    map-specific handling, but they are not obliged to. The file\n    pipeline (:func:`render_map`/:func:`render_maps`) does not call this\n    method; it renders each page through :meth:`render_layout_element`.\n\n    Args:\n        map_: The map to render.\n\n    Raises:\n        ValueError: If the map has no layout to render.\n    \"\"\"\n    if map_.layout is None:\n        raise ValueError(\n            \"map has no layout to render (its layout is None); \"\n            \"a layout-less map (e.g. an SBML map) cannot be rendered\"\n        )\n    self.render_layout_element(map_.layout)\n</code></pre>"},{"location":"api_reference/rendering/skia/#momapy.rendering.skia.SkiaRenderer.restore","title":"restore","text":"<pre><code>restore() -&gt; None\n</code></pre> <p>Set the current state to the last saved state.</p> Source code in <code>src/momapy/rendering/core.py</code> <pre><code>def restore(self) -&gt; None:\n    \"\"\"Set the current state to the last saved state.\"\"\"\n    if len(self._states) &gt; 0:\n        state = self._states.pop()\n        self.set_current_state(state)\n        self.self_restore()\n    else:\n        raise RuntimeError(\n            \"restore() called with an empty state stack: no matching \"\n            \"save() to restore from\"\n        )\n</code></pre>"},{"location":"api_reference/rendering/skia/#momapy.rendering.skia.SkiaRenderer.save","title":"save","text":"<pre><code>save() -&gt; None\n</code></pre> <p>Save the current state.</p> Source code in <code>src/momapy/rendering/core.py</code> <pre><code>def save(self) -&gt; None:\n    \"\"\"Save the current state.\"\"\"\n    self._states.append(copy.deepcopy(self.get_current_state()))\n    self.self_save()\n</code></pre>"},{"location":"api_reference/rendering/skia/#momapy.rendering.skia.SkiaRenderer.self_restore","title":"self_restore","text":"<pre><code>self_restore() -&gt; None\n</code></pre> <p>Restore the Skia canvas state.</p> <p>This method restores the Skia canvas to the state saved by the most recent call to self_save().</p> Source code in <code>src/momapy/rendering/skia.py</code> <pre><code>def self_restore(self) -&gt; None:\n    \"\"\"Restore the Skia canvas state.\n\n    This method restores the Skia canvas to the state saved by the\n    most recent call to self_save().\n    \"\"\"\n    self.canvas.restore()\n</code></pre>"},{"location":"api_reference/rendering/skia/#momapy.rendering.skia.SkiaRenderer.self_save","title":"self_save","text":"<pre><code>self_save() -&gt; None\n</code></pre> <p>Save the Skia canvas state.</p> <p>This method saves the current state of the Skia canvas, including transformations and clipping regions.</p> Source code in <code>src/momapy/rendering/skia.py</code> <pre><code>def self_save(self) -&gt; None:\n    \"\"\"Save the Skia canvas state.\n\n    This method saves the current state of the Skia canvas, including\n    transformations and clipping regions.\n    \"\"\"\n    self.canvas.save()\n</code></pre>"},{"location":"api_reference/rendering/skia/#momapy.rendering.skia.SkiaRenderer.set_current_state","title":"set_current_state","text":"<pre><code>set_current_state(state: dict[str, Any]) -&gt; None\n</code></pre> <p>Set the current state to the given state.</p> Source code in <code>src/momapy/rendering/core.py</code> <pre><code>def set_current_state(self, state: dict[str, typing.Any]) -&gt; None:\n    \"\"\"Set the current state to the given state.\"\"\"\n    for attr_name, attr_value in state.items():\n        self.set_current_value(attr_name, attr_value)\n</code></pre>"},{"location":"api_reference/rendering/skia/#momapy.rendering.skia.SkiaRenderer.set_current_state_from_drawing_element","title":"set_current_state_from_drawing_element","text":"<pre><code>set_current_state_from_drawing_element(drawing_element: DrawingElement) -&gt; None\n</code></pre> <p>Set the current state to a state given by a drawing element.</p> Source code in <code>src/momapy/rendering/core.py</code> <pre><code>def set_current_state_from_drawing_element(\n    self, drawing_element: DrawingElement\n) -&gt; None:\n    \"\"\"Set the current state to a state given by a drawing element.\"\"\"\n    state = self._get_state_from_drawing_element(drawing_element)\n    self.set_current_state(state)\n</code></pre>"},{"location":"api_reference/rendering/skia/#momapy.rendering.skia.SkiaRenderer.set_current_value","title":"set_current_value","text":"<pre><code>set_current_value(attr_name: str, attr_value: Any) -&gt; None\n</code></pre> <p>Set the current value for an attribute.</p> Source code in <code>src/momapy/rendering/core.py</code> <pre><code>def set_current_value(self, attr_name: str, attr_value: typing.Any) -&gt; None:\n    \"\"\"Set the current value for an attribute.\"\"\"\n    if attr_value is None:\n        attr_d = PRESENTATION_ATTRIBUTES[attr_name]\n        if not attr_d[\"inherited\"]:\n            attr_value = self.initial_values.get(attr_name)\n            if attr_value is None:\n                attr_value = attr_d[\"initial\"]\n            if attr_value is None:\n                attr_value = INITIAL_VALUES[attr_name]\n    if attr_name == \"font_weight\":\n        if isinstance(attr_value, FontWeight):\n            if attr_value == FontWeight.NORMAL or attr_value == FontWeight.BOLD:\n                attr_value = self.font_weight_value_mapping[attr_value]\n            elif attr_value == FontWeight.BOLDER:\n                attr_value = self.get_bolder_font_weight(\n                    self.get_current_value(\"font_weight\")\n                )\n            elif attr_value == FontWeight.LIGHTER:\n                attr_value = self.get_lighter_font_weight(\n                    self.get_current_value(\"font_weight\")\n                )\n    if attr_value is not None:\n        self._current_state[attr_name] = attr_value\n</code></pre>"},{"location":"api_reference/rendering/svg_native/","title":"SVG-native","text":""},{"location":"api_reference/rendering/svg_native/#momapy.rendering.svg_native","title":"momapy.rendering.svg_native","text":"<p>Classes for rendering in the SVG format.</p> <p>Classes:</p> Name Description <code>SVGElement</code> <p>Class for SVG elements.</p> <code>SVGNativeCompatRenderer</code> <p>Renderer for SVG with compatibility mode filters.</p> <code>SVGNativeRenderer</code> <p>Renderer implementation for generating native SVG output.</p>"},{"location":"api_reference/rendering/svg_native/#momapy.rendering.svg_native.SVGElement","title":"SVGElement  <code>dataclass</code>","text":"<pre><code>SVGElement(name: str, value: str | None = None, attributes: dict[str, Any] = dict(), elements: list[SVGElement] = list())\n</code></pre> <p>               Bases: <code>object</code></p> <p>Class for SVG elements.</p> <p>This class represents an SVG element with a name, optional text value, attributes, and child elements.</p> <p>Examples:</p> <pre><code>element = SVGElement(\n    name=\"rect\",\n    attributes={\"x\": \"0\", \"y\": \"0\", \"width\": \"100\", \"height\": \"100\"}\n)\nprint(element)\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>The tag name of the SVG element (e.g. 'svg', 'rect', 'path')</p> required <code>value</code> <code>str | None</code> <p>The optional text content of the element</p> <code>None</code> <code>attributes</code> <code>dict[str, Any]</code> <p>dict() -&gt; new empty dictionary dict(mapping) -&gt; new dictionary initialized from a mapping object's     (key, value) pairs dict(iterable) -&gt; new dictionary initialized as if via:     d = {}     for k, v in iterable:         d[k] = v dict(**kwargs) -&gt; new dictionary initialized with the name=value pairs     in the keyword argument list.  For example:  dict(one=1, two=2)</p> <code>&lt;class 'dict'&gt;</code> <code>elements</code> <code>list[str]</code> <p>Built-in mutable sequence.</p> <p>If no argument is given, the constructor creates a new empty list. The argument must be an iterable if specified.</p> <code>&lt;dynamic&gt;</code> <p>Methods:</p> Name Description <code>__str__</code> <p>Return the SVG string representation of the element.</p> <code>add_element</code> <p>Add a sub-element to the SVG element.</p> <code>to_string</code> <p>Return the SVG string representing the element.</p>"},{"location":"api_reference/rendering/svg_native/#momapy.rendering.svg_native.SVGElement.__str__","title":"__str__","text":"<pre><code>__str__() -&gt; str\n</code></pre> <p>Return the SVG string representation of the element.</p> Source code in <code>src/momapy/rendering/svg_native.py</code> <pre><code>def __str__(self) -&gt; str:\n    \"\"\"Return the SVG string representation of the element.\"\"\"\n    return self.to_string()\n</code></pre>"},{"location":"api_reference/rendering/svg_native/#momapy.rendering.svg_native.SVGElement.add_element","title":"add_element","text":"<pre><code>add_element(element: SVGElement) -&gt; None\n</code></pre> <p>Add a sub-element to the SVG element.</p> <p>Parameters:</p> Name Type Description Default <code>element</code> <code>SVGElement</code> <p>The child SVGElement to add</p> required Source code in <code>src/momapy/rendering/svg_native.py</code> <pre><code>def add_element(self, element: \"SVGElement\") -&gt; None:\n    \"\"\"Add a sub-element to the SVG element.\n\n    Args:\n        element: The child SVGElement to add\n    \"\"\"\n    self.elements.append(element)\n</code></pre>"},{"location":"api_reference/rendering/svg_native/#momapy.rendering.svg_native.SVGElement.to_string","title":"to_string","text":"<pre><code>to_string(indent: int = 0) -&gt; str\n</code></pre> <p>Return the SVG string representing the element.</p> <p>Parameters:</p> Name Type Description Default <code>indent</code> <code>int</code> <p>The indentation level (number of tabs)</p> <code>0</code> <p>Returns:</p> Type Description <code>str</code> <p>The SVG markup as a string</p> Source code in <code>src/momapy/rendering/svg_native.py</code> <pre><code>def to_string(self, indent: int = 0) -&gt; str:\n    \"\"\"Return the SVG string representing the element.\n\n    Args:\n        indent: The indentation level (number of tabs)\n\n    Returns:\n        The SVG markup as a string\n    \"\"\"\n    s_indent = \"\\t\" * indent\n    s_value = f\"{s_indent}{self.value}\\n\" if self.value is not None else \"\"\n    if self.attributes:\n        l_s_attributes = []\n        for attr_name, attr_value in self.attributes.items():\n            s_attr_name = attr_name\n            s_attr_value = f'\"{attr_value}\"'\n            s_attribute = f\"{s_attr_name}={s_attr_value}\"\n            l_s_attributes.append(s_attribute)\n        s_attributes = f\" {' '.join(l_s_attributes)}\"\n    else:\n        s_attributes = \"\"\n    if self.elements:\n        s_elements = \"\\n\".join(\n            [child.to_string(indent + 1) for child in self.elements]\n        )\n        s_elements += \"\\n\"\n    else:\n        s_elements = \"\"\n    return f\"{s_indent}&lt;{self.name}{s_attributes}&gt;\\n{s_value}{s_elements}{s_indent}&lt;/{self.name}&gt;\"\n</code></pre>"},{"location":"api_reference/rendering/svg_native/#momapy.rendering.svg_native.SVGNativeCompatRenderer","title":"SVGNativeCompatRenderer  <code>dataclass</code>","text":"<pre><code>SVGNativeCompatRenderer(svg: SVGElement, _config: dict[str, Any] = dict(), _filter_elements: list[SVGElement] = list())\n</code></pre> <p>               Bases: <code>SVGNativeRenderer</code></p> <p>Renderer for SVG with compatibility mode filters.</p> <p>This renderer extends SVGNativeRenderer to provide compatibility with older SVG viewers by converting filters to a compatible format.</p> <p>Examples:</p> <pre><code>renderer = SVGNativeCompatRenderer.from_file(\"output.svg\", 800, 600, \"svg\")\nrenderer.begin_session()\nrenderer.render_layout_element(layout_element)\nrenderer.end_session()\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>svg</code> <code>SVGElement</code> <p>The root SVG element that will contain all rendered content</p> required <p>Methods:</p> Name Description <code>begin_session</code> <p>Begin a rendering session.</p> <code>end_session</code> <p>End the rendering session and save the output.</p> <code>from_file</code> <p>Create an SVGNativeRenderer instance from a file path.</p> <code>get_bolder_font_weight</code> <p>Return the lightest font weight bolder than the given font weight.</p> <code>get_lighter_font_weight</code> <p>Return the boldest font weight lighter than the given font weight.</p> <code>new_page</code> <p>Create a new page in the output document.</p> <code>render_drawing_element</code> <p>Render a drawing element to the output.</p> <code>render_layout_element</code> <p>Render a layout element to the output.</p> <code>render_map</code> <p>Render a map.</p> <p>Attributes:</p> Name Type Description <code>default_format</code> <code>str | None</code> <p>The format used when <code>from_file</code> is called with <code>format_=None</code>.</p>"},{"location":"api_reference/rendering/svg_native/#momapy.rendering.svg_native.SVGNativeCompatRenderer.begin_session","title":"begin_session","text":"<pre><code>begin_session() -&gt; None\n</code></pre> <p>Begin a rendering session.</p> <p>This method initializes the rendering context. For SVGNativeRenderer, no explicit initialization is needed as the SVG element is created during instantiation.</p> Source code in <code>src/momapy/rendering/svg_native.py</code> <pre><code>def begin_session(self) -&gt; None:\n    \"\"\"Begin a rendering session.\n\n    This method initializes the rendering context. For SVGNativeRenderer,\n    no explicit initialization is needed as the SVG element is created\n    during instantiation.\n    \"\"\"\n    pass\n</code></pre>"},{"location":"api_reference/rendering/svg_native/#momapy.rendering.svg_native.SVGNativeCompatRenderer.default_format","title":"default_format  <code>class-attribute</code>","text":"<pre><code>default_format: str | None = 'svg'\n</code></pre> <p>The format used when <code>from_file</code> is called with <code>format_=None</code>.</p> <p>Subclasses set this to one of their :attr:<code>supported_formats</code>.</p>"},{"location":"api_reference/rendering/svg_native/#momapy.rendering.svg_native.SVGNativeCompatRenderer.end_session","title":"end_session","text":"<pre><code>end_session() -&gt; None\n</code></pre> <p>End the rendering session and save the output.</p> <p>This method finalizes the SVG document, adds any filter definitions to the defs section, and writes the output to the file.</p> Source code in <code>src/momapy/rendering/svg_native.py</code> <pre><code>def end_session(self) -&gt; None:\n    \"\"\"End the rendering session and save the output.\n\n    This method finalizes the SVG document, adds any filter definitions\n    to the defs section, and writes the output to the file.\n    \"\"\"\n    if self._filter_elements:\n        defs = SVGElement(name=\"defs\", elements=self._filter_elements)\n        self.svg.add_element(defs)\n    if self._config.get(\"output_file\") is not None:\n        with open(self._config[\"output_file\"], \"w\", encoding=\"utf-8\") as f:\n            f.write(str(self.svg))\n</code></pre>"},{"location":"api_reference/rendering/svg_native/#momapy.rendering.svg_native.SVGNativeCompatRenderer.from_file","title":"from_file  <code>classmethod</code>","text":"<pre><code>from_file(file_path: str | PathLike, width: float, height: float, format_: str | None = None) -&gt; Self\n</code></pre> <p>Create an SVGNativeRenderer instance from a file path.</p> <p>Parameters:</p> Name Type Description Default <code>file_path</code> <code>str | PathLike</code> <p>The output file path</p> required <code>width</code> <code>float</code> <p>The width of the SVG canvas</p> required <code>height</code> <code>float</code> <p>The height of the SVG canvas</p> required <code>format_</code> <code>str | None</code> <p>The output format. <code>None</code> selects the backend's :attr:<code>default_format</code> (\"svg\").</p> <code>None</code> <p>Returns:</p> Type Description <code>Self</code> <p>A new SVGNativeRenderer instance</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the format is not supported</p> <p>Examples:</p> <pre><code>renderer = SVGNativeRenderer.from_file(\"output.svg\", 800, 600, \"svg\")\n</code></pre> Source code in <code>src/momapy/rendering/svg_native.py</code> <pre><code>@classmethod\ndef from_file(\n    cls,\n    file_path: str | os.PathLike,\n    width: float,\n    height: float,\n    format_: str | None = None,\n) -&gt; typing_extensions.Self:\n    \"\"\"Create an SVGNativeRenderer instance from a file path.\n\n    Args:\n        file_path: The output file path\n        width: The width of the SVG canvas\n        height: The height of the SVG canvas\n        format_: The output format. ``None`` selects the backend's\n            :attr:`default_format` (\"svg\").\n\n    Returns:\n        A new SVGNativeRenderer instance\n\n    Raises:\n        ValueError: If the format is not supported\n\n    Examples:\n        ```python\n        renderer = SVGNativeRenderer.from_file(\"output.svg\", 800, 600, \"svg\")\n        ```\n    \"\"\"\n    if format_ is None:\n        format_ = cls.default_format\n    if format_ not in cls.supported_formats:\n        raise ValueError(\n            f\"unsupported format {format_!r}: expected one of \"\n            f\"{', '.join(cls.supported_formats)}\"\n        )\n    check_parent_dir_exists(file_path)\n    config = {}\n    config[\"output_file\"] = file_path\n    config[\"width\"] = width\n    config[\"height\"] = height\n    config[\"format\"] = format_\n    svg = SVGElement(\n        name=\"svg\",\n        attributes={\n            \"xmlns\": \"http://www.w3.org/2000/svg\",\n            \"viewBox\": f\"0 0 {width} {height}\",\n        },\n    )\n    return cls(svg=svg, _config=config)\n</code></pre>"},{"location":"api_reference/rendering/svg_native/#momapy.rendering.svg_native.SVGNativeCompatRenderer.get_bolder_font_weight","title":"get_bolder_font_weight  <code>classmethod</code>","text":"<pre><code>get_bolder_font_weight(font_weight: FontWeight | float) -&gt; float\n</code></pre> <p>Return the lightest font weight bolder than the given font weight.</p> Source code in <code>src/momapy/rendering/core.py</code> <pre><code>@classmethod\ndef get_bolder_font_weight(cls, font_weight: FontWeight | float) -&gt; float:\n    \"\"\"Return the lightest font weight bolder than the given font weight.\"\"\"\n    if isinstance(font_weight, FontWeight):\n        font_weight = cls.font_weight_value_mapping.get(font_weight)\n        if font_weight is None:\n            raise ValueError(\n                f\"font weight must be a float, {FontWeight.NORMAL}, or {FontWeight.BOLD}\"\n            )\n    if font_weight &lt; 400:\n        new_font_weight = 400.0\n    elif font_weight &lt; 600:\n        new_font_weight = 700.0\n    else:\n        new_font_weight = 900.0\n    return new_font_weight\n</code></pre>"},{"location":"api_reference/rendering/svg_native/#momapy.rendering.svg_native.SVGNativeCompatRenderer.get_lighter_font_weight","title":"get_lighter_font_weight  <code>classmethod</code>","text":"<pre><code>get_lighter_font_weight(font_weight: FontWeight | float) -&gt; float\n</code></pre> <p>Return the boldest font weight lighter than the given font weight.</p> Source code in <code>src/momapy/rendering/core.py</code> <pre><code>@classmethod\ndef get_lighter_font_weight(cls, font_weight: FontWeight | float) -&gt; float:\n    \"\"\"Return the boldest font weight lighter than the given font weight.\"\"\"\n    if isinstance(font_weight, FontWeight):\n        font_weight = cls.font_weight_value_mapping.get(font_weight)\n        if font_weight is None:\n            raise ValueError(\n                f\"font weight must be a float, {FontWeight.NORMAL}, or {FontWeight.BOLD}\"\n            )\n    if font_weight &gt; 700:\n        new_font_weight = 700.0\n    elif font_weight &gt; 500:\n        new_font_weight = 400.0\n    else:\n        new_font_weight = 100.0\n    return new_font_weight\n</code></pre>"},{"location":"api_reference/rendering/svg_native/#momapy.rendering.svg_native.SVGNativeCompatRenderer.new_page","title":"new_page","text":"<pre><code>new_page(width: float, height: float) -&gt; None\n</code></pre> <p>Create a new page in the output document.</p> <p>Parameters:</p> Name Type Description Default <code>width</code> <code>float</code> <p>The width of the new page</p> required <code>height</code> <code>float</code> <p>The height of the new page</p> required Note <p>SVG format does not support multiple pages. This method is a no-op.</p> Source code in <code>src/momapy/rendering/svg_native.py</code> <pre><code>def new_page(self, width: float, height: float) -&gt; None:\n    \"\"\"Create a new page in the output document.\n\n    Args:\n        width: The width of the new page\n        height: The height of the new page\n\n    Note:\n        SVG format does not support multiple pages. This method is a no-op.\n    \"\"\"\n    pass\n</code></pre>"},{"location":"api_reference/rendering/svg_native/#momapy.rendering.svg_native.SVGNativeCompatRenderer.render_drawing_element","title":"render_drawing_element","text":"<pre><code>render_drawing_element(drawing_element: DrawingElement) -&gt; None\n</code></pre> <p>Render a drawing element to the output.</p> <p>Parameters:</p> Name Type Description Default <code>drawing_element</code> <code>DrawingElement</code> <p>The drawing element to render</p> required <p>This method converts the drawing element to an SVG element and adds it to the SVG document.</p> Source code in <code>src/momapy/rendering/svg_native.py</code> <pre><code>def render_drawing_element(self, drawing_element: DrawingElement) -&gt; None:\n    \"\"\"Render a drawing element to the output.\n\n    Args:\n        drawing_element: The drawing element to render\n\n    This method converts the drawing element to an SVG element\n    and adds it to the SVG document.\n    \"\"\"\n    element = self._make_drawing_element_element(drawing_element)\n    self.svg.add_element(element)\n</code></pre>"},{"location":"api_reference/rendering/svg_native/#momapy.rendering.svg_native.SVGNativeCompatRenderer.render_layout_element","title":"render_layout_element","text":"<pre><code>render_layout_element(layout_element: LayoutElement) -&gt; None\n</code></pre> <p>Render a layout element to the output.</p> <p>Parameters:</p> Name Type Description Default <code>layout_element</code> <code>LayoutElement</code> <p>The layout element to render</p> required Source code in <code>src/momapy/rendering/svg_native.py</code> <pre><code>def render_layout_element(self, layout_element: LayoutElement) -&gt; None:\n    \"\"\"Render a layout element to the output.\n\n    Args:\n        layout_element: The layout element to render\n    \"\"\"\n    drawing_elements = layout_element.drawing_elements()\n    for drawing_element in drawing_elements:\n        self.render_drawing_element(drawing_element)\n</code></pre>"},{"location":"api_reference/rendering/svg_native/#momapy.rendering.svg_native.SVGNativeCompatRenderer.render_map","title":"render_map","text":"<pre><code>render_map(map_: Map) -&gt; None\n</code></pre> <p>Render a map.</p> <p>This is a convenience method, not part of the abstract contract: the default implementation renders the map's layout via :meth:<code>render_layout_element</code>, which is what every built-in backend needs. Subclasses may override it if a backend requires map-specific handling, but they are not obliged to. The file pipeline (:func:<code>render_map</code>/:func:<code>render_maps</code>) does not call this method; it renders each page through :meth:<code>render_layout_element</code>.</p> <p>Parameters:</p> Name Type Description Default <code>map_</code> <code>Map</code> <p>The map to render.</p> required <p>Raises:</p> Type Description <code>ValueError</code> <p>If the map has no layout to render.</p> Source code in <code>src/momapy/rendering/core.py</code> <pre><code>def render_map(self, map_: Map) -&gt; None:\n    \"\"\"Render a map.\n\n    This is a convenience method, **not** part of the abstract\n    contract: the default implementation renders the map's layout via\n    :meth:`render_layout_element`, which is what every built-in backend\n    needs. Subclasses may override it if a backend requires\n    map-specific handling, but they are not obliged to. The file\n    pipeline (:func:`render_map`/:func:`render_maps`) does not call this\n    method; it renders each page through :meth:`render_layout_element`.\n\n    Args:\n        map_: The map to render.\n\n    Raises:\n        ValueError: If the map has no layout to render.\n    \"\"\"\n    if map_.layout is None:\n        raise ValueError(\n            \"map has no layout to render (its layout is None); \"\n            \"a layout-less map (e.g. an SBML map) cannot be rendered\"\n        )\n    self.render_layout_element(map_.layout)\n</code></pre>"},{"location":"api_reference/rendering/svg_native/#momapy.rendering.svg_native.SVGNativeRenderer","title":"SVGNativeRenderer  <code>dataclass</code>","text":"<pre><code>SVGNativeRenderer(svg: SVGElement, _config: dict[str, Any] = dict(), _filter_elements: list[SVGElement] = list())\n</code></pre> <p>               Bases: <code>Renderer</code>, <code>SupportsFileOutput</code></p> <p>Renderer implementation for generating native SVG output.</p> <p>This renderer creates SVG markup directly without external dependencies. It supports all standard SVG features including filters, transformations, and presentation attributes.</p> <p>Examples:</p> <pre><code>from momapy.meta.nodes import Rectangle\nimport momapy.geometry\n\n# Create a layout element to render\nnode = Rectangle(\n    position=momapy.geometry.Point(100.0, 100.0),\n    width=200.0,\n    height=100.0\n)\n\n# Create renderer and render the element\nrenderer = SVGNativeRenderer.from_file(\"output.svg\", 800, 600, \"svg\")\nrenderer.begin_session()\nrenderer.render_layout_element(node)\nrenderer.end_session()\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>svg</code> <code>SVGElement</code> <p>The root SVG element that will contain all rendered content</p> required <p>Methods:</p> Name Description <code>begin_session</code> <p>Begin a rendering session.</p> <code>end_session</code> <p>End the rendering session and save the output.</p> <code>from_file</code> <p>Create an SVGNativeRenderer instance from a file path.</p> <code>get_bolder_font_weight</code> <p>Return the lightest font weight bolder than the given font weight.</p> <code>get_lighter_font_weight</code> <p>Return the boldest font weight lighter than the given font weight.</p> <code>new_page</code> <p>Create a new page in the output document.</p> <code>render_drawing_element</code> <p>Render a drawing element to the output.</p> <code>render_layout_element</code> <p>Render a layout element to the output.</p> <code>render_map</code> <p>Render a map.</p> <p>Attributes:</p> Name Type Description <code>default_format</code> <code>str | None</code> <p>The format used when <code>from_file</code> is called with <code>format_=None</code>.</p>"},{"location":"api_reference/rendering/svg_native/#momapy.rendering.svg_native.SVGNativeRenderer.begin_session","title":"begin_session","text":"<pre><code>begin_session() -&gt; None\n</code></pre> <p>Begin a rendering session.</p> <p>This method initializes the rendering context. For SVGNativeRenderer, no explicit initialization is needed as the SVG element is created during instantiation.</p> Source code in <code>src/momapy/rendering/svg_native.py</code> <pre><code>def begin_session(self) -&gt; None:\n    \"\"\"Begin a rendering session.\n\n    This method initializes the rendering context. For SVGNativeRenderer,\n    no explicit initialization is needed as the SVG element is created\n    during instantiation.\n    \"\"\"\n    pass\n</code></pre>"},{"location":"api_reference/rendering/svg_native/#momapy.rendering.svg_native.SVGNativeRenderer.default_format","title":"default_format  <code>class-attribute</code>","text":"<pre><code>default_format: str | None = 'svg'\n</code></pre> <p>The format used when <code>from_file</code> is called with <code>format_=None</code>.</p> <p>Subclasses set this to one of their :attr:<code>supported_formats</code>.</p>"},{"location":"api_reference/rendering/svg_native/#momapy.rendering.svg_native.SVGNativeRenderer.end_session","title":"end_session","text":"<pre><code>end_session() -&gt; None\n</code></pre> <p>End the rendering session and save the output.</p> <p>This method finalizes the SVG document, adds any filter definitions to the defs section, and writes the output to the file.</p> Source code in <code>src/momapy/rendering/svg_native.py</code> <pre><code>def end_session(self) -&gt; None:\n    \"\"\"End the rendering session and save the output.\n\n    This method finalizes the SVG document, adds any filter definitions\n    to the defs section, and writes the output to the file.\n    \"\"\"\n    if self._filter_elements:\n        defs = SVGElement(name=\"defs\", elements=self._filter_elements)\n        self.svg.add_element(defs)\n    if self._config.get(\"output_file\") is not None:\n        with open(self._config[\"output_file\"], \"w\", encoding=\"utf-8\") as f:\n            f.write(str(self.svg))\n</code></pre>"},{"location":"api_reference/rendering/svg_native/#momapy.rendering.svg_native.SVGNativeRenderer.from_file","title":"from_file  <code>classmethod</code>","text":"<pre><code>from_file(file_path: str | PathLike, width: float, height: float, format_: str | None = None) -&gt; Self\n</code></pre> <p>Create an SVGNativeRenderer instance from a file path.</p> <p>Parameters:</p> Name Type Description Default <code>file_path</code> <code>str | PathLike</code> <p>The output file path</p> required <code>width</code> <code>float</code> <p>The width of the SVG canvas</p> required <code>height</code> <code>float</code> <p>The height of the SVG canvas</p> required <code>format_</code> <code>str | None</code> <p>The output format. <code>None</code> selects the backend's :attr:<code>default_format</code> (\"svg\").</p> <code>None</code> <p>Returns:</p> Type Description <code>Self</code> <p>A new SVGNativeRenderer instance</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the format is not supported</p> <p>Examples:</p> <pre><code>renderer = SVGNativeRenderer.from_file(\"output.svg\", 800, 600, \"svg\")\n</code></pre> Source code in <code>src/momapy/rendering/svg_native.py</code> <pre><code>@classmethod\ndef from_file(\n    cls,\n    file_path: str | os.PathLike,\n    width: float,\n    height: float,\n    format_: str | None = None,\n) -&gt; typing_extensions.Self:\n    \"\"\"Create an SVGNativeRenderer instance from a file path.\n\n    Args:\n        file_path: The output file path\n        width: The width of the SVG canvas\n        height: The height of the SVG canvas\n        format_: The output format. ``None`` selects the backend's\n            :attr:`default_format` (\"svg\").\n\n    Returns:\n        A new SVGNativeRenderer instance\n\n    Raises:\n        ValueError: If the format is not supported\n\n    Examples:\n        ```python\n        renderer = SVGNativeRenderer.from_file(\"output.svg\", 800, 600, \"svg\")\n        ```\n    \"\"\"\n    if format_ is None:\n        format_ = cls.default_format\n    if format_ not in cls.supported_formats:\n        raise ValueError(\n            f\"unsupported format {format_!r}: expected one of \"\n            f\"{', '.join(cls.supported_formats)}\"\n        )\n    check_parent_dir_exists(file_path)\n    config = {}\n    config[\"output_file\"] = file_path\n    config[\"width\"] = width\n    config[\"height\"] = height\n    config[\"format\"] = format_\n    svg = SVGElement(\n        name=\"svg\",\n        attributes={\n            \"xmlns\": \"http://www.w3.org/2000/svg\",\n            \"viewBox\": f\"0 0 {width} {height}\",\n        },\n    )\n    return cls(svg=svg, _config=config)\n</code></pre>"},{"location":"api_reference/rendering/svg_native/#momapy.rendering.svg_native.SVGNativeRenderer.get_bolder_font_weight","title":"get_bolder_font_weight  <code>classmethod</code>","text":"<pre><code>get_bolder_font_weight(font_weight: FontWeight | float) -&gt; float\n</code></pre> <p>Return the lightest font weight bolder than the given font weight.</p> Source code in <code>src/momapy/rendering/core.py</code> <pre><code>@classmethod\ndef get_bolder_font_weight(cls, font_weight: FontWeight | float) -&gt; float:\n    \"\"\"Return the lightest font weight bolder than the given font weight.\"\"\"\n    if isinstance(font_weight, FontWeight):\n        font_weight = cls.font_weight_value_mapping.get(font_weight)\n        if font_weight is None:\n            raise ValueError(\n                f\"font weight must be a float, {FontWeight.NORMAL}, or {FontWeight.BOLD}\"\n            )\n    if font_weight &lt; 400:\n        new_font_weight = 400.0\n    elif font_weight &lt; 600:\n        new_font_weight = 700.0\n    else:\n        new_font_weight = 900.0\n    return new_font_weight\n</code></pre>"},{"location":"api_reference/rendering/svg_native/#momapy.rendering.svg_native.SVGNativeRenderer.get_lighter_font_weight","title":"get_lighter_font_weight  <code>classmethod</code>","text":"<pre><code>get_lighter_font_weight(font_weight: FontWeight | float) -&gt; float\n</code></pre> <p>Return the boldest font weight lighter than the given font weight.</p> Source code in <code>src/momapy/rendering/core.py</code> <pre><code>@classmethod\ndef get_lighter_font_weight(cls, font_weight: FontWeight | float) -&gt; float:\n    \"\"\"Return the boldest font weight lighter than the given font weight.\"\"\"\n    if isinstance(font_weight, FontWeight):\n        font_weight = cls.font_weight_value_mapping.get(font_weight)\n        if font_weight is None:\n            raise ValueError(\n                f\"font weight must be a float, {FontWeight.NORMAL}, or {FontWeight.BOLD}\"\n            )\n    if font_weight &gt; 700:\n        new_font_weight = 700.0\n    elif font_weight &gt; 500:\n        new_font_weight = 400.0\n    else:\n        new_font_weight = 100.0\n    return new_font_weight\n</code></pre>"},{"location":"api_reference/rendering/svg_native/#momapy.rendering.svg_native.SVGNativeRenderer.new_page","title":"new_page","text":"<pre><code>new_page(width: float, height: float) -&gt; None\n</code></pre> <p>Create a new page in the output document.</p> <p>Parameters:</p> Name Type Description Default <code>width</code> <code>float</code> <p>The width of the new page</p> required <code>height</code> <code>float</code> <p>The height of the new page</p> required Note <p>SVG format does not support multiple pages. This method is a no-op.</p> Source code in <code>src/momapy/rendering/svg_native.py</code> <pre><code>def new_page(self, width: float, height: float) -&gt; None:\n    \"\"\"Create a new page in the output document.\n\n    Args:\n        width: The width of the new page\n        height: The height of the new page\n\n    Note:\n        SVG format does not support multiple pages. This method is a no-op.\n    \"\"\"\n    pass\n</code></pre>"},{"location":"api_reference/rendering/svg_native/#momapy.rendering.svg_native.SVGNativeRenderer.render_drawing_element","title":"render_drawing_element","text":"<pre><code>render_drawing_element(drawing_element: DrawingElement) -&gt; None\n</code></pre> <p>Render a drawing element to the output.</p> <p>Parameters:</p> Name Type Description Default <code>drawing_element</code> <code>DrawingElement</code> <p>The drawing element to render</p> required <p>This method converts the drawing element to an SVG element and adds it to the SVG document.</p> Source code in <code>src/momapy/rendering/svg_native.py</code> <pre><code>def render_drawing_element(self, drawing_element: DrawingElement) -&gt; None:\n    \"\"\"Render a drawing element to the output.\n\n    Args:\n        drawing_element: The drawing element to render\n\n    This method converts the drawing element to an SVG element\n    and adds it to the SVG document.\n    \"\"\"\n    element = self._make_drawing_element_element(drawing_element)\n    self.svg.add_element(element)\n</code></pre>"},{"location":"api_reference/rendering/svg_native/#momapy.rendering.svg_native.SVGNativeRenderer.render_layout_element","title":"render_layout_element","text":"<pre><code>render_layout_element(layout_element: LayoutElement) -&gt; None\n</code></pre> <p>Render a layout element to the output.</p> <p>Parameters:</p> Name Type Description Default <code>layout_element</code> <code>LayoutElement</code> <p>The layout element to render</p> required Source code in <code>src/momapy/rendering/svg_native.py</code> <pre><code>def render_layout_element(self, layout_element: LayoutElement) -&gt; None:\n    \"\"\"Render a layout element to the output.\n\n    Args:\n        layout_element: The layout element to render\n    \"\"\"\n    drawing_elements = layout_element.drawing_elements()\n    for drawing_element in drawing_elements:\n        self.render_drawing_element(drawing_element)\n</code></pre>"},{"location":"api_reference/rendering/svg_native/#momapy.rendering.svg_native.SVGNativeRenderer.render_map","title":"render_map","text":"<pre><code>render_map(map_: Map) -&gt; None\n</code></pre> <p>Render a map.</p> <p>This is a convenience method, not part of the abstract contract: the default implementation renders the map's layout via :meth:<code>render_layout_element</code>, which is what every built-in backend needs. Subclasses may override it if a backend requires map-specific handling, but they are not obliged to. The file pipeline (:func:<code>render_map</code>/:func:<code>render_maps</code>) does not call this method; it renders each page through :meth:<code>render_layout_element</code>.</p> <p>Parameters:</p> Name Type Description Default <code>map_</code> <code>Map</code> <p>The map to render.</p> required <p>Raises:</p> Type Description <code>ValueError</code> <p>If the map has no layout to render.</p> Source code in <code>src/momapy/rendering/core.py</code> <pre><code>def render_map(self, map_: Map) -&gt; None:\n    \"\"\"Render a map.\n\n    This is a convenience method, **not** part of the abstract\n    contract: the default implementation renders the map's layout via\n    :meth:`render_layout_element`, which is what every built-in backend\n    needs. Subclasses may override it if a backend requires\n    map-specific handling, but they are not obliged to. The file\n    pipeline (:func:`render_map`/:func:`render_maps`) does not call this\n    method; it renders each page through :meth:`render_layout_element`.\n\n    Args:\n        map_: The map to render.\n\n    Raises:\n        ValueError: If the map has no layout to render.\n    \"\"\"\n    if map_.layout is None:\n        raise ValueError(\n            \"map has no layout to render (its layout is None); \"\n            \"a layout-less map (e.g. an SBML map) cannot be rendered\"\n        )\n    self.render_layout_element(map_.layout)\n</code></pre>"},{"location":"api_reference/sbgn/af/","title":"AF","text":""},{"location":"api_reference/sbgn/af/#momapy.sbgn.af","title":"momapy.sbgn.af","text":"<p>SBGN Activity Flow (AF) subpackage facade.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af--layout-model-mapping-catalogue","title":"Layout-model mapping catalogue","text":"<p>This section lists, for each model-element category in SBGN-AF, the shape of the corresponding key in LayoutModelMapping. See LayoutModelMapping for the general concepts (singleton keys, frozenset keys, anchors).</p> <p>Singleton keys (one layout element represents the model element):</p> Model element Layout element used as the key Compartment CompartmentLayout BiologicalActivity BiologicalActivityLayout Phenotype PhenotypeLayout UnitOfInformation and subclasses (e.g. MacromoleculeUnitOfInformation, NucleicAcidFeatureUnitOfInformation, SimpleChemicalUnitOfInformation, ComplexUnitOfInformation, UnspecifiedEntityUnitOfInformation, PerturbationUnitOfInformation) The corresponding <code>*UnitOfInformationLayout</code> (e.g. MacromoleculeUnitOfInformationLayout, PerturbationUnitOfInformationLayout) Submap SubmapLayout LogicalOperatorInput LogicArcLayout TagReference, TerminalReference EquivalenceArcLayout <p>Frozenset keys (a cluster of layout elements jointly represents the model element; the anchor is the layout that stands for the cluster on its own and must be passed as <code>anchor=</code> when calling add_mapping):</p> Model element Members of the frozenset key Anchor LogicalOperator and subclasses (e.g. AndOperator, OrOperator, NotOperator, DelayOperator) The operator <code>*Layout</code> (e.g. AndOperatorLayout, DelayOperatorLayout) + every LogicArcLayout input + every target layout those logic arcs point to The operator <code>*Layout</code> Influence and subclasses (e.g. UnknownInfluence, PositiveInfluence, NegativeInfluence, NecessaryStimulation) The influence arc layout (e.g. UnknownInfluenceLayout, PositiveInfluenceLayout, NecessaryStimulationLayout) + all layouts in the source cluster (resolved via the source's own frozenset key if it has one, else the source layout itself) + all layouts in the target cluster (resolved the same way) The influence arc layout Tag or Terminal carrying TagReference or TerminalReference arcs The TagLayout or TerminalLayout + every EquivalenceArcLayout reference arc + every referenced activity layout The TagLayout or TerminalLayout <p>Standalone Tag and Terminal instances (with no reference arcs) use a singleton key: TagLayout or TerminalLayout.</p> <p>Modules:</p> Name Description <code>layout</code> <p>Layout classes for SBGN Activity Flow (AF) maps.</p> <code>map</code> <p>Map classes for SBGN Activity Flow (AF) maps.</p> <code>model</code> <p>Model classes for SBGN Activity Flow (AF) maps.</p> <p>Classes:</p> Name Description <code>Activity</code> <p>Abstract base class for activities.</p> <code>AndOperator</code> <p>AND operator.</p> <code>AndOperatorLayout</code> <p>AND operator layout.</p> <code>BiologicalActivity</code> <p>Biological activity.</p> <code>BiologicalActivityLayout</code> <p>Biological activity layout.</p> <code>Compartment</code> <p>Compartment.</p> <code>CompartmentLayout</code> <p>Compartment layout.</p> <code>ComplexUnitOfInformation</code> <p>Complex unit of information.</p> <code>ComplexUnitOfInformationLayout</code> <p>Complex unit of information layout.</p> <code>DelayOperator</code> <p>Delay operator.</p> <code>DelayOperatorLayout</code> <p>Delay operator layout.</p> <code>EquivalenceArcLayout</code> <p>Equivalence arc layout.</p> <code>Influence</code> <p>Abstract base class for influences.</p> <code>LogicArcLayout</code> <p>Logic arc layout.</p> <code>LogicalOperator</code> <p>Abstract base class for logical operators.</p> <code>LogicalOperatorInput</code> <p>Logical operator input.</p> <code>MacromoleculeUnitOfInformation</code> <p>Macromolecule unit of information.</p> <code>MacromoleculeUnitOfInformationLayout</code> <p>Macromolecule unit of information layout.</p> <code>NecessaryStimulation</code> <p>Necessary stimulation.</p> <code>NecessaryStimulationLayout</code> <p>Necessary stimulation layout.</p> <code>NegativeInfluence</code> <p>Negative influence.</p> <code>NegativeInfluenceLayout</code> <p>Negative influence layout.</p> <code>NotOperator</code> <p>NOT operator.</p> <code>NotOperatorLayout</code> <p>NOT operator layout.</p> <code>NucleicAcidFeatureUnitOfInformation</code> <p>Nucleic acid feature unit of information.</p> <code>NucleicAcidFeatureUnitOfInformationLayout</code> <p>Nucleic acid feature unit of information layout.</p> <code>OrOperator</code> <p>OR operator.</p> <code>OrOperatorLayout</code> <p>OR operator layout.</p> <code>PerturbationUnitOfInformation</code> <p>Perturbation unit of information.</p> <code>PerturbationUnitOfInformationLayout</code> <p>Perturbation unit of information layout.</p> <code>Phenotype</code> <p>Phenotype.</p> <code>PhenotypeLayout</code> <p>Phenotype layout.</p> <code>PositiveInfluence</code> <p>Positive influence.</p> <code>PositiveInfluenceLayout</code> <p>Positive influence layout.</p> <code>SBGNAFLayout</code> <p>SBGN AF layout.</p> <code>SBGNAFMap</code> <p>Class for SBGN-AF maps.</p> <code>SBGNAFModel</code> <p>SBGN AF model.</p> <code>SimpleChemicalUnitOfInformation</code> <p>Simple chemical unit of information.</p> <code>SimpleChemicalUnitOfInformationLayout</code> <p>Simple chemical unit of information layout.</p> <code>Submap</code> <p>Submap.</p> <code>SubmapLayout</code> <p>Submap layout.</p> <code>Tag</code> <p>Tag.</p> <code>TagLayout</code> <p>Tag layout.</p> <code>TagReference</code> <p>Tag reference.</p> <code>Terminal</code> <p>Terminal.</p> <code>TerminalLayout</code> <p>Terminal layout.</p> <code>TerminalReference</code> <p>Terminal reference.</p> <code>UnitOfInformation</code> <p>Abstract base class for units of information.</p> <code>UnitOfInformationLayout</code> <p>Unit of information layout.</p> <code>UnknownInfluence</code> <p>Unknown influence.</p> <code>UnknownInfluenceLayout</code> <p>Unknown influence layout.</p> <code>UnspecifiedEntityUnitOfInformation</code> <p>Unspecified entity unit of information.</p> <code>UnspecifiedEntityUnitOfInformationLayout</code> <p>Unspecified entity unit of information layout.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.Activity","title":"Activity  <code>dataclass</code>","text":"<pre><code>Activity(*, id_: str = make_uuid4_as_str(), label: str | None = None, compartment: Compartment | None = None)\n</code></pre> <p>               Bases: <code>SBGNModelElement</code></p> <p>Abstract base class for activities.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'ba54c304-9dae-4176-91b8-dffb39afc578'</code> <code>label</code> <code>str | None</code> <p>The label of the activity.</p> <code>None</code> <code>compartment</code> <code>Compartment | None</code> <p>The compartment containing this activity.</p> <code>None</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.Activity.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.AndOperator","title":"AndOperator  <code>dataclass</code>","text":"<pre><code>AndOperator(*, id_: str = make_uuid4_as_str(), inputs: frozenset[LogicalOperatorInput] = frozenset())\n</code></pre> <p>               Bases: <code>LogicalOperator</code></p> <p>AND operator.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'a813f262-5290-43bd-80dd-428e6148a969'</code> <code>inputs</code> <code>frozenset[LogicalOperatorInput]</code> <p>Input connections to the logical operator.</p> <code>&lt;dynamic&gt;</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.AndOperator.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.AndOperatorLayout","title":"AndOperatorLayout  <code>dataclass</code>","text":"<pre><code>AndOperatorLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = white, filter_: NoneValueType | Filter | None = None, height: float = 30.0, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = 1.25, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 30.0, text: str = 'AND', font_family: str = DEFAULT_FONT_FAMILY, font_fill: Color | NoneValueType = black, font_stroke: Color | NoneValueType = NoneValue, font_style: FontStyle = NORMAL, font_weight: FontWeight | float = NORMAL, orientation: Orientation = HORIZONTAL, left_to_right: bool = True, left_connector_length: float = 10.0, right_connector_length: float = 10.0, left_connector_stroke: NoneValueType | Color | None = None, left_connector_stroke_width: float | None = None, left_connector_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, left_connector_stroke_dashoffset: float | None = None, left_connector_fill: NoneValueType | Color | None = None, left_connector_transform: NoneValueType | tuple[Transformation, ...] | None = None, left_connector_filter: NoneValueType | Filter | None = None, right_connector_stroke: NoneValueType | Color | None = None, right_connector_stroke_width: float | None = None, right_connector_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, right_connector_stroke_dashoffset: float | None = None, right_connector_fill: NoneValueType | Color | None = None, right_connector_transform: NoneValueType | tuple[Transformation, ...] | None = None, right_connector_filter: NoneValueType | Filter | None = None)\n</code></pre> <p>               Bases: <code>_ConnectorsMixin</code>, <code>_SimpleMixin</code>, <code>_TextMixin</code>, <code>SBGNNode</code></p> <p>AND operator layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'796226cc-0dc0-4d64-93aa-ff12de6e6aa7'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>30.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The stroke width of the node</p> <code>1.25</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>30.0</code> <code>text</code> <code>str</code> <p>The text displayed inside the node.</p> <code>'AND'</code> <code>font_family</code> <code>str</code> <p>Font family for the text.</p> <code>'DejaVu Sans'</code> <code>font_fill</code> <code>Color | NoneValueType</code> <p>Fill color for the text.</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>font_stroke</code> <code>Color | NoneValueType</code> <p>Stroke color for the text outline.</p> <code>&lt;momapy.drawing.NoneValueType object at 0x7f8b17323e00&gt;</code> <code>font_style</code> <code>FontStyle</code> <p>Font style (normal, italic, etc.).</p> <code>&lt;FontStyle.NORMAL: 0&gt;</code> <code>font_weight</code> <code>FontWeight | float</code> <p>Font weight (normal, bold, etc.).</p> <code>&lt;FontWeight.NORMAL: 0&gt;</code> <code>orientation</code> <code>Orientation</code> <p>Orientation of connectors (HORIZONTAL or VERTICAL).</p> <code>&lt;Orientation.HORIZONTAL: 1&gt;</code> <code>left_to_right</code> <code>bool</code> <p>Whether connectors flow left to right.</p> <code>True</code> <code>left_connector_length</code> <code>float</code> <p>Length of the left connector.</p> <code>10.0</code> <code>right_connector_length</code> <code>float</code> <p>Length of the right connector.</p> <code>10.0</code> <code>left_connector_stroke</code> <code>NoneValueType | Color | None</code> <p>Stroke color for the left connector line.</p> <code>None</code> <code>left_connector_stroke_width</code> <code>float | None</code> <p>Stroke width for the left connector line.</p> <code>None</code> <code>left_connector_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>Dash pattern for the left connector line.</p> <code>None</code> <code>left_connector_stroke_dashoffset</code> <code>float | None</code> <p>Dash offset for the left connector line.</p> <code>None</code> <code>left_connector_fill</code> <code>NoneValueType | Color | None</code> <p>Fill color for the left connector.</p> <code>None</code> <code>left_connector_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>Transformations applied to the left connector.</p> <code>None</code> <code>left_connector_filter</code> <code>NoneValueType | Filter | None</code> <p>Filter applied to the left connector.</p> <code>None</code> <code>right_connector_stroke</code> <code>NoneValueType | Color | None</code> <p>Stroke color for the right connector line.</p> <code>None</code> <code>right_connector_stroke_width</code> <code>float | None</code> <p>Stroke width for the right connector line.</p> <code>None</code> <code>right_connector_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>Dash pattern for the right connector line.</p> <code>None</code> <code>right_connector_stroke_dashoffset</code> <code>float | None</code> <p>Dash offset for the right connector line.</p> <code>None</code> <code>right_connector_fill</code> <code>NoneValueType | Color | None</code> <p>Fill color for the right connector.</p> <code>None</code> <code>right_connector_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>Transformations applied to the right connector.</p> <code>None</code> <code>right_connector_filter</code> <code>NoneValueType | Filter | None</code> <p>Filter applied to the right connector.</p> <code>None</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>angle</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Get the east (right) anchor point.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Return the label center anchor of the node.</p> <code>left_connector_base</code> <p>Get the base point of the left connector.</p> <code>left_connector_tip</code> <p>Get the tip point of the left connector.</p> <code>north</code> <p>Get the north (top) anchor point.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>right_connector_base</code> <p>Get the base point of the right connector.</p> <code>right_connector_tip</code> <p>Get the tip point of the right connector.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Get the south (bottom) anchor point.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Get the west (left) anchor point.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.AndOperatorLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.AndOperatorLayout.angle","title":"angle","text":"<pre><code>angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.AndOperatorLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.AndOperatorLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.AndOperatorLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.AndOperatorLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.AndOperatorLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.AndOperatorLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.AndOperatorLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.AndOperatorLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.AndOperatorLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Get the east (right) anchor point.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point on the east side of the element.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Get the east (right) anchor point.\n\n    Returns:\n        Point on the east side of the element.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x + self.width / 2, self.y)\n    else:\n        return Point(self.x + self.width / 2 + self.right_connector_length, self.y)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.AndOperatorLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.AndOperatorLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.AndOperatorLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.AndOperatorLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.AndOperatorLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Return the label center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Return the label center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.AndOperatorLayout.left_connector_base","title":"left_connector_base","text":"<pre><code>left_connector_base() -&gt; Point\n</code></pre> <p>Get the base point of the left connector.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point where the left connector attaches to the shape.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def left_connector_base(self) -&gt; Point:\n    \"\"\"Get the base point of the left connector.\n\n    Returns:\n        Point where the left connector attaches to the shape.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x, self.y - self.height / 2)\n    else:\n        return Point(self.x - self.width / 2, self.y)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.AndOperatorLayout.left_connector_tip","title":"left_connector_tip","text":"<pre><code>left_connector_tip() -&gt; Point\n</code></pre> <p>Get the tip point of the left connector.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point at the end of the left connector line.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def left_connector_tip(self) -&gt; Point:\n    \"\"\"Get the tip point of the left connector.\n\n    Returns:\n        Point at the end of the left connector line.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x, self.y - self.height / 2 - self.left_connector_length)\n    else:\n        return Point(self.x - self.width / 2 - self.left_connector_length, self.y)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.AndOperatorLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Get the north (top) anchor point.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point on the north side of the element.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Get the north (top) anchor point.\n\n    Returns:\n        Point on the north side of the element.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x, self.y - self.height / 2 - self.left_connector_length)\n    else:\n        return Point(self.x, self.y - self.height / 2)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.AndOperatorLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.AndOperatorLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.AndOperatorLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.AndOperatorLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.AndOperatorLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.AndOperatorLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.AndOperatorLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.AndOperatorLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.AndOperatorLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.AndOperatorLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.AndOperatorLayout.right_connector_base","title":"right_connector_base","text":"<pre><code>right_connector_base() -&gt; Point\n</code></pre> <p>Get the base point of the right connector.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point where the right connector attaches to the shape.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def right_connector_base(self) -&gt; Point:\n    \"\"\"Get the base point of the right connector.\n\n    Returns:\n        Point where the right connector attaches to the shape.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x, self.y + self.height / 2)\n    else:\n        return Point(self.x + self.width / 2, self.y)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.AndOperatorLayout.right_connector_tip","title":"right_connector_tip","text":"<pre><code>right_connector_tip() -&gt; Point\n</code></pre> <p>Get the tip point of the right connector.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point at the end of the right connector line.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def right_connector_tip(self) -&gt; Point:\n    \"\"\"Get the tip point of the right connector.\n\n    Returns:\n        Point at the end of the right connector line.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x, self.y + self.height / 2 + self.right_connector_length)\n    else:\n        return Point(self.x + self.width / 2 + self.right_connector_length, self.y)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.AndOperatorLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.AndOperatorLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Get the south (bottom) anchor point.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point on the south side of the element.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Get the south (bottom) anchor point.\n\n    Returns:\n        Point on the south side of the element.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x, self.y + self.height / 2 + self.right_connector_length)\n    else:\n        return Point(self.x, self.y + self.height / 2)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.AndOperatorLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.AndOperatorLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.AndOperatorLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.AndOperatorLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.AndOperatorLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.AndOperatorLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Get the west (left) anchor point.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point on the west side of the element.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Get the west (left) anchor point.\n\n    Returns:\n        Point on the west side of the element.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x - self.width / 2, self.y)\n    else:\n        return Point(self.x - self.width / 2 - self.left_connector_length, self.y)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.AndOperatorLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.AndOperatorLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.AndOperatorLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.AndOperatorLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.BiologicalActivity","title":"BiologicalActivity  <code>dataclass</code>","text":"<pre><code>BiologicalActivity(*, id_: str = make_uuid4_as_str(), label: str | None = None, compartment: Compartment | None = None, units_of_information: frozenset[UnitOfInformation] = frozenset())\n</code></pre> <p>               Bases: <code>Activity</code></p> <p>Biological activity.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'1da210a0-661f-4201-93f8-0456b4ac6fa1'</code> <code>label</code> <code>str | None</code> <p>The label of the activity.</p> <code>None</code> <code>compartment</code> <code>Compartment | None</code> <p>The compartment containing this activity.</p> <code>None</code> <code>units_of_information</code> <code>frozenset[UnitOfInformation]</code> <p>Units of information for the activity.</p> <code>&lt;dynamic&gt;</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.BiologicalActivity.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.BiologicalActivityLayout","title":"BiologicalActivityLayout  <code>dataclass</code>","text":"<pre><code>BiologicalActivityLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = white, filter_: NoneValueType | Filter | None = None, height: float = 30.0, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = 1.25, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 60.0)\n</code></pre> <p>               Bases: <code>_SimpleMixin</code>, <code>SBGNNode</code></p> <p>Biological activity layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'941ca10d-1be5-4104-83e6-3cfa1dd4ba55'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>30.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The stroke width of the node</p> <code>1.25</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>60.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>angle</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Return the east anchor of the node.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Return the label center anchor of the node.</p> <code>north</code> <p>Return the north anchor of the node.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Return the south anchor of the node.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Return the west anchor of the node.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.BiologicalActivityLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.BiologicalActivityLayout.angle","title":"angle","text":"<pre><code>angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.BiologicalActivityLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.BiologicalActivityLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.BiologicalActivityLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.BiologicalActivityLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.BiologicalActivityLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.BiologicalActivityLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.BiologicalActivityLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.BiologicalActivityLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.BiologicalActivityLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Return the east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Return the east anchor of the node.\"\"\"\n    return self.own_angle(0)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.BiologicalActivityLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.BiologicalActivityLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.BiologicalActivityLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.BiologicalActivityLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.BiologicalActivityLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Return the label center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Return the label center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.BiologicalActivityLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Return the north anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Return the north anchor of the node.\"\"\"\n    return self.own_angle(90)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.BiologicalActivityLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.BiologicalActivityLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.BiologicalActivityLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.BiologicalActivityLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.BiologicalActivityLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.BiologicalActivityLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.BiologicalActivityLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.BiologicalActivityLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.BiologicalActivityLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.BiologicalActivityLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.BiologicalActivityLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.BiologicalActivityLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Return the south anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Return the south anchor of the node.\"\"\"\n    return self.own_angle(270)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.BiologicalActivityLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.BiologicalActivityLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.BiologicalActivityLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.BiologicalActivityLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.BiologicalActivityLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.BiologicalActivityLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Return the west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Return the west anchor of the node.\"\"\"\n    return self.own_angle(180)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.BiologicalActivityLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.BiologicalActivityLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.BiologicalActivityLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.BiologicalActivityLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.Compartment","title":"Compartment  <code>dataclass</code>","text":"<pre><code>Compartment(*, id_: str = make_uuid4_as_str(), label: str | None = None, units_of_information: frozenset[UnitOfInformation] = frozenset())\n</code></pre> <p>               Bases: <code>SBGNModelElement</code></p> <p>Compartment.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'b078e661-5d76-428b-a7d1-68ed78c2069a'</code> <code>label</code> <code>str | None</code> <p>The label of the compartment.</p> <code>None</code> <code>units_of_information</code> <code>frozenset[UnitOfInformation]</code> <p>The units of information of the compartment.</p> <code>&lt;dynamic&gt;</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.Compartment.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.CompartmentLayout","title":"CompartmentLayout  <code>dataclass</code>","text":"<pre><code>CompartmentLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = white, filter_: NoneValueType | Filter | None = None, height: float = 80.0, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float = 3.25, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 80.0, rounded_corners: float = 5.0)\n</code></pre> <p>               Bases: <code>_SimpleMixin</code>, <code>SBGNNode</code></p> <p>Compartment layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'3e60e5fd-62bd-4411-a0f4-50287b9f040b'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>80.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float</code> <p>The stroke width of the node</p> <code>3.25</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>80.0</code> <code>rounded_corners</code> <code>float</code> <p>The radius of the rounded corners.</p> <code>5.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>angle</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Return the east anchor of the node.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Return the label center anchor of the node.</p> <code>north</code> <p>Return the north anchor of the node.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Return the south anchor of the node.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Return the west anchor of the node.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.CompartmentLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.CompartmentLayout.angle","title":"angle","text":"<pre><code>angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.CompartmentLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.CompartmentLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.CompartmentLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.CompartmentLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.CompartmentLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.CompartmentLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.CompartmentLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.CompartmentLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.CompartmentLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Return the east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Return the east anchor of the node.\"\"\"\n    return self.own_angle(0)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.CompartmentLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.CompartmentLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.CompartmentLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.CompartmentLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.CompartmentLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Return the label center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Return the label center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.CompartmentLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Return the north anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Return the north anchor of the node.\"\"\"\n    return self.own_angle(90)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.CompartmentLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.CompartmentLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.CompartmentLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.CompartmentLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.CompartmentLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.CompartmentLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.CompartmentLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.CompartmentLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.CompartmentLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.CompartmentLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.CompartmentLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.CompartmentLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Return the south anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Return the south anchor of the node.\"\"\"\n    return self.own_angle(270)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.CompartmentLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.CompartmentLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.CompartmentLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.CompartmentLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.CompartmentLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.CompartmentLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Return the west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Return the west anchor of the node.\"\"\"\n    return self.own_angle(180)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.CompartmentLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.CompartmentLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.CompartmentLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.CompartmentLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.ComplexUnitOfInformation","title":"ComplexUnitOfInformation  <code>dataclass</code>","text":"<pre><code>ComplexUnitOfInformation(*, id_: str = make_uuid4_as_str(), label: str | None = None)\n</code></pre> <p>               Bases: <code>UnitOfInformation</code></p> <p>Complex unit of information.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'ee8d1f5a-b2c6-4a9d-865f-8625a01ae603'</code> <code>label</code> <code>str | None</code> <p>The label of the unit of information.</p> <code>None</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.ComplexUnitOfInformation.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.ComplexUnitOfInformationLayout","title":"ComplexUnitOfInformationLayout  <code>dataclass</code>","text":"<pre><code>ComplexUnitOfInformationLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = white, filter_: NoneValueType | Filter | None = None, height: float = 12.0, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = 1.25, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 18.0, cut_corners: float = 3.0)\n</code></pre> <p>               Bases: <code>_SimpleMixin</code>, <code>SBGNNode</code></p> <p>Complex unit of information layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'20312679-c04b-4eef-a0b7-d223fefdadb7'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>12.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The stroke width of the node</p> <code>1.25</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>18.0</code> <code>cut_corners</code> <code>float</code> <p>The size of the cut corners.</p> <code>3.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>angle</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Return the east anchor of the node.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Return the label center anchor of the node.</p> <code>north</code> <p>Return the north anchor of the node.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Return the south anchor of the node.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Return the west anchor of the node.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.ComplexUnitOfInformationLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.ComplexUnitOfInformationLayout.angle","title":"angle","text":"<pre><code>angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.ComplexUnitOfInformationLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.ComplexUnitOfInformationLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.ComplexUnitOfInformationLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.ComplexUnitOfInformationLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.ComplexUnitOfInformationLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.ComplexUnitOfInformationLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.ComplexUnitOfInformationLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.ComplexUnitOfInformationLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.ComplexUnitOfInformationLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Return the east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Return the east anchor of the node.\"\"\"\n    return self.own_angle(0)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.ComplexUnitOfInformationLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.ComplexUnitOfInformationLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.ComplexUnitOfInformationLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.ComplexUnitOfInformationLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.ComplexUnitOfInformationLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Return the label center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Return the label center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.ComplexUnitOfInformationLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Return the north anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Return the north anchor of the node.\"\"\"\n    return self.own_angle(90)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.ComplexUnitOfInformationLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.ComplexUnitOfInformationLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.ComplexUnitOfInformationLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.ComplexUnitOfInformationLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.ComplexUnitOfInformationLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.ComplexUnitOfInformationLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.ComplexUnitOfInformationLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.ComplexUnitOfInformationLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.ComplexUnitOfInformationLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.ComplexUnitOfInformationLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.ComplexUnitOfInformationLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.ComplexUnitOfInformationLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Return the south anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Return the south anchor of the node.\"\"\"\n    return self.own_angle(270)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.ComplexUnitOfInformationLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.ComplexUnitOfInformationLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.ComplexUnitOfInformationLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.ComplexUnitOfInformationLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.ComplexUnitOfInformationLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.ComplexUnitOfInformationLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Return the west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Return the west anchor of the node.\"\"\"\n    return self.own_angle(180)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.ComplexUnitOfInformationLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.ComplexUnitOfInformationLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.ComplexUnitOfInformationLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.ComplexUnitOfInformationLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.DelayOperator","title":"DelayOperator  <code>dataclass</code>","text":"<pre><code>DelayOperator(*, id_: str = make_uuid4_as_str(), inputs: frozenset[LogicalOperatorInput] = frozenset())\n</code></pre> <p>               Bases: <code>LogicalOperator</code></p> <p>Delay operator.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'ae3fa1d0-069e-495f-80e7-15419f0ac4c8'</code> <code>inputs</code> <code>frozenset[LogicalOperatorInput]</code> <p>Input connections to the logical operator.</p> <code>&lt;dynamic&gt;</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.DelayOperator.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.DelayOperatorLayout","title":"DelayOperatorLayout  <code>dataclass</code>","text":"<pre><code>DelayOperatorLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = white, filter_: NoneValueType | Filter | None = None, height: float = 30.0, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = 1.25, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 30.0, text: str = '\u03c4', font_family: str = DEFAULT_FONT_FAMILY, font_fill: Color | NoneValueType = black, font_stroke: Color | NoneValueType = NoneValue, font_style: FontStyle = NORMAL, font_weight: FontWeight | float = NORMAL, orientation: Orientation = HORIZONTAL, left_to_right: bool = True, left_connector_length: float = 10.0, right_connector_length: float = 10.0, left_connector_stroke: NoneValueType | Color | None = None, left_connector_stroke_width: float | None = None, left_connector_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, left_connector_stroke_dashoffset: float | None = None, left_connector_fill: NoneValueType | Color | None = None, left_connector_transform: NoneValueType | tuple[Transformation, ...] | None = None, left_connector_filter: NoneValueType | Filter | None = None, right_connector_stroke: NoneValueType | Color | None = None, right_connector_stroke_width: float | None = None, right_connector_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, right_connector_stroke_dashoffset: float | None = None, right_connector_fill: NoneValueType | Color | None = None, right_connector_transform: NoneValueType | tuple[Transformation, ...] | None = None, right_connector_filter: NoneValueType | Filter | None = None)\n</code></pre> <p>               Bases: <code>_ConnectorsMixin</code>, <code>_SimpleMixin</code>, <code>_TextMixin</code>, <code>SBGNNode</code></p> <p>Delay operator layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'7cf7602f-294b-4b42-a0e6-ea994e67425a'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>30.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The stroke width of the node</p> <code>1.25</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>30.0</code> <code>text</code> <code>str</code> <p>The text displayed inside the node.</p> <code>'\u03c4'</code> <code>font_family</code> <code>str</code> <p>Font family for the text.</p> <code>'DejaVu Sans'</code> <code>font_fill</code> <code>Color | NoneValueType</code> <p>Fill color for the text.</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>font_stroke</code> <code>Color | NoneValueType</code> <p>Stroke color for the text outline.</p> <code>&lt;momapy.drawing.NoneValueType object at 0x7f8b17323e00&gt;</code> <code>font_style</code> <code>FontStyle</code> <p>Font style (normal, italic, etc.).</p> <code>&lt;FontStyle.NORMAL: 0&gt;</code> <code>font_weight</code> <code>FontWeight | float</code> <p>Font weight (normal, bold, etc.).</p> <code>&lt;FontWeight.NORMAL: 0&gt;</code> <code>orientation</code> <code>Orientation</code> <p>Orientation of connectors (HORIZONTAL or VERTICAL).</p> <code>&lt;Orientation.HORIZONTAL: 1&gt;</code> <code>left_to_right</code> <code>bool</code> <p>Whether connectors flow left to right.</p> <code>True</code> <code>left_connector_length</code> <code>float</code> <p>Length of the left connector.</p> <code>10.0</code> <code>right_connector_length</code> <code>float</code> <p>Length of the right connector.</p> <code>10.0</code> <code>left_connector_stroke</code> <code>NoneValueType | Color | None</code> <p>Stroke color for the left connector line.</p> <code>None</code> <code>left_connector_stroke_width</code> <code>float | None</code> <p>Stroke width for the left connector line.</p> <code>None</code> <code>left_connector_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>Dash pattern for the left connector line.</p> <code>None</code> <code>left_connector_stroke_dashoffset</code> <code>float | None</code> <p>Dash offset for the left connector line.</p> <code>None</code> <code>left_connector_fill</code> <code>NoneValueType | Color | None</code> <p>Fill color for the left connector.</p> <code>None</code> <code>left_connector_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>Transformations applied to the left connector.</p> <code>None</code> <code>left_connector_filter</code> <code>NoneValueType | Filter | None</code> <p>Filter applied to the left connector.</p> <code>None</code> <code>right_connector_stroke</code> <code>NoneValueType | Color | None</code> <p>Stroke color for the right connector line.</p> <code>None</code> <code>right_connector_stroke_width</code> <code>float | None</code> <p>Stroke width for the right connector line.</p> <code>None</code> <code>right_connector_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>Dash pattern for the right connector line.</p> <code>None</code> <code>right_connector_stroke_dashoffset</code> <code>float | None</code> <p>Dash offset for the right connector line.</p> <code>None</code> <code>right_connector_fill</code> <code>NoneValueType | Color | None</code> <p>Fill color for the right connector.</p> <code>None</code> <code>right_connector_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>Transformations applied to the right connector.</p> <code>None</code> <code>right_connector_filter</code> <code>NoneValueType | Filter | None</code> <p>Filter applied to the right connector.</p> <code>None</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>angle</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Get the east (right) anchor point.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Return the label center anchor of the node.</p> <code>left_connector_base</code> <p>Get the base point of the left connector.</p> <code>left_connector_tip</code> <p>Get the tip point of the left connector.</p> <code>north</code> <p>Get the north (top) anchor point.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>right_connector_base</code> <p>Get the base point of the right connector.</p> <code>right_connector_tip</code> <p>Get the tip point of the right connector.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Get the south (bottom) anchor point.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Get the west (left) anchor point.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.DelayOperatorLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.DelayOperatorLayout.angle","title":"angle","text":"<pre><code>angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.DelayOperatorLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.DelayOperatorLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.DelayOperatorLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.DelayOperatorLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.DelayOperatorLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.DelayOperatorLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.DelayOperatorLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.DelayOperatorLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.DelayOperatorLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Get the east (right) anchor point.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point on the east side of the element.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Get the east (right) anchor point.\n\n    Returns:\n        Point on the east side of the element.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x + self.width / 2, self.y)\n    else:\n        return Point(self.x + self.width / 2 + self.right_connector_length, self.y)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.DelayOperatorLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.DelayOperatorLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.DelayOperatorLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.DelayOperatorLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.DelayOperatorLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Return the label center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Return the label center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.DelayOperatorLayout.left_connector_base","title":"left_connector_base","text":"<pre><code>left_connector_base() -&gt; Point\n</code></pre> <p>Get the base point of the left connector.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point where the left connector attaches to the shape.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def left_connector_base(self) -&gt; Point:\n    \"\"\"Get the base point of the left connector.\n\n    Returns:\n        Point where the left connector attaches to the shape.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x, self.y - self.height / 2)\n    else:\n        return Point(self.x - self.width / 2, self.y)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.DelayOperatorLayout.left_connector_tip","title":"left_connector_tip","text":"<pre><code>left_connector_tip() -&gt; Point\n</code></pre> <p>Get the tip point of the left connector.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point at the end of the left connector line.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def left_connector_tip(self) -&gt; Point:\n    \"\"\"Get the tip point of the left connector.\n\n    Returns:\n        Point at the end of the left connector line.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x, self.y - self.height / 2 - self.left_connector_length)\n    else:\n        return Point(self.x - self.width / 2 - self.left_connector_length, self.y)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.DelayOperatorLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Get the north (top) anchor point.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point on the north side of the element.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Get the north (top) anchor point.\n\n    Returns:\n        Point on the north side of the element.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x, self.y - self.height / 2 - self.left_connector_length)\n    else:\n        return Point(self.x, self.y - self.height / 2)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.DelayOperatorLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.DelayOperatorLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.DelayOperatorLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.DelayOperatorLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.DelayOperatorLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.DelayOperatorLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.DelayOperatorLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.DelayOperatorLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.DelayOperatorLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.DelayOperatorLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.DelayOperatorLayout.right_connector_base","title":"right_connector_base","text":"<pre><code>right_connector_base() -&gt; Point\n</code></pre> <p>Get the base point of the right connector.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point where the right connector attaches to the shape.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def right_connector_base(self) -&gt; Point:\n    \"\"\"Get the base point of the right connector.\n\n    Returns:\n        Point where the right connector attaches to the shape.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x, self.y + self.height / 2)\n    else:\n        return Point(self.x + self.width / 2, self.y)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.DelayOperatorLayout.right_connector_tip","title":"right_connector_tip","text":"<pre><code>right_connector_tip() -&gt; Point\n</code></pre> <p>Get the tip point of the right connector.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point at the end of the right connector line.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def right_connector_tip(self) -&gt; Point:\n    \"\"\"Get the tip point of the right connector.\n\n    Returns:\n        Point at the end of the right connector line.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x, self.y + self.height / 2 + self.right_connector_length)\n    else:\n        return Point(self.x + self.width / 2 + self.right_connector_length, self.y)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.DelayOperatorLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.DelayOperatorLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Get the south (bottom) anchor point.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point on the south side of the element.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Get the south (bottom) anchor point.\n\n    Returns:\n        Point on the south side of the element.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x, self.y + self.height / 2 + self.right_connector_length)\n    else:\n        return Point(self.x, self.y + self.height / 2)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.DelayOperatorLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.DelayOperatorLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.DelayOperatorLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.DelayOperatorLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.DelayOperatorLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.DelayOperatorLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Get the west (left) anchor point.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point on the west side of the element.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Get the west (left) anchor point.\n\n    Returns:\n        Point on the west side of the element.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x - self.width / 2, self.y)\n    else:\n        return Point(self.x - self.width / 2 - self.left_connector_length, self.y)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.DelayOperatorLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.DelayOperatorLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.DelayOperatorLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.DelayOperatorLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.EquivalenceArcLayout","title":"EquivalenceArcLayout  <code>dataclass</code>","text":"<pre><code>EquivalenceArcLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, end_shorten: float = 0.0, fill: NoneValueType | Color | None = None, filter_: NoneValueType | Filter | None = None, path_fill: NoneValueType | Color | None = NoneValue, path_filter: NoneValueType | Filter | None = None, path_stroke: NoneValueType | Color | None = black, path_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, path_stroke_dashoffset: NoneValueType | float | None = None, path_stroke_width: float | None = 1.25, path_transform: NoneValueType | tuple[Transformation, ...] | None = None, stroke: NoneValueType | Color | None = None, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: NoneValueType | float | None = None, segments: tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...] = tuple(), source: LayoutElement | None = None, start_shorten: float = 0.0, target: LayoutElement | None = None, transform: NoneValueType | tuple[Transformation, ...] | None = None, arrowhead_fill: NoneValueType | Color | None = white, arrowhead_filter: NoneValueType | Filter | None = None, arrowhead_stroke: NoneValueType | Color | None = black, arrowhead_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, arrowhead_stroke_dashoffset: NoneValueType | float | None = None, arrowhead_stroke_width: float | None = 1.25, arrowhead_transform: NoneValueType | tuple[Transformation, ...] | None = None)\n</code></pre> <p>               Bases: <code>SBGNSingleHeadedArc</code></p> <p>Equivalence arc layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'8dd7e0b0-a666-48b3-adc1-51746d1f0958'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>end_shorten</code> <code>float</code> <p>The length the end of the arc will be shorten by</p> <code>0.0</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the arc</p> <code>None</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the arc</p> <code>None</code> <code>path_fill</code> <code>NoneValueType | Color | None</code> <p>The path fill color of the arc</p> <code>&lt;momapy.drawing.NoneValueType object at 0x7f8b17323e00&gt;</code> <code>path_filter</code> <code>NoneValueType | Filter | None</code> <p>The path filter of the arc</p> <code>None</code> <code>path_stroke</code> <code>NoneValueType | Color | None</code> <p>The path stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>path_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The path stroke dasharray of the arc</p> <code>None</code> <code>path_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The path stroke dashoffset of the arc</p> <code>None</code> <code>path_stroke_width</code> <code>float | None</code> <p>The path stroke width of the arc</p> <code>1.25</code> <code>path_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The path transform of the arc</p> <code>None</code> <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the arc</p> <code>None</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the arc</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the arc</p> <code>None</code> <code>stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the arc</p> <code>None</code> <code>segments</code> <code>tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...]</code> <p>The path segments of the arc</p> <code>&lt;dynamic&gt;</code> <code>source</code> <code>LayoutElement | None</code> <p>The source of the arc</p> <code>None</code> <code>start_shorten</code> <code>float</code> <p>The length the start of the arc will be shorten by</p> <code>0.0</code> <code>target</code> <code>LayoutElement | None</code> <p>The target of the arc</p> <code>None</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the arc</p> <code>None</code> <code>arrowhead_fill</code> <code>NoneValueType | Color | None</code> <p>The arrowhead fill color of the arc</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>arrowhead_filter</code> <code>NoneValueType | Filter | None</code> <p>The arrowhead filter of the arc</p> <code>None</code> <code>arrowhead_stroke</code> <code>NoneValueType | Color | None</code> <p>The arrowhead stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>arrowhead_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The arrowhead stroke dasharray of the arc</p> <code>None</code> <code>arrowhead_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The arrowhead stroke dashoffset of the arc</p> <code>None</code> <code>arrowhead_stroke_width</code> <code>float | None</code> <p>The arrowhead stroke width of the arc</p> <code>1.25</code> <code>arrowhead_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The arrowhead transform of the arc</p> <code>None</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>arrowhead_base</code> <p>Return the arrowhead base anchor point of the single-headed arc.</p> <code>arrowhead_bbox</code> <p>Return the bounding box of the single-headed arc arrowhead.</p> <code>arrowhead_border</code> <p>Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.</p> <code>arrowhead_drawing_elements</code> <p>Return the drawing elements of the single-headed arc arrowhead.</p> <code>arrowhead_length</code> <p>Return the length of the single-headed arc arrowhead.</p> <code>arrowhead_tip</code> <p>Return the arrowhead tip anchor point of the single-headed arc.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>childless</code> <p>Return a copy of the arc with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>end_point</code> <p>Return the ending point of the arc.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>fraction</code> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <code>length</code> <p>Return the total length of the arc path.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_children</code> <p>Return the self children of the arc.</p> <code>own_drawing_elements</code> <p>Return the self drawing elements of the single-headed arc.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>path_drawing_elements</code> <p>Return the drawing elements of the single-headed arc path.</p> <code>points</code> <p>Return the points of the arc path.</p> <code>start_point</code> <p>Return the starting point of the arc.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.EquivalenceArcLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.EquivalenceArcLayout.arrowhead_base","title":"arrowhead_base","text":"<pre><code>arrowhead_base() -&gt; Point\n</code></pre> <p>Return the arrowhead base anchor point of the single-headed arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_base(self) -&gt; Point:\n    \"\"\"Return the arrowhead base anchor point of the single-headed arc.\"\"\"\n    arrowhead_length = self.arrowhead_length()\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return self.arrowhead_tip() - (arrowhead_length, 0)\n    fraction = 1 - (arrowhead_length + self.end_shorten) / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.EquivalenceArcLayout.arrowhead_bbox","title":"arrowhead_bbox","text":"<pre><code>arrowhead_bbox() -&gt; Bbox\n</code></pre> <p>Return the bounding box of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_bbox(self) -&gt; Bbox:\n    \"\"\"Return the bounding box of the single-headed arc arrowhead.\"\"\"\n    return get_drawing_elements_bbox(self.arrowhead_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.EquivalenceArcLayout.arrowhead_border","title":"arrowhead_border","text":"<pre><code>arrowhead_border(point: Point) -&gt; Point\n</code></pre> <p>Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_border(self, point: Point) -&gt; Point:\n    \"\"\"Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    point = get_drawing_elements_border(self.arrowhead_drawing_elements(), point)\n    if point is None:\n        return self.arrowhead_tip()\n    return point\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.EquivalenceArcLayout.arrowhead_drawing_elements","title":"arrowhead_drawing_elements","text":"<pre><code>arrowhead_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_drawing_elements(\n    self,\n) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the single-headed arc arrowhead.\"\"\"\n    drawing_elements = self._arrowhead_border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_arrowhead\",\n        elements=tuple(drawing_elements),\n        fill=self.arrowhead_fill,\n        filter_=self.arrowhead_filter,\n        id_=f\"{self.id_}_arrowhead\",\n        stroke=self.arrowhead_stroke,\n        stroke_dasharray=self.arrowhead_stroke_dasharray,\n        stroke_dashoffset=self.arrowhead_stroke_dashoffset,\n        stroke_width=self.arrowhead_stroke_width,\n        transform=self.arrowhead_transform,\n    )\n    transformation = self._get_arrowhead_transformation()\n    group = group.transformed(transformation)\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.EquivalenceArcLayout.arrowhead_length","title":"arrowhead_length","text":"<pre><code>arrowhead_length() -&gt; float\n</code></pre> <p>Return the length of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_length(self) -&gt; float:\n    \"\"\"Return the length of the single-headed arc arrowhead.\"\"\"\n    bbox = get_drawing_elements_bbox(self._arrowhead_border_drawing_elements())\n    if math.isnan(bbox.width):\n        return 0.0\n    return bbox.east().x\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.EquivalenceArcLayout.arrowhead_tip","title":"arrowhead_tip","text":"<pre><code>arrowhead_tip() -&gt; Point\n</code></pre> <p>Return the arrowhead tip anchor point of the single-headed arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_tip(self) -&gt; Point:\n    \"\"\"Return the arrowhead tip anchor point of the single-headed arc.\"\"\"\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return segment.p2\n    fraction = 1 - self.end_shorten / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.EquivalenceArcLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.EquivalenceArcLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the arc with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the arc with no children.\"\"\"\n    return dataclasses.replace(self, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.EquivalenceArcLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.EquivalenceArcLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.EquivalenceArcLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.EquivalenceArcLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.EquivalenceArcLayout.end_point","title":"end_point","text":"<pre><code>end_point() -&gt; Point\n</code></pre> <p>Return the ending point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_point(self) -&gt; Point:\n    \"\"\"Return the ending point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[-1]\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.EquivalenceArcLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.EquivalenceArcLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.EquivalenceArcLayout.fraction","title":"fraction","text":"<pre><code>fraction(fraction: float) -&gt; tuple[Point, float]\n</code></pre> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def fraction(self, fraction: float) -&gt; tuple[Point, float]:\n    \"\"\"Return the position and angle on the arc at a given fraction (of the total arc length).\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    current_length = 0\n    length_to_reach = fraction * self.length()\n    for segment in self.segments:\n        current_length += segment.length()\n        if current_length &gt;= length_to_reach:\n            break\n    segment_start_length = current_length - segment.length()\n    segment_fraction = (length_to_reach - segment_start_length) / segment.length()\n    position, angle = segment.get_position_and_angle_at_fraction(segment_fraction)\n    return position, angle\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.EquivalenceArcLayout.length","title":"length","text":"<pre><code>length() -&gt; float\n</code></pre> <p>Return the total length of the arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def length(self) -&gt; float:\n    \"\"\"Return the total length of the arc path.\"\"\"\n    return sum([segment.length() for segment in self.segments])\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.EquivalenceArcLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.EquivalenceArcLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the arc.\"\"\"\n    return []\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.EquivalenceArcLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the self drawing elements of the single-headed arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the self drawing elements of the single-headed arc.\"\"\"\n    drawing_elements = (\n        self.path_drawing_elements() + self.arrowhead_drawing_elements()\n    )\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.EquivalenceArcLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.EquivalenceArcLayout.path_drawing_elements","title":"path_drawing_elements","text":"<pre><code>path_drawing_elements() -&gt; list[Path]\n</code></pre> <p>Return the drawing elements of the single-headed arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def path_drawing_elements(self) -&gt; list[Path]:\n    \"\"\"Return the drawing elements of the single-headed arc path.\"\"\"\n    arrowhead_length = self.arrowhead_length()\n    if len(self.segments) == 1:\n        segment = (\n            self.segments[0]\n            .shortened(self.start_shorten, \"start\")\n            .shortened(self.end_shorten + arrowhead_length, \"end\")\n        )\n        actions = [\n            MoveTo(segment.p1),\n            self._make_path_action_from_segment(segment),\n        ]\n    else:\n        first_segment = self.segments[0].shortened(self.start_shorten, \"start\")\n        last_segment = self.segments[-1].shortened(\n            self.end_shorten + arrowhead_length, \"end\"\n        )\n        actions = [\n            MoveTo(first_segment.p1),\n            self._make_path_action_from_segment(first_segment),\n        ]\n        for segment in self.segments[1:-1]:\n            action = self._make_path_action_from_segment(segment)\n            actions.append(action)\n        actions.append(self._make_path_action_from_segment(last_segment))\n    path = Path(\n        actions=tuple(actions),\n        class_=f\"{type(self).__name__}_path\",\n        fill=self.path_fill,\n        filter_=self.path_filter,\n        id_=f\"{self.id_}_path\",\n        stroke=self.path_stroke,\n        stroke_dasharray=self.path_stroke_dasharray,\n        stroke_dashoffset=self.path_stroke_dashoffset,\n        stroke_width=self.path_stroke_width,\n        transform=self.path_transform,\n    )\n    return [path]\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.EquivalenceArcLayout.points","title":"points","text":"<pre><code>points() -&gt; list[Point]\n</code></pre> <p>Return the points of the arc path.</p> <p>An arc with no segments has no points and returns an empty list.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def points(self) -&gt; list[Point]:\n    \"\"\"Return the points of the arc path.\n\n    An arc with no segments has no points and returns an empty list.\n    \"\"\"\n    if not self.segments:\n        return []\n    points = []\n    for segment in self.segments:\n        points.append(segment.p1)\n    points.append(segment.p2)\n    return points\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.EquivalenceArcLayout.start_point","title":"start_point","text":"<pre><code>start_point() -&gt; Point\n</code></pre> <p>Return the starting point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_point(self) -&gt; Point:\n    \"\"\"Return the starting point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[0]\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.EquivalenceArcLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.Influence","title":"Influence  <code>dataclass</code>","text":"<pre><code>Influence(*, id_: str = make_uuid4_as_str(), source: BiologicalActivity | LogicalOperator, target: Activity)\n</code></pre> <p>               Bases: <code>SBGNModelElement</code></p> <p>Abstract base class for influences.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'6417c0b1-ecfe-4312-8ac3-9483239c4ed3'</code> <code>source</code> <code>BiologicalActivity | LogicalOperator</code> <p>The source activity or logical operator.</p> required <code>target</code> <code>Activity</code> <p>The target activity being influenced.</p> required <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.Influence.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.LogicArcLayout","title":"LogicArcLayout  <code>dataclass</code>","text":"<pre><code>LogicArcLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, end_shorten: float = 0.0, fill: NoneValueType | Color | None = None, filter_: NoneValueType | Filter | None = None, path_fill: NoneValueType | Color | None = NoneValue, path_filter: NoneValueType | Filter | None = None, path_stroke: NoneValueType | Color | None = black, path_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, path_stroke_dashoffset: NoneValueType | float | None = None, path_stroke_width: float | None = 1.25, path_transform: NoneValueType | tuple[Transformation, ...] | None = None, stroke: NoneValueType | Color | None = None, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: NoneValueType | float | None = None, segments: tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...] = tuple(), source: LayoutElement | None = None, start_shorten: float = 0.0, target: LayoutElement | None = None, transform: NoneValueType | tuple[Transformation, ...] | None = None, arrowhead_fill: NoneValueType | Color | None = white, arrowhead_filter: NoneValueType | Filter | None = None, arrowhead_stroke: NoneValueType | Color | None = black, arrowhead_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, arrowhead_stroke_dashoffset: NoneValueType | float | None = None, arrowhead_stroke_width: float | None = 1.25, arrowhead_transform: NoneValueType | tuple[Transformation, ...] | None = None)\n</code></pre> <p>               Bases: <code>SBGNSingleHeadedArc</code></p> <p>Logic arc layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'6ac2e4b4-37eb-49c5-9adb-5fe435b409d9'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>end_shorten</code> <code>float</code> <p>The length the end of the arc will be shorten by</p> <code>0.0</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the arc</p> <code>None</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the arc</p> <code>None</code> <code>path_fill</code> <code>NoneValueType | Color | None</code> <p>The path fill color of the arc</p> <code>&lt;momapy.drawing.NoneValueType object at 0x7f8b17323e00&gt;</code> <code>path_filter</code> <code>NoneValueType | Filter | None</code> <p>The path filter of the arc</p> <code>None</code> <code>path_stroke</code> <code>NoneValueType | Color | None</code> <p>The path stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>path_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The path stroke dasharray of the arc</p> <code>None</code> <code>path_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The path stroke dashoffset of the arc</p> <code>None</code> <code>path_stroke_width</code> <code>float | None</code> <p>The path stroke width of the arc</p> <code>1.25</code> <code>path_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The path transform of the arc</p> <code>None</code> <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the arc</p> <code>None</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the arc</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the arc</p> <code>None</code> <code>stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the arc</p> <code>None</code> <code>segments</code> <code>tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...]</code> <p>The path segments of the arc</p> <code>&lt;dynamic&gt;</code> <code>source</code> <code>LayoutElement | None</code> <p>The source of the arc</p> <code>None</code> <code>start_shorten</code> <code>float</code> <p>The length the start of the arc will be shorten by</p> <code>0.0</code> <code>target</code> <code>LayoutElement | None</code> <p>The target of the arc</p> <code>None</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the arc</p> <code>None</code> <code>arrowhead_fill</code> <code>NoneValueType | Color | None</code> <p>The arrowhead fill color of the arc</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>arrowhead_filter</code> <code>NoneValueType | Filter | None</code> <p>The arrowhead filter of the arc</p> <code>None</code> <code>arrowhead_stroke</code> <code>NoneValueType | Color | None</code> <p>The arrowhead stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>arrowhead_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The arrowhead stroke dasharray of the arc</p> <code>None</code> <code>arrowhead_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The arrowhead stroke dashoffset of the arc</p> <code>None</code> <code>arrowhead_stroke_width</code> <code>float | None</code> <p>The arrowhead stroke width of the arc</p> <code>1.25</code> <code>arrowhead_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The arrowhead transform of the arc</p> <code>None</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>arrowhead_base</code> <p>Return the arrowhead base anchor point of the single-headed arc.</p> <code>arrowhead_bbox</code> <p>Return the bounding box of the single-headed arc arrowhead.</p> <code>arrowhead_border</code> <p>Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.</p> <code>arrowhead_drawing_elements</code> <p>Return the drawing elements of the single-headed arc arrowhead.</p> <code>arrowhead_length</code> <p>Return the length of the single-headed arc arrowhead.</p> <code>arrowhead_tip</code> <p>Return the arrowhead tip anchor point of the single-headed arc.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>childless</code> <p>Return a copy of the arc with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>end_point</code> <p>Return the ending point of the arc.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>fraction</code> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <code>length</code> <p>Return the total length of the arc path.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_children</code> <p>Return the self children of the arc.</p> <code>own_drawing_elements</code> <p>Return the self drawing elements of the single-headed arc.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>path_drawing_elements</code> <p>Return the drawing elements of the single-headed arc path.</p> <code>points</code> <p>Return the points of the arc path.</p> <code>start_point</code> <p>Return the starting point of the arc.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.LogicArcLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.LogicArcLayout.arrowhead_base","title":"arrowhead_base","text":"<pre><code>arrowhead_base() -&gt; Point\n</code></pre> <p>Return the arrowhead base anchor point of the single-headed arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_base(self) -&gt; Point:\n    \"\"\"Return the arrowhead base anchor point of the single-headed arc.\"\"\"\n    arrowhead_length = self.arrowhead_length()\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return self.arrowhead_tip() - (arrowhead_length, 0)\n    fraction = 1 - (arrowhead_length + self.end_shorten) / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.LogicArcLayout.arrowhead_bbox","title":"arrowhead_bbox","text":"<pre><code>arrowhead_bbox() -&gt; Bbox\n</code></pre> <p>Return the bounding box of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_bbox(self) -&gt; Bbox:\n    \"\"\"Return the bounding box of the single-headed arc arrowhead.\"\"\"\n    return get_drawing_elements_bbox(self.arrowhead_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.LogicArcLayout.arrowhead_border","title":"arrowhead_border","text":"<pre><code>arrowhead_border(point: Point) -&gt; Point\n</code></pre> <p>Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_border(self, point: Point) -&gt; Point:\n    \"\"\"Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    point = get_drawing_elements_border(self.arrowhead_drawing_elements(), point)\n    if point is None:\n        return self.arrowhead_tip()\n    return point\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.LogicArcLayout.arrowhead_drawing_elements","title":"arrowhead_drawing_elements","text":"<pre><code>arrowhead_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_drawing_elements(\n    self,\n) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the single-headed arc arrowhead.\"\"\"\n    drawing_elements = self._arrowhead_border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_arrowhead\",\n        elements=tuple(drawing_elements),\n        fill=self.arrowhead_fill,\n        filter_=self.arrowhead_filter,\n        id_=f\"{self.id_}_arrowhead\",\n        stroke=self.arrowhead_stroke,\n        stroke_dasharray=self.arrowhead_stroke_dasharray,\n        stroke_dashoffset=self.arrowhead_stroke_dashoffset,\n        stroke_width=self.arrowhead_stroke_width,\n        transform=self.arrowhead_transform,\n    )\n    transformation = self._get_arrowhead_transformation()\n    group = group.transformed(transformation)\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.LogicArcLayout.arrowhead_length","title":"arrowhead_length","text":"<pre><code>arrowhead_length() -&gt; float\n</code></pre> <p>Return the length of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_length(self) -&gt; float:\n    \"\"\"Return the length of the single-headed arc arrowhead.\"\"\"\n    bbox = get_drawing_elements_bbox(self._arrowhead_border_drawing_elements())\n    if math.isnan(bbox.width):\n        return 0.0\n    return bbox.east().x\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.LogicArcLayout.arrowhead_tip","title":"arrowhead_tip","text":"<pre><code>arrowhead_tip() -&gt; Point\n</code></pre> <p>Return the arrowhead tip anchor point of the single-headed arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_tip(self) -&gt; Point:\n    \"\"\"Return the arrowhead tip anchor point of the single-headed arc.\"\"\"\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return segment.p2\n    fraction = 1 - self.end_shorten / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.LogicArcLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.LogicArcLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the arc with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the arc with no children.\"\"\"\n    return dataclasses.replace(self, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.LogicArcLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.LogicArcLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.LogicArcLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.LogicArcLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.LogicArcLayout.end_point","title":"end_point","text":"<pre><code>end_point() -&gt; Point\n</code></pre> <p>Return the ending point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_point(self) -&gt; Point:\n    \"\"\"Return the ending point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[-1]\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.LogicArcLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.LogicArcLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.LogicArcLayout.fraction","title":"fraction","text":"<pre><code>fraction(fraction: float) -&gt; tuple[Point, float]\n</code></pre> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def fraction(self, fraction: float) -&gt; tuple[Point, float]:\n    \"\"\"Return the position and angle on the arc at a given fraction (of the total arc length).\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    current_length = 0\n    length_to_reach = fraction * self.length()\n    for segment in self.segments:\n        current_length += segment.length()\n        if current_length &gt;= length_to_reach:\n            break\n    segment_start_length = current_length - segment.length()\n    segment_fraction = (length_to_reach - segment_start_length) / segment.length()\n    position, angle = segment.get_position_and_angle_at_fraction(segment_fraction)\n    return position, angle\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.LogicArcLayout.length","title":"length","text":"<pre><code>length() -&gt; float\n</code></pre> <p>Return the total length of the arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def length(self) -&gt; float:\n    \"\"\"Return the total length of the arc path.\"\"\"\n    return sum([segment.length() for segment in self.segments])\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.LogicArcLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.LogicArcLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the arc.\"\"\"\n    return []\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.LogicArcLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the self drawing elements of the single-headed arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the self drawing elements of the single-headed arc.\"\"\"\n    drawing_elements = (\n        self.path_drawing_elements() + self.arrowhead_drawing_elements()\n    )\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.LogicArcLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.LogicArcLayout.path_drawing_elements","title":"path_drawing_elements","text":"<pre><code>path_drawing_elements() -&gt; list[Path]\n</code></pre> <p>Return the drawing elements of the single-headed arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def path_drawing_elements(self) -&gt; list[Path]:\n    \"\"\"Return the drawing elements of the single-headed arc path.\"\"\"\n    arrowhead_length = self.arrowhead_length()\n    if len(self.segments) == 1:\n        segment = (\n            self.segments[0]\n            .shortened(self.start_shorten, \"start\")\n            .shortened(self.end_shorten + arrowhead_length, \"end\")\n        )\n        actions = [\n            MoveTo(segment.p1),\n            self._make_path_action_from_segment(segment),\n        ]\n    else:\n        first_segment = self.segments[0].shortened(self.start_shorten, \"start\")\n        last_segment = self.segments[-1].shortened(\n            self.end_shorten + arrowhead_length, \"end\"\n        )\n        actions = [\n            MoveTo(first_segment.p1),\n            self._make_path_action_from_segment(first_segment),\n        ]\n        for segment in self.segments[1:-1]:\n            action = self._make_path_action_from_segment(segment)\n            actions.append(action)\n        actions.append(self._make_path_action_from_segment(last_segment))\n    path = Path(\n        actions=tuple(actions),\n        class_=f\"{type(self).__name__}_path\",\n        fill=self.path_fill,\n        filter_=self.path_filter,\n        id_=f\"{self.id_}_path\",\n        stroke=self.path_stroke,\n        stroke_dasharray=self.path_stroke_dasharray,\n        stroke_dashoffset=self.path_stroke_dashoffset,\n        stroke_width=self.path_stroke_width,\n        transform=self.path_transform,\n    )\n    return [path]\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.LogicArcLayout.points","title":"points","text":"<pre><code>points() -&gt; list[Point]\n</code></pre> <p>Return the points of the arc path.</p> <p>An arc with no segments has no points and returns an empty list.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def points(self) -&gt; list[Point]:\n    \"\"\"Return the points of the arc path.\n\n    An arc with no segments has no points and returns an empty list.\n    \"\"\"\n    if not self.segments:\n        return []\n    points = []\n    for segment in self.segments:\n        points.append(segment.p1)\n    points.append(segment.p2)\n    return points\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.LogicArcLayout.start_point","title":"start_point","text":"<pre><code>start_point() -&gt; Point\n</code></pre> <p>Return the starting point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_point(self) -&gt; Point:\n    \"\"\"Return the starting point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[0]\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.LogicArcLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.LogicalOperator","title":"LogicalOperator  <code>dataclass</code>","text":"<pre><code>LogicalOperator(*, id_: str = make_uuid4_as_str(), inputs: frozenset[LogicalOperatorInput] = frozenset())\n</code></pre> <p>               Bases: <code>SBGNModelElement</code></p> <p>Abstract base class for logical operators.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'2c969ad4-4092-4580-87d9-747aa40c59f5'</code> <code>inputs</code> <code>frozenset[LogicalOperatorInput]</code> <p>Input connections to the logical operator.</p> <code>&lt;dynamic&gt;</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.LogicalOperator.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.LogicalOperatorInput","title":"LogicalOperatorInput  <code>dataclass</code>","text":"<pre><code>LogicalOperatorInput(*, id_: str = make_uuid4_as_str(), referred_element: Union[BiologicalActivity, ForwardRef(LogicalOperator, module=__name__)])\n</code></pre> <p>               Bases: <code>SBGNRole</code></p> <p>Logical operator input.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'b7977d19-be03-49ca-a535-2d358aa27768'</code> <code>referred_element</code> <code>BiologicalActivity | ForwardRef</code> <p>The biological activity or logical operator providing the input.</p> required <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.LogicalOperatorInput.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.MacromoleculeUnitOfInformation","title":"MacromoleculeUnitOfInformation  <code>dataclass</code>","text":"<pre><code>MacromoleculeUnitOfInformation(*, id_: str = make_uuid4_as_str(), label: str | None = None)\n</code></pre> <p>               Bases: <code>UnitOfInformation</code></p> <p>Macromolecule unit of information.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'02047b17-1175-4b91-b811-c72ccb9aef00'</code> <code>label</code> <code>str | None</code> <p>The label of the unit of information.</p> <code>None</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.MacromoleculeUnitOfInformation.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.MacromoleculeUnitOfInformationLayout","title":"MacromoleculeUnitOfInformationLayout  <code>dataclass</code>","text":"<pre><code>MacromoleculeUnitOfInformationLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = white, filter_: NoneValueType | Filter | None = None, height: float = 12.0, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = 1.25, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 18.0, rounded_corners: float = 3.0)\n</code></pre> <p>               Bases: <code>_SimpleMixin</code>, <code>SBGNNode</code></p> <p>Macromolecule unit of information layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'95239865-bc84-4f68-a12f-deedf537ecd3'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>12.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The stroke width of the node</p> <code>1.25</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>18.0</code> <code>rounded_corners</code> <code>float</code> <p>The radius of the rounded corners.</p> <code>3.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>angle</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Return the east anchor of the node.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Return the label center anchor of the node.</p> <code>north</code> <p>Return the north anchor of the node.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Return the south anchor of the node.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Return the west anchor of the node.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.MacromoleculeUnitOfInformationLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.MacromoleculeUnitOfInformationLayout.angle","title":"angle","text":"<pre><code>angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.MacromoleculeUnitOfInformationLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.MacromoleculeUnitOfInformationLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.MacromoleculeUnitOfInformationLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.MacromoleculeUnitOfInformationLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.MacromoleculeUnitOfInformationLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.MacromoleculeUnitOfInformationLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.MacromoleculeUnitOfInformationLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.MacromoleculeUnitOfInformationLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.MacromoleculeUnitOfInformationLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Return the east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Return the east anchor of the node.\"\"\"\n    return self.own_angle(0)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.MacromoleculeUnitOfInformationLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.MacromoleculeUnitOfInformationLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.MacromoleculeUnitOfInformationLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.MacromoleculeUnitOfInformationLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.MacromoleculeUnitOfInformationLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Return the label center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Return the label center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.MacromoleculeUnitOfInformationLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Return the north anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Return the north anchor of the node.\"\"\"\n    return self.own_angle(90)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.MacromoleculeUnitOfInformationLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.MacromoleculeUnitOfInformationLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.MacromoleculeUnitOfInformationLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.MacromoleculeUnitOfInformationLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.MacromoleculeUnitOfInformationLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.MacromoleculeUnitOfInformationLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.MacromoleculeUnitOfInformationLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.MacromoleculeUnitOfInformationLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.MacromoleculeUnitOfInformationLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.MacromoleculeUnitOfInformationLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.MacromoleculeUnitOfInformationLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.MacromoleculeUnitOfInformationLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Return the south anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Return the south anchor of the node.\"\"\"\n    return self.own_angle(270)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.MacromoleculeUnitOfInformationLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.MacromoleculeUnitOfInformationLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.MacromoleculeUnitOfInformationLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.MacromoleculeUnitOfInformationLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.MacromoleculeUnitOfInformationLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.MacromoleculeUnitOfInformationLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Return the west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Return the west anchor of the node.\"\"\"\n    return self.own_angle(180)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.MacromoleculeUnitOfInformationLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.MacromoleculeUnitOfInformationLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.MacromoleculeUnitOfInformationLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.MacromoleculeUnitOfInformationLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NecessaryStimulation","title":"NecessaryStimulation  <code>dataclass</code>","text":"<pre><code>NecessaryStimulation(*, id_: str = make_uuid4_as_str(), source: BiologicalActivity | LogicalOperator, target: Activity)\n</code></pre> <p>               Bases: <code>Influence</code></p> <p>Necessary stimulation.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'2befb73f-7c2c-49da-a497-c0928ba30d56'</code> <code>source</code> <code>BiologicalActivity | LogicalOperator</code> <p>The source activity or logical operator.</p> required <code>target</code> <code>Activity</code> <p>The target activity being influenced.</p> required <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NecessaryStimulation.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NecessaryStimulationLayout","title":"NecessaryStimulationLayout  <code>dataclass</code>","text":"<pre><code>NecessaryStimulationLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, end_shorten: float = 0.0, fill: NoneValueType | Color | None = None, filter_: NoneValueType | Filter | None = None, path_fill: NoneValueType | Color | None = NoneValue, path_filter: NoneValueType | Filter | None = None, path_stroke: NoneValueType | Color | None = black, path_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, path_stroke_dashoffset: NoneValueType | float | None = None, path_stroke_width: float | None = 1.25, path_transform: NoneValueType | tuple[Transformation, ...] | None = None, stroke: NoneValueType | Color | None = None, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: NoneValueType | float | None = None, segments: tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...] = tuple(), source: LayoutElement | None = None, start_shorten: float = 0.0, target: LayoutElement | None = None, transform: NoneValueType | tuple[Transformation, ...] | None = None, arrowhead_fill: NoneValueType | Color | None = white, arrowhead_filter: NoneValueType | Filter | None = None, arrowhead_stroke: NoneValueType | Color | None = black, arrowhead_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, arrowhead_stroke_dashoffset: NoneValueType | float | None = None, arrowhead_stroke_width: float | None = 1.25, arrowhead_transform: NoneValueType | tuple[Transformation, ...] | None = None, arrowhead_bar_height: float = 12.0, arrowhead_sep: float = 3.0, arrowhead_triangle_height: float = 10.0, arrowhead_triangle_width: float = 10.0)\n</code></pre> <p>               Bases: <code>SBGNSingleHeadedArc</code></p> <p>Necessary stimulation layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'8ed9d148-f94f-4a79-bf54-2529aa572aed'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>end_shorten</code> <code>float</code> <p>The length the end of the arc will be shorten by</p> <code>0.0</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the arc</p> <code>None</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the arc</p> <code>None</code> <code>path_fill</code> <code>NoneValueType | Color | None</code> <p>The path fill color of the arc</p> <code>&lt;momapy.drawing.NoneValueType object at 0x7f8b17323e00&gt;</code> <code>path_filter</code> <code>NoneValueType | Filter | None</code> <p>The path filter of the arc</p> <code>None</code> <code>path_stroke</code> <code>NoneValueType | Color | None</code> <p>The path stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>path_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The path stroke dasharray of the arc</p> <code>None</code> <code>path_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The path stroke dashoffset of the arc</p> <code>None</code> <code>path_stroke_width</code> <code>float | None</code> <p>The path stroke width of the arc</p> <code>1.25</code> <code>path_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The path transform of the arc</p> <code>None</code> <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the arc</p> <code>None</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the arc</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the arc</p> <code>None</code> <code>stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the arc</p> <code>None</code> <code>segments</code> <code>tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...]</code> <p>The path segments of the arc</p> <code>&lt;dynamic&gt;</code> <code>source</code> <code>LayoutElement | None</code> <p>The source of the arc</p> <code>None</code> <code>start_shorten</code> <code>float</code> <p>The length the start of the arc will be shorten by</p> <code>0.0</code> <code>target</code> <code>LayoutElement | None</code> <p>The target of the arc</p> <code>None</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the arc</p> <code>None</code> <code>arrowhead_fill</code> <code>NoneValueType | Color | None</code> <p>The arrowhead fill color of the arc</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>arrowhead_filter</code> <code>NoneValueType | Filter | None</code> <p>The arrowhead filter of the arc</p> <code>None</code> <code>arrowhead_stroke</code> <code>NoneValueType | Color | None</code> <p>The arrowhead stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>arrowhead_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The arrowhead stroke dasharray of the arc</p> <code>None</code> <code>arrowhead_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The arrowhead stroke dashoffset of the arc</p> <code>None</code> <code>arrowhead_stroke_width</code> <code>float | None</code> <p>The arrowhead stroke width of the arc</p> <code>1.25</code> <code>arrowhead_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The arrowhead transform of the arc</p> <code>None</code> <code>arrowhead_bar_height</code> <code>float</code> <p>The height of the arrowhead bar.</p> <code>12.0</code> <code>arrowhead_sep</code> <code>float</code> <p>The separation between the bar and the triangle.</p> <code>3.0</code> <code>arrowhead_triangle_height</code> <code>float</code> <p>The height of the arrowhead triangle.</p> <code>10.0</code> <code>arrowhead_triangle_width</code> <code>float</code> <p>The width of the arrowhead triangle.</p> <code>10.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>arrowhead_base</code> <p>Return the arrowhead base anchor point of the single-headed arc.</p> <code>arrowhead_bbox</code> <p>Return the bounding box of the single-headed arc arrowhead.</p> <code>arrowhead_border</code> <p>Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.</p> <code>arrowhead_drawing_elements</code> <p>Return the drawing elements of the single-headed arc arrowhead.</p> <code>arrowhead_length</code> <p>Return the length of the single-headed arc arrowhead.</p> <code>arrowhead_tip</code> <p>Return the arrowhead tip anchor point of the single-headed arc.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>childless</code> <p>Return a copy of the arc with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>end_point</code> <p>Return the ending point of the arc.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>fraction</code> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <code>length</code> <p>Return the total length of the arc path.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_children</code> <p>Return the self children of the arc.</p> <code>own_drawing_elements</code> <p>Return the self drawing elements of the single-headed arc.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>path_drawing_elements</code> <p>Return the drawing elements of the single-headed arc path.</p> <code>points</code> <p>Return the points of the arc path.</p> <code>start_point</code> <p>Return the starting point of the arc.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NecessaryStimulationLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NecessaryStimulationLayout.arrowhead_base","title":"arrowhead_base","text":"<pre><code>arrowhead_base() -&gt; Point\n</code></pre> <p>Return the arrowhead base anchor point of the single-headed arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_base(self) -&gt; Point:\n    \"\"\"Return the arrowhead base anchor point of the single-headed arc.\"\"\"\n    arrowhead_length = self.arrowhead_length()\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return self.arrowhead_tip() - (arrowhead_length, 0)\n    fraction = 1 - (arrowhead_length + self.end_shorten) / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NecessaryStimulationLayout.arrowhead_bbox","title":"arrowhead_bbox","text":"<pre><code>arrowhead_bbox() -&gt; Bbox\n</code></pre> <p>Return the bounding box of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_bbox(self) -&gt; Bbox:\n    \"\"\"Return the bounding box of the single-headed arc arrowhead.\"\"\"\n    return get_drawing_elements_bbox(self.arrowhead_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NecessaryStimulationLayout.arrowhead_border","title":"arrowhead_border","text":"<pre><code>arrowhead_border(point: Point) -&gt; Point\n</code></pre> <p>Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_border(self, point: Point) -&gt; Point:\n    \"\"\"Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    point = get_drawing_elements_border(self.arrowhead_drawing_elements(), point)\n    if point is None:\n        return self.arrowhead_tip()\n    return point\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NecessaryStimulationLayout.arrowhead_drawing_elements","title":"arrowhead_drawing_elements","text":"<pre><code>arrowhead_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_drawing_elements(\n    self,\n) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the single-headed arc arrowhead.\"\"\"\n    drawing_elements = self._arrowhead_border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_arrowhead\",\n        elements=tuple(drawing_elements),\n        fill=self.arrowhead_fill,\n        filter_=self.arrowhead_filter,\n        id_=f\"{self.id_}_arrowhead\",\n        stroke=self.arrowhead_stroke,\n        stroke_dasharray=self.arrowhead_stroke_dasharray,\n        stroke_dashoffset=self.arrowhead_stroke_dashoffset,\n        stroke_width=self.arrowhead_stroke_width,\n        transform=self.arrowhead_transform,\n    )\n    transformation = self._get_arrowhead_transformation()\n    group = group.transformed(transformation)\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NecessaryStimulationLayout.arrowhead_length","title":"arrowhead_length","text":"<pre><code>arrowhead_length() -&gt; float\n</code></pre> <p>Return the length of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_length(self) -&gt; float:\n    \"\"\"Return the length of the single-headed arc arrowhead.\"\"\"\n    bbox = get_drawing_elements_bbox(self._arrowhead_border_drawing_elements())\n    if math.isnan(bbox.width):\n        return 0.0\n    return bbox.east().x\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NecessaryStimulationLayout.arrowhead_tip","title":"arrowhead_tip","text":"<pre><code>arrowhead_tip() -&gt; Point\n</code></pre> <p>Return the arrowhead tip anchor point of the single-headed arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_tip(self) -&gt; Point:\n    \"\"\"Return the arrowhead tip anchor point of the single-headed arc.\"\"\"\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return segment.p2\n    fraction = 1 - self.end_shorten / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NecessaryStimulationLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NecessaryStimulationLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the arc with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the arc with no children.\"\"\"\n    return dataclasses.replace(self, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NecessaryStimulationLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NecessaryStimulationLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NecessaryStimulationLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NecessaryStimulationLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NecessaryStimulationLayout.end_point","title":"end_point","text":"<pre><code>end_point() -&gt; Point\n</code></pre> <p>Return the ending point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_point(self) -&gt; Point:\n    \"\"\"Return the ending point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[-1]\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NecessaryStimulationLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NecessaryStimulationLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NecessaryStimulationLayout.fraction","title":"fraction","text":"<pre><code>fraction(fraction: float) -&gt; tuple[Point, float]\n</code></pre> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def fraction(self, fraction: float) -&gt; tuple[Point, float]:\n    \"\"\"Return the position and angle on the arc at a given fraction (of the total arc length).\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    current_length = 0\n    length_to_reach = fraction * self.length()\n    for segment in self.segments:\n        current_length += segment.length()\n        if current_length &gt;= length_to_reach:\n            break\n    segment_start_length = current_length - segment.length()\n    segment_fraction = (length_to_reach - segment_start_length) / segment.length()\n    position, angle = segment.get_position_and_angle_at_fraction(segment_fraction)\n    return position, angle\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NecessaryStimulationLayout.length","title":"length","text":"<pre><code>length() -&gt; float\n</code></pre> <p>Return the total length of the arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def length(self) -&gt; float:\n    \"\"\"Return the total length of the arc path.\"\"\"\n    return sum([segment.length() for segment in self.segments])\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NecessaryStimulationLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NecessaryStimulationLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the arc.\"\"\"\n    return []\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NecessaryStimulationLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the self drawing elements of the single-headed arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the self drawing elements of the single-headed arc.\"\"\"\n    drawing_elements = (\n        self.path_drawing_elements() + self.arrowhead_drawing_elements()\n    )\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NecessaryStimulationLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NecessaryStimulationLayout.path_drawing_elements","title":"path_drawing_elements","text":"<pre><code>path_drawing_elements() -&gt; list[Path]\n</code></pre> <p>Return the drawing elements of the single-headed arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def path_drawing_elements(self) -&gt; list[Path]:\n    \"\"\"Return the drawing elements of the single-headed arc path.\"\"\"\n    arrowhead_length = self.arrowhead_length()\n    if len(self.segments) == 1:\n        segment = (\n            self.segments[0]\n            .shortened(self.start_shorten, \"start\")\n            .shortened(self.end_shorten + arrowhead_length, \"end\")\n        )\n        actions = [\n            MoveTo(segment.p1),\n            self._make_path_action_from_segment(segment),\n        ]\n    else:\n        first_segment = self.segments[0].shortened(self.start_shorten, \"start\")\n        last_segment = self.segments[-1].shortened(\n            self.end_shorten + arrowhead_length, \"end\"\n        )\n        actions = [\n            MoveTo(first_segment.p1),\n            self._make_path_action_from_segment(first_segment),\n        ]\n        for segment in self.segments[1:-1]:\n            action = self._make_path_action_from_segment(segment)\n            actions.append(action)\n        actions.append(self._make_path_action_from_segment(last_segment))\n    path = Path(\n        actions=tuple(actions),\n        class_=f\"{type(self).__name__}_path\",\n        fill=self.path_fill,\n        filter_=self.path_filter,\n        id_=f\"{self.id_}_path\",\n        stroke=self.path_stroke,\n        stroke_dasharray=self.path_stroke_dasharray,\n        stroke_dashoffset=self.path_stroke_dashoffset,\n        stroke_width=self.path_stroke_width,\n        transform=self.path_transform,\n    )\n    return [path]\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NecessaryStimulationLayout.points","title":"points","text":"<pre><code>points() -&gt; list[Point]\n</code></pre> <p>Return the points of the arc path.</p> <p>An arc with no segments has no points and returns an empty list.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def points(self) -&gt; list[Point]:\n    \"\"\"Return the points of the arc path.\n\n    An arc with no segments has no points and returns an empty list.\n    \"\"\"\n    if not self.segments:\n        return []\n    points = []\n    for segment in self.segments:\n        points.append(segment.p1)\n    points.append(segment.p2)\n    return points\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NecessaryStimulationLayout.start_point","title":"start_point","text":"<pre><code>start_point() -&gt; Point\n</code></pre> <p>Return the starting point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_point(self) -&gt; Point:\n    \"\"\"Return the starting point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[0]\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NecessaryStimulationLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NegativeInfluence","title":"NegativeInfluence  <code>dataclass</code>","text":"<pre><code>NegativeInfluence(*, id_: str = make_uuid4_as_str(), source: BiologicalActivity | LogicalOperator, target: Activity)\n</code></pre> <p>               Bases: <code>Influence</code></p> <p>Negative influence.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'519c0c65-011b-4985-8c1a-8681623966a7'</code> <code>source</code> <code>BiologicalActivity | LogicalOperator</code> <p>The source activity or logical operator.</p> required <code>target</code> <code>Activity</code> <p>The target activity being influenced.</p> required <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NegativeInfluence.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NegativeInfluenceLayout","title":"NegativeInfluenceLayout  <code>dataclass</code>","text":"<pre><code>NegativeInfluenceLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, end_shorten: float = 0.0, fill: NoneValueType | Color | None = None, filter_: NoneValueType | Filter | None = None, path_fill: NoneValueType | Color | None = NoneValue, path_filter: NoneValueType | Filter | None = None, path_stroke: NoneValueType | Color | None = black, path_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, path_stroke_dashoffset: NoneValueType | float | None = None, path_stroke_width: float | None = 1.25, path_transform: NoneValueType | tuple[Transformation, ...] | None = None, stroke: NoneValueType | Color | None = None, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: NoneValueType | float | None = None, segments: tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...] = tuple(), source: LayoutElement | None = None, start_shorten: float = 0.0, target: LayoutElement | None = None, transform: NoneValueType | tuple[Transformation, ...] | None = None, arrowhead_fill: NoneValueType | Color | None = white, arrowhead_filter: NoneValueType | Filter | None = None, arrowhead_stroke: NoneValueType | Color | None = black, arrowhead_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, arrowhead_stroke_dashoffset: NoneValueType | float | None = None, arrowhead_stroke_width: float | None = 1.25, arrowhead_transform: NoneValueType | tuple[Transformation, ...] | None = None, arrowhead_height: float = 10.0)\n</code></pre> <p>               Bases: <code>SBGNSingleHeadedArc</code></p> <p>Negative influence layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'9f7589a5-b6f7-462a-b779-0b52f2dd9af4'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>end_shorten</code> <code>float</code> <p>The length the end of the arc will be shorten by</p> <code>0.0</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the arc</p> <code>None</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the arc</p> <code>None</code> <code>path_fill</code> <code>NoneValueType | Color | None</code> <p>The path fill color of the arc</p> <code>&lt;momapy.drawing.NoneValueType object at 0x7f8b17323e00&gt;</code> <code>path_filter</code> <code>NoneValueType | Filter | None</code> <p>The path filter of the arc</p> <code>None</code> <code>path_stroke</code> <code>NoneValueType | Color | None</code> <p>The path stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>path_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The path stroke dasharray of the arc</p> <code>None</code> <code>path_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The path stroke dashoffset of the arc</p> <code>None</code> <code>path_stroke_width</code> <code>float | None</code> <p>The path stroke width of the arc</p> <code>1.25</code> <code>path_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The path transform of the arc</p> <code>None</code> <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the arc</p> <code>None</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the arc</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the arc</p> <code>None</code> <code>stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the arc</p> <code>None</code> <code>segments</code> <code>tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...]</code> <p>The path segments of the arc</p> <code>&lt;dynamic&gt;</code> <code>source</code> <code>LayoutElement | None</code> <p>The source of the arc</p> <code>None</code> <code>start_shorten</code> <code>float</code> <p>The length the start of the arc will be shorten by</p> <code>0.0</code> <code>target</code> <code>LayoutElement | None</code> <p>The target of the arc</p> <code>None</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the arc</p> <code>None</code> <code>arrowhead_fill</code> <code>NoneValueType | Color | None</code> <p>The arrowhead fill color of the arc</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>arrowhead_filter</code> <code>NoneValueType | Filter | None</code> <p>The arrowhead filter of the arc</p> <code>None</code> <code>arrowhead_stroke</code> <code>NoneValueType | Color | None</code> <p>The arrowhead stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>arrowhead_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The arrowhead stroke dasharray of the arc</p> <code>None</code> <code>arrowhead_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The arrowhead stroke dashoffset of the arc</p> <code>None</code> <code>arrowhead_stroke_width</code> <code>float | None</code> <p>The arrowhead stroke width of the arc</p> <code>1.25</code> <code>arrowhead_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The arrowhead transform of the arc</p> <code>None</code> <code>arrowhead_height</code> <code>float</code> <p>The height of the arrowhead.</p> <code>10.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>arrowhead_base</code> <p>Return the arrowhead base anchor point of the single-headed arc.</p> <code>arrowhead_bbox</code> <p>Return the bounding box of the single-headed arc arrowhead.</p> <code>arrowhead_border</code> <p>Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.</p> <code>arrowhead_drawing_elements</code> <p>Return the drawing elements of the single-headed arc arrowhead.</p> <code>arrowhead_length</code> <p>Return the length of the single-headed arc arrowhead.</p> <code>arrowhead_tip</code> <p>Return the arrowhead tip anchor point of the single-headed arc.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>childless</code> <p>Return a copy of the arc with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>end_point</code> <p>Return the ending point of the arc.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>fraction</code> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <code>length</code> <p>Return the total length of the arc path.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_children</code> <p>Return the self children of the arc.</p> <code>own_drawing_elements</code> <p>Return the self drawing elements of the single-headed arc.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>path_drawing_elements</code> <p>Return the drawing elements of the single-headed arc path.</p> <code>points</code> <p>Return the points of the arc path.</p> <code>start_point</code> <p>Return the starting point of the arc.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NegativeInfluenceLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NegativeInfluenceLayout.arrowhead_base","title":"arrowhead_base","text":"<pre><code>arrowhead_base() -&gt; Point\n</code></pre> <p>Return the arrowhead base anchor point of the single-headed arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_base(self) -&gt; Point:\n    \"\"\"Return the arrowhead base anchor point of the single-headed arc.\"\"\"\n    arrowhead_length = self.arrowhead_length()\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return self.arrowhead_tip() - (arrowhead_length, 0)\n    fraction = 1 - (arrowhead_length + self.end_shorten) / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NegativeInfluenceLayout.arrowhead_bbox","title":"arrowhead_bbox","text":"<pre><code>arrowhead_bbox() -&gt; Bbox\n</code></pre> <p>Return the bounding box of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_bbox(self) -&gt; Bbox:\n    \"\"\"Return the bounding box of the single-headed arc arrowhead.\"\"\"\n    return get_drawing_elements_bbox(self.arrowhead_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NegativeInfluenceLayout.arrowhead_border","title":"arrowhead_border","text":"<pre><code>arrowhead_border(point: Point) -&gt; Point\n</code></pre> <p>Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_border(self, point: Point) -&gt; Point:\n    \"\"\"Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    point = get_drawing_elements_border(self.arrowhead_drawing_elements(), point)\n    if point is None:\n        return self.arrowhead_tip()\n    return point\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NegativeInfluenceLayout.arrowhead_drawing_elements","title":"arrowhead_drawing_elements","text":"<pre><code>arrowhead_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_drawing_elements(\n    self,\n) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the single-headed arc arrowhead.\"\"\"\n    drawing_elements = self._arrowhead_border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_arrowhead\",\n        elements=tuple(drawing_elements),\n        fill=self.arrowhead_fill,\n        filter_=self.arrowhead_filter,\n        id_=f\"{self.id_}_arrowhead\",\n        stroke=self.arrowhead_stroke,\n        stroke_dasharray=self.arrowhead_stroke_dasharray,\n        stroke_dashoffset=self.arrowhead_stroke_dashoffset,\n        stroke_width=self.arrowhead_stroke_width,\n        transform=self.arrowhead_transform,\n    )\n    transformation = self._get_arrowhead_transformation()\n    group = group.transformed(transformation)\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NegativeInfluenceLayout.arrowhead_length","title":"arrowhead_length","text":"<pre><code>arrowhead_length() -&gt; float\n</code></pre> <p>Return the length of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_length(self) -&gt; float:\n    \"\"\"Return the length of the single-headed arc arrowhead.\"\"\"\n    bbox = get_drawing_elements_bbox(self._arrowhead_border_drawing_elements())\n    if math.isnan(bbox.width):\n        return 0.0\n    return bbox.east().x\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NegativeInfluenceLayout.arrowhead_tip","title":"arrowhead_tip","text":"<pre><code>arrowhead_tip() -&gt; Point\n</code></pre> <p>Return the arrowhead tip anchor point of the single-headed arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_tip(self) -&gt; Point:\n    \"\"\"Return the arrowhead tip anchor point of the single-headed arc.\"\"\"\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return segment.p2\n    fraction = 1 - self.end_shorten / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NegativeInfluenceLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NegativeInfluenceLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the arc with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the arc with no children.\"\"\"\n    return dataclasses.replace(self, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NegativeInfluenceLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NegativeInfluenceLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NegativeInfluenceLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NegativeInfluenceLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NegativeInfluenceLayout.end_point","title":"end_point","text":"<pre><code>end_point() -&gt; Point\n</code></pre> <p>Return the ending point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_point(self) -&gt; Point:\n    \"\"\"Return the ending point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[-1]\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NegativeInfluenceLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NegativeInfluenceLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NegativeInfluenceLayout.fraction","title":"fraction","text":"<pre><code>fraction(fraction: float) -&gt; tuple[Point, float]\n</code></pre> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def fraction(self, fraction: float) -&gt; tuple[Point, float]:\n    \"\"\"Return the position and angle on the arc at a given fraction (of the total arc length).\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    current_length = 0\n    length_to_reach = fraction * self.length()\n    for segment in self.segments:\n        current_length += segment.length()\n        if current_length &gt;= length_to_reach:\n            break\n    segment_start_length = current_length - segment.length()\n    segment_fraction = (length_to_reach - segment_start_length) / segment.length()\n    position, angle = segment.get_position_and_angle_at_fraction(segment_fraction)\n    return position, angle\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NegativeInfluenceLayout.length","title":"length","text":"<pre><code>length() -&gt; float\n</code></pre> <p>Return the total length of the arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def length(self) -&gt; float:\n    \"\"\"Return the total length of the arc path.\"\"\"\n    return sum([segment.length() for segment in self.segments])\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NegativeInfluenceLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NegativeInfluenceLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the arc.\"\"\"\n    return []\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NegativeInfluenceLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the self drawing elements of the single-headed arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the self drawing elements of the single-headed arc.\"\"\"\n    drawing_elements = (\n        self.path_drawing_elements() + self.arrowhead_drawing_elements()\n    )\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NegativeInfluenceLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NegativeInfluenceLayout.path_drawing_elements","title":"path_drawing_elements","text":"<pre><code>path_drawing_elements() -&gt; list[Path]\n</code></pre> <p>Return the drawing elements of the single-headed arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def path_drawing_elements(self) -&gt; list[Path]:\n    \"\"\"Return the drawing elements of the single-headed arc path.\"\"\"\n    arrowhead_length = self.arrowhead_length()\n    if len(self.segments) == 1:\n        segment = (\n            self.segments[0]\n            .shortened(self.start_shorten, \"start\")\n            .shortened(self.end_shorten + arrowhead_length, \"end\")\n        )\n        actions = [\n            MoveTo(segment.p1),\n            self._make_path_action_from_segment(segment),\n        ]\n    else:\n        first_segment = self.segments[0].shortened(self.start_shorten, \"start\")\n        last_segment = self.segments[-1].shortened(\n            self.end_shorten + arrowhead_length, \"end\"\n        )\n        actions = [\n            MoveTo(first_segment.p1),\n            self._make_path_action_from_segment(first_segment),\n        ]\n        for segment in self.segments[1:-1]:\n            action = self._make_path_action_from_segment(segment)\n            actions.append(action)\n        actions.append(self._make_path_action_from_segment(last_segment))\n    path = Path(\n        actions=tuple(actions),\n        class_=f\"{type(self).__name__}_path\",\n        fill=self.path_fill,\n        filter_=self.path_filter,\n        id_=f\"{self.id_}_path\",\n        stroke=self.path_stroke,\n        stroke_dasharray=self.path_stroke_dasharray,\n        stroke_dashoffset=self.path_stroke_dashoffset,\n        stroke_width=self.path_stroke_width,\n        transform=self.path_transform,\n    )\n    return [path]\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NegativeInfluenceLayout.points","title":"points","text":"<pre><code>points() -&gt; list[Point]\n</code></pre> <p>Return the points of the arc path.</p> <p>An arc with no segments has no points and returns an empty list.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def points(self) -&gt; list[Point]:\n    \"\"\"Return the points of the arc path.\n\n    An arc with no segments has no points and returns an empty list.\n    \"\"\"\n    if not self.segments:\n        return []\n    points = []\n    for segment in self.segments:\n        points.append(segment.p1)\n    points.append(segment.p2)\n    return points\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NegativeInfluenceLayout.start_point","title":"start_point","text":"<pre><code>start_point() -&gt; Point\n</code></pre> <p>Return the starting point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_point(self) -&gt; Point:\n    \"\"\"Return the starting point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[0]\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NegativeInfluenceLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NotOperator","title":"NotOperator  <code>dataclass</code>","text":"<pre><code>NotOperator(*, id_: str = make_uuid4_as_str(), inputs: frozenset[LogicalOperatorInput] = frozenset())\n</code></pre> <p>               Bases: <code>LogicalOperator</code></p> <p>NOT operator.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'2ba24a01-a1ea-4fbb-b1d9-7f9b6fdca493'</code> <code>inputs</code> <code>frozenset[LogicalOperatorInput]</code> <p>Input connections to the logical operator.</p> <code>&lt;dynamic&gt;</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NotOperator.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NotOperatorLayout","title":"NotOperatorLayout  <code>dataclass</code>","text":"<pre><code>NotOperatorLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = white, filter_: NoneValueType | Filter | None = None, height: float = 30.0, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = 1.25, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 30.0, text: str = 'NOT', font_family: str = DEFAULT_FONT_FAMILY, font_fill: Color | NoneValueType = black, font_stroke: Color | NoneValueType = NoneValue, font_style: FontStyle = NORMAL, font_weight: FontWeight | float = NORMAL, orientation: Orientation = HORIZONTAL, left_to_right: bool = True, left_connector_length: float = 10.0, right_connector_length: float = 10.0, left_connector_stroke: NoneValueType | Color | None = None, left_connector_stroke_width: float | None = None, left_connector_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, left_connector_stroke_dashoffset: float | None = None, left_connector_fill: NoneValueType | Color | None = None, left_connector_transform: NoneValueType | tuple[Transformation, ...] | None = None, left_connector_filter: NoneValueType | Filter | None = None, right_connector_stroke: NoneValueType | Color | None = None, right_connector_stroke_width: float | None = None, right_connector_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, right_connector_stroke_dashoffset: float | None = None, right_connector_fill: NoneValueType | Color | None = None, right_connector_transform: NoneValueType | tuple[Transformation, ...] | None = None, right_connector_filter: NoneValueType | Filter | None = None)\n</code></pre> <p>               Bases: <code>_ConnectorsMixin</code>, <code>_SimpleMixin</code>, <code>_TextMixin</code>, <code>SBGNNode</code></p> <p>NOT operator layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'84e121f6-956b-4afe-a41a-a116ccb56c23'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>30.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The stroke width of the node</p> <code>1.25</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>30.0</code> <code>text</code> <code>str</code> <p>The text displayed inside the node.</p> <code>'NOT'</code> <code>font_family</code> <code>str</code> <p>Font family for the text.</p> <code>'DejaVu Sans'</code> <code>font_fill</code> <code>Color | NoneValueType</code> <p>Fill color for the text.</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>font_stroke</code> <code>Color | NoneValueType</code> <p>Stroke color for the text outline.</p> <code>&lt;momapy.drawing.NoneValueType object at 0x7f8b17323e00&gt;</code> <code>font_style</code> <code>FontStyle</code> <p>Font style (normal, italic, etc.).</p> <code>&lt;FontStyle.NORMAL: 0&gt;</code> <code>font_weight</code> <code>FontWeight | float</code> <p>Font weight (normal, bold, etc.).</p> <code>&lt;FontWeight.NORMAL: 0&gt;</code> <code>orientation</code> <code>Orientation</code> <p>Orientation of connectors (HORIZONTAL or VERTICAL).</p> <code>&lt;Orientation.HORIZONTAL: 1&gt;</code> <code>left_to_right</code> <code>bool</code> <p>Whether connectors flow left to right.</p> <code>True</code> <code>left_connector_length</code> <code>float</code> <p>Length of the left connector.</p> <code>10.0</code> <code>right_connector_length</code> <code>float</code> <p>Length of the right connector.</p> <code>10.0</code> <code>left_connector_stroke</code> <code>NoneValueType | Color | None</code> <p>Stroke color for the left connector line.</p> <code>None</code> <code>left_connector_stroke_width</code> <code>float | None</code> <p>Stroke width for the left connector line.</p> <code>None</code> <code>left_connector_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>Dash pattern for the left connector line.</p> <code>None</code> <code>left_connector_stroke_dashoffset</code> <code>float | None</code> <p>Dash offset for the left connector line.</p> <code>None</code> <code>left_connector_fill</code> <code>NoneValueType | Color | None</code> <p>Fill color for the left connector.</p> <code>None</code> <code>left_connector_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>Transformations applied to the left connector.</p> <code>None</code> <code>left_connector_filter</code> <code>NoneValueType | Filter | None</code> <p>Filter applied to the left connector.</p> <code>None</code> <code>right_connector_stroke</code> <code>NoneValueType | Color | None</code> <p>Stroke color for the right connector line.</p> <code>None</code> <code>right_connector_stroke_width</code> <code>float | None</code> <p>Stroke width for the right connector line.</p> <code>None</code> <code>right_connector_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>Dash pattern for the right connector line.</p> <code>None</code> <code>right_connector_stroke_dashoffset</code> <code>float | None</code> <p>Dash offset for the right connector line.</p> <code>None</code> <code>right_connector_fill</code> <code>NoneValueType | Color | None</code> <p>Fill color for the right connector.</p> <code>None</code> <code>right_connector_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>Transformations applied to the right connector.</p> <code>None</code> <code>right_connector_filter</code> <code>NoneValueType | Filter | None</code> <p>Filter applied to the right connector.</p> <code>None</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>angle</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Get the east (right) anchor point.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Return the label center anchor of the node.</p> <code>left_connector_base</code> <p>Get the base point of the left connector.</p> <code>left_connector_tip</code> <p>Get the tip point of the left connector.</p> <code>north</code> <p>Get the north (top) anchor point.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>right_connector_base</code> <p>Get the base point of the right connector.</p> <code>right_connector_tip</code> <p>Get the tip point of the right connector.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Get the south (bottom) anchor point.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Get the west (left) anchor point.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NotOperatorLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NotOperatorLayout.angle","title":"angle","text":"<pre><code>angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NotOperatorLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NotOperatorLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NotOperatorLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NotOperatorLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NotOperatorLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NotOperatorLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NotOperatorLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NotOperatorLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NotOperatorLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Get the east (right) anchor point.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point on the east side of the element.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Get the east (right) anchor point.\n\n    Returns:\n        Point on the east side of the element.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x + self.width / 2, self.y)\n    else:\n        return Point(self.x + self.width / 2 + self.right_connector_length, self.y)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NotOperatorLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NotOperatorLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NotOperatorLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NotOperatorLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NotOperatorLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Return the label center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Return the label center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NotOperatorLayout.left_connector_base","title":"left_connector_base","text":"<pre><code>left_connector_base() -&gt; Point\n</code></pre> <p>Get the base point of the left connector.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point where the left connector attaches to the shape.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def left_connector_base(self) -&gt; Point:\n    \"\"\"Get the base point of the left connector.\n\n    Returns:\n        Point where the left connector attaches to the shape.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x, self.y - self.height / 2)\n    else:\n        return Point(self.x - self.width / 2, self.y)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NotOperatorLayout.left_connector_tip","title":"left_connector_tip","text":"<pre><code>left_connector_tip() -&gt; Point\n</code></pre> <p>Get the tip point of the left connector.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point at the end of the left connector line.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def left_connector_tip(self) -&gt; Point:\n    \"\"\"Get the tip point of the left connector.\n\n    Returns:\n        Point at the end of the left connector line.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x, self.y - self.height / 2 - self.left_connector_length)\n    else:\n        return Point(self.x - self.width / 2 - self.left_connector_length, self.y)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NotOperatorLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Get the north (top) anchor point.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point on the north side of the element.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Get the north (top) anchor point.\n\n    Returns:\n        Point on the north side of the element.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x, self.y - self.height / 2 - self.left_connector_length)\n    else:\n        return Point(self.x, self.y - self.height / 2)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NotOperatorLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NotOperatorLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NotOperatorLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NotOperatorLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NotOperatorLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NotOperatorLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NotOperatorLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NotOperatorLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NotOperatorLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NotOperatorLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NotOperatorLayout.right_connector_base","title":"right_connector_base","text":"<pre><code>right_connector_base() -&gt; Point\n</code></pre> <p>Get the base point of the right connector.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point where the right connector attaches to the shape.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def right_connector_base(self) -&gt; Point:\n    \"\"\"Get the base point of the right connector.\n\n    Returns:\n        Point where the right connector attaches to the shape.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x, self.y + self.height / 2)\n    else:\n        return Point(self.x + self.width / 2, self.y)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NotOperatorLayout.right_connector_tip","title":"right_connector_tip","text":"<pre><code>right_connector_tip() -&gt; Point\n</code></pre> <p>Get the tip point of the right connector.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point at the end of the right connector line.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def right_connector_tip(self) -&gt; Point:\n    \"\"\"Get the tip point of the right connector.\n\n    Returns:\n        Point at the end of the right connector line.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x, self.y + self.height / 2 + self.right_connector_length)\n    else:\n        return Point(self.x + self.width / 2 + self.right_connector_length, self.y)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NotOperatorLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NotOperatorLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Get the south (bottom) anchor point.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point on the south side of the element.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Get the south (bottom) anchor point.\n\n    Returns:\n        Point on the south side of the element.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x, self.y + self.height / 2 + self.right_connector_length)\n    else:\n        return Point(self.x, self.y + self.height / 2)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NotOperatorLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NotOperatorLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NotOperatorLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NotOperatorLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NotOperatorLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NotOperatorLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Get the west (left) anchor point.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point on the west side of the element.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Get the west (left) anchor point.\n\n    Returns:\n        Point on the west side of the element.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x - self.width / 2, self.y)\n    else:\n        return Point(self.x - self.width / 2 - self.left_connector_length, self.y)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NotOperatorLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NotOperatorLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NotOperatorLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NotOperatorLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NucleicAcidFeatureUnitOfInformation","title":"NucleicAcidFeatureUnitOfInformation  <code>dataclass</code>","text":"<pre><code>NucleicAcidFeatureUnitOfInformation(*, id_: str = make_uuid4_as_str(), label: str | None = None)\n</code></pre> <p>               Bases: <code>UnitOfInformation</code></p> <p>Nucleic acid feature unit of information.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'3cabd61c-b3bd-4dac-a092-dae4634723a9'</code> <code>label</code> <code>str | None</code> <p>The label of the unit of information.</p> <code>None</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NucleicAcidFeatureUnitOfInformation.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NucleicAcidFeatureUnitOfInformationLayout","title":"NucleicAcidFeatureUnitOfInformationLayout  <code>dataclass</code>","text":"<pre><code>NucleicAcidFeatureUnitOfInformationLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = white, filter_: NoneValueType | Filter | None = None, height: float = 12.0, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = 1.25, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 18.0, rounded_corners: float = 3.0)\n</code></pre> <p>               Bases: <code>_SimpleMixin</code>, <code>SBGNNode</code></p> <p>Nucleic acid feature unit of information layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'752a91d4-c8f8-4594-9fd5-7ba421756734'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>12.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The stroke width of the node</p> <code>1.25</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>18.0</code> <code>rounded_corners</code> <code>float</code> <p>The radius of the rounded corners.</p> <code>3.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>angle</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Return the east anchor of the node.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Return the label center anchor of the node.</p> <code>north</code> <p>Return the north anchor of the node.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Return the south anchor of the node.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Return the west anchor of the node.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NucleicAcidFeatureUnitOfInformationLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NucleicAcidFeatureUnitOfInformationLayout.angle","title":"angle","text":"<pre><code>angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NucleicAcidFeatureUnitOfInformationLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NucleicAcidFeatureUnitOfInformationLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NucleicAcidFeatureUnitOfInformationLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NucleicAcidFeatureUnitOfInformationLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NucleicAcidFeatureUnitOfInformationLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NucleicAcidFeatureUnitOfInformationLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NucleicAcidFeatureUnitOfInformationLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NucleicAcidFeatureUnitOfInformationLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NucleicAcidFeatureUnitOfInformationLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Return the east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Return the east anchor of the node.\"\"\"\n    return self.own_angle(0)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NucleicAcidFeatureUnitOfInformationLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NucleicAcidFeatureUnitOfInformationLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NucleicAcidFeatureUnitOfInformationLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NucleicAcidFeatureUnitOfInformationLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NucleicAcidFeatureUnitOfInformationLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Return the label center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Return the label center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NucleicAcidFeatureUnitOfInformationLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Return the north anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Return the north anchor of the node.\"\"\"\n    return self.own_angle(90)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NucleicAcidFeatureUnitOfInformationLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NucleicAcidFeatureUnitOfInformationLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NucleicAcidFeatureUnitOfInformationLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NucleicAcidFeatureUnitOfInformationLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NucleicAcidFeatureUnitOfInformationLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NucleicAcidFeatureUnitOfInformationLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NucleicAcidFeatureUnitOfInformationLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NucleicAcidFeatureUnitOfInformationLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NucleicAcidFeatureUnitOfInformationLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NucleicAcidFeatureUnitOfInformationLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NucleicAcidFeatureUnitOfInformationLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NucleicAcidFeatureUnitOfInformationLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Return the south anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Return the south anchor of the node.\"\"\"\n    return self.own_angle(270)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NucleicAcidFeatureUnitOfInformationLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NucleicAcidFeatureUnitOfInformationLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NucleicAcidFeatureUnitOfInformationLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NucleicAcidFeatureUnitOfInformationLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NucleicAcidFeatureUnitOfInformationLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NucleicAcidFeatureUnitOfInformationLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Return the west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Return the west anchor of the node.\"\"\"\n    return self.own_angle(180)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NucleicAcidFeatureUnitOfInformationLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NucleicAcidFeatureUnitOfInformationLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NucleicAcidFeatureUnitOfInformationLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.NucleicAcidFeatureUnitOfInformationLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.OrOperator","title":"OrOperator  <code>dataclass</code>","text":"<pre><code>OrOperator(*, id_: str = make_uuid4_as_str(), inputs: frozenset[LogicalOperatorInput] = frozenset())\n</code></pre> <p>               Bases: <code>LogicalOperator</code></p> <p>OR operator.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'3c4da547-1f72-4c1b-a0b2-e2e59807003f'</code> <code>inputs</code> <code>frozenset[LogicalOperatorInput]</code> <p>Input connections to the logical operator.</p> <code>&lt;dynamic&gt;</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.OrOperator.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.OrOperatorLayout","title":"OrOperatorLayout  <code>dataclass</code>","text":"<pre><code>OrOperatorLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = white, filter_: NoneValueType | Filter | None = None, height: float = 30.0, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = 1.25, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 30.0, text: str = 'OR', font_family: str = DEFAULT_FONT_FAMILY, font_fill: Color | NoneValueType = black, font_stroke: Color | NoneValueType = NoneValue, font_style: FontStyle = NORMAL, font_weight: FontWeight | float = NORMAL, orientation: Orientation = HORIZONTAL, left_to_right: bool = True, left_connector_length: float = 10.0, right_connector_length: float = 10.0, left_connector_stroke: NoneValueType | Color | None = None, left_connector_stroke_width: float | None = None, left_connector_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, left_connector_stroke_dashoffset: float | None = None, left_connector_fill: NoneValueType | Color | None = None, left_connector_transform: NoneValueType | tuple[Transformation, ...] | None = None, left_connector_filter: NoneValueType | Filter | None = None, right_connector_stroke: NoneValueType | Color | None = None, right_connector_stroke_width: float | None = None, right_connector_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, right_connector_stroke_dashoffset: float | None = None, right_connector_fill: NoneValueType | Color | None = None, right_connector_transform: NoneValueType | tuple[Transformation, ...] | None = None, right_connector_filter: NoneValueType | Filter | None = None)\n</code></pre> <p>               Bases: <code>_ConnectorsMixin</code>, <code>_SimpleMixin</code>, <code>_TextMixin</code>, <code>SBGNNode</code></p> <p>OR operator layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'f4e6b979-1a4a-44e0-a34a-74bfabbf9e89'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>30.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The stroke width of the node</p> <code>1.25</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>30.0</code> <code>text</code> <code>str</code> <p>The text displayed inside the node.</p> <code>'OR'</code> <code>font_family</code> <code>str</code> <p>Font family for the text.</p> <code>'DejaVu Sans'</code> <code>font_fill</code> <code>Color | NoneValueType</code> <p>Fill color for the text.</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>font_stroke</code> <code>Color | NoneValueType</code> <p>Stroke color for the text outline.</p> <code>&lt;momapy.drawing.NoneValueType object at 0x7f8b17323e00&gt;</code> <code>font_style</code> <code>FontStyle</code> <p>Font style (normal, italic, etc.).</p> <code>&lt;FontStyle.NORMAL: 0&gt;</code> <code>font_weight</code> <code>FontWeight | float</code> <p>Font weight (normal, bold, etc.).</p> <code>&lt;FontWeight.NORMAL: 0&gt;</code> <code>orientation</code> <code>Orientation</code> <p>Orientation of connectors (HORIZONTAL or VERTICAL).</p> <code>&lt;Orientation.HORIZONTAL: 1&gt;</code> <code>left_to_right</code> <code>bool</code> <p>Whether connectors flow left to right.</p> <code>True</code> <code>left_connector_length</code> <code>float</code> <p>Length of the left connector.</p> <code>10.0</code> <code>right_connector_length</code> <code>float</code> <p>Length of the right connector.</p> <code>10.0</code> <code>left_connector_stroke</code> <code>NoneValueType | Color | None</code> <p>Stroke color for the left connector line.</p> <code>None</code> <code>left_connector_stroke_width</code> <code>float | None</code> <p>Stroke width for the left connector line.</p> <code>None</code> <code>left_connector_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>Dash pattern for the left connector line.</p> <code>None</code> <code>left_connector_stroke_dashoffset</code> <code>float | None</code> <p>Dash offset for the left connector line.</p> <code>None</code> <code>left_connector_fill</code> <code>NoneValueType | Color | None</code> <p>Fill color for the left connector.</p> <code>None</code> <code>left_connector_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>Transformations applied to the left connector.</p> <code>None</code> <code>left_connector_filter</code> <code>NoneValueType | Filter | None</code> <p>Filter applied to the left connector.</p> <code>None</code> <code>right_connector_stroke</code> <code>NoneValueType | Color | None</code> <p>Stroke color for the right connector line.</p> <code>None</code> <code>right_connector_stroke_width</code> <code>float | None</code> <p>Stroke width for the right connector line.</p> <code>None</code> <code>right_connector_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>Dash pattern for the right connector line.</p> <code>None</code> <code>right_connector_stroke_dashoffset</code> <code>float | None</code> <p>Dash offset for the right connector line.</p> <code>None</code> <code>right_connector_fill</code> <code>NoneValueType | Color | None</code> <p>Fill color for the right connector.</p> <code>None</code> <code>right_connector_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>Transformations applied to the right connector.</p> <code>None</code> <code>right_connector_filter</code> <code>NoneValueType | Filter | None</code> <p>Filter applied to the right connector.</p> <code>None</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>angle</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Get the east (right) anchor point.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Return the label center anchor of the node.</p> <code>left_connector_base</code> <p>Get the base point of the left connector.</p> <code>left_connector_tip</code> <p>Get the tip point of the left connector.</p> <code>north</code> <p>Get the north (top) anchor point.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>right_connector_base</code> <p>Get the base point of the right connector.</p> <code>right_connector_tip</code> <p>Get the tip point of the right connector.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Get the south (bottom) anchor point.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Get the west (left) anchor point.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.OrOperatorLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.OrOperatorLayout.angle","title":"angle","text":"<pre><code>angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.OrOperatorLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.OrOperatorLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.OrOperatorLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.OrOperatorLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.OrOperatorLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.OrOperatorLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.OrOperatorLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.OrOperatorLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.OrOperatorLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Get the east (right) anchor point.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point on the east side of the element.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Get the east (right) anchor point.\n\n    Returns:\n        Point on the east side of the element.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x + self.width / 2, self.y)\n    else:\n        return Point(self.x + self.width / 2 + self.right_connector_length, self.y)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.OrOperatorLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.OrOperatorLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.OrOperatorLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.OrOperatorLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.OrOperatorLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Return the label center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Return the label center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.OrOperatorLayout.left_connector_base","title":"left_connector_base","text":"<pre><code>left_connector_base() -&gt; Point\n</code></pre> <p>Get the base point of the left connector.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point where the left connector attaches to the shape.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def left_connector_base(self) -&gt; Point:\n    \"\"\"Get the base point of the left connector.\n\n    Returns:\n        Point where the left connector attaches to the shape.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x, self.y - self.height / 2)\n    else:\n        return Point(self.x - self.width / 2, self.y)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.OrOperatorLayout.left_connector_tip","title":"left_connector_tip","text":"<pre><code>left_connector_tip() -&gt; Point\n</code></pre> <p>Get the tip point of the left connector.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point at the end of the left connector line.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def left_connector_tip(self) -&gt; Point:\n    \"\"\"Get the tip point of the left connector.\n\n    Returns:\n        Point at the end of the left connector line.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x, self.y - self.height / 2 - self.left_connector_length)\n    else:\n        return Point(self.x - self.width / 2 - self.left_connector_length, self.y)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.OrOperatorLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Get the north (top) anchor point.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point on the north side of the element.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Get the north (top) anchor point.\n\n    Returns:\n        Point on the north side of the element.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x, self.y - self.height / 2 - self.left_connector_length)\n    else:\n        return Point(self.x, self.y - self.height / 2)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.OrOperatorLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.OrOperatorLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.OrOperatorLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.OrOperatorLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.OrOperatorLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.OrOperatorLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.OrOperatorLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.OrOperatorLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.OrOperatorLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.OrOperatorLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.OrOperatorLayout.right_connector_base","title":"right_connector_base","text":"<pre><code>right_connector_base() -&gt; Point\n</code></pre> <p>Get the base point of the right connector.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point where the right connector attaches to the shape.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def right_connector_base(self) -&gt; Point:\n    \"\"\"Get the base point of the right connector.\n\n    Returns:\n        Point where the right connector attaches to the shape.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x, self.y + self.height / 2)\n    else:\n        return Point(self.x + self.width / 2, self.y)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.OrOperatorLayout.right_connector_tip","title":"right_connector_tip","text":"<pre><code>right_connector_tip() -&gt; Point\n</code></pre> <p>Get the tip point of the right connector.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point at the end of the right connector line.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def right_connector_tip(self) -&gt; Point:\n    \"\"\"Get the tip point of the right connector.\n\n    Returns:\n        Point at the end of the right connector line.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x, self.y + self.height / 2 + self.right_connector_length)\n    else:\n        return Point(self.x + self.width / 2 + self.right_connector_length, self.y)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.OrOperatorLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.OrOperatorLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Get the south (bottom) anchor point.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point on the south side of the element.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Get the south (bottom) anchor point.\n\n    Returns:\n        Point on the south side of the element.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x, self.y + self.height / 2 + self.right_connector_length)\n    else:\n        return Point(self.x, self.y + self.height / 2)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.OrOperatorLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.OrOperatorLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.OrOperatorLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.OrOperatorLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.OrOperatorLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.OrOperatorLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Get the west (left) anchor point.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point on the west side of the element.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Get the west (left) anchor point.\n\n    Returns:\n        Point on the west side of the element.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x - self.width / 2, self.y)\n    else:\n        return Point(self.x - self.width / 2 - self.left_connector_length, self.y)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.OrOperatorLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.OrOperatorLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.OrOperatorLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.OrOperatorLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PerturbationUnitOfInformation","title":"PerturbationUnitOfInformation  <code>dataclass</code>","text":"<pre><code>PerturbationUnitOfInformation(*, id_: str = make_uuid4_as_str(), label: str | None = None)\n</code></pre> <p>               Bases: <code>UnitOfInformation</code></p> <p>Perturbation unit of information.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'0d50b7e4-32d5-45e1-863d-e4f514e8aa80'</code> <code>label</code> <code>str | None</code> <p>The label of the unit of information.</p> <code>None</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PerturbationUnitOfInformation.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PerturbationUnitOfInformationLayout","title":"PerturbationUnitOfInformationLayout  <code>dataclass</code>","text":"<pre><code>PerturbationUnitOfInformationLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = white, filter_: NoneValueType | Filter | None = None, height: float = 12.0, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = 1.25, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 18.0, angle: float = 70.0)\n</code></pre> <p>               Bases: <code>_SimpleMixin</code>, <code>SBGNNode</code></p> <p>Perturbation unit of information layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'6d383dd1-b1c4-4e39-a176-ee43ccb32d40'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>12.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The stroke width of the node</p> <code>1.25</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>18.0</code> <code>angle</code> <code>float</code> <p>The angle of the notched sides.</p> <code>70.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Return the east anchor of the node.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Return the label center anchor of the node.</p> <code>north</code> <p>Return the north anchor of the node.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Return the south anchor of the node.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Return the west anchor of the node.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>angle</code> <code>float</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PerturbationUnitOfInformationLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PerturbationUnitOfInformationLayout.angle","title":"angle  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>angle: float = dataclasses.field(default=70.0, metadata={'description': 'The angle of the notched sides.'})\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PerturbationUnitOfInformationLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PerturbationUnitOfInformationLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PerturbationUnitOfInformationLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PerturbationUnitOfInformationLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PerturbationUnitOfInformationLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PerturbationUnitOfInformationLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PerturbationUnitOfInformationLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PerturbationUnitOfInformationLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PerturbationUnitOfInformationLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Return the east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Return the east anchor of the node.\"\"\"\n    return self.own_angle(0)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PerturbationUnitOfInformationLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PerturbationUnitOfInformationLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PerturbationUnitOfInformationLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PerturbationUnitOfInformationLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PerturbationUnitOfInformationLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Return the label center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Return the label center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PerturbationUnitOfInformationLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Return the north anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Return the north anchor of the node.\"\"\"\n    return self.own_angle(90)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PerturbationUnitOfInformationLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PerturbationUnitOfInformationLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PerturbationUnitOfInformationLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PerturbationUnitOfInformationLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PerturbationUnitOfInformationLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PerturbationUnitOfInformationLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PerturbationUnitOfInformationLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PerturbationUnitOfInformationLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PerturbationUnitOfInformationLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PerturbationUnitOfInformationLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PerturbationUnitOfInformationLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PerturbationUnitOfInformationLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Return the south anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Return the south anchor of the node.\"\"\"\n    return self.own_angle(270)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PerturbationUnitOfInformationLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PerturbationUnitOfInformationLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PerturbationUnitOfInformationLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PerturbationUnitOfInformationLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PerturbationUnitOfInformationLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PerturbationUnitOfInformationLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Return the west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Return the west anchor of the node.\"\"\"\n    return self.own_angle(180)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PerturbationUnitOfInformationLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PerturbationUnitOfInformationLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PerturbationUnitOfInformationLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PerturbationUnitOfInformationLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.Phenotype","title":"Phenotype  <code>dataclass</code>","text":"<pre><code>Phenotype(*, id_: str = make_uuid4_as_str(), label: str | None = None, compartment: Compartment | None = None)\n</code></pre> <p>               Bases: <code>Activity</code></p> <p>Phenotype.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'abdde4f5-d030-43ca-94b2-6b137c9617de'</code> <code>label</code> <code>str | None</code> <p>The label of the activity.</p> <code>None</code> <code>compartment</code> <code>Compartment | None</code> <p>The compartment containing this activity.</p> <code>None</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.Phenotype.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PhenotypeLayout","title":"PhenotypeLayout  <code>dataclass</code>","text":"<pre><code>PhenotypeLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = white, filter_: NoneValueType | Filter | None = None, height: float = 30.0, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = 1.25, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 60.0, angle: float = 70.0)\n</code></pre> <p>               Bases: <code>_SimpleMixin</code>, <code>SBGNNode</code></p> <p>Phenotype layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'711b813e-f1ff-437b-a40e-610fe1cd988d'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>30.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The stroke width of the node</p> <code>1.25</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>60.0</code> <code>angle</code> <code>float</code> <p>The angle of the slanted sides.</p> <code>70.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Return the east anchor of the node.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Return the label center anchor of the node.</p> <code>north</code> <p>Return the north anchor of the node.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Return the south anchor of the node.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Return the west anchor of the node.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>angle</code> <code>float</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PhenotypeLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PhenotypeLayout.angle","title":"angle  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>angle: float = dataclasses.field(default=70.0, metadata={'description': 'The angle of the slanted sides.'})\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PhenotypeLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PhenotypeLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PhenotypeLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PhenotypeLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PhenotypeLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PhenotypeLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PhenotypeLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PhenotypeLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PhenotypeLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Return the east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Return the east anchor of the node.\"\"\"\n    return self.own_angle(0)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PhenotypeLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PhenotypeLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PhenotypeLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PhenotypeLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PhenotypeLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Return the label center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Return the label center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PhenotypeLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Return the north anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Return the north anchor of the node.\"\"\"\n    return self.own_angle(90)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PhenotypeLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PhenotypeLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PhenotypeLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PhenotypeLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PhenotypeLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PhenotypeLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PhenotypeLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PhenotypeLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PhenotypeLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PhenotypeLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PhenotypeLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PhenotypeLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Return the south anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Return the south anchor of the node.\"\"\"\n    return self.own_angle(270)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PhenotypeLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PhenotypeLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PhenotypeLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PhenotypeLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PhenotypeLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PhenotypeLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Return the west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Return the west anchor of the node.\"\"\"\n    return self.own_angle(180)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PhenotypeLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PhenotypeLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PhenotypeLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PhenotypeLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PositiveInfluence","title":"PositiveInfluence  <code>dataclass</code>","text":"<pre><code>PositiveInfluence(*, id_: str = make_uuid4_as_str(), source: BiologicalActivity | LogicalOperator, target: Activity)\n</code></pre> <p>               Bases: <code>Influence</code></p> <p>Positive influence.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'5546a955-c471-475d-b960-c866f77b3b52'</code> <code>source</code> <code>BiologicalActivity | LogicalOperator</code> <p>The source activity or logical operator.</p> required <code>target</code> <code>Activity</code> <p>The target activity being influenced.</p> required <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PositiveInfluence.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PositiveInfluenceLayout","title":"PositiveInfluenceLayout  <code>dataclass</code>","text":"<pre><code>PositiveInfluenceLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, end_shorten: float = 0.0, fill: NoneValueType | Color | None = None, filter_: NoneValueType | Filter | None = None, path_fill: NoneValueType | Color | None = NoneValue, path_filter: NoneValueType | Filter | None = None, path_stroke: NoneValueType | Color | None = black, path_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, path_stroke_dashoffset: NoneValueType | float | None = None, path_stroke_width: float | None = 1.25, path_transform: NoneValueType | tuple[Transformation, ...] | None = None, stroke: NoneValueType | Color | None = None, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: NoneValueType | float | None = None, segments: tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...] = tuple(), source: LayoutElement | None = None, start_shorten: float = 0.0, target: LayoutElement | None = None, transform: NoneValueType | tuple[Transformation, ...] | None = None, arrowhead_fill: NoneValueType | Color | None = white, arrowhead_filter: NoneValueType | Filter | None = None, arrowhead_stroke: NoneValueType | Color | None = black, arrowhead_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, arrowhead_stroke_dashoffset: NoneValueType | float | None = None, arrowhead_stroke_width: float | None = 1.25, arrowhead_transform: NoneValueType | tuple[Transformation, ...] | None = None, arrowhead_height: float = 10.0, arrowhead_width: float = 10.0)\n</code></pre> <p>               Bases: <code>SBGNSingleHeadedArc</code></p> <p>Positive influence layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'c81493f8-70ae-4207-ab96-151291079337'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>end_shorten</code> <code>float</code> <p>The length the end of the arc will be shorten by</p> <code>0.0</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the arc</p> <code>None</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the arc</p> <code>None</code> <code>path_fill</code> <code>NoneValueType | Color | None</code> <p>The path fill color of the arc</p> <code>&lt;momapy.drawing.NoneValueType object at 0x7f8b17323e00&gt;</code> <code>path_filter</code> <code>NoneValueType | Filter | None</code> <p>The path filter of the arc</p> <code>None</code> <code>path_stroke</code> <code>NoneValueType | Color | None</code> <p>The path stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>path_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The path stroke dasharray of the arc</p> <code>None</code> <code>path_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The path stroke dashoffset of the arc</p> <code>None</code> <code>path_stroke_width</code> <code>float | None</code> <p>The path stroke width of the arc</p> <code>1.25</code> <code>path_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The path transform of the arc</p> <code>None</code> <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the arc</p> <code>None</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the arc</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the arc</p> <code>None</code> <code>stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the arc</p> <code>None</code> <code>segments</code> <code>tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...]</code> <p>The path segments of the arc</p> <code>&lt;dynamic&gt;</code> <code>source</code> <code>LayoutElement | None</code> <p>The source of the arc</p> <code>None</code> <code>start_shorten</code> <code>float</code> <p>The length the start of the arc will be shorten by</p> <code>0.0</code> <code>target</code> <code>LayoutElement | None</code> <p>The target of the arc</p> <code>None</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the arc</p> <code>None</code> <code>arrowhead_fill</code> <code>NoneValueType | Color | None</code> <p>The arrowhead fill color of the arc</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>arrowhead_filter</code> <code>NoneValueType | Filter | None</code> <p>The arrowhead filter of the arc</p> <code>None</code> <code>arrowhead_stroke</code> <code>NoneValueType | Color | None</code> <p>The arrowhead stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>arrowhead_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The arrowhead stroke dasharray of the arc</p> <code>None</code> <code>arrowhead_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The arrowhead stroke dashoffset of the arc</p> <code>None</code> <code>arrowhead_stroke_width</code> <code>float | None</code> <p>The arrowhead stroke width of the arc</p> <code>1.25</code> <code>arrowhead_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The arrowhead transform of the arc</p> <code>None</code> <code>arrowhead_height</code> <code>float</code> <p>The height of the arrowhead.</p> <code>10.0</code> <code>arrowhead_width</code> <code>float</code> <p>The width of the arrowhead.</p> <code>10.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>arrowhead_base</code> <p>Return the arrowhead base anchor point of the single-headed arc.</p> <code>arrowhead_bbox</code> <p>Return the bounding box of the single-headed arc arrowhead.</p> <code>arrowhead_border</code> <p>Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.</p> <code>arrowhead_drawing_elements</code> <p>Return the drawing elements of the single-headed arc arrowhead.</p> <code>arrowhead_length</code> <p>Return the length of the single-headed arc arrowhead.</p> <code>arrowhead_tip</code> <p>Return the arrowhead tip anchor point of the single-headed arc.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>childless</code> <p>Return a copy of the arc with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>end_point</code> <p>Return the ending point of the arc.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>fraction</code> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <code>length</code> <p>Return the total length of the arc path.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_children</code> <p>Return the self children of the arc.</p> <code>own_drawing_elements</code> <p>Return the self drawing elements of the single-headed arc.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>path_drawing_elements</code> <p>Return the drawing elements of the single-headed arc path.</p> <code>points</code> <p>Return the points of the arc path.</p> <code>start_point</code> <p>Return the starting point of the arc.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PositiveInfluenceLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PositiveInfluenceLayout.arrowhead_base","title":"arrowhead_base","text":"<pre><code>arrowhead_base() -&gt; Point\n</code></pre> <p>Return the arrowhead base anchor point of the single-headed arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_base(self) -&gt; Point:\n    \"\"\"Return the arrowhead base anchor point of the single-headed arc.\"\"\"\n    arrowhead_length = self.arrowhead_length()\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return self.arrowhead_tip() - (arrowhead_length, 0)\n    fraction = 1 - (arrowhead_length + self.end_shorten) / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PositiveInfluenceLayout.arrowhead_bbox","title":"arrowhead_bbox","text":"<pre><code>arrowhead_bbox() -&gt; Bbox\n</code></pre> <p>Return the bounding box of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_bbox(self) -&gt; Bbox:\n    \"\"\"Return the bounding box of the single-headed arc arrowhead.\"\"\"\n    return get_drawing_elements_bbox(self.arrowhead_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PositiveInfluenceLayout.arrowhead_border","title":"arrowhead_border","text":"<pre><code>arrowhead_border(point: Point) -&gt; Point\n</code></pre> <p>Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_border(self, point: Point) -&gt; Point:\n    \"\"\"Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    point = get_drawing_elements_border(self.arrowhead_drawing_elements(), point)\n    if point is None:\n        return self.arrowhead_tip()\n    return point\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PositiveInfluenceLayout.arrowhead_drawing_elements","title":"arrowhead_drawing_elements","text":"<pre><code>arrowhead_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_drawing_elements(\n    self,\n) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the single-headed arc arrowhead.\"\"\"\n    drawing_elements = self._arrowhead_border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_arrowhead\",\n        elements=tuple(drawing_elements),\n        fill=self.arrowhead_fill,\n        filter_=self.arrowhead_filter,\n        id_=f\"{self.id_}_arrowhead\",\n        stroke=self.arrowhead_stroke,\n        stroke_dasharray=self.arrowhead_stroke_dasharray,\n        stroke_dashoffset=self.arrowhead_stroke_dashoffset,\n        stroke_width=self.arrowhead_stroke_width,\n        transform=self.arrowhead_transform,\n    )\n    transformation = self._get_arrowhead_transformation()\n    group = group.transformed(transformation)\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PositiveInfluenceLayout.arrowhead_length","title":"arrowhead_length","text":"<pre><code>arrowhead_length() -&gt; float\n</code></pre> <p>Return the length of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_length(self) -&gt; float:\n    \"\"\"Return the length of the single-headed arc arrowhead.\"\"\"\n    bbox = get_drawing_elements_bbox(self._arrowhead_border_drawing_elements())\n    if math.isnan(bbox.width):\n        return 0.0\n    return bbox.east().x\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PositiveInfluenceLayout.arrowhead_tip","title":"arrowhead_tip","text":"<pre><code>arrowhead_tip() -&gt; Point\n</code></pre> <p>Return the arrowhead tip anchor point of the single-headed arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_tip(self) -&gt; Point:\n    \"\"\"Return the arrowhead tip anchor point of the single-headed arc.\"\"\"\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return segment.p2\n    fraction = 1 - self.end_shorten / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PositiveInfluenceLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PositiveInfluenceLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the arc with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the arc with no children.\"\"\"\n    return dataclasses.replace(self, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PositiveInfluenceLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PositiveInfluenceLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PositiveInfluenceLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PositiveInfluenceLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PositiveInfluenceLayout.end_point","title":"end_point","text":"<pre><code>end_point() -&gt; Point\n</code></pre> <p>Return the ending point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_point(self) -&gt; Point:\n    \"\"\"Return the ending point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[-1]\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PositiveInfluenceLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PositiveInfluenceLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PositiveInfluenceLayout.fraction","title":"fraction","text":"<pre><code>fraction(fraction: float) -&gt; tuple[Point, float]\n</code></pre> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def fraction(self, fraction: float) -&gt; tuple[Point, float]:\n    \"\"\"Return the position and angle on the arc at a given fraction (of the total arc length).\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    current_length = 0\n    length_to_reach = fraction * self.length()\n    for segment in self.segments:\n        current_length += segment.length()\n        if current_length &gt;= length_to_reach:\n            break\n    segment_start_length = current_length - segment.length()\n    segment_fraction = (length_to_reach - segment_start_length) / segment.length()\n    position, angle = segment.get_position_and_angle_at_fraction(segment_fraction)\n    return position, angle\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PositiveInfluenceLayout.length","title":"length","text":"<pre><code>length() -&gt; float\n</code></pre> <p>Return the total length of the arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def length(self) -&gt; float:\n    \"\"\"Return the total length of the arc path.\"\"\"\n    return sum([segment.length() for segment in self.segments])\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PositiveInfluenceLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PositiveInfluenceLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the arc.\"\"\"\n    return []\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PositiveInfluenceLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the self drawing elements of the single-headed arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the self drawing elements of the single-headed arc.\"\"\"\n    drawing_elements = (\n        self.path_drawing_elements() + self.arrowhead_drawing_elements()\n    )\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PositiveInfluenceLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PositiveInfluenceLayout.path_drawing_elements","title":"path_drawing_elements","text":"<pre><code>path_drawing_elements() -&gt; list[Path]\n</code></pre> <p>Return the drawing elements of the single-headed arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def path_drawing_elements(self) -&gt; list[Path]:\n    \"\"\"Return the drawing elements of the single-headed arc path.\"\"\"\n    arrowhead_length = self.arrowhead_length()\n    if len(self.segments) == 1:\n        segment = (\n            self.segments[0]\n            .shortened(self.start_shorten, \"start\")\n            .shortened(self.end_shorten + arrowhead_length, \"end\")\n        )\n        actions = [\n            MoveTo(segment.p1),\n            self._make_path_action_from_segment(segment),\n        ]\n    else:\n        first_segment = self.segments[0].shortened(self.start_shorten, \"start\")\n        last_segment = self.segments[-1].shortened(\n            self.end_shorten + arrowhead_length, \"end\"\n        )\n        actions = [\n            MoveTo(first_segment.p1),\n            self._make_path_action_from_segment(first_segment),\n        ]\n        for segment in self.segments[1:-1]:\n            action = self._make_path_action_from_segment(segment)\n            actions.append(action)\n        actions.append(self._make_path_action_from_segment(last_segment))\n    path = Path(\n        actions=tuple(actions),\n        class_=f\"{type(self).__name__}_path\",\n        fill=self.path_fill,\n        filter_=self.path_filter,\n        id_=f\"{self.id_}_path\",\n        stroke=self.path_stroke,\n        stroke_dasharray=self.path_stroke_dasharray,\n        stroke_dashoffset=self.path_stroke_dashoffset,\n        stroke_width=self.path_stroke_width,\n        transform=self.path_transform,\n    )\n    return [path]\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PositiveInfluenceLayout.points","title":"points","text":"<pre><code>points() -&gt; list[Point]\n</code></pre> <p>Return the points of the arc path.</p> <p>An arc with no segments has no points and returns an empty list.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def points(self) -&gt; list[Point]:\n    \"\"\"Return the points of the arc path.\n\n    An arc with no segments has no points and returns an empty list.\n    \"\"\"\n    if not self.segments:\n        return []\n    points = []\n    for segment in self.segments:\n        points.append(segment.p1)\n    points.append(segment.p2)\n    return points\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PositiveInfluenceLayout.start_point","title":"start_point","text":"<pre><code>start_point() -&gt; Point\n</code></pre> <p>Return the starting point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_point(self) -&gt; Point:\n    \"\"\"Return the starting point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[0]\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.PositiveInfluenceLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SBGNAFLayout","title":"SBGNAFLayout  <code>dataclass</code>","text":"<pre><code>SBGNAFLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = white, filter_: NoneValueType | Filter | None = None, height: float, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = None, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: NoneValueType | float | None = None, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float)\n</code></pre> <p>               Bases: <code>SBGNLayout</code></p> <p>SBGN AF layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'710ae1f4-377d-4221-9e80-328ef6526f0e'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> required <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>None</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the node</p> <code>None</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> required <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>angle</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Return the east anchor of the node.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>is_sublayout</code> <p>Return <code>True</code> if the layout is a sublayout of another given layout, <code>False</code> otherwise.</p> <code>label_center</code> <p>Return the label center anchor of the node.</p> <code>north</code> <p>Return the north anchor of the node.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Return the south anchor of the node.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Return the west anchor of the node.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SBGNAFLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SBGNAFLayout.angle","title":"angle","text":"<pre><code>angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SBGNAFLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SBGNAFLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SBGNAFLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SBGNAFLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SBGNAFLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SBGNAFLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SBGNAFLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SBGNAFLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SBGNAFLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Return the east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Return the east anchor of the node.\"\"\"\n    return self.own_angle(0)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SBGNAFLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SBGNAFLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SBGNAFLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SBGNAFLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SBGNAFLayout.is_sublayout","title":"is_sublayout","text":"<pre><code>is_sublayout(other: Layout, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout is a sublayout of another given layout, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def is_sublayout(\n    self, other: \"Layout\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout is a sublayout of another given layout, `False` otherwise.\"\"\"\n\n    def _is_sublist(\n        list1: list[LayoutElement],\n        list2: list[LayoutElement],\n        unordered: bool = False,\n    ) -&gt; bool:\n        if not unordered:\n            i = 0\n            for elem1 in list1:\n                elem2 = list2[i]\n                while elem2 != elem1 and i &lt; len(list2) - 1:\n                    i += 1\n                    elem2 = list2[i]\n                if not elem2 == elem1:\n                    return False\n                i += 1\n        else:\n            dlist1 = collections.defaultdict(int)\n            dlist2 = collections.defaultdict(int)\n            for elem1 in list1:\n                dlist1[elem1] += 1\n            for elem2 in list2:\n                dlist2[elem2] += 1\n            for elem in dlist1:\n                if dlist1[elem] &gt; dlist2[elem]:\n                    return False\n        return True\n\n    if self.childless() != other.childless():\n        return False\n    if flattened:\n        return _is_sublist(\n            self.flattened()[1:],\n            other.flattened()[1:],\n            unordered=unordered,\n        )\n    return _is_sublist(self.children(), other.children(), unordered=unordered)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SBGNAFLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Return the label center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Return the label center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SBGNAFLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Return the north anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Return the north anchor of the node.\"\"\"\n    return self.own_angle(90)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SBGNAFLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SBGNAFLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SBGNAFLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SBGNAFLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SBGNAFLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SBGNAFLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SBGNAFLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SBGNAFLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SBGNAFLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SBGNAFLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SBGNAFLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SBGNAFLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Return the south anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Return the south anchor of the node.\"\"\"\n    return self.own_angle(270)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SBGNAFLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SBGNAFLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SBGNAFLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SBGNAFLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SBGNAFLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SBGNAFLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Return the west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Return the west anchor of the node.\"\"\"\n    return self.own_angle(180)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SBGNAFLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SBGNAFLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SBGNAFLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SBGNAFLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SBGNAFMap","title":"SBGNAFMap  <code>dataclass</code>","text":"<pre><code>SBGNAFMap(*, id_: str = make_uuid4_as_str(), model: SBGNAFModel | None = None, layout: SBGNAFLayout | None = None, layout_model_mapping: LayoutModelMapping | None = None)\n</code></pre> <p>               Bases: <code>SBGNMap</code></p> <p>Class for SBGN-AF maps.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'349d1785-aa2c-4dff-a096-3e837d0c0d31'</code> <code>model</code> <code>SBGNAFModel | None</code> <p>The model of the map</p> <code>None</code> <code>layout</code> <code>SBGNAFLayout | None</code> <p>The layout of the map</p> <code>None</code> <code>layout_model_mapping</code> <code>LayoutModelMapping | None</code> <p>The layout model mapping of the map</p> <code>None</code> <p>Methods:</p> Name Description <code>get_mapping</code> <p>Return the model element or layout elements mapped to <code>map_element</code>.</p> <code>is_submap</code> <p>Return <code>True</code> if the <code>Map</code> is a submap of another given map, <code>False</code> otherwise.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SBGNAFMap.get_mapping","title":"get_mapping","text":"<pre><code>get_mapping(map_element: MapElement) -&gt; ModelElement | list[LayoutElement | frozenset[LayoutElement]] | None\n</code></pre> <p>Return the model element or layout elements mapped to <code>map_element</code>.</p> <p>The lookup is bidirectional: a layout key (a singleton or frozenset) resolves to its model element, and a model element resolves to the list of layout keys mapped to it. Forwards to <code>layout_model_mapping.get_mapping</code>.</p> <p>Returns <code>None</code> when the map has no <code>layout_model_mapping</code> (for example a layout-less map such as one read from SBML).</p> Source code in <code>src/momapy/core/map.py</code> <pre><code>def get_mapping(\n    self,\n    map_element: \"MapElement\",\n) -&gt; \"ModelElement | list[LayoutElement | frozenset[LayoutElement]] | None\":\n    \"\"\"Return the model element or layout elements mapped to `map_element`.\n\n    The lookup is bidirectional: a layout key (a singleton or frozenset)\n    resolves to its model element, and a model element resolves to the\n    list of layout keys mapped to it. Forwards to\n    `layout_model_mapping.get_mapping`.\n\n    Returns `None` when the map has no `layout_model_mapping` (for\n    example a layout-less map such as one read from SBML).\n    \"\"\"\n    if self.layout_model_mapping is None:\n        return None\n    return self.layout_model_mapping.get_mapping(map_element)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SBGNAFMap.is_submap","title":"is_submap","text":"<pre><code>is_submap(other: Map) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the <code>Map</code> is a submap of another given map, <code>False</code> otherwise.</p> <p>A complete map is never a submap of an incomplete one: if either <code>self</code> or <code>other</code> has a <code>None</code> model, layout, or layout-model mapping, this returns <code>False</code>.</p> Source code in <code>src/momapy/core/map.py</code> <pre><code>def is_submap(self, other: \"Map\") -&gt; bool:\n    \"\"\"Return `True` if the `Map` is a submap of another given map, `False` otherwise.\n\n    A complete map is never a submap of an incomplete one: if either `self`\n    or `other` has a `None` model, layout, or layout-model mapping, this\n    returns `False`.\n    \"\"\"\n    if (\n        self.model is None\n        or self.layout is None\n        or self.layout_model_mapping is None\n        or other.model is None\n        or other.layout is None\n        or other.layout_model_mapping is None\n    ):\n        return False\n    return (\n        self.model.is_submodel(other.model)\n        and self.layout.is_sublayout(other.layout)\n        and self.layout_model_mapping.is_submapping(other.layout_model_mapping)\n    )\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SBGNAFModel","title":"SBGNAFModel  <code>dataclass</code>","text":"<pre><code>SBGNAFModel(*, id_: str = make_uuid4_as_str(), activities: frozenset[Activity] = frozenset(), compartments: frozenset[Compartment] = frozenset(), influences: frozenset[Influence] = frozenset(), logical_operators: frozenset[LogicalOperator] = frozenset(), submaps: frozenset[Submap] = frozenset(), tags: frozenset[Tag] = frozenset())\n</code></pre> <p>               Bases: <code>SBGNModel</code></p> <p>SBGN AF model.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'ea9d3d93-9ce2-40c8-8936-b5678f72ecfe'</code> <code>activities</code> <code>frozenset[Activity]</code> <p>The activities of the SBGN-AF model.</p> <code>&lt;dynamic&gt;</code> <code>compartments</code> <code>frozenset[Compartment]</code> <p>The compartments of the SBGN-AF model.</p> <code>&lt;dynamic&gt;</code> <code>influences</code> <code>frozenset[Influence]</code> <p>The influences of the SBGN-AF model.</p> <code>&lt;dynamic&gt;</code> <code>logical_operators</code> <code>frozenset[LogicalOperator]</code> <p>The logical operators of the SBGN-AF model.</p> <code>&lt;dynamic&gt;</code> <code>submaps</code> <code>frozenset[Submap]</code> <p>The submaps of the SBGN-AF model.</p> <code>&lt;dynamic&gt;</code> <code>tags</code> <code>frozenset[Tag]</code> <p>The tags of the SBGN-AF model.</p> <code>&lt;dynamic&gt;</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from this <code>Model</code>.</p> <code>is_submodel</code> <p>Check if this model is a submodel of another model.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SBGNAFModel.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from this <code>Model</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers of the <code>Model</code>, deduplicating by object identity. The <code>Model</code> itself is not a <code>ModelElement</code> and is not included.</p> <p>This mirrors <code>ModelElement.descendants</code> and contrasts with <code>LayoutElement.descendants</code>, which walks the explicit <code>children()</code> tree (visual containment) rather than reference reachability.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order.</p> Source code in <code>src/momapy/core/model.py</code> <pre><code>def descendants(self) -&gt; list[ModelElement]:\n    \"\"\"Return every `ModelElement` reachable from this `Model`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers of the `Model`, deduplicating by\n    object identity. The `Model` itself is not a `ModelElement` and is\n    not included.\n\n    This mirrors\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and contrasts with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which walks the explicit `children()` tree (visual *containment*)\n    rather than reference reachability.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order.\n    \"\"\"\n    seen: set[int] = set()\n    result: list[ModelElement] = []\n    for field in dataclasses.fields(type(self)):\n        _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SBGNAFModel.is_submodel","title":"is_submodel","text":"<pre><code>is_submodel(other: SBGNAFModel) -&gt; bool\n</code></pre> <p>Check if this model is a submodel of another model.</p> <p>Parameters:</p> Name Type Description Default <code>other</code> <code>SBGNAFModel</code> <p>Another SBGN-AF model to compare against.</p> required <p>Returns:</p> Type Description <code>bool</code> <p>True if this model is a submodel of <code>other</code>, False otherwise.</p> Source code in <code>src/momapy/sbgn/af/model.py</code> <pre><code>def is_submodel(self, other: \"SBGNAFModel\") -&gt; bool:\n    \"\"\"Check if this model is a submodel of another model.\n\n    Args:\n        other: Another SBGN-AF model to compare against.\n\n    Returns:\n        True if this model is a submodel of `other`, False otherwise.\n    \"\"\"\n    return (\n        self.activities.issubset(other.activities)\n        and self.compartments.issubset(other.compartments)\n        and self.influences.issubset(other.influences)\n        and self.logical_operators.issubset(other.logical_operators)\n        and self.submaps.issubset(other.submaps)\n        and self.tags.issubset(other.tags)\n    )\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SimpleChemicalUnitOfInformation","title":"SimpleChemicalUnitOfInformation  <code>dataclass</code>","text":"<pre><code>SimpleChemicalUnitOfInformation(*, id_: str = make_uuid4_as_str(), label: str | None = None)\n</code></pre> <p>               Bases: <code>UnitOfInformation</code></p> <p>Simple chemical unit of information.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'a250addf-e4f0-4a61-9182-65165863c089'</code> <code>label</code> <code>str | None</code> <p>The label of the unit of information.</p> <code>None</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SimpleChemicalUnitOfInformation.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SimpleChemicalUnitOfInformationLayout","title":"SimpleChemicalUnitOfInformationLayout  <code>dataclass</code>","text":"<pre><code>SimpleChemicalUnitOfInformationLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = white, filter_: NoneValueType | Filter | None = None, height: float = 12.0, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = 1.25, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 12.0)\n</code></pre> <p>               Bases: <code>_SimpleMixin</code>, <code>SBGNNode</code></p> <p>Simple chemical unit of information layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'fb6b6916-3453-49b2-b217-594d85816afc'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>12.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The stroke width of the node</p> <code>1.25</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>12.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>angle</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Return the east anchor of the node.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Return the label center anchor of the node.</p> <code>north</code> <p>Return the north anchor of the node.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Return the south anchor of the node.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Return the west anchor of the node.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SimpleChemicalUnitOfInformationLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SimpleChemicalUnitOfInformationLayout.angle","title":"angle","text":"<pre><code>angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SimpleChemicalUnitOfInformationLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SimpleChemicalUnitOfInformationLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SimpleChemicalUnitOfInformationLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SimpleChemicalUnitOfInformationLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SimpleChemicalUnitOfInformationLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SimpleChemicalUnitOfInformationLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SimpleChemicalUnitOfInformationLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SimpleChemicalUnitOfInformationLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SimpleChemicalUnitOfInformationLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Return the east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Return the east anchor of the node.\"\"\"\n    return self.own_angle(0)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SimpleChemicalUnitOfInformationLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SimpleChemicalUnitOfInformationLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SimpleChemicalUnitOfInformationLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SimpleChemicalUnitOfInformationLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SimpleChemicalUnitOfInformationLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Return the label center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Return the label center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SimpleChemicalUnitOfInformationLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Return the north anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Return the north anchor of the node.\"\"\"\n    return self.own_angle(90)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SimpleChemicalUnitOfInformationLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SimpleChemicalUnitOfInformationLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SimpleChemicalUnitOfInformationLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SimpleChemicalUnitOfInformationLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SimpleChemicalUnitOfInformationLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SimpleChemicalUnitOfInformationLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SimpleChemicalUnitOfInformationLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SimpleChemicalUnitOfInformationLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SimpleChemicalUnitOfInformationLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SimpleChemicalUnitOfInformationLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SimpleChemicalUnitOfInformationLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SimpleChemicalUnitOfInformationLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Return the south anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Return the south anchor of the node.\"\"\"\n    return self.own_angle(270)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SimpleChemicalUnitOfInformationLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SimpleChemicalUnitOfInformationLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SimpleChemicalUnitOfInformationLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SimpleChemicalUnitOfInformationLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SimpleChemicalUnitOfInformationLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SimpleChemicalUnitOfInformationLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Return the west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Return the west anchor of the node.\"\"\"\n    return self.own_angle(180)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SimpleChemicalUnitOfInformationLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SimpleChemicalUnitOfInformationLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SimpleChemicalUnitOfInformationLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SimpleChemicalUnitOfInformationLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.Submap","title":"Submap  <code>dataclass</code>","text":"<pre><code>Submap(*, id_: str = make_uuid4_as_str(), label: str | None = None, terminals: frozenset[Terminal] = frozenset())\n</code></pre> <p>               Bases: <code>SBGNModelElement</code></p> <p>Submap.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'2fc5052f-a8d3-46a9-af83-59d1bd9a6dd1'</code> <code>label</code> <code>str | None</code> <p>The label of the submap.</p> <code>None</code> <code>terminals</code> <code>frozenset[Terminal]</code> <p>The terminals of the submap.</p> <code>&lt;dynamic&gt;</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.Submap.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SubmapLayout","title":"SubmapLayout  <code>dataclass</code>","text":"<pre><code>SubmapLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = white, filter_: NoneValueType | Filter | None = None, height: float = 80.0, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float = 2.25, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 80.0)\n</code></pre> <p>               Bases: <code>_SimpleMixin</code>, <code>SBGNNode</code></p> <p>Submap layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'ebb40bc8-cc70-4a33-ba7c-9e51b6f4a072'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>80.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float</code> <p>The stroke width of the node</p> <code>2.25</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>80.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>angle</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Return the east anchor of the node.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Return the label center anchor of the node.</p> <code>north</code> <p>Return the north anchor of the node.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Return the south anchor of the node.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Return the west anchor of the node.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SubmapLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SubmapLayout.angle","title":"angle","text":"<pre><code>angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SubmapLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SubmapLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SubmapLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SubmapLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SubmapLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SubmapLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SubmapLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SubmapLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SubmapLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Return the east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Return the east anchor of the node.\"\"\"\n    return self.own_angle(0)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SubmapLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SubmapLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SubmapLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SubmapLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SubmapLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Return the label center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Return the label center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SubmapLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Return the north anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Return the north anchor of the node.\"\"\"\n    return self.own_angle(90)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SubmapLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SubmapLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SubmapLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SubmapLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SubmapLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SubmapLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SubmapLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SubmapLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SubmapLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SubmapLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SubmapLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SubmapLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Return the south anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Return the south anchor of the node.\"\"\"\n    return self.own_angle(270)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SubmapLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SubmapLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SubmapLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SubmapLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SubmapLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SubmapLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Return the west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Return the west anchor of the node.\"\"\"\n    return self.own_angle(180)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SubmapLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SubmapLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SubmapLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.SubmapLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.Tag","title":"Tag  <code>dataclass</code>","text":"<pre><code>Tag(*, id_: str = make_uuid4_as_str(), label: str | None = None, referred_element: TagReference | None = None)\n</code></pre> <p>               Bases: <code>SBGNModelElement</code></p> <p>Tag.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'0e4ab4e0-5cc7-4041-8f57-6489c965268d'</code> <code>label</code> <code>str | None</code> <p>The label of the tag.</p> <code>None</code> <code>referred_element</code> <code>TagReference | None</code> <p>The element referred to by the tag.</p> <code>None</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.Tag.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.TagLayout","title":"TagLayout  <code>dataclass</code>","text":"<pre><code>TagLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = white, filter_: NoneValueType | Filter | None = None, height: float = 35.0, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = 1.25, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 35.0, direction: Direction = RIGHT, angle: float = 70.0)\n</code></pre> <p>               Bases: <code>_SimpleMixin</code>, <code>SBGNNode</code></p> <p>Tag layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'8f64e9ab-168f-4ff4-9ba8-77693dda9d56'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>35.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The stroke width of the node</p> <code>1.25</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>35.0</code> <code>direction</code> <code>Direction</code> <p>The direction the tag points to.</p> <code>&lt;Direction.RIGHT: 2&gt;</code> <code>angle</code> <code>float</code> <p>The angle of the pointed side.</p> <code>70.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Return the east anchor of the node.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Return the label center anchor of the node.</p> <code>north</code> <p>Return the north anchor of the node.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Return the south anchor of the node.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Return the west anchor of the node.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>angle</code> <code>float</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.TagLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.TagLayout.angle","title":"angle  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>angle: float = dataclasses.field(default=70.0, metadata={'description': 'The angle of the pointed side.'})\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.TagLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.TagLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.TagLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.TagLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.TagLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.TagLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.TagLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.TagLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.TagLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Return the east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Return the east anchor of the node.\"\"\"\n    return self.own_angle(0)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.TagLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.TagLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.TagLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.TagLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.TagLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Return the label center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Return the label center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.TagLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Return the north anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Return the north anchor of the node.\"\"\"\n    return self.own_angle(90)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.TagLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.TagLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.TagLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.TagLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.TagLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.TagLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.TagLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.TagLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.TagLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.TagLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.TagLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.TagLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Return the south anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Return the south anchor of the node.\"\"\"\n    return self.own_angle(270)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.TagLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.TagLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.TagLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.TagLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.TagLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.TagLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Return the west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Return the west anchor of the node.\"\"\"\n    return self.own_angle(180)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.TagLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.TagLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.TagLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.TagLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.TagReference","title":"TagReference  <code>dataclass</code>","text":"<pre><code>TagReference(*, id_: str = make_uuid4_as_str(), referred_element: Activity | Compartment)\n</code></pre> <p>               Bases: <code>SBGNRole</code></p> <p>Tag reference.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'5c4fd0ba-e65c-40d4-9926-56e5dfb2ff51'</code> <code>referred_element</code> <code>Activity | Compartment</code> <p>The activity or compartment being referenced.</p> required <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.TagReference.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.Terminal","title":"Terminal  <code>dataclass</code>","text":"<pre><code>Terminal(*, id_: str = make_uuid4_as_str(), label: str | None = None, referred_element: TerminalReference | None = None)\n</code></pre> <p>               Bases: <code>SBGNAuxiliaryUnit</code></p> <p>Terminal.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'2718b92f-3aa4-47d7-826d-16d32eedad42'</code> <code>label</code> <code>str | None</code> <p>The label of the terminal.</p> <code>None</code> <code>referred_element</code> <code>TerminalReference | None</code> <p>The element referred to by the terminal.</p> <code>None</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.Terminal.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.TerminalLayout","title":"TerminalLayout  <code>dataclass</code>","text":"<pre><code>TerminalLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = white, filter_: NoneValueType | Filter | None = None, height: float = 35.0, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = 1.25, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 35.0, direction: Direction = RIGHT, angle: float = 70.0)\n</code></pre> <p>               Bases: <code>_SimpleMixin</code>, <code>SBGNNode</code></p> <p>Terminal layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'42aefbd1-b3ca-42a5-bc58-7a13beb5ad1a'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>35.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The stroke width of the node</p> <code>1.25</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>35.0</code> <code>direction</code> <code>Direction</code> <p>The direction the terminal points to.</p> <code>&lt;Direction.RIGHT: 2&gt;</code> <code>angle</code> <code>float</code> <p>The angle of the notched side.</p> <code>70.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Return the east anchor of the node.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Return the label center anchor of the node.</p> <code>north</code> <p>Return the north anchor of the node.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Return the south anchor of the node.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Return the west anchor of the node.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>angle</code> <code>float</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.TerminalLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.TerminalLayout.angle","title":"angle  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>angle: float = dataclasses.field(default=70.0, metadata={'description': 'The angle of the notched side.'})\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.TerminalLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.TerminalLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.TerminalLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.TerminalLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.TerminalLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.TerminalLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.TerminalLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.TerminalLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.TerminalLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Return the east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Return the east anchor of the node.\"\"\"\n    return self.own_angle(0)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.TerminalLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.TerminalLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.TerminalLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.TerminalLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.TerminalLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Return the label center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Return the label center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.TerminalLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Return the north anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Return the north anchor of the node.\"\"\"\n    return self.own_angle(90)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.TerminalLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.TerminalLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.TerminalLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.TerminalLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.TerminalLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.TerminalLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.TerminalLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.TerminalLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.TerminalLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.TerminalLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.TerminalLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.TerminalLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Return the south anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Return the south anchor of the node.\"\"\"\n    return self.own_angle(270)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.TerminalLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.TerminalLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.TerminalLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.TerminalLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.TerminalLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.TerminalLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Return the west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Return the west anchor of the node.\"\"\"\n    return self.own_angle(180)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.TerminalLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.TerminalLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.TerminalLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.TerminalLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.TerminalReference","title":"TerminalReference  <code>dataclass</code>","text":"<pre><code>TerminalReference(*, id_: str = make_uuid4_as_str(), referred_element: Activity | Compartment)\n</code></pre> <p>               Bases: <code>SBGNRole</code></p> <p>Terminal reference.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'c4db8c6b-78c5-4fe8-96fd-3ef1475c36e8'</code> <code>referred_element</code> <code>Activity | Compartment</code> <p>The activity or compartment being referenced.</p> required <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.TerminalReference.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnitOfInformation","title":"UnitOfInformation  <code>dataclass</code>","text":"<pre><code>UnitOfInformation(*, id_: str = make_uuid4_as_str(), label: str | None = None)\n</code></pre> <p>               Bases: <code>SBGNAuxiliaryUnit</code></p> <p>Abstract base class for units of information.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'ed9c699e-fc01-4f3d-b89d-0a863eb3054d'</code> <code>label</code> <code>str | None</code> <p>The label of the unit of information.</p> <code>None</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnitOfInformation.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnitOfInformationLayout","title":"UnitOfInformationLayout  <code>dataclass</code>","text":"<pre><code>UnitOfInformationLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = white, filter_: NoneValueType | Filter | None = None, height: float = 12.0, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = 1.25, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 18.0)\n</code></pre> <p>               Bases: <code>_SimpleMixin</code>, <code>SBGNNode</code></p> <p>Unit of information layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'772cbebe-dc92-444e-a64c-f1faade3c207'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>12.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The stroke width of the node</p> <code>1.25</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>18.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>angle</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Return the east anchor of the node.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Return the label center anchor of the node.</p> <code>north</code> <p>Return the north anchor of the node.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Return the south anchor of the node.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Return the west anchor of the node.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnitOfInformationLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnitOfInformationLayout.angle","title":"angle","text":"<pre><code>angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnitOfInformationLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnitOfInformationLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnitOfInformationLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnitOfInformationLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnitOfInformationLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnitOfInformationLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnitOfInformationLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnitOfInformationLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnitOfInformationLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Return the east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Return the east anchor of the node.\"\"\"\n    return self.own_angle(0)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnitOfInformationLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnitOfInformationLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnitOfInformationLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnitOfInformationLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnitOfInformationLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Return the label center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Return the label center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnitOfInformationLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Return the north anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Return the north anchor of the node.\"\"\"\n    return self.own_angle(90)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnitOfInformationLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnitOfInformationLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnitOfInformationLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnitOfInformationLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnitOfInformationLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnitOfInformationLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnitOfInformationLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnitOfInformationLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnitOfInformationLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnitOfInformationLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnitOfInformationLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnitOfInformationLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Return the south anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Return the south anchor of the node.\"\"\"\n    return self.own_angle(270)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnitOfInformationLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnitOfInformationLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnitOfInformationLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnitOfInformationLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnitOfInformationLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnitOfInformationLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Return the west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Return the west anchor of the node.\"\"\"\n    return self.own_angle(180)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnitOfInformationLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnitOfInformationLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnitOfInformationLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnitOfInformationLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnknownInfluence","title":"UnknownInfluence  <code>dataclass</code>","text":"<pre><code>UnknownInfluence(*, id_: str = make_uuid4_as_str(), source: BiologicalActivity | LogicalOperator, target: Activity)\n</code></pre> <p>               Bases: <code>Influence</code></p> <p>Unknown influence.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'d606faec-2921-41fa-8be5-00aca396f23c'</code> <code>source</code> <code>BiologicalActivity | LogicalOperator</code> <p>The source activity or logical operator.</p> required <code>target</code> <code>Activity</code> <p>The target activity being influenced.</p> required <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnknownInfluence.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnknownInfluenceLayout","title":"UnknownInfluenceLayout  <code>dataclass</code>","text":"<pre><code>UnknownInfluenceLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, end_shorten: float = 0.0, fill: NoneValueType | Color | None = None, filter_: NoneValueType | Filter | None = None, path_fill: NoneValueType | Color | None = NoneValue, path_filter: NoneValueType | Filter | None = None, path_stroke: NoneValueType | Color | None = black, path_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, path_stroke_dashoffset: NoneValueType | float | None = None, path_stroke_width: float | None = 1.25, path_transform: NoneValueType | tuple[Transformation, ...] | None = None, stroke: NoneValueType | Color | None = None, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: NoneValueType | float | None = None, segments: tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...] = tuple(), source: LayoutElement | None = None, start_shorten: float = 0.0, target: LayoutElement | None = None, transform: NoneValueType | tuple[Transformation, ...] | None = None, arrowhead_fill: NoneValueType | Color | None = white, arrowhead_filter: NoneValueType | Filter | None = None, arrowhead_stroke: NoneValueType | Color | None = black, arrowhead_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, arrowhead_stroke_dashoffset: NoneValueType | float | None = None, arrowhead_stroke_width: float | None = 1.25, arrowhead_transform: NoneValueType | tuple[Transformation, ...] | None = None, arrowhead_height: float = 10.0, arrowhead_width: float = 10.0)\n</code></pre> <p>               Bases: <code>SBGNSingleHeadedArc</code></p> <p>Unknown influence layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'62160382-ae0c-4bc6-b096-5f5609826b6a'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>end_shorten</code> <code>float</code> <p>The length the end of the arc will be shorten by</p> <code>0.0</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the arc</p> <code>None</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the arc</p> <code>None</code> <code>path_fill</code> <code>NoneValueType | Color | None</code> <p>The path fill color of the arc</p> <code>&lt;momapy.drawing.NoneValueType object at 0x7f8b17323e00&gt;</code> <code>path_filter</code> <code>NoneValueType | Filter | None</code> <p>The path filter of the arc</p> <code>None</code> <code>path_stroke</code> <code>NoneValueType | Color | None</code> <p>The path stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>path_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The path stroke dasharray of the arc</p> <code>None</code> <code>path_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The path stroke dashoffset of the arc</p> <code>None</code> <code>path_stroke_width</code> <code>float | None</code> <p>The path stroke width of the arc</p> <code>1.25</code> <code>path_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The path transform of the arc</p> <code>None</code> <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the arc</p> <code>None</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the arc</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the arc</p> <code>None</code> <code>stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the arc</p> <code>None</code> <code>segments</code> <code>tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...]</code> <p>The path segments of the arc</p> <code>&lt;dynamic&gt;</code> <code>source</code> <code>LayoutElement | None</code> <p>The source of the arc</p> <code>None</code> <code>start_shorten</code> <code>float</code> <p>The length the start of the arc will be shorten by</p> <code>0.0</code> <code>target</code> <code>LayoutElement | None</code> <p>The target of the arc</p> <code>None</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the arc</p> <code>None</code> <code>arrowhead_fill</code> <code>NoneValueType | Color | None</code> <p>The arrowhead fill color of the arc</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>arrowhead_filter</code> <code>NoneValueType | Filter | None</code> <p>The arrowhead filter of the arc</p> <code>None</code> <code>arrowhead_stroke</code> <code>NoneValueType | Color | None</code> <p>The arrowhead stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>arrowhead_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The arrowhead stroke dasharray of the arc</p> <code>None</code> <code>arrowhead_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The arrowhead stroke dashoffset of the arc</p> <code>None</code> <code>arrowhead_stroke_width</code> <code>float | None</code> <p>The arrowhead stroke width of the arc</p> <code>1.25</code> <code>arrowhead_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The arrowhead transform of the arc</p> <code>None</code> <code>arrowhead_height</code> <code>float</code> <p>The height of the arrowhead.</p> <code>10.0</code> <code>arrowhead_width</code> <code>float</code> <p>The width of the arrowhead.</p> <code>10.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>arrowhead_base</code> <p>Return the arrowhead base anchor point of the single-headed arc.</p> <code>arrowhead_bbox</code> <p>Return the bounding box of the single-headed arc arrowhead.</p> <code>arrowhead_border</code> <p>Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.</p> <code>arrowhead_drawing_elements</code> <p>Return the drawing elements of the single-headed arc arrowhead.</p> <code>arrowhead_length</code> <p>Return the length of the single-headed arc arrowhead.</p> <code>arrowhead_tip</code> <p>Return the arrowhead tip anchor point of the single-headed arc.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>childless</code> <p>Return a copy of the arc with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>end_point</code> <p>Return the ending point of the arc.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>fraction</code> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <code>length</code> <p>Return the total length of the arc path.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_children</code> <p>Return the self children of the arc.</p> <code>own_drawing_elements</code> <p>Return the self drawing elements of the single-headed arc.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>path_drawing_elements</code> <p>Return the drawing elements of the single-headed arc path.</p> <code>points</code> <p>Return the points of the arc path.</p> <code>start_point</code> <p>Return the starting point of the arc.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnknownInfluenceLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnknownInfluenceLayout.arrowhead_base","title":"arrowhead_base","text":"<pre><code>arrowhead_base() -&gt; Point\n</code></pre> <p>Return the arrowhead base anchor point of the single-headed arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_base(self) -&gt; Point:\n    \"\"\"Return the arrowhead base anchor point of the single-headed arc.\"\"\"\n    arrowhead_length = self.arrowhead_length()\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return self.arrowhead_tip() - (arrowhead_length, 0)\n    fraction = 1 - (arrowhead_length + self.end_shorten) / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnknownInfluenceLayout.arrowhead_bbox","title":"arrowhead_bbox","text":"<pre><code>arrowhead_bbox() -&gt; Bbox\n</code></pre> <p>Return the bounding box of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_bbox(self) -&gt; Bbox:\n    \"\"\"Return the bounding box of the single-headed arc arrowhead.\"\"\"\n    return get_drawing_elements_bbox(self.arrowhead_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnknownInfluenceLayout.arrowhead_border","title":"arrowhead_border","text":"<pre><code>arrowhead_border(point: Point) -&gt; Point\n</code></pre> <p>Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_border(self, point: Point) -&gt; Point:\n    \"\"\"Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    point = get_drawing_elements_border(self.arrowhead_drawing_elements(), point)\n    if point is None:\n        return self.arrowhead_tip()\n    return point\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnknownInfluenceLayout.arrowhead_drawing_elements","title":"arrowhead_drawing_elements","text":"<pre><code>arrowhead_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_drawing_elements(\n    self,\n) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the single-headed arc arrowhead.\"\"\"\n    drawing_elements = self._arrowhead_border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_arrowhead\",\n        elements=tuple(drawing_elements),\n        fill=self.arrowhead_fill,\n        filter_=self.arrowhead_filter,\n        id_=f\"{self.id_}_arrowhead\",\n        stroke=self.arrowhead_stroke,\n        stroke_dasharray=self.arrowhead_stroke_dasharray,\n        stroke_dashoffset=self.arrowhead_stroke_dashoffset,\n        stroke_width=self.arrowhead_stroke_width,\n        transform=self.arrowhead_transform,\n    )\n    transformation = self._get_arrowhead_transformation()\n    group = group.transformed(transformation)\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnknownInfluenceLayout.arrowhead_length","title":"arrowhead_length","text":"<pre><code>arrowhead_length() -&gt; float\n</code></pre> <p>Return the length of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_length(self) -&gt; float:\n    \"\"\"Return the length of the single-headed arc arrowhead.\"\"\"\n    bbox = get_drawing_elements_bbox(self._arrowhead_border_drawing_elements())\n    if math.isnan(bbox.width):\n        return 0.0\n    return bbox.east().x\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnknownInfluenceLayout.arrowhead_tip","title":"arrowhead_tip","text":"<pre><code>arrowhead_tip() -&gt; Point\n</code></pre> <p>Return the arrowhead tip anchor point of the single-headed arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_tip(self) -&gt; Point:\n    \"\"\"Return the arrowhead tip anchor point of the single-headed arc.\"\"\"\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return segment.p2\n    fraction = 1 - self.end_shorten / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnknownInfluenceLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnknownInfluenceLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the arc with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the arc with no children.\"\"\"\n    return dataclasses.replace(self, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnknownInfluenceLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnknownInfluenceLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnknownInfluenceLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnknownInfluenceLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnknownInfluenceLayout.end_point","title":"end_point","text":"<pre><code>end_point() -&gt; Point\n</code></pre> <p>Return the ending point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_point(self) -&gt; Point:\n    \"\"\"Return the ending point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[-1]\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnknownInfluenceLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnknownInfluenceLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnknownInfluenceLayout.fraction","title":"fraction","text":"<pre><code>fraction(fraction: float) -&gt; tuple[Point, float]\n</code></pre> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def fraction(self, fraction: float) -&gt; tuple[Point, float]:\n    \"\"\"Return the position and angle on the arc at a given fraction (of the total arc length).\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    current_length = 0\n    length_to_reach = fraction * self.length()\n    for segment in self.segments:\n        current_length += segment.length()\n        if current_length &gt;= length_to_reach:\n            break\n    segment_start_length = current_length - segment.length()\n    segment_fraction = (length_to_reach - segment_start_length) / segment.length()\n    position, angle = segment.get_position_and_angle_at_fraction(segment_fraction)\n    return position, angle\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnknownInfluenceLayout.length","title":"length","text":"<pre><code>length() -&gt; float\n</code></pre> <p>Return the total length of the arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def length(self) -&gt; float:\n    \"\"\"Return the total length of the arc path.\"\"\"\n    return sum([segment.length() for segment in self.segments])\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnknownInfluenceLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnknownInfluenceLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the arc.\"\"\"\n    return []\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnknownInfluenceLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the self drawing elements of the single-headed arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the self drawing elements of the single-headed arc.\"\"\"\n    drawing_elements = (\n        self.path_drawing_elements() + self.arrowhead_drawing_elements()\n    )\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnknownInfluenceLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnknownInfluenceLayout.path_drawing_elements","title":"path_drawing_elements","text":"<pre><code>path_drawing_elements() -&gt; list[Path]\n</code></pre> <p>Return the drawing elements of the single-headed arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def path_drawing_elements(self) -&gt; list[Path]:\n    \"\"\"Return the drawing elements of the single-headed arc path.\"\"\"\n    arrowhead_length = self.arrowhead_length()\n    if len(self.segments) == 1:\n        segment = (\n            self.segments[0]\n            .shortened(self.start_shorten, \"start\")\n            .shortened(self.end_shorten + arrowhead_length, \"end\")\n        )\n        actions = [\n            MoveTo(segment.p1),\n            self._make_path_action_from_segment(segment),\n        ]\n    else:\n        first_segment = self.segments[0].shortened(self.start_shorten, \"start\")\n        last_segment = self.segments[-1].shortened(\n            self.end_shorten + arrowhead_length, \"end\"\n        )\n        actions = [\n            MoveTo(first_segment.p1),\n            self._make_path_action_from_segment(first_segment),\n        ]\n        for segment in self.segments[1:-1]:\n            action = self._make_path_action_from_segment(segment)\n            actions.append(action)\n        actions.append(self._make_path_action_from_segment(last_segment))\n    path = Path(\n        actions=tuple(actions),\n        class_=f\"{type(self).__name__}_path\",\n        fill=self.path_fill,\n        filter_=self.path_filter,\n        id_=f\"{self.id_}_path\",\n        stroke=self.path_stroke,\n        stroke_dasharray=self.path_stroke_dasharray,\n        stroke_dashoffset=self.path_stroke_dashoffset,\n        stroke_width=self.path_stroke_width,\n        transform=self.path_transform,\n    )\n    return [path]\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnknownInfluenceLayout.points","title":"points","text":"<pre><code>points() -&gt; list[Point]\n</code></pre> <p>Return the points of the arc path.</p> <p>An arc with no segments has no points and returns an empty list.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def points(self) -&gt; list[Point]:\n    \"\"\"Return the points of the arc path.\n\n    An arc with no segments has no points and returns an empty list.\n    \"\"\"\n    if not self.segments:\n        return []\n    points = []\n    for segment in self.segments:\n        points.append(segment.p1)\n    points.append(segment.p2)\n    return points\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnknownInfluenceLayout.start_point","title":"start_point","text":"<pre><code>start_point() -&gt; Point\n</code></pre> <p>Return the starting point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_point(self) -&gt; Point:\n    \"\"\"Return the starting point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[0]\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnknownInfluenceLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnspecifiedEntityUnitOfInformation","title":"UnspecifiedEntityUnitOfInformation  <code>dataclass</code>","text":"<pre><code>UnspecifiedEntityUnitOfInformation(*, id_: str = make_uuid4_as_str(), label: str | None = None)\n</code></pre> <p>               Bases: <code>UnitOfInformation</code></p> <p>Unspecified entity unit of information.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'94511a62-0a45-4e7d-892d-d8f2e653b01f'</code> <code>label</code> <code>str | None</code> <p>The label of the unit of information.</p> <code>None</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnspecifiedEntityUnitOfInformation.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnspecifiedEntityUnitOfInformationLayout","title":"UnspecifiedEntityUnitOfInformationLayout  <code>dataclass</code>","text":"<pre><code>UnspecifiedEntityUnitOfInformationLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = white, filter_: NoneValueType | Filter | None = None, height: float = 12.0, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = 1.25, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 18.0)\n</code></pre> <p>               Bases: <code>_SimpleMixin</code>, <code>SBGNNode</code></p> <p>Unspecified entity unit of information layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'6d4bf4d9-6dc1-467e-959b-b0eb0866e816'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>12.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The stroke width of the node</p> <code>1.25</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>18.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>angle</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Return the east anchor of the node.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Return the label center anchor of the node.</p> <code>north</code> <p>Return the north anchor of the node.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Return the south anchor of the node.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Return the west anchor of the node.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnspecifiedEntityUnitOfInformationLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnspecifiedEntityUnitOfInformationLayout.angle","title":"angle","text":"<pre><code>angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnspecifiedEntityUnitOfInformationLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnspecifiedEntityUnitOfInformationLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnspecifiedEntityUnitOfInformationLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnspecifiedEntityUnitOfInformationLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnspecifiedEntityUnitOfInformationLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnspecifiedEntityUnitOfInformationLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnspecifiedEntityUnitOfInformationLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnspecifiedEntityUnitOfInformationLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnspecifiedEntityUnitOfInformationLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Return the east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Return the east anchor of the node.\"\"\"\n    return self.own_angle(0)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnspecifiedEntityUnitOfInformationLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnspecifiedEntityUnitOfInformationLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnspecifiedEntityUnitOfInformationLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnspecifiedEntityUnitOfInformationLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnspecifiedEntityUnitOfInformationLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Return the label center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Return the label center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnspecifiedEntityUnitOfInformationLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Return the north anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Return the north anchor of the node.\"\"\"\n    return self.own_angle(90)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnspecifiedEntityUnitOfInformationLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnspecifiedEntityUnitOfInformationLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnspecifiedEntityUnitOfInformationLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnspecifiedEntityUnitOfInformationLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnspecifiedEntityUnitOfInformationLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnspecifiedEntityUnitOfInformationLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnspecifiedEntityUnitOfInformationLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnspecifiedEntityUnitOfInformationLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnspecifiedEntityUnitOfInformationLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnspecifiedEntityUnitOfInformationLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnspecifiedEntityUnitOfInformationLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnspecifiedEntityUnitOfInformationLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Return the south anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Return the south anchor of the node.\"\"\"\n    return self.own_angle(270)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnspecifiedEntityUnitOfInformationLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnspecifiedEntityUnitOfInformationLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnspecifiedEntityUnitOfInformationLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnspecifiedEntityUnitOfInformationLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnspecifiedEntityUnitOfInformationLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnspecifiedEntityUnitOfInformationLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Return the west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Return the west anchor of the node.\"\"\"\n    return self.own_angle(180)\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnspecifiedEntityUnitOfInformationLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnspecifiedEntityUnitOfInformationLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnspecifiedEntityUnitOfInformationLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/sbgn/af/#momapy.sbgn.af.UnspecifiedEntityUnitOfInformationLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/","title":"PD","text":""},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd","title":"momapy.sbgn.pd","text":"<p>SBGN Process Description (PD) subpackage facade.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd--layout-model-mapping-catalogue","title":"Layout-model mapping catalogue","text":"<p>This section lists, for each model-element category in SBGN-PD, the shape of the corresponding key in LayoutModelMapping. See LayoutModelMapping for the general concepts (singleton keys, frozenset keys, anchors).</p> <p>Singleton keys (one layout element represents the model element):</p> Model element Layout element used as the key Compartment CompartmentLayout EntityPool and subclasses (e.g. Macromolecule, NucleicAcidFeature, SimpleChemical, Complex, UnspecifiedEntity, PerturbingAgent, and every Multimer variant) The corresponding <code>*Layout</code> (e.g. MacromoleculeLayout, ComplexLayout, PerturbingAgentLayout) Subunit and subclasses The corresponding <code>*SubunitLayout</code> StateVariable StateVariableLayout UnitOfInformation UnitOfInformationLayout Submap SubmapLayout Reactant ConsumptionLayout Product ProductionLayout LogicalOperatorInput LogicArcLayout TagReference, TerminalReference EquivalenceArcLayout Phenotype PhenotypeLayout <p>Frozenset keys (a cluster of layout elements jointly represents the model element; the anchor is the layout that stands for the cluster on its own and must be passed as <code>anchor=</code> when calling add_mapping):</p> Model element Members of the frozenset key Anchor Process and subclasses (e.g. StoichiometricProcess, GenericProcess, UncertainProcess, Association, Dissociation, OmittedProcess) The process <code>*Layout</code> (e.g. GenericProcessLayout, AssociationLayout, DissociationLayout) + every ConsumptionLayout and ProductionLayout attached to the process + every target layout (the entity-pool layouts those arcs point to) The process <code>*Layout</code> LogicalOperator and subclasses (e.g. AndOperator, OrOperator, NotOperator, EquivalenceOperator) The operator <code>*Layout</code> (e.g. AndOperatorLayout, OrOperatorLayout) + every LogicArcLayout input + every target layout those logic arcs point to The operator <code>*Layout</code> Modulation and subclasses (e.g. Stimulation, Inhibition, Catalysis, NecessaryStimulation) The modulation arc layout (e.g. ModulationLayout, StimulationLayout, InhibitionLayout) + all layouts in the source cluster (resolved via the source's own frozenset key if it has one, else the source layout itself) + all layouts in the target cluster (resolved the same way) The modulation arc layout Tag or Terminal carrying TagReference or TerminalReference arcs The TagLayout or TerminalLayout + every EquivalenceArcLayout reference arc + every referenced entity layout The TagLayout or TerminalLayout <p>Standalone Tag and Terminal instances (with no reference arcs) use a singleton key: TagLayout or TerminalLayout.</p> <p>Empty-set glyphs (EmptySetLayout) have no model singleton. They are members of the frozenset key of the process they attach to, and the two boolean flags <code>has_external_source</code> and <code>has_external_sink</code> on the StoichiometricProcess record which side of the process the empty set represents.</p> <p>Modules:</p> Name Description <code>layout</code> <p>Layout classes for SBGN Process Description (PD) maps.</p> <code>map</code> <p>Map classes for SBGN Process Description (PD) maps.</p> <code>model</code> <p>Model classes for SBGN Process Description (PD) maps.</p> <p>Classes:</p> Name Description <code>AndOperator</code> <p>AND operator.</p> <code>AndOperatorLayout</code> <p>AND operator layout.</p> <code>Association</code> <p>Association.</p> <code>AssociationLayout</code> <p>Association layout.</p> <code>CardinalityLayout</code> <p>Cardinality layout.</p> <code>Catalysis</code> <p>Catalysis.</p> <code>CatalysisLayout</code> <p>Catalysis layout.</p> <code>Compartment</code> <p>Compartment.</p> <code>CompartmentLayout</code> <p>Compartment layout.</p> <code>Complex</code> <p>Complex.</p> <code>ComplexLayout</code> <p>Complex layout.</p> <code>ComplexMultimer</code> <p>Complex multimer.</p> <code>ComplexMultimerLayout</code> <p>Complex multimer layout.</p> <code>ComplexMultimerSubunit</code> <p>Complex multimer subunit.</p> <code>ComplexMultimerSubunitLayout</code> <p>Complex multimer subunit layout.</p> <code>ComplexSubunit</code> <p>Complex subunit.</p> <code>ComplexSubunitLayout</code> <p>Complex subunit layout.</p> <code>ConsumptionLayout</code> <p>Consumption layout.</p> <code>Dissociation</code> <p>Dissociation.</p> <code>DissociationLayout</code> <p>Dissociation layout.</p> <code>EmptySetLayout</code> <p>Empty set layout.</p> <code>EntityPool</code> <p>Abstract base class for entity pools.</p> <code>EquivalenceArcLayout</code> <p>Equivalence arc layout.</p> <code>EquivalenceOperator</code> <p>Equivalence operator.</p> <code>EquivalenceOperatorInput</code> <p>Equivalence operator input.</p> <code>EquivalenceOperatorLayout</code> <p>Equivalence operator layout.</p> <code>EquivalenceOperatorOutput</code> <p>Equivalence operator output.</p> <code>FluxRole</code> <p>Abstract base class for flux roles.</p> <code>GenericProcess</code> <p>Generic process.</p> <code>GenericProcessLayout</code> <p>Generic process layout.</p> <code>Inhibition</code> <p>Inhibition.</p> <code>InhibitionLayout</code> <p>Inhibition layout.</p> <code>LogicArcLayout</code> <p>Logic arc layout.</p> <code>LogicalOperator</code> <p>Abstract base class for logical operators.</p> <code>LogicalOperatorInput</code> <p>Logical operator input.</p> <code>Macromolecule</code> <p>Macromolecule.</p> <code>MacromoleculeLayout</code> <p>Macromolecule layout.</p> <code>MacromoleculeMultimer</code> <p>Macromolecule multimer.</p> <code>MacromoleculeMultimerLayout</code> <p>Macromolecule multimer layout.</p> <code>MacromoleculeMultimerSubunit</code> <p>Macromolecule multimer subunit.</p> <code>MacromoleculeMultimerSubunitLayout</code> <p>Macromolecule multimer subunit layout.</p> <code>MacromoleculeSubunit</code> <p>Macromolecule subunit.</p> <code>MacromoleculeSubunitLayout</code> <p>Macromolecule subunit layout.</p> <code>Modulation</code> <p>Modulation.</p> <code>ModulationLayout</code> <p>Modulation layout.</p> <code>Multimer</code> <p>Abstract base class for multimers.</p> <code>MultimerSubunit</code> <p>Abstract base class for multimer subunits.</p> <code>NecessaryStimulation</code> <p>Necessary stimulation.</p> <code>NecessaryStimulationLayout</code> <p>Necessary stimulation layout.</p> <code>NotOperator</code> <p>NOT operator.</p> <code>NotOperatorLayout</code> <p>NOT operator layout.</p> <code>NucleicAcidFeature</code> <p>Nucleic acid feature.</p> <code>NucleicAcidFeatureLayout</code> <p>Nucleic acid feature layout.</p> <code>NucleicAcidFeatureMultimer</code> <p>Nucleic acid feature multimer.</p> <code>NucleicAcidFeatureMultimerLayout</code> <p>Nucleic acid feature multimer layout.</p> <code>NucleicAcidFeatureMultimerSubunit</code> <p>Nucleic acid feature multimer subunit.</p> <code>NucleicAcidFeatureMultimerSubunitLayout</code> <p>Nucleic acid feature multimer subunit layout.</p> <code>NucleicAcidFeatureSubunit</code> <p>Nucleic acid feature subunit.</p> <code>NucleicAcidFeatureSubunitLayout</code> <p>Nucleic acid feature subunit layout.</p> <code>OmittedProcess</code> <p>Omitted process.</p> <code>OmittedProcessLayout</code> <p>Omitted process layout.</p> <code>OrOperator</code> <p>OR operator.</p> <code>OrOperatorLayout</code> <p>OR operator layout.</p> <code>PerturbingAgent</code> <p>Perturbing agent.</p> <code>PerturbingAgentLayout</code> <p>Perturbing agent layout.</p> <code>Phenotype</code> <p>Phenotype.</p> <code>PhenotypeLayout</code> <p>Phenotype layout.</p> <code>Process</code> <p>Abstract base class for processes.</p> <code>Product</code> <p>Product.</p> <code>ProductionLayout</code> <p>Production layout.</p> <code>Reactant</code> <p>Reactant.</p> <code>SBGNPDLayout</code> <p>SBGN PD layout.</p> <code>SBGNPDMap</code> <p>Class for SBGN-PD maps.</p> <code>SBGNPDModel</code> <p>SBGN PD model.</p> <code>SimpleChemical</code> <p>Simple chemical.</p> <code>SimpleChemicalLayout</code> <p>Simple chemical layout.</p> <code>SimpleChemicalMultimer</code> <p>Simple chemical multimer.</p> <code>SimpleChemicalMultimerLayout</code> <p>Simple chemical multimer layout.</p> <code>SimpleChemicalMultimerSubunit</code> <p>Simple chemical multimer subunit.</p> <code>SimpleChemicalMultimerSubunitLayout</code> <p>Simple chemical multimer subunit layout.</p> <code>SimpleChemicalSubunit</code> <p>Simple chemical subunit.</p> <code>SimpleChemicalSubunitLayout</code> <p>Simple chemical subunit layout.</p> <code>StateVariable</code> <p>State variable.</p> <code>StateVariableLayout</code> <p>State variable layout.</p> <code>Stimulation</code> <p>Stimulation.</p> <code>StimulationLayout</code> <p>Stimulation layout.</p> <code>StoichiometricProcess</code> <p>Abstract base class for stoichiometric processes.</p> <code>Submap</code> <p>Submap.</p> <code>SubmapLayout</code> <p>Submap layout.</p> <code>Subunit</code> <p>Abstract base class for subunits.</p> <code>Tag</code> <p>Tag.</p> <code>TagLayout</code> <p>Tag layout.</p> <code>TagReference</code> <p>Tag reference.</p> <code>Terminal</code> <p>Terminal.</p> <code>TerminalLayout</code> <p>Terminal layout.</p> <code>TerminalReference</code> <p>Terminal reference.</p> <code>UncertainProcess</code> <p>Uncertain process.</p> <code>UncertainProcessLayout</code> <p>Uncertain process layout.</p> <code>UnitOfInformation</code> <p>Unit of information.</p> <code>UnitOfInformationLayout</code> <p>Unit of information layout.</p> <code>UnspecifiedEntity</code> <p>Unspecified entity.</p> <code>UnspecifiedEntityLayout</code> <p>Unspecified entity layout.</p> <code>UnspecifiedEntitySubunit</code> <p>Unspecified entity subunit.</p> <code>UnspecifiedEntitySubunitLayout</code> <p>Unspecified entity subunit layout.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.AndOperator","title":"AndOperator  <code>dataclass</code>","text":"<pre><code>AndOperator(*, id_: str = make_uuid4_as_str(), inputs: frozenset[LogicalOperatorInput] = frozenset())\n</code></pre> <p>               Bases: <code>LogicalOperator</code></p> <p>AND operator.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'8bf6d59a-2cce-48f8-8fa6-33700ab02450'</code> <code>inputs</code> <code>frozenset[LogicalOperatorInput]</code> <p>The inputs of the logical operator</p> <code>&lt;dynamic&gt;</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.AndOperator.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.AndOperatorLayout","title":"AndOperatorLayout  <code>dataclass</code>","text":"<pre><code>AndOperatorLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = white, filter_: NoneValueType | Filter | None = None, height: float = 30.0, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = 1.25, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 30.0, text: str = 'AND', font_family: str = DEFAULT_FONT_FAMILY, font_fill: Color | NoneValueType = black, font_stroke: Color | NoneValueType = NoneValue, font_style: FontStyle = NORMAL, font_weight: FontWeight | float = NORMAL, orientation: Orientation = HORIZONTAL, left_to_right: bool = True, left_connector_length: float = 10.0, right_connector_length: float = 10.0, left_connector_stroke: NoneValueType | Color | None = None, left_connector_stroke_width: float | None = None, left_connector_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, left_connector_stroke_dashoffset: float | None = None, left_connector_fill: NoneValueType | Color | None = None, left_connector_transform: NoneValueType | tuple[Transformation, ...] | None = None, left_connector_filter: NoneValueType | Filter | None = None, right_connector_stroke: NoneValueType | Color | None = None, right_connector_stroke_width: float | None = None, right_connector_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, right_connector_stroke_dashoffset: float | None = None, right_connector_fill: NoneValueType | Color | None = None, right_connector_transform: NoneValueType | tuple[Transformation, ...] | None = None, right_connector_filter: NoneValueType | Filter | None = None)\n</code></pre> <p>               Bases: <code>_ConnectorsMixin</code>, <code>_SimpleMixin</code>, <code>_TextMixin</code>, <code>SBGNNode</code></p> <p>AND operator layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'f44e39e4-3a07-408d-a8eb-42861f6323fd'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>30.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The stroke width of the node</p> <code>1.25</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>30.0</code> <code>text</code> <code>str</code> <p>The text drawn inside the operator glyph</p> <code>'AND'</code> <code>font_family</code> <code>str</code> <p>Font family for the text.</p> <code>'DejaVu Sans'</code> <code>font_fill</code> <code>Color | NoneValueType</code> <p>Fill color for the text.</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>font_stroke</code> <code>Color | NoneValueType</code> <p>Stroke color for the text outline.</p> <code>&lt;momapy.drawing.NoneValueType object at 0x7f8b17323e00&gt;</code> <code>font_style</code> <code>FontStyle</code> <p>Font style (normal, italic, etc.).</p> <code>&lt;FontStyle.NORMAL: 0&gt;</code> <code>font_weight</code> <code>FontWeight | float</code> <p>Font weight (normal, bold, etc.).</p> <code>&lt;FontWeight.NORMAL: 0&gt;</code> <code>orientation</code> <code>Orientation</code> <p>Orientation of connectors (HORIZONTAL or VERTICAL).</p> <code>&lt;Orientation.HORIZONTAL: 1&gt;</code> <code>left_to_right</code> <code>bool</code> <p>Whether connectors flow left to right.</p> <code>True</code> <code>left_connector_length</code> <code>float</code> <p>Length of the left connector.</p> <code>10.0</code> <code>right_connector_length</code> <code>float</code> <p>Length of the right connector.</p> <code>10.0</code> <code>left_connector_stroke</code> <code>NoneValueType | Color | None</code> <p>Stroke color for the left connector line.</p> <code>None</code> <code>left_connector_stroke_width</code> <code>float | None</code> <p>Stroke width for the left connector line.</p> <code>None</code> <code>left_connector_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>Dash pattern for the left connector line.</p> <code>None</code> <code>left_connector_stroke_dashoffset</code> <code>float | None</code> <p>Dash offset for the left connector line.</p> <code>None</code> <code>left_connector_fill</code> <code>NoneValueType | Color | None</code> <p>Fill color for the left connector.</p> <code>None</code> <code>left_connector_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>Transformations applied to the left connector.</p> <code>None</code> <code>left_connector_filter</code> <code>NoneValueType | Filter | None</code> <p>Filter applied to the left connector.</p> <code>None</code> <code>right_connector_stroke</code> <code>NoneValueType | Color | None</code> <p>Stroke color for the right connector line.</p> <code>None</code> <code>right_connector_stroke_width</code> <code>float | None</code> <p>Stroke width for the right connector line.</p> <code>None</code> <code>right_connector_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>Dash pattern for the right connector line.</p> <code>None</code> <code>right_connector_stroke_dashoffset</code> <code>float | None</code> <p>Dash offset for the right connector line.</p> <code>None</code> <code>right_connector_fill</code> <code>NoneValueType | Color | None</code> <p>Fill color for the right connector.</p> <code>None</code> <code>right_connector_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>Transformations applied to the right connector.</p> <code>None</code> <code>right_connector_filter</code> <code>NoneValueType | Filter | None</code> <p>Filter applied to the right connector.</p> <code>None</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>angle</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Get the east (right) anchor point.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Return the label center anchor of the node.</p> <code>left_connector_base</code> <p>Get the base point of the left connector.</p> <code>left_connector_tip</code> <p>Get the tip point of the left connector.</p> <code>north</code> <p>Get the north (top) anchor point.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>right_connector_base</code> <p>Get the base point of the right connector.</p> <code>right_connector_tip</code> <p>Get the tip point of the right connector.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Get the south (bottom) anchor point.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Get the west (left) anchor point.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.AndOperatorLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.AndOperatorLayout.angle","title":"angle","text":"<pre><code>angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.AndOperatorLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.AndOperatorLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.AndOperatorLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.AndOperatorLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.AndOperatorLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.AndOperatorLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.AndOperatorLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.AndOperatorLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.AndOperatorLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Get the east (right) anchor point.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point on the east side of the element.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Get the east (right) anchor point.\n\n    Returns:\n        Point on the east side of the element.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x + self.width / 2, self.y)\n    else:\n        return Point(self.x + self.width / 2 + self.right_connector_length, self.y)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.AndOperatorLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.AndOperatorLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.AndOperatorLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.AndOperatorLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.AndOperatorLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Return the label center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Return the label center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.AndOperatorLayout.left_connector_base","title":"left_connector_base","text":"<pre><code>left_connector_base() -&gt; Point\n</code></pre> <p>Get the base point of the left connector.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point where the left connector attaches to the shape.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def left_connector_base(self) -&gt; Point:\n    \"\"\"Get the base point of the left connector.\n\n    Returns:\n        Point where the left connector attaches to the shape.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x, self.y - self.height / 2)\n    else:\n        return Point(self.x - self.width / 2, self.y)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.AndOperatorLayout.left_connector_tip","title":"left_connector_tip","text":"<pre><code>left_connector_tip() -&gt; Point\n</code></pre> <p>Get the tip point of the left connector.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point at the end of the left connector line.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def left_connector_tip(self) -&gt; Point:\n    \"\"\"Get the tip point of the left connector.\n\n    Returns:\n        Point at the end of the left connector line.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x, self.y - self.height / 2 - self.left_connector_length)\n    else:\n        return Point(self.x - self.width / 2 - self.left_connector_length, self.y)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.AndOperatorLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Get the north (top) anchor point.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point on the north side of the element.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Get the north (top) anchor point.\n\n    Returns:\n        Point on the north side of the element.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x, self.y - self.height / 2 - self.left_connector_length)\n    else:\n        return Point(self.x, self.y - self.height / 2)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.AndOperatorLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.AndOperatorLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.AndOperatorLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.AndOperatorLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.AndOperatorLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.AndOperatorLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.AndOperatorLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.AndOperatorLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.AndOperatorLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.AndOperatorLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.AndOperatorLayout.right_connector_base","title":"right_connector_base","text":"<pre><code>right_connector_base() -&gt; Point\n</code></pre> <p>Get the base point of the right connector.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point where the right connector attaches to the shape.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def right_connector_base(self) -&gt; Point:\n    \"\"\"Get the base point of the right connector.\n\n    Returns:\n        Point where the right connector attaches to the shape.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x, self.y + self.height / 2)\n    else:\n        return Point(self.x + self.width / 2, self.y)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.AndOperatorLayout.right_connector_tip","title":"right_connector_tip","text":"<pre><code>right_connector_tip() -&gt; Point\n</code></pre> <p>Get the tip point of the right connector.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point at the end of the right connector line.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def right_connector_tip(self) -&gt; Point:\n    \"\"\"Get the tip point of the right connector.\n\n    Returns:\n        Point at the end of the right connector line.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x, self.y + self.height / 2 + self.right_connector_length)\n    else:\n        return Point(self.x + self.width / 2 + self.right_connector_length, self.y)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.AndOperatorLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.AndOperatorLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Get the south (bottom) anchor point.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point on the south side of the element.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Get the south (bottom) anchor point.\n\n    Returns:\n        Point on the south side of the element.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x, self.y + self.height / 2 + self.right_connector_length)\n    else:\n        return Point(self.x, self.y + self.height / 2)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.AndOperatorLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.AndOperatorLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.AndOperatorLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.AndOperatorLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.AndOperatorLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.AndOperatorLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Get the west (left) anchor point.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point on the west side of the element.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Get the west (left) anchor point.\n\n    Returns:\n        Point on the west side of the element.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x - self.width / 2, self.y)\n    else:\n        return Point(self.x - self.width / 2 - self.left_connector_length, self.y)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.AndOperatorLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.AndOperatorLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.AndOperatorLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.AndOperatorLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.Association","title":"Association  <code>dataclass</code>","text":"<pre><code>Association(*, id_: str = make_uuid4_as_str(), reactants: frozenset[Reactant] = frozenset(), products: frozenset[Product] = frozenset(), reversible: bool = False, has_external_source: bool = False, has_external_sink: bool = False)\n</code></pre> <p>               Bases: <code>GenericProcess</code></p> <p>Association.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'25f921e8-a8be-4c06-b3fe-60d1f87c8003'</code> <code>reactants</code> <code>frozenset[Reactant]</code> <p>The reactants of the stoichiometric process</p> <code>&lt;dynamic&gt;</code> <code>products</code> <code>frozenset[Product]</code> <p>The products of the stoichiometric process</p> <code>&lt;dynamic&gt;</code> <code>reversible</code> <code>bool</code> <p>Whether the stoichiometric process is reversible or not</p> <code>False</code> <code>has_external_source</code> <code>bool</code> <p>Whether the process has an unspecified external source (an empty-set / source-and-sink reactant in SBGN PD).</p> <code>False</code> <code>has_external_sink</code> <code>bool</code> <p>Whether the process has an unspecified external sink (an empty-set / source-and-sink product in SBGN PD).</p> <code>False</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.Association.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.AssociationLayout","title":"AssociationLayout  <code>dataclass</code>","text":"<pre><code>AssociationLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = black, filter_: NoneValueType | Filter | None = None, height: float = 20.0, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = 1.25, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 20.0, orientation: Orientation = HORIZONTAL, left_to_right: bool = True, left_connector_length: float = 10.0, right_connector_length: float = 10.0, left_connector_stroke: NoneValueType | Color | None = None, left_connector_stroke_width: float | None = None, left_connector_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, left_connector_stroke_dashoffset: float | None = None, left_connector_fill: NoneValueType | Color | None = None, left_connector_transform: NoneValueType | tuple[Transformation, ...] | None = None, left_connector_filter: NoneValueType | Filter | None = None, right_connector_stroke: NoneValueType | Color | None = None, right_connector_stroke_width: float | None = None, right_connector_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, right_connector_stroke_dashoffset: float | None = None, right_connector_fill: NoneValueType | Color | None = None, right_connector_transform: NoneValueType | tuple[Transformation, ...] | None = None, right_connector_filter: NoneValueType | Filter | None = None)\n</code></pre> <p>               Bases: <code>_ConnectorsMixin</code>, <code>_SimpleMixin</code>, <code>SBGNNode</code></p> <p>Association layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'739b6d08-2439-4e6e-8c6a-8400c9201b1c'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>20.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The stroke width of the node</p> <code>1.25</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>20.0</code> <code>orientation</code> <code>Orientation</code> <p>Orientation of connectors (HORIZONTAL or VERTICAL).</p> <code>&lt;Orientation.HORIZONTAL: 1&gt;</code> <code>left_to_right</code> <code>bool</code> <p>Whether connectors flow left to right.</p> <code>True</code> <code>left_connector_length</code> <code>float</code> <p>Length of the left connector.</p> <code>10.0</code> <code>right_connector_length</code> <code>float</code> <p>Length of the right connector.</p> <code>10.0</code> <code>left_connector_stroke</code> <code>NoneValueType | Color | None</code> <p>Stroke color for the left connector line.</p> <code>None</code> <code>left_connector_stroke_width</code> <code>float | None</code> <p>Stroke width for the left connector line.</p> <code>None</code> <code>left_connector_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>Dash pattern for the left connector line.</p> <code>None</code> <code>left_connector_stroke_dashoffset</code> <code>float | None</code> <p>Dash offset for the left connector line.</p> <code>None</code> <code>left_connector_fill</code> <code>NoneValueType | Color | None</code> <p>Fill color for the left connector.</p> <code>None</code> <code>left_connector_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>Transformations applied to the left connector.</p> <code>None</code> <code>left_connector_filter</code> <code>NoneValueType | Filter | None</code> <p>Filter applied to the left connector.</p> <code>None</code> <code>right_connector_stroke</code> <code>NoneValueType | Color | None</code> <p>Stroke color for the right connector line.</p> <code>None</code> <code>right_connector_stroke_width</code> <code>float | None</code> <p>Stroke width for the right connector line.</p> <code>None</code> <code>right_connector_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>Dash pattern for the right connector line.</p> <code>None</code> <code>right_connector_stroke_dashoffset</code> <code>float | None</code> <p>Dash offset for the right connector line.</p> <code>None</code> <code>right_connector_fill</code> <code>NoneValueType | Color | None</code> <p>Fill color for the right connector.</p> <code>None</code> <code>right_connector_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>Transformations applied to the right connector.</p> <code>None</code> <code>right_connector_filter</code> <code>NoneValueType | Filter | None</code> <p>Filter applied to the right connector.</p> <code>None</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>angle</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Get the east (right) anchor point.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Return the label center anchor of the node.</p> <code>left_connector_base</code> <p>Get the base point of the left connector.</p> <code>left_connector_tip</code> <p>Get the tip point of the left connector.</p> <code>north</code> <p>Get the north (top) anchor point.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>right_connector_base</code> <p>Get the base point of the right connector.</p> <code>right_connector_tip</code> <p>Get the tip point of the right connector.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Get the south (bottom) anchor point.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Get the west (left) anchor point.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.AssociationLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.AssociationLayout.angle","title":"angle","text":"<pre><code>angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.AssociationLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.AssociationLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.AssociationLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.AssociationLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.AssociationLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.AssociationLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.AssociationLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.AssociationLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.AssociationLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Get the east (right) anchor point.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point on the east side of the element.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Get the east (right) anchor point.\n\n    Returns:\n        Point on the east side of the element.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x + self.width / 2, self.y)\n    else:\n        return Point(self.x + self.width / 2 + self.right_connector_length, self.y)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.AssociationLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.AssociationLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.AssociationLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.AssociationLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.AssociationLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Return the label center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Return the label center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.AssociationLayout.left_connector_base","title":"left_connector_base","text":"<pre><code>left_connector_base() -&gt; Point\n</code></pre> <p>Get the base point of the left connector.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point where the left connector attaches to the shape.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def left_connector_base(self) -&gt; Point:\n    \"\"\"Get the base point of the left connector.\n\n    Returns:\n        Point where the left connector attaches to the shape.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x, self.y - self.height / 2)\n    else:\n        return Point(self.x - self.width / 2, self.y)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.AssociationLayout.left_connector_tip","title":"left_connector_tip","text":"<pre><code>left_connector_tip() -&gt; Point\n</code></pre> <p>Get the tip point of the left connector.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point at the end of the left connector line.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def left_connector_tip(self) -&gt; Point:\n    \"\"\"Get the tip point of the left connector.\n\n    Returns:\n        Point at the end of the left connector line.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x, self.y - self.height / 2 - self.left_connector_length)\n    else:\n        return Point(self.x - self.width / 2 - self.left_connector_length, self.y)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.AssociationLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Get the north (top) anchor point.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point on the north side of the element.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Get the north (top) anchor point.\n\n    Returns:\n        Point on the north side of the element.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x, self.y - self.height / 2 - self.left_connector_length)\n    else:\n        return Point(self.x, self.y - self.height / 2)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.AssociationLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.AssociationLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.AssociationLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.AssociationLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.AssociationLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.AssociationLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.AssociationLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.AssociationLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.AssociationLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.AssociationLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.AssociationLayout.right_connector_base","title":"right_connector_base","text":"<pre><code>right_connector_base() -&gt; Point\n</code></pre> <p>Get the base point of the right connector.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point where the right connector attaches to the shape.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def right_connector_base(self) -&gt; Point:\n    \"\"\"Get the base point of the right connector.\n\n    Returns:\n        Point where the right connector attaches to the shape.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x, self.y + self.height / 2)\n    else:\n        return Point(self.x + self.width / 2, self.y)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.AssociationLayout.right_connector_tip","title":"right_connector_tip","text":"<pre><code>right_connector_tip() -&gt; Point\n</code></pre> <p>Get the tip point of the right connector.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point at the end of the right connector line.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def right_connector_tip(self) -&gt; Point:\n    \"\"\"Get the tip point of the right connector.\n\n    Returns:\n        Point at the end of the right connector line.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x, self.y + self.height / 2 + self.right_connector_length)\n    else:\n        return Point(self.x + self.width / 2 + self.right_connector_length, self.y)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.AssociationLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.AssociationLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Get the south (bottom) anchor point.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point on the south side of the element.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Get the south (bottom) anchor point.\n\n    Returns:\n        Point on the south side of the element.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x, self.y + self.height / 2 + self.right_connector_length)\n    else:\n        return Point(self.x, self.y + self.height / 2)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.AssociationLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.AssociationLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.AssociationLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.AssociationLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.AssociationLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.AssociationLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Get the west (left) anchor point.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point on the west side of the element.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Get the west (left) anchor point.\n\n    Returns:\n        Point on the west side of the element.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x - self.width / 2, self.y)\n    else:\n        return Point(self.x - self.width / 2 - self.left_connector_length, self.y)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.AssociationLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.AssociationLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.AssociationLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.AssociationLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CardinalityLayout","title":"CardinalityLayout  <code>dataclass</code>","text":"<pre><code>CardinalityLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = white, filter_: NoneValueType | Filter | None = None, height: float = 19.0, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = 1.25, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 12.0)\n</code></pre> <p>               Bases: <code>_SimpleMixin</code>, <code>SBGNNode</code></p> <p>Cardinality layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'5b7a99ce-5c7c-4d3d-b138-a2f10b358c74'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>19.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The stroke width of the node</p> <code>1.25</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>12.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>angle</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Return the east anchor of the node.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Return the label center anchor of the node.</p> <code>north</code> <p>Return the north anchor of the node.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Return the south anchor of the node.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Return the west anchor of the node.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CardinalityLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CardinalityLayout.angle","title":"angle","text":"<pre><code>angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CardinalityLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CardinalityLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CardinalityLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CardinalityLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CardinalityLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CardinalityLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CardinalityLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CardinalityLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CardinalityLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Return the east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Return the east anchor of the node.\"\"\"\n    return self.own_angle(0)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CardinalityLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CardinalityLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CardinalityLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CardinalityLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CardinalityLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Return the label center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Return the label center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CardinalityLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Return the north anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Return the north anchor of the node.\"\"\"\n    return self.own_angle(90)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CardinalityLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CardinalityLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CardinalityLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CardinalityLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CardinalityLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CardinalityLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CardinalityLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CardinalityLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CardinalityLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CardinalityLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CardinalityLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CardinalityLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Return the south anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Return the south anchor of the node.\"\"\"\n    return self.own_angle(270)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CardinalityLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CardinalityLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CardinalityLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CardinalityLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CardinalityLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CardinalityLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Return the west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Return the west anchor of the node.\"\"\"\n    return self.own_angle(180)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CardinalityLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CardinalityLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CardinalityLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CardinalityLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.Catalysis","title":"Catalysis  <code>dataclass</code>","text":"<pre><code>Catalysis(*, id_: str = make_uuid4_as_str(), source: EntityPool | LogicalOperator, target: Process)\n</code></pre> <p>               Bases: <code>Stimulation</code></p> <p>Catalysis.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'7e3d16b6-601d-4b53-a7a8-984b7a564321'</code> <code>source</code> <code>EntityPool | LogicalOperator</code> <p>The source of the modulation</p> required <code>target</code> <code>Process</code> <p>The target of the modulation</p> required <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.Catalysis.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CatalysisLayout","title":"CatalysisLayout  <code>dataclass</code>","text":"<pre><code>CatalysisLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, end_shorten: float = 0.0, fill: NoneValueType | Color | None = None, filter_: NoneValueType | Filter | None = None, path_fill: NoneValueType | Color | None = NoneValue, path_filter: NoneValueType | Filter | None = None, path_stroke: NoneValueType | Color | None = black, path_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, path_stroke_dashoffset: NoneValueType | float | None = None, path_stroke_width: float | None = 1.25, path_transform: NoneValueType | tuple[Transformation, ...] | None = None, stroke: NoneValueType | Color | None = None, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: NoneValueType | float | None = None, segments: tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...] = tuple(), source: LayoutElement | None = None, start_shorten: float = 0.0, target: LayoutElement | None = None, transform: NoneValueType | tuple[Transformation, ...] | None = None, arrowhead_fill: NoneValueType | Color | None = white, arrowhead_filter: NoneValueType | Filter | None = None, arrowhead_stroke: NoneValueType | Color | None = black, arrowhead_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, arrowhead_stroke_dashoffset: NoneValueType | float | None = None, arrowhead_stroke_width: float | None = 1.25, arrowhead_transform: NoneValueType | tuple[Transformation, ...] | None = None, arrowhead_height: float = 10.0, arrowhead_width: float = 10.0)\n</code></pre> <p>               Bases: <code>SBGNSingleHeadedArc</code></p> <p>Catalysis layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'5007458f-9a39-4692-9ac4-baf3ce2343a9'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>end_shorten</code> <code>float</code> <p>The length the end of the arc will be shorten by</p> <code>0.0</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the arc</p> <code>None</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the arc</p> <code>None</code> <code>path_fill</code> <code>NoneValueType | Color | None</code> <p>The path fill color of the arc</p> <code>&lt;momapy.drawing.NoneValueType object at 0x7f8b17323e00&gt;</code> <code>path_filter</code> <code>NoneValueType | Filter | None</code> <p>The path filter of the arc</p> <code>None</code> <code>path_stroke</code> <code>NoneValueType | Color | None</code> <p>The path stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>path_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The path stroke dasharray of the arc</p> <code>None</code> <code>path_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The path stroke dashoffset of the arc</p> <code>None</code> <code>path_stroke_width</code> <code>float | None</code> <p>The path stroke width of the arc</p> <code>1.25</code> <code>path_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The path transform of the arc</p> <code>None</code> <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the arc</p> <code>None</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the arc</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the arc</p> <code>None</code> <code>stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the arc</p> <code>None</code> <code>segments</code> <code>tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...]</code> <p>The path segments of the arc</p> <code>&lt;dynamic&gt;</code> <code>source</code> <code>LayoutElement | None</code> <p>The source of the arc</p> <code>None</code> <code>start_shorten</code> <code>float</code> <p>The length the start of the arc will be shorten by</p> <code>0.0</code> <code>target</code> <code>LayoutElement | None</code> <p>The target of the arc</p> <code>None</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the arc</p> <code>None</code> <code>arrowhead_fill</code> <code>NoneValueType | Color | None</code> <p>The arrowhead fill color of the arc</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>arrowhead_filter</code> <code>NoneValueType | Filter | None</code> <p>The arrowhead filter of the arc</p> <code>None</code> <code>arrowhead_stroke</code> <code>NoneValueType | Color | None</code> <p>The arrowhead stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>arrowhead_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The arrowhead stroke dasharray of the arc</p> <code>None</code> <code>arrowhead_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The arrowhead stroke dashoffset of the arc</p> <code>None</code> <code>arrowhead_stroke_width</code> <code>float | None</code> <p>The arrowhead stroke width of the arc</p> <code>1.25</code> <code>arrowhead_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The arrowhead transform of the arc</p> <code>None</code> <code>arrowhead_height</code> <code>float</code> <p>The height of the arrowhead</p> <code>10.0</code> <code>arrowhead_width</code> <code>float</code> <p>The width of the arrowhead</p> <code>10.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>arrowhead_base</code> <p>Return the arrowhead base anchor point of the single-headed arc.</p> <code>arrowhead_bbox</code> <p>Return the bounding box of the single-headed arc arrowhead.</p> <code>arrowhead_border</code> <p>Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.</p> <code>arrowhead_drawing_elements</code> <p>Return the drawing elements of the single-headed arc arrowhead.</p> <code>arrowhead_length</code> <p>Return the length of the single-headed arc arrowhead.</p> <code>arrowhead_tip</code> <p>Return the arrowhead tip anchor point of the single-headed arc.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>childless</code> <p>Return a copy of the arc with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>end_point</code> <p>Return the ending point of the arc.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>fraction</code> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <code>length</code> <p>Return the total length of the arc path.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_children</code> <p>Return the self children of the arc.</p> <code>own_drawing_elements</code> <p>Return the self drawing elements of the single-headed arc.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>path_drawing_elements</code> <p>Return the drawing elements of the single-headed arc path.</p> <code>points</code> <p>Return the points of the arc path.</p> <code>start_point</code> <p>Return the starting point of the arc.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CatalysisLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CatalysisLayout.arrowhead_base","title":"arrowhead_base","text":"<pre><code>arrowhead_base() -&gt; Point\n</code></pre> <p>Return the arrowhead base anchor point of the single-headed arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_base(self) -&gt; Point:\n    \"\"\"Return the arrowhead base anchor point of the single-headed arc.\"\"\"\n    arrowhead_length = self.arrowhead_length()\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return self.arrowhead_tip() - (arrowhead_length, 0)\n    fraction = 1 - (arrowhead_length + self.end_shorten) / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CatalysisLayout.arrowhead_bbox","title":"arrowhead_bbox","text":"<pre><code>arrowhead_bbox() -&gt; Bbox\n</code></pre> <p>Return the bounding box of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_bbox(self) -&gt; Bbox:\n    \"\"\"Return the bounding box of the single-headed arc arrowhead.\"\"\"\n    return get_drawing_elements_bbox(self.arrowhead_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CatalysisLayout.arrowhead_border","title":"arrowhead_border","text":"<pre><code>arrowhead_border(point: Point) -&gt; Point\n</code></pre> <p>Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_border(self, point: Point) -&gt; Point:\n    \"\"\"Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    point = get_drawing_elements_border(self.arrowhead_drawing_elements(), point)\n    if point is None:\n        return self.arrowhead_tip()\n    return point\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CatalysisLayout.arrowhead_drawing_elements","title":"arrowhead_drawing_elements","text":"<pre><code>arrowhead_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_drawing_elements(\n    self,\n) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the single-headed arc arrowhead.\"\"\"\n    drawing_elements = self._arrowhead_border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_arrowhead\",\n        elements=tuple(drawing_elements),\n        fill=self.arrowhead_fill,\n        filter_=self.arrowhead_filter,\n        id_=f\"{self.id_}_arrowhead\",\n        stroke=self.arrowhead_stroke,\n        stroke_dasharray=self.arrowhead_stroke_dasharray,\n        stroke_dashoffset=self.arrowhead_stroke_dashoffset,\n        stroke_width=self.arrowhead_stroke_width,\n        transform=self.arrowhead_transform,\n    )\n    transformation = self._get_arrowhead_transformation()\n    group = group.transformed(transformation)\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CatalysisLayout.arrowhead_length","title":"arrowhead_length","text":"<pre><code>arrowhead_length() -&gt; float\n</code></pre> <p>Return the length of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_length(self) -&gt; float:\n    \"\"\"Return the length of the single-headed arc arrowhead.\"\"\"\n    bbox = get_drawing_elements_bbox(self._arrowhead_border_drawing_elements())\n    if math.isnan(bbox.width):\n        return 0.0\n    return bbox.east().x\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CatalysisLayout.arrowhead_tip","title":"arrowhead_tip","text":"<pre><code>arrowhead_tip() -&gt; Point\n</code></pre> <p>Return the arrowhead tip anchor point of the single-headed arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_tip(self) -&gt; Point:\n    \"\"\"Return the arrowhead tip anchor point of the single-headed arc.\"\"\"\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return segment.p2\n    fraction = 1 - self.end_shorten / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CatalysisLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CatalysisLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the arc with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the arc with no children.\"\"\"\n    return dataclasses.replace(self, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CatalysisLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CatalysisLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CatalysisLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CatalysisLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CatalysisLayout.end_point","title":"end_point","text":"<pre><code>end_point() -&gt; Point\n</code></pre> <p>Return the ending point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_point(self) -&gt; Point:\n    \"\"\"Return the ending point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[-1]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CatalysisLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CatalysisLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CatalysisLayout.fraction","title":"fraction","text":"<pre><code>fraction(fraction: float) -&gt; tuple[Point, float]\n</code></pre> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def fraction(self, fraction: float) -&gt; tuple[Point, float]:\n    \"\"\"Return the position and angle on the arc at a given fraction (of the total arc length).\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    current_length = 0\n    length_to_reach = fraction * self.length()\n    for segment in self.segments:\n        current_length += segment.length()\n        if current_length &gt;= length_to_reach:\n            break\n    segment_start_length = current_length - segment.length()\n    segment_fraction = (length_to_reach - segment_start_length) / segment.length()\n    position, angle = segment.get_position_and_angle_at_fraction(segment_fraction)\n    return position, angle\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CatalysisLayout.length","title":"length","text":"<pre><code>length() -&gt; float\n</code></pre> <p>Return the total length of the arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def length(self) -&gt; float:\n    \"\"\"Return the total length of the arc path.\"\"\"\n    return sum([segment.length() for segment in self.segments])\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CatalysisLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CatalysisLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the arc.\"\"\"\n    return []\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CatalysisLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the self drawing elements of the single-headed arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the self drawing elements of the single-headed arc.\"\"\"\n    drawing_elements = (\n        self.path_drawing_elements() + self.arrowhead_drawing_elements()\n    )\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CatalysisLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CatalysisLayout.path_drawing_elements","title":"path_drawing_elements","text":"<pre><code>path_drawing_elements() -&gt; list[Path]\n</code></pre> <p>Return the drawing elements of the single-headed arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def path_drawing_elements(self) -&gt; list[Path]:\n    \"\"\"Return the drawing elements of the single-headed arc path.\"\"\"\n    arrowhead_length = self.arrowhead_length()\n    if len(self.segments) == 1:\n        segment = (\n            self.segments[0]\n            .shortened(self.start_shorten, \"start\")\n            .shortened(self.end_shorten + arrowhead_length, \"end\")\n        )\n        actions = [\n            MoveTo(segment.p1),\n            self._make_path_action_from_segment(segment),\n        ]\n    else:\n        first_segment = self.segments[0].shortened(self.start_shorten, \"start\")\n        last_segment = self.segments[-1].shortened(\n            self.end_shorten + arrowhead_length, \"end\"\n        )\n        actions = [\n            MoveTo(first_segment.p1),\n            self._make_path_action_from_segment(first_segment),\n        ]\n        for segment in self.segments[1:-1]:\n            action = self._make_path_action_from_segment(segment)\n            actions.append(action)\n        actions.append(self._make_path_action_from_segment(last_segment))\n    path = Path(\n        actions=tuple(actions),\n        class_=f\"{type(self).__name__}_path\",\n        fill=self.path_fill,\n        filter_=self.path_filter,\n        id_=f\"{self.id_}_path\",\n        stroke=self.path_stroke,\n        stroke_dasharray=self.path_stroke_dasharray,\n        stroke_dashoffset=self.path_stroke_dashoffset,\n        stroke_width=self.path_stroke_width,\n        transform=self.path_transform,\n    )\n    return [path]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CatalysisLayout.points","title":"points","text":"<pre><code>points() -&gt; list[Point]\n</code></pre> <p>Return the points of the arc path.</p> <p>An arc with no segments has no points and returns an empty list.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def points(self) -&gt; list[Point]:\n    \"\"\"Return the points of the arc path.\n\n    An arc with no segments has no points and returns an empty list.\n    \"\"\"\n    if not self.segments:\n        return []\n    points = []\n    for segment in self.segments:\n        points.append(segment.p1)\n    points.append(segment.p2)\n    return points\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CatalysisLayout.start_point","title":"start_point","text":"<pre><code>start_point() -&gt; Point\n</code></pre> <p>Return the starting point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_point(self) -&gt; Point:\n    \"\"\"Return the starting point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[0]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CatalysisLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.Compartment","title":"Compartment  <code>dataclass</code>","text":"<pre><code>Compartment(*, id_: str = make_uuid4_as_str(), label: str | None = None, units_of_information: frozenset[UnitOfInformation] = frozenset())\n</code></pre> <p>               Bases: <code>SBGNModelElement</code></p> <p>Compartment.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'f733900a-883c-4eca-859b-beff5b625264'</code> <code>label</code> <code>str | None</code> <p>The label of the compartment</p> <code>None</code> <code>units_of_information</code> <code>frozenset[UnitOfInformation]</code> <p>The units of information of the compartment</p> <code>&lt;dynamic&gt;</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.Compartment.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CompartmentLayout","title":"CompartmentLayout  <code>dataclass</code>","text":"<pre><code>CompartmentLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = white, filter_: NoneValueType | Filter | None = None, height: float = 80.0, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float = 3.25, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 80.0, rounded_corners: float = 5.0)\n</code></pre> <p>               Bases: <code>_SimpleMixin</code>, <code>SBGNNode</code></p> <p>Compartment layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'10826833-0e67-4d96-9ed1-824f34bc273f'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>80.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float</code> <p>The stroke width of the node</p> <code>3.25</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>80.0</code> <code>rounded_corners</code> <code>float</code> <p>The corner radius of the compartment layout</p> <code>5.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>angle</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Return the east anchor of the node.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Return the label center anchor of the node.</p> <code>north</code> <p>Return the north anchor of the node.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Return the south anchor of the node.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Return the west anchor of the node.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CompartmentLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CompartmentLayout.angle","title":"angle","text":"<pre><code>angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CompartmentLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CompartmentLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CompartmentLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CompartmentLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CompartmentLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CompartmentLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CompartmentLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CompartmentLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CompartmentLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Return the east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Return the east anchor of the node.\"\"\"\n    return self.own_angle(0)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CompartmentLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CompartmentLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CompartmentLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CompartmentLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CompartmentLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Return the label center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Return the label center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CompartmentLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Return the north anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Return the north anchor of the node.\"\"\"\n    return self.own_angle(90)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CompartmentLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CompartmentLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CompartmentLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CompartmentLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CompartmentLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CompartmentLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CompartmentLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CompartmentLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CompartmentLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CompartmentLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CompartmentLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CompartmentLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Return the south anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Return the south anchor of the node.\"\"\"\n    return self.own_angle(270)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CompartmentLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CompartmentLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CompartmentLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CompartmentLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CompartmentLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CompartmentLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Return the west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Return the west anchor of the node.\"\"\"\n    return self.own_angle(180)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CompartmentLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CompartmentLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CompartmentLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.CompartmentLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.Complex","title":"Complex  <code>dataclass</code>","text":"<pre><code>Complex(*, id_: str = make_uuid4_as_str(), compartment: Compartment | None = None, label: str | None = None, state_variables: frozenset[StateVariable] = frozenset(), units_of_information: frozenset[UnitOfInformation] = frozenset(), subunits: frozenset[Subunit] = frozenset())\n</code></pre> <p>               Bases: <code>EntityPool</code></p> <p>Complex.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'46e71a61-70f3-4ae0-9dad-158c2083e797'</code> <code>compartment</code> <code>Compartment | None</code> <p>The compartment of the entity pool</p> <code>None</code> <code>label</code> <code>str | None</code> <p>The label of the complex</p> <code>None</code> <code>state_variables</code> <code>frozenset[StateVariable]</code> <p>The state variables of the complex</p> <code>&lt;dynamic&gt;</code> <code>units_of_information</code> <code>frozenset[UnitOfInformation]</code> <p>The units of information of the complex</p> <code>&lt;dynamic&gt;</code> <code>subunits</code> <code>frozenset[Subunit]</code> <p>The subunits of the complex</p> <code>&lt;dynamic&gt;</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.Complex.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexLayout","title":"ComplexLayout  <code>dataclass</code>","text":"<pre><code>ComplexLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = white, filter_: NoneValueType | Filter | None = None, height: float = 44.0, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = 1.25, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 44.0, cut_corners: float = 5.0)\n</code></pre> <p>               Bases: <code>_SimpleMixin</code>, <code>SBGNNode</code></p> <p>Complex layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'b2fbc291-0e4d-4a7e-80e9-cc208f68558a'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>44.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The stroke width of the node</p> <code>1.25</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>44.0</code> <code>cut_corners</code> <code>float</code> <p>The size of the cut corners of the complex layout</p> <code>5.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>angle</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Return the east anchor of the node.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Return the label center anchor of the node.</p> <code>north</code> <p>Return the north anchor of the node.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Return the south anchor of the node.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Return the west anchor of the node.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexLayout.angle","title":"angle","text":"<pre><code>angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Return the east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Return the east anchor of the node.\"\"\"\n    return self.own_angle(0)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Return the label center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Return the label center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Return the north anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Return the north anchor of the node.\"\"\"\n    return self.own_angle(90)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Return the south anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Return the south anchor of the node.\"\"\"\n    return self.own_angle(270)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Return the west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Return the west anchor of the node.\"\"\"\n    return self.own_angle(180)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexMultimer","title":"ComplexMultimer  <code>dataclass</code>","text":"<pre><code>ComplexMultimer(*, id_: str = make_uuid4_as_str(), compartment: Compartment | None = None, label: str | None = None, state_variables: frozenset[StateVariable] = frozenset(), units_of_information: frozenset[UnitOfInformation] = frozenset(), subunits: frozenset[Subunit] = frozenset(), cardinality: int | None = None)\n</code></pre> <p>               Bases: <code>Multimer</code></p> <p>Complex multimer.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'b345364b-45cd-404f-9c2d-2f894f1db91d'</code> <code>compartment</code> <code>Compartment | None</code> <p>The compartment of the entity pool</p> <code>None</code> <code>label</code> <code>str | None</code> <p>The label of the complex</p> <code>None</code> <code>state_variables</code> <code>frozenset[StateVariable]</code> <p>The state variables of the complex</p> <code>&lt;dynamic&gt;</code> <code>units_of_information</code> <code>frozenset[UnitOfInformation]</code> <p>The units of information of the complex</p> <code>&lt;dynamic&gt;</code> <code>subunits</code> <code>frozenset[Subunit]</code> <p>The subunits of the complex</p> <code>&lt;dynamic&gt;</code> <code>cardinality</code> <code>int | None</code> <p>The cardinality of the multimer</p> <code>None</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexMultimer.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexMultimerLayout","title":"ComplexMultimerLayout  <code>dataclass</code>","text":"<pre><code>ComplexMultimerLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = white, filter_: NoneValueType | Filter | None = None, height: float = 44.0, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = 1.25, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 44.0, offset: float = 3.0, subunits_stroke: tuple[NoneValueType | Color, ...] | None = None, subunits_stroke_width: tuple[NoneValueType | float, ...] | None = None, subunits_stroke_dasharray: tuple[NoneValueType | tuple[float, ...], ...] | None = None, subunits_stroke_dashoffset: tuple[float, ...] | None = None, subunits_fill: tuple[NoneValueType | Color, ...] | None = None, subunits_transform: tuple[NoneValueType | tuple[Transformation, ...], ...] | None = None, subunits_filter: tuple[NoneValueType | Filter, ...] | None = None, cut_corners: float = 5.0)\n</code></pre> <p>               Bases: <code>_MultiMixin</code>, <code>SBGNNode</code></p> <p>Complex multimer layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'5f48806c-dc86-440b-ad9c-9022261e17f4'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>44.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The stroke width of the node</p> <code>1.25</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>44.0</code> <code>offset</code> <code>float</code> <p>Offset distance between stacked units.</p> <code>3.0</code> <code>subunits_stroke</code> <code>tuple[NoneValueType | Color, ...] | None</code> <p>Tuple of stroke colors for each subunit.</p> <code>None</code> <code>subunits_stroke_width</code> <code>tuple[NoneValueType | float, ...] | None</code> <p>Tuple of stroke widths for each subunit.</p> <code>None</code> <code>subunits_stroke_dasharray</code> <code>tuple[NoneValueType | tuple[float, ...], ...] | None</code> <p>Tuple of dash patterns for each subunit.</p> <code>None</code> <code>subunits_stroke_dashoffset</code> <code>tuple[float, ...] | None</code> <p>Tuple of dash offsets for each subunit.</p> <code>None</code> <code>subunits_fill</code> <code>tuple[NoneValueType | Color, ...] | None</code> <p>Tuple of fill colors for each subunit.</p> <code>None</code> <code>subunits_transform</code> <code>tuple[NoneValueType | tuple[Transformation, ...], ...] | None</code> <p>Tuple of transformations for each subunit.</p> <code>None</code> <code>subunits_filter</code> <code>tuple[NoneValueType | Filter, ...] | None</code> <p>Tuple of filters for each subunit.</p> <code>None</code> <code>cut_corners</code> <code>float</code> <p>The size of the cut corners of the complex multimer layout</p> <code>5.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>angle</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Return the east anchor of the node.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Get the center position for the node label.</p> <code>north</code> <p>Return the north anchor of the node.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Return the south anchor of the node.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Return the west anchor of the node.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexMultimerLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexMultimerLayout.angle","title":"angle","text":"<pre><code>angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexMultimerLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexMultimerLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexMultimerLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexMultimerLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexMultimerLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexMultimerLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexMultimerLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexMultimerLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexMultimerLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Return the east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Return the east anchor of the node.\"\"\"\n    return self.own_angle(0)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexMultimerLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexMultimerLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexMultimerLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexMultimerLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexMultimerLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Get the center position for the node label.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point where the label should be centered.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Get the center position for the node label.\n\n    Returns:\n        Point where the label should be centered.\n    \"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2 - (self._n - 1) * self.offset,\n        self.height / 2 - height / 2 - (self._n - 1) * self.offset,\n    )\n    return self._make_subunit_shape(\n        position=position, width=width, height=height\n    ).position\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexMultimerLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Return the north anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Return the north anchor of the node.\"\"\"\n    return self.own_angle(90)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexMultimerLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexMultimerLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexMultimerLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexMultimerLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexMultimerLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexMultimerLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexMultimerLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexMultimerLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexMultimerLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexMultimerLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexMultimerLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexMultimerLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Return the south anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Return the south anchor of the node.\"\"\"\n    return self.own_angle(270)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexMultimerLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexMultimerLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexMultimerLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexMultimerLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexMultimerLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexMultimerLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Return the west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Return the west anchor of the node.\"\"\"\n    return self.own_angle(180)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexMultimerLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexMultimerLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexMultimerLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexMultimerLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexMultimerSubunit","title":"ComplexMultimerSubunit  <code>dataclass</code>","text":"<pre><code>ComplexMultimerSubunit(*, id_: str = make_uuid4_as_str(), label: str | None = None, state_variables: frozenset[StateVariable] = frozenset(), units_of_information: frozenset[UnitOfInformation] = frozenset(), subunits: frozenset[Subunit] = frozenset(), cardinality: int | None = None)\n</code></pre> <p>               Bases: <code>MultimerSubunit</code></p> <p>Complex multimer subunit.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'2a8b4c1e-d8b9-4b05-a732-3c596f140996'</code> <code>label</code> <code>str | None</code> <p>The label of the subunit</p> <code>None</code> <code>state_variables</code> <code>frozenset[StateVariable]</code> <p>The state variables of the complex subunit</p> <code>&lt;dynamic&gt;</code> <code>units_of_information</code> <code>frozenset[UnitOfInformation]</code> <p>The units of information of the complex subunit</p> <code>&lt;dynamic&gt;</code> <code>subunits</code> <code>frozenset[Subunit]</code> <p>The subunits of the complex subunit</p> <code>&lt;dynamic&gt;</code> <code>cardinality</code> <code>int | None</code> <p>The cardinality of the multimer subunit</p> <code>None</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexMultimerSubunit.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexMultimerSubunitLayout","title":"ComplexMultimerSubunitLayout  <code>dataclass</code>","text":"<pre><code>ComplexMultimerSubunitLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = white, filter_: NoneValueType | Filter | None = None, height: float = 30.0, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = 1.25, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 60.0, offset: float = 3.0, subunits_stroke: tuple[NoneValueType | Color, ...] | None = None, subunits_stroke_width: tuple[NoneValueType | float, ...] | None = None, subunits_stroke_dasharray: tuple[NoneValueType | tuple[float, ...], ...] | None = None, subunits_stroke_dashoffset: tuple[float, ...] | None = None, subunits_fill: tuple[NoneValueType | Color, ...] | None = None, subunits_transform: tuple[NoneValueType | tuple[Transformation, ...], ...] | None = None, subunits_filter: tuple[NoneValueType | Filter, ...] | None = None, cut_corners: float = 5.0)\n</code></pre> <p>               Bases: <code>_MultiMixin</code>, <code>SBGNNode</code></p> <p>Complex multimer subunit layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'c4eef232-2952-4bdf-838f-cdd9ffd6a2f3'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>30.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The stroke width of the node</p> <code>1.25</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>60.0</code> <code>offset</code> <code>float</code> <p>Offset distance between stacked units.</p> <code>3.0</code> <code>subunits_stroke</code> <code>tuple[NoneValueType | Color, ...] | None</code> <p>Tuple of stroke colors for each subunit.</p> <code>None</code> <code>subunits_stroke_width</code> <code>tuple[NoneValueType | float, ...] | None</code> <p>Tuple of stroke widths for each subunit.</p> <code>None</code> <code>subunits_stroke_dasharray</code> <code>tuple[NoneValueType | tuple[float, ...], ...] | None</code> <p>Tuple of dash patterns for each subunit.</p> <code>None</code> <code>subunits_stroke_dashoffset</code> <code>tuple[float, ...] | None</code> <p>Tuple of dash offsets for each subunit.</p> <code>None</code> <code>subunits_fill</code> <code>tuple[NoneValueType | Color, ...] | None</code> <p>Tuple of fill colors for each subunit.</p> <code>None</code> <code>subunits_transform</code> <code>tuple[NoneValueType | tuple[Transformation, ...], ...] | None</code> <p>Tuple of transformations for each subunit.</p> <code>None</code> <code>subunits_filter</code> <code>tuple[NoneValueType | Filter, ...] | None</code> <p>Tuple of filters for each subunit.</p> <code>None</code> <code>cut_corners</code> <code>float</code> <p>The size of the cut corners of the complex multimer subunit layout</p> <code>5.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>angle</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Return the east anchor of the node.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Get the center position for the node label.</p> <code>north</code> <p>Return the north anchor of the node.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Return the south anchor of the node.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Return the west anchor of the node.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexMultimerSubunitLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexMultimerSubunitLayout.angle","title":"angle","text":"<pre><code>angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexMultimerSubunitLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexMultimerSubunitLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexMultimerSubunitLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexMultimerSubunitLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexMultimerSubunitLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexMultimerSubunitLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexMultimerSubunitLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexMultimerSubunitLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexMultimerSubunitLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Return the east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Return the east anchor of the node.\"\"\"\n    return self.own_angle(0)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexMultimerSubunitLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexMultimerSubunitLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexMultimerSubunitLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexMultimerSubunitLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexMultimerSubunitLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Get the center position for the node label.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point where the label should be centered.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Get the center position for the node label.\n\n    Returns:\n        Point where the label should be centered.\n    \"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2 - (self._n - 1) * self.offset,\n        self.height / 2 - height / 2 - (self._n - 1) * self.offset,\n    )\n    return self._make_subunit_shape(\n        position=position, width=width, height=height\n    ).position\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexMultimerSubunitLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Return the north anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Return the north anchor of the node.\"\"\"\n    return self.own_angle(90)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexMultimerSubunitLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexMultimerSubunitLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexMultimerSubunitLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexMultimerSubunitLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexMultimerSubunitLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexMultimerSubunitLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexMultimerSubunitLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexMultimerSubunitLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexMultimerSubunitLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexMultimerSubunitLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexMultimerSubunitLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexMultimerSubunitLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Return the south anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Return the south anchor of the node.\"\"\"\n    return self.own_angle(270)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexMultimerSubunitLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexMultimerSubunitLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexMultimerSubunitLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexMultimerSubunitLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexMultimerSubunitLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexMultimerSubunitLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Return the west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Return the west anchor of the node.\"\"\"\n    return self.own_angle(180)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexMultimerSubunitLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexMultimerSubunitLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexMultimerSubunitLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexMultimerSubunitLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexSubunit","title":"ComplexSubunit  <code>dataclass</code>","text":"<pre><code>ComplexSubunit(*, id_: str = make_uuid4_as_str(), label: str | None = None, state_variables: frozenset[StateVariable] = frozenset(), units_of_information: frozenset[UnitOfInformation] = frozenset(), subunits: frozenset[Subunit] = frozenset())\n</code></pre> <p>               Bases: <code>Subunit</code></p> <p>Complex subunit.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'7e3fd554-97d5-4c91-8546-9dca45c04d45'</code> <code>label</code> <code>str | None</code> <p>The label of the subunit</p> <code>None</code> <code>state_variables</code> <code>frozenset[StateVariable]</code> <p>The state variables of the complex subunit</p> <code>&lt;dynamic&gt;</code> <code>units_of_information</code> <code>frozenset[UnitOfInformation]</code> <p>The units of information of the complex subunit</p> <code>&lt;dynamic&gt;</code> <code>subunits</code> <code>frozenset[Subunit]</code> <p>The subunits of the complex subunit</p> <code>&lt;dynamic&gt;</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexSubunit.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexSubunitLayout","title":"ComplexSubunitLayout  <code>dataclass</code>","text":"<pre><code>ComplexSubunitLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = white, filter_: NoneValueType | Filter | None = None, height: float = 30.0, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = 1.25, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 60.0, cut_corners: float = 5.0)\n</code></pre> <p>               Bases: <code>_SimpleMixin</code>, <code>SBGNNode</code></p> <p>Complex subunit layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'4f3e6dc3-3eaf-4fa3-9ad5-fe517e1c4007'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>30.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The stroke width of the node</p> <code>1.25</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>60.0</code> <code>cut_corners</code> <code>float</code> <p>The size of the cut corners of the complex subunit layout</p> <code>5.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>angle</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Return the east anchor of the node.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Return the label center anchor of the node.</p> <code>north</code> <p>Return the north anchor of the node.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Return the south anchor of the node.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Return the west anchor of the node.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexSubunitLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexSubunitLayout.angle","title":"angle","text":"<pre><code>angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexSubunitLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexSubunitLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexSubunitLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexSubunitLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexSubunitLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexSubunitLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexSubunitLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexSubunitLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexSubunitLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Return the east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Return the east anchor of the node.\"\"\"\n    return self.own_angle(0)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexSubunitLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexSubunitLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexSubunitLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexSubunitLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexSubunitLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Return the label center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Return the label center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexSubunitLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Return the north anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Return the north anchor of the node.\"\"\"\n    return self.own_angle(90)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexSubunitLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexSubunitLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexSubunitLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexSubunitLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexSubunitLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexSubunitLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexSubunitLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexSubunitLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexSubunitLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexSubunitLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexSubunitLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexSubunitLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Return the south anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Return the south anchor of the node.\"\"\"\n    return self.own_angle(270)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexSubunitLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexSubunitLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexSubunitLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexSubunitLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexSubunitLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexSubunitLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Return the west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Return the west anchor of the node.\"\"\"\n    return self.own_angle(180)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexSubunitLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexSubunitLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexSubunitLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ComplexSubunitLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ConsumptionLayout","title":"ConsumptionLayout  <code>dataclass</code>","text":"<pre><code>ConsumptionLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, end_shorten: float = 0.0, fill: NoneValueType | Color | None = None, filter_: NoneValueType | Filter | None = None, path_fill: NoneValueType | Color | None = NoneValue, path_filter: NoneValueType | Filter | None = None, path_stroke: NoneValueType | Color | None = black, path_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, path_stroke_dashoffset: NoneValueType | float | None = None, path_stroke_width: float | None = 1.25, path_transform: NoneValueType | tuple[Transformation, ...] | None = None, stroke: NoneValueType | Color | None = None, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: NoneValueType | float | None = None, segments: tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...] = tuple(), source: LayoutElement | None = None, start_shorten: float = 0.0, target: LayoutElement | None = None, transform: NoneValueType | tuple[Transformation, ...] | None = None, arrowhead_fill: NoneValueType | Color | None = white, arrowhead_filter: NoneValueType | Filter | None = None, arrowhead_stroke: NoneValueType | Color | None = black, arrowhead_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, arrowhead_stroke_dashoffset: NoneValueType | float | None = None, arrowhead_stroke_width: float | None = 1.25, arrowhead_transform: NoneValueType | tuple[Transformation, ...] | None = None)\n</code></pre> <p>               Bases: <code>SBGNSingleHeadedArc</code></p> <p>Consumption layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'bb167667-7be0-4cec-a742-1cec1c42b185'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>end_shorten</code> <code>float</code> <p>The length the end of the arc will be shorten by</p> <code>0.0</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the arc</p> <code>None</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the arc</p> <code>None</code> <code>path_fill</code> <code>NoneValueType | Color | None</code> <p>The path fill color of the arc</p> <code>&lt;momapy.drawing.NoneValueType object at 0x7f8b17323e00&gt;</code> <code>path_filter</code> <code>NoneValueType | Filter | None</code> <p>The path filter of the arc</p> <code>None</code> <code>path_stroke</code> <code>NoneValueType | Color | None</code> <p>The path stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>path_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The path stroke dasharray of the arc</p> <code>None</code> <code>path_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The path stroke dashoffset of the arc</p> <code>None</code> <code>path_stroke_width</code> <code>float | None</code> <p>The path stroke width of the arc</p> <code>1.25</code> <code>path_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The path transform of the arc</p> <code>None</code> <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the arc</p> <code>None</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the arc</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the arc</p> <code>None</code> <code>stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the arc</p> <code>None</code> <code>segments</code> <code>tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...]</code> <p>The path segments of the arc</p> <code>&lt;dynamic&gt;</code> <code>source</code> <code>LayoutElement | None</code> <p>The source of the arc</p> <code>None</code> <code>start_shorten</code> <code>float</code> <p>The length the start of the arc will be shorten by</p> <code>0.0</code> <code>target</code> <code>LayoutElement | None</code> <p>The target of the arc</p> <code>None</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the arc</p> <code>None</code> <code>arrowhead_fill</code> <code>NoneValueType | Color | None</code> <p>The arrowhead fill color of the arc</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>arrowhead_filter</code> <code>NoneValueType | Filter | None</code> <p>The arrowhead filter of the arc</p> <code>None</code> <code>arrowhead_stroke</code> <code>NoneValueType | Color | None</code> <p>The arrowhead stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>arrowhead_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The arrowhead stroke dasharray of the arc</p> <code>None</code> <code>arrowhead_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The arrowhead stroke dashoffset of the arc</p> <code>None</code> <code>arrowhead_stroke_width</code> <code>float | None</code> <p>The arrowhead stroke width of the arc</p> <code>1.25</code> <code>arrowhead_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The arrowhead transform of the arc</p> <code>None</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>arrowhead_base</code> <p>Return the arrowhead base anchor point of the single-headed arc.</p> <code>arrowhead_bbox</code> <p>Return the bounding box of the single-headed arc arrowhead.</p> <code>arrowhead_border</code> <p>Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.</p> <code>arrowhead_drawing_elements</code> <p>Return the drawing elements of the single-headed arc arrowhead.</p> <code>arrowhead_length</code> <p>Return the length of the single-headed arc arrowhead.</p> <code>arrowhead_tip</code> <p>Return the arrowhead tip anchor point of the single-headed arc.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>childless</code> <p>Return a copy of the arc with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>end_point</code> <p>Return the ending point of the arc.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>fraction</code> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <code>length</code> <p>Return the total length of the arc path.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_children</code> <p>Return the self children of the arc.</p> <code>own_drawing_elements</code> <p>Return the self drawing elements of the single-headed arc.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>path_drawing_elements</code> <p>Return the drawing elements of the single-headed arc path.</p> <code>points</code> <p>Return the points of the arc path.</p> <code>start_point</code> <p>Return the starting point of the arc.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ConsumptionLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ConsumptionLayout.arrowhead_base","title":"arrowhead_base","text":"<pre><code>arrowhead_base() -&gt; Point\n</code></pre> <p>Return the arrowhead base anchor point of the single-headed arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_base(self) -&gt; Point:\n    \"\"\"Return the arrowhead base anchor point of the single-headed arc.\"\"\"\n    arrowhead_length = self.arrowhead_length()\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return self.arrowhead_tip() - (arrowhead_length, 0)\n    fraction = 1 - (arrowhead_length + self.end_shorten) / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ConsumptionLayout.arrowhead_bbox","title":"arrowhead_bbox","text":"<pre><code>arrowhead_bbox() -&gt; Bbox\n</code></pre> <p>Return the bounding box of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_bbox(self) -&gt; Bbox:\n    \"\"\"Return the bounding box of the single-headed arc arrowhead.\"\"\"\n    return get_drawing_elements_bbox(self.arrowhead_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ConsumptionLayout.arrowhead_border","title":"arrowhead_border","text":"<pre><code>arrowhead_border(point: Point) -&gt; Point\n</code></pre> <p>Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_border(self, point: Point) -&gt; Point:\n    \"\"\"Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    point = get_drawing_elements_border(self.arrowhead_drawing_elements(), point)\n    if point is None:\n        return self.arrowhead_tip()\n    return point\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ConsumptionLayout.arrowhead_drawing_elements","title":"arrowhead_drawing_elements","text":"<pre><code>arrowhead_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_drawing_elements(\n    self,\n) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the single-headed arc arrowhead.\"\"\"\n    drawing_elements = self._arrowhead_border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_arrowhead\",\n        elements=tuple(drawing_elements),\n        fill=self.arrowhead_fill,\n        filter_=self.arrowhead_filter,\n        id_=f\"{self.id_}_arrowhead\",\n        stroke=self.arrowhead_stroke,\n        stroke_dasharray=self.arrowhead_stroke_dasharray,\n        stroke_dashoffset=self.arrowhead_stroke_dashoffset,\n        stroke_width=self.arrowhead_stroke_width,\n        transform=self.arrowhead_transform,\n    )\n    transformation = self._get_arrowhead_transformation()\n    group = group.transformed(transformation)\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ConsumptionLayout.arrowhead_length","title":"arrowhead_length","text":"<pre><code>arrowhead_length() -&gt; float\n</code></pre> <p>Return the length of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_length(self) -&gt; float:\n    \"\"\"Return the length of the single-headed arc arrowhead.\"\"\"\n    bbox = get_drawing_elements_bbox(self._arrowhead_border_drawing_elements())\n    if math.isnan(bbox.width):\n        return 0.0\n    return bbox.east().x\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ConsumptionLayout.arrowhead_tip","title":"arrowhead_tip","text":"<pre><code>arrowhead_tip() -&gt; Point\n</code></pre> <p>Return the arrowhead tip anchor point of the single-headed arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_tip(self) -&gt; Point:\n    \"\"\"Return the arrowhead tip anchor point of the single-headed arc.\"\"\"\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return segment.p2\n    fraction = 1 - self.end_shorten / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ConsumptionLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ConsumptionLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the arc with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the arc with no children.\"\"\"\n    return dataclasses.replace(self, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ConsumptionLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ConsumptionLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ConsumptionLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ConsumptionLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ConsumptionLayout.end_point","title":"end_point","text":"<pre><code>end_point() -&gt; Point\n</code></pre> <p>Return the ending point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_point(self) -&gt; Point:\n    \"\"\"Return the ending point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[-1]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ConsumptionLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ConsumptionLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ConsumptionLayout.fraction","title":"fraction","text":"<pre><code>fraction(fraction: float) -&gt; tuple[Point, float]\n</code></pre> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def fraction(self, fraction: float) -&gt; tuple[Point, float]:\n    \"\"\"Return the position and angle on the arc at a given fraction (of the total arc length).\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    current_length = 0\n    length_to_reach = fraction * self.length()\n    for segment in self.segments:\n        current_length += segment.length()\n        if current_length &gt;= length_to_reach:\n            break\n    segment_start_length = current_length - segment.length()\n    segment_fraction = (length_to_reach - segment_start_length) / segment.length()\n    position, angle = segment.get_position_and_angle_at_fraction(segment_fraction)\n    return position, angle\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ConsumptionLayout.length","title":"length","text":"<pre><code>length() -&gt; float\n</code></pre> <p>Return the total length of the arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def length(self) -&gt; float:\n    \"\"\"Return the total length of the arc path.\"\"\"\n    return sum([segment.length() for segment in self.segments])\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ConsumptionLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ConsumptionLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the arc.\"\"\"\n    return []\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ConsumptionLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the self drawing elements of the single-headed arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the self drawing elements of the single-headed arc.\"\"\"\n    drawing_elements = (\n        self.path_drawing_elements() + self.arrowhead_drawing_elements()\n    )\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ConsumptionLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ConsumptionLayout.path_drawing_elements","title":"path_drawing_elements","text":"<pre><code>path_drawing_elements() -&gt; list[Path]\n</code></pre> <p>Return the drawing elements of the single-headed arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def path_drawing_elements(self) -&gt; list[Path]:\n    \"\"\"Return the drawing elements of the single-headed arc path.\"\"\"\n    arrowhead_length = self.arrowhead_length()\n    if len(self.segments) == 1:\n        segment = (\n            self.segments[0]\n            .shortened(self.start_shorten, \"start\")\n            .shortened(self.end_shorten + arrowhead_length, \"end\")\n        )\n        actions = [\n            MoveTo(segment.p1),\n            self._make_path_action_from_segment(segment),\n        ]\n    else:\n        first_segment = self.segments[0].shortened(self.start_shorten, \"start\")\n        last_segment = self.segments[-1].shortened(\n            self.end_shorten + arrowhead_length, \"end\"\n        )\n        actions = [\n            MoveTo(first_segment.p1),\n            self._make_path_action_from_segment(first_segment),\n        ]\n        for segment in self.segments[1:-1]:\n            action = self._make_path_action_from_segment(segment)\n            actions.append(action)\n        actions.append(self._make_path_action_from_segment(last_segment))\n    path = Path(\n        actions=tuple(actions),\n        class_=f\"{type(self).__name__}_path\",\n        fill=self.path_fill,\n        filter_=self.path_filter,\n        id_=f\"{self.id_}_path\",\n        stroke=self.path_stroke,\n        stroke_dasharray=self.path_stroke_dasharray,\n        stroke_dashoffset=self.path_stroke_dashoffset,\n        stroke_width=self.path_stroke_width,\n        transform=self.path_transform,\n    )\n    return [path]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ConsumptionLayout.points","title":"points","text":"<pre><code>points() -&gt; list[Point]\n</code></pre> <p>Return the points of the arc path.</p> <p>An arc with no segments has no points and returns an empty list.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def points(self) -&gt; list[Point]:\n    \"\"\"Return the points of the arc path.\n\n    An arc with no segments has no points and returns an empty list.\n    \"\"\"\n    if not self.segments:\n        return []\n    points = []\n    for segment in self.segments:\n        points.append(segment.p1)\n    points.append(segment.p2)\n    return points\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ConsumptionLayout.start_point","title":"start_point","text":"<pre><code>start_point() -&gt; Point\n</code></pre> <p>Return the starting point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_point(self) -&gt; Point:\n    \"\"\"Return the starting point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[0]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ConsumptionLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.Dissociation","title":"Dissociation  <code>dataclass</code>","text":"<pre><code>Dissociation(*, id_: str = make_uuid4_as_str(), reactants: frozenset[Reactant] = frozenset(), products: frozenset[Product] = frozenset(), reversible: bool = False, has_external_source: bool = False, has_external_sink: bool = False)\n</code></pre> <p>               Bases: <code>GenericProcess</code></p> <p>Dissociation.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'8a17cf36-9029-45a3-a9c3-9c950d7c9222'</code> <code>reactants</code> <code>frozenset[Reactant]</code> <p>The reactants of the stoichiometric process</p> <code>&lt;dynamic&gt;</code> <code>products</code> <code>frozenset[Product]</code> <p>The products of the stoichiometric process</p> <code>&lt;dynamic&gt;</code> <code>reversible</code> <code>bool</code> <p>Whether the stoichiometric process is reversible or not</p> <code>False</code> <code>has_external_source</code> <code>bool</code> <p>Whether the process has an unspecified external source (an empty-set / source-and-sink reactant in SBGN PD).</p> <code>False</code> <code>has_external_sink</code> <code>bool</code> <p>Whether the process has an unspecified external sink (an empty-set / source-and-sink product in SBGN PD).</p> <code>False</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.Dissociation.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.DissociationLayout","title":"DissociationLayout  <code>dataclass</code>","text":"<pre><code>DissociationLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = white, filter_: NoneValueType | Filter | None = None, height: float = 20.0, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = 1.25, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 20.0, orientation: Orientation = HORIZONTAL, left_to_right: bool = True, left_connector_length: float = 10.0, right_connector_length: float = 10.0, left_connector_stroke: NoneValueType | Color | None = None, left_connector_stroke_width: float | None = None, left_connector_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, left_connector_stroke_dashoffset: float | None = None, left_connector_fill: NoneValueType | Color | None = None, left_connector_transform: NoneValueType | tuple[Transformation, ...] | None = None, left_connector_filter: NoneValueType | Filter | None = None, right_connector_stroke: NoneValueType | Color | None = None, right_connector_stroke_width: float | None = None, right_connector_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, right_connector_stroke_dashoffset: float | None = None, right_connector_fill: NoneValueType | Color | None = None, right_connector_transform: NoneValueType | tuple[Transformation, ...] | None = None, right_connector_filter: NoneValueType | Filter | None = None, sep: float = 3.0)\n</code></pre> <p>               Bases: <code>_ConnectorsMixin</code>, <code>_SimpleMixin</code>, <code>SBGNNode</code></p> <p>Dissociation layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'718d1281-8c26-45cd-b72c-934bfcdc1246'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>20.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The stroke width of the node</p> <code>1.25</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>20.0</code> <code>orientation</code> <code>Orientation</code> <p>Orientation of connectors (HORIZONTAL or VERTICAL).</p> <code>&lt;Orientation.HORIZONTAL: 1&gt;</code> <code>left_to_right</code> <code>bool</code> <p>Whether connectors flow left to right.</p> <code>True</code> <code>left_connector_length</code> <code>float</code> <p>Length of the left connector.</p> <code>10.0</code> <code>right_connector_length</code> <code>float</code> <p>Length of the right connector.</p> <code>10.0</code> <code>left_connector_stroke</code> <code>NoneValueType | Color | None</code> <p>Stroke color for the left connector line.</p> <code>None</code> <code>left_connector_stroke_width</code> <code>float | None</code> <p>Stroke width for the left connector line.</p> <code>None</code> <code>left_connector_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>Dash pattern for the left connector line.</p> <code>None</code> <code>left_connector_stroke_dashoffset</code> <code>float | None</code> <p>Dash offset for the left connector line.</p> <code>None</code> <code>left_connector_fill</code> <code>NoneValueType | Color | None</code> <p>Fill color for the left connector.</p> <code>None</code> <code>left_connector_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>Transformations applied to the left connector.</p> <code>None</code> <code>left_connector_filter</code> <code>NoneValueType | Filter | None</code> <p>Filter applied to the left connector.</p> <code>None</code> <code>right_connector_stroke</code> <code>NoneValueType | Color | None</code> <p>Stroke color for the right connector line.</p> <code>None</code> <code>right_connector_stroke_width</code> <code>float | None</code> <p>Stroke width for the right connector line.</p> <code>None</code> <code>right_connector_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>Dash pattern for the right connector line.</p> <code>None</code> <code>right_connector_stroke_dashoffset</code> <code>float | None</code> <p>Dash offset for the right connector line.</p> <code>None</code> <code>right_connector_fill</code> <code>NoneValueType | Color | None</code> <p>Fill color for the right connector.</p> <code>None</code> <code>right_connector_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>Transformations applied to the right connector.</p> <code>None</code> <code>right_connector_filter</code> <code>NoneValueType | Filter | None</code> <p>Filter applied to the right connector.</p> <code>None</code> <code>sep</code> <code>float</code> <p>The separation between the outer and inner circles</p> <code>3.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>angle</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Get the east (right) anchor point.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Return the label center anchor of the node.</p> <code>left_connector_base</code> <p>Get the base point of the left connector.</p> <code>left_connector_tip</code> <p>Get the tip point of the left connector.</p> <code>north</code> <p>Get the north (top) anchor point.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>right_connector_base</code> <p>Get the base point of the right connector.</p> <code>right_connector_tip</code> <p>Get the tip point of the right connector.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Get the south (bottom) anchor point.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Get the west (left) anchor point.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.DissociationLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.DissociationLayout.angle","title":"angle","text":"<pre><code>angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.DissociationLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.DissociationLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.DissociationLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.DissociationLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.DissociationLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.DissociationLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.DissociationLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.DissociationLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.DissociationLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Get the east (right) anchor point.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point on the east side of the element.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Get the east (right) anchor point.\n\n    Returns:\n        Point on the east side of the element.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x + self.width / 2, self.y)\n    else:\n        return Point(self.x + self.width / 2 + self.right_connector_length, self.y)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.DissociationLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.DissociationLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.DissociationLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.DissociationLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.DissociationLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Return the label center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Return the label center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.DissociationLayout.left_connector_base","title":"left_connector_base","text":"<pre><code>left_connector_base() -&gt; Point\n</code></pre> <p>Get the base point of the left connector.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point where the left connector attaches to the shape.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def left_connector_base(self) -&gt; Point:\n    \"\"\"Get the base point of the left connector.\n\n    Returns:\n        Point where the left connector attaches to the shape.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x, self.y - self.height / 2)\n    else:\n        return Point(self.x - self.width / 2, self.y)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.DissociationLayout.left_connector_tip","title":"left_connector_tip","text":"<pre><code>left_connector_tip() -&gt; Point\n</code></pre> <p>Get the tip point of the left connector.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point at the end of the left connector line.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def left_connector_tip(self) -&gt; Point:\n    \"\"\"Get the tip point of the left connector.\n\n    Returns:\n        Point at the end of the left connector line.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x, self.y - self.height / 2 - self.left_connector_length)\n    else:\n        return Point(self.x - self.width / 2 - self.left_connector_length, self.y)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.DissociationLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Get the north (top) anchor point.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point on the north side of the element.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Get the north (top) anchor point.\n\n    Returns:\n        Point on the north side of the element.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x, self.y - self.height / 2 - self.left_connector_length)\n    else:\n        return Point(self.x, self.y - self.height / 2)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.DissociationLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.DissociationLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.DissociationLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.DissociationLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.DissociationLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.DissociationLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.DissociationLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.DissociationLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.DissociationLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.DissociationLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.DissociationLayout.right_connector_base","title":"right_connector_base","text":"<pre><code>right_connector_base() -&gt; Point\n</code></pre> <p>Get the base point of the right connector.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point where the right connector attaches to the shape.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def right_connector_base(self) -&gt; Point:\n    \"\"\"Get the base point of the right connector.\n\n    Returns:\n        Point where the right connector attaches to the shape.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x, self.y + self.height / 2)\n    else:\n        return Point(self.x + self.width / 2, self.y)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.DissociationLayout.right_connector_tip","title":"right_connector_tip","text":"<pre><code>right_connector_tip() -&gt; Point\n</code></pre> <p>Get the tip point of the right connector.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point at the end of the right connector line.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def right_connector_tip(self) -&gt; Point:\n    \"\"\"Get the tip point of the right connector.\n\n    Returns:\n        Point at the end of the right connector line.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x, self.y + self.height / 2 + self.right_connector_length)\n    else:\n        return Point(self.x + self.width / 2 + self.right_connector_length, self.y)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.DissociationLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.DissociationLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Get the south (bottom) anchor point.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point on the south side of the element.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Get the south (bottom) anchor point.\n\n    Returns:\n        Point on the south side of the element.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x, self.y + self.height / 2 + self.right_connector_length)\n    else:\n        return Point(self.x, self.y + self.height / 2)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.DissociationLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.DissociationLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.DissociationLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.DissociationLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.DissociationLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.DissociationLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Get the west (left) anchor point.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point on the west side of the element.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Get the west (left) anchor point.\n\n    Returns:\n        Point on the west side of the element.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x - self.width / 2, self.y)\n    else:\n        return Point(self.x - self.width / 2 - self.left_connector_length, self.y)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.DissociationLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.DissociationLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.DissociationLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.DissociationLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EmptySetLayout","title":"EmptySetLayout  <code>dataclass</code>","text":"<pre><code>EmptySetLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = white, filter_: NoneValueType | Filter | None = None, height: float = 22.0, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = 1.25, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 22.0)\n</code></pre> <p>               Bases: <code>_SimpleMixin</code>, <code>SBGNNode</code></p> <p>Empty set layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'061d0bf4-5a1a-4b65-906e-9c99948e3900'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>22.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The stroke width of the node</p> <code>1.25</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>22.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>angle</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Return the east anchor of the node.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Return the label center anchor of the node.</p> <code>north</code> <p>Return the north anchor of the node.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Return the south anchor of the node.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Return the west anchor of the node.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EmptySetLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EmptySetLayout.angle","title":"angle","text":"<pre><code>angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EmptySetLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EmptySetLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EmptySetLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EmptySetLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EmptySetLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EmptySetLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EmptySetLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EmptySetLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EmptySetLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Return the east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Return the east anchor of the node.\"\"\"\n    return self.own_angle(0)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EmptySetLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EmptySetLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EmptySetLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EmptySetLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EmptySetLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Return the label center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Return the label center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EmptySetLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Return the north anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Return the north anchor of the node.\"\"\"\n    return self.own_angle(90)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EmptySetLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EmptySetLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EmptySetLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EmptySetLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EmptySetLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EmptySetLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EmptySetLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EmptySetLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EmptySetLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EmptySetLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EmptySetLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EmptySetLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Return the south anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Return the south anchor of the node.\"\"\"\n    return self.own_angle(270)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EmptySetLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EmptySetLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EmptySetLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EmptySetLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EmptySetLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EmptySetLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Return the west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Return the west anchor of the node.\"\"\"\n    return self.own_angle(180)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EmptySetLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EmptySetLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EmptySetLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EmptySetLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EntityPool","title":"EntityPool  <code>dataclass</code>","text":"<pre><code>EntityPool(*, id_: str = make_uuid4_as_str(), compartment: Compartment | None = None)\n</code></pre> <p>               Bases: <code>SBGNModelElement</code></p> <p>Abstract base class for entity pools.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'9f47281a-aa63-492d-9370-dd3913b15f36'</code> <code>compartment</code> <code>Compartment | None</code> <p>The compartment of the entity pool</p> <code>None</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EntityPool.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EquivalenceArcLayout","title":"EquivalenceArcLayout  <code>dataclass</code>","text":"<pre><code>EquivalenceArcLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, end_shorten: float = 0.0, fill: NoneValueType | Color | None = None, filter_: NoneValueType | Filter | None = None, path_fill: NoneValueType | Color | None = NoneValue, path_filter: NoneValueType | Filter | None = None, path_stroke: NoneValueType | Color | None = black, path_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, path_stroke_dashoffset: NoneValueType | float | None = None, path_stroke_width: float | None = 1.25, path_transform: NoneValueType | tuple[Transformation, ...] | None = None, stroke: NoneValueType | Color | None = None, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: NoneValueType | float | None = None, segments: tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...] = tuple(), source: LayoutElement | None = None, start_shorten: float = 0.0, target: LayoutElement | None = None, transform: NoneValueType | tuple[Transformation, ...] | None = None, arrowhead_fill: NoneValueType | Color | None = white, arrowhead_filter: NoneValueType | Filter | None = None, arrowhead_stroke: NoneValueType | Color | None = black, arrowhead_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, arrowhead_stroke_dashoffset: NoneValueType | float | None = None, arrowhead_stroke_width: float | None = 1.25, arrowhead_transform: NoneValueType | tuple[Transformation, ...] | None = None)\n</code></pre> <p>               Bases: <code>SBGNSingleHeadedArc</code></p> <p>Equivalence arc layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'a7f6a450-1176-4466-b727-64d1f82713e0'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>end_shorten</code> <code>float</code> <p>The length the end of the arc will be shorten by</p> <code>0.0</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the arc</p> <code>None</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the arc</p> <code>None</code> <code>path_fill</code> <code>NoneValueType | Color | None</code> <p>The path fill color of the arc</p> <code>&lt;momapy.drawing.NoneValueType object at 0x7f8b17323e00&gt;</code> <code>path_filter</code> <code>NoneValueType | Filter | None</code> <p>The path filter of the arc</p> <code>None</code> <code>path_stroke</code> <code>NoneValueType | Color | None</code> <p>The path stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>path_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The path stroke dasharray of the arc</p> <code>None</code> <code>path_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The path stroke dashoffset of the arc</p> <code>None</code> <code>path_stroke_width</code> <code>float | None</code> <p>The path stroke width of the arc</p> <code>1.25</code> <code>path_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The path transform of the arc</p> <code>None</code> <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the arc</p> <code>None</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the arc</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the arc</p> <code>None</code> <code>stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the arc</p> <code>None</code> <code>segments</code> <code>tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...]</code> <p>The path segments of the arc</p> <code>&lt;dynamic&gt;</code> <code>source</code> <code>LayoutElement | None</code> <p>The source of the arc</p> <code>None</code> <code>start_shorten</code> <code>float</code> <p>The length the start of the arc will be shorten by</p> <code>0.0</code> <code>target</code> <code>LayoutElement | None</code> <p>The target of the arc</p> <code>None</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the arc</p> <code>None</code> <code>arrowhead_fill</code> <code>NoneValueType | Color | None</code> <p>The arrowhead fill color of the arc</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>arrowhead_filter</code> <code>NoneValueType | Filter | None</code> <p>The arrowhead filter of the arc</p> <code>None</code> <code>arrowhead_stroke</code> <code>NoneValueType | Color | None</code> <p>The arrowhead stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>arrowhead_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The arrowhead stroke dasharray of the arc</p> <code>None</code> <code>arrowhead_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The arrowhead stroke dashoffset of the arc</p> <code>None</code> <code>arrowhead_stroke_width</code> <code>float | None</code> <p>The arrowhead stroke width of the arc</p> <code>1.25</code> <code>arrowhead_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The arrowhead transform of the arc</p> <code>None</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>arrowhead_base</code> <p>Return the arrowhead base anchor point of the single-headed arc.</p> <code>arrowhead_bbox</code> <p>Return the bounding box of the single-headed arc arrowhead.</p> <code>arrowhead_border</code> <p>Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.</p> <code>arrowhead_drawing_elements</code> <p>Return the drawing elements of the single-headed arc arrowhead.</p> <code>arrowhead_length</code> <p>Return the length of the single-headed arc arrowhead.</p> <code>arrowhead_tip</code> <p>Return the arrowhead tip anchor point of the single-headed arc.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>childless</code> <p>Return a copy of the arc with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>end_point</code> <p>Return the ending point of the arc.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>fraction</code> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <code>length</code> <p>Return the total length of the arc path.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_children</code> <p>Return the self children of the arc.</p> <code>own_drawing_elements</code> <p>Return the self drawing elements of the single-headed arc.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>path_drawing_elements</code> <p>Return the drawing elements of the single-headed arc path.</p> <code>points</code> <p>Return the points of the arc path.</p> <code>start_point</code> <p>Return the starting point of the arc.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EquivalenceArcLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EquivalenceArcLayout.arrowhead_base","title":"arrowhead_base","text":"<pre><code>arrowhead_base() -&gt; Point\n</code></pre> <p>Return the arrowhead base anchor point of the single-headed arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_base(self) -&gt; Point:\n    \"\"\"Return the arrowhead base anchor point of the single-headed arc.\"\"\"\n    arrowhead_length = self.arrowhead_length()\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return self.arrowhead_tip() - (arrowhead_length, 0)\n    fraction = 1 - (arrowhead_length + self.end_shorten) / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EquivalenceArcLayout.arrowhead_bbox","title":"arrowhead_bbox","text":"<pre><code>arrowhead_bbox() -&gt; Bbox\n</code></pre> <p>Return the bounding box of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_bbox(self) -&gt; Bbox:\n    \"\"\"Return the bounding box of the single-headed arc arrowhead.\"\"\"\n    return get_drawing_elements_bbox(self.arrowhead_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EquivalenceArcLayout.arrowhead_border","title":"arrowhead_border","text":"<pre><code>arrowhead_border(point: Point) -&gt; Point\n</code></pre> <p>Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_border(self, point: Point) -&gt; Point:\n    \"\"\"Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    point = get_drawing_elements_border(self.arrowhead_drawing_elements(), point)\n    if point is None:\n        return self.arrowhead_tip()\n    return point\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EquivalenceArcLayout.arrowhead_drawing_elements","title":"arrowhead_drawing_elements","text":"<pre><code>arrowhead_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_drawing_elements(\n    self,\n) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the single-headed arc arrowhead.\"\"\"\n    drawing_elements = self._arrowhead_border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_arrowhead\",\n        elements=tuple(drawing_elements),\n        fill=self.arrowhead_fill,\n        filter_=self.arrowhead_filter,\n        id_=f\"{self.id_}_arrowhead\",\n        stroke=self.arrowhead_stroke,\n        stroke_dasharray=self.arrowhead_stroke_dasharray,\n        stroke_dashoffset=self.arrowhead_stroke_dashoffset,\n        stroke_width=self.arrowhead_stroke_width,\n        transform=self.arrowhead_transform,\n    )\n    transformation = self._get_arrowhead_transformation()\n    group = group.transformed(transformation)\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EquivalenceArcLayout.arrowhead_length","title":"arrowhead_length","text":"<pre><code>arrowhead_length() -&gt; float\n</code></pre> <p>Return the length of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_length(self) -&gt; float:\n    \"\"\"Return the length of the single-headed arc arrowhead.\"\"\"\n    bbox = get_drawing_elements_bbox(self._arrowhead_border_drawing_elements())\n    if math.isnan(bbox.width):\n        return 0.0\n    return bbox.east().x\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EquivalenceArcLayout.arrowhead_tip","title":"arrowhead_tip","text":"<pre><code>arrowhead_tip() -&gt; Point\n</code></pre> <p>Return the arrowhead tip anchor point of the single-headed arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_tip(self) -&gt; Point:\n    \"\"\"Return the arrowhead tip anchor point of the single-headed arc.\"\"\"\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return segment.p2\n    fraction = 1 - self.end_shorten / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EquivalenceArcLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EquivalenceArcLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the arc with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the arc with no children.\"\"\"\n    return dataclasses.replace(self, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EquivalenceArcLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EquivalenceArcLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EquivalenceArcLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EquivalenceArcLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EquivalenceArcLayout.end_point","title":"end_point","text":"<pre><code>end_point() -&gt; Point\n</code></pre> <p>Return the ending point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_point(self) -&gt; Point:\n    \"\"\"Return the ending point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[-1]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EquivalenceArcLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EquivalenceArcLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EquivalenceArcLayout.fraction","title":"fraction","text":"<pre><code>fraction(fraction: float) -&gt; tuple[Point, float]\n</code></pre> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def fraction(self, fraction: float) -&gt; tuple[Point, float]:\n    \"\"\"Return the position and angle on the arc at a given fraction (of the total arc length).\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    current_length = 0\n    length_to_reach = fraction * self.length()\n    for segment in self.segments:\n        current_length += segment.length()\n        if current_length &gt;= length_to_reach:\n            break\n    segment_start_length = current_length - segment.length()\n    segment_fraction = (length_to_reach - segment_start_length) / segment.length()\n    position, angle = segment.get_position_and_angle_at_fraction(segment_fraction)\n    return position, angle\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EquivalenceArcLayout.length","title":"length","text":"<pre><code>length() -&gt; float\n</code></pre> <p>Return the total length of the arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def length(self) -&gt; float:\n    \"\"\"Return the total length of the arc path.\"\"\"\n    return sum([segment.length() for segment in self.segments])\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EquivalenceArcLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EquivalenceArcLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the arc.\"\"\"\n    return []\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EquivalenceArcLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the self drawing elements of the single-headed arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the self drawing elements of the single-headed arc.\"\"\"\n    drawing_elements = (\n        self.path_drawing_elements() + self.arrowhead_drawing_elements()\n    )\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EquivalenceArcLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EquivalenceArcLayout.path_drawing_elements","title":"path_drawing_elements","text":"<pre><code>path_drawing_elements() -&gt; list[Path]\n</code></pre> <p>Return the drawing elements of the single-headed arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def path_drawing_elements(self) -&gt; list[Path]:\n    \"\"\"Return the drawing elements of the single-headed arc path.\"\"\"\n    arrowhead_length = self.arrowhead_length()\n    if len(self.segments) == 1:\n        segment = (\n            self.segments[0]\n            .shortened(self.start_shorten, \"start\")\n            .shortened(self.end_shorten + arrowhead_length, \"end\")\n        )\n        actions = [\n            MoveTo(segment.p1),\n            self._make_path_action_from_segment(segment),\n        ]\n    else:\n        first_segment = self.segments[0].shortened(self.start_shorten, \"start\")\n        last_segment = self.segments[-1].shortened(\n            self.end_shorten + arrowhead_length, \"end\"\n        )\n        actions = [\n            MoveTo(first_segment.p1),\n            self._make_path_action_from_segment(first_segment),\n        ]\n        for segment in self.segments[1:-1]:\n            action = self._make_path_action_from_segment(segment)\n            actions.append(action)\n        actions.append(self._make_path_action_from_segment(last_segment))\n    path = Path(\n        actions=tuple(actions),\n        class_=f\"{type(self).__name__}_path\",\n        fill=self.path_fill,\n        filter_=self.path_filter,\n        id_=f\"{self.id_}_path\",\n        stroke=self.path_stroke,\n        stroke_dasharray=self.path_stroke_dasharray,\n        stroke_dashoffset=self.path_stroke_dashoffset,\n        stroke_width=self.path_stroke_width,\n        transform=self.path_transform,\n    )\n    return [path]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EquivalenceArcLayout.points","title":"points","text":"<pre><code>points() -&gt; list[Point]\n</code></pre> <p>Return the points of the arc path.</p> <p>An arc with no segments has no points and returns an empty list.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def points(self) -&gt; list[Point]:\n    \"\"\"Return the points of the arc path.\n\n    An arc with no segments has no points and returns an empty list.\n    \"\"\"\n    if not self.segments:\n        return []\n    points = []\n    for segment in self.segments:\n        points.append(segment.p1)\n    points.append(segment.p2)\n    return points\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EquivalenceArcLayout.start_point","title":"start_point","text":"<pre><code>start_point() -&gt; Point\n</code></pre> <p>Return the starting point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_point(self) -&gt; Point:\n    \"\"\"Return the starting point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[0]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EquivalenceArcLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EquivalenceOperator","title":"EquivalenceOperator  <code>dataclass</code>","text":"<pre><code>EquivalenceOperator(*, id_: str = make_uuid4_as_str(), inputs: frozenset[EquivalenceOperatorInput] = frozenset(), output: EquivalenceOperatorOutput | None = None)\n</code></pre> <p>               Bases: <code>SBGNModelElement</code></p> <p>Equivalence operator.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'a7b17101-2b77-4c55-9b12-6f477692fb93'</code> <code>inputs</code> <code>frozenset[EquivalenceOperatorInput]</code> <p>The inputs of the equivalence operator</p> <code>&lt;dynamic&gt;</code> <code>output</code> <code>EquivalenceOperatorOutput | None</code> <p>The output of the equivalence operator</p> <code>None</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EquivalenceOperator.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EquivalenceOperatorInput","title":"EquivalenceOperatorInput  <code>dataclass</code>","text":"<pre><code>EquivalenceOperatorInput(*, id_: str = make_uuid4_as_str(), referred_element: EntityPool)\n</code></pre> <p>               Bases: <code>SBGNRole</code></p> <p>Equivalence operator input.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'ed0f1790-b349-4995-b919-9b1d78de6b73'</code> <code>referred_element</code> <code>EntityPool</code> <p>The element of the equivalence operator input</p> required <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EquivalenceOperatorInput.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EquivalenceOperatorLayout","title":"EquivalenceOperatorLayout  <code>dataclass</code>","text":"<pre><code>EquivalenceOperatorLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = white, filter_: NoneValueType | Filter | None = None, height: float = 30.0, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = 1.25, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 30.0, text: str = '\u2261', font_family: str = DEFAULT_FONT_FAMILY, font_fill: Color | NoneValueType = black, font_stroke: Color | NoneValueType = NoneValue, font_style: FontStyle = NORMAL, font_weight: FontWeight | float = NORMAL, orientation: Orientation = HORIZONTAL, left_to_right: bool = True, left_connector_length: float = 10.0, right_connector_length: float = 10.0, left_connector_stroke: NoneValueType | Color | None = None, left_connector_stroke_width: float | None = None, left_connector_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, left_connector_stroke_dashoffset: float | None = None, left_connector_fill: NoneValueType | Color | None = None, left_connector_transform: NoneValueType | tuple[Transformation, ...] | None = None, left_connector_filter: NoneValueType | Filter | None = None, right_connector_stroke: NoneValueType | Color | None = None, right_connector_stroke_width: float | None = None, right_connector_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, right_connector_stroke_dashoffset: float | None = None, right_connector_fill: NoneValueType | Color | None = None, right_connector_transform: NoneValueType | tuple[Transformation, ...] | None = None, right_connector_filter: NoneValueType | Filter | None = None)\n</code></pre> <p>               Bases: <code>_ConnectorsMixin</code>, <code>_SimpleMixin</code>, <code>_TextMixin</code>, <code>SBGNNode</code></p> <p>Equivalence operator layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'d6f71d3c-f44a-4047-840d-587859297469'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>30.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The stroke width of the node</p> <code>1.25</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>30.0</code> <code>text</code> <code>str</code> <p>The text drawn inside the operator glyph</p> <code>'\u2261'</code> <code>font_family</code> <code>str</code> <p>Font family for the text.</p> <code>'DejaVu Sans'</code> <code>font_fill</code> <code>Color | NoneValueType</code> <p>Fill color for the text.</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>font_stroke</code> <code>Color | NoneValueType</code> <p>Stroke color for the text outline.</p> <code>&lt;momapy.drawing.NoneValueType object at 0x7f8b17323e00&gt;</code> <code>font_style</code> <code>FontStyle</code> <p>Font style (normal, italic, etc.).</p> <code>&lt;FontStyle.NORMAL: 0&gt;</code> <code>font_weight</code> <code>FontWeight | float</code> <p>Font weight (normal, bold, etc.).</p> <code>&lt;FontWeight.NORMAL: 0&gt;</code> <code>orientation</code> <code>Orientation</code> <p>Orientation of connectors (HORIZONTAL or VERTICAL).</p> <code>&lt;Orientation.HORIZONTAL: 1&gt;</code> <code>left_to_right</code> <code>bool</code> <p>Whether connectors flow left to right.</p> <code>True</code> <code>left_connector_length</code> <code>float</code> <p>Length of the left connector.</p> <code>10.0</code> <code>right_connector_length</code> <code>float</code> <p>Length of the right connector.</p> <code>10.0</code> <code>left_connector_stroke</code> <code>NoneValueType | Color | None</code> <p>Stroke color for the left connector line.</p> <code>None</code> <code>left_connector_stroke_width</code> <code>float | None</code> <p>Stroke width for the left connector line.</p> <code>None</code> <code>left_connector_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>Dash pattern for the left connector line.</p> <code>None</code> <code>left_connector_stroke_dashoffset</code> <code>float | None</code> <p>Dash offset for the left connector line.</p> <code>None</code> <code>left_connector_fill</code> <code>NoneValueType | Color | None</code> <p>Fill color for the left connector.</p> <code>None</code> <code>left_connector_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>Transformations applied to the left connector.</p> <code>None</code> <code>left_connector_filter</code> <code>NoneValueType | Filter | None</code> <p>Filter applied to the left connector.</p> <code>None</code> <code>right_connector_stroke</code> <code>NoneValueType | Color | None</code> <p>Stroke color for the right connector line.</p> <code>None</code> <code>right_connector_stroke_width</code> <code>float | None</code> <p>Stroke width for the right connector line.</p> <code>None</code> <code>right_connector_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>Dash pattern for the right connector line.</p> <code>None</code> <code>right_connector_stroke_dashoffset</code> <code>float | None</code> <p>Dash offset for the right connector line.</p> <code>None</code> <code>right_connector_fill</code> <code>NoneValueType | Color | None</code> <p>Fill color for the right connector.</p> <code>None</code> <code>right_connector_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>Transformations applied to the right connector.</p> <code>None</code> <code>right_connector_filter</code> <code>NoneValueType | Filter | None</code> <p>Filter applied to the right connector.</p> <code>None</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>angle</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Get the east (right) anchor point.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Return the label center anchor of the node.</p> <code>left_connector_base</code> <p>Get the base point of the left connector.</p> <code>left_connector_tip</code> <p>Get the tip point of the left connector.</p> <code>north</code> <p>Get the north (top) anchor point.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>right_connector_base</code> <p>Get the base point of the right connector.</p> <code>right_connector_tip</code> <p>Get the tip point of the right connector.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Get the south (bottom) anchor point.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Get the west (left) anchor point.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EquivalenceOperatorLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EquivalenceOperatorLayout.angle","title":"angle","text":"<pre><code>angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EquivalenceOperatorLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EquivalenceOperatorLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EquivalenceOperatorLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EquivalenceOperatorLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EquivalenceOperatorLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EquivalenceOperatorLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EquivalenceOperatorLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EquivalenceOperatorLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EquivalenceOperatorLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Get the east (right) anchor point.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point on the east side of the element.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Get the east (right) anchor point.\n\n    Returns:\n        Point on the east side of the element.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x + self.width / 2, self.y)\n    else:\n        return Point(self.x + self.width / 2 + self.right_connector_length, self.y)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EquivalenceOperatorLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EquivalenceOperatorLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EquivalenceOperatorLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EquivalenceOperatorLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EquivalenceOperatorLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Return the label center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Return the label center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EquivalenceOperatorLayout.left_connector_base","title":"left_connector_base","text":"<pre><code>left_connector_base() -&gt; Point\n</code></pre> <p>Get the base point of the left connector.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point where the left connector attaches to the shape.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def left_connector_base(self) -&gt; Point:\n    \"\"\"Get the base point of the left connector.\n\n    Returns:\n        Point where the left connector attaches to the shape.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x, self.y - self.height / 2)\n    else:\n        return Point(self.x - self.width / 2, self.y)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EquivalenceOperatorLayout.left_connector_tip","title":"left_connector_tip","text":"<pre><code>left_connector_tip() -&gt; Point\n</code></pre> <p>Get the tip point of the left connector.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point at the end of the left connector line.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def left_connector_tip(self) -&gt; Point:\n    \"\"\"Get the tip point of the left connector.\n\n    Returns:\n        Point at the end of the left connector line.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x, self.y - self.height / 2 - self.left_connector_length)\n    else:\n        return Point(self.x - self.width / 2 - self.left_connector_length, self.y)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EquivalenceOperatorLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Get the north (top) anchor point.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point on the north side of the element.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Get the north (top) anchor point.\n\n    Returns:\n        Point on the north side of the element.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x, self.y - self.height / 2 - self.left_connector_length)\n    else:\n        return Point(self.x, self.y - self.height / 2)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EquivalenceOperatorLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EquivalenceOperatorLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EquivalenceOperatorLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EquivalenceOperatorLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EquivalenceOperatorLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EquivalenceOperatorLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EquivalenceOperatorLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EquivalenceOperatorLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EquivalenceOperatorLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EquivalenceOperatorLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EquivalenceOperatorLayout.right_connector_base","title":"right_connector_base","text":"<pre><code>right_connector_base() -&gt; Point\n</code></pre> <p>Get the base point of the right connector.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point where the right connector attaches to the shape.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def right_connector_base(self) -&gt; Point:\n    \"\"\"Get the base point of the right connector.\n\n    Returns:\n        Point where the right connector attaches to the shape.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x, self.y + self.height / 2)\n    else:\n        return Point(self.x + self.width / 2, self.y)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EquivalenceOperatorLayout.right_connector_tip","title":"right_connector_tip","text":"<pre><code>right_connector_tip() -&gt; Point\n</code></pre> <p>Get the tip point of the right connector.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point at the end of the right connector line.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def right_connector_tip(self) -&gt; Point:\n    \"\"\"Get the tip point of the right connector.\n\n    Returns:\n        Point at the end of the right connector line.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x, self.y + self.height / 2 + self.right_connector_length)\n    else:\n        return Point(self.x + self.width / 2 + self.right_connector_length, self.y)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EquivalenceOperatorLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EquivalenceOperatorLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Get the south (bottom) anchor point.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point on the south side of the element.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Get the south (bottom) anchor point.\n\n    Returns:\n        Point on the south side of the element.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x, self.y + self.height / 2 + self.right_connector_length)\n    else:\n        return Point(self.x, self.y + self.height / 2)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EquivalenceOperatorLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EquivalenceOperatorLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EquivalenceOperatorLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EquivalenceOperatorLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EquivalenceOperatorLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EquivalenceOperatorLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Get the west (left) anchor point.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point on the west side of the element.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Get the west (left) anchor point.\n\n    Returns:\n        Point on the west side of the element.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x - self.width / 2, self.y)\n    else:\n        return Point(self.x - self.width / 2 - self.left_connector_length, self.y)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EquivalenceOperatorLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EquivalenceOperatorLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EquivalenceOperatorLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EquivalenceOperatorLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EquivalenceOperatorOutput","title":"EquivalenceOperatorOutput  <code>dataclass</code>","text":"<pre><code>EquivalenceOperatorOutput(*, id_: str = make_uuid4_as_str(), referred_element: EntityPool)\n</code></pre> <p>               Bases: <code>SBGNRole</code></p> <p>Equivalence operator output.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'a7ef39e6-f010-4bd1-a7ae-c4d0d0e36697'</code> <code>referred_element</code> <code>EntityPool</code> <p>The element of the equivalence operator output</p> required <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.EquivalenceOperatorOutput.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.FluxRole","title":"FluxRole  <code>dataclass</code>","text":"<pre><code>FluxRole(*, id_: str = make_uuid4_as_str(), referred_element: EntityPool, stoichiometry: float | None = None)\n</code></pre> <p>               Bases: <code>SBGNRole</code></p> <p>Abstract base class for flux roles.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'ec0e956e-03cb-448c-9364-edfb307e38b0'</code> <code>referred_element</code> <code>EntityPool</code> <p>The entity pool of the flux role</p> required <code>stoichiometry</code> <code>float | None</code> <p>The stoichiometry of the flux role</p> <code>None</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.FluxRole.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.GenericProcess","title":"GenericProcess  <code>dataclass</code>","text":"<pre><code>GenericProcess(*, id_: str = make_uuid4_as_str(), reactants: frozenset[Reactant] = frozenset(), products: frozenset[Product] = frozenset(), reversible: bool = False, has_external_source: bool = False, has_external_sink: bool = False)\n</code></pre> <p>               Bases: <code>StoichiometricProcess</code></p> <p>Generic process.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'234f9ef6-e430-4047-be9b-6c3d0ff21c10'</code> <code>reactants</code> <code>frozenset[Reactant]</code> <p>The reactants of the stoichiometric process</p> <code>&lt;dynamic&gt;</code> <code>products</code> <code>frozenset[Product]</code> <p>The products of the stoichiometric process</p> <code>&lt;dynamic&gt;</code> <code>reversible</code> <code>bool</code> <p>Whether the stoichiometric process is reversible or not</p> <code>False</code> <code>has_external_source</code> <code>bool</code> <p>Whether the process has an unspecified external source (an empty-set / source-and-sink reactant in SBGN PD).</p> <code>False</code> <code>has_external_sink</code> <code>bool</code> <p>Whether the process has an unspecified external sink (an empty-set / source-and-sink product in SBGN PD).</p> <code>False</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.GenericProcess.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.GenericProcessLayout","title":"GenericProcessLayout  <code>dataclass</code>","text":"<pre><code>GenericProcessLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = white, filter_: NoneValueType | Filter | None = None, height: float = 20.0, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = 1.25, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 20.0, orientation: Orientation = HORIZONTAL, left_to_right: bool = True, left_connector_length: float = 10.0, right_connector_length: float = 10.0, left_connector_stroke: NoneValueType | Color | None = None, left_connector_stroke_width: float | None = None, left_connector_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, left_connector_stroke_dashoffset: float | None = None, left_connector_fill: NoneValueType | Color | None = None, left_connector_transform: NoneValueType | tuple[Transformation, ...] | None = None, left_connector_filter: NoneValueType | Filter | None = None, right_connector_stroke: NoneValueType | Color | None = None, right_connector_stroke_width: float | None = None, right_connector_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, right_connector_stroke_dashoffset: float | None = None, right_connector_fill: NoneValueType | Color | None = None, right_connector_transform: NoneValueType | tuple[Transformation, ...] | None = None, right_connector_filter: NoneValueType | Filter | None = None)\n</code></pre> <p>               Bases: <code>_ConnectorsMixin</code>, <code>_SimpleMixin</code>, <code>SBGNNode</code></p> <p>Generic process layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'9feff9f8-7cfb-45ea-b176-1844901a2c58'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>20.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The stroke width of the node</p> <code>1.25</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>20.0</code> <code>orientation</code> <code>Orientation</code> <p>Orientation of connectors (HORIZONTAL or VERTICAL).</p> <code>&lt;Orientation.HORIZONTAL: 1&gt;</code> <code>left_to_right</code> <code>bool</code> <p>Whether connectors flow left to right.</p> <code>True</code> <code>left_connector_length</code> <code>float</code> <p>Length of the left connector.</p> <code>10.0</code> <code>right_connector_length</code> <code>float</code> <p>Length of the right connector.</p> <code>10.0</code> <code>left_connector_stroke</code> <code>NoneValueType | Color | None</code> <p>Stroke color for the left connector line.</p> <code>None</code> <code>left_connector_stroke_width</code> <code>float | None</code> <p>Stroke width for the left connector line.</p> <code>None</code> <code>left_connector_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>Dash pattern for the left connector line.</p> <code>None</code> <code>left_connector_stroke_dashoffset</code> <code>float | None</code> <p>Dash offset for the left connector line.</p> <code>None</code> <code>left_connector_fill</code> <code>NoneValueType | Color | None</code> <p>Fill color for the left connector.</p> <code>None</code> <code>left_connector_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>Transformations applied to the left connector.</p> <code>None</code> <code>left_connector_filter</code> <code>NoneValueType | Filter | None</code> <p>Filter applied to the left connector.</p> <code>None</code> <code>right_connector_stroke</code> <code>NoneValueType | Color | None</code> <p>Stroke color for the right connector line.</p> <code>None</code> <code>right_connector_stroke_width</code> <code>float | None</code> <p>Stroke width for the right connector line.</p> <code>None</code> <code>right_connector_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>Dash pattern for the right connector line.</p> <code>None</code> <code>right_connector_stroke_dashoffset</code> <code>float | None</code> <p>Dash offset for the right connector line.</p> <code>None</code> <code>right_connector_fill</code> <code>NoneValueType | Color | None</code> <p>Fill color for the right connector.</p> <code>None</code> <code>right_connector_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>Transformations applied to the right connector.</p> <code>None</code> <code>right_connector_filter</code> <code>NoneValueType | Filter | None</code> <p>Filter applied to the right connector.</p> <code>None</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>angle</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Get the east (right) anchor point.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Return the label center anchor of the node.</p> <code>left_connector_base</code> <p>Get the base point of the left connector.</p> <code>left_connector_tip</code> <p>Get the tip point of the left connector.</p> <code>north</code> <p>Get the north (top) anchor point.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>right_connector_base</code> <p>Get the base point of the right connector.</p> <code>right_connector_tip</code> <p>Get the tip point of the right connector.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Get the south (bottom) anchor point.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Get the west (left) anchor point.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.GenericProcessLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.GenericProcessLayout.angle","title":"angle","text":"<pre><code>angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.GenericProcessLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.GenericProcessLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.GenericProcessLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.GenericProcessLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.GenericProcessLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.GenericProcessLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.GenericProcessLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.GenericProcessLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.GenericProcessLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Get the east (right) anchor point.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point on the east side of the element.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Get the east (right) anchor point.\n\n    Returns:\n        Point on the east side of the element.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x + self.width / 2, self.y)\n    else:\n        return Point(self.x + self.width / 2 + self.right_connector_length, self.y)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.GenericProcessLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.GenericProcessLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.GenericProcessLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.GenericProcessLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.GenericProcessLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Return the label center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Return the label center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.GenericProcessLayout.left_connector_base","title":"left_connector_base","text":"<pre><code>left_connector_base() -&gt; Point\n</code></pre> <p>Get the base point of the left connector.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point where the left connector attaches to the shape.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def left_connector_base(self) -&gt; Point:\n    \"\"\"Get the base point of the left connector.\n\n    Returns:\n        Point where the left connector attaches to the shape.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x, self.y - self.height / 2)\n    else:\n        return Point(self.x - self.width / 2, self.y)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.GenericProcessLayout.left_connector_tip","title":"left_connector_tip","text":"<pre><code>left_connector_tip() -&gt; Point\n</code></pre> <p>Get the tip point of the left connector.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point at the end of the left connector line.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def left_connector_tip(self) -&gt; Point:\n    \"\"\"Get the tip point of the left connector.\n\n    Returns:\n        Point at the end of the left connector line.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x, self.y - self.height / 2 - self.left_connector_length)\n    else:\n        return Point(self.x - self.width / 2 - self.left_connector_length, self.y)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.GenericProcessLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Get the north (top) anchor point.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point on the north side of the element.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Get the north (top) anchor point.\n\n    Returns:\n        Point on the north side of the element.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x, self.y - self.height / 2 - self.left_connector_length)\n    else:\n        return Point(self.x, self.y - self.height / 2)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.GenericProcessLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.GenericProcessLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.GenericProcessLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.GenericProcessLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.GenericProcessLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.GenericProcessLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.GenericProcessLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.GenericProcessLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.GenericProcessLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.GenericProcessLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.GenericProcessLayout.right_connector_base","title":"right_connector_base","text":"<pre><code>right_connector_base() -&gt; Point\n</code></pre> <p>Get the base point of the right connector.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point where the right connector attaches to the shape.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def right_connector_base(self) -&gt; Point:\n    \"\"\"Get the base point of the right connector.\n\n    Returns:\n        Point where the right connector attaches to the shape.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x, self.y + self.height / 2)\n    else:\n        return Point(self.x + self.width / 2, self.y)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.GenericProcessLayout.right_connector_tip","title":"right_connector_tip","text":"<pre><code>right_connector_tip() -&gt; Point\n</code></pre> <p>Get the tip point of the right connector.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point at the end of the right connector line.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def right_connector_tip(self) -&gt; Point:\n    \"\"\"Get the tip point of the right connector.\n\n    Returns:\n        Point at the end of the right connector line.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x, self.y + self.height / 2 + self.right_connector_length)\n    else:\n        return Point(self.x + self.width / 2 + self.right_connector_length, self.y)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.GenericProcessLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.GenericProcessLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Get the south (bottom) anchor point.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point on the south side of the element.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Get the south (bottom) anchor point.\n\n    Returns:\n        Point on the south side of the element.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x, self.y + self.height / 2 + self.right_connector_length)\n    else:\n        return Point(self.x, self.y + self.height / 2)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.GenericProcessLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.GenericProcessLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.GenericProcessLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.GenericProcessLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.GenericProcessLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.GenericProcessLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Get the west (left) anchor point.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point on the west side of the element.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Get the west (left) anchor point.\n\n    Returns:\n        Point on the west side of the element.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x - self.width / 2, self.y)\n    else:\n        return Point(self.x - self.width / 2 - self.left_connector_length, self.y)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.GenericProcessLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.GenericProcessLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.GenericProcessLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.GenericProcessLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.Inhibition","title":"Inhibition  <code>dataclass</code>","text":"<pre><code>Inhibition(*, id_: str = make_uuid4_as_str(), source: EntityPool | LogicalOperator, target: Process)\n</code></pre> <p>               Bases: <code>Modulation</code></p> <p>Inhibition.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'c2dccc65-274d-42ef-8b7b-b05352d15fd5'</code> <code>source</code> <code>EntityPool | LogicalOperator</code> <p>The source of the modulation</p> required <code>target</code> <code>Process</code> <p>The target of the modulation</p> required <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.Inhibition.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.InhibitionLayout","title":"InhibitionLayout  <code>dataclass</code>","text":"<pre><code>InhibitionLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, end_shorten: float = 0.0, fill: NoneValueType | Color | None = None, filter_: NoneValueType | Filter | None = None, path_fill: NoneValueType | Color | None = NoneValue, path_filter: NoneValueType | Filter | None = None, path_stroke: NoneValueType | Color | None = black, path_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, path_stroke_dashoffset: NoneValueType | float | None = None, path_stroke_width: float | None = 1.25, path_transform: NoneValueType | tuple[Transformation, ...] | None = None, stroke: NoneValueType | Color | None = None, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: NoneValueType | float | None = None, segments: tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...] = tuple(), source: LayoutElement | None = None, start_shorten: float = 0.0, target: LayoutElement | None = None, transform: NoneValueType | tuple[Transformation, ...] | None = None, arrowhead_fill: NoneValueType | Color | None = white, arrowhead_filter: NoneValueType | Filter | None = None, arrowhead_stroke: NoneValueType | Color | None = black, arrowhead_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, arrowhead_stroke_dashoffset: NoneValueType | float | None = None, arrowhead_stroke_width: float | None = 1.25, arrowhead_transform: NoneValueType | tuple[Transformation, ...] | None = None, arrowhead_height: float = 10.0)\n</code></pre> <p>               Bases: <code>SBGNSingleHeadedArc</code></p> <p>Inhibition layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'f3d968aa-0c0a-4161-94a8-2829df2bfa3a'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>end_shorten</code> <code>float</code> <p>The length the end of the arc will be shorten by</p> <code>0.0</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the arc</p> <code>None</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the arc</p> <code>None</code> <code>path_fill</code> <code>NoneValueType | Color | None</code> <p>The path fill color of the arc</p> <code>&lt;momapy.drawing.NoneValueType object at 0x7f8b17323e00&gt;</code> <code>path_filter</code> <code>NoneValueType | Filter | None</code> <p>The path filter of the arc</p> <code>None</code> <code>path_stroke</code> <code>NoneValueType | Color | None</code> <p>The path stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>path_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The path stroke dasharray of the arc</p> <code>None</code> <code>path_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The path stroke dashoffset of the arc</p> <code>None</code> <code>path_stroke_width</code> <code>float | None</code> <p>The path stroke width of the arc</p> <code>1.25</code> <code>path_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The path transform of the arc</p> <code>None</code> <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the arc</p> <code>None</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the arc</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the arc</p> <code>None</code> <code>stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the arc</p> <code>None</code> <code>segments</code> <code>tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...]</code> <p>The path segments of the arc</p> <code>&lt;dynamic&gt;</code> <code>source</code> <code>LayoutElement | None</code> <p>The source of the arc</p> <code>None</code> <code>start_shorten</code> <code>float</code> <p>The length the start of the arc will be shorten by</p> <code>0.0</code> <code>target</code> <code>LayoutElement | None</code> <p>The target of the arc</p> <code>None</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the arc</p> <code>None</code> <code>arrowhead_fill</code> <code>NoneValueType | Color | None</code> <p>The arrowhead fill color of the arc</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>arrowhead_filter</code> <code>NoneValueType | Filter | None</code> <p>The arrowhead filter of the arc</p> <code>None</code> <code>arrowhead_stroke</code> <code>NoneValueType | Color | None</code> <p>The arrowhead stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>arrowhead_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The arrowhead stroke dasharray of the arc</p> <code>None</code> <code>arrowhead_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The arrowhead stroke dashoffset of the arc</p> <code>None</code> <code>arrowhead_stroke_width</code> <code>float | None</code> <p>The arrowhead stroke width of the arc</p> <code>1.25</code> <code>arrowhead_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The arrowhead transform of the arc</p> <code>None</code> <code>arrowhead_height</code> <code>float</code> <p>The height of the bar arrowhead</p> <code>10.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>arrowhead_base</code> <p>Return the arrowhead base anchor point of the single-headed arc.</p> <code>arrowhead_bbox</code> <p>Return the bounding box of the single-headed arc arrowhead.</p> <code>arrowhead_border</code> <p>Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.</p> <code>arrowhead_drawing_elements</code> <p>Return the drawing elements of the single-headed arc arrowhead.</p> <code>arrowhead_length</code> <p>Return the length of the single-headed arc arrowhead.</p> <code>arrowhead_tip</code> <p>Return the arrowhead tip anchor point of the single-headed arc.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>childless</code> <p>Return a copy of the arc with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>end_point</code> <p>Return the ending point of the arc.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>fraction</code> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <code>length</code> <p>Return the total length of the arc path.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_children</code> <p>Return the self children of the arc.</p> <code>own_drawing_elements</code> <p>Return the self drawing elements of the single-headed arc.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>path_drawing_elements</code> <p>Return the drawing elements of the single-headed arc path.</p> <code>points</code> <p>Return the points of the arc path.</p> <code>start_point</code> <p>Return the starting point of the arc.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.InhibitionLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.InhibitionLayout.arrowhead_base","title":"arrowhead_base","text":"<pre><code>arrowhead_base() -&gt; Point\n</code></pre> <p>Return the arrowhead base anchor point of the single-headed arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_base(self) -&gt; Point:\n    \"\"\"Return the arrowhead base anchor point of the single-headed arc.\"\"\"\n    arrowhead_length = self.arrowhead_length()\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return self.arrowhead_tip() - (arrowhead_length, 0)\n    fraction = 1 - (arrowhead_length + self.end_shorten) / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.InhibitionLayout.arrowhead_bbox","title":"arrowhead_bbox","text":"<pre><code>arrowhead_bbox() -&gt; Bbox\n</code></pre> <p>Return the bounding box of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_bbox(self) -&gt; Bbox:\n    \"\"\"Return the bounding box of the single-headed arc arrowhead.\"\"\"\n    return get_drawing_elements_bbox(self.arrowhead_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.InhibitionLayout.arrowhead_border","title":"arrowhead_border","text":"<pre><code>arrowhead_border(point: Point) -&gt; Point\n</code></pre> <p>Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_border(self, point: Point) -&gt; Point:\n    \"\"\"Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    point = get_drawing_elements_border(self.arrowhead_drawing_elements(), point)\n    if point is None:\n        return self.arrowhead_tip()\n    return point\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.InhibitionLayout.arrowhead_drawing_elements","title":"arrowhead_drawing_elements","text":"<pre><code>arrowhead_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_drawing_elements(\n    self,\n) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the single-headed arc arrowhead.\"\"\"\n    drawing_elements = self._arrowhead_border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_arrowhead\",\n        elements=tuple(drawing_elements),\n        fill=self.arrowhead_fill,\n        filter_=self.arrowhead_filter,\n        id_=f\"{self.id_}_arrowhead\",\n        stroke=self.arrowhead_stroke,\n        stroke_dasharray=self.arrowhead_stroke_dasharray,\n        stroke_dashoffset=self.arrowhead_stroke_dashoffset,\n        stroke_width=self.arrowhead_stroke_width,\n        transform=self.arrowhead_transform,\n    )\n    transformation = self._get_arrowhead_transformation()\n    group = group.transformed(transformation)\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.InhibitionLayout.arrowhead_length","title":"arrowhead_length","text":"<pre><code>arrowhead_length() -&gt; float\n</code></pre> <p>Return the length of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_length(self) -&gt; float:\n    \"\"\"Return the length of the single-headed arc arrowhead.\"\"\"\n    bbox = get_drawing_elements_bbox(self._arrowhead_border_drawing_elements())\n    if math.isnan(bbox.width):\n        return 0.0\n    return bbox.east().x\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.InhibitionLayout.arrowhead_tip","title":"arrowhead_tip","text":"<pre><code>arrowhead_tip() -&gt; Point\n</code></pre> <p>Return the arrowhead tip anchor point of the single-headed arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_tip(self) -&gt; Point:\n    \"\"\"Return the arrowhead tip anchor point of the single-headed arc.\"\"\"\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return segment.p2\n    fraction = 1 - self.end_shorten / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.InhibitionLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.InhibitionLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the arc with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the arc with no children.\"\"\"\n    return dataclasses.replace(self, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.InhibitionLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.InhibitionLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.InhibitionLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.InhibitionLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.InhibitionLayout.end_point","title":"end_point","text":"<pre><code>end_point() -&gt; Point\n</code></pre> <p>Return the ending point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_point(self) -&gt; Point:\n    \"\"\"Return the ending point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[-1]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.InhibitionLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.InhibitionLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.InhibitionLayout.fraction","title":"fraction","text":"<pre><code>fraction(fraction: float) -&gt; tuple[Point, float]\n</code></pre> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def fraction(self, fraction: float) -&gt; tuple[Point, float]:\n    \"\"\"Return the position and angle on the arc at a given fraction (of the total arc length).\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    current_length = 0\n    length_to_reach = fraction * self.length()\n    for segment in self.segments:\n        current_length += segment.length()\n        if current_length &gt;= length_to_reach:\n            break\n    segment_start_length = current_length - segment.length()\n    segment_fraction = (length_to_reach - segment_start_length) / segment.length()\n    position, angle = segment.get_position_and_angle_at_fraction(segment_fraction)\n    return position, angle\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.InhibitionLayout.length","title":"length","text":"<pre><code>length() -&gt; float\n</code></pre> <p>Return the total length of the arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def length(self) -&gt; float:\n    \"\"\"Return the total length of the arc path.\"\"\"\n    return sum([segment.length() for segment in self.segments])\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.InhibitionLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.InhibitionLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the arc.\"\"\"\n    return []\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.InhibitionLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the self drawing elements of the single-headed arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the self drawing elements of the single-headed arc.\"\"\"\n    drawing_elements = (\n        self.path_drawing_elements() + self.arrowhead_drawing_elements()\n    )\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.InhibitionLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.InhibitionLayout.path_drawing_elements","title":"path_drawing_elements","text":"<pre><code>path_drawing_elements() -&gt; list[Path]\n</code></pre> <p>Return the drawing elements of the single-headed arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def path_drawing_elements(self) -&gt; list[Path]:\n    \"\"\"Return the drawing elements of the single-headed arc path.\"\"\"\n    arrowhead_length = self.arrowhead_length()\n    if len(self.segments) == 1:\n        segment = (\n            self.segments[0]\n            .shortened(self.start_shorten, \"start\")\n            .shortened(self.end_shorten + arrowhead_length, \"end\")\n        )\n        actions = [\n            MoveTo(segment.p1),\n            self._make_path_action_from_segment(segment),\n        ]\n    else:\n        first_segment = self.segments[0].shortened(self.start_shorten, \"start\")\n        last_segment = self.segments[-1].shortened(\n            self.end_shorten + arrowhead_length, \"end\"\n        )\n        actions = [\n            MoveTo(first_segment.p1),\n            self._make_path_action_from_segment(first_segment),\n        ]\n        for segment in self.segments[1:-1]:\n            action = self._make_path_action_from_segment(segment)\n            actions.append(action)\n        actions.append(self._make_path_action_from_segment(last_segment))\n    path = Path(\n        actions=tuple(actions),\n        class_=f\"{type(self).__name__}_path\",\n        fill=self.path_fill,\n        filter_=self.path_filter,\n        id_=f\"{self.id_}_path\",\n        stroke=self.path_stroke,\n        stroke_dasharray=self.path_stroke_dasharray,\n        stroke_dashoffset=self.path_stroke_dashoffset,\n        stroke_width=self.path_stroke_width,\n        transform=self.path_transform,\n    )\n    return [path]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.InhibitionLayout.points","title":"points","text":"<pre><code>points() -&gt; list[Point]\n</code></pre> <p>Return the points of the arc path.</p> <p>An arc with no segments has no points and returns an empty list.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def points(self) -&gt; list[Point]:\n    \"\"\"Return the points of the arc path.\n\n    An arc with no segments has no points and returns an empty list.\n    \"\"\"\n    if not self.segments:\n        return []\n    points = []\n    for segment in self.segments:\n        points.append(segment.p1)\n    points.append(segment.p2)\n    return points\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.InhibitionLayout.start_point","title":"start_point","text":"<pre><code>start_point() -&gt; Point\n</code></pre> <p>Return the starting point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_point(self) -&gt; Point:\n    \"\"\"Return the starting point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[0]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.InhibitionLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.LogicArcLayout","title":"LogicArcLayout  <code>dataclass</code>","text":"<pre><code>LogicArcLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, end_shorten: float = 0.0, fill: NoneValueType | Color | None = None, filter_: NoneValueType | Filter | None = None, path_fill: NoneValueType | Color | None = NoneValue, path_filter: NoneValueType | Filter | None = None, path_stroke: NoneValueType | Color | None = black, path_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, path_stroke_dashoffset: NoneValueType | float | None = None, path_stroke_width: float | None = 1.25, path_transform: NoneValueType | tuple[Transformation, ...] | None = None, stroke: NoneValueType | Color | None = None, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: NoneValueType | float | None = None, segments: tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...] = tuple(), source: LayoutElement | None = None, start_shorten: float = 0.0, target: LayoutElement | None = None, transform: NoneValueType | tuple[Transformation, ...] | None = None, arrowhead_fill: NoneValueType | Color | None = white, arrowhead_filter: NoneValueType | Filter | None = None, arrowhead_stroke: NoneValueType | Color | None = black, arrowhead_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, arrowhead_stroke_dashoffset: NoneValueType | float | None = None, arrowhead_stroke_width: float | None = 1.25, arrowhead_transform: NoneValueType | tuple[Transformation, ...] | None = None)\n</code></pre> <p>               Bases: <code>SBGNSingleHeadedArc</code></p> <p>Logic arc layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'3650608e-864e-4b21-bba2-56f0b41d30df'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>end_shorten</code> <code>float</code> <p>The length the end of the arc will be shorten by</p> <code>0.0</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the arc</p> <code>None</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the arc</p> <code>None</code> <code>path_fill</code> <code>NoneValueType | Color | None</code> <p>The path fill color of the arc</p> <code>&lt;momapy.drawing.NoneValueType object at 0x7f8b17323e00&gt;</code> <code>path_filter</code> <code>NoneValueType | Filter | None</code> <p>The path filter of the arc</p> <code>None</code> <code>path_stroke</code> <code>NoneValueType | Color | None</code> <p>The path stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>path_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The path stroke dasharray of the arc</p> <code>None</code> <code>path_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The path stroke dashoffset of the arc</p> <code>None</code> <code>path_stroke_width</code> <code>float | None</code> <p>The path stroke width of the arc</p> <code>1.25</code> <code>path_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The path transform of the arc</p> <code>None</code> <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the arc</p> <code>None</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the arc</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the arc</p> <code>None</code> <code>stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the arc</p> <code>None</code> <code>segments</code> <code>tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...]</code> <p>The path segments of the arc</p> <code>&lt;dynamic&gt;</code> <code>source</code> <code>LayoutElement | None</code> <p>The source of the arc</p> <code>None</code> <code>start_shorten</code> <code>float</code> <p>The length the start of the arc will be shorten by</p> <code>0.0</code> <code>target</code> <code>LayoutElement | None</code> <p>The target of the arc</p> <code>None</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the arc</p> <code>None</code> <code>arrowhead_fill</code> <code>NoneValueType | Color | None</code> <p>The arrowhead fill color of the arc</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>arrowhead_filter</code> <code>NoneValueType | Filter | None</code> <p>The arrowhead filter of the arc</p> <code>None</code> <code>arrowhead_stroke</code> <code>NoneValueType | Color | None</code> <p>The arrowhead stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>arrowhead_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The arrowhead stroke dasharray of the arc</p> <code>None</code> <code>arrowhead_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The arrowhead stroke dashoffset of the arc</p> <code>None</code> <code>arrowhead_stroke_width</code> <code>float | None</code> <p>The arrowhead stroke width of the arc</p> <code>1.25</code> <code>arrowhead_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The arrowhead transform of the arc</p> <code>None</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>arrowhead_base</code> <p>Return the arrowhead base anchor point of the single-headed arc.</p> <code>arrowhead_bbox</code> <p>Return the bounding box of the single-headed arc arrowhead.</p> <code>arrowhead_border</code> <p>Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.</p> <code>arrowhead_drawing_elements</code> <p>Return the drawing elements of the single-headed arc arrowhead.</p> <code>arrowhead_length</code> <p>Return the length of the single-headed arc arrowhead.</p> <code>arrowhead_tip</code> <p>Return the arrowhead tip anchor point of the single-headed arc.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>childless</code> <p>Return a copy of the arc with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>end_point</code> <p>Return the ending point of the arc.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>fraction</code> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <code>length</code> <p>Return the total length of the arc path.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_children</code> <p>Return the self children of the arc.</p> <code>own_drawing_elements</code> <p>Return the self drawing elements of the single-headed arc.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>path_drawing_elements</code> <p>Return the drawing elements of the single-headed arc path.</p> <code>points</code> <p>Return the points of the arc path.</p> <code>start_point</code> <p>Return the starting point of the arc.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.LogicArcLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.LogicArcLayout.arrowhead_base","title":"arrowhead_base","text":"<pre><code>arrowhead_base() -&gt; Point\n</code></pre> <p>Return the arrowhead base anchor point of the single-headed arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_base(self) -&gt; Point:\n    \"\"\"Return the arrowhead base anchor point of the single-headed arc.\"\"\"\n    arrowhead_length = self.arrowhead_length()\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return self.arrowhead_tip() - (arrowhead_length, 0)\n    fraction = 1 - (arrowhead_length + self.end_shorten) / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.LogicArcLayout.arrowhead_bbox","title":"arrowhead_bbox","text":"<pre><code>arrowhead_bbox() -&gt; Bbox\n</code></pre> <p>Return the bounding box of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_bbox(self) -&gt; Bbox:\n    \"\"\"Return the bounding box of the single-headed arc arrowhead.\"\"\"\n    return get_drawing_elements_bbox(self.arrowhead_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.LogicArcLayout.arrowhead_border","title":"arrowhead_border","text":"<pre><code>arrowhead_border(point: Point) -&gt; Point\n</code></pre> <p>Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_border(self, point: Point) -&gt; Point:\n    \"\"\"Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    point = get_drawing_elements_border(self.arrowhead_drawing_elements(), point)\n    if point is None:\n        return self.arrowhead_tip()\n    return point\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.LogicArcLayout.arrowhead_drawing_elements","title":"arrowhead_drawing_elements","text":"<pre><code>arrowhead_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_drawing_elements(\n    self,\n) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the single-headed arc arrowhead.\"\"\"\n    drawing_elements = self._arrowhead_border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_arrowhead\",\n        elements=tuple(drawing_elements),\n        fill=self.arrowhead_fill,\n        filter_=self.arrowhead_filter,\n        id_=f\"{self.id_}_arrowhead\",\n        stroke=self.arrowhead_stroke,\n        stroke_dasharray=self.arrowhead_stroke_dasharray,\n        stroke_dashoffset=self.arrowhead_stroke_dashoffset,\n        stroke_width=self.arrowhead_stroke_width,\n        transform=self.arrowhead_transform,\n    )\n    transformation = self._get_arrowhead_transformation()\n    group = group.transformed(transformation)\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.LogicArcLayout.arrowhead_length","title":"arrowhead_length","text":"<pre><code>arrowhead_length() -&gt; float\n</code></pre> <p>Return the length of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_length(self) -&gt; float:\n    \"\"\"Return the length of the single-headed arc arrowhead.\"\"\"\n    bbox = get_drawing_elements_bbox(self._arrowhead_border_drawing_elements())\n    if math.isnan(bbox.width):\n        return 0.0\n    return bbox.east().x\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.LogicArcLayout.arrowhead_tip","title":"arrowhead_tip","text":"<pre><code>arrowhead_tip() -&gt; Point\n</code></pre> <p>Return the arrowhead tip anchor point of the single-headed arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_tip(self) -&gt; Point:\n    \"\"\"Return the arrowhead tip anchor point of the single-headed arc.\"\"\"\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return segment.p2\n    fraction = 1 - self.end_shorten / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.LogicArcLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.LogicArcLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the arc with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the arc with no children.\"\"\"\n    return dataclasses.replace(self, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.LogicArcLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.LogicArcLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.LogicArcLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.LogicArcLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.LogicArcLayout.end_point","title":"end_point","text":"<pre><code>end_point() -&gt; Point\n</code></pre> <p>Return the ending point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_point(self) -&gt; Point:\n    \"\"\"Return the ending point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[-1]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.LogicArcLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.LogicArcLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.LogicArcLayout.fraction","title":"fraction","text":"<pre><code>fraction(fraction: float) -&gt; tuple[Point, float]\n</code></pre> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def fraction(self, fraction: float) -&gt; tuple[Point, float]:\n    \"\"\"Return the position and angle on the arc at a given fraction (of the total arc length).\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    current_length = 0\n    length_to_reach = fraction * self.length()\n    for segment in self.segments:\n        current_length += segment.length()\n        if current_length &gt;= length_to_reach:\n            break\n    segment_start_length = current_length - segment.length()\n    segment_fraction = (length_to_reach - segment_start_length) / segment.length()\n    position, angle = segment.get_position_and_angle_at_fraction(segment_fraction)\n    return position, angle\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.LogicArcLayout.length","title":"length","text":"<pre><code>length() -&gt; float\n</code></pre> <p>Return the total length of the arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def length(self) -&gt; float:\n    \"\"\"Return the total length of the arc path.\"\"\"\n    return sum([segment.length() for segment in self.segments])\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.LogicArcLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.LogicArcLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the arc.\"\"\"\n    return []\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.LogicArcLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the self drawing elements of the single-headed arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the self drawing elements of the single-headed arc.\"\"\"\n    drawing_elements = (\n        self.path_drawing_elements() + self.arrowhead_drawing_elements()\n    )\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.LogicArcLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.LogicArcLayout.path_drawing_elements","title":"path_drawing_elements","text":"<pre><code>path_drawing_elements() -&gt; list[Path]\n</code></pre> <p>Return the drawing elements of the single-headed arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def path_drawing_elements(self) -&gt; list[Path]:\n    \"\"\"Return the drawing elements of the single-headed arc path.\"\"\"\n    arrowhead_length = self.arrowhead_length()\n    if len(self.segments) == 1:\n        segment = (\n            self.segments[0]\n            .shortened(self.start_shorten, \"start\")\n            .shortened(self.end_shorten + arrowhead_length, \"end\")\n        )\n        actions = [\n            MoveTo(segment.p1),\n            self._make_path_action_from_segment(segment),\n        ]\n    else:\n        first_segment = self.segments[0].shortened(self.start_shorten, \"start\")\n        last_segment = self.segments[-1].shortened(\n            self.end_shorten + arrowhead_length, \"end\"\n        )\n        actions = [\n            MoveTo(first_segment.p1),\n            self._make_path_action_from_segment(first_segment),\n        ]\n        for segment in self.segments[1:-1]:\n            action = self._make_path_action_from_segment(segment)\n            actions.append(action)\n        actions.append(self._make_path_action_from_segment(last_segment))\n    path = Path(\n        actions=tuple(actions),\n        class_=f\"{type(self).__name__}_path\",\n        fill=self.path_fill,\n        filter_=self.path_filter,\n        id_=f\"{self.id_}_path\",\n        stroke=self.path_stroke,\n        stroke_dasharray=self.path_stroke_dasharray,\n        stroke_dashoffset=self.path_stroke_dashoffset,\n        stroke_width=self.path_stroke_width,\n        transform=self.path_transform,\n    )\n    return [path]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.LogicArcLayout.points","title":"points","text":"<pre><code>points() -&gt; list[Point]\n</code></pre> <p>Return the points of the arc path.</p> <p>An arc with no segments has no points and returns an empty list.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def points(self) -&gt; list[Point]:\n    \"\"\"Return the points of the arc path.\n\n    An arc with no segments has no points and returns an empty list.\n    \"\"\"\n    if not self.segments:\n        return []\n    points = []\n    for segment in self.segments:\n        points.append(segment.p1)\n    points.append(segment.p2)\n    return points\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.LogicArcLayout.start_point","title":"start_point","text":"<pre><code>start_point() -&gt; Point\n</code></pre> <p>Return the starting point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_point(self) -&gt; Point:\n    \"\"\"Return the starting point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[0]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.LogicArcLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.LogicalOperator","title":"LogicalOperator  <code>dataclass</code>","text":"<pre><code>LogicalOperator(*, id_: str = make_uuid4_as_str(), inputs: frozenset[LogicalOperatorInput] = frozenset())\n</code></pre> <p>               Bases: <code>SBGNModelElement</code></p> <p>Abstract base class for logical operators.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'a1a917c4-8bd1-48a2-9b46-79e1b18e3b31'</code> <code>inputs</code> <code>frozenset[LogicalOperatorInput]</code> <p>The inputs of the logical operator</p> <code>&lt;dynamic&gt;</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.LogicalOperator.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.LogicalOperatorInput","title":"LogicalOperatorInput  <code>dataclass</code>","text":"<pre><code>LogicalOperatorInput(*, id_: str = make_uuid4_as_str(), referred_element: Union[EntityPool, ForwardRef(LogicalOperator, module=__name__)])\n</code></pre> <p>               Bases: <code>SBGNRole</code></p> <p>Logical operator input.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'f177b720-3526-41cb-bcab-fecf0d6767cc'</code> <code>referred_element</code> <code>EntityPool | ForwardRef</code> <p>The element of the logical operator input</p> required <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.LogicalOperatorInput.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.Macromolecule","title":"Macromolecule  <code>dataclass</code>","text":"<pre><code>Macromolecule(*, id_: str = make_uuid4_as_str(), compartment: Compartment | None = None, label: str | None = None, state_variables: frozenset[StateVariable] = frozenset(), units_of_information: frozenset[UnitOfInformation] = frozenset())\n</code></pre> <p>               Bases: <code>EntityPool</code></p> <p>Macromolecule.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'3b690fe0-5d7d-4e04-9f44-1f743bc1a161'</code> <code>compartment</code> <code>Compartment | None</code> <p>The compartment of the entity pool</p> <code>None</code> <code>label</code> <code>str | None</code> <p>The label of the macromolecule</p> <code>None</code> <code>state_variables</code> <code>frozenset[StateVariable]</code> <p>The state variables of the macromolecule</p> <code>&lt;dynamic&gt;</code> <code>units_of_information</code> <code>frozenset[UnitOfInformation]</code> <p>The units of information of the macromolecule</p> <code>&lt;dynamic&gt;</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.Macromolecule.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeLayout","title":"MacromoleculeLayout  <code>dataclass</code>","text":"<pre><code>MacromoleculeLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = white, filter_: NoneValueType | Filter | None = None, height: float = 30.0, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = 1.25, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 60.0, rounded_corners: float = 5.0)\n</code></pre> <p>               Bases: <code>_SimpleMixin</code>, <code>SBGNNode</code></p> <p>Macromolecule layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'efa6fc28-2008-44bf-a735-9598b8daef96'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>30.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The stroke width of the node</p> <code>1.25</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>60.0</code> <code>rounded_corners</code> <code>float</code> <p>The corner radius of the macromolecule layout</p> <code>5.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>angle</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Return the east anchor of the node.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Return the label center anchor of the node.</p> <code>north</code> <p>Return the north anchor of the node.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Return the south anchor of the node.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Return the west anchor of the node.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeLayout.angle","title":"angle","text":"<pre><code>angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Return the east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Return the east anchor of the node.\"\"\"\n    return self.own_angle(0)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Return the label center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Return the label center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Return the north anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Return the north anchor of the node.\"\"\"\n    return self.own_angle(90)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Return the south anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Return the south anchor of the node.\"\"\"\n    return self.own_angle(270)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Return the west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Return the west anchor of the node.\"\"\"\n    return self.own_angle(180)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeMultimer","title":"MacromoleculeMultimer  <code>dataclass</code>","text":"<pre><code>MacromoleculeMultimer(*, id_: str = make_uuid4_as_str(), compartment: Compartment | None = None, label: str | None = None, state_variables: frozenset[StateVariable] = frozenset(), units_of_information: frozenset[UnitOfInformation] = frozenset(), subunits: frozenset[Subunit] = frozenset(), cardinality: int | None = None)\n</code></pre> <p>               Bases: <code>Multimer</code></p> <p>Macromolecule multimer.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'8e98d165-aadf-4f95-8602-deb4aa521a50'</code> <code>compartment</code> <code>Compartment | None</code> <p>The compartment of the entity pool</p> <code>None</code> <code>label</code> <code>str | None</code> <p>The label of the complex</p> <code>None</code> <code>state_variables</code> <code>frozenset[StateVariable]</code> <p>The state variables of the complex</p> <code>&lt;dynamic&gt;</code> <code>units_of_information</code> <code>frozenset[UnitOfInformation]</code> <p>The units of information of the complex</p> <code>&lt;dynamic&gt;</code> <code>subunits</code> <code>frozenset[Subunit]</code> <p>The subunits of the complex</p> <code>&lt;dynamic&gt;</code> <code>cardinality</code> <code>int | None</code> <p>The cardinality of the multimer</p> <code>None</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeMultimer.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeMultimerLayout","title":"MacromoleculeMultimerLayout  <code>dataclass</code>","text":"<pre><code>MacromoleculeMultimerLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = white, filter_: NoneValueType | Filter | None = None, height: float = 30.0, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = 1.25, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 60.0, offset: float = 3.0, subunits_stroke: tuple[NoneValueType | Color, ...] | None = None, subunits_stroke_width: tuple[NoneValueType | float, ...] | None = None, subunits_stroke_dasharray: tuple[NoneValueType | tuple[float, ...], ...] | None = None, subunits_stroke_dashoffset: tuple[float, ...] | None = None, subunits_fill: tuple[NoneValueType | Color, ...] | None = None, subunits_transform: tuple[NoneValueType | tuple[Transformation, ...], ...] | None = None, subunits_filter: tuple[NoneValueType | Filter, ...] | None = None, rounded_corners: float = 5.0)\n</code></pre> <p>               Bases: <code>_MultiMixin</code>, <code>SBGNNode</code></p> <p>Macromolecule multimer layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'58605c38-009d-4482-a2ad-f370f68168e8'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>30.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The stroke width of the node</p> <code>1.25</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>60.0</code> <code>offset</code> <code>float</code> <p>Offset distance between stacked units.</p> <code>3.0</code> <code>subunits_stroke</code> <code>tuple[NoneValueType | Color, ...] | None</code> <p>Tuple of stroke colors for each subunit.</p> <code>None</code> <code>subunits_stroke_width</code> <code>tuple[NoneValueType | float, ...] | None</code> <p>Tuple of stroke widths for each subunit.</p> <code>None</code> <code>subunits_stroke_dasharray</code> <code>tuple[NoneValueType | tuple[float, ...], ...] | None</code> <p>Tuple of dash patterns for each subunit.</p> <code>None</code> <code>subunits_stroke_dashoffset</code> <code>tuple[float, ...] | None</code> <p>Tuple of dash offsets for each subunit.</p> <code>None</code> <code>subunits_fill</code> <code>tuple[NoneValueType | Color, ...] | None</code> <p>Tuple of fill colors for each subunit.</p> <code>None</code> <code>subunits_transform</code> <code>tuple[NoneValueType | tuple[Transformation, ...], ...] | None</code> <p>Tuple of transformations for each subunit.</p> <code>None</code> <code>subunits_filter</code> <code>tuple[NoneValueType | Filter, ...] | None</code> <p>Tuple of filters for each subunit.</p> <code>None</code> <code>rounded_corners</code> <code>float</code> <p>The corner radius of the macromolecule multimer layout</p> <code>5.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>angle</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Return the east anchor of the node.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Get the center position for the node label.</p> <code>north</code> <p>Return the north anchor of the node.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Return the south anchor of the node.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Return the west anchor of the node.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeMultimerLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeMultimerLayout.angle","title":"angle","text":"<pre><code>angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeMultimerLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeMultimerLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeMultimerLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeMultimerLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeMultimerLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeMultimerLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeMultimerLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeMultimerLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeMultimerLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Return the east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Return the east anchor of the node.\"\"\"\n    return self.own_angle(0)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeMultimerLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeMultimerLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeMultimerLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeMultimerLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeMultimerLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Get the center position for the node label.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point where the label should be centered.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Get the center position for the node label.\n\n    Returns:\n        Point where the label should be centered.\n    \"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2 - (self._n - 1) * self.offset,\n        self.height / 2 - height / 2 - (self._n - 1) * self.offset,\n    )\n    return self._make_subunit_shape(\n        position=position, width=width, height=height\n    ).position\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeMultimerLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Return the north anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Return the north anchor of the node.\"\"\"\n    return self.own_angle(90)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeMultimerLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeMultimerLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeMultimerLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeMultimerLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeMultimerLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeMultimerLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeMultimerLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeMultimerLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeMultimerLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeMultimerLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeMultimerLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeMultimerLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Return the south anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Return the south anchor of the node.\"\"\"\n    return self.own_angle(270)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeMultimerLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeMultimerLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeMultimerLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeMultimerLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeMultimerLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeMultimerLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Return the west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Return the west anchor of the node.\"\"\"\n    return self.own_angle(180)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeMultimerLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeMultimerLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeMultimerLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeMultimerLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeMultimerSubunit","title":"MacromoleculeMultimerSubunit  <code>dataclass</code>","text":"<pre><code>MacromoleculeMultimerSubunit(*, id_: str = make_uuid4_as_str(), label: str | None = None, state_variables: frozenset[StateVariable] = frozenset(), units_of_information: frozenset[UnitOfInformation] = frozenset(), subunits: frozenset[Subunit] = frozenset(), cardinality: int | None = None)\n</code></pre> <p>               Bases: <code>MultimerSubunit</code></p> <p>Macromolecule multimer subunit.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'ab99a86e-4937-4f0e-8050-0b595bda86ff'</code> <code>label</code> <code>str | None</code> <p>The label of the subunit</p> <code>None</code> <code>state_variables</code> <code>frozenset[StateVariable]</code> <p>The state variables of the complex subunit</p> <code>&lt;dynamic&gt;</code> <code>units_of_information</code> <code>frozenset[UnitOfInformation]</code> <p>The units of information of the complex subunit</p> <code>&lt;dynamic&gt;</code> <code>subunits</code> <code>frozenset[Subunit]</code> <p>The subunits of the complex subunit</p> <code>&lt;dynamic&gt;</code> <code>cardinality</code> <code>int | None</code> <p>The cardinality of the multimer subunit</p> <code>None</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeMultimerSubunit.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeMultimerSubunitLayout","title":"MacromoleculeMultimerSubunitLayout  <code>dataclass</code>","text":"<pre><code>MacromoleculeMultimerSubunitLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = white, filter_: NoneValueType | Filter | None = None, height: float = 30.0, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = 1.25, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 60.0, offset: float = 3.0, subunits_stroke: tuple[NoneValueType | Color, ...] | None = None, subunits_stroke_width: tuple[NoneValueType | float, ...] | None = None, subunits_stroke_dasharray: tuple[NoneValueType | tuple[float, ...], ...] | None = None, subunits_stroke_dashoffset: tuple[float, ...] | None = None, subunits_fill: tuple[NoneValueType | Color, ...] | None = None, subunits_transform: tuple[NoneValueType | tuple[Transformation, ...], ...] | None = None, subunits_filter: tuple[NoneValueType | Filter, ...] | None = None, rounded_corners: float = 5.0)\n</code></pre> <p>               Bases: <code>_MultiMixin</code>, <code>SBGNNode</code></p> <p>Macromolecule multimer subunit layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'7954cf04-9e66-4801-85ba-cf8327cf3e0c'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>30.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The stroke width of the node</p> <code>1.25</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>60.0</code> <code>offset</code> <code>float</code> <p>Offset distance between stacked units.</p> <code>3.0</code> <code>subunits_stroke</code> <code>tuple[NoneValueType | Color, ...] | None</code> <p>Tuple of stroke colors for each subunit.</p> <code>None</code> <code>subunits_stroke_width</code> <code>tuple[NoneValueType | float, ...] | None</code> <p>Tuple of stroke widths for each subunit.</p> <code>None</code> <code>subunits_stroke_dasharray</code> <code>tuple[NoneValueType | tuple[float, ...], ...] | None</code> <p>Tuple of dash patterns for each subunit.</p> <code>None</code> <code>subunits_stroke_dashoffset</code> <code>tuple[float, ...] | None</code> <p>Tuple of dash offsets for each subunit.</p> <code>None</code> <code>subunits_fill</code> <code>tuple[NoneValueType | Color, ...] | None</code> <p>Tuple of fill colors for each subunit.</p> <code>None</code> <code>subunits_transform</code> <code>tuple[NoneValueType | tuple[Transformation, ...], ...] | None</code> <p>Tuple of transformations for each subunit.</p> <code>None</code> <code>subunits_filter</code> <code>tuple[NoneValueType | Filter, ...] | None</code> <p>Tuple of filters for each subunit.</p> <code>None</code> <code>rounded_corners</code> <code>float</code> <p>The corner radius of the macromolecule multimer subunit layout</p> <code>5.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>angle</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Return the east anchor of the node.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Get the center position for the node label.</p> <code>north</code> <p>Return the north anchor of the node.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Return the south anchor of the node.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Return the west anchor of the node.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeMultimerSubunitLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeMultimerSubunitLayout.angle","title":"angle","text":"<pre><code>angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeMultimerSubunitLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeMultimerSubunitLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeMultimerSubunitLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeMultimerSubunitLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeMultimerSubunitLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeMultimerSubunitLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeMultimerSubunitLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeMultimerSubunitLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeMultimerSubunitLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Return the east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Return the east anchor of the node.\"\"\"\n    return self.own_angle(0)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeMultimerSubunitLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeMultimerSubunitLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeMultimerSubunitLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeMultimerSubunitLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeMultimerSubunitLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Get the center position for the node label.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point where the label should be centered.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Get the center position for the node label.\n\n    Returns:\n        Point where the label should be centered.\n    \"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2 - (self._n - 1) * self.offset,\n        self.height / 2 - height / 2 - (self._n - 1) * self.offset,\n    )\n    return self._make_subunit_shape(\n        position=position, width=width, height=height\n    ).position\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeMultimerSubunitLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Return the north anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Return the north anchor of the node.\"\"\"\n    return self.own_angle(90)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeMultimerSubunitLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeMultimerSubunitLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeMultimerSubunitLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeMultimerSubunitLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeMultimerSubunitLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeMultimerSubunitLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeMultimerSubunitLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeMultimerSubunitLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeMultimerSubunitLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeMultimerSubunitLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeMultimerSubunitLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeMultimerSubunitLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Return the south anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Return the south anchor of the node.\"\"\"\n    return self.own_angle(270)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeMultimerSubunitLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeMultimerSubunitLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeMultimerSubunitLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeMultimerSubunitLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeMultimerSubunitLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeMultimerSubunitLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Return the west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Return the west anchor of the node.\"\"\"\n    return self.own_angle(180)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeMultimerSubunitLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeMultimerSubunitLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeMultimerSubunitLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeMultimerSubunitLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeSubunit","title":"MacromoleculeSubunit  <code>dataclass</code>","text":"<pre><code>MacromoleculeSubunit(*, id_: str = make_uuid4_as_str(), label: str | None = None, state_variables: frozenset[StateVariable] = frozenset(), units_of_information: frozenset[UnitOfInformation] = frozenset())\n</code></pre> <p>               Bases: <code>Subunit</code></p> <p>Macromolecule subunit.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'cf0162ba-d74b-40e8-a10d-b000d79ee4d9'</code> <code>label</code> <code>str | None</code> <p>The label of the subunit</p> <code>None</code> <code>state_variables</code> <code>frozenset[StateVariable]</code> <p>The state variables of the macromolecule subunit</p> <code>&lt;dynamic&gt;</code> <code>units_of_information</code> <code>frozenset[UnitOfInformation]</code> <p>The units of information of the macromolecule subunit</p> <code>&lt;dynamic&gt;</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeSubunit.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeSubunitLayout","title":"MacromoleculeSubunitLayout  <code>dataclass</code>","text":"<pre><code>MacromoleculeSubunitLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = white, filter_: NoneValueType | Filter | None = None, height: float = 30.0, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = 1.25, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 60.0, rounded_corners: float = 5.0)\n</code></pre> <p>               Bases: <code>_SimpleMixin</code>, <code>SBGNNode</code></p> <p>Macromolecule subunit layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'d85c5e92-1df9-4e8d-a2e6-c6ee9b6eda69'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>30.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The stroke width of the node</p> <code>1.25</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>60.0</code> <code>rounded_corners</code> <code>float</code> <p>The corner radius of the macromolecule subunit layout</p> <code>5.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>angle</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Return the east anchor of the node.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Return the label center anchor of the node.</p> <code>north</code> <p>Return the north anchor of the node.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Return the south anchor of the node.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Return the west anchor of the node.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeSubunitLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeSubunitLayout.angle","title":"angle","text":"<pre><code>angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeSubunitLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeSubunitLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeSubunitLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeSubunitLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeSubunitLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeSubunitLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeSubunitLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeSubunitLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeSubunitLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Return the east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Return the east anchor of the node.\"\"\"\n    return self.own_angle(0)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeSubunitLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeSubunitLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeSubunitLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeSubunitLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeSubunitLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Return the label center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Return the label center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeSubunitLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Return the north anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Return the north anchor of the node.\"\"\"\n    return self.own_angle(90)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeSubunitLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeSubunitLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeSubunitLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeSubunitLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeSubunitLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeSubunitLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeSubunitLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeSubunitLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeSubunitLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeSubunitLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeSubunitLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeSubunitLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Return the south anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Return the south anchor of the node.\"\"\"\n    return self.own_angle(270)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeSubunitLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeSubunitLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeSubunitLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeSubunitLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeSubunitLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeSubunitLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Return the west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Return the west anchor of the node.\"\"\"\n    return self.own_angle(180)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeSubunitLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeSubunitLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeSubunitLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MacromoleculeSubunitLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.Modulation","title":"Modulation  <code>dataclass</code>","text":"<pre><code>Modulation(*, id_: str = make_uuid4_as_str(), source: EntityPool | LogicalOperator, target: Process)\n</code></pre> <p>               Bases: <code>SBGNModelElement</code></p> <p>Modulation.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'25c4651b-fe2d-43bb-ab19-17eced61f8ad'</code> <code>source</code> <code>EntityPool | LogicalOperator</code> <p>The source of the modulation</p> required <code>target</code> <code>Process</code> <p>The target of the modulation</p> required <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.Modulation.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ModulationLayout","title":"ModulationLayout  <code>dataclass</code>","text":"<pre><code>ModulationLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, end_shorten: float = 0.0, fill: NoneValueType | Color | None = None, filter_: NoneValueType | Filter | None = None, path_fill: NoneValueType | Color | None = NoneValue, path_filter: NoneValueType | Filter | None = None, path_stroke: NoneValueType | Color | None = black, path_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, path_stroke_dashoffset: NoneValueType | float | None = None, path_stroke_width: float | None = 1.25, path_transform: NoneValueType | tuple[Transformation, ...] | None = None, stroke: NoneValueType | Color | None = None, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: NoneValueType | float | None = None, segments: tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...] = tuple(), source: LayoutElement | None = None, start_shorten: float = 0.0, target: LayoutElement | None = None, transform: NoneValueType | tuple[Transformation, ...] | None = None, arrowhead_fill: NoneValueType | Color | None = white, arrowhead_filter: NoneValueType | Filter | None = None, arrowhead_stroke: NoneValueType | Color | None = black, arrowhead_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, arrowhead_stroke_dashoffset: NoneValueType | float | None = None, arrowhead_stroke_width: float | None = 1.25, arrowhead_transform: NoneValueType | tuple[Transformation, ...] | None = None, arrowhead_height: float = 10.0, arrowhead_width: float = 10.0)\n</code></pre> <p>               Bases: <code>SBGNSingleHeadedArc</code></p> <p>Modulation layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'b419d0f2-3dcb-4dfd-86d0-89c4fb41c915'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>end_shorten</code> <code>float</code> <p>The length the end of the arc will be shorten by</p> <code>0.0</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the arc</p> <code>None</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the arc</p> <code>None</code> <code>path_fill</code> <code>NoneValueType | Color | None</code> <p>The path fill color of the arc</p> <code>&lt;momapy.drawing.NoneValueType object at 0x7f8b17323e00&gt;</code> <code>path_filter</code> <code>NoneValueType | Filter | None</code> <p>The path filter of the arc</p> <code>None</code> <code>path_stroke</code> <code>NoneValueType | Color | None</code> <p>The path stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>path_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The path stroke dasharray of the arc</p> <code>None</code> <code>path_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The path stroke dashoffset of the arc</p> <code>None</code> <code>path_stroke_width</code> <code>float | None</code> <p>The path stroke width of the arc</p> <code>1.25</code> <code>path_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The path transform of the arc</p> <code>None</code> <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the arc</p> <code>None</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the arc</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the arc</p> <code>None</code> <code>stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the arc</p> <code>None</code> <code>segments</code> <code>tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...]</code> <p>The path segments of the arc</p> <code>&lt;dynamic&gt;</code> <code>source</code> <code>LayoutElement | None</code> <p>The source of the arc</p> <code>None</code> <code>start_shorten</code> <code>float</code> <p>The length the start of the arc will be shorten by</p> <code>0.0</code> <code>target</code> <code>LayoutElement | None</code> <p>The target of the arc</p> <code>None</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the arc</p> <code>None</code> <code>arrowhead_fill</code> <code>NoneValueType | Color | None</code> <p>The arrowhead fill color of the arc</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>arrowhead_filter</code> <code>NoneValueType | Filter | None</code> <p>The arrowhead filter of the arc</p> <code>None</code> <code>arrowhead_stroke</code> <code>NoneValueType | Color | None</code> <p>The arrowhead stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>arrowhead_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The arrowhead stroke dasharray of the arc</p> <code>None</code> <code>arrowhead_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The arrowhead stroke dashoffset of the arc</p> <code>None</code> <code>arrowhead_stroke_width</code> <code>float | None</code> <p>The arrowhead stroke width of the arc</p> <code>1.25</code> <code>arrowhead_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The arrowhead transform of the arc</p> <code>None</code> <code>arrowhead_height</code> <code>float</code> <p>The height of the arrowhead</p> <code>10.0</code> <code>arrowhead_width</code> <code>float</code> <p>The width of the arrowhead</p> <code>10.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>arrowhead_base</code> <p>Return the arrowhead base anchor point of the single-headed arc.</p> <code>arrowhead_bbox</code> <p>Return the bounding box of the single-headed arc arrowhead.</p> <code>arrowhead_border</code> <p>Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.</p> <code>arrowhead_drawing_elements</code> <p>Return the drawing elements of the single-headed arc arrowhead.</p> <code>arrowhead_length</code> <p>Return the length of the single-headed arc arrowhead.</p> <code>arrowhead_tip</code> <p>Return the arrowhead tip anchor point of the single-headed arc.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>childless</code> <p>Return a copy of the arc with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>end_point</code> <p>Return the ending point of the arc.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>fraction</code> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <code>length</code> <p>Return the total length of the arc path.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_children</code> <p>Return the self children of the arc.</p> <code>own_drawing_elements</code> <p>Return the self drawing elements of the single-headed arc.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>path_drawing_elements</code> <p>Return the drawing elements of the single-headed arc path.</p> <code>points</code> <p>Return the points of the arc path.</p> <code>start_point</code> <p>Return the starting point of the arc.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ModulationLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ModulationLayout.arrowhead_base","title":"arrowhead_base","text":"<pre><code>arrowhead_base() -&gt; Point\n</code></pre> <p>Return the arrowhead base anchor point of the single-headed arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_base(self) -&gt; Point:\n    \"\"\"Return the arrowhead base anchor point of the single-headed arc.\"\"\"\n    arrowhead_length = self.arrowhead_length()\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return self.arrowhead_tip() - (arrowhead_length, 0)\n    fraction = 1 - (arrowhead_length + self.end_shorten) / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ModulationLayout.arrowhead_bbox","title":"arrowhead_bbox","text":"<pre><code>arrowhead_bbox() -&gt; Bbox\n</code></pre> <p>Return the bounding box of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_bbox(self) -&gt; Bbox:\n    \"\"\"Return the bounding box of the single-headed arc arrowhead.\"\"\"\n    return get_drawing_elements_bbox(self.arrowhead_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ModulationLayout.arrowhead_border","title":"arrowhead_border","text":"<pre><code>arrowhead_border(point: Point) -&gt; Point\n</code></pre> <p>Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_border(self, point: Point) -&gt; Point:\n    \"\"\"Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    point = get_drawing_elements_border(self.arrowhead_drawing_elements(), point)\n    if point is None:\n        return self.arrowhead_tip()\n    return point\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ModulationLayout.arrowhead_drawing_elements","title":"arrowhead_drawing_elements","text":"<pre><code>arrowhead_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_drawing_elements(\n    self,\n) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the single-headed arc arrowhead.\"\"\"\n    drawing_elements = self._arrowhead_border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_arrowhead\",\n        elements=tuple(drawing_elements),\n        fill=self.arrowhead_fill,\n        filter_=self.arrowhead_filter,\n        id_=f\"{self.id_}_arrowhead\",\n        stroke=self.arrowhead_stroke,\n        stroke_dasharray=self.arrowhead_stroke_dasharray,\n        stroke_dashoffset=self.arrowhead_stroke_dashoffset,\n        stroke_width=self.arrowhead_stroke_width,\n        transform=self.arrowhead_transform,\n    )\n    transformation = self._get_arrowhead_transformation()\n    group = group.transformed(transformation)\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ModulationLayout.arrowhead_length","title":"arrowhead_length","text":"<pre><code>arrowhead_length() -&gt; float\n</code></pre> <p>Return the length of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_length(self) -&gt; float:\n    \"\"\"Return the length of the single-headed arc arrowhead.\"\"\"\n    bbox = get_drawing_elements_bbox(self._arrowhead_border_drawing_elements())\n    if math.isnan(bbox.width):\n        return 0.0\n    return bbox.east().x\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ModulationLayout.arrowhead_tip","title":"arrowhead_tip","text":"<pre><code>arrowhead_tip() -&gt; Point\n</code></pre> <p>Return the arrowhead tip anchor point of the single-headed arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_tip(self) -&gt; Point:\n    \"\"\"Return the arrowhead tip anchor point of the single-headed arc.\"\"\"\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return segment.p2\n    fraction = 1 - self.end_shorten / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ModulationLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ModulationLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the arc with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the arc with no children.\"\"\"\n    return dataclasses.replace(self, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ModulationLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ModulationLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ModulationLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ModulationLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ModulationLayout.end_point","title":"end_point","text":"<pre><code>end_point() -&gt; Point\n</code></pre> <p>Return the ending point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_point(self) -&gt; Point:\n    \"\"\"Return the ending point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[-1]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ModulationLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ModulationLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ModulationLayout.fraction","title":"fraction","text":"<pre><code>fraction(fraction: float) -&gt; tuple[Point, float]\n</code></pre> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def fraction(self, fraction: float) -&gt; tuple[Point, float]:\n    \"\"\"Return the position and angle on the arc at a given fraction (of the total arc length).\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    current_length = 0\n    length_to_reach = fraction * self.length()\n    for segment in self.segments:\n        current_length += segment.length()\n        if current_length &gt;= length_to_reach:\n            break\n    segment_start_length = current_length - segment.length()\n    segment_fraction = (length_to_reach - segment_start_length) / segment.length()\n    position, angle = segment.get_position_and_angle_at_fraction(segment_fraction)\n    return position, angle\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ModulationLayout.length","title":"length","text":"<pre><code>length() -&gt; float\n</code></pre> <p>Return the total length of the arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def length(self) -&gt; float:\n    \"\"\"Return the total length of the arc path.\"\"\"\n    return sum([segment.length() for segment in self.segments])\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ModulationLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ModulationLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the arc.\"\"\"\n    return []\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ModulationLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the self drawing elements of the single-headed arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the self drawing elements of the single-headed arc.\"\"\"\n    drawing_elements = (\n        self.path_drawing_elements() + self.arrowhead_drawing_elements()\n    )\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ModulationLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ModulationLayout.path_drawing_elements","title":"path_drawing_elements","text":"<pre><code>path_drawing_elements() -&gt; list[Path]\n</code></pre> <p>Return the drawing elements of the single-headed arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def path_drawing_elements(self) -&gt; list[Path]:\n    \"\"\"Return the drawing elements of the single-headed arc path.\"\"\"\n    arrowhead_length = self.arrowhead_length()\n    if len(self.segments) == 1:\n        segment = (\n            self.segments[0]\n            .shortened(self.start_shorten, \"start\")\n            .shortened(self.end_shorten + arrowhead_length, \"end\")\n        )\n        actions = [\n            MoveTo(segment.p1),\n            self._make_path_action_from_segment(segment),\n        ]\n    else:\n        first_segment = self.segments[0].shortened(self.start_shorten, \"start\")\n        last_segment = self.segments[-1].shortened(\n            self.end_shorten + arrowhead_length, \"end\"\n        )\n        actions = [\n            MoveTo(first_segment.p1),\n            self._make_path_action_from_segment(first_segment),\n        ]\n        for segment in self.segments[1:-1]:\n            action = self._make_path_action_from_segment(segment)\n            actions.append(action)\n        actions.append(self._make_path_action_from_segment(last_segment))\n    path = Path(\n        actions=tuple(actions),\n        class_=f\"{type(self).__name__}_path\",\n        fill=self.path_fill,\n        filter_=self.path_filter,\n        id_=f\"{self.id_}_path\",\n        stroke=self.path_stroke,\n        stroke_dasharray=self.path_stroke_dasharray,\n        stroke_dashoffset=self.path_stroke_dashoffset,\n        stroke_width=self.path_stroke_width,\n        transform=self.path_transform,\n    )\n    return [path]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ModulationLayout.points","title":"points","text":"<pre><code>points() -&gt; list[Point]\n</code></pre> <p>Return the points of the arc path.</p> <p>An arc with no segments has no points and returns an empty list.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def points(self) -&gt; list[Point]:\n    \"\"\"Return the points of the arc path.\n\n    An arc with no segments has no points and returns an empty list.\n    \"\"\"\n    if not self.segments:\n        return []\n    points = []\n    for segment in self.segments:\n        points.append(segment.p1)\n    points.append(segment.p2)\n    return points\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ModulationLayout.start_point","title":"start_point","text":"<pre><code>start_point() -&gt; Point\n</code></pre> <p>Return the starting point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_point(self) -&gt; Point:\n    \"\"\"Return the starting point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[0]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ModulationLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.Multimer","title":"Multimer  <code>dataclass</code>","text":"<pre><code>Multimer(*, id_: str = make_uuid4_as_str(), compartment: Compartment | None = None, label: str | None = None, state_variables: frozenset[StateVariable] = frozenset(), units_of_information: frozenset[UnitOfInformation] = frozenset(), subunits: frozenset[Subunit] = frozenset(), cardinality: int | None = None)\n</code></pre> <p>               Bases: <code>Complex</code></p> <p>Abstract base class for multimers.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'53171c71-84d5-4606-a5a2-1a0a5579f4c5'</code> <code>compartment</code> <code>Compartment | None</code> <p>The compartment of the entity pool</p> <code>None</code> <code>label</code> <code>str | None</code> <p>The label of the complex</p> <code>None</code> <code>state_variables</code> <code>frozenset[StateVariable]</code> <p>The state variables of the complex</p> <code>&lt;dynamic&gt;</code> <code>units_of_information</code> <code>frozenset[UnitOfInformation]</code> <p>The units of information of the complex</p> <code>&lt;dynamic&gt;</code> <code>subunits</code> <code>frozenset[Subunit]</code> <p>The subunits of the complex</p> <code>&lt;dynamic&gt;</code> <code>cardinality</code> <code>int | None</code> <p>The cardinality of the multimer</p> <code>None</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.Multimer.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MultimerSubunit","title":"MultimerSubunit  <code>dataclass</code>","text":"<pre><code>MultimerSubunit(*, id_: str = make_uuid4_as_str(), label: str | None = None, state_variables: frozenset[StateVariable] = frozenset(), units_of_information: frozenset[UnitOfInformation] = frozenset(), subunits: frozenset[Subunit] = frozenset(), cardinality: int | None = None)\n</code></pre> <p>               Bases: <code>ComplexSubunit</code></p> <p>Abstract base class for multimer subunits.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'473f0331-d066-4368-9d32-66ce3fe8ef4d'</code> <code>label</code> <code>str | None</code> <p>The label of the subunit</p> <code>None</code> <code>state_variables</code> <code>frozenset[StateVariable]</code> <p>The state variables of the complex subunit</p> <code>&lt;dynamic&gt;</code> <code>units_of_information</code> <code>frozenset[UnitOfInformation]</code> <p>The units of information of the complex subunit</p> <code>&lt;dynamic&gt;</code> <code>subunits</code> <code>frozenset[Subunit]</code> <p>The subunits of the complex subunit</p> <code>&lt;dynamic&gt;</code> <code>cardinality</code> <code>int | None</code> <p>The cardinality of the multimer subunit</p> <code>None</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.MultimerSubunit.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NecessaryStimulation","title":"NecessaryStimulation  <code>dataclass</code>","text":"<pre><code>NecessaryStimulation(*, id_: str = make_uuid4_as_str(), source: EntityPool | LogicalOperator, target: Process)\n</code></pre> <p>               Bases: <code>Stimulation</code></p> <p>Necessary stimulation.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'7c327fb5-bbd4-4c9e-b388-9f487fc6d35b'</code> <code>source</code> <code>EntityPool | LogicalOperator</code> <p>The source of the modulation</p> required <code>target</code> <code>Process</code> <p>The target of the modulation</p> required <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NecessaryStimulation.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NecessaryStimulationLayout","title":"NecessaryStimulationLayout  <code>dataclass</code>","text":"<pre><code>NecessaryStimulationLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, end_shorten: float = 0.0, fill: NoneValueType | Color | None = None, filter_: NoneValueType | Filter | None = None, path_fill: NoneValueType | Color | None = NoneValue, path_filter: NoneValueType | Filter | None = None, path_stroke: NoneValueType | Color | None = black, path_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, path_stroke_dashoffset: NoneValueType | float | None = None, path_stroke_width: float | None = 1.25, path_transform: NoneValueType | tuple[Transformation, ...] | None = None, stroke: NoneValueType | Color | None = None, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: NoneValueType | float | None = None, segments: tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...] = tuple(), source: LayoutElement | None = None, start_shorten: float = 0.0, target: LayoutElement | None = None, transform: NoneValueType | tuple[Transformation, ...] | None = None, arrowhead_fill: NoneValueType | Color | None = white, arrowhead_filter: NoneValueType | Filter | None = None, arrowhead_stroke: NoneValueType | Color | None = black, arrowhead_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, arrowhead_stroke_dashoffset: NoneValueType | float | None = None, arrowhead_stroke_width: float | None = 1.25, arrowhead_transform: NoneValueType | tuple[Transformation, ...] | None = None, arrowhead_bar_height: float = 12.0, arrowhead_sep: float = 3.0, arrowhead_triangle_height: float = 10.0, arrowhead_triangle_width: float = 10.0)\n</code></pre> <p>               Bases: <code>SBGNSingleHeadedArc</code></p> <p>Necessary stimulation layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'b306908d-fdc9-4d0d-b30e-d6a9de2df413'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>end_shorten</code> <code>float</code> <p>The length the end of the arc will be shorten by</p> <code>0.0</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the arc</p> <code>None</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the arc</p> <code>None</code> <code>path_fill</code> <code>NoneValueType | Color | None</code> <p>The path fill color of the arc</p> <code>&lt;momapy.drawing.NoneValueType object at 0x7f8b17323e00&gt;</code> <code>path_filter</code> <code>NoneValueType | Filter | None</code> <p>The path filter of the arc</p> <code>None</code> <code>path_stroke</code> <code>NoneValueType | Color | None</code> <p>The path stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>path_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The path stroke dasharray of the arc</p> <code>None</code> <code>path_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The path stroke dashoffset of the arc</p> <code>None</code> <code>path_stroke_width</code> <code>float | None</code> <p>The path stroke width of the arc</p> <code>1.25</code> <code>path_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The path transform of the arc</p> <code>None</code> <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the arc</p> <code>None</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the arc</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the arc</p> <code>None</code> <code>stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the arc</p> <code>None</code> <code>segments</code> <code>tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...]</code> <p>The path segments of the arc</p> <code>&lt;dynamic&gt;</code> <code>source</code> <code>LayoutElement | None</code> <p>The source of the arc</p> <code>None</code> <code>start_shorten</code> <code>float</code> <p>The length the start of the arc will be shorten by</p> <code>0.0</code> <code>target</code> <code>LayoutElement | None</code> <p>The target of the arc</p> <code>None</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the arc</p> <code>None</code> <code>arrowhead_fill</code> <code>NoneValueType | Color | None</code> <p>The arrowhead fill color of the arc</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>arrowhead_filter</code> <code>NoneValueType | Filter | None</code> <p>The arrowhead filter of the arc</p> <code>None</code> <code>arrowhead_stroke</code> <code>NoneValueType | Color | None</code> <p>The arrowhead stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>arrowhead_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The arrowhead stroke dasharray of the arc</p> <code>None</code> <code>arrowhead_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The arrowhead stroke dashoffset of the arc</p> <code>None</code> <code>arrowhead_stroke_width</code> <code>float | None</code> <p>The arrowhead stroke width of the arc</p> <code>1.25</code> <code>arrowhead_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The arrowhead transform of the arc</p> <code>None</code> <code>arrowhead_bar_height</code> <code>float</code> <p>The height of the bar of the arrowhead</p> <code>12.0</code> <code>arrowhead_sep</code> <code>float</code> <p>The separation between the bar and the triangle</p> <code>3.0</code> <code>arrowhead_triangle_height</code> <code>float</code> <p>The height of the triangle of the arrowhead</p> <code>10.0</code> <code>arrowhead_triangle_width</code> <code>float</code> <p>The width of the triangle of the arrowhead</p> <code>10.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>arrowhead_base</code> <p>Return the arrowhead base anchor point of the single-headed arc.</p> <code>arrowhead_bbox</code> <p>Return the bounding box of the single-headed arc arrowhead.</p> <code>arrowhead_border</code> <p>Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.</p> <code>arrowhead_drawing_elements</code> <p>Return the drawing elements of the single-headed arc arrowhead.</p> <code>arrowhead_length</code> <p>Return the length of the single-headed arc arrowhead.</p> <code>arrowhead_tip</code> <p>Return the arrowhead tip anchor point of the single-headed arc.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>childless</code> <p>Return a copy of the arc with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>end_point</code> <p>Return the ending point of the arc.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>fraction</code> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <code>length</code> <p>Return the total length of the arc path.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_children</code> <p>Return the self children of the arc.</p> <code>own_drawing_elements</code> <p>Return the self drawing elements of the single-headed arc.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>path_drawing_elements</code> <p>Return the drawing elements of the single-headed arc path.</p> <code>points</code> <p>Return the points of the arc path.</p> <code>start_point</code> <p>Return the starting point of the arc.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NecessaryStimulationLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NecessaryStimulationLayout.arrowhead_base","title":"arrowhead_base","text":"<pre><code>arrowhead_base() -&gt; Point\n</code></pre> <p>Return the arrowhead base anchor point of the single-headed arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_base(self) -&gt; Point:\n    \"\"\"Return the arrowhead base anchor point of the single-headed arc.\"\"\"\n    arrowhead_length = self.arrowhead_length()\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return self.arrowhead_tip() - (arrowhead_length, 0)\n    fraction = 1 - (arrowhead_length + self.end_shorten) / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NecessaryStimulationLayout.arrowhead_bbox","title":"arrowhead_bbox","text":"<pre><code>arrowhead_bbox() -&gt; Bbox\n</code></pre> <p>Return the bounding box of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_bbox(self) -&gt; Bbox:\n    \"\"\"Return the bounding box of the single-headed arc arrowhead.\"\"\"\n    return get_drawing_elements_bbox(self.arrowhead_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NecessaryStimulationLayout.arrowhead_border","title":"arrowhead_border","text":"<pre><code>arrowhead_border(point: Point) -&gt; Point\n</code></pre> <p>Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_border(self, point: Point) -&gt; Point:\n    \"\"\"Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    point = get_drawing_elements_border(self.arrowhead_drawing_elements(), point)\n    if point is None:\n        return self.arrowhead_tip()\n    return point\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NecessaryStimulationLayout.arrowhead_drawing_elements","title":"arrowhead_drawing_elements","text":"<pre><code>arrowhead_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_drawing_elements(\n    self,\n) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the single-headed arc arrowhead.\"\"\"\n    drawing_elements = self._arrowhead_border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_arrowhead\",\n        elements=tuple(drawing_elements),\n        fill=self.arrowhead_fill,\n        filter_=self.arrowhead_filter,\n        id_=f\"{self.id_}_arrowhead\",\n        stroke=self.arrowhead_stroke,\n        stroke_dasharray=self.arrowhead_stroke_dasharray,\n        stroke_dashoffset=self.arrowhead_stroke_dashoffset,\n        stroke_width=self.arrowhead_stroke_width,\n        transform=self.arrowhead_transform,\n    )\n    transformation = self._get_arrowhead_transformation()\n    group = group.transformed(transformation)\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NecessaryStimulationLayout.arrowhead_length","title":"arrowhead_length","text":"<pre><code>arrowhead_length() -&gt; float\n</code></pre> <p>Return the length of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_length(self) -&gt; float:\n    \"\"\"Return the length of the single-headed arc arrowhead.\"\"\"\n    bbox = get_drawing_elements_bbox(self._arrowhead_border_drawing_elements())\n    if math.isnan(bbox.width):\n        return 0.0\n    return bbox.east().x\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NecessaryStimulationLayout.arrowhead_tip","title":"arrowhead_tip","text":"<pre><code>arrowhead_tip() -&gt; Point\n</code></pre> <p>Return the arrowhead tip anchor point of the single-headed arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_tip(self) -&gt; Point:\n    \"\"\"Return the arrowhead tip anchor point of the single-headed arc.\"\"\"\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return segment.p2\n    fraction = 1 - self.end_shorten / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NecessaryStimulationLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NecessaryStimulationLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the arc with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the arc with no children.\"\"\"\n    return dataclasses.replace(self, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NecessaryStimulationLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NecessaryStimulationLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NecessaryStimulationLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NecessaryStimulationLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NecessaryStimulationLayout.end_point","title":"end_point","text":"<pre><code>end_point() -&gt; Point\n</code></pre> <p>Return the ending point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_point(self) -&gt; Point:\n    \"\"\"Return the ending point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[-1]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NecessaryStimulationLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NecessaryStimulationLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NecessaryStimulationLayout.fraction","title":"fraction","text":"<pre><code>fraction(fraction: float) -&gt; tuple[Point, float]\n</code></pre> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def fraction(self, fraction: float) -&gt; tuple[Point, float]:\n    \"\"\"Return the position and angle on the arc at a given fraction (of the total arc length).\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    current_length = 0\n    length_to_reach = fraction * self.length()\n    for segment in self.segments:\n        current_length += segment.length()\n        if current_length &gt;= length_to_reach:\n            break\n    segment_start_length = current_length - segment.length()\n    segment_fraction = (length_to_reach - segment_start_length) / segment.length()\n    position, angle = segment.get_position_and_angle_at_fraction(segment_fraction)\n    return position, angle\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NecessaryStimulationLayout.length","title":"length","text":"<pre><code>length() -&gt; float\n</code></pre> <p>Return the total length of the arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def length(self) -&gt; float:\n    \"\"\"Return the total length of the arc path.\"\"\"\n    return sum([segment.length() for segment in self.segments])\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NecessaryStimulationLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NecessaryStimulationLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the arc.\"\"\"\n    return []\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NecessaryStimulationLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the self drawing elements of the single-headed arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the self drawing elements of the single-headed arc.\"\"\"\n    drawing_elements = (\n        self.path_drawing_elements() + self.arrowhead_drawing_elements()\n    )\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NecessaryStimulationLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NecessaryStimulationLayout.path_drawing_elements","title":"path_drawing_elements","text":"<pre><code>path_drawing_elements() -&gt; list[Path]\n</code></pre> <p>Return the drawing elements of the single-headed arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def path_drawing_elements(self) -&gt; list[Path]:\n    \"\"\"Return the drawing elements of the single-headed arc path.\"\"\"\n    arrowhead_length = self.arrowhead_length()\n    if len(self.segments) == 1:\n        segment = (\n            self.segments[0]\n            .shortened(self.start_shorten, \"start\")\n            .shortened(self.end_shorten + arrowhead_length, \"end\")\n        )\n        actions = [\n            MoveTo(segment.p1),\n            self._make_path_action_from_segment(segment),\n        ]\n    else:\n        first_segment = self.segments[0].shortened(self.start_shorten, \"start\")\n        last_segment = self.segments[-1].shortened(\n            self.end_shorten + arrowhead_length, \"end\"\n        )\n        actions = [\n            MoveTo(first_segment.p1),\n            self._make_path_action_from_segment(first_segment),\n        ]\n        for segment in self.segments[1:-1]:\n            action = self._make_path_action_from_segment(segment)\n            actions.append(action)\n        actions.append(self._make_path_action_from_segment(last_segment))\n    path = Path(\n        actions=tuple(actions),\n        class_=f\"{type(self).__name__}_path\",\n        fill=self.path_fill,\n        filter_=self.path_filter,\n        id_=f\"{self.id_}_path\",\n        stroke=self.path_stroke,\n        stroke_dasharray=self.path_stroke_dasharray,\n        stroke_dashoffset=self.path_stroke_dashoffset,\n        stroke_width=self.path_stroke_width,\n        transform=self.path_transform,\n    )\n    return [path]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NecessaryStimulationLayout.points","title":"points","text":"<pre><code>points() -&gt; list[Point]\n</code></pre> <p>Return the points of the arc path.</p> <p>An arc with no segments has no points and returns an empty list.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def points(self) -&gt; list[Point]:\n    \"\"\"Return the points of the arc path.\n\n    An arc with no segments has no points and returns an empty list.\n    \"\"\"\n    if not self.segments:\n        return []\n    points = []\n    for segment in self.segments:\n        points.append(segment.p1)\n    points.append(segment.p2)\n    return points\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NecessaryStimulationLayout.start_point","title":"start_point","text":"<pre><code>start_point() -&gt; Point\n</code></pre> <p>Return the starting point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_point(self) -&gt; Point:\n    \"\"\"Return the starting point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[0]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NecessaryStimulationLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NotOperator","title":"NotOperator  <code>dataclass</code>","text":"<pre><code>NotOperator(*, id_: str = make_uuid4_as_str(), inputs: frozenset[LogicalOperatorInput] = frozenset())\n</code></pre> <p>               Bases: <code>LogicalOperator</code></p> <p>NOT operator.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'12fffc31-212f-45f6-8311-5eb02b7c6b59'</code> <code>inputs</code> <code>frozenset[LogicalOperatorInput]</code> <p>The inputs of the logical operator</p> <code>&lt;dynamic&gt;</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NotOperator.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NotOperatorLayout","title":"NotOperatorLayout  <code>dataclass</code>","text":"<pre><code>NotOperatorLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = white, filter_: NoneValueType | Filter | None = None, height: float = 30.0, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = 1.25, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 30.0, text: str = 'NOT', font_family: str = DEFAULT_FONT_FAMILY, font_fill: Color | NoneValueType = black, font_stroke: Color | NoneValueType = NoneValue, font_style: FontStyle = NORMAL, font_weight: FontWeight | float = NORMAL, orientation: Orientation = HORIZONTAL, left_to_right: bool = True, left_connector_length: float = 10.0, right_connector_length: float = 10.0, left_connector_stroke: NoneValueType | Color | None = None, left_connector_stroke_width: float | None = None, left_connector_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, left_connector_stroke_dashoffset: float | None = None, left_connector_fill: NoneValueType | Color | None = None, left_connector_transform: NoneValueType | tuple[Transformation, ...] | None = None, left_connector_filter: NoneValueType | Filter | None = None, right_connector_stroke: NoneValueType | Color | None = None, right_connector_stroke_width: float | None = None, right_connector_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, right_connector_stroke_dashoffset: float | None = None, right_connector_fill: NoneValueType | Color | None = None, right_connector_transform: NoneValueType | tuple[Transformation, ...] | None = None, right_connector_filter: NoneValueType | Filter | None = None)\n</code></pre> <p>               Bases: <code>_ConnectorsMixin</code>, <code>_SimpleMixin</code>, <code>_TextMixin</code>, <code>SBGNNode</code></p> <p>NOT operator layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'f45918ca-3af1-4974-9631-d5cd925a3b90'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>30.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The stroke width of the node</p> <code>1.25</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>30.0</code> <code>text</code> <code>str</code> <p>The text drawn inside the operator glyph</p> <code>'NOT'</code> <code>font_family</code> <code>str</code> <p>Font family for the text.</p> <code>'DejaVu Sans'</code> <code>font_fill</code> <code>Color | NoneValueType</code> <p>Fill color for the text.</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>font_stroke</code> <code>Color | NoneValueType</code> <p>Stroke color for the text outline.</p> <code>&lt;momapy.drawing.NoneValueType object at 0x7f8b17323e00&gt;</code> <code>font_style</code> <code>FontStyle</code> <p>Font style (normal, italic, etc.).</p> <code>&lt;FontStyle.NORMAL: 0&gt;</code> <code>font_weight</code> <code>FontWeight | float</code> <p>Font weight (normal, bold, etc.).</p> <code>&lt;FontWeight.NORMAL: 0&gt;</code> <code>orientation</code> <code>Orientation</code> <p>Orientation of connectors (HORIZONTAL or VERTICAL).</p> <code>&lt;Orientation.HORIZONTAL: 1&gt;</code> <code>left_to_right</code> <code>bool</code> <p>Whether connectors flow left to right.</p> <code>True</code> <code>left_connector_length</code> <code>float</code> <p>Length of the left connector.</p> <code>10.0</code> <code>right_connector_length</code> <code>float</code> <p>Length of the right connector.</p> <code>10.0</code> <code>left_connector_stroke</code> <code>NoneValueType | Color | None</code> <p>Stroke color for the left connector line.</p> <code>None</code> <code>left_connector_stroke_width</code> <code>float | None</code> <p>Stroke width for the left connector line.</p> <code>None</code> <code>left_connector_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>Dash pattern for the left connector line.</p> <code>None</code> <code>left_connector_stroke_dashoffset</code> <code>float | None</code> <p>Dash offset for the left connector line.</p> <code>None</code> <code>left_connector_fill</code> <code>NoneValueType | Color | None</code> <p>Fill color for the left connector.</p> <code>None</code> <code>left_connector_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>Transformations applied to the left connector.</p> <code>None</code> <code>left_connector_filter</code> <code>NoneValueType | Filter | None</code> <p>Filter applied to the left connector.</p> <code>None</code> <code>right_connector_stroke</code> <code>NoneValueType | Color | None</code> <p>Stroke color for the right connector line.</p> <code>None</code> <code>right_connector_stroke_width</code> <code>float | None</code> <p>Stroke width for the right connector line.</p> <code>None</code> <code>right_connector_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>Dash pattern for the right connector line.</p> <code>None</code> <code>right_connector_stroke_dashoffset</code> <code>float | None</code> <p>Dash offset for the right connector line.</p> <code>None</code> <code>right_connector_fill</code> <code>NoneValueType | Color | None</code> <p>Fill color for the right connector.</p> <code>None</code> <code>right_connector_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>Transformations applied to the right connector.</p> <code>None</code> <code>right_connector_filter</code> <code>NoneValueType | Filter | None</code> <p>Filter applied to the right connector.</p> <code>None</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>angle</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Get the east (right) anchor point.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Return the label center anchor of the node.</p> <code>left_connector_base</code> <p>Get the base point of the left connector.</p> <code>left_connector_tip</code> <p>Get the tip point of the left connector.</p> <code>north</code> <p>Get the north (top) anchor point.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>right_connector_base</code> <p>Get the base point of the right connector.</p> <code>right_connector_tip</code> <p>Get the tip point of the right connector.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Get the south (bottom) anchor point.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Get the west (left) anchor point.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NotOperatorLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NotOperatorLayout.angle","title":"angle","text":"<pre><code>angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NotOperatorLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NotOperatorLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NotOperatorLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NotOperatorLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NotOperatorLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NotOperatorLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NotOperatorLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NotOperatorLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NotOperatorLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Get the east (right) anchor point.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point on the east side of the element.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Get the east (right) anchor point.\n\n    Returns:\n        Point on the east side of the element.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x + self.width / 2, self.y)\n    else:\n        return Point(self.x + self.width / 2 + self.right_connector_length, self.y)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NotOperatorLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NotOperatorLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NotOperatorLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NotOperatorLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NotOperatorLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Return the label center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Return the label center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NotOperatorLayout.left_connector_base","title":"left_connector_base","text":"<pre><code>left_connector_base() -&gt; Point\n</code></pre> <p>Get the base point of the left connector.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point where the left connector attaches to the shape.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def left_connector_base(self) -&gt; Point:\n    \"\"\"Get the base point of the left connector.\n\n    Returns:\n        Point where the left connector attaches to the shape.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x, self.y - self.height / 2)\n    else:\n        return Point(self.x - self.width / 2, self.y)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NotOperatorLayout.left_connector_tip","title":"left_connector_tip","text":"<pre><code>left_connector_tip() -&gt; Point\n</code></pre> <p>Get the tip point of the left connector.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point at the end of the left connector line.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def left_connector_tip(self) -&gt; Point:\n    \"\"\"Get the tip point of the left connector.\n\n    Returns:\n        Point at the end of the left connector line.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x, self.y - self.height / 2 - self.left_connector_length)\n    else:\n        return Point(self.x - self.width / 2 - self.left_connector_length, self.y)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NotOperatorLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Get the north (top) anchor point.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point on the north side of the element.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Get the north (top) anchor point.\n\n    Returns:\n        Point on the north side of the element.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x, self.y - self.height / 2 - self.left_connector_length)\n    else:\n        return Point(self.x, self.y - self.height / 2)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NotOperatorLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NotOperatorLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NotOperatorLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NotOperatorLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NotOperatorLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NotOperatorLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NotOperatorLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NotOperatorLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NotOperatorLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NotOperatorLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NotOperatorLayout.right_connector_base","title":"right_connector_base","text":"<pre><code>right_connector_base() -&gt; Point\n</code></pre> <p>Get the base point of the right connector.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point where the right connector attaches to the shape.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def right_connector_base(self) -&gt; Point:\n    \"\"\"Get the base point of the right connector.\n\n    Returns:\n        Point where the right connector attaches to the shape.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x, self.y + self.height / 2)\n    else:\n        return Point(self.x + self.width / 2, self.y)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NotOperatorLayout.right_connector_tip","title":"right_connector_tip","text":"<pre><code>right_connector_tip() -&gt; Point\n</code></pre> <p>Get the tip point of the right connector.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point at the end of the right connector line.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def right_connector_tip(self) -&gt; Point:\n    \"\"\"Get the tip point of the right connector.\n\n    Returns:\n        Point at the end of the right connector line.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x, self.y + self.height / 2 + self.right_connector_length)\n    else:\n        return Point(self.x + self.width / 2 + self.right_connector_length, self.y)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NotOperatorLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NotOperatorLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Get the south (bottom) anchor point.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point on the south side of the element.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Get the south (bottom) anchor point.\n\n    Returns:\n        Point on the south side of the element.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x, self.y + self.height / 2 + self.right_connector_length)\n    else:\n        return Point(self.x, self.y + self.height / 2)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NotOperatorLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NotOperatorLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NotOperatorLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NotOperatorLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NotOperatorLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NotOperatorLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Get the west (left) anchor point.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point on the west side of the element.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Get the west (left) anchor point.\n\n    Returns:\n        Point on the west side of the element.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x - self.width / 2, self.y)\n    else:\n        return Point(self.x - self.width / 2 - self.left_connector_length, self.y)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NotOperatorLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NotOperatorLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NotOperatorLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NotOperatorLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeature","title":"NucleicAcidFeature  <code>dataclass</code>","text":"<pre><code>NucleicAcidFeature(*, id_: str = make_uuid4_as_str(), compartment: Compartment | None = None, label: str | None = None, state_variables: frozenset[StateVariable] = frozenset(), units_of_information: frozenset[UnitOfInformation] = frozenset())\n</code></pre> <p>               Bases: <code>EntityPool</code></p> <p>Nucleic acid feature.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'af4532f5-de47-40a1-b080-8c4ac9de2cd4'</code> <code>compartment</code> <code>Compartment | None</code> <p>The compartment of the entity pool</p> <code>None</code> <code>label</code> <code>str | None</code> <p>The label of the nucleic acid feature</p> <code>None</code> <code>state_variables</code> <code>frozenset[StateVariable]</code> <p>The state variables of the nucleic acid feature</p> <code>&lt;dynamic&gt;</code> <code>units_of_information</code> <code>frozenset[UnitOfInformation]</code> <p>The units of information of the nucleic acid feature</p> <code>&lt;dynamic&gt;</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeature.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureLayout","title":"NucleicAcidFeatureLayout  <code>dataclass</code>","text":"<pre><code>NucleicAcidFeatureLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = white, filter_: NoneValueType | Filter | None = None, height: float = 30.0, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = 1.25, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 60.0, rounded_corners: float = 5.0)\n</code></pre> <p>               Bases: <code>_SimpleMixin</code>, <code>SBGNNode</code></p> <p>Nucleic acid feature layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'695a6020-c206-462b-8192-f844d93b9f1a'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>30.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The stroke width of the node</p> <code>1.25</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>60.0</code> <code>rounded_corners</code> <code>float</code> <p>The corner radius of the nucleic acid feature layout</p> <code>5.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>angle</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Return the east anchor of the node.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Return the label center anchor of the node.</p> <code>north</code> <p>Return the north anchor of the node.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Return the south anchor of the node.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Return the west anchor of the node.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureLayout.angle","title":"angle","text":"<pre><code>angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Return the east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Return the east anchor of the node.\"\"\"\n    return self.own_angle(0)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Return the label center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Return the label center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Return the north anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Return the north anchor of the node.\"\"\"\n    return self.own_angle(90)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Return the south anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Return the south anchor of the node.\"\"\"\n    return self.own_angle(270)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Return the west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Return the west anchor of the node.\"\"\"\n    return self.own_angle(180)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureMultimer","title":"NucleicAcidFeatureMultimer  <code>dataclass</code>","text":"<pre><code>NucleicAcidFeatureMultimer(*, id_: str = make_uuid4_as_str(), compartment: Compartment | None = None, label: str | None = None, state_variables: frozenset[StateVariable] = frozenset(), units_of_information: frozenset[UnitOfInformation] = frozenset(), subunits: frozenset[Subunit] = frozenset(), cardinality: int | None = None)\n</code></pre> <p>               Bases: <code>Multimer</code></p> <p>Nucleic acid feature multimer.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'98e588ee-629a-4417-8fe2-5eacb47ba7b1'</code> <code>compartment</code> <code>Compartment | None</code> <p>The compartment of the entity pool</p> <code>None</code> <code>label</code> <code>str | None</code> <p>The label of the complex</p> <code>None</code> <code>state_variables</code> <code>frozenset[StateVariable]</code> <p>The state variables of the complex</p> <code>&lt;dynamic&gt;</code> <code>units_of_information</code> <code>frozenset[UnitOfInformation]</code> <p>The units of information of the complex</p> <code>&lt;dynamic&gt;</code> <code>subunits</code> <code>frozenset[Subunit]</code> <p>The subunits of the complex</p> <code>&lt;dynamic&gt;</code> <code>cardinality</code> <code>int | None</code> <p>The cardinality of the multimer</p> <code>None</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureMultimer.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureMultimerLayout","title":"NucleicAcidFeatureMultimerLayout  <code>dataclass</code>","text":"<pre><code>NucleicAcidFeatureMultimerLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = white, filter_: NoneValueType | Filter | None = None, height: float = 30.0, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = 1.25, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 60.0, offset: float = 3.0, subunits_stroke: tuple[NoneValueType | Color, ...] | None = None, subunits_stroke_width: tuple[NoneValueType | float, ...] | None = None, subunits_stroke_dasharray: tuple[NoneValueType | tuple[float, ...], ...] | None = None, subunits_stroke_dashoffset: tuple[float, ...] | None = None, subunits_fill: tuple[NoneValueType | Color, ...] | None = None, subunits_transform: tuple[NoneValueType | tuple[Transformation, ...], ...] | None = None, subunits_filter: tuple[NoneValueType | Filter, ...] | None = None, rounded_corners: float = 5.0)\n</code></pre> <p>               Bases: <code>_MultiMixin</code>, <code>SBGNNode</code></p> <p>Nucleic acid feature multimer layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'adfe07d4-3771-4501-a03c-9d8a6e3da50d'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>30.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The stroke width of the node</p> <code>1.25</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>60.0</code> <code>offset</code> <code>float</code> <p>Offset distance between stacked units.</p> <code>3.0</code> <code>subunits_stroke</code> <code>tuple[NoneValueType | Color, ...] | None</code> <p>Tuple of stroke colors for each subunit.</p> <code>None</code> <code>subunits_stroke_width</code> <code>tuple[NoneValueType | float, ...] | None</code> <p>Tuple of stroke widths for each subunit.</p> <code>None</code> <code>subunits_stroke_dasharray</code> <code>tuple[NoneValueType | tuple[float, ...], ...] | None</code> <p>Tuple of dash patterns for each subunit.</p> <code>None</code> <code>subunits_stroke_dashoffset</code> <code>tuple[float, ...] | None</code> <p>Tuple of dash offsets for each subunit.</p> <code>None</code> <code>subunits_fill</code> <code>tuple[NoneValueType | Color, ...] | None</code> <p>Tuple of fill colors for each subunit.</p> <code>None</code> <code>subunits_transform</code> <code>tuple[NoneValueType | tuple[Transformation, ...], ...] | None</code> <p>Tuple of transformations for each subunit.</p> <code>None</code> <code>subunits_filter</code> <code>tuple[NoneValueType | Filter, ...] | None</code> <p>Tuple of filters for each subunit.</p> <code>None</code> <code>rounded_corners</code> <code>float</code> <p>The corner radius of the nucleic acid feature multimer layout</p> <code>5.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>angle</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Return the east anchor of the node.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Get the center position for the node label.</p> <code>north</code> <p>Return the north anchor of the node.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Return the south anchor of the node.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Return the west anchor of the node.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureMultimerLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureMultimerLayout.angle","title":"angle","text":"<pre><code>angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureMultimerLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureMultimerLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureMultimerLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureMultimerLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureMultimerLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureMultimerLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureMultimerLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureMultimerLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureMultimerLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Return the east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Return the east anchor of the node.\"\"\"\n    return self.own_angle(0)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureMultimerLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureMultimerLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureMultimerLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureMultimerLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureMultimerLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Get the center position for the node label.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point where the label should be centered.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Get the center position for the node label.\n\n    Returns:\n        Point where the label should be centered.\n    \"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2 - (self._n - 1) * self.offset,\n        self.height / 2 - height / 2 - (self._n - 1) * self.offset,\n    )\n    return self._make_subunit_shape(\n        position=position, width=width, height=height\n    ).position\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureMultimerLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Return the north anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Return the north anchor of the node.\"\"\"\n    return self.own_angle(90)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureMultimerLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureMultimerLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureMultimerLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureMultimerLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureMultimerLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureMultimerLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureMultimerLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureMultimerLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureMultimerLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureMultimerLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureMultimerLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureMultimerLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Return the south anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Return the south anchor of the node.\"\"\"\n    return self.own_angle(270)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureMultimerLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureMultimerLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureMultimerLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureMultimerLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureMultimerLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureMultimerLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Return the west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Return the west anchor of the node.\"\"\"\n    return self.own_angle(180)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureMultimerLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureMultimerLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureMultimerLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureMultimerLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureMultimerSubunit","title":"NucleicAcidFeatureMultimerSubunit  <code>dataclass</code>","text":"<pre><code>NucleicAcidFeatureMultimerSubunit(*, id_: str = make_uuid4_as_str(), label: str | None = None, state_variables: frozenset[StateVariable] = frozenset(), units_of_information: frozenset[UnitOfInformation] = frozenset(), subunits: frozenset[Subunit] = frozenset(), cardinality: int | None = None)\n</code></pre> <p>               Bases: <code>MultimerSubunit</code></p> <p>Nucleic acid feature multimer subunit.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'0253f419-a4a6-4fa2-bae2-2e85dff56d72'</code> <code>label</code> <code>str | None</code> <p>The label of the subunit</p> <code>None</code> <code>state_variables</code> <code>frozenset[StateVariable]</code> <p>The state variables of the complex subunit</p> <code>&lt;dynamic&gt;</code> <code>units_of_information</code> <code>frozenset[UnitOfInformation]</code> <p>The units of information of the complex subunit</p> <code>&lt;dynamic&gt;</code> <code>subunits</code> <code>frozenset[Subunit]</code> <p>The subunits of the complex subunit</p> <code>&lt;dynamic&gt;</code> <code>cardinality</code> <code>int | None</code> <p>The cardinality of the multimer subunit</p> <code>None</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureMultimerSubunit.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureMultimerSubunitLayout","title":"NucleicAcidFeatureMultimerSubunitLayout  <code>dataclass</code>","text":"<pre><code>NucleicAcidFeatureMultimerSubunitLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = white, filter_: NoneValueType | Filter | None = None, height: float = 30.0, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = 1.25, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 60.0, offset: float = 3.0, subunits_stroke: tuple[NoneValueType | Color, ...] | None = None, subunits_stroke_width: tuple[NoneValueType | float, ...] | None = None, subunits_stroke_dasharray: tuple[NoneValueType | tuple[float, ...], ...] | None = None, subunits_stroke_dashoffset: tuple[float, ...] | None = None, subunits_fill: tuple[NoneValueType | Color, ...] | None = None, subunits_transform: tuple[NoneValueType | tuple[Transformation, ...], ...] | None = None, subunits_filter: tuple[NoneValueType | Filter, ...] | None = None, rounded_corners: float = 5.0)\n</code></pre> <p>               Bases: <code>_MultiMixin</code>, <code>SBGNNode</code></p> <p>Nucleic acid feature multimer subunit layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'82097416-4f2b-4960-925a-05395aeaa846'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>30.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The stroke width of the node</p> <code>1.25</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>60.0</code> <code>offset</code> <code>float</code> <p>Offset distance between stacked units.</p> <code>3.0</code> <code>subunits_stroke</code> <code>tuple[NoneValueType | Color, ...] | None</code> <p>Tuple of stroke colors for each subunit.</p> <code>None</code> <code>subunits_stroke_width</code> <code>tuple[NoneValueType | float, ...] | None</code> <p>Tuple of stroke widths for each subunit.</p> <code>None</code> <code>subunits_stroke_dasharray</code> <code>tuple[NoneValueType | tuple[float, ...], ...] | None</code> <p>Tuple of dash patterns for each subunit.</p> <code>None</code> <code>subunits_stroke_dashoffset</code> <code>tuple[float, ...] | None</code> <p>Tuple of dash offsets for each subunit.</p> <code>None</code> <code>subunits_fill</code> <code>tuple[NoneValueType | Color, ...] | None</code> <p>Tuple of fill colors for each subunit.</p> <code>None</code> <code>subunits_transform</code> <code>tuple[NoneValueType | tuple[Transformation, ...], ...] | None</code> <p>Tuple of transformations for each subunit.</p> <code>None</code> <code>subunits_filter</code> <code>tuple[NoneValueType | Filter, ...] | None</code> <p>Tuple of filters for each subunit.</p> <code>None</code> <code>rounded_corners</code> <code>float</code> <p>The corner radius of the nucleic acid feature multimer subunit layout</p> <code>5.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>angle</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Return the east anchor of the node.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Get the center position for the node label.</p> <code>north</code> <p>Return the north anchor of the node.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Return the south anchor of the node.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Return the west anchor of the node.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureMultimerSubunitLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureMultimerSubunitLayout.angle","title":"angle","text":"<pre><code>angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureMultimerSubunitLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureMultimerSubunitLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureMultimerSubunitLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureMultimerSubunitLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureMultimerSubunitLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureMultimerSubunitLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureMultimerSubunitLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureMultimerSubunitLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureMultimerSubunitLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Return the east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Return the east anchor of the node.\"\"\"\n    return self.own_angle(0)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureMultimerSubunitLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureMultimerSubunitLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureMultimerSubunitLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureMultimerSubunitLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureMultimerSubunitLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Get the center position for the node label.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point where the label should be centered.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Get the center position for the node label.\n\n    Returns:\n        Point where the label should be centered.\n    \"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2 - (self._n - 1) * self.offset,\n        self.height / 2 - height / 2 - (self._n - 1) * self.offset,\n    )\n    return self._make_subunit_shape(\n        position=position, width=width, height=height\n    ).position\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureMultimerSubunitLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Return the north anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Return the north anchor of the node.\"\"\"\n    return self.own_angle(90)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureMultimerSubunitLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureMultimerSubunitLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureMultimerSubunitLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureMultimerSubunitLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureMultimerSubunitLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureMultimerSubunitLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureMultimerSubunitLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureMultimerSubunitLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureMultimerSubunitLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureMultimerSubunitLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureMultimerSubunitLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureMultimerSubunitLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Return the south anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Return the south anchor of the node.\"\"\"\n    return self.own_angle(270)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureMultimerSubunitLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureMultimerSubunitLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureMultimerSubunitLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureMultimerSubunitLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureMultimerSubunitLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureMultimerSubunitLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Return the west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Return the west anchor of the node.\"\"\"\n    return self.own_angle(180)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureMultimerSubunitLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureMultimerSubunitLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureMultimerSubunitLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureMultimerSubunitLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureSubunit","title":"NucleicAcidFeatureSubunit  <code>dataclass</code>","text":"<pre><code>NucleicAcidFeatureSubunit(*, id_: str = make_uuid4_as_str(), label: str | None = None, state_variables: frozenset[StateVariable] = frozenset(), units_of_information: frozenset[UnitOfInformation] = frozenset())\n</code></pre> <p>               Bases: <code>Subunit</code></p> <p>Nucleic acid feature subunit.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'01ae73ac-c482-4ab8-8cd1-4c45248bd142'</code> <code>label</code> <code>str | None</code> <p>The label of the subunit</p> <code>None</code> <code>state_variables</code> <code>frozenset[StateVariable]</code> <p>The state variables of the nucleic acid feature subunit</p> <code>&lt;dynamic&gt;</code> <code>units_of_information</code> <code>frozenset[UnitOfInformation]</code> <p>The units of information of the nucleic acid feature subunit</p> <code>&lt;dynamic&gt;</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureSubunit.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureSubunitLayout","title":"NucleicAcidFeatureSubunitLayout  <code>dataclass</code>","text":"<pre><code>NucleicAcidFeatureSubunitLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = white, filter_: NoneValueType | Filter | None = None, height: float = 30.0, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = 1.25, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 60.0, rounded_corners: float = 5.0)\n</code></pre> <p>               Bases: <code>_SimpleMixin</code>, <code>SBGNNode</code></p> <p>Nucleic acid feature subunit layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'9acf3a54-34ed-44a6-b15f-7712794c839e'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>30.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The stroke width of the node</p> <code>1.25</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>60.0</code> <code>rounded_corners</code> <code>float</code> <p>The corner radius of the nucleic acid feature subunit layout</p> <code>5.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>angle</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Return the east anchor of the node.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Return the label center anchor of the node.</p> <code>north</code> <p>Return the north anchor of the node.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Return the south anchor of the node.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Return the west anchor of the node.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureSubunitLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureSubunitLayout.angle","title":"angle","text":"<pre><code>angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureSubunitLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureSubunitLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureSubunitLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureSubunitLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureSubunitLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureSubunitLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureSubunitLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureSubunitLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureSubunitLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Return the east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Return the east anchor of the node.\"\"\"\n    return self.own_angle(0)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureSubunitLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureSubunitLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureSubunitLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureSubunitLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureSubunitLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Return the label center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Return the label center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureSubunitLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Return the north anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Return the north anchor of the node.\"\"\"\n    return self.own_angle(90)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureSubunitLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureSubunitLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureSubunitLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureSubunitLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureSubunitLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureSubunitLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureSubunitLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureSubunitLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureSubunitLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureSubunitLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureSubunitLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureSubunitLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Return the south anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Return the south anchor of the node.\"\"\"\n    return self.own_angle(270)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureSubunitLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureSubunitLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureSubunitLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureSubunitLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureSubunitLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureSubunitLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Return the west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Return the west anchor of the node.\"\"\"\n    return self.own_angle(180)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureSubunitLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureSubunitLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureSubunitLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.NucleicAcidFeatureSubunitLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OmittedProcess","title":"OmittedProcess  <code>dataclass</code>","text":"<pre><code>OmittedProcess(*, id_: str = make_uuid4_as_str(), reactants: frozenset[Reactant] = frozenset(), products: frozenset[Product] = frozenset(), reversible: bool = False, has_external_source: bool = False, has_external_sink: bool = False)\n</code></pre> <p>               Bases: <code>GenericProcess</code></p> <p>Omitted process.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'3f67f1af-7ec0-49ef-a94c-5120266cacd9'</code> <code>reactants</code> <code>frozenset[Reactant]</code> <p>The reactants of the stoichiometric process</p> <code>&lt;dynamic&gt;</code> <code>products</code> <code>frozenset[Product]</code> <p>The products of the stoichiometric process</p> <code>&lt;dynamic&gt;</code> <code>reversible</code> <code>bool</code> <p>Whether the stoichiometric process is reversible or not</p> <code>False</code> <code>has_external_source</code> <code>bool</code> <p>Whether the process has an unspecified external source (an empty-set / source-and-sink reactant in SBGN PD).</p> <code>False</code> <code>has_external_sink</code> <code>bool</code> <p>Whether the process has an unspecified external sink (an empty-set / source-and-sink product in SBGN PD).</p> <code>False</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OmittedProcess.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OmittedProcessLayout","title":"OmittedProcessLayout  <code>dataclass</code>","text":"<pre><code>OmittedProcessLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = white, filter_: NoneValueType | Filter | None = None, height: float = 20.0, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = 1.25, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 20.0, text: str = '\\\\\\\\', font_family: str = DEFAULT_FONT_FAMILY, font_fill: Color | NoneValueType = black, font_stroke: Color | NoneValueType = NoneValue, font_style: FontStyle = NORMAL, font_weight: FontWeight | float = NORMAL, orientation: Orientation = HORIZONTAL, left_to_right: bool = True, left_connector_length: float = 10.0, right_connector_length: float = 10.0, left_connector_stroke: NoneValueType | Color | None = None, left_connector_stroke_width: float | None = None, left_connector_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, left_connector_stroke_dashoffset: float | None = None, left_connector_fill: NoneValueType | Color | None = None, left_connector_transform: NoneValueType | tuple[Transformation, ...] | None = None, left_connector_filter: NoneValueType | Filter | None = None, right_connector_stroke: NoneValueType | Color | None = None, right_connector_stroke_width: float | None = None, right_connector_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, right_connector_stroke_dashoffset: float | None = None, right_connector_fill: NoneValueType | Color | None = None, right_connector_transform: NoneValueType | tuple[Transformation, ...] | None = None, right_connector_filter: NoneValueType | Filter | None = None)\n</code></pre> <p>               Bases: <code>_ConnectorsMixin</code>, <code>_SimpleMixin</code>, <code>_TextMixin</code>, <code>SBGNNode</code></p> <p>Omitted process layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'c8ce35e4-17f6-4e78-aa9f-5795fdd59554'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>20.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The stroke width of the node</p> <code>1.25</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>20.0</code> <code>text</code> <code>str</code> <p>The text drawn inside the process glyph</p> <code>'\\\\\\\\'</code> <code>font_family</code> <code>str</code> <p>Font family for the text.</p> <code>'DejaVu Sans'</code> <code>font_fill</code> <code>Color | NoneValueType</code> <p>Fill color for the text.</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>font_stroke</code> <code>Color | NoneValueType</code> <p>Stroke color for the text outline.</p> <code>&lt;momapy.drawing.NoneValueType object at 0x7f8b17323e00&gt;</code> <code>font_style</code> <code>FontStyle</code> <p>Font style (normal, italic, etc.).</p> <code>&lt;FontStyle.NORMAL: 0&gt;</code> <code>font_weight</code> <code>FontWeight | float</code> <p>Font weight (normal, bold, etc.).</p> <code>&lt;FontWeight.NORMAL: 0&gt;</code> <code>orientation</code> <code>Orientation</code> <p>Orientation of connectors (HORIZONTAL or VERTICAL).</p> <code>&lt;Orientation.HORIZONTAL: 1&gt;</code> <code>left_to_right</code> <code>bool</code> <p>Whether connectors flow left to right.</p> <code>True</code> <code>left_connector_length</code> <code>float</code> <p>Length of the left connector.</p> <code>10.0</code> <code>right_connector_length</code> <code>float</code> <p>Length of the right connector.</p> <code>10.0</code> <code>left_connector_stroke</code> <code>NoneValueType | Color | None</code> <p>Stroke color for the left connector line.</p> <code>None</code> <code>left_connector_stroke_width</code> <code>float | None</code> <p>Stroke width for the left connector line.</p> <code>None</code> <code>left_connector_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>Dash pattern for the left connector line.</p> <code>None</code> <code>left_connector_stroke_dashoffset</code> <code>float | None</code> <p>Dash offset for the left connector line.</p> <code>None</code> <code>left_connector_fill</code> <code>NoneValueType | Color | None</code> <p>Fill color for the left connector.</p> <code>None</code> <code>left_connector_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>Transformations applied to the left connector.</p> <code>None</code> <code>left_connector_filter</code> <code>NoneValueType | Filter | None</code> <p>Filter applied to the left connector.</p> <code>None</code> <code>right_connector_stroke</code> <code>NoneValueType | Color | None</code> <p>Stroke color for the right connector line.</p> <code>None</code> <code>right_connector_stroke_width</code> <code>float | None</code> <p>Stroke width for the right connector line.</p> <code>None</code> <code>right_connector_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>Dash pattern for the right connector line.</p> <code>None</code> <code>right_connector_stroke_dashoffset</code> <code>float | None</code> <p>Dash offset for the right connector line.</p> <code>None</code> <code>right_connector_fill</code> <code>NoneValueType | Color | None</code> <p>Fill color for the right connector.</p> <code>None</code> <code>right_connector_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>Transformations applied to the right connector.</p> <code>None</code> <code>right_connector_filter</code> <code>NoneValueType | Filter | None</code> <p>Filter applied to the right connector.</p> <code>None</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>angle</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Get the east (right) anchor point.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Return the label center anchor of the node.</p> <code>left_connector_base</code> <p>Get the base point of the left connector.</p> <code>left_connector_tip</code> <p>Get the tip point of the left connector.</p> <code>north</code> <p>Get the north (top) anchor point.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>right_connector_base</code> <p>Get the base point of the right connector.</p> <code>right_connector_tip</code> <p>Get the tip point of the right connector.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Get the south (bottom) anchor point.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Get the west (left) anchor point.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OmittedProcessLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OmittedProcessLayout.angle","title":"angle","text":"<pre><code>angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OmittedProcessLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OmittedProcessLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OmittedProcessLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OmittedProcessLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OmittedProcessLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OmittedProcessLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OmittedProcessLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OmittedProcessLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OmittedProcessLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Get the east (right) anchor point.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point on the east side of the element.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Get the east (right) anchor point.\n\n    Returns:\n        Point on the east side of the element.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x + self.width / 2, self.y)\n    else:\n        return Point(self.x + self.width / 2 + self.right_connector_length, self.y)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OmittedProcessLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OmittedProcessLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OmittedProcessLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OmittedProcessLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OmittedProcessLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Return the label center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Return the label center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OmittedProcessLayout.left_connector_base","title":"left_connector_base","text":"<pre><code>left_connector_base() -&gt; Point\n</code></pre> <p>Get the base point of the left connector.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point where the left connector attaches to the shape.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def left_connector_base(self) -&gt; Point:\n    \"\"\"Get the base point of the left connector.\n\n    Returns:\n        Point where the left connector attaches to the shape.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x, self.y - self.height / 2)\n    else:\n        return Point(self.x - self.width / 2, self.y)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OmittedProcessLayout.left_connector_tip","title":"left_connector_tip","text":"<pre><code>left_connector_tip() -&gt; Point\n</code></pre> <p>Get the tip point of the left connector.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point at the end of the left connector line.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def left_connector_tip(self) -&gt; Point:\n    \"\"\"Get the tip point of the left connector.\n\n    Returns:\n        Point at the end of the left connector line.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x, self.y - self.height / 2 - self.left_connector_length)\n    else:\n        return Point(self.x - self.width / 2 - self.left_connector_length, self.y)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OmittedProcessLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Get the north (top) anchor point.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point on the north side of the element.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Get the north (top) anchor point.\n\n    Returns:\n        Point on the north side of the element.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x, self.y - self.height / 2 - self.left_connector_length)\n    else:\n        return Point(self.x, self.y - self.height / 2)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OmittedProcessLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OmittedProcessLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OmittedProcessLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OmittedProcessLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OmittedProcessLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OmittedProcessLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OmittedProcessLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OmittedProcessLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OmittedProcessLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OmittedProcessLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OmittedProcessLayout.right_connector_base","title":"right_connector_base","text":"<pre><code>right_connector_base() -&gt; Point\n</code></pre> <p>Get the base point of the right connector.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point where the right connector attaches to the shape.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def right_connector_base(self) -&gt; Point:\n    \"\"\"Get the base point of the right connector.\n\n    Returns:\n        Point where the right connector attaches to the shape.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x, self.y + self.height / 2)\n    else:\n        return Point(self.x + self.width / 2, self.y)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OmittedProcessLayout.right_connector_tip","title":"right_connector_tip","text":"<pre><code>right_connector_tip() -&gt; Point\n</code></pre> <p>Get the tip point of the right connector.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point at the end of the right connector line.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def right_connector_tip(self) -&gt; Point:\n    \"\"\"Get the tip point of the right connector.\n\n    Returns:\n        Point at the end of the right connector line.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x, self.y + self.height / 2 + self.right_connector_length)\n    else:\n        return Point(self.x + self.width / 2 + self.right_connector_length, self.y)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OmittedProcessLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OmittedProcessLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Get the south (bottom) anchor point.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point on the south side of the element.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Get the south (bottom) anchor point.\n\n    Returns:\n        Point on the south side of the element.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x, self.y + self.height / 2 + self.right_connector_length)\n    else:\n        return Point(self.x, self.y + self.height / 2)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OmittedProcessLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OmittedProcessLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OmittedProcessLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OmittedProcessLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OmittedProcessLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OmittedProcessLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Get the west (left) anchor point.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point on the west side of the element.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Get the west (left) anchor point.\n\n    Returns:\n        Point on the west side of the element.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x - self.width / 2, self.y)\n    else:\n        return Point(self.x - self.width / 2 - self.left_connector_length, self.y)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OmittedProcessLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OmittedProcessLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OmittedProcessLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OmittedProcessLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OrOperator","title":"OrOperator  <code>dataclass</code>","text":"<pre><code>OrOperator(*, id_: str = make_uuid4_as_str(), inputs: frozenset[LogicalOperatorInput] = frozenset())\n</code></pre> <p>               Bases: <code>LogicalOperator</code></p> <p>OR operator.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'b9dbcf66-4a7e-43cf-bc58-518589cac735'</code> <code>inputs</code> <code>frozenset[LogicalOperatorInput]</code> <p>The inputs of the logical operator</p> <code>&lt;dynamic&gt;</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OrOperator.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OrOperatorLayout","title":"OrOperatorLayout  <code>dataclass</code>","text":"<pre><code>OrOperatorLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = white, filter_: NoneValueType | Filter | None = None, height: float = 30.0, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = 1.25, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 30.0, text: str = 'OR', font_family: str = DEFAULT_FONT_FAMILY, font_fill: Color | NoneValueType = black, font_stroke: Color | NoneValueType = NoneValue, font_style: FontStyle = NORMAL, font_weight: FontWeight | float = NORMAL, orientation: Orientation = HORIZONTAL, left_to_right: bool = True, left_connector_length: float = 10.0, right_connector_length: float = 10.0, left_connector_stroke: NoneValueType | Color | None = None, left_connector_stroke_width: float | None = None, left_connector_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, left_connector_stroke_dashoffset: float | None = None, left_connector_fill: NoneValueType | Color | None = None, left_connector_transform: NoneValueType | tuple[Transformation, ...] | None = None, left_connector_filter: NoneValueType | Filter | None = None, right_connector_stroke: NoneValueType | Color | None = None, right_connector_stroke_width: float | None = None, right_connector_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, right_connector_stroke_dashoffset: float | None = None, right_connector_fill: NoneValueType | Color | None = None, right_connector_transform: NoneValueType | tuple[Transformation, ...] | None = None, right_connector_filter: NoneValueType | Filter | None = None)\n</code></pre> <p>               Bases: <code>_ConnectorsMixin</code>, <code>_SimpleMixin</code>, <code>_TextMixin</code>, <code>SBGNNode</code></p> <p>OR operator layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'6f6fb881-ef60-4754-b3e8-55f03e6fcb8a'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>30.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The stroke width of the node</p> <code>1.25</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>30.0</code> <code>text</code> <code>str</code> <p>The text drawn inside the operator glyph</p> <code>'OR'</code> <code>font_family</code> <code>str</code> <p>Font family for the text.</p> <code>'DejaVu Sans'</code> <code>font_fill</code> <code>Color | NoneValueType</code> <p>Fill color for the text.</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>font_stroke</code> <code>Color | NoneValueType</code> <p>Stroke color for the text outline.</p> <code>&lt;momapy.drawing.NoneValueType object at 0x7f8b17323e00&gt;</code> <code>font_style</code> <code>FontStyle</code> <p>Font style (normal, italic, etc.).</p> <code>&lt;FontStyle.NORMAL: 0&gt;</code> <code>font_weight</code> <code>FontWeight | float</code> <p>Font weight (normal, bold, etc.).</p> <code>&lt;FontWeight.NORMAL: 0&gt;</code> <code>orientation</code> <code>Orientation</code> <p>Orientation of connectors (HORIZONTAL or VERTICAL).</p> <code>&lt;Orientation.HORIZONTAL: 1&gt;</code> <code>left_to_right</code> <code>bool</code> <p>Whether connectors flow left to right.</p> <code>True</code> <code>left_connector_length</code> <code>float</code> <p>Length of the left connector.</p> <code>10.0</code> <code>right_connector_length</code> <code>float</code> <p>Length of the right connector.</p> <code>10.0</code> <code>left_connector_stroke</code> <code>NoneValueType | Color | None</code> <p>Stroke color for the left connector line.</p> <code>None</code> <code>left_connector_stroke_width</code> <code>float | None</code> <p>Stroke width for the left connector line.</p> <code>None</code> <code>left_connector_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>Dash pattern for the left connector line.</p> <code>None</code> <code>left_connector_stroke_dashoffset</code> <code>float | None</code> <p>Dash offset for the left connector line.</p> <code>None</code> <code>left_connector_fill</code> <code>NoneValueType | Color | None</code> <p>Fill color for the left connector.</p> <code>None</code> <code>left_connector_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>Transformations applied to the left connector.</p> <code>None</code> <code>left_connector_filter</code> <code>NoneValueType | Filter | None</code> <p>Filter applied to the left connector.</p> <code>None</code> <code>right_connector_stroke</code> <code>NoneValueType | Color | None</code> <p>Stroke color for the right connector line.</p> <code>None</code> <code>right_connector_stroke_width</code> <code>float | None</code> <p>Stroke width for the right connector line.</p> <code>None</code> <code>right_connector_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>Dash pattern for the right connector line.</p> <code>None</code> <code>right_connector_stroke_dashoffset</code> <code>float | None</code> <p>Dash offset for the right connector line.</p> <code>None</code> <code>right_connector_fill</code> <code>NoneValueType | Color | None</code> <p>Fill color for the right connector.</p> <code>None</code> <code>right_connector_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>Transformations applied to the right connector.</p> <code>None</code> <code>right_connector_filter</code> <code>NoneValueType | Filter | None</code> <p>Filter applied to the right connector.</p> <code>None</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>angle</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Get the east (right) anchor point.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Return the label center anchor of the node.</p> <code>left_connector_base</code> <p>Get the base point of the left connector.</p> <code>left_connector_tip</code> <p>Get the tip point of the left connector.</p> <code>north</code> <p>Get the north (top) anchor point.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>right_connector_base</code> <p>Get the base point of the right connector.</p> <code>right_connector_tip</code> <p>Get the tip point of the right connector.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Get the south (bottom) anchor point.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Get the west (left) anchor point.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OrOperatorLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OrOperatorLayout.angle","title":"angle","text":"<pre><code>angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OrOperatorLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OrOperatorLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OrOperatorLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OrOperatorLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OrOperatorLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OrOperatorLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OrOperatorLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OrOperatorLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OrOperatorLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Get the east (right) anchor point.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point on the east side of the element.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Get the east (right) anchor point.\n\n    Returns:\n        Point on the east side of the element.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x + self.width / 2, self.y)\n    else:\n        return Point(self.x + self.width / 2 + self.right_connector_length, self.y)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OrOperatorLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OrOperatorLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OrOperatorLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OrOperatorLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OrOperatorLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Return the label center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Return the label center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OrOperatorLayout.left_connector_base","title":"left_connector_base","text":"<pre><code>left_connector_base() -&gt; Point\n</code></pre> <p>Get the base point of the left connector.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point where the left connector attaches to the shape.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def left_connector_base(self) -&gt; Point:\n    \"\"\"Get the base point of the left connector.\n\n    Returns:\n        Point where the left connector attaches to the shape.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x, self.y - self.height / 2)\n    else:\n        return Point(self.x - self.width / 2, self.y)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OrOperatorLayout.left_connector_tip","title":"left_connector_tip","text":"<pre><code>left_connector_tip() -&gt; Point\n</code></pre> <p>Get the tip point of the left connector.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point at the end of the left connector line.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def left_connector_tip(self) -&gt; Point:\n    \"\"\"Get the tip point of the left connector.\n\n    Returns:\n        Point at the end of the left connector line.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x, self.y - self.height / 2 - self.left_connector_length)\n    else:\n        return Point(self.x - self.width / 2 - self.left_connector_length, self.y)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OrOperatorLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Get the north (top) anchor point.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point on the north side of the element.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Get the north (top) anchor point.\n\n    Returns:\n        Point on the north side of the element.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x, self.y - self.height / 2 - self.left_connector_length)\n    else:\n        return Point(self.x, self.y - self.height / 2)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OrOperatorLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OrOperatorLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OrOperatorLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OrOperatorLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OrOperatorLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OrOperatorLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OrOperatorLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OrOperatorLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OrOperatorLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OrOperatorLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OrOperatorLayout.right_connector_base","title":"right_connector_base","text":"<pre><code>right_connector_base() -&gt; Point\n</code></pre> <p>Get the base point of the right connector.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point where the right connector attaches to the shape.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def right_connector_base(self) -&gt; Point:\n    \"\"\"Get the base point of the right connector.\n\n    Returns:\n        Point where the right connector attaches to the shape.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x, self.y + self.height / 2)\n    else:\n        return Point(self.x + self.width / 2, self.y)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OrOperatorLayout.right_connector_tip","title":"right_connector_tip","text":"<pre><code>right_connector_tip() -&gt; Point\n</code></pre> <p>Get the tip point of the right connector.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point at the end of the right connector line.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def right_connector_tip(self) -&gt; Point:\n    \"\"\"Get the tip point of the right connector.\n\n    Returns:\n        Point at the end of the right connector line.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x, self.y + self.height / 2 + self.right_connector_length)\n    else:\n        return Point(self.x + self.width / 2 + self.right_connector_length, self.y)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OrOperatorLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OrOperatorLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Get the south (bottom) anchor point.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point on the south side of the element.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Get the south (bottom) anchor point.\n\n    Returns:\n        Point on the south side of the element.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x, self.y + self.height / 2 + self.right_connector_length)\n    else:\n        return Point(self.x, self.y + self.height / 2)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OrOperatorLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OrOperatorLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OrOperatorLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OrOperatorLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OrOperatorLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OrOperatorLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Get the west (left) anchor point.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point on the west side of the element.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Get the west (left) anchor point.\n\n    Returns:\n        Point on the west side of the element.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x - self.width / 2, self.y)\n    else:\n        return Point(self.x - self.width / 2 - self.left_connector_length, self.y)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OrOperatorLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OrOperatorLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OrOperatorLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.OrOperatorLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.PerturbingAgent","title":"PerturbingAgent  <code>dataclass</code>","text":"<pre><code>PerturbingAgent(*, id_: str = make_uuid4_as_str(), compartment: Compartment | None = None, label: str | None = None)\n</code></pre> <p>               Bases: <code>EntityPool</code></p> <p>Perturbing agent.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'47b1acfe-ecfc-4a85-aab5-81e9b4ff2959'</code> <code>compartment</code> <code>Compartment | None</code> <p>The compartment of the entity pool</p> <code>None</code> <code>label</code> <code>str | None</code> <p>The label of the perturbing agent</p> <code>None</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.PerturbingAgent.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.PerturbingAgentLayout","title":"PerturbingAgentLayout  <code>dataclass</code>","text":"<pre><code>PerturbingAgentLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = white, filter_: NoneValueType | Filter | None = None, height: float = 30.0, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = 1.25, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 60.0, angle: float = 70.0)\n</code></pre> <p>               Bases: <code>_SimpleMixin</code>, <code>SBGNNode</code></p> <p>Perturbing agent layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'54b2a039-2d8d-4ec6-af58-cd7985628fd1'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>30.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The stroke width of the node</p> <code>1.25</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>60.0</code> <code>angle</code> <code>float</code> <p>The angle of the hexagon notches of the perturbing agent layout</p> <code>70.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Return the east anchor of the node.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Return the label center anchor of the node.</p> <code>north</code> <p>Return the north anchor of the node.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Return the south anchor of the node.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Return the west anchor of the node.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>angle</code> <code>float</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.PerturbingAgentLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.PerturbingAgentLayout.angle","title":"angle  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>angle: float = dataclasses.field(default=70.0, metadata={'description': 'The angle of the hexagon notches of the perturbing agent layout'})\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.PerturbingAgentLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.PerturbingAgentLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.PerturbingAgentLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.PerturbingAgentLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.PerturbingAgentLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.PerturbingAgentLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.PerturbingAgentLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.PerturbingAgentLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.PerturbingAgentLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Return the east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Return the east anchor of the node.\"\"\"\n    return self.own_angle(0)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.PerturbingAgentLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.PerturbingAgentLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.PerturbingAgentLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.PerturbingAgentLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.PerturbingAgentLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Return the label center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Return the label center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.PerturbingAgentLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Return the north anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Return the north anchor of the node.\"\"\"\n    return self.own_angle(90)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.PerturbingAgentLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.PerturbingAgentLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.PerturbingAgentLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.PerturbingAgentLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.PerturbingAgentLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.PerturbingAgentLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.PerturbingAgentLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.PerturbingAgentLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.PerturbingAgentLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.PerturbingAgentLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.PerturbingAgentLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.PerturbingAgentLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Return the south anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Return the south anchor of the node.\"\"\"\n    return self.own_angle(270)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.PerturbingAgentLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.PerturbingAgentLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.PerturbingAgentLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.PerturbingAgentLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.PerturbingAgentLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.PerturbingAgentLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Return the west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Return the west anchor of the node.\"\"\"\n    return self.own_angle(180)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.PerturbingAgentLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.PerturbingAgentLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.PerturbingAgentLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.PerturbingAgentLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.Phenotype","title":"Phenotype  <code>dataclass</code>","text":"<pre><code>Phenotype(*, id_: str = make_uuid4_as_str(), label: str | None = None)\n</code></pre> <p>               Bases: <code>Process</code></p> <p>Phenotype.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'e2cf9eee-cc06-484e-aff3-4321e962a75e'</code> <code>label</code> <code>str | None</code> <p>The label of the phenotype</p> <code>None</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.Phenotype.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.PhenotypeLayout","title":"PhenotypeLayout  <code>dataclass</code>","text":"<pre><code>PhenotypeLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = white, filter_: NoneValueType | Filter | None = None, height: float = 30.0, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = 1.25, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 60.0, angle: float = 70.0)\n</code></pre> <p>               Bases: <code>_SimpleMixin</code>, <code>SBGNNode</code></p> <p>Phenotype layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'4ee0b87c-6e3f-4392-86a5-1f9d4b89446d'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>30.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The stroke width of the node</p> <code>1.25</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>60.0</code> <code>angle</code> <code>float</code> <p>The angle of the hexagon points of the phenotype layout</p> <code>70.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Return the east anchor of the node.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Return the label center anchor of the node.</p> <code>north</code> <p>Return the north anchor of the node.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Return the south anchor of the node.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Return the west anchor of the node.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>angle</code> <code>float</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.PhenotypeLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.PhenotypeLayout.angle","title":"angle  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>angle: float = dataclasses.field(default=70.0, metadata={'description': 'The angle of the hexagon points of the phenotype layout'})\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.PhenotypeLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.PhenotypeLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.PhenotypeLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.PhenotypeLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.PhenotypeLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.PhenotypeLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.PhenotypeLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.PhenotypeLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.PhenotypeLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Return the east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Return the east anchor of the node.\"\"\"\n    return self.own_angle(0)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.PhenotypeLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.PhenotypeLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.PhenotypeLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.PhenotypeLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.PhenotypeLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Return the label center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Return the label center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.PhenotypeLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Return the north anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Return the north anchor of the node.\"\"\"\n    return self.own_angle(90)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.PhenotypeLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.PhenotypeLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.PhenotypeLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.PhenotypeLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.PhenotypeLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.PhenotypeLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.PhenotypeLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.PhenotypeLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.PhenotypeLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.PhenotypeLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.PhenotypeLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.PhenotypeLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Return the south anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Return the south anchor of the node.\"\"\"\n    return self.own_angle(270)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.PhenotypeLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.PhenotypeLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.PhenotypeLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.PhenotypeLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.PhenotypeLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.PhenotypeLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Return the west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Return the west anchor of the node.\"\"\"\n    return self.own_angle(180)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.PhenotypeLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.PhenotypeLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.PhenotypeLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.PhenotypeLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.Process","title":"Process  <code>dataclass</code>","text":"<pre><code>Process(*, id_: str = make_uuid4_as_str())\n</code></pre> <p>               Bases: <code>SBGNModelElement</code></p> <p>Abstract base class for processes.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'92572310-3820-448e-af72-b8dbd703701d'</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.Process.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.Product","title":"Product  <code>dataclass</code>","text":"<pre><code>Product(*, id_: str = make_uuid4_as_str(), referred_element: EntityPool, stoichiometry: float | None = None)\n</code></pre> <p>               Bases: <code>FluxRole</code></p> <p>Product.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'dbe2ae95-b12f-4794-b3cc-1adcc36dc3dc'</code> <code>referred_element</code> <code>EntityPool</code> <p>The entity pool of the flux role</p> required <code>stoichiometry</code> <code>float | None</code> <p>The stoichiometry of the flux role</p> <code>None</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.Product.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ProductionLayout","title":"ProductionLayout  <code>dataclass</code>","text":"<pre><code>ProductionLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, end_shorten: float = 0.0, fill: NoneValueType | Color | None = None, filter_: NoneValueType | Filter | None = None, path_fill: NoneValueType | Color | None = NoneValue, path_filter: NoneValueType | Filter | None = None, path_stroke: NoneValueType | Color | None = black, path_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, path_stroke_dashoffset: NoneValueType | float | None = None, path_stroke_width: float | None = 1.25, path_transform: NoneValueType | tuple[Transformation, ...] | None = None, stroke: NoneValueType | Color | None = None, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: NoneValueType | float | None = None, segments: tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...] = tuple(), source: LayoutElement | None = None, start_shorten: float = 0.0, target: LayoutElement | None = None, transform: NoneValueType | tuple[Transformation, ...] | None = None, arrowhead_fill: NoneValueType | Color | None = black, arrowhead_filter: NoneValueType | Filter | None = None, arrowhead_stroke: NoneValueType | Color | None = black, arrowhead_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, arrowhead_stroke_dashoffset: NoneValueType | float | None = None, arrowhead_stroke_width: float | None = 1.25, arrowhead_transform: NoneValueType | tuple[Transformation, ...] | None = None, arrowhead_height: float = 10.0, arrowhead_width: float = 10.0)\n</code></pre> <p>               Bases: <code>SBGNSingleHeadedArc</code></p> <p>Production layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'f5d16cc2-2d8a-4636-970e-344aa8ec72aa'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>end_shorten</code> <code>float</code> <p>The length the end of the arc will be shorten by</p> <code>0.0</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the arc</p> <code>None</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the arc</p> <code>None</code> <code>path_fill</code> <code>NoneValueType | Color | None</code> <p>The path fill color of the arc</p> <code>&lt;momapy.drawing.NoneValueType object at 0x7f8b17323e00&gt;</code> <code>path_filter</code> <code>NoneValueType | Filter | None</code> <p>The path filter of the arc</p> <code>None</code> <code>path_stroke</code> <code>NoneValueType | Color | None</code> <p>The path stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>path_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The path stroke dasharray of the arc</p> <code>None</code> <code>path_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The path stroke dashoffset of the arc</p> <code>None</code> <code>path_stroke_width</code> <code>float | None</code> <p>The path stroke width of the arc</p> <code>1.25</code> <code>path_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The path transform of the arc</p> <code>None</code> <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the arc</p> <code>None</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the arc</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the arc</p> <code>None</code> <code>stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the arc</p> <code>None</code> <code>segments</code> <code>tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...]</code> <p>The path segments of the arc</p> <code>&lt;dynamic&gt;</code> <code>source</code> <code>LayoutElement | None</code> <p>The source of the arc</p> <code>None</code> <code>start_shorten</code> <code>float</code> <p>The length the start of the arc will be shorten by</p> <code>0.0</code> <code>target</code> <code>LayoutElement | None</code> <p>The target of the arc</p> <code>None</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the arc</p> <code>None</code> <code>arrowhead_fill</code> <code>NoneValueType | Color | None</code> <p>The arrowhead fill color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>arrowhead_filter</code> <code>NoneValueType | Filter | None</code> <p>The arrowhead filter of the arc</p> <code>None</code> <code>arrowhead_stroke</code> <code>NoneValueType | Color | None</code> <p>The arrowhead stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>arrowhead_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The arrowhead stroke dasharray of the arc</p> <code>None</code> <code>arrowhead_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The arrowhead stroke dashoffset of the arc</p> <code>None</code> <code>arrowhead_stroke_width</code> <code>float | None</code> <p>The arrowhead stroke width of the arc</p> <code>1.25</code> <code>arrowhead_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The arrowhead transform of the arc</p> <code>None</code> <code>arrowhead_height</code> <code>float</code> <p>The height of the arrowhead</p> <code>10.0</code> <code>arrowhead_width</code> <code>float</code> <p>The width of the arrowhead</p> <code>10.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>arrowhead_base</code> <p>Return the arrowhead base anchor point of the single-headed arc.</p> <code>arrowhead_bbox</code> <p>Return the bounding box of the single-headed arc arrowhead.</p> <code>arrowhead_border</code> <p>Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.</p> <code>arrowhead_drawing_elements</code> <p>Return the drawing elements of the single-headed arc arrowhead.</p> <code>arrowhead_length</code> <p>Return the length of the single-headed arc arrowhead.</p> <code>arrowhead_tip</code> <p>Return the arrowhead tip anchor point of the single-headed arc.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>childless</code> <p>Return a copy of the arc with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>end_point</code> <p>Return the ending point of the arc.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>fraction</code> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <code>length</code> <p>Return the total length of the arc path.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_children</code> <p>Return the self children of the arc.</p> <code>own_drawing_elements</code> <p>Return the self drawing elements of the single-headed arc.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>path_drawing_elements</code> <p>Return the drawing elements of the single-headed arc path.</p> <code>points</code> <p>Return the points of the arc path.</p> <code>start_point</code> <p>Return the starting point of the arc.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ProductionLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ProductionLayout.arrowhead_base","title":"arrowhead_base","text":"<pre><code>arrowhead_base() -&gt; Point\n</code></pre> <p>Return the arrowhead base anchor point of the single-headed arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_base(self) -&gt; Point:\n    \"\"\"Return the arrowhead base anchor point of the single-headed arc.\"\"\"\n    arrowhead_length = self.arrowhead_length()\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return self.arrowhead_tip() - (arrowhead_length, 0)\n    fraction = 1 - (arrowhead_length + self.end_shorten) / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ProductionLayout.arrowhead_bbox","title":"arrowhead_bbox","text":"<pre><code>arrowhead_bbox() -&gt; Bbox\n</code></pre> <p>Return the bounding box of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_bbox(self) -&gt; Bbox:\n    \"\"\"Return the bounding box of the single-headed arc arrowhead.\"\"\"\n    return get_drawing_elements_bbox(self.arrowhead_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ProductionLayout.arrowhead_border","title":"arrowhead_border","text":"<pre><code>arrowhead_border(point: Point) -&gt; Point\n</code></pre> <p>Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_border(self, point: Point) -&gt; Point:\n    \"\"\"Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    point = get_drawing_elements_border(self.arrowhead_drawing_elements(), point)\n    if point is None:\n        return self.arrowhead_tip()\n    return point\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ProductionLayout.arrowhead_drawing_elements","title":"arrowhead_drawing_elements","text":"<pre><code>arrowhead_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_drawing_elements(\n    self,\n) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the single-headed arc arrowhead.\"\"\"\n    drawing_elements = self._arrowhead_border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_arrowhead\",\n        elements=tuple(drawing_elements),\n        fill=self.arrowhead_fill,\n        filter_=self.arrowhead_filter,\n        id_=f\"{self.id_}_arrowhead\",\n        stroke=self.arrowhead_stroke,\n        stroke_dasharray=self.arrowhead_stroke_dasharray,\n        stroke_dashoffset=self.arrowhead_stroke_dashoffset,\n        stroke_width=self.arrowhead_stroke_width,\n        transform=self.arrowhead_transform,\n    )\n    transformation = self._get_arrowhead_transformation()\n    group = group.transformed(transformation)\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ProductionLayout.arrowhead_length","title":"arrowhead_length","text":"<pre><code>arrowhead_length() -&gt; float\n</code></pre> <p>Return the length of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_length(self) -&gt; float:\n    \"\"\"Return the length of the single-headed arc arrowhead.\"\"\"\n    bbox = get_drawing_elements_bbox(self._arrowhead_border_drawing_elements())\n    if math.isnan(bbox.width):\n        return 0.0\n    return bbox.east().x\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ProductionLayout.arrowhead_tip","title":"arrowhead_tip","text":"<pre><code>arrowhead_tip() -&gt; Point\n</code></pre> <p>Return the arrowhead tip anchor point of the single-headed arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_tip(self) -&gt; Point:\n    \"\"\"Return the arrowhead tip anchor point of the single-headed arc.\"\"\"\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return segment.p2\n    fraction = 1 - self.end_shorten / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ProductionLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ProductionLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the arc with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the arc with no children.\"\"\"\n    return dataclasses.replace(self, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ProductionLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ProductionLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ProductionLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ProductionLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ProductionLayout.end_point","title":"end_point","text":"<pre><code>end_point() -&gt; Point\n</code></pre> <p>Return the ending point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_point(self) -&gt; Point:\n    \"\"\"Return the ending point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[-1]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ProductionLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ProductionLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ProductionLayout.fraction","title":"fraction","text":"<pre><code>fraction(fraction: float) -&gt; tuple[Point, float]\n</code></pre> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def fraction(self, fraction: float) -&gt; tuple[Point, float]:\n    \"\"\"Return the position and angle on the arc at a given fraction (of the total arc length).\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    current_length = 0\n    length_to_reach = fraction * self.length()\n    for segment in self.segments:\n        current_length += segment.length()\n        if current_length &gt;= length_to_reach:\n            break\n    segment_start_length = current_length - segment.length()\n    segment_fraction = (length_to_reach - segment_start_length) / segment.length()\n    position, angle = segment.get_position_and_angle_at_fraction(segment_fraction)\n    return position, angle\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ProductionLayout.length","title":"length","text":"<pre><code>length() -&gt; float\n</code></pre> <p>Return the total length of the arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def length(self) -&gt; float:\n    \"\"\"Return the total length of the arc path.\"\"\"\n    return sum([segment.length() for segment in self.segments])\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ProductionLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ProductionLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the arc.\"\"\"\n    return []\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ProductionLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the self drawing elements of the single-headed arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the self drawing elements of the single-headed arc.\"\"\"\n    drawing_elements = (\n        self.path_drawing_elements() + self.arrowhead_drawing_elements()\n    )\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ProductionLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ProductionLayout.path_drawing_elements","title":"path_drawing_elements","text":"<pre><code>path_drawing_elements() -&gt; list[Path]\n</code></pre> <p>Return the drawing elements of the single-headed arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def path_drawing_elements(self) -&gt; list[Path]:\n    \"\"\"Return the drawing elements of the single-headed arc path.\"\"\"\n    arrowhead_length = self.arrowhead_length()\n    if len(self.segments) == 1:\n        segment = (\n            self.segments[0]\n            .shortened(self.start_shorten, \"start\")\n            .shortened(self.end_shorten + arrowhead_length, \"end\")\n        )\n        actions = [\n            MoveTo(segment.p1),\n            self._make_path_action_from_segment(segment),\n        ]\n    else:\n        first_segment = self.segments[0].shortened(self.start_shorten, \"start\")\n        last_segment = self.segments[-1].shortened(\n            self.end_shorten + arrowhead_length, \"end\"\n        )\n        actions = [\n            MoveTo(first_segment.p1),\n            self._make_path_action_from_segment(first_segment),\n        ]\n        for segment in self.segments[1:-1]:\n            action = self._make_path_action_from_segment(segment)\n            actions.append(action)\n        actions.append(self._make_path_action_from_segment(last_segment))\n    path = Path(\n        actions=tuple(actions),\n        class_=f\"{type(self).__name__}_path\",\n        fill=self.path_fill,\n        filter_=self.path_filter,\n        id_=f\"{self.id_}_path\",\n        stroke=self.path_stroke,\n        stroke_dasharray=self.path_stroke_dasharray,\n        stroke_dashoffset=self.path_stroke_dashoffset,\n        stroke_width=self.path_stroke_width,\n        transform=self.path_transform,\n    )\n    return [path]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ProductionLayout.points","title":"points","text":"<pre><code>points() -&gt; list[Point]\n</code></pre> <p>Return the points of the arc path.</p> <p>An arc with no segments has no points and returns an empty list.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def points(self) -&gt; list[Point]:\n    \"\"\"Return the points of the arc path.\n\n    An arc with no segments has no points and returns an empty list.\n    \"\"\"\n    if not self.segments:\n        return []\n    points = []\n    for segment in self.segments:\n        points.append(segment.p1)\n    points.append(segment.p2)\n    return points\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ProductionLayout.start_point","title":"start_point","text":"<pre><code>start_point() -&gt; Point\n</code></pre> <p>Return the starting point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_point(self) -&gt; Point:\n    \"\"\"Return the starting point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[0]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.ProductionLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.Reactant","title":"Reactant  <code>dataclass</code>","text":"<pre><code>Reactant(*, id_: str = make_uuid4_as_str(), referred_element: EntityPool, stoichiometry: float | None = None)\n</code></pre> <p>               Bases: <code>FluxRole</code></p> <p>Reactant.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'34e3814c-83fa-401c-8174-6ab5e1f20d1f'</code> <code>referred_element</code> <code>EntityPool</code> <p>The entity pool of the flux role</p> required <code>stoichiometry</code> <code>float | None</code> <p>The stoichiometry of the flux role</p> <code>None</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.Reactant.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SBGNPDLayout","title":"SBGNPDLayout  <code>dataclass</code>","text":"<pre><code>SBGNPDLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = white, filter_: NoneValueType | Filter | None = None, height: float, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = None, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: NoneValueType | float | None = None, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float)\n</code></pre> <p>               Bases: <code>SBGNLayout</code></p> <p>SBGN PD layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'8b08254b-c9ae-4eb2-bef7-43ca1359cf5d'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> required <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>None</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the node</p> <code>None</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> required <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>angle</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Return the east anchor of the node.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>is_sublayout</code> <p>Return <code>True</code> if the layout is a sublayout of another given layout, <code>False</code> otherwise.</p> <code>label_center</code> <p>Return the label center anchor of the node.</p> <code>north</code> <p>Return the north anchor of the node.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Return the south anchor of the node.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Return the west anchor of the node.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SBGNPDLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SBGNPDLayout.angle","title":"angle","text":"<pre><code>angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SBGNPDLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SBGNPDLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SBGNPDLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SBGNPDLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SBGNPDLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SBGNPDLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SBGNPDLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SBGNPDLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SBGNPDLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Return the east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Return the east anchor of the node.\"\"\"\n    return self.own_angle(0)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SBGNPDLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SBGNPDLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SBGNPDLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SBGNPDLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SBGNPDLayout.is_sublayout","title":"is_sublayout","text":"<pre><code>is_sublayout(other: Layout, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout is a sublayout of another given layout, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def is_sublayout(\n    self, other: \"Layout\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout is a sublayout of another given layout, `False` otherwise.\"\"\"\n\n    def _is_sublist(\n        list1: list[LayoutElement],\n        list2: list[LayoutElement],\n        unordered: bool = False,\n    ) -&gt; bool:\n        if not unordered:\n            i = 0\n            for elem1 in list1:\n                elem2 = list2[i]\n                while elem2 != elem1 and i &lt; len(list2) - 1:\n                    i += 1\n                    elem2 = list2[i]\n                if not elem2 == elem1:\n                    return False\n                i += 1\n        else:\n            dlist1 = collections.defaultdict(int)\n            dlist2 = collections.defaultdict(int)\n            for elem1 in list1:\n                dlist1[elem1] += 1\n            for elem2 in list2:\n                dlist2[elem2] += 1\n            for elem in dlist1:\n                if dlist1[elem] &gt; dlist2[elem]:\n                    return False\n        return True\n\n    if self.childless() != other.childless():\n        return False\n    if flattened:\n        return _is_sublist(\n            self.flattened()[1:],\n            other.flattened()[1:],\n            unordered=unordered,\n        )\n    return _is_sublist(self.children(), other.children(), unordered=unordered)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SBGNPDLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Return the label center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Return the label center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SBGNPDLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Return the north anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Return the north anchor of the node.\"\"\"\n    return self.own_angle(90)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SBGNPDLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SBGNPDLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SBGNPDLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SBGNPDLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SBGNPDLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SBGNPDLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SBGNPDLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SBGNPDLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SBGNPDLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SBGNPDLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SBGNPDLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SBGNPDLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Return the south anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Return the south anchor of the node.\"\"\"\n    return self.own_angle(270)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SBGNPDLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SBGNPDLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SBGNPDLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SBGNPDLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SBGNPDLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SBGNPDLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Return the west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Return the west anchor of the node.\"\"\"\n    return self.own_angle(180)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SBGNPDLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SBGNPDLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SBGNPDLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SBGNPDLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SBGNPDMap","title":"SBGNPDMap  <code>dataclass</code>","text":"<pre><code>SBGNPDMap(*, id_: str = make_uuid4_as_str(), model: SBGNPDModel | None = None, layout: SBGNPDLayout | None = None, layout_model_mapping: LayoutModelMapping | None = None)\n</code></pre> <p>               Bases: <code>SBGNMap</code></p> <p>Class for SBGN-PD maps.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'946db2aa-31be-4ec2-93cf-a010d548d603'</code> <code>model</code> <code>SBGNPDModel | None</code> <p>The model of the map</p> <code>None</code> <code>layout</code> <code>SBGNPDLayout | None</code> <p>The layout of the map</p> <code>None</code> <code>layout_model_mapping</code> <code>LayoutModelMapping | None</code> <p>The layout model mapping of the map</p> <code>None</code> <p>Methods:</p> Name Description <code>get_mapping</code> <p>Return the model element or layout elements mapped to <code>map_element</code>.</p> <code>is_submap</code> <p>Return <code>True</code> if the <code>Map</code> is a submap of another given map, <code>False</code> otherwise.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SBGNPDMap.get_mapping","title":"get_mapping","text":"<pre><code>get_mapping(map_element: MapElement) -&gt; ModelElement | list[LayoutElement | frozenset[LayoutElement]] | None\n</code></pre> <p>Return the model element or layout elements mapped to <code>map_element</code>.</p> <p>The lookup is bidirectional: a layout key (a singleton or frozenset) resolves to its model element, and a model element resolves to the list of layout keys mapped to it. Forwards to <code>layout_model_mapping.get_mapping</code>.</p> <p>Returns <code>None</code> when the map has no <code>layout_model_mapping</code> (for example a layout-less map such as one read from SBML).</p> Source code in <code>src/momapy/core/map.py</code> <pre><code>def get_mapping(\n    self,\n    map_element: \"MapElement\",\n) -&gt; \"ModelElement | list[LayoutElement | frozenset[LayoutElement]] | None\":\n    \"\"\"Return the model element or layout elements mapped to `map_element`.\n\n    The lookup is bidirectional: a layout key (a singleton or frozenset)\n    resolves to its model element, and a model element resolves to the\n    list of layout keys mapped to it. Forwards to\n    `layout_model_mapping.get_mapping`.\n\n    Returns `None` when the map has no `layout_model_mapping` (for\n    example a layout-less map such as one read from SBML).\n    \"\"\"\n    if self.layout_model_mapping is None:\n        return None\n    return self.layout_model_mapping.get_mapping(map_element)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SBGNPDMap.is_submap","title":"is_submap","text":"<pre><code>is_submap(other: Map) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the <code>Map</code> is a submap of another given map, <code>False</code> otherwise.</p> <p>A complete map is never a submap of an incomplete one: if either <code>self</code> or <code>other</code> has a <code>None</code> model, layout, or layout-model mapping, this returns <code>False</code>.</p> Source code in <code>src/momapy/core/map.py</code> <pre><code>def is_submap(self, other: \"Map\") -&gt; bool:\n    \"\"\"Return `True` if the `Map` is a submap of another given map, `False` otherwise.\n\n    A complete map is never a submap of an incomplete one: if either `self`\n    or `other` has a `None` model, layout, or layout-model mapping, this\n    returns `False`.\n    \"\"\"\n    if (\n        self.model is None\n        or self.layout is None\n        or self.layout_model_mapping is None\n        or other.model is None\n        or other.layout is None\n        or other.layout_model_mapping is None\n    ):\n        return False\n    return (\n        self.model.is_submodel(other.model)\n        and self.layout.is_sublayout(other.layout)\n        and self.layout_model_mapping.is_submapping(other.layout_model_mapping)\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SBGNPDModel","title":"SBGNPDModel  <code>dataclass</code>","text":"<pre><code>SBGNPDModel(*, id_: str = make_uuid4_as_str(), entity_pools: frozenset[EntityPool] = frozenset(), processes: frozenset[Process] = frozenset(), compartments: frozenset[Compartment] = frozenset(), modulations: frozenset[Modulation] = frozenset(), logical_operators: frozenset[LogicalOperator] = frozenset(), equivalence_operators: frozenset[EquivalenceOperator] = frozenset(), submaps: frozenset[Submap] = frozenset(), tags: frozenset[Tag] = frozenset())\n</code></pre> <p>               Bases: <code>SBGNModel</code></p> <p>SBGN PD model.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'bc243ba4-2d7c-46ce-a991-0f222a6b9d81'</code> <code>entity_pools</code> <code>frozenset[EntityPool]</code> <p>The entity pools of the SBGN-PD model</p> <code>&lt;dynamic&gt;</code> <code>processes</code> <code>frozenset[Process]</code> <p>The processes of the SBGN-PD model</p> <code>&lt;dynamic&gt;</code> <code>compartments</code> <code>frozenset[Compartment]</code> <p>The compartments of the SBGN-PD model</p> <code>&lt;dynamic&gt;</code> <code>modulations</code> <code>frozenset[Modulation]</code> <p>The modulations of the SBGN-PD model</p> <code>&lt;dynamic&gt;</code> <code>logical_operators</code> <code>frozenset[LogicalOperator]</code> <p>The logical operators of the SBGN-PD model</p> <code>&lt;dynamic&gt;</code> <code>equivalence_operators</code> <code>frozenset[EquivalenceOperator]</code> <p>The equivalence operators of the SBGN-PD model</p> <code>&lt;dynamic&gt;</code> <code>submaps</code> <code>frozenset[Submap]</code> <p>The submaps of the SBGN-PD model</p> <code>&lt;dynamic&gt;</code> <code>tags</code> <code>frozenset[Tag]</code> <p>The tags of the SBGN-PD model</p> <code>&lt;dynamic&gt;</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from this <code>Model</code>.</p> <code>is_ovav</code> <p>Check if the model respects the Once a Variable Always a Variable (OVAV) rule.</p> <code>is_submodel</code> <p>Check if this model is a submodel of another model.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SBGNPDModel.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from this <code>Model</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers of the <code>Model</code>, deduplicating by object identity. The <code>Model</code> itself is not a <code>ModelElement</code> and is not included.</p> <p>This mirrors <code>ModelElement.descendants</code> and contrasts with <code>LayoutElement.descendants</code>, which walks the explicit <code>children()</code> tree (visual containment) rather than reference reachability.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order.</p> Source code in <code>src/momapy/core/model.py</code> <pre><code>def descendants(self) -&gt; list[ModelElement]:\n    \"\"\"Return every `ModelElement` reachable from this `Model`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers of the `Model`, deduplicating by\n    object identity. The `Model` itself is not a `ModelElement` and is\n    not included.\n\n    This mirrors\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and contrasts with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which walks the explicit `children()` tree (visual *containment*)\n    rather than reference reachability.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order.\n    \"\"\"\n    seen: set[int] = set()\n    result: list[ModelElement] = []\n    for field in dataclasses.fields(type(self)):\n        _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SBGNPDModel.is_ovav","title":"is_ovav","text":"<pre><code>is_ovav() -&gt; bool\n</code></pre> <p>Check if the model respects the Once a Variable Always a Variable (OVAV) rule.</p> <p>Returns:</p> Type Description <code>bool</code> <p>True if the SBGN-PD model respects the OVAV rule, False otherwise.</p> Source code in <code>src/momapy/sbgn/pd/model.py</code> <pre><code>def is_ovav(self) -&gt; bool:\n    \"\"\"Check if the model respects the Once a Variable Always a Variable (OVAV) rule.\n\n    Returns:\n        True if the SBGN-PD model respects the OVAV rule, False otherwise.\n    \"\"\"\n    subunit_cls_entity_pool_cls_mapping = {\n        MacromoleculeSubunit: Macromolecule,\n        NucleicAcidFeatureSubunit: NucleicAcidFeature,\n        ComplexSubunit: Complex,\n        SimpleChemicalSubunit: SimpleChemical,\n        MacromoleculeMultimerSubunit: MacromoleculeMultimer,\n        NucleicAcidFeatureMultimerSubunit: NucleicAcidFeatureMultimer,\n        ComplexMultimerSubunit: ComplexMultimer,\n        SimpleChemicalMultimerSubunit: SimpleChemicalMultimer,\n    }\n\n    def _check_entities(\n        entities: typing.Iterable[typing.Any],\n        entity_variables_mapping: dict[typing.Any, set[typing.Any]] | None = None,\n    ) -&gt; bool:\n        if entity_variables_mapping is None:\n            entity_variables_mapping = {}\n        for entity in entities:\n            if hasattr(entity, \"state_variables\"):\n                variables = set([sv.variable for sv in entity.state_variables])\n                attributes = []\n                for field in dataclasses.fields(entity):\n                    if field.name != \"state_variables\":\n                        attributes.append(field.name)\n                args = {attr: getattr(entity, attr) for attr in attributes}\n                if isinstance(entity, Subunit):\n                    cls = subunit_cls_entity_pool_cls_mapping[type(entity)]\n                else:\n                    cls = type(entity)\n                entity_no_svs = cls(**args)\n                if entity_no_svs not in entity_variables_mapping:\n                    entity_variables_mapping[entity_no_svs] = variables\n                else:\n                    if entity_variables_mapping[entity_no_svs] != variables:\n                        return False\n            if hasattr(entity, \"subunits\"):\n                is_ovav = _check_entities(entity.subunits, entity_variables_mapping)\n                if not is_ovav:\n                    return False\n        return True\n\n    return _check_entities(self.entity_pools)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SBGNPDModel.is_submodel","title":"is_submodel","text":"<pre><code>is_submodel(other: SBGNPDModel) -&gt; bool\n</code></pre> <p>Check if this model is a submodel of another model.</p> <p>Parameters:</p> Name Type Description Default <code>other</code> <code>SBGNPDModel</code> <p>Another SBGN-PD model to compare against.</p> required <p>Returns:</p> Type Description <code>bool</code> <p>True if this model is a submodel of <code>other</code>, False otherwise.</p> Source code in <code>src/momapy/sbgn/pd/model.py</code> <pre><code>def is_submodel(self, other: \"SBGNPDModel\") -&gt; bool:\n    \"\"\"Check if this model is a submodel of another model.\n\n    Args:\n        other: Another SBGN-PD model to compare against.\n\n    Returns:\n        True if this model is a submodel of `other`, False otherwise.\n    \"\"\"\n    return (\n        self.entity_pools.issubset(other.entity_pools)\n        and self.processes.issubset(other.processes)\n        and self.compartments.issubset(other.compartments)\n        and self.modulations.issubset(other.modulations)\n        and self.logical_operators.issubset(other.logical_operators)\n        and self.equivalence_operators.issubset(other.equivalence_operators)\n        and self.submaps.issubset(other.submaps)\n        and self.tags.issubset(other.tags)\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemical","title":"SimpleChemical  <code>dataclass</code>","text":"<pre><code>SimpleChemical(*, id_: str = make_uuid4_as_str(), compartment: Compartment | None = None, label: str | None = None, state_variables: frozenset[StateVariable] = frozenset(), units_of_information: frozenset[UnitOfInformation] = frozenset())\n</code></pre> <p>               Bases: <code>EntityPool</code></p> <p>Simple chemical.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'27c760ea-ffa3-441c-81f7-10b1985dcc46'</code> <code>compartment</code> <code>Compartment | None</code> <p>The compartment of the entity pool</p> <code>None</code> <code>label</code> <code>str | None</code> <p>The label of the simple chemical</p> <code>None</code> <code>state_variables</code> <code>frozenset[StateVariable]</code> <p>The state variables of the simple chemical</p> <code>&lt;dynamic&gt;</code> <code>units_of_information</code> <code>frozenset[UnitOfInformation]</code> <p>The units of information of the simple chemical</p> <code>&lt;dynamic&gt;</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemical.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalLayout","title":"SimpleChemicalLayout  <code>dataclass</code>","text":"<pre><code>SimpleChemicalLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = white, filter_: NoneValueType | Filter | None = None, height: float = 30.0, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = 1.25, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 30.0)\n</code></pre> <p>               Bases: <code>_SimpleMixin</code>, <code>SBGNNode</code></p> <p>Simple chemical layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'5e6cf2d1-7b4c-405d-a4a6-5858802f9349'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>30.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The stroke width of the node</p> <code>1.25</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>30.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>angle</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Return the east anchor of the node.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Return the label center anchor of the node.</p> <code>north</code> <p>Return the north anchor of the node.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Return the south anchor of the node.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Return the west anchor of the node.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalLayout.angle","title":"angle","text":"<pre><code>angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Return the east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Return the east anchor of the node.\"\"\"\n    return self.own_angle(0)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Return the label center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Return the label center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Return the north anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Return the north anchor of the node.\"\"\"\n    return self.own_angle(90)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Return the south anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Return the south anchor of the node.\"\"\"\n    return self.own_angle(270)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Return the west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Return the west anchor of the node.\"\"\"\n    return self.own_angle(180)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalMultimer","title":"SimpleChemicalMultimer  <code>dataclass</code>","text":"<pre><code>SimpleChemicalMultimer(*, id_: str = make_uuid4_as_str(), compartment: Compartment | None = None, label: str | None = None, state_variables: frozenset[StateVariable] = frozenset(), units_of_information: frozenset[UnitOfInformation] = frozenset(), subunits: frozenset[Subunit] = frozenset(), cardinality: int | None = None)\n</code></pre> <p>               Bases: <code>Multimer</code></p> <p>Simple chemical multimer.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'d363a586-b7e0-4728-af01-838bd8dd61d1'</code> <code>compartment</code> <code>Compartment | None</code> <p>The compartment of the entity pool</p> <code>None</code> <code>label</code> <code>str | None</code> <p>The label of the complex</p> <code>None</code> <code>state_variables</code> <code>frozenset[StateVariable]</code> <p>The state variables of the complex</p> <code>&lt;dynamic&gt;</code> <code>units_of_information</code> <code>frozenset[UnitOfInformation]</code> <p>The units of information of the complex</p> <code>&lt;dynamic&gt;</code> <code>subunits</code> <code>frozenset[Subunit]</code> <p>The subunits of the complex</p> <code>&lt;dynamic&gt;</code> <code>cardinality</code> <code>int | None</code> <p>The cardinality of the multimer</p> <code>None</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalMultimer.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalMultimerLayout","title":"SimpleChemicalMultimerLayout  <code>dataclass</code>","text":"<pre><code>SimpleChemicalMultimerLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = white, filter_: NoneValueType | Filter | None = None, height: float = 30.0, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = 1.25, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 30.0, offset: float = 3.0, subunits_stroke: tuple[NoneValueType | Color, ...] | None = None, subunits_stroke_width: tuple[NoneValueType | float, ...] | None = None, subunits_stroke_dasharray: tuple[NoneValueType | tuple[float, ...], ...] | None = None, subunits_stroke_dashoffset: tuple[float, ...] | None = None, subunits_fill: tuple[NoneValueType | Color, ...] | None = None, subunits_transform: tuple[NoneValueType | tuple[Transformation, ...], ...] | None = None, subunits_filter: tuple[NoneValueType | Filter, ...] | None = None)\n</code></pre> <p>               Bases: <code>_MultiMixin</code>, <code>SBGNNode</code></p> <p>Simple chemical multimer layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'7d7634dc-8176-4430-9b2f-943c2282e3ff'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>30.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The stroke width of the node</p> <code>1.25</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>30.0</code> <code>offset</code> <code>float</code> <p>Offset distance between stacked units.</p> <code>3.0</code> <code>subunits_stroke</code> <code>tuple[NoneValueType | Color, ...] | None</code> <p>Tuple of stroke colors for each subunit.</p> <code>None</code> <code>subunits_stroke_width</code> <code>tuple[NoneValueType | float, ...] | None</code> <p>Tuple of stroke widths for each subunit.</p> <code>None</code> <code>subunits_stroke_dasharray</code> <code>tuple[NoneValueType | tuple[float, ...], ...] | None</code> <p>Tuple of dash patterns for each subunit.</p> <code>None</code> <code>subunits_stroke_dashoffset</code> <code>tuple[float, ...] | None</code> <p>Tuple of dash offsets for each subunit.</p> <code>None</code> <code>subunits_fill</code> <code>tuple[NoneValueType | Color, ...] | None</code> <p>Tuple of fill colors for each subunit.</p> <code>None</code> <code>subunits_transform</code> <code>tuple[NoneValueType | tuple[Transformation, ...], ...] | None</code> <p>Tuple of transformations for each subunit.</p> <code>None</code> <code>subunits_filter</code> <code>tuple[NoneValueType | Filter, ...] | None</code> <p>Tuple of filters for each subunit.</p> <code>None</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>angle</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Return the east anchor of the node.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Get the center position for the node label.</p> <code>north</code> <p>Return the north anchor of the node.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Return the south anchor of the node.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Return the west anchor of the node.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalMultimerLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalMultimerLayout.angle","title":"angle","text":"<pre><code>angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalMultimerLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalMultimerLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalMultimerLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalMultimerLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalMultimerLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalMultimerLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalMultimerLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalMultimerLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalMultimerLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Return the east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Return the east anchor of the node.\"\"\"\n    return self.own_angle(0)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalMultimerLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalMultimerLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalMultimerLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalMultimerLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalMultimerLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Get the center position for the node label.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point where the label should be centered.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Get the center position for the node label.\n\n    Returns:\n        Point where the label should be centered.\n    \"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2 - (self._n - 1) * self.offset,\n        self.height / 2 - height / 2 - (self._n - 1) * self.offset,\n    )\n    return self._make_subunit_shape(\n        position=position, width=width, height=height\n    ).position\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalMultimerLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Return the north anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Return the north anchor of the node.\"\"\"\n    return self.own_angle(90)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalMultimerLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalMultimerLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalMultimerLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalMultimerLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalMultimerLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalMultimerLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalMultimerLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalMultimerLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalMultimerLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalMultimerLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalMultimerLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalMultimerLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Return the south anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Return the south anchor of the node.\"\"\"\n    return self.own_angle(270)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalMultimerLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalMultimerLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalMultimerLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalMultimerLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalMultimerLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalMultimerLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Return the west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Return the west anchor of the node.\"\"\"\n    return self.own_angle(180)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalMultimerLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalMultimerLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalMultimerLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalMultimerLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalMultimerSubunit","title":"SimpleChemicalMultimerSubunit  <code>dataclass</code>","text":"<pre><code>SimpleChemicalMultimerSubunit(*, id_: str = make_uuid4_as_str(), label: str | None = None, state_variables: frozenset[StateVariable] = frozenset(), units_of_information: frozenset[UnitOfInformation] = frozenset(), subunits: frozenset[Subunit] = frozenset(), cardinality: int | None = None)\n</code></pre> <p>               Bases: <code>MultimerSubunit</code></p> <p>Simple chemical multimer subunit.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'838040a9-73c7-466e-bdbb-eacc09f0da88'</code> <code>label</code> <code>str | None</code> <p>The label of the subunit</p> <code>None</code> <code>state_variables</code> <code>frozenset[StateVariable]</code> <p>The state variables of the complex subunit</p> <code>&lt;dynamic&gt;</code> <code>units_of_information</code> <code>frozenset[UnitOfInformation]</code> <p>The units of information of the complex subunit</p> <code>&lt;dynamic&gt;</code> <code>subunits</code> <code>frozenset[Subunit]</code> <p>The subunits of the complex subunit</p> <code>&lt;dynamic&gt;</code> <code>cardinality</code> <code>int | None</code> <p>The cardinality of the multimer subunit</p> <code>None</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalMultimerSubunit.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalMultimerSubunitLayout","title":"SimpleChemicalMultimerSubunitLayout  <code>dataclass</code>","text":"<pre><code>SimpleChemicalMultimerSubunitLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = white, filter_: NoneValueType | Filter | None = None, height: float = 30.0, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = 1.25, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 60.0, offset: float = 3.0, subunits_stroke: tuple[NoneValueType | Color, ...] | None = None, subunits_stroke_width: tuple[NoneValueType | float, ...] | None = None, subunits_stroke_dasharray: tuple[NoneValueType | tuple[float, ...], ...] | None = None, subunits_stroke_dashoffset: tuple[float, ...] | None = None, subunits_fill: tuple[NoneValueType | Color, ...] | None = None, subunits_transform: tuple[NoneValueType | tuple[Transformation, ...], ...] | None = None, subunits_filter: tuple[NoneValueType | Filter, ...] | None = None)\n</code></pre> <p>               Bases: <code>_MultiMixin</code>, <code>SBGNNode</code></p> <p>Simple chemical multimer subunit layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'18e7aba9-14b2-48ea-b3d4-0fcf9f8871d7'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>30.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The stroke width of the node</p> <code>1.25</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>60.0</code> <code>offset</code> <code>float</code> <p>Offset distance between stacked units.</p> <code>3.0</code> <code>subunits_stroke</code> <code>tuple[NoneValueType | Color, ...] | None</code> <p>Tuple of stroke colors for each subunit.</p> <code>None</code> <code>subunits_stroke_width</code> <code>tuple[NoneValueType | float, ...] | None</code> <p>Tuple of stroke widths for each subunit.</p> <code>None</code> <code>subunits_stroke_dasharray</code> <code>tuple[NoneValueType | tuple[float, ...], ...] | None</code> <p>Tuple of dash patterns for each subunit.</p> <code>None</code> <code>subunits_stroke_dashoffset</code> <code>tuple[float, ...] | None</code> <p>Tuple of dash offsets for each subunit.</p> <code>None</code> <code>subunits_fill</code> <code>tuple[NoneValueType | Color, ...] | None</code> <p>Tuple of fill colors for each subunit.</p> <code>None</code> <code>subunits_transform</code> <code>tuple[NoneValueType | tuple[Transformation, ...], ...] | None</code> <p>Tuple of transformations for each subunit.</p> <code>None</code> <code>subunits_filter</code> <code>tuple[NoneValueType | Filter, ...] | None</code> <p>Tuple of filters for each subunit.</p> <code>None</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>angle</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Return the east anchor of the node.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Get the center position for the node label.</p> <code>north</code> <p>Return the north anchor of the node.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Return the south anchor of the node.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Return the west anchor of the node.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalMultimerSubunitLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalMultimerSubunitLayout.angle","title":"angle","text":"<pre><code>angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalMultimerSubunitLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalMultimerSubunitLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalMultimerSubunitLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalMultimerSubunitLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalMultimerSubunitLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalMultimerSubunitLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalMultimerSubunitLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalMultimerSubunitLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalMultimerSubunitLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Return the east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Return the east anchor of the node.\"\"\"\n    return self.own_angle(0)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalMultimerSubunitLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalMultimerSubunitLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalMultimerSubunitLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalMultimerSubunitLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalMultimerSubunitLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Get the center position for the node label.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point where the label should be centered.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Get the center position for the node label.\n\n    Returns:\n        Point where the label should be centered.\n    \"\"\"\n    width = self.width - self.offset * (self._n - 1)\n    height = self.height - self.offset * (self._n - 1)\n    position = self.position + (\n        self.width / 2 - width / 2 - (self._n - 1) * self.offset,\n        self.height / 2 - height / 2 - (self._n - 1) * self.offset,\n    )\n    return self._make_subunit_shape(\n        position=position, width=width, height=height\n    ).position\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalMultimerSubunitLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Return the north anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Return the north anchor of the node.\"\"\"\n    return self.own_angle(90)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalMultimerSubunitLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalMultimerSubunitLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalMultimerSubunitLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalMultimerSubunitLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalMultimerSubunitLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalMultimerSubunitLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalMultimerSubunitLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalMultimerSubunitLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalMultimerSubunitLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalMultimerSubunitLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalMultimerSubunitLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalMultimerSubunitLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Return the south anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Return the south anchor of the node.\"\"\"\n    return self.own_angle(270)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalMultimerSubunitLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalMultimerSubunitLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalMultimerSubunitLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalMultimerSubunitLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalMultimerSubunitLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalMultimerSubunitLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Return the west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Return the west anchor of the node.\"\"\"\n    return self.own_angle(180)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalMultimerSubunitLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalMultimerSubunitLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalMultimerSubunitLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalMultimerSubunitLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalSubunit","title":"SimpleChemicalSubunit  <code>dataclass</code>","text":"<pre><code>SimpleChemicalSubunit(*, id_: str = make_uuid4_as_str(), label: str | None = None, state_variables: frozenset[StateVariable] = frozenset(), units_of_information: frozenset[UnitOfInformation] = frozenset())\n</code></pre> <p>               Bases: <code>Subunit</code></p> <p>Simple chemical subunit.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'9aae762e-763e-4ac4-a52b-b5b829e4b3ef'</code> <code>label</code> <code>str | None</code> <p>The label of the subunit</p> <code>None</code> <code>state_variables</code> <code>frozenset[StateVariable]</code> <p>The state variables of the simple chemical subunit</p> <code>&lt;dynamic&gt;</code> <code>units_of_information</code> <code>frozenset[UnitOfInformation]</code> <p>The units of information of the simple chemical subunit</p> <code>&lt;dynamic&gt;</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalSubunit.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalSubunitLayout","title":"SimpleChemicalSubunitLayout  <code>dataclass</code>","text":"<pre><code>SimpleChemicalSubunitLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = white, filter_: NoneValueType | Filter | None = None, height: float = 30.0, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = 1.25, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 30.0)\n</code></pre> <p>               Bases: <code>_SimpleMixin</code>, <code>SBGNNode</code></p> <p>Simple chemical subunit layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'ae4f25b3-457f-49e5-bfe3-11732b5adce2'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>30.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The stroke width of the node</p> <code>1.25</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>30.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>angle</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Return the east anchor of the node.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Return the label center anchor of the node.</p> <code>north</code> <p>Return the north anchor of the node.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Return the south anchor of the node.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Return the west anchor of the node.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalSubunitLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalSubunitLayout.angle","title":"angle","text":"<pre><code>angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalSubunitLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalSubunitLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalSubunitLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalSubunitLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalSubunitLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalSubunitLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalSubunitLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalSubunitLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalSubunitLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Return the east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Return the east anchor of the node.\"\"\"\n    return self.own_angle(0)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalSubunitLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalSubunitLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalSubunitLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalSubunitLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalSubunitLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Return the label center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Return the label center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalSubunitLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Return the north anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Return the north anchor of the node.\"\"\"\n    return self.own_angle(90)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalSubunitLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalSubunitLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalSubunitLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalSubunitLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalSubunitLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalSubunitLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalSubunitLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalSubunitLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalSubunitLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalSubunitLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalSubunitLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalSubunitLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Return the south anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Return the south anchor of the node.\"\"\"\n    return self.own_angle(270)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalSubunitLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalSubunitLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalSubunitLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalSubunitLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalSubunitLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalSubunitLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Return the west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Return the west anchor of the node.\"\"\"\n    return self.own_angle(180)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalSubunitLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalSubunitLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalSubunitLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SimpleChemicalSubunitLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.StateVariable","title":"StateVariable  <code>dataclass</code>","text":"<pre><code>StateVariable(*, id_: str = make_uuid4_as_str(), variable: str | None = None, value: str | None = None, order: int | None = None)\n</code></pre> <p>               Bases: <code>SBGNAuxiliaryUnit</code></p> <p>State variable.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'e1a349af-4125-402a-83f5-750d0e81a094'</code> <code>variable</code> <code>str | None</code> <p>The variable of the state variable</p> <code>None</code> <code>value</code> <code>str | None</code> <p>The value of the state variable</p> <code>None</code> <code>order</code> <code>int | None</code> <p>The order of the state variable. This is used to distinguish between two or more state variables with undefined variable (i.e., set to <code>None</code>)</p> <code>None</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.StateVariable.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.StateVariableLayout","title":"StateVariableLayout  <code>dataclass</code>","text":"<pre><code>StateVariableLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = white, filter_: NoneValueType | Filter | None = None, height: float = 12.0, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = 1.25, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 12.0)\n</code></pre> <p>               Bases: <code>_SimpleMixin</code>, <code>SBGNNode</code></p> <p>State variable layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'36fb414d-237b-4aec-ba3d-4f94f5aba23c'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the state variable layout</p> <code>12.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The stroke width of the node</p> <code>1.25</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the state variable layout</p> <code>12.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>angle</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Return the east anchor of the node.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Return the label center anchor of the node.</p> <code>north</code> <p>Return the north anchor of the node.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Return the south anchor of the node.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Return the west anchor of the node.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.StateVariableLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.StateVariableLayout.angle","title":"angle","text":"<pre><code>angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.StateVariableLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.StateVariableLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.StateVariableLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.StateVariableLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.StateVariableLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.StateVariableLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.StateVariableLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.StateVariableLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.StateVariableLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Return the east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Return the east anchor of the node.\"\"\"\n    return self.own_angle(0)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.StateVariableLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.StateVariableLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.StateVariableLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.StateVariableLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.StateVariableLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Return the label center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Return the label center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.StateVariableLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Return the north anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Return the north anchor of the node.\"\"\"\n    return self.own_angle(90)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.StateVariableLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.StateVariableLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.StateVariableLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.StateVariableLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.StateVariableLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.StateVariableLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.StateVariableLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.StateVariableLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.StateVariableLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.StateVariableLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.StateVariableLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.StateVariableLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Return the south anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Return the south anchor of the node.\"\"\"\n    return self.own_angle(270)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.StateVariableLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.StateVariableLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.StateVariableLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.StateVariableLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.StateVariableLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.StateVariableLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Return the west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Return the west anchor of the node.\"\"\"\n    return self.own_angle(180)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.StateVariableLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.StateVariableLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.StateVariableLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.StateVariableLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.Stimulation","title":"Stimulation  <code>dataclass</code>","text":"<pre><code>Stimulation(*, id_: str = make_uuid4_as_str(), source: EntityPool | LogicalOperator, target: Process)\n</code></pre> <p>               Bases: <code>Modulation</code></p> <p>Stimulation.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'5dd19c57-ec41-4d7d-a947-22d6295a808a'</code> <code>source</code> <code>EntityPool | LogicalOperator</code> <p>The source of the modulation</p> required <code>target</code> <code>Process</code> <p>The target of the modulation</p> required <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.Stimulation.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.StimulationLayout","title":"StimulationLayout  <code>dataclass</code>","text":"<pre><code>StimulationLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, end_shorten: float = 0.0, fill: NoneValueType | Color | None = None, filter_: NoneValueType | Filter | None = None, path_fill: NoneValueType | Color | None = NoneValue, path_filter: NoneValueType | Filter | None = None, path_stroke: NoneValueType | Color | None = black, path_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, path_stroke_dashoffset: NoneValueType | float | None = None, path_stroke_width: float | None = 1.25, path_transform: NoneValueType | tuple[Transformation, ...] | None = None, stroke: NoneValueType | Color | None = None, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: NoneValueType | float | None = None, segments: tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...] = tuple(), source: LayoutElement | None = None, start_shorten: float = 0.0, target: LayoutElement | None = None, transform: NoneValueType | tuple[Transformation, ...] | None = None, arrowhead_fill: NoneValueType | Color | None = white, arrowhead_filter: NoneValueType | Filter | None = None, arrowhead_stroke: NoneValueType | Color | None = black, arrowhead_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, arrowhead_stroke_dashoffset: NoneValueType | float | None = None, arrowhead_stroke_width: float | None = 1.25, arrowhead_transform: NoneValueType | tuple[Transformation, ...] | None = None, arrowhead_height: float = 10.0, arrowhead_width: float = 10.0)\n</code></pre> <p>               Bases: <code>SBGNSingleHeadedArc</code></p> <p>Stimulation layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'d5589945-3a18-417f-831c-dff4fcce2f09'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>end_shorten</code> <code>float</code> <p>The length the end of the arc will be shorten by</p> <code>0.0</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the arc</p> <code>None</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the arc</p> <code>None</code> <code>path_fill</code> <code>NoneValueType | Color | None</code> <p>The path fill color of the arc</p> <code>&lt;momapy.drawing.NoneValueType object at 0x7f8b17323e00&gt;</code> <code>path_filter</code> <code>NoneValueType | Filter | None</code> <p>The path filter of the arc</p> <code>None</code> <code>path_stroke</code> <code>NoneValueType | Color | None</code> <p>The path stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>path_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The path stroke dasharray of the arc</p> <code>None</code> <code>path_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The path stroke dashoffset of the arc</p> <code>None</code> <code>path_stroke_width</code> <code>float | None</code> <p>The path stroke width of the arc</p> <code>1.25</code> <code>path_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The path transform of the arc</p> <code>None</code> <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the arc</p> <code>None</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the arc</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the arc</p> <code>None</code> <code>stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the arc</p> <code>None</code> <code>segments</code> <code>tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...]</code> <p>The path segments of the arc</p> <code>&lt;dynamic&gt;</code> <code>source</code> <code>LayoutElement | None</code> <p>The source of the arc</p> <code>None</code> <code>start_shorten</code> <code>float</code> <p>The length the start of the arc will be shorten by</p> <code>0.0</code> <code>target</code> <code>LayoutElement | None</code> <p>The target of the arc</p> <code>None</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the arc</p> <code>None</code> <code>arrowhead_fill</code> <code>NoneValueType | Color | None</code> <p>The arrowhead fill color of the arc</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>arrowhead_filter</code> <code>NoneValueType | Filter | None</code> <p>The arrowhead filter of the arc</p> <code>None</code> <code>arrowhead_stroke</code> <code>NoneValueType | Color | None</code> <p>The arrowhead stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>arrowhead_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The arrowhead stroke dasharray of the arc</p> <code>None</code> <code>arrowhead_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The arrowhead stroke dashoffset of the arc</p> <code>None</code> <code>arrowhead_stroke_width</code> <code>float | None</code> <p>The arrowhead stroke width of the arc</p> <code>1.25</code> <code>arrowhead_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The arrowhead transform of the arc</p> <code>None</code> <code>arrowhead_height</code> <code>float</code> <p>The height of the arrowhead</p> <code>10.0</code> <code>arrowhead_width</code> <code>float</code> <p>The width of the arrowhead</p> <code>10.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>arrowhead_base</code> <p>Return the arrowhead base anchor point of the single-headed arc.</p> <code>arrowhead_bbox</code> <p>Return the bounding box of the single-headed arc arrowhead.</p> <code>arrowhead_border</code> <p>Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.</p> <code>arrowhead_drawing_elements</code> <p>Return the drawing elements of the single-headed arc arrowhead.</p> <code>arrowhead_length</code> <p>Return the length of the single-headed arc arrowhead.</p> <code>arrowhead_tip</code> <p>Return the arrowhead tip anchor point of the single-headed arc.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>childless</code> <p>Return a copy of the arc with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>end_point</code> <p>Return the ending point of the arc.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>fraction</code> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <code>length</code> <p>Return the total length of the arc path.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_children</code> <p>Return the self children of the arc.</p> <code>own_drawing_elements</code> <p>Return the self drawing elements of the single-headed arc.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>path_drawing_elements</code> <p>Return the drawing elements of the single-headed arc path.</p> <code>points</code> <p>Return the points of the arc path.</p> <code>start_point</code> <p>Return the starting point of the arc.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.StimulationLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.StimulationLayout.arrowhead_base","title":"arrowhead_base","text":"<pre><code>arrowhead_base() -&gt; Point\n</code></pre> <p>Return the arrowhead base anchor point of the single-headed arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_base(self) -&gt; Point:\n    \"\"\"Return the arrowhead base anchor point of the single-headed arc.\"\"\"\n    arrowhead_length = self.arrowhead_length()\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return self.arrowhead_tip() - (arrowhead_length, 0)\n    fraction = 1 - (arrowhead_length + self.end_shorten) / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.StimulationLayout.arrowhead_bbox","title":"arrowhead_bbox","text":"<pre><code>arrowhead_bbox() -&gt; Bbox\n</code></pre> <p>Return the bounding box of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_bbox(self) -&gt; Bbox:\n    \"\"\"Return the bounding box of the single-headed arc arrowhead.\"\"\"\n    return get_drawing_elements_bbox(self.arrowhead_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.StimulationLayout.arrowhead_border","title":"arrowhead_border","text":"<pre><code>arrowhead_border(point: Point) -&gt; Point\n</code></pre> <p>Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_border(self, point: Point) -&gt; Point:\n    \"\"\"Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    point = get_drawing_elements_border(self.arrowhead_drawing_elements(), point)\n    if point is None:\n        return self.arrowhead_tip()\n    return point\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.StimulationLayout.arrowhead_drawing_elements","title":"arrowhead_drawing_elements","text":"<pre><code>arrowhead_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_drawing_elements(\n    self,\n) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the single-headed arc arrowhead.\"\"\"\n    drawing_elements = self._arrowhead_border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_arrowhead\",\n        elements=tuple(drawing_elements),\n        fill=self.arrowhead_fill,\n        filter_=self.arrowhead_filter,\n        id_=f\"{self.id_}_arrowhead\",\n        stroke=self.arrowhead_stroke,\n        stroke_dasharray=self.arrowhead_stroke_dasharray,\n        stroke_dashoffset=self.arrowhead_stroke_dashoffset,\n        stroke_width=self.arrowhead_stroke_width,\n        transform=self.arrowhead_transform,\n    )\n    transformation = self._get_arrowhead_transformation()\n    group = group.transformed(transformation)\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.StimulationLayout.arrowhead_length","title":"arrowhead_length","text":"<pre><code>arrowhead_length() -&gt; float\n</code></pre> <p>Return the length of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_length(self) -&gt; float:\n    \"\"\"Return the length of the single-headed arc arrowhead.\"\"\"\n    bbox = get_drawing_elements_bbox(self._arrowhead_border_drawing_elements())\n    if math.isnan(bbox.width):\n        return 0.0\n    return bbox.east().x\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.StimulationLayout.arrowhead_tip","title":"arrowhead_tip","text":"<pre><code>arrowhead_tip() -&gt; Point\n</code></pre> <p>Return the arrowhead tip anchor point of the single-headed arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_tip(self) -&gt; Point:\n    \"\"\"Return the arrowhead tip anchor point of the single-headed arc.\"\"\"\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return segment.p2\n    fraction = 1 - self.end_shorten / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.StimulationLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.StimulationLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the arc with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the arc with no children.\"\"\"\n    return dataclasses.replace(self, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.StimulationLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.StimulationLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.StimulationLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.StimulationLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.StimulationLayout.end_point","title":"end_point","text":"<pre><code>end_point() -&gt; Point\n</code></pre> <p>Return the ending point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_point(self) -&gt; Point:\n    \"\"\"Return the ending point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[-1]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.StimulationLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.StimulationLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.StimulationLayout.fraction","title":"fraction","text":"<pre><code>fraction(fraction: float) -&gt; tuple[Point, float]\n</code></pre> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def fraction(self, fraction: float) -&gt; tuple[Point, float]:\n    \"\"\"Return the position and angle on the arc at a given fraction (of the total arc length).\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    current_length = 0\n    length_to_reach = fraction * self.length()\n    for segment in self.segments:\n        current_length += segment.length()\n        if current_length &gt;= length_to_reach:\n            break\n    segment_start_length = current_length - segment.length()\n    segment_fraction = (length_to_reach - segment_start_length) / segment.length()\n    position, angle = segment.get_position_and_angle_at_fraction(segment_fraction)\n    return position, angle\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.StimulationLayout.length","title":"length","text":"<pre><code>length() -&gt; float\n</code></pre> <p>Return the total length of the arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def length(self) -&gt; float:\n    \"\"\"Return the total length of the arc path.\"\"\"\n    return sum([segment.length() for segment in self.segments])\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.StimulationLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.StimulationLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the arc.\"\"\"\n    return []\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.StimulationLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the self drawing elements of the single-headed arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the self drawing elements of the single-headed arc.\"\"\"\n    drawing_elements = (\n        self.path_drawing_elements() + self.arrowhead_drawing_elements()\n    )\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.StimulationLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.StimulationLayout.path_drawing_elements","title":"path_drawing_elements","text":"<pre><code>path_drawing_elements() -&gt; list[Path]\n</code></pre> <p>Return the drawing elements of the single-headed arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def path_drawing_elements(self) -&gt; list[Path]:\n    \"\"\"Return the drawing elements of the single-headed arc path.\"\"\"\n    arrowhead_length = self.arrowhead_length()\n    if len(self.segments) == 1:\n        segment = (\n            self.segments[0]\n            .shortened(self.start_shorten, \"start\")\n            .shortened(self.end_shorten + arrowhead_length, \"end\")\n        )\n        actions = [\n            MoveTo(segment.p1),\n            self._make_path_action_from_segment(segment),\n        ]\n    else:\n        first_segment = self.segments[0].shortened(self.start_shorten, \"start\")\n        last_segment = self.segments[-1].shortened(\n            self.end_shorten + arrowhead_length, \"end\"\n        )\n        actions = [\n            MoveTo(first_segment.p1),\n            self._make_path_action_from_segment(first_segment),\n        ]\n        for segment in self.segments[1:-1]:\n            action = self._make_path_action_from_segment(segment)\n            actions.append(action)\n        actions.append(self._make_path_action_from_segment(last_segment))\n    path = Path(\n        actions=tuple(actions),\n        class_=f\"{type(self).__name__}_path\",\n        fill=self.path_fill,\n        filter_=self.path_filter,\n        id_=f\"{self.id_}_path\",\n        stroke=self.path_stroke,\n        stroke_dasharray=self.path_stroke_dasharray,\n        stroke_dashoffset=self.path_stroke_dashoffset,\n        stroke_width=self.path_stroke_width,\n        transform=self.path_transform,\n    )\n    return [path]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.StimulationLayout.points","title":"points","text":"<pre><code>points() -&gt; list[Point]\n</code></pre> <p>Return the points of the arc path.</p> <p>An arc with no segments has no points and returns an empty list.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def points(self) -&gt; list[Point]:\n    \"\"\"Return the points of the arc path.\n\n    An arc with no segments has no points and returns an empty list.\n    \"\"\"\n    if not self.segments:\n        return []\n    points = []\n    for segment in self.segments:\n        points.append(segment.p1)\n    points.append(segment.p2)\n    return points\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.StimulationLayout.start_point","title":"start_point","text":"<pre><code>start_point() -&gt; Point\n</code></pre> <p>Return the starting point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_point(self) -&gt; Point:\n    \"\"\"Return the starting point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[0]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.StimulationLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.StoichiometricProcess","title":"StoichiometricProcess  <code>dataclass</code>","text":"<pre><code>StoichiometricProcess(*, id_: str = make_uuid4_as_str(), reactants: frozenset[Reactant] = frozenset(), products: frozenset[Product] = frozenset(), reversible: bool = False, has_external_source: bool = False, has_external_sink: bool = False)\n</code></pre> <p>               Bases: <code>Process</code></p> <p>Abstract base class for stoichiometric processes.</p> <p>SBGN PD's empty-set glyph (used as a source-and-sink for unspecified external flux) is not represented as a member of <code>reactants</code> or <code>products</code>. Instead, it is encoded as the boolean flags <code>has_external_source</code> (an empty-set on the reactant side) and <code>has_external_sink</code> (an empty-set on the product side).</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'ce6b1940-6efa-4902-9b54-a2eedccb8960'</code> <code>reactants</code> <code>frozenset[Reactant]</code> <p>The reactants of the stoichiometric process</p> <code>&lt;dynamic&gt;</code> <code>products</code> <code>frozenset[Product]</code> <p>The products of the stoichiometric process</p> <code>&lt;dynamic&gt;</code> <code>reversible</code> <code>bool</code> <p>Whether the stoichiometric process is reversible or not</p> <code>False</code> <code>has_external_source</code> <code>bool</code> <p>Whether the process has an unspecified external source (an empty-set / source-and-sink reactant in SBGN PD).</p> <code>False</code> <code>has_external_sink</code> <code>bool</code> <p>Whether the process has an unspecified external sink (an empty-set / source-and-sink product in SBGN PD).</p> <code>False</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.StoichiometricProcess.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.Submap","title":"Submap  <code>dataclass</code>","text":"<pre><code>Submap(*, id_: str = make_uuid4_as_str(), label: str | None = None, terminals: frozenset[Terminal] = frozenset())\n</code></pre> <p>               Bases: <code>SBGNModelElement</code></p> <p>Submap.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'61b9b793-299b-403a-b440-f3ad294097fb'</code> <code>label</code> <code>str | None</code> <p>The label of the submap</p> <code>None</code> <code>terminals</code> <code>frozenset[Terminal]</code> <p>The terminals of the submap</p> <code>&lt;dynamic&gt;</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.Submap.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SubmapLayout","title":"SubmapLayout  <code>dataclass</code>","text":"<pre><code>SubmapLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = white, filter_: NoneValueType | Filter | None = None, height: float = 80.0, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float = 2.25, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 80.0)\n</code></pre> <p>               Bases: <code>_SimpleMixin</code>, <code>SBGNNode</code></p> <p>Submap layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'3088090b-fcfc-4214-8ee9-1c3160bb55fb'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>80.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float</code> <p>The stroke width of the node</p> <code>2.25</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>80.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>angle</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Return the east anchor of the node.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Return the label center anchor of the node.</p> <code>north</code> <p>Return the north anchor of the node.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Return the south anchor of the node.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Return the west anchor of the node.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SubmapLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SubmapLayout.angle","title":"angle","text":"<pre><code>angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SubmapLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SubmapLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SubmapLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SubmapLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SubmapLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SubmapLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SubmapLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SubmapLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SubmapLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Return the east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Return the east anchor of the node.\"\"\"\n    return self.own_angle(0)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SubmapLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SubmapLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SubmapLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SubmapLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SubmapLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Return the label center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Return the label center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SubmapLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Return the north anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Return the north anchor of the node.\"\"\"\n    return self.own_angle(90)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SubmapLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SubmapLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SubmapLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SubmapLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SubmapLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SubmapLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SubmapLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SubmapLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SubmapLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SubmapLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SubmapLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SubmapLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Return the south anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Return the south anchor of the node.\"\"\"\n    return self.own_angle(270)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SubmapLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SubmapLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SubmapLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SubmapLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SubmapLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SubmapLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Return the west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Return the west anchor of the node.\"\"\"\n    return self.own_angle(180)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SubmapLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SubmapLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SubmapLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.SubmapLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.Subunit","title":"Subunit  <code>dataclass</code>","text":"<pre><code>Subunit(*, id_: str = make_uuid4_as_str(), label: str | None = None)\n</code></pre> <p>               Bases: <code>SBGNAuxiliaryUnit</code></p> <p>Abstract base class for subunits.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'cba9c0a1-61ee-4c4c-a831-15458e2cb3e5'</code> <code>label</code> <code>str | None</code> <p>The label of the subunit</p> <code>None</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.Subunit.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.Tag","title":"Tag  <code>dataclass</code>","text":"<pre><code>Tag(*, id_: str = make_uuid4_as_str(), label: str | None = None, referred_element: TagReference | None = None)\n</code></pre> <p>               Bases: <code>SBGNModelElement</code></p> <p>Tag.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'7a259157-dd15-415a-a643-26d9d7f17fb9'</code> <code>label</code> <code>str | None</code> <p>The label of the tag</p> <code>None</code> <code>referred_element</code> <code>TagReference | None</code> <p>The element referred to by the tag</p> <code>None</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.Tag.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.TagLayout","title":"TagLayout  <code>dataclass</code>","text":"<pre><code>TagLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = white, filter_: NoneValueType | Filter | None = None, height: float = 35.0, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = 1.25, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 35.0, direction: Direction = RIGHT, angle: float = 70.0)\n</code></pre> <p>               Bases: <code>_SimpleMixin</code>, <code>SBGNNode</code></p> <p>Tag layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'6a2640e0-4947-43d3-913a-15bcca51e3e1'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>35.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The stroke width of the node</p> <code>1.25</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>35.0</code> <code>direction</code> <code>Direction</code> <p>The direction the tag glyph points to</p> <code>&lt;Direction.RIGHT: 2&gt;</code> <code>angle</code> <code>float</code> <p>The angle of the pointed end of the tag glyph</p> <code>70.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Return the east anchor of the node.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Return the label center anchor of the node.</p> <code>north</code> <p>Return the north anchor of the node.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Return the south anchor of the node.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Return the west anchor of the node.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>angle</code> <code>float</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.TagLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.TagLayout.angle","title":"angle  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>angle: float = dataclasses.field(default=70.0, metadata={'description': 'The angle of the pointed end of the tag glyph'})\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.TagLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.TagLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.TagLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.TagLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.TagLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.TagLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.TagLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.TagLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.TagLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Return the east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Return the east anchor of the node.\"\"\"\n    return self.own_angle(0)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.TagLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.TagLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.TagLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.TagLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.TagLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Return the label center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Return the label center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.TagLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Return the north anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Return the north anchor of the node.\"\"\"\n    return self.own_angle(90)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.TagLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.TagLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.TagLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.TagLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.TagLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.TagLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.TagLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.TagLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.TagLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.TagLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.TagLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.TagLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Return the south anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Return the south anchor of the node.\"\"\"\n    return self.own_angle(270)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.TagLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.TagLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.TagLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.TagLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.TagLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.TagLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Return the west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Return the west anchor of the node.\"\"\"\n    return self.own_angle(180)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.TagLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.TagLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.TagLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.TagLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.TagReference","title":"TagReference  <code>dataclass</code>","text":"<pre><code>TagReference(*, id_: str = make_uuid4_as_str(), referred_element: EntityPool | Compartment)\n</code></pre> <p>               Bases: <code>SBGNRole</code></p> <p>Tag reference.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'7ab83f07-2eec-4592-8c4e-a8f7479353af'</code> <code>referred_element</code> <code>EntityPool | Compartment</code> <p>The element of the tag reference</p> required <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.TagReference.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.Terminal","title":"Terminal  <code>dataclass</code>","text":"<pre><code>Terminal(*, id_: str = make_uuid4_as_str(), label: str | None = None, referred_element: TerminalReference | None = None)\n</code></pre> <p>               Bases: <code>SBGNAuxiliaryUnit</code></p> <p>Terminal.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'1e8a9349-bd76-44a8-a93b-9cca639477b8'</code> <code>label</code> <code>str | None</code> <p>The label of the terminal</p> <code>None</code> <code>referred_element</code> <code>TerminalReference | None</code> <p>The element referred to by the terminal</p> <code>None</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.Terminal.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.TerminalLayout","title":"TerminalLayout  <code>dataclass</code>","text":"<pre><code>TerminalLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = white, filter_: NoneValueType | Filter | None = None, height: float = 35.0, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = 1.25, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 35.0, direction: Direction = RIGHT, angle: float = 70.0)\n</code></pre> <p>               Bases: <code>_SimpleMixin</code>, <code>SBGNNode</code></p> <p>Terminal layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'4ad7668c-5322-42b6-8abc-d17cffc2405f'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>35.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The stroke width of the node</p> <code>1.25</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>35.0</code> <code>direction</code> <code>Direction</code> <p>The direction the terminal glyph points to</p> <code>&lt;Direction.RIGHT: 2&gt;</code> <code>angle</code> <code>float</code> <p>The angle of the pointed end of the terminal glyph</p> <code>70.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Return the east anchor of the node.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Return the label center anchor of the node.</p> <code>north</code> <p>Return the north anchor of the node.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Return the south anchor of the node.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Return the west anchor of the node.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>angle</code> <code>float</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.TerminalLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.TerminalLayout.angle","title":"angle  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>angle: float = dataclasses.field(default=70.0, metadata={'description': 'The angle of the pointed end of the terminal glyph'})\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.TerminalLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.TerminalLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.TerminalLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.TerminalLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.TerminalLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.TerminalLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.TerminalLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.TerminalLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.TerminalLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Return the east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Return the east anchor of the node.\"\"\"\n    return self.own_angle(0)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.TerminalLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.TerminalLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.TerminalLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.TerminalLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.TerminalLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Return the label center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Return the label center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.TerminalLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Return the north anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Return the north anchor of the node.\"\"\"\n    return self.own_angle(90)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.TerminalLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.TerminalLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.TerminalLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.TerminalLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.TerminalLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.TerminalLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.TerminalLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.TerminalLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.TerminalLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.TerminalLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.TerminalLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.TerminalLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Return the south anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Return the south anchor of the node.\"\"\"\n    return self.own_angle(270)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.TerminalLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.TerminalLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.TerminalLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.TerminalLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.TerminalLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.TerminalLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Return the west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Return the west anchor of the node.\"\"\"\n    return self.own_angle(180)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.TerminalLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.TerminalLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.TerminalLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.TerminalLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.TerminalReference","title":"TerminalReference  <code>dataclass</code>","text":"<pre><code>TerminalReference(*, id_: str = make_uuid4_as_str(), referred_element: EntityPool | Compartment)\n</code></pre> <p>               Bases: <code>SBGNRole</code></p> <p>Terminal reference.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'a7cd4d62-0452-4665-bcf3-bd70e8384e73'</code> <code>referred_element</code> <code>EntityPool | Compartment</code> <p>The element of the terminal reference</p> required <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.TerminalReference.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UncertainProcess","title":"UncertainProcess  <code>dataclass</code>","text":"<pre><code>UncertainProcess(*, id_: str = make_uuid4_as_str(), reactants: frozenset[Reactant] = frozenset(), products: frozenset[Product] = frozenset(), reversible: bool = False, has_external_source: bool = False, has_external_sink: bool = False)\n</code></pre> <p>               Bases: <code>StoichiometricProcess</code></p> <p>Uncertain process.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'9a8538ec-a0bc-4869-8a13-38e715f79bc0'</code> <code>reactants</code> <code>frozenset[Reactant]</code> <p>The reactants of the stoichiometric process</p> <code>&lt;dynamic&gt;</code> <code>products</code> <code>frozenset[Product]</code> <p>The products of the stoichiometric process</p> <code>&lt;dynamic&gt;</code> <code>reversible</code> <code>bool</code> <p>Whether the stoichiometric process is reversible or not</p> <code>False</code> <code>has_external_source</code> <code>bool</code> <p>Whether the process has an unspecified external source (an empty-set / source-and-sink reactant in SBGN PD).</p> <code>False</code> <code>has_external_sink</code> <code>bool</code> <p>Whether the process has an unspecified external sink (an empty-set / source-and-sink product in SBGN PD).</p> <code>False</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UncertainProcess.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UncertainProcessLayout","title":"UncertainProcessLayout  <code>dataclass</code>","text":"<pre><code>UncertainProcessLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = white, filter_: NoneValueType | Filter | None = None, height: float = 20.0, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = 1.25, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 20.0, text: str = '?', font_family: str = DEFAULT_FONT_FAMILY, font_fill: Color | NoneValueType = black, font_stroke: Color | NoneValueType = NoneValue, font_style: FontStyle = NORMAL, font_weight: FontWeight | float = NORMAL, orientation: Orientation = HORIZONTAL, left_to_right: bool = True, left_connector_length: float = 10.0, right_connector_length: float = 10.0, left_connector_stroke: NoneValueType | Color | None = None, left_connector_stroke_width: float | None = None, left_connector_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, left_connector_stroke_dashoffset: float | None = None, left_connector_fill: NoneValueType | Color | None = None, left_connector_transform: NoneValueType | tuple[Transformation, ...] | None = None, left_connector_filter: NoneValueType | Filter | None = None, right_connector_stroke: NoneValueType | Color | None = None, right_connector_stroke_width: float | None = None, right_connector_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, right_connector_stroke_dashoffset: float | None = None, right_connector_fill: NoneValueType | Color | None = None, right_connector_transform: NoneValueType | tuple[Transformation, ...] | None = None, right_connector_filter: NoneValueType | Filter | None = None)\n</code></pre> <p>               Bases: <code>_ConnectorsMixin</code>, <code>_SimpleMixin</code>, <code>_TextMixin</code>, <code>SBGNNode</code></p> <p>Uncertain process layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'763953bd-9e92-4b85-bf8d-246617684fa6'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>20.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The stroke width of the node</p> <code>1.25</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>20.0</code> <code>text</code> <code>str</code> <p>The text drawn inside the process glyph</p> <code>'?'</code> <code>font_family</code> <code>str</code> <p>Font family for the text.</p> <code>'DejaVu Sans'</code> <code>font_fill</code> <code>Color | NoneValueType</code> <p>Fill color for the text.</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>font_stroke</code> <code>Color | NoneValueType</code> <p>Stroke color for the text outline.</p> <code>&lt;momapy.drawing.NoneValueType object at 0x7f8b17323e00&gt;</code> <code>font_style</code> <code>FontStyle</code> <p>Font style (normal, italic, etc.).</p> <code>&lt;FontStyle.NORMAL: 0&gt;</code> <code>font_weight</code> <code>FontWeight | float</code> <p>Font weight (normal, bold, etc.).</p> <code>&lt;FontWeight.NORMAL: 0&gt;</code> <code>orientation</code> <code>Orientation</code> <p>Orientation of connectors (HORIZONTAL or VERTICAL).</p> <code>&lt;Orientation.HORIZONTAL: 1&gt;</code> <code>left_to_right</code> <code>bool</code> <p>Whether connectors flow left to right.</p> <code>True</code> <code>left_connector_length</code> <code>float</code> <p>Length of the left connector.</p> <code>10.0</code> <code>right_connector_length</code> <code>float</code> <p>Length of the right connector.</p> <code>10.0</code> <code>left_connector_stroke</code> <code>NoneValueType | Color | None</code> <p>Stroke color for the left connector line.</p> <code>None</code> <code>left_connector_stroke_width</code> <code>float | None</code> <p>Stroke width for the left connector line.</p> <code>None</code> <code>left_connector_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>Dash pattern for the left connector line.</p> <code>None</code> <code>left_connector_stroke_dashoffset</code> <code>float | None</code> <p>Dash offset for the left connector line.</p> <code>None</code> <code>left_connector_fill</code> <code>NoneValueType | Color | None</code> <p>Fill color for the left connector.</p> <code>None</code> <code>left_connector_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>Transformations applied to the left connector.</p> <code>None</code> <code>left_connector_filter</code> <code>NoneValueType | Filter | None</code> <p>Filter applied to the left connector.</p> <code>None</code> <code>right_connector_stroke</code> <code>NoneValueType | Color | None</code> <p>Stroke color for the right connector line.</p> <code>None</code> <code>right_connector_stroke_width</code> <code>float | None</code> <p>Stroke width for the right connector line.</p> <code>None</code> <code>right_connector_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>Dash pattern for the right connector line.</p> <code>None</code> <code>right_connector_stroke_dashoffset</code> <code>float | None</code> <p>Dash offset for the right connector line.</p> <code>None</code> <code>right_connector_fill</code> <code>NoneValueType | Color | None</code> <p>Fill color for the right connector.</p> <code>None</code> <code>right_connector_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>Transformations applied to the right connector.</p> <code>None</code> <code>right_connector_filter</code> <code>NoneValueType | Filter | None</code> <p>Filter applied to the right connector.</p> <code>None</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>angle</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Get the east (right) anchor point.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Return the label center anchor of the node.</p> <code>left_connector_base</code> <p>Get the base point of the left connector.</p> <code>left_connector_tip</code> <p>Get the tip point of the left connector.</p> <code>north</code> <p>Get the north (top) anchor point.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>right_connector_base</code> <p>Get the base point of the right connector.</p> <code>right_connector_tip</code> <p>Get the tip point of the right connector.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Get the south (bottom) anchor point.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Get the west (left) anchor point.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UncertainProcessLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UncertainProcessLayout.angle","title":"angle","text":"<pre><code>angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UncertainProcessLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UncertainProcessLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UncertainProcessLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UncertainProcessLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UncertainProcessLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UncertainProcessLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UncertainProcessLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UncertainProcessLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UncertainProcessLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Get the east (right) anchor point.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point on the east side of the element.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Get the east (right) anchor point.\n\n    Returns:\n        Point on the east side of the element.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x + self.width / 2, self.y)\n    else:\n        return Point(self.x + self.width / 2 + self.right_connector_length, self.y)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UncertainProcessLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UncertainProcessLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UncertainProcessLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UncertainProcessLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UncertainProcessLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Return the label center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Return the label center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UncertainProcessLayout.left_connector_base","title":"left_connector_base","text":"<pre><code>left_connector_base() -&gt; Point\n</code></pre> <p>Get the base point of the left connector.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point where the left connector attaches to the shape.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def left_connector_base(self) -&gt; Point:\n    \"\"\"Get the base point of the left connector.\n\n    Returns:\n        Point where the left connector attaches to the shape.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x, self.y - self.height / 2)\n    else:\n        return Point(self.x - self.width / 2, self.y)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UncertainProcessLayout.left_connector_tip","title":"left_connector_tip","text":"<pre><code>left_connector_tip() -&gt; Point\n</code></pre> <p>Get the tip point of the left connector.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point at the end of the left connector line.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def left_connector_tip(self) -&gt; Point:\n    \"\"\"Get the tip point of the left connector.\n\n    Returns:\n        Point at the end of the left connector line.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x, self.y - self.height / 2 - self.left_connector_length)\n    else:\n        return Point(self.x - self.width / 2 - self.left_connector_length, self.y)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UncertainProcessLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Get the north (top) anchor point.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point on the north side of the element.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Get the north (top) anchor point.\n\n    Returns:\n        Point on the north side of the element.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x, self.y - self.height / 2 - self.left_connector_length)\n    else:\n        return Point(self.x, self.y - self.height / 2)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UncertainProcessLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UncertainProcessLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UncertainProcessLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UncertainProcessLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UncertainProcessLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UncertainProcessLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UncertainProcessLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UncertainProcessLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UncertainProcessLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UncertainProcessLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UncertainProcessLayout.right_connector_base","title":"right_connector_base","text":"<pre><code>right_connector_base() -&gt; Point\n</code></pre> <p>Get the base point of the right connector.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point where the right connector attaches to the shape.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def right_connector_base(self) -&gt; Point:\n    \"\"\"Get the base point of the right connector.\n\n    Returns:\n        Point where the right connector attaches to the shape.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x, self.y + self.height / 2)\n    else:\n        return Point(self.x + self.width / 2, self.y)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UncertainProcessLayout.right_connector_tip","title":"right_connector_tip","text":"<pre><code>right_connector_tip() -&gt; Point\n</code></pre> <p>Get the tip point of the right connector.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point at the end of the right connector line.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def right_connector_tip(self) -&gt; Point:\n    \"\"\"Get the tip point of the right connector.\n\n    Returns:\n        Point at the end of the right connector line.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x, self.y + self.height / 2 + self.right_connector_length)\n    else:\n        return Point(self.x + self.width / 2 + self.right_connector_length, self.y)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UncertainProcessLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UncertainProcessLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Get the south (bottom) anchor point.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point on the south side of the element.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Get the south (bottom) anchor point.\n\n    Returns:\n        Point on the south side of the element.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x, self.y + self.height / 2 + self.right_connector_length)\n    else:\n        return Point(self.x, self.y + self.height / 2)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UncertainProcessLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UncertainProcessLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UncertainProcessLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UncertainProcessLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UncertainProcessLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UncertainProcessLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Get the west (left) anchor point.</p> <p>Returns:</p> Type Description <code>Point</code> <p>Point on the west side of the element.</p> Source code in <code>src/momapy/sbgn/elements.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Get the west (left) anchor point.\n\n    Returns:\n        Point on the west side of the element.\n    \"\"\"\n    if self.orientation == Orientation.VERTICAL:\n        return Point(self.x - self.width / 2, self.y)\n    else:\n        return Point(self.x - self.width / 2 - self.left_connector_length, self.y)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UncertainProcessLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UncertainProcessLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UncertainProcessLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UncertainProcessLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnitOfInformation","title":"UnitOfInformation  <code>dataclass</code>","text":"<pre><code>UnitOfInformation(*, id_: str = make_uuid4_as_str(), value: str, prefix: str | None = None)\n</code></pre> <p>               Bases: <code>SBGNAuxiliaryUnit</code></p> <p>Unit of information.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'17495eff-5974-4336-b0a1-9902f3c20e4c'</code> <code>value</code> <code>str</code> <p>The value of the unit of information</p> required <code>prefix</code> <code>str | None</code> <p>The prefix of the unit of information</p> <code>None</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnitOfInformation.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnitOfInformationLayout","title":"UnitOfInformationLayout  <code>dataclass</code>","text":"<pre><code>UnitOfInformationLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = white, filter_: NoneValueType | Filter | None = None, height: float = 12.0, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = 1.25, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 18.0)\n</code></pre> <p>               Bases: <code>_SimpleMixin</code>, <code>SBGNNode</code></p> <p>Unit of information layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'9edb8e6a-cbf6-40d8-9092-58fa98ee9799'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>12.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The stroke width of the node</p> <code>1.25</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>18.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>angle</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Return the east anchor of the node.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Return the label center anchor of the node.</p> <code>north</code> <p>Return the north anchor of the node.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Return the south anchor of the node.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Return the west anchor of the node.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnitOfInformationLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnitOfInformationLayout.angle","title":"angle","text":"<pre><code>angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnitOfInformationLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnitOfInformationLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnitOfInformationLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnitOfInformationLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnitOfInformationLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnitOfInformationLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnitOfInformationLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnitOfInformationLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnitOfInformationLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Return the east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Return the east anchor of the node.\"\"\"\n    return self.own_angle(0)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnitOfInformationLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnitOfInformationLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnitOfInformationLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnitOfInformationLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnitOfInformationLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Return the label center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Return the label center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnitOfInformationLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Return the north anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Return the north anchor of the node.\"\"\"\n    return self.own_angle(90)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnitOfInformationLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnitOfInformationLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnitOfInformationLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnitOfInformationLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnitOfInformationLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnitOfInformationLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnitOfInformationLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnitOfInformationLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnitOfInformationLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnitOfInformationLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnitOfInformationLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnitOfInformationLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Return the south anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Return the south anchor of the node.\"\"\"\n    return self.own_angle(270)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnitOfInformationLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnitOfInformationLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnitOfInformationLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnitOfInformationLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnitOfInformationLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnitOfInformationLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Return the west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Return the west anchor of the node.\"\"\"\n    return self.own_angle(180)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnitOfInformationLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnitOfInformationLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnitOfInformationLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnitOfInformationLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnspecifiedEntity","title":"UnspecifiedEntity  <code>dataclass</code>","text":"<pre><code>UnspecifiedEntity(*, id_: str = make_uuid4_as_str(), compartment: Compartment | None = None, label: str | None = None)\n</code></pre> <p>               Bases: <code>EntityPool</code></p> <p>Unspecified entity.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'66427693-f73a-49b7-9636-256a1315aa65'</code> <code>compartment</code> <code>Compartment | None</code> <p>The compartment of the entity pool</p> <code>None</code> <code>label</code> <code>str | None</code> <p>The label of the unspecified entity</p> <code>None</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnspecifiedEntity.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnspecifiedEntityLayout","title":"UnspecifiedEntityLayout  <code>dataclass</code>","text":"<pre><code>UnspecifiedEntityLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = white, filter_: NoneValueType | Filter | None = None, height: float = 30.0, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = 1.25, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 60.0)\n</code></pre> <p>               Bases: <code>_SimpleMixin</code>, <code>SBGNNode</code></p> <p>Unspecified entity layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'78a1070d-8945-4f05-9482-3628b5587234'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>30.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The stroke width of the node</p> <code>1.25</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>60.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>angle</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Return the east anchor of the node.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Return the label center anchor of the node.</p> <code>north</code> <p>Return the north anchor of the node.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Return the south anchor of the node.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Return the west anchor of the node.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnspecifiedEntityLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnspecifiedEntityLayout.angle","title":"angle","text":"<pre><code>angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnspecifiedEntityLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnspecifiedEntityLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnspecifiedEntityLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnspecifiedEntityLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnspecifiedEntityLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnspecifiedEntityLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnspecifiedEntityLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnspecifiedEntityLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnspecifiedEntityLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Return the east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Return the east anchor of the node.\"\"\"\n    return self.own_angle(0)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnspecifiedEntityLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnspecifiedEntityLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnspecifiedEntityLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnspecifiedEntityLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnspecifiedEntityLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Return the label center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Return the label center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnspecifiedEntityLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Return the north anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Return the north anchor of the node.\"\"\"\n    return self.own_angle(90)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnspecifiedEntityLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnspecifiedEntityLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnspecifiedEntityLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnspecifiedEntityLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnspecifiedEntityLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnspecifiedEntityLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnspecifiedEntityLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnspecifiedEntityLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnspecifiedEntityLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnspecifiedEntityLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnspecifiedEntityLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnspecifiedEntityLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Return the south anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Return the south anchor of the node.\"\"\"\n    return self.own_angle(270)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnspecifiedEntityLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnspecifiedEntityLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnspecifiedEntityLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnspecifiedEntityLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnspecifiedEntityLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnspecifiedEntityLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Return the west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Return the west anchor of the node.\"\"\"\n    return self.own_angle(180)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnspecifiedEntityLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnspecifiedEntityLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnspecifiedEntityLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnspecifiedEntityLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnspecifiedEntitySubunit","title":"UnspecifiedEntitySubunit  <code>dataclass</code>","text":"<pre><code>UnspecifiedEntitySubunit(*, id_: str = make_uuid4_as_str(), label: str | None = None)\n</code></pre> <p>               Bases: <code>Subunit</code></p> <p>Unspecified entity subunit.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'fa0812e5-cba1-4c3a-ab33-e68634b7a5fe'</code> <code>label</code> <code>str | None</code> <p>The label of the subunit</p> <code>None</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnspecifiedEntitySubunit.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnspecifiedEntitySubunitLayout","title":"UnspecifiedEntitySubunitLayout  <code>dataclass</code>","text":"<pre><code>UnspecifiedEntitySubunitLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = white, filter_: NoneValueType | Filter | None = None, height: float = 30.0, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = 1.25, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float = 60.0)\n</code></pre> <p>               Bases: <code>_SimpleMixin</code>, <code>SBGNNode</code></p> <p>Unspecified entity subunit layout.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'a7018f00-4ecd-4dd4-8e05-9de7333929e5'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> <code>30.0</code> <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The stroke width of the node</p> <code>1.25</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> <code>60.0</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>angle</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Return the east anchor of the node.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Return the label center anchor of the node.</p> <code>north</code> <p>Return the north anchor of the node.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Return the south anchor of the node.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Return the west anchor of the node.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnspecifiedEntitySubunitLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnspecifiedEntitySubunitLayout.angle","title":"angle","text":"<pre><code>angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnspecifiedEntitySubunitLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnspecifiedEntitySubunitLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnspecifiedEntitySubunitLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnspecifiedEntitySubunitLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnspecifiedEntitySubunitLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnspecifiedEntitySubunitLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnspecifiedEntitySubunitLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnspecifiedEntitySubunitLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnspecifiedEntitySubunitLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Return the east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Return the east anchor of the node.\"\"\"\n    return self.own_angle(0)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnspecifiedEntitySubunitLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnspecifiedEntitySubunitLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnspecifiedEntitySubunitLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnspecifiedEntitySubunitLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnspecifiedEntitySubunitLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Return the label center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Return the label center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnspecifiedEntitySubunitLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Return the north anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Return the north anchor of the node.\"\"\"\n    return self.own_angle(90)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnspecifiedEntitySubunitLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnspecifiedEntitySubunitLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnspecifiedEntitySubunitLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnspecifiedEntitySubunitLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnspecifiedEntitySubunitLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnspecifiedEntitySubunitLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnspecifiedEntitySubunitLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnspecifiedEntitySubunitLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnspecifiedEntitySubunitLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnspecifiedEntitySubunitLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnspecifiedEntitySubunitLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnspecifiedEntitySubunitLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Return the south anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Return the south anchor of the node.\"\"\"\n    return self.own_angle(270)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnspecifiedEntitySubunitLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnspecifiedEntitySubunitLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnspecifiedEntitySubunitLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnspecifiedEntitySubunitLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnspecifiedEntitySubunitLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnspecifiedEntitySubunitLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Return the west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Return the west anchor of the node.\"\"\"\n    return self.own_angle(180)\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnspecifiedEntitySubunitLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnspecifiedEntitySubunitLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnspecifiedEntitySubunitLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/sbgn/pd/#momapy.sbgn.pd.UnspecifiedEntitySubunitLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/sbgn/","title":"SBGN","text":""},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn","title":"momapy.sbgn","text":"<p>SBGN (Systems Biology Graphical Notation) subpackage.</p> <p>Abstract bases live here; concrete classes for each sublanguage are in the <code>pd</code> (Process Description) and <code>af</code> (Activity Flow) subpackages.</p> <p>Modules:</p> Name Description <code>af</code> <p>SBGN Activity Flow (AF) subpackage facade.</p> <code>elements</code> <p>Abstract base classes and mixins for SBGN model and layout elements.</p> <code>io</code> <p>Input/output module for SBGN maps.</p> <code>layout</code> <p>Abstract base class for SBGN layouts.</p> <code>map</code> <p>Abstract base class for SBGN maps.</p> <code>model</code> <p>Abstract base class for SBGN models.</p> <code>pd</code> <p>SBGN Process Description (PD) subpackage facade.</p> <code>styling</code> <p>Styling module for SBGN maps.</p> <code>utils</code> <p>Utility functions for SBGN map manipulation.</p> <p>Classes:</p> Name Description <code>SBGNAuxiliaryUnit</code> <p>Abstract base class for SBGN auxiliary units.</p> <code>SBGNDoubleHeadedArc</code> <p>Abstract base class for SBGN double-headed arcs.</p> <code>SBGNLayout</code> <p>Base class for SBGN layouts.</p> <code>SBGNMap</code> <p>Abstract base class for SBGN maps.</p> <code>SBGNModel</code> <p>Abstract base class for SBGN models.</p> <code>SBGNModelElement</code> <p>Abstract base class for SBGN model elements.</p> <code>SBGNNode</code> <p>Abstract base class for SBGN nodes.</p> <code>SBGNRole</code> <p>Abstract base class for SBGN roles.</p> <code>SBGNSingleHeadedArc</code> <p>Abstract base class for SBGN single-headed arcs.</p>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNAuxiliaryUnit","title":"SBGNAuxiliaryUnit  <code>dataclass</code>","text":"<pre><code>SBGNAuxiliaryUnit(*, id_: str = make_uuid4_as_str())\n</code></pre> <p>               Bases: <code>SBGNModelElement</code></p> <p>Abstract base class for SBGN auxiliary units.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'b65838fa-f2b4-41cb-ad00-cf0b118d5ff0'</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNAuxiliaryUnit.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNDoubleHeadedArc","title":"SBGNDoubleHeadedArc  <code>dataclass</code>","text":"<pre><code>SBGNDoubleHeadedArc(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, end_shorten: float = 0.0, fill: NoneValueType | Color | None = None, filter_: NoneValueType | Filter | None = None, path_fill: NoneValueType | Color | None = NoneValue, path_filter: NoneValueType | Filter | None = None, path_stroke: NoneValueType | Color | None = black, path_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, path_stroke_dashoffset: NoneValueType | float | None = None, path_stroke_width: float | None = 1.25, path_transform: NoneValueType | tuple[Transformation, ...] | None = None, stroke: NoneValueType | Color | None = None, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: NoneValueType | float | None = None, segments: tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...] = tuple(), source: LayoutElement | None = None, start_shorten: float = 0.0, target: LayoutElement | None = None, transform: NoneValueType | tuple[Transformation, ...] | None = None, end_arrowhead_fill: NoneValueType | Color | None = white, end_arrowhead_filter: NoneValueType | Filter | None = None, end_arrowhead_stroke: NoneValueType | Color | None = black, end_arrowhead_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, end_arrowhead_stroke_dashoffset: NoneValueType | float | None = None, end_arrowhead_stroke_width: float | None = 1.25, end_arrowhead_transform: NoneValueType | tuple[Transformation, ...] | None = None, start_arrowhead_fill: NoneValueType | Color | None = white, start_arrowhead_filter: NoneValueType | Filter | None = None, start_arrowhead_stroke: NoneValueType | Color | None = black, start_arrowhead_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, start_arrowhead_stroke_dashoffset: NoneValueType | float | None = None, start_arrowhead_stroke_width: float | None = 1.25, start_arrowhead_transform: NoneValueType | tuple[Transformation, ...] | None = None)\n</code></pre> <p>               Bases: <code>DoubleHeadedArc</code></p> <p>Abstract base class for SBGN double-headed arcs.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'ba4eed13-7394-45a3-a446-ce00a07fe6e5'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>end_shorten</code> <code>float</code> <p>The length the end of the arc will be shorten by</p> <code>0.0</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the arc</p> <code>None</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the arc</p> <code>None</code> <code>path_fill</code> <code>NoneValueType | Color | None</code> <p>The path fill color of the arc</p> <code>&lt;momapy.drawing.NoneValueType object at 0x7f8b17323e00&gt;</code> <code>path_filter</code> <code>NoneValueType | Filter | None</code> <p>The path filter of the arc</p> <code>None</code> <code>path_stroke</code> <code>NoneValueType | Color | None</code> <p>The path stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>path_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The path stroke dasharray of the arc</p> <code>None</code> <code>path_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The path stroke dashoffset of the arc</p> <code>None</code> <code>path_stroke_width</code> <code>float | None</code> <p>The path stroke width of the arc</p> <code>1.25</code> <code>path_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The path transform of the arc</p> <code>None</code> <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the arc</p> <code>None</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the arc</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the arc</p> <code>None</code> <code>stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the arc</p> <code>None</code> <code>segments</code> <code>tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...]</code> <p>The path segments of the arc</p> <code>&lt;dynamic&gt;</code> <code>source</code> <code>LayoutElement | None</code> <p>The source of the arc</p> <code>None</code> <code>start_shorten</code> <code>float</code> <p>The length the start of the arc will be shorten by</p> <code>0.0</code> <code>target</code> <code>LayoutElement | None</code> <p>The target of the arc</p> <code>None</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the arc</p> <code>None</code> <code>end_arrowhead_fill</code> <code>NoneValueType | Color | None</code> <p>The end arrowhead fill color of the arc</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>end_arrowhead_filter</code> <code>NoneValueType | Filter | None</code> <p>The end arrowhead filter of the arc</p> <code>None</code> <code>end_arrowhead_stroke</code> <code>NoneValueType | Color | None</code> <p>The end arrowhead stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>end_arrowhead_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The end arrowhead stroke dasharray of the arc</p> <code>None</code> <code>end_arrowhead_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The end arrowhead stroke dashoffset of the arc</p> <code>None</code> <code>end_arrowhead_stroke_width</code> <code>float | None</code> <p>The end arrowhead stroke width of the arc</p> <code>1.25</code> <code>end_arrowhead_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The end arrowhead transform of the arc</p> <code>None</code> <code>start_arrowhead_fill</code> <code>NoneValueType | Color | None</code> <p>The start arrowhead fill color of the arc</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>start_arrowhead_filter</code> <code>NoneValueType | Filter | None</code> <p>The start arrowhead filter of the arc</p> <code>None</code> <code>start_arrowhead_stroke</code> <code>NoneValueType | Color | None</code> <p>The start arrowhead stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>start_arrowhead_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The start arrowhead stroke dasharray of the arc</p> <code>None</code> <code>start_arrowhead_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The start arrowhead stroke dashoffset of the arc</p> <code>None</code> <code>start_arrowhead_stroke_width</code> <code>float | None</code> <p>The start arrowhead stroke width of the arc</p> <code>1.25</code> <code>start_arrowhead_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The start arrowhead transform of the arc</p> <code>None</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>childless</code> <p>Return a copy of the arc with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>end_arrowhead_base</code> <p>Return the base anchor point of the double-headed arc end arrowhead.</p> <code>end_arrowhead_bbox</code> <p>Return the bounding box of the double-headed arc end arrowhead.</p> <code>end_arrowhead_border</code> <p>Return the point at the intersection of the drawing elements of the double-headed arc end arrowhead and the line going through the center of these drawing elements and the given point.</p> <code>end_arrowhead_drawing_elements</code> <p>Return the drawing elements of the double-headed arc end arrowhead.</p> <code>end_arrowhead_length</code> <p>Return the length of the double-headed arc end arrowhead.</p> <code>end_arrowhead_tip</code> <p>Return the tip anchor point of the double-headed arc end arrowhead.</p> <code>end_point</code> <p>Return the ending point of the arc.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>fraction</code> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <code>length</code> <p>Return the total length of the arc path.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_children</code> <p>Return the self children of the arc.</p> <code>own_drawing_elements</code> <p>Return the self drawing elements of the double-headed arc. These include the drawing elements of the arc path, the start arrowhead, and the end arrowhead.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>path_drawing_elements</code> <p>Return the drawing elements of the double-headed arc path.</p> <code>points</code> <p>Return the points of the arc path.</p> <code>start_arrowhead_base</code> <p>Return the base anchor point of the double-headed arc start arrowhead.</p> <code>start_arrowhead_bbox</code> <p>Return the bounding box of the double-headed arc start arrowhead.</p> <code>start_arrowhead_border</code> <p>Return the point at the intersection of the drawing elements of the double-headed arc start arrowhead and the line going through the center of these drawing elements and the given point.</p> <code>start_arrowhead_drawing_elements</code> <p>Return the drawing elements of the double-headed arc start arrowhead.</p> <code>start_arrowhead_length</code> <p>Return the length of the double-headed arc start arrowhead.</p> <code>start_arrowhead_tip</code> <p>Return the tip anchor point of the double-headed arc start arrowhead.</p> <code>start_point</code> <p>Return the starting point of the arc.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNDoubleHeadedArc.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNDoubleHeadedArc.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNDoubleHeadedArc.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the arc with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the arc with no children.\"\"\"\n    return dataclasses.replace(self, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNDoubleHeadedArc.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNDoubleHeadedArc.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNDoubleHeadedArc.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNDoubleHeadedArc.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNDoubleHeadedArc.end_arrowhead_base","title":"end_arrowhead_base","text":"<pre><code>end_arrowhead_base() -&gt; Point\n</code></pre> <p>Return the base anchor point of the double-headed arc end arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_arrowhead_base(self) -&gt; Point:\n    \"\"\"Return the base anchor point of the double-headed arc end arrowhead.\"\"\"\n    arrowhead_length = self.end_arrowhead_length()\n    if arrowhead_length == 0:\n        return self.end_point()\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return self.end_arrowhead_tip() - (arrowhead_length, 0)\n    fraction = 1 - (arrowhead_length + self.end_shorten) / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNDoubleHeadedArc.end_arrowhead_bbox","title":"end_arrowhead_bbox","text":"<pre><code>end_arrowhead_bbox() -&gt; Bbox\n</code></pre> <p>Return the bounding box of the double-headed arc end arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_arrowhead_bbox(self) -&gt; Bbox:\n    \"\"\"Return the bounding box of the double-headed arc end arrowhead.\"\"\"\n    return get_drawing_elements_bbox(self.end_arrowhead_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNDoubleHeadedArc.end_arrowhead_border","title":"end_arrowhead_border","text":"<pre><code>end_arrowhead_border(point: Point) -&gt; Point\n</code></pre> <p>Return the point at the intersection of the drawing elements of the double-headed arc end arrowhead and the line going through the center of these drawing elements and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_arrowhead_border(self, point: Point) -&gt; Point:\n    \"\"\"Return the point at the intersection of the drawing elements of the double-headed arc end arrowhead and the line going through the center of these drawing elements and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    point = get_drawing_elements_border(\n        self.end_arrowhead_drawing_elements(), point\n    )\n    if point is None:\n        return self.end_arrowhead_tip()\n    return point\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNDoubleHeadedArc.end_arrowhead_drawing_elements","title":"end_arrowhead_drawing_elements","text":"<pre><code>end_arrowhead_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the double-headed arc end arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_arrowhead_drawing_elements(\n    self,\n) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the double-headed arc end arrowhead.\"\"\"\n    drawing_elements = self._end_arrowhead_border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_end_arrowhead\",\n        elements=tuple(drawing_elements),\n        fill=self.end_arrowhead_fill,\n        filter_=self.end_arrowhead_filter,\n        id_=f\"{self.id_}_end_arrowhead\",\n        stroke=self.end_arrowhead_stroke,\n        stroke_width=self.end_arrowhead_stroke_width,\n        stroke_dasharray=self.end_arrowhead_stroke_dasharray,\n        stroke_dashoffset=self.end_arrowhead_stroke_dashoffset,\n        transform=self.end_arrowhead_transform,\n    )\n    transformation = self._get_end_arrowhead_transformation()\n    group = group.transformed(transformation)\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNDoubleHeadedArc.end_arrowhead_length","title":"end_arrowhead_length","text":"<pre><code>end_arrowhead_length() -&gt; float\n</code></pre> <p>Return the length of the double-headed arc end arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_arrowhead_length(self) -&gt; float:\n    \"\"\"Return the length of the double-headed arc end arrowhead.\"\"\"\n    bbox = get_drawing_elements_bbox(self._end_arrowhead_border_drawing_elements())\n    if math.isnan(bbox.width):\n        return 0.0\n    return bbox.east().x\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNDoubleHeadedArc.end_arrowhead_tip","title":"end_arrowhead_tip","text":"<pre><code>end_arrowhead_tip() -&gt; Point\n</code></pre> <p>Return the tip anchor point of the double-headed arc end arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_arrowhead_tip(self) -&gt; Point:\n    \"\"\"Return the tip anchor point of the double-headed arc end arrowhead.\"\"\"\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return segment.p2\n    fraction = 1 - self.end_shorten / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNDoubleHeadedArc.end_point","title":"end_point","text":"<pre><code>end_point() -&gt; Point\n</code></pre> <p>Return the ending point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_point(self) -&gt; Point:\n    \"\"\"Return the ending point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[-1]\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNDoubleHeadedArc.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNDoubleHeadedArc.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNDoubleHeadedArc.fraction","title":"fraction","text":"<pre><code>fraction(fraction: float) -&gt; tuple[Point, float]\n</code></pre> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def fraction(self, fraction: float) -&gt; tuple[Point, float]:\n    \"\"\"Return the position and angle on the arc at a given fraction (of the total arc length).\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    current_length = 0\n    length_to_reach = fraction * self.length()\n    for segment in self.segments:\n        current_length += segment.length()\n        if current_length &gt;= length_to_reach:\n            break\n    segment_start_length = current_length - segment.length()\n    segment_fraction = (length_to_reach - segment_start_length) / segment.length()\n    position, angle = segment.get_position_and_angle_at_fraction(segment_fraction)\n    return position, angle\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNDoubleHeadedArc.length","title":"length","text":"<pre><code>length() -&gt; float\n</code></pre> <p>Return the total length of the arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def length(self) -&gt; float:\n    \"\"\"Return the total length of the arc path.\"\"\"\n    return sum([segment.length() for segment in self.segments])\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNDoubleHeadedArc.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNDoubleHeadedArc.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the arc.\"\"\"\n    return []\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNDoubleHeadedArc.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the self drawing elements of the double-headed arc. These include the drawing elements of the arc path, the start arrowhead, and the end arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the self drawing elements of the double-headed arc. These include the drawing elements of the arc path, the start arrowhead, and the end arrowhead.\"\"\"\n    drawing_elements = (\n        self.path_drawing_elements()\n        + self.start_arrowhead_drawing_elements()\n        + self.end_arrowhead_drawing_elements()\n    )\n\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}_own\",\n        fill=self.fill,\n        filter_=self.filter_,\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNDoubleHeadedArc.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNDoubleHeadedArc.path_drawing_elements","title":"path_drawing_elements","text":"<pre><code>path_drawing_elements() -&gt; list[Path]\n</code></pre> <p>Return the drawing elements of the double-headed arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def path_drawing_elements(self) -&gt; list[Path]:\n    \"\"\"Return the drawing elements of the double-headed arc path.\"\"\"\n    start_arrowhead_length = self.start_arrowhead_length()\n    end_arrowhead_length = self.end_arrowhead_length()\n    if len(self.segments) == 1:\n        segment = (\n            self.segments[0]\n            .shortened(self.start_shorten + start_arrowhead_length, \"start\")\n            .shortened(self.end_shorten + end_arrowhead_length, \"end\")\n        )\n        actions = [\n            MoveTo(segment.p1),\n            self._make_path_action_from_segment(segment),\n        ]\n    else:\n        first_segment = self.segments[0].shortened(\n            self.start_shorten + start_arrowhead_length, \"start\"\n        )\n        last_segment = self.segments[-1].shortened(\n            self.end_shorten + end_arrowhead_length, \"end\"\n        )\n        actions = [\n            MoveTo(first_segment.p1),\n            self._make_path_action_from_segment(first_segment),\n        ]\n        for segment in self.segments[1:-1]:\n            action = self._make_path_action_from_segment(segment)\n            actions.append(action)\n        actions.append(self._make_path_action_from_segment(last_segment))\n    path = Path(\n        actions=tuple(actions),\n        class_=f\"{type(self).__name__}_path\",\n        fill=self.path_fill,\n        filter_=self.path_filter,\n        id_=f\"{self.id_}_path\",\n        stroke=self.path_stroke,\n        stroke_dasharray=self.path_stroke_dasharray,\n        stroke_dashoffset=self.path_stroke_dashoffset,\n        stroke_width=self.path_stroke_width,\n        transform=self.path_transform,\n    )\n    return [path]\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNDoubleHeadedArc.points","title":"points","text":"<pre><code>points() -&gt; list[Point]\n</code></pre> <p>Return the points of the arc path.</p> <p>An arc with no segments has no points and returns an empty list.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def points(self) -&gt; list[Point]:\n    \"\"\"Return the points of the arc path.\n\n    An arc with no segments has no points and returns an empty list.\n    \"\"\"\n    if not self.segments:\n        return []\n    points = []\n    for segment in self.segments:\n        points.append(segment.p1)\n    points.append(segment.p2)\n    return points\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNDoubleHeadedArc.start_arrowhead_base","title":"start_arrowhead_base","text":"<pre><code>start_arrowhead_base() -&gt; Point\n</code></pre> <p>Return the base anchor point of the double-headed arc start arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_arrowhead_base(self) -&gt; Point:\n    \"\"\"Return the base anchor point of the double-headed arc start arrowhead.\"\"\"\n    arrowhead_length = self.start_arrowhead_length()\n    if arrowhead_length == 0:\n        return self.start_point()\n    segment = self.segments[0]\n    segment = Segment(segment.p2, segment.p1)\n    segment_length = segment.length()\n    if segment_length == 0:\n        return self.start_arrowhead_tip() + (arrowhead_length, 0)\n    fraction = 1 - (arrowhead_length + self.start_shorten) / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNDoubleHeadedArc.start_arrowhead_bbox","title":"start_arrowhead_bbox","text":"<pre><code>start_arrowhead_bbox() -&gt; Bbox\n</code></pre> <p>Return the bounding box of the double-headed arc start arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_arrowhead_bbox(self) -&gt; Bbox:\n    \"\"\"Return the bounding box of the double-headed arc start arrowhead.\"\"\"\n    return get_drawing_elements_bbox(self.start_arrowhead_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNDoubleHeadedArc.start_arrowhead_border","title":"start_arrowhead_border","text":"<pre><code>start_arrowhead_border(point: Point) -&gt; Point\n</code></pre> <p>Return the point at the intersection of the drawing elements of the double-headed arc start arrowhead and the line going through the center of these drawing elements and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_arrowhead_border(self, point: Point) -&gt; Point:\n    \"\"\"Return the point at the intersection of the drawing elements of the double-headed arc start arrowhead and the line going through the center of these drawing elements and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    point = get_drawing_elements_border(\n        self.start_arrowhead_drawing_elements(), point\n    )\n    if point is None:\n        return self.start_arrowhead_tip()\n    return point\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNDoubleHeadedArc.start_arrowhead_drawing_elements","title":"start_arrowhead_drawing_elements","text":"<pre><code>start_arrowhead_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the double-headed arc start arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_arrowhead_drawing_elements(\n    self,\n) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the double-headed arc start arrowhead.\"\"\"\n    drawing_elements = self._start_arrowhead_border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_start_arrowhead\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}_start_arrowhead\",\n        fill=self.start_arrowhead_fill,\n        filter_=self.start_arrowhead_filter,\n        stroke=self.start_arrowhead_stroke,\n        stroke_dasharray=self.start_arrowhead_stroke_dasharray,\n        stroke_dashoffset=self.start_arrowhead_stroke_dashoffset,\n        stroke_width=self.start_arrowhead_stroke_width,\n        transform=self.start_arrowhead_transform,\n    )\n    transformation = self._get_start_arrowhead_transformation()\n    group = group.transformed(transformation)\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNDoubleHeadedArc.start_arrowhead_length","title":"start_arrowhead_length","text":"<pre><code>start_arrowhead_length() -&gt; float\n</code></pre> <p>Return the length of the double-headed arc start arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_arrowhead_length(self) -&gt; float:\n    \"\"\"Return the length of the double-headed arc start arrowhead.\"\"\"\n    bbox = get_drawing_elements_bbox(\n        self._start_arrowhead_border_drawing_elements()\n    )\n    if math.isnan(bbox.width):\n        return 0.0\n    return abs(bbox.west().x)\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNDoubleHeadedArc.start_arrowhead_tip","title":"start_arrowhead_tip","text":"<pre><code>start_arrowhead_tip() -&gt; Point\n</code></pre> <p>Return the tip anchor point of the double-headed arc start arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_arrowhead_tip(self) -&gt; Point:\n    \"\"\"Return the tip anchor point of the double-headed arc start arrowhead.\"\"\"\n    segment = self.segments[0]\n    segment = Segment(segment.p2, segment.p1)\n    segment_length = segment.length()\n    if segment_length == 0:\n        return segment.p2\n    fraction = 1 - self.start_shorten / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNDoubleHeadedArc.start_point","title":"start_point","text":"<pre><code>start_point() -&gt; Point\n</code></pre> <p>Return the starting point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_point(self) -&gt; Point:\n    \"\"\"Return the starting point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[0]\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNDoubleHeadedArc.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNLayout","title":"SBGNLayout  <code>dataclass</code>","text":"<pre><code>SBGNLayout(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = white, filter_: NoneValueType | Filter | None = None, height: float, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = None, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: NoneValueType | float | None = None, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float)\n</code></pre> <p>               Bases: <code>Layout</code></p> <p>Base class for SBGN layouts.</p> <p>SBGN layouts define the visual representation of SBGN models, including the positions and styles of glyphs.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'ac500afa-020a-4543-9d1a-34871dc84500'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> required <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>None</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the node</p> <code>None</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> required <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>angle</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Return the east anchor of the node.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>is_sublayout</code> <p>Return <code>True</code> if the layout is a sublayout of another given layout, <code>False</code> otherwise.</p> <code>label_center</code> <p>Return the label center anchor of the node.</p> <code>north</code> <p>Return the north anchor of the node.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Return the south anchor of the node.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Return the west anchor of the node.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNLayout.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNLayout.angle","title":"angle","text":"<pre><code>angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNLayout.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNLayout.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNLayout.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNLayout.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNLayout.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNLayout.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNLayout.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNLayout.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNLayout.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Return the east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Return the east anchor of the node.\"\"\"\n    return self.own_angle(0)\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNLayout.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNLayout.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNLayout.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNLayout.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNLayout.is_sublayout","title":"is_sublayout","text":"<pre><code>is_sublayout(other: Layout, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout is a sublayout of another given layout, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def is_sublayout(\n    self, other: \"Layout\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout is a sublayout of another given layout, `False` otherwise.\"\"\"\n\n    def _is_sublist(\n        list1: list[LayoutElement],\n        list2: list[LayoutElement],\n        unordered: bool = False,\n    ) -&gt; bool:\n        if not unordered:\n            i = 0\n            for elem1 in list1:\n                elem2 = list2[i]\n                while elem2 != elem1 and i &lt; len(list2) - 1:\n                    i += 1\n                    elem2 = list2[i]\n                if not elem2 == elem1:\n                    return False\n                i += 1\n        else:\n            dlist1 = collections.defaultdict(int)\n            dlist2 = collections.defaultdict(int)\n            for elem1 in list1:\n                dlist1[elem1] += 1\n            for elem2 in list2:\n                dlist2[elem2] += 1\n            for elem in dlist1:\n                if dlist1[elem] &gt; dlist2[elem]:\n                    return False\n        return True\n\n    if self.childless() != other.childless():\n        return False\n    if flattened:\n        return _is_sublist(\n            self.flattened()[1:],\n            other.flattened()[1:],\n            unordered=unordered,\n        )\n    return _is_sublist(self.children(), other.children(), unordered=unordered)\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNLayout.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Return the label center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Return the label center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNLayout.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Return the north anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Return the north anchor of the node.\"\"\"\n    return self.own_angle(90)\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNLayout.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNLayout.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNLayout.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNLayout.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNLayout.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNLayout.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNLayout.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNLayout.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNLayout.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNLayout.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNLayout.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNLayout.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Return the south anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Return the south anchor of the node.\"\"\"\n    return self.own_angle(270)\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNLayout.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNLayout.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNLayout.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNLayout.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNLayout.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNLayout.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Return the west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Return the west anchor of the node.\"\"\"\n    return self.own_angle(180)\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNLayout.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNLayout.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNLayout.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNLayout.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNMap","title":"SBGNMap  <code>dataclass</code>","text":"<pre><code>SBGNMap(*, id_: str = make_uuid4_as_str(), model: SBGNModel | None = None, layout: SBGNLayout | None = None, layout_model_mapping: LayoutModelMapping | None = None)\n</code></pre> <p>               Bases: <code>Map</code></p> <p>Abstract base class for SBGN maps.</p> <p>SBGN maps combine a model and its visual layout into a complete diagram representation.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'572f3154-53a5-4ad8-a28a-0cf2a2b0cc7e'</code> <code>model</code> <code>SBGNModel | None</code> <p>The model of the map</p> <code>None</code> <code>layout</code> <code>SBGNLayout | None</code> <p>The layout of the map</p> <code>None</code> <code>layout_model_mapping</code> <code>LayoutModelMapping | None</code> <p>The layout model mapping of the map</p> <code>None</code> <p>Methods:</p> Name Description <code>get_mapping</code> <p>Return the model element or layout elements mapped to <code>map_element</code>.</p> <code>is_submap</code> <p>Return <code>True</code> if the <code>Map</code> is a submap of another given map, <code>False</code> otherwise.</p>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNMap.get_mapping","title":"get_mapping","text":"<pre><code>get_mapping(map_element: MapElement) -&gt; ModelElement | list[LayoutElement | frozenset[LayoutElement]] | None\n</code></pre> <p>Return the model element or layout elements mapped to <code>map_element</code>.</p> <p>The lookup is bidirectional: a layout key (a singleton or frozenset) resolves to its model element, and a model element resolves to the list of layout keys mapped to it. Forwards to <code>layout_model_mapping.get_mapping</code>.</p> <p>Returns <code>None</code> when the map has no <code>layout_model_mapping</code> (for example a layout-less map such as one read from SBML).</p> Source code in <code>src/momapy/core/map.py</code> <pre><code>def get_mapping(\n    self,\n    map_element: \"MapElement\",\n) -&gt; \"ModelElement | list[LayoutElement | frozenset[LayoutElement]] | None\":\n    \"\"\"Return the model element or layout elements mapped to `map_element`.\n\n    The lookup is bidirectional: a layout key (a singleton or frozenset)\n    resolves to its model element, and a model element resolves to the\n    list of layout keys mapped to it. Forwards to\n    `layout_model_mapping.get_mapping`.\n\n    Returns `None` when the map has no `layout_model_mapping` (for\n    example a layout-less map such as one read from SBML).\n    \"\"\"\n    if self.layout_model_mapping is None:\n        return None\n    return self.layout_model_mapping.get_mapping(map_element)\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNMap.is_submap","title":"is_submap","text":"<pre><code>is_submap(other: Map) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the <code>Map</code> is a submap of another given map, <code>False</code> otherwise.</p> <p>A complete map is never a submap of an incomplete one: if either <code>self</code> or <code>other</code> has a <code>None</code> model, layout, or layout-model mapping, this returns <code>False</code>.</p> Source code in <code>src/momapy/core/map.py</code> <pre><code>def is_submap(self, other: \"Map\") -&gt; bool:\n    \"\"\"Return `True` if the `Map` is a submap of another given map, `False` otherwise.\n\n    A complete map is never a submap of an incomplete one: if either `self`\n    or `other` has a `None` model, layout, or layout-model mapping, this\n    returns `False`.\n    \"\"\"\n    if (\n        self.model is None\n        or self.layout is None\n        or self.layout_model_mapping is None\n        or other.model is None\n        or other.layout is None\n        or other.layout_model_mapping is None\n    ):\n        return False\n    return (\n        self.model.is_submodel(other.model)\n        and self.layout.is_sublayout(other.layout)\n        and self.layout_model_mapping.is_submapping(other.layout_model_mapping)\n    )\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNModel","title":"SBGNModel  <code>dataclass</code>","text":"<pre><code>SBGNModel(*, id_: str = make_uuid4_as_str())\n</code></pre> <p>               Bases: <code>Model</code></p> <p>Abstract base class for SBGN models.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'5dc2e990-ac36-4cf9-ba85-8fd9326e8869'</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from this <code>Model</code>.</p> <code>is_submodel</code> <p>Return whether this model is a submodel of another model.</p>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNModel.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from this <code>Model</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers of the <code>Model</code>, deduplicating by object identity. The <code>Model</code> itself is not a <code>ModelElement</code> and is not included.</p> <p>This mirrors <code>ModelElement.descendants</code> and contrasts with <code>LayoutElement.descendants</code>, which walks the explicit <code>children()</code> tree (visual containment) rather than reference reachability.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order.</p> Source code in <code>src/momapy/core/model.py</code> <pre><code>def descendants(self) -&gt; list[ModelElement]:\n    \"\"\"Return every `ModelElement` reachable from this `Model`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers of the `Model`, deduplicating by\n    object identity. The `Model` itself is not a `ModelElement` and is\n    not included.\n\n    This mirrors\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and contrasts with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which walks the explicit `children()` tree (visual *containment*)\n    rather than reference reachability.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order.\n    \"\"\"\n    seen: set[int] = set()\n    result: list[ModelElement] = []\n    for field in dataclasses.fields(type(self)):\n        _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNModel.is_submodel","title":"is_submodel  <code>abstractmethod</code>","text":"<pre><code>is_submodel(other: Model) -&gt; bool\n</code></pre> <p>Return whether this model is a submodel of another model.</p> <p>Parameters:</p> Name Type Description Default <code>other</code> <code>Model</code> <p>The model to test against.</p> required <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> if this model is a submodel of <code>other</code>.</p> Source code in <code>src/momapy/core/model.py</code> <pre><code>@abc.abstractmethod\ndef is_submodel(self, other: \"Model\") -&gt; bool:\n    \"\"\"Return whether this model is a submodel of another model.\n\n    Args:\n        other: The model to test against.\n\n    Returns:\n        `True` if this model is a submodel of `other`.\n    \"\"\"\n    pass\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNModelElement","title":"SBGNModelElement  <code>dataclass</code>","text":"<pre><code>SBGNModelElement(*, id_: str = make_uuid4_as_str())\n</code></pre> <p>               Bases: <code>ModelElement</code></p> <p>Abstract base class for SBGN model elements.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'d413c96f-5df5-4c73-8605-994f453f6ca9'</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNModelElement.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNNode","title":"SBGNNode  <code>dataclass</code>","text":"<pre><code>SBGNNode(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, fill: NoneValueType | Color | None = white, filter_: NoneValueType | Filter | None = None, height: float, label: TextLayout | None = None, position: Point, stroke: NoneValueType | Color | None = black, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: float | None = 1.25, transform: NoneValueType | tuple[Transformation, ...] | None = None, width: float)\n</code></pre> <p>               Bases: <code>Node</code></p> <p>Abstract base class for SBGN nodes.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'67a225a3-a638-4718-b56e-a18ab0e5df29'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the node</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the node</p> <code>None</code> <code>height</code> <code>float</code> <p>The height of the node</p> required <code>label</code> <code>TextLayout | None</code> <p>The label of the node</p> <code>None</code> <code>position</code> <code>Point</code> <p>The position of the node</p> required <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the node</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the node</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the node</p> <code>None</code> <code>stroke_width</code> <code>float | None</code> <p>The stroke width of the node</p> <code>1.25</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the node</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of the node</p> required <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>angle</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>border</code> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>center</code> <p>Return the center anchor of the node.</p> <code>childless</code> <p>Return a copy of the node with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>east</code> <p>Return the east anchor of the node.</p> <code>east_north_east</code> <p>Return the east north east anchor of the node.</p> <code>east_south_east</code> <p>Return the east south east anchor of the node.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>label_center</code> <p>Return the label center anchor of the node.</p> <code>north</code> <p>Return the north anchor of the node.</p> <code>north_east</code> <p>Return the north east anchor of the node.</p> <code>north_north_east</code> <p>Return the north north east anchor of the node.</p> <code>north_north_west</code> <p>Return the north north west anchor of the node.</p> <code>north_west</code> <p>Return the north west anchor of the node.</p> <code>own_angle</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_border</code> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <code>own_children</code> <p>Return the self children of the node. A node has unique child that is its label.</p> <code>own_drawing_elements</code> <p>Return the node's own drawing elements.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>size</code> <p>Return the size of the node.</p> <code>south</code> <p>Return the south anchor of the node.</p> <code>south_east</code> <p>Return the south east anchor of the node.</p> <code>south_south_east</code> <p>Return the south south east anchor of the node.</p> <code>south_south_west</code> <p>Return the south south west anchor of the node.</p> <code>south_west</code> <p>Return the south west anchor of the node.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p> <code>west</code> <p>Return the west anchor of the node.</p> <code>west_north_west</code> <p>Return the west north west anchor of the node.</p> <code>west_south_west</code> <p>Return the west south west anchor of the node.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>float</code> <p>Return the x coordinate of the node.</p> <code>y</code> <code>float</code> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNNode.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNNode.angle","title":"angle","text":"<pre><code>angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNNode.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNNode.border","title":"border","text":"<pre><code>border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNNode.center","title":"center","text":"<pre><code>center() -&gt; Point\n</code></pre> <p>Return the center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def center(self) -&gt; Point:\n    \"\"\"Return the center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNNode.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the node with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the node with no children.\"\"\"\n    return dataclasses.replace(self, label=None, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNNode.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNNode.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNNode.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNNode.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNNode.east","title":"east","text":"<pre><code>east() -&gt; Point\n</code></pre> <p>Return the east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east(self) -&gt; Point:\n    \"\"\"Return the east anchor of the node.\"\"\"\n    return self.own_angle(0)\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNNode.east_north_east","title":"east_north_east","text":"<pre><code>east_north_east() -&gt; Point\n</code></pre> <p>Return the east north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_north_east(self) -&gt; Point:\n    \"\"\"Return the east north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNNode.east_south_east","title":"east_south_east","text":"<pre><code>east_south_east() -&gt; Point\n</code></pre> <p>Return the east south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def east_south_east(self) -&gt; Point:\n    \"\"\"Return the east south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNNode.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNNode.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNNode.label_center","title":"label_center","text":"<pre><code>label_center() -&gt; Point\n</code></pre> <p>Return the label center anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def label_center(self) -&gt; Point:\n    \"\"\"Return the label center anchor of the node.\"\"\"\n    return self.position\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNNode.north","title":"north","text":"<pre><code>north() -&gt; Point\n</code></pre> <p>Return the north anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north(self) -&gt; Point:\n    \"\"\"Return the north anchor of the node.\"\"\"\n    return self.own_angle(90)\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNNode.north_east","title":"north_east","text":"<pre><code>north_east() -&gt; Point\n</code></pre> <p>Return the north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_east(self) -&gt; Point:\n    \"\"\"Return the north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNNode.north_north_east","title":"north_north_east","text":"<pre><code>north_north_east() -&gt; Point\n</code></pre> <p>Return the north north east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_east(self) -&gt; Point:\n    \"\"\"Return the north north east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, -self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNNode.north_north_west","title":"north_north_west","text":"<pre><code>north_north_west() -&gt; Point\n</code></pre> <p>Return the north north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_north_west(self) -&gt; Point:\n    \"\"\"Return the north north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNNode.north_west","title":"north_west","text":"<pre><code>north_west() -&gt; Point\n</code></pre> <p>Return the north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def north_west(self) -&gt; Point:\n    \"\"\"Return the north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNNode.own_angle","title":"own_angle","text":"<pre><code>own_angle(angle: float, unit: Literal['degrees', 'radians'] = 'degrees') -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_angle(\n    self,\n    angle: float,\n    unit: typing.Literal[\"degrees\", \"radians\"] = \"degrees\",\n) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line passing through the center anchor point of the node and at a given angle from the horizontal.\"\"\"\n    return get_drawing_elements_angle(\n        drawing_elements=self.own_drawing_elements(),\n        angle=angle,\n        unit=unit,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNNode.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNNode.own_border","title":"own_border","text":"<pre><code>own_border(point: Point) -&gt; Point | None\n</code></pre> <p>Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_border(self, point: Point) -&gt; Point | None:\n    \"\"\"Return the point on the border of the node that intersects the self drawing elements of the node with the line formed of the center anchor point of the node and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    return get_drawing_elements_border(\n        drawing_elements=self.own_drawing_elements(),\n        point=point,\n        center=self.center(),\n    )\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNNode.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the node. A node has unique child that is its label.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the node. A node has unique child that is its label.\"\"\"\n    if self.label is not None:\n        return [self.label]\n    return []\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNNode.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the node's own drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the node's own drawing elements.\"\"\"\n    drawing_elements = self._border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNNode.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNNode.size","title":"size","text":"<pre><code>size() -&gt; tuple[float, float]\n</code></pre> <p>Return the size of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def size(self) -&gt; tuple[float, float]:\n    \"\"\"Return the size of the node.\"\"\"\n    return (self.width, self.height)\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNNode.south","title":"south","text":"<pre><code>south() -&gt; Point\n</code></pre> <p>Return the south anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south(self) -&gt; Point:\n    \"\"\"Return the south anchor of the node.\"\"\"\n    return self.own_angle(270)\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNNode.south_east","title":"south_east","text":"<pre><code>south_east() -&gt; Point\n</code></pre> <p>Return the south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_east(self) -&gt; Point:\n    \"\"\"Return the south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNNode.south_south_east","title":"south_south_east","text":"<pre><code>south_south_east() -&gt; Point\n</code></pre> <p>Return the south south east anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_east(self) -&gt; Point:\n    \"\"\"Return the south south east anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNNode.south_south_west","title":"south_south_west","text":"<pre><code>south_south_west() -&gt; Point\n</code></pre> <p>Return the south south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_south_west(self) -&gt; Point:\n    \"\"\"Return the south south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 4, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNNode.south_west","title":"south_west","text":"<pre><code>south_west() -&gt; Point\n</code></pre> <p>Return the south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def south_west(self) -&gt; Point:\n    \"\"\"Return the south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 2))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNNode.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNNode.west","title":"west","text":"<pre><code>west() -&gt; Point\n</code></pre> <p>Return the west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west(self) -&gt; Point:\n    \"\"\"Return the west anchor of the node.\"\"\"\n    return self.own_angle(180)\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNNode.west_north_west","title":"west_north_west","text":"<pre><code>west_north_west() -&gt; Point\n</code></pre> <p>Return the west north west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_north_west(self) -&gt; Point:\n    \"\"\"Return the west north west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() - (self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNNode.west_south_west","title":"west_south_west","text":"<pre><code>west_south_west() -&gt; Point\n</code></pre> <p>Return the west south west anchor of the node.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def west_south_west(self) -&gt; Point:\n    \"\"\"Return the west south west anchor of the node.\"\"\"\n    line = Line(self.center(), self.center() + (-self.width / 2, self.height / 4))\n    angle = -line.get_angle_to_horizontal()\n    return self.own_angle(angle, unit=\"radians\")\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNNode.x","title":"x  <code>property</code>","text":"<pre><code>x: float\n</code></pre> <p>Return the x coordinate of the node.</p>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNNode.y","title":"y  <code>property</code>","text":"<pre><code>y: float\n</code></pre> <p>Return the y coordinate of the node.</p>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNRole","title":"SBGNRole  <code>dataclass</code>","text":"<pre><code>SBGNRole(*, id_: str = make_uuid4_as_str(), referred_element: SBGNModelElement)\n</code></pre> <p>               Bases: <code>SBGNModelElement</code></p> <p>Abstract base class for SBGN roles.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'aaa08408-6af3-4570-aa77-e2f70854aef4'</code> <code>referred_element</code> <code>SBGNModelElement</code> <p>The SBGN model element this role refers to.</p> required <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNRole.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNSingleHeadedArc","title":"SBGNSingleHeadedArc  <code>dataclass</code>","text":"<pre><code>SBGNSingleHeadedArc(*, id_: str = make_uuid4_as_str(), layout_elements: tuple[LayoutElement, ...] = tuple(), group_fill: NoneValueType | Color | None = None, group_fill_rule: FillRule | None = None, group_filter: NoneValueType | Filter | None = None, group_font_family: str | None = None, group_font_size: float | None = None, group_font_style: FontStyle | None = None, group_font_weight: FontWeight | float | None = None, group_stroke: NoneValueType | Color | None = None, group_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, group_stroke_dashoffset: NoneValueType | float | None = None, group_stroke_width: NoneValueType | float | None = None, group_text_anchor: TextAnchor | None = None, group_transform: NoneValueType | tuple[Transformation, ...] | None = None, end_shorten: float = 0.0, fill: NoneValueType | Color | None = None, filter_: NoneValueType | Filter | None = None, path_fill: NoneValueType | Color | None = NoneValue, path_filter: NoneValueType | Filter | None = None, path_stroke: NoneValueType | Color | None = black, path_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, path_stroke_dashoffset: NoneValueType | float | None = None, path_stroke_width: float | None = 1.25, path_transform: NoneValueType | tuple[Transformation, ...] | None = None, stroke: NoneValueType | Color | None = None, stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, stroke_dashoffset: NoneValueType | float | None = None, stroke_width: NoneValueType | float | None = None, segments: tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...] = tuple(), source: LayoutElement | None = None, start_shorten: float = 0.0, target: LayoutElement | None = None, transform: NoneValueType | tuple[Transformation, ...] | None = None, arrowhead_fill: NoneValueType | Color | None = white, arrowhead_filter: NoneValueType | Filter | None = None, arrowhead_stroke: NoneValueType | Color | None = black, arrowhead_stroke_dasharray: NoneValueType | tuple[float, ...] | None = None, arrowhead_stroke_dashoffset: NoneValueType | float | None = None, arrowhead_stroke_width: float | None = 1.25, arrowhead_transform: NoneValueType | tuple[Transformation, ...] | None = None)\n</code></pre> <p>               Bases: <code>SingleHeadedArc</code></p> <p>Abstract base class for SBGN single-headed arcs.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'17f1b29d-665e-418e-be5e-35d65ecab860'</code> <code>layout_elements</code> <code>tuple[LayoutElement, ...]</code> <p>The sub-layout elements of the group layout. These are part of the children of the group layout</p> <code>&lt;dynamic&gt;</code> <code>group_fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the group layout</p> <code>None</code> <code>group_fill_rule</code> <code>FillRule | None</code> <p>The fill rule of the group layout</p> <code>None</code> <code>group_filter</code> <code>NoneValueType | Filter | None</code> <p>The filter of the group layout</p> <code>None</code> <code>group_font_family</code> <code>str | None</code> <p>The font family of the group layout</p> <code>None</code> <code>group_font_size</code> <code>float | None</code> <p>The font size of the group layout</p> <code>None</code> <code>group_font_style</code> <code>FontStyle | None</code> <p>The font style of the group layout</p> <code>None</code> <code>group_font_weight</code> <code>FontWeight | float | None</code> <p>The font weight of the group layout</p> <code>None</code> <code>group_stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the group layout</p> <code>None</code> <code>group_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the group layout</p> <code>None</code> <code>group_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the group layout</p> <code>None</code> <code>group_stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the group layout</p> <code>None</code> <code>group_text_anchor</code> <code>TextAnchor | None</code> <p>The text anchor of the group layout</p> <code>None</code> <code>group_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the group layout</p> <code>None</code> <code>end_shorten</code> <code>float</code> <p>The length the end of the arc will be shorten by</p> <code>0.0</code> <code>fill</code> <code>NoneValueType | Color | None</code> <p>The fill color of the arc</p> <code>None</code> <code>filter_</code> <code>NoneValueType | Filter | None</code> <p>The filter of the arc</p> <code>None</code> <code>path_fill</code> <code>NoneValueType | Color | None</code> <p>The path fill color of the arc</p> <code>&lt;momapy.drawing.NoneValueType object at 0x7f8b17323e00&gt;</code> <code>path_filter</code> <code>NoneValueType | Filter | None</code> <p>The path filter of the arc</p> <code>None</code> <code>path_stroke</code> <code>NoneValueType | Color | None</code> <p>The path stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>path_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The path stroke dasharray of the arc</p> <code>None</code> <code>path_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The path stroke dashoffset of the arc</p> <code>None</code> <code>path_stroke_width</code> <code>float | None</code> <p>The path stroke width of the arc</p> <code>1.25</code> <code>path_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The path transform of the arc</p> <code>None</code> <code>stroke</code> <code>NoneValueType | Color | None</code> <p>The stroke color of the arc</p> <code>None</code> <code>stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The stroke dasharray of the arc</p> <code>None</code> <code>stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The stroke dashoffset of the arc</p> <code>None</code> <code>stroke_width</code> <code>NoneValueType | float | None</code> <p>The stroke width of the arc</p> <code>None</code> <code>segments</code> <code>tuple[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc, ...]</code> <p>The path segments of the arc</p> <code>&lt;dynamic&gt;</code> <code>source</code> <code>LayoutElement | None</code> <p>The source of the arc</p> <code>None</code> <code>start_shorten</code> <code>float</code> <p>The length the start of the arc will be shorten by</p> <code>0.0</code> <code>target</code> <code>LayoutElement | None</code> <p>The target of the arc</p> <code>None</code> <code>transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The transform of the arc</p> <code>None</code> <code>arrowhead_fill</code> <code>NoneValueType | Color | None</code> <p>The arrowhead fill color of the arc</p> <code>Color(red=255, green=255, blue=255, alpha=1.0)</code> <code>arrowhead_filter</code> <code>NoneValueType | Filter | None</code> <p>The arrowhead filter of the arc</p> <code>None</code> <code>arrowhead_stroke</code> <code>NoneValueType | Color | None</code> <p>The arrowhead stroke color of the arc</p> <code>Color(red=0, green=0, blue=0, alpha=1.0)</code> <code>arrowhead_stroke_dasharray</code> <code>NoneValueType | tuple[float, ...] | None</code> <p>The arrowhead stroke dasharray of the arc</p> <code>None</code> <code>arrowhead_stroke_dashoffset</code> <code>NoneValueType | float | None</code> <p>The arrowhead stroke dashoffset of the arc</p> <code>None</code> <code>arrowhead_stroke_width</code> <code>float | None</code> <p>The arrowhead stroke width of the arc</p> <code>1.25</code> <code>arrowhead_transform</code> <code>NoneValueType | tuple[Transformation, ...] | None</code> <p>The arrowhead transform of the arc</p> <code>None</code> <p>Methods:</p> Name Description <code>anchor_point</code> <p>Return an anchor point of the layout element.</p> <code>arrowhead_base</code> <p>Return the arrowhead base anchor point of the single-headed arc.</p> <code>arrowhead_bbox</code> <p>Return the bounding box of the single-headed arc arrowhead.</p> <code>arrowhead_border</code> <p>Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.</p> <code>arrowhead_drawing_elements</code> <p>Return the drawing elements of the single-headed arc arrowhead.</p> <code>arrowhead_length</code> <p>Return the length of the single-headed arc arrowhead.</p> <code>arrowhead_tip</code> <p>Return the arrowhead tip anchor point of the single-headed arc.</p> <code>bbox</code> <p>Compute and return the bounding box of the group layout element.</p> <code>childless</code> <p>Return a copy of the arc with no children.</p> <code>children</code> <p>Return the children of the group layout.</p> <code>contains</code> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> <code>descendants</code> <p>Return the descendants of the layout element.</p> <code>drawing_elements</code> <p>Return the drawing elements of the group layout.</p> <code>end_point</code> <p>Return the ending point of the arc.</p> <code>equals</code> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> <code>flattened</code> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> <code>fraction</code> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <code>length</code> <p>Return the total length of the arc path.</p> <code>own_bbox</code> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> <code>own_children</code> <p>Return the self children of the arc.</p> <code>own_drawing_elements</code> <p>Return the self drawing elements of the single-headed arc.</p> <code>own_to_geometry</code> <p>Return a list of geometry primitives from the self drawing elements.</p> <code>path_drawing_elements</code> <p>Return the drawing elements of the single-headed arc path.</p> <code>points</code> <p>Return the points of the arc path.</p> <code>start_point</code> <p>Return the starting point of the arc.</p> <code>to_geometry</code> <p>Return a list of geometry primitives from the drawing elements.</p>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNSingleHeadedArc.anchor_point","title":"anchor_point","text":"<pre><code>anchor_point(anchor_name: str) -&gt; Point\n</code></pre> <p>Return an anchor point of the layout element.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def anchor_point(self, anchor_name: str) -&gt; Point:\n    \"\"\"Return an anchor point of the layout element.\"\"\"\n    return getattr(self, anchor_name)()\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNSingleHeadedArc.arrowhead_base","title":"arrowhead_base","text":"<pre><code>arrowhead_base() -&gt; Point\n</code></pre> <p>Return the arrowhead base anchor point of the single-headed arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_base(self) -&gt; Point:\n    \"\"\"Return the arrowhead base anchor point of the single-headed arc.\"\"\"\n    arrowhead_length = self.arrowhead_length()\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return self.arrowhead_tip() - (arrowhead_length, 0)\n    fraction = 1 - (arrowhead_length + self.end_shorten) / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNSingleHeadedArc.arrowhead_bbox","title":"arrowhead_bbox","text":"<pre><code>arrowhead_bbox() -&gt; Bbox\n</code></pre> <p>Return the bounding box of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_bbox(self) -&gt; Bbox:\n    \"\"\"Return the bounding box of the single-headed arc arrowhead.\"\"\"\n    return get_drawing_elements_bbox(self.arrowhead_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNSingleHeadedArc.arrowhead_border","title":"arrowhead_border","text":"<pre><code>arrowhead_border(point: Point) -&gt; Point\n</code></pre> <p>Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.</p> <p>When there are multiple intersection points, the one closest to the given point is returned.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_border(self, point: Point) -&gt; Point:\n    \"\"\"Return the point at the intersection of the drawing elements of the single-headed arc arrowhead and the line going through the center of these drawing elements and the given point.\n\n    When there are multiple intersection points, the one closest to the given point is returned.\n    \"\"\"\n    point = get_drawing_elements_border(self.arrowhead_drawing_elements(), point)\n    if point is None:\n        return self.arrowhead_tip()\n    return point\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNSingleHeadedArc.arrowhead_drawing_elements","title":"arrowhead_drawing_elements","text":"<pre><code>arrowhead_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_drawing_elements(\n    self,\n) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the single-headed arc arrowhead.\"\"\"\n    drawing_elements = self._arrowhead_border_drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}_arrowhead\",\n        elements=tuple(drawing_elements),\n        fill=self.arrowhead_fill,\n        filter_=self.arrowhead_filter,\n        id_=f\"{self.id_}_arrowhead\",\n        stroke=self.arrowhead_stroke,\n        stroke_dasharray=self.arrowhead_stroke_dasharray,\n        stroke_dashoffset=self.arrowhead_stroke_dashoffset,\n        stroke_width=self.arrowhead_stroke_width,\n        transform=self.arrowhead_transform,\n    )\n    transformation = self._get_arrowhead_transformation()\n    group = group.transformed(transformation)\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNSingleHeadedArc.arrowhead_length","title":"arrowhead_length","text":"<pre><code>arrowhead_length() -&gt; float\n</code></pre> <p>Return the length of the single-headed arc arrowhead.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_length(self) -&gt; float:\n    \"\"\"Return the length of the single-headed arc arrowhead.\"\"\"\n    bbox = get_drawing_elements_bbox(self._arrowhead_border_drawing_elements())\n    if math.isnan(bbox.width):\n        return 0.0\n    return bbox.east().x\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNSingleHeadedArc.arrowhead_tip","title":"arrowhead_tip","text":"<pre><code>arrowhead_tip() -&gt; Point\n</code></pre> <p>Return the arrowhead tip anchor point of the single-headed arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def arrowhead_tip(self) -&gt; Point:\n    \"\"\"Return the arrowhead tip anchor point of the single-headed arc.\"\"\"\n    segment = self.segments[-1]\n    segment_length = segment.length()\n    if segment_length == 0:\n        return segment.p2\n    fraction = 1 - self.end_shorten / segment_length\n    return segment.get_position_at_fraction(fraction)\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNSingleHeadedArc.bbox","title":"bbox","text":"<pre><code>bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the group layout element.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the group layout element.\"\"\"\n    own_bbox = self.own_bbox()\n    bboxes = [child.bbox() for child in self.children()]\n    min_x = own_bbox.north_west().x\n    min_y = own_bbox.north_west().y\n    max_x = own_bbox.south_east().x\n    max_y = own_bbox.south_east().y\n    for bbox in bboxes:\n        if bbox.north_west().x &lt; min_x:\n            min_x = bbox.north_west().x\n        if bbox.north_west().y &lt; min_y:\n            min_y = bbox.north_west().y\n        if bbox.south_east().x &gt; max_x:\n            max_x = bbox.south_east().x\n        if bbox.south_east().y &gt; max_y:\n            max_y = bbox.south_east().y\n    bbox = Bbox(\n        Point(min_x / 2 + max_x / 2, min_y / 2 + max_y / 2),\n        max_x - min_x,\n        max_y - min_y,\n    )\n    return bbox\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNSingleHeadedArc.childless","title":"childless","text":"<pre><code>childless() -&gt; Self\n</code></pre> <p>Return a copy of the arc with no children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def childless(self) -&gt; typing_extensions.Self:\n    \"\"\"Return a copy of the arc with no children.\"\"\"\n    return dataclasses.replace(self, layout_elements=tuple([]))\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNSingleHeadedArc.children","title":"children","text":"<pre><code>children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the children of the group layout.</p> <p>These are the self children of the group layout (returned by the <code>own_children</code> method) and the other children of the group layout (given by the <code>layout_elements</code> attribute).</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the children of the group layout.\n\n    These are the self children of the group layout (returned by the `own_children` method) and the other children of the group layout (given by the `layout_elements` attribute).\n    \"\"\"\n    return self.own_children() + list(self.layout_elements)\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNSingleHeadedArc.contains","title":"contains","text":"<pre><code>contains(other: LayoutElement) -&gt; bool\n</code></pre> <p>Return <code>True</code> if another layout element is a descendant of the layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def contains(self, other: \"LayoutElement\") -&gt; bool:\n    \"\"\"Return `True` if another layout element is a descendant of the layout element, `False` otherwise.\"\"\"\n    return other in self.descendants()\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNSingleHeadedArc.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[LayoutElement]\n</code></pre> <p>Return the descendants of the layout element.</p> <p>This walks the explicit <code>children()</code> tree (i.e. visual containment) depth-first, returning every layout element in the contained subtree (without <code>self</code>).</p> <p>Note the deliberate contrast with <code>ModelElement.descendants</code> and <code>Model.descendants</code>, which reflectively walk the dataclass field-graph (i.e. reference reachability) and deduplicate by object identity. A model element referenced by several parents therefore appears once across the model graph, whereas layout descendants are strictly the contained subtree and a layout element contained under two parents is reached through each.</p> <p>Returns:</p> Type Description <code>list[LayoutElement]</code> <p>The list of descendant layout elements in visit order.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return the descendants of the layout element.\n\n    This walks the explicit `children()` tree (i.e. visual\n    *containment*) depth-first, returning every layout element in the\n    contained subtree (without `self`).\n\n    Note the deliberate contrast with\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and [`Model.descendants`][momapy.core.model.Model.descendants],\n    which reflectively walk the dataclass field-graph (i.e. *reference\n    reachability*) and deduplicate by object identity. A model element\n    referenced by several parents therefore appears once across the\n    model graph, whereas layout descendants are strictly the contained\n    subtree and a layout element contained under two parents is reached\n    through each.\n\n    Returns:\n        The list of descendant layout elements in visit order.\n    \"\"\"\n    descendants = []\n    for child in self.children():\n        descendants.append(child)\n        descendants += child.descendants()\n    return descendants\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNSingleHeadedArc.drawing_elements","title":"drawing_elements","text":"<pre><code>drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the drawing elements of the group layout.</p> <p>The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the drawing elements of the group layout.\n\n    The returned drawing elements are a group drawing element formed of the self drawing elements of the group layout and the drawing elements of its children.\n    \"\"\"\n    drawing_elements = self.own_drawing_elements()\n    for child in self.children():\n        if child is not None:\n            drawing_elements += child.drawing_elements()\n    group = Group(\n        class_=f\"{type(self).__name__}\",\n        elements=tuple(drawing_elements),\n        id_=f\"{self.id_}\",\n        fill=self.group_fill,\n        fill_rule=self.group_fill_rule,\n        filter_=self.group_filter,\n        font_family=self.group_font_family,\n        font_size=self.group_font_size,\n        font_style=self.group_font_style,\n        font_weight=self.group_font_weight,\n        stroke=self.group_stroke,\n        stroke_dasharray=self.group_stroke_dasharray,\n        stroke_dashoffset=self.group_stroke_dashoffset,\n        stroke_width=self.group_stroke_width,\n        text_anchor=self.group_text_anchor,\n        transform=self.group_transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNSingleHeadedArc.end_point","title":"end_point","text":"<pre><code>end_point() -&gt; Point\n</code></pre> <p>Return the ending point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def end_point(self) -&gt; Point:\n    \"\"\"Return the ending point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[-1]\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNSingleHeadedArc.equals","title":"equals","text":"<pre><code>equals(other: LayoutElement, flattened: bool = False, unordered: bool = False) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the layout element is equal to another layout element, <code>False</code> otherwise.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def equals(\n    self, other: \"LayoutElement\", flattened: bool = False, unordered: bool = False\n) -&gt; bool:\n    \"\"\"Return `True` if the layout element is equal to another layout element, `False` otherwise.\"\"\"\n    if type(self) is type(other):\n        if not flattened:\n            return self == other\n        else:\n            if not unordered:\n                return self.flattened() == other.flattened()\n            else:\n                return set(self.flattened()) == set(other.flattened())\n    return False\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNSingleHeadedArc.flattened","title":"flattened","text":"<pre><code>flattened() -&gt; list[LayoutElement]\n</code></pre> <p>Return a list containing copy of the layout element with no children and all its descendants with no children.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def flattened(self) -&gt; list[\"LayoutElement\"]:\n    \"\"\"Return a list containing copy of the layout element with no children and all its descendants with no children.\"\"\"\n    flattened = [self.childless()]\n    for child in self.children():\n        flattened += child.flattened()\n    return flattened\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNSingleHeadedArc.fraction","title":"fraction","text":"<pre><code>fraction(fraction: float) -&gt; tuple[Point, float]\n</code></pre> <p>Return the position and angle on the arc at a given fraction (of the total arc length).</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def fraction(self, fraction: float) -&gt; tuple[Point, float]:\n    \"\"\"Return the position and angle on the arc at a given fraction (of the total arc length).\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    current_length = 0\n    length_to_reach = fraction * self.length()\n    for segment in self.segments:\n        current_length += segment.length()\n        if current_length &gt;= length_to_reach:\n            break\n    segment_start_length = current_length - segment.length()\n    segment_fraction = (length_to_reach - segment_start_length) / segment.length()\n    position, angle = segment.get_position_and_angle_at_fraction(segment_fraction)\n    return position, angle\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNSingleHeadedArc.length","title":"length","text":"<pre><code>length() -&gt; float\n</code></pre> <p>Return the total length of the arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def length(self) -&gt; float:\n    \"\"\"Return the total length of the arc path.\"\"\"\n    return sum([segment.length() for segment in self.segments])\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNSingleHeadedArc.own_bbox","title":"own_bbox","text":"<pre><code>own_bbox() -&gt; Bbox\n</code></pre> <p>Compute and return the bounding box of the self drawing element of the group layout.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_bbox(self) -&gt; Bbox:\n    \"\"\"Compute and return the bounding box of the self drawing element of the group layout.\"\"\"\n    primitives = self.own_to_geometry()\n    if not primitives:\n        return Bbox(Point(0.0, 0.0), 0, 0)\n    bboxes = [p.bbox() for p in primitives]\n    return Bbox.union(bboxes)\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNSingleHeadedArc.own_children","title":"own_children","text":"<pre><code>own_children() -&gt; list[LayoutElement]\n</code></pre> <p>Return the self children of the arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_children(self) -&gt; list[LayoutElement]:\n    \"\"\"Return the self children of the arc.\"\"\"\n    return []\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNSingleHeadedArc.own_drawing_elements","title":"own_drawing_elements","text":"<pre><code>own_drawing_elements() -&gt; list[DrawingElement]\n</code></pre> <p>Return the self drawing elements of the single-headed arc.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_drawing_elements(self) -&gt; list[DrawingElement]:\n    \"\"\"Return the self drawing elements of the single-headed arc.\"\"\"\n    drawing_elements = (\n        self.path_drawing_elements() + self.arrowhead_drawing_elements()\n    )\n    group = Group(\n        class_=f\"{type(self).__name__}_own\",\n        elements=tuple(drawing_elements),\n        fill=self.fill,\n        filter_=self.filter_,\n        id_=f\"{self.id_}_own\",\n        stroke=self.stroke,\n        stroke_dasharray=self.stroke_dasharray,\n        stroke_dashoffset=self.stroke_dashoffset,\n        stroke_width=self.stroke_width,\n        transform=self.transform,\n    )\n    return [group]\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNSingleHeadedArc.own_to_geometry","title":"own_to_geometry","text":"<pre><code>own_to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the self drawing elements.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def own_to_geometry(\n    self,\n) -&gt; list[\n    Segment | QuadraticBezierCurve | CubicBezierCurve | GeometryEllipticalArc\n]:\n    \"\"\"Return a list of geometry primitives from the self drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.own_drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNSingleHeadedArc.path_drawing_elements","title":"path_drawing_elements","text":"<pre><code>path_drawing_elements() -&gt; list[Path]\n</code></pre> <p>Return the drawing elements of the single-headed arc path.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def path_drawing_elements(self) -&gt; list[Path]:\n    \"\"\"Return the drawing elements of the single-headed arc path.\"\"\"\n    arrowhead_length = self.arrowhead_length()\n    if len(self.segments) == 1:\n        segment = (\n            self.segments[0]\n            .shortened(self.start_shorten, \"start\")\n            .shortened(self.end_shorten + arrowhead_length, \"end\")\n        )\n        actions = [\n            MoveTo(segment.p1),\n            self._make_path_action_from_segment(segment),\n        ]\n    else:\n        first_segment = self.segments[0].shortened(self.start_shorten, \"start\")\n        last_segment = self.segments[-1].shortened(\n            self.end_shorten + arrowhead_length, \"end\"\n        )\n        actions = [\n            MoveTo(first_segment.p1),\n            self._make_path_action_from_segment(first_segment),\n        ]\n        for segment in self.segments[1:-1]:\n            action = self._make_path_action_from_segment(segment)\n            actions.append(action)\n        actions.append(self._make_path_action_from_segment(last_segment))\n    path = Path(\n        actions=tuple(actions),\n        class_=f\"{type(self).__name__}_path\",\n        fill=self.path_fill,\n        filter_=self.path_filter,\n        id_=f\"{self.id_}_path\",\n        stroke=self.path_stroke,\n        stroke_dasharray=self.path_stroke_dasharray,\n        stroke_dashoffset=self.path_stroke_dashoffset,\n        stroke_width=self.path_stroke_width,\n        transform=self.path_transform,\n    )\n    return [path]\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNSingleHeadedArc.points","title":"points","text":"<pre><code>points() -&gt; list[Point]\n</code></pre> <p>Return the points of the arc path.</p> <p>An arc with no segments has no points and returns an empty list.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def points(self) -&gt; list[Point]:\n    \"\"\"Return the points of the arc path.\n\n    An arc with no segments has no points and returns an empty list.\n    \"\"\"\n    if not self.segments:\n        return []\n    points = []\n    for segment in self.segments:\n        points.append(segment.p1)\n    points.append(segment.p2)\n    return points\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNSingleHeadedArc.start_point","title":"start_point","text":"<pre><code>start_point() -&gt; Point\n</code></pre> <p>Return the starting point of the arc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arc has no segments.</p> Source code in <code>src/momapy/core/layout.py</code> <pre><code>def start_point(self) -&gt; Point:\n    \"\"\"Return the starting point of the arc.\n\n    Raises:\n        ValueError: If the arc has no segments.\n    \"\"\"\n    if not self.segments:\n        raise ValueError(\"arc has no segments\")\n    return self.points()[0]\n</code></pre>"},{"location":"api_reference/sbgn/sbgn/#momapy.sbgn.SBGNSingleHeadedArc.to_geometry","title":"to_geometry","text":"<pre><code>to_geometry() -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]\n</code></pre> <p>Return a list of geometry primitives from the drawing elements.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def to_geometry(\n    self,\n) -&gt; list[Segment | QuadraticBezierCurve | CubicBezierCurve | EllipticalArc]:\n    \"\"\"Return a list of geometry primitives from the drawing elements.\"\"\"\n    return drawing_elements_to_geometry(self.drawing_elements())\n</code></pre>"},{"location":"api_reference/sbgn/io/sbgnml/","title":"ID assignment","text":"<p>When reading an SBGN-ML file, every element follows a consistent pattern:</p> <ul> <li>Model <code>id_</code> = <code>f\"{xml_id}_model\"</code></li> <li>Layout <code>id_</code> = <code>xml_id</code></li> </ul> <p>Both <code>xml_id_to_model_element</code> and <code>xml_id_to_layout_element</code> are keyed by the raw <code>xml_id</code>.</p> Element type Model <code>id_</code> Layout <code>id_</code> Registered Notes Compartment <code>f\"{glyph_id}_model\"</code> <code>glyph_id</code> yes EntityPool / Subunit <code>f\"{glyph_id}_model\"</code> <code>glyph_id</code> yes Activity <code>f\"{glyph_id}_model\"</code> <code>glyph_id</code> yes StateVariable <code>f\"{glyph_id}_model\"</code> <code>glyph_id</code> yes Frozen child UnitOfInformation <code>f\"{glyph_id}_model\"</code> <code>glyph_id</code> yes Frozen child Submap <code>f\"{glyph_id}_model\"</code> <code>glyph_id</code> yes Terminal / Tag <code>f\"{glyph_id}_model\"</code> <code>glyph_id</code> yes TerminalRef / TagRef <code>f\"{arc_id}_model\"</code> <code>arc_id</code> yes Frozen child StoichiometricProcess <code>f\"{glyph_id}_model\"</code> <code>glyph_id</code> yes Reactant <code>f\"{arc_id}_model\"</code> <code>arc_id</code> yes Frozen child Product <code>f\"{arc_id}_model\"</code> <code>arc_id</code> yes Frozen child LogicalOperator <code>f\"{glyph_id}_model\"</code> <code>glyph_id</code> yes LogicalOperatorInput <code>f\"{arc_id}_model\"</code> <code>arc_id</code> yes Frozen child Modulation <code>f\"{arc_id}_model\"</code> <code>arc_id</code> yes Phenotype <code>f\"{glyph_id}_model\"</code> <code>glyph_id</code> yes In <code>model.processes</code> <p>Map, model, and layout IDs come from <code>&lt;map id=\"...\"&gt;</code>:</p> <ul> <li><code>map_.id_</code> = <code>map_id</code></li> <li><code>model.id_</code> = <code>f\"{map_id}_model\"</code></li> <li><code>layout.id_</code> = <code>f\"{map_id}_layout\"</code></li> </ul> <p>These are only set for SBGN-ML 0.3. SBGN-ML 0.2 has no map id, so the builder falls back to UUID defaults.</p>"},{"location":"api_reference/sbgn/io/sbgnml/#api","title":"API","text":""},{"location":"api_reference/sbgn/io/sbgnml/#momapy.sbgn.io.sbgnml","title":"momapy.sbgn.io.sbgnml","text":"<p>Classes for reading and writing SBGN-ML files.</p> <p>Modules:</p> Name Description <code>reader</code> <p>SBGN-ML reader classes.</p> <code>writer</code> <p>SBGN-ML writer classes.</p> <p>Classes:</p> Name Description <code>SBGNML0_2Reader</code> <p>Class for SBGN-ML 0.2 reader objects.</p> <code>SBGNML0_3Reader</code> <p>Class for SBGN-ML 0.3 reader objects.</p> <code>SBGNML0_3Writer</code> <p>Class for SBGN-ML 0.3 writer objects.</p>"},{"location":"api_reference/sbgn/io/sbgnml/#momapy.sbgn.io.sbgnml.SBGNML0_2Reader","title":"SBGNML0_2Reader","text":"<p>               Bases: <code>_SBGNMLReader</code></p> <p>Class for SBGN-ML 0.2 reader objects.</p> <p>Methods:</p> Name Description <code>check_file</code> <p>Return <code>True</code> if the file is an SBGN-ML 0.2 document.</p> <code>read</code> <p>Read an SBGN-ML file and return a reader result object.</p>"},{"location":"api_reference/sbgn/io/sbgnml/#momapy.sbgn.io.sbgnml.SBGNML0_2Reader.check_file","title":"check_file  <code>classmethod</code>","text":"<pre><code>check_file(file_path: str | PathLike) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the file is an SBGN-ML 0.2 document.</p> <p>Parameters:</p> Name Type Description Default <code>file_path</code> <code>str | PathLike</code> <p>Path of the file to check.</p> required <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> if the file references the SBGN-ML 0.2 namespace, <code>False</code></p> <code>bool</code> <p>otherwise.</p> Source code in <code>src/momapy/sbgn/io/sbgnml/reader.py</code> <pre><code>@classmethod\ndef check_file(cls, file_path: str | os.PathLike) -&gt; bool:\n    \"\"\"Return `True` if the file is an SBGN-ML 0.2 document.\n\n    Args:\n        file_path: Path of the file to check.\n\n    Returns:\n        `True` if the file references the SBGN-ML 0.2 namespace, `False`\n        otherwise.\n    \"\"\"\n    try:\n        with open(file_path, \"rb\") as f:\n            for line in f:\n                if b\"http://sbgn.org/libsbgn/0.2\" in line:\n                    return True\n        return False\n    except Exception:\n        return False\n</code></pre>"},{"location":"api_reference/sbgn/io/sbgnml/#momapy.sbgn.io.sbgnml.SBGNML0_2Reader.read","title":"read  <code>classmethod</code>","text":"<pre><code>read(file_path: str | PathLike, return_type: Literal['map', 'model', 'layout'] = 'map', with_model: bool = True, with_layout: bool = True, with_annotations: bool = True, with_notes: bool = True, xsep: float = 0.0, ysep: float = 0.0, **options: Any) -&gt; ReaderResult\n</code></pre> <p>Read an SBGN-ML file and return a reader result object.</p> <p>Parameters:</p> Name Type Description Default <code>file_path</code> <code>str | PathLike</code> <p>Path of the SBGN-ML file to read.</p> required <code>return_type</code> <code>Literal['map', 'model', 'layout']</code> <p>Shape of <code>result.obj</code>: <code>\"map\"</code> (default) returns an SBGN map, <code>\"model\"</code> returns the bare model, <code>\"layout\"</code> returns the bare layout.</p> <code>'map'</code> <code>with_model</code> <code>bool</code> <p>Whether to build the model. When <code>False</code> and <code>return_type=\"map\"</code>, the map's <code>model</code> is <code>None</code>. Defaults to <code>True</code>.</p> <code>True</code> <code>with_layout</code> <code>bool</code> <p>Whether to build the layout. When <code>False</code> and <code>return_type=\"map\"</code>, the map's <code>layout</code> is <code>None</code>. Defaults to <code>True</code>.</p> <code>True</code> <code>with_annotations</code> <code>bool</code> <p>Whether to read annotations. Defaults to <code>True</code>.</p> <code>True</code> <code>with_notes</code> <code>bool</code> <p>Whether to read notes. Defaults to <code>True</code>.</p> <code>True</code> <code>xsep</code> <code>float</code> <p>Horizontal padding added around the fitted layout elements when the map has no explicit bounding box (passed to <code>set_fit</code>). Defaults to <code>0.0</code>.</p> <code>0.0</code> <code>ysep</code> <code>float</code> <p>Vertical padding added around the fitted layout elements when the map has no explicit bounding box (passed to <code>set_fit</code>). Defaults to <code>0.0</code>.</p> <code>0.0</code> <code>options</code> <code>Any</code> <p>Additional reader-specific options (ignored).</p> <code>{}</code> <p>Returns:</p> Type Description <code>ReaderResult</code> <p>A <code>ReaderResult</code> whose <code>obj</code> is a map, model, or layout depending</p> <code>ReaderResult</code> <p>on <code>return_type</code>.</p> Source code in <code>src/momapy/sbgn/io/sbgnml/reader.py</code> <pre><code>@classmethod\ndef read(\n    cls,\n    file_path: str | os.PathLike,\n    return_type: typing.Literal[\"map\", \"model\", \"layout\"] = \"map\",\n    with_model: bool = True,\n    with_layout: bool = True,\n    with_annotations: bool = True,\n    with_notes: bool = True,\n    xsep: float = 0.0,\n    ysep: float = 0.0,\n    **options: typing.Any,\n) -&gt; ReaderResult:\n    \"\"\"Read an SBGN-ML file and return a reader result object.\n\n    Args:\n        file_path: Path of the SBGN-ML file to read.\n        return_type: Shape of `result.obj`: `\"map\"` (default) returns an\n            SBGN map, `\"model\"` returns the bare model, `\"layout\"` returns\n            the bare layout.\n        with_model: Whether to build the model. When `False` and\n            `return_type=\"map\"`, the map's `model` is `None`. Defaults to\n            `True`.\n        with_layout: Whether to build the layout. When `False` and\n            `return_type=\"map\"`, the map's `layout` is `None`. Defaults to\n            `True`.\n        with_annotations: Whether to read annotations. Defaults to `True`.\n        with_notes: Whether to read notes. Defaults to `True`.\n        xsep: Horizontal padding added around the fitted layout elements\n            when the map has no explicit bounding box (passed to\n            `set_fit`). Defaults to `0.0`.\n        ysep: Vertical padding added around the fitted layout elements\n            when the map has no explicit bounding box (passed to\n            `set_fit`). Defaults to `0.0`.\n        options: Additional reader-specific options (ignored).\n\n    Returns:\n        A `ReaderResult` whose `obj` is a map, model, or layout depending\n        on `return_type`.\n    \"\"\"\n    check_file_exists(file_path)\n    sbgnml_document = lxml.objectify.parse(file_path)\n    sbgnml_sbgn = sbgnml_document.getroot()\n    (\n        obj,\n        annotations,\n        notes,\n        id_to_element,\n        source_id_to_model_element,\n        source_id_to_layout_element,\n        source_id_to_annotations,\n        source_id_to_notes,\n    ) = cls._make_main_obj(\n        sbgnml_map=sbgnml_sbgn.map,\n        return_type=return_type,\n        with_model=with_model,\n        with_layout=with_layout,\n        with_annotations=with_annotations,\n        with_notes=with_notes,\n        xsep=xsep,\n        ysep=ysep,\n    )\n    result = ReaderResult(\n        obj=obj,\n        element_to_notes=notes,\n        element_to_annotations=annotations,\n        id_to_element=id_to_element,\n        source_id_to_model_element=source_id_to_model_element,\n        source_id_to_layout_element=source_id_to_layout_element,\n        source_id_to_annotations=source_id_to_annotations,\n        source_id_to_notes=source_id_to_notes,\n        file_path=file_path,\n    )\n    return result\n</code></pre>"},{"location":"api_reference/sbgn/io/sbgnml/#momapy.sbgn.io.sbgnml.SBGNML0_3Reader","title":"SBGNML0_3Reader","text":"<p>               Bases: <code>_SBGNMLReader</code></p> <p>Class for SBGN-ML 0.3 reader objects.</p> <p>Methods:</p> Name Description <code>check_file</code> <p>Return <code>True</code> if the file is an SBGN-ML 0.3 document.</p> <code>read</code> <p>Read an SBGN-ML file and return a reader result object.</p>"},{"location":"api_reference/sbgn/io/sbgnml/#momapy.sbgn.io.sbgnml.SBGNML0_3Reader.check_file","title":"check_file  <code>classmethod</code>","text":"<pre><code>check_file(file_path: str | PathLike) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the file is an SBGN-ML 0.3 document.</p> <p>Parameters:</p> Name Type Description Default <code>file_path</code> <code>str | PathLike</code> <p>Path of the file to check.</p> required <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> if the file references the SBGN-ML 0.3 namespace, <code>False</code></p> <code>bool</code> <p>otherwise.</p> Source code in <code>src/momapy/sbgn/io/sbgnml/reader.py</code> <pre><code>@classmethod\ndef check_file(cls, file_path: str | os.PathLike) -&gt; bool:\n    \"\"\"Return `True` if the file is an SBGN-ML 0.3 document.\n\n    Args:\n        file_path: Path of the file to check.\n\n    Returns:\n        `True` if the file references the SBGN-ML 0.3 namespace, `False`\n        otherwise.\n    \"\"\"\n    try:\n        with open(file_path, \"rb\") as f:\n            for line in f:\n                if b\"http://sbgn.org/libsbgn/0.3\" in line:\n                    return True\n        return False\n    except Exception:\n        return False\n</code></pre>"},{"location":"api_reference/sbgn/io/sbgnml/#momapy.sbgn.io.sbgnml.SBGNML0_3Reader.read","title":"read  <code>classmethod</code>","text":"<pre><code>read(file_path: str | PathLike, return_type: Literal['map', 'model', 'layout'] = 'map', with_model: bool = True, with_layout: bool = True, with_annotations: bool = True, with_notes: bool = True, xsep: float = 0.0, ysep: float = 0.0, **options: Any) -&gt; ReaderResult\n</code></pre> <p>Read an SBGN-ML file and return a reader result object.</p> <p>Parameters:</p> Name Type Description Default <code>file_path</code> <code>str | PathLike</code> <p>Path of the SBGN-ML file to read.</p> required <code>return_type</code> <code>Literal['map', 'model', 'layout']</code> <p>Shape of <code>result.obj</code>: <code>\"map\"</code> (default) returns an SBGN map, <code>\"model\"</code> returns the bare model, <code>\"layout\"</code> returns the bare layout.</p> <code>'map'</code> <code>with_model</code> <code>bool</code> <p>Whether to build the model. When <code>False</code> and <code>return_type=\"map\"</code>, the map's <code>model</code> is <code>None</code>. Defaults to <code>True</code>.</p> <code>True</code> <code>with_layout</code> <code>bool</code> <p>Whether to build the layout. When <code>False</code> and <code>return_type=\"map\"</code>, the map's <code>layout</code> is <code>None</code>. Defaults to <code>True</code>.</p> <code>True</code> <code>with_annotations</code> <code>bool</code> <p>Whether to read annotations. Defaults to <code>True</code>.</p> <code>True</code> <code>with_notes</code> <code>bool</code> <p>Whether to read notes. Defaults to <code>True</code>.</p> <code>True</code> <code>xsep</code> <code>float</code> <p>Horizontal padding added around the fitted layout elements when the map has no explicit bounding box (passed to <code>set_fit</code>). Defaults to <code>0.0</code>.</p> <code>0.0</code> <code>ysep</code> <code>float</code> <p>Vertical padding added around the fitted layout elements when the map has no explicit bounding box (passed to <code>set_fit</code>). Defaults to <code>0.0</code>.</p> <code>0.0</code> <code>options</code> <code>Any</code> <p>Additional reader-specific options (ignored).</p> <code>{}</code> <p>Returns:</p> Type Description <code>ReaderResult</code> <p>A <code>ReaderResult</code> whose <code>obj</code> is a map, model, or layout depending</p> <code>ReaderResult</code> <p>on <code>return_type</code>.</p> Source code in <code>src/momapy/sbgn/io/sbgnml/reader.py</code> <pre><code>@classmethod\ndef read(\n    cls,\n    file_path: str | os.PathLike,\n    return_type: typing.Literal[\"map\", \"model\", \"layout\"] = \"map\",\n    with_model: bool = True,\n    with_layout: bool = True,\n    with_annotations: bool = True,\n    with_notes: bool = True,\n    xsep: float = 0.0,\n    ysep: float = 0.0,\n    **options: typing.Any,\n) -&gt; ReaderResult:\n    \"\"\"Read an SBGN-ML file and return a reader result object.\n\n    Args:\n        file_path: Path of the SBGN-ML file to read.\n        return_type: Shape of `result.obj`: `\"map\"` (default) returns an\n            SBGN map, `\"model\"` returns the bare model, `\"layout\"` returns\n            the bare layout.\n        with_model: Whether to build the model. When `False` and\n            `return_type=\"map\"`, the map's `model` is `None`. Defaults to\n            `True`.\n        with_layout: Whether to build the layout. When `False` and\n            `return_type=\"map\"`, the map's `layout` is `None`. Defaults to\n            `True`.\n        with_annotations: Whether to read annotations. Defaults to `True`.\n        with_notes: Whether to read notes. Defaults to `True`.\n        xsep: Horizontal padding added around the fitted layout elements\n            when the map has no explicit bounding box (passed to\n            `set_fit`). Defaults to `0.0`.\n        ysep: Vertical padding added around the fitted layout elements\n            when the map has no explicit bounding box (passed to\n            `set_fit`). Defaults to `0.0`.\n        options: Additional reader-specific options (ignored).\n\n    Returns:\n        A `ReaderResult` whose `obj` is a map, model, or layout depending\n        on `return_type`.\n    \"\"\"\n    check_file_exists(file_path)\n    sbgnml_document = lxml.objectify.parse(file_path)\n    sbgnml_sbgn = sbgnml_document.getroot()\n    (\n        obj,\n        annotations,\n        notes,\n        id_to_element,\n        source_id_to_model_element,\n        source_id_to_layout_element,\n        source_id_to_annotations,\n        source_id_to_notes,\n    ) = cls._make_main_obj(\n        sbgnml_map=sbgnml_sbgn.map,\n        return_type=return_type,\n        with_model=with_model,\n        with_layout=with_layout,\n        with_annotations=with_annotations,\n        with_notes=with_notes,\n        xsep=xsep,\n        ysep=ysep,\n    )\n    result = ReaderResult(\n        obj=obj,\n        element_to_notes=notes,\n        element_to_annotations=annotations,\n        id_to_element=id_to_element,\n        source_id_to_model_element=source_id_to_model_element,\n        source_id_to_layout_element=source_id_to_layout_element,\n        source_id_to_annotations=source_id_to_annotations,\n        source_id_to_notes=source_id_to_notes,\n        file_path=file_path,\n    )\n    return result\n</code></pre>"},{"location":"api_reference/sbgn/io/sbgnml/#momapy.sbgn.io.sbgnml.SBGNML0_3Writer","title":"SBGNML0_3Writer","text":"<p>               Bases: <code>Writer</code></p> <p>Class for SBGN-ML 0.3 writer objects.</p> <p>The single SBGN-ML writer (registered under both <code>sbgnml</code> and <code>sbgnml-0.3</code>). All serialization logic lives in module-level functions; this class only provides the <code>write</code> entry point.</p> <p>Methods:</p> Name Description <code>write</code> <p>Write an SBGN map to an SBGN-ML file.</p>"},{"location":"api_reference/sbgn/io/sbgnml/#momapy.sbgn.io.sbgnml.SBGNML0_3Writer.write","title":"write  <code>classmethod</code>","text":"<pre><code>write(obj: SBGNMap, file_path: str | PathLike, element_to_annotations: dict | None = None, element_to_notes: dict | None = None, source_id_to_model_element: dict | None = None, source_id_to_layout_element: dict | None = None, source_id_to_annotations: dict | None = None, source_id_to_notes: dict | None = None, with_annotations: bool = True, with_notes: bool = True, **options: Any) -&gt; WriterResult\n</code></pre> <p>Write an SBGN map to an SBGN-ML file.</p> <p>Parameters:</p> Name Type Description Default <code>obj</code> <code>SBGNMap</code> <p>The SBGN map to serialize.</p> required <code>file_path</code> <code>str | PathLike</code> <p>Destination file path.</p> required <code>element_to_annotations</code> <code>dict | None</code> <p>Optional per-element annotation dict.</p> <code>None</code> <code>element_to_notes</code> <code>dict | None</code> <p>Optional per-element notes dict.</p> <code>None</code> <code>source_id_to_model_element</code> <code>dict | None</code> <p>Optional source ID to model element mapping from ReaderResult.</p> <code>None</code> <code>source_id_to_layout_element</code> <code>dict | None</code> <p>Optional source ID to layout element mapping from ReaderResult.</p> <code>None</code> <code>source_id_to_annotations</code> <code>dict | None</code> <p>Optional per-source-id annotations from ReaderResult.</p> <code>None</code> <code>source_id_to_notes</code> <code>dict | None</code> <p>Optional per-source-id notes from ReaderResult.</p> <code>None</code> <code>with_annotations</code> <code>bool</code> <p>Whether to write annotations.</p> <code>True</code> <code>with_notes</code> <code>bool</code> <p>Whether to write notes.</p> <code>True</code> <code>options</code> <code>Any</code> <p>Additional options (accepted and ignored).</p> <code>{}</code> <p>Returns:</p> Type Description <code>WriterResult</code> <p>WriterResult containing the written object and file path.</p> Source code in <code>src/momapy/sbgn/io/sbgnml/writer.py</code> <pre><code>@classmethod\ndef write(\n    cls,\n    obj: SBGNMap,\n    file_path: str | os.PathLike,\n    element_to_annotations: dict | None = None,\n    element_to_notes: dict | None = None,\n    source_id_to_model_element: dict | None = None,\n    source_id_to_layout_element: dict | None = None,\n    source_id_to_annotations: dict | None = None,\n    source_id_to_notes: dict | None = None,\n    with_annotations: bool = True,\n    with_notes: bool = True,\n    **options: typing.Any,\n) -&gt; WriterResult:\n    \"\"\"Write an SBGN map to an SBGN-ML file.\n\n    Args:\n        obj: The SBGN map to serialize.\n        file_path: Destination file path.\n        element_to_annotations: Optional per-element annotation dict.\n        element_to_notes: Optional per-element notes dict.\n        source_id_to_model_element: Optional source ID to model\n            element mapping from ReaderResult.\n        source_id_to_layout_element: Optional source ID to layout\n            element mapping from ReaderResult.\n        source_id_to_annotations: Optional per-source-id annotations\n            from ReaderResult.\n        source_id_to_notes: Optional per-source-id notes from\n            ReaderResult.\n        with_annotations: Whether to write annotations.\n        with_notes: Whether to write notes.\n        options: Additional options (accepted and ignored).\n\n    Returns:\n        WriterResult containing the written object and file path.\n    \"\"\"\n    if obj.model is None or obj.layout is None:\n        raise ValueError(\"cannot write a map without both a model and a layout\")\n    check_parent_dir_exists(file_path)\n    if element_to_annotations is None:\n        element_to_annotations = {}\n    if element_to_notes is None:\n        element_to_notes = {}\n    writing_context = WritingContext(\n        map_=obj,\n        element_to_annotations=element_to_annotations,\n        element_to_notes=element_to_notes,\n        source_id_to_model_element=source_id_to_model_element,\n        source_id_to_layout_element=source_id_to_layout_element,\n        source_id_to_annotations=source_id_to_annotations,\n        source_id_to_notes=source_id_to_notes,\n        with_annotations=with_annotations,\n        with_notes=with_notes,\n    )\n    sbgnml_sbgn = make_lxml_element(\"sbgn\", nsmap=NSMAP)\n    sbgnml_map = make_sbgnml_map(writing_context)\n    sbgnml_sbgn.append(sbgnml_map)\n    with open(file_path, \"w\", encoding=\"utf-8\") as f:\n        f.write(\n            lxml.etree.tostring(\n                sbgnml_sbgn, pretty_print=True, xml_declaration=True\n            ).decode()\n        )\n    return WriterResult(obj=obj, file_path=file_path)\n</code></pre>"},{"location":"api_reference/sbml/sbml/","title":"SBML","text":""},{"location":"api_reference/sbml/sbml/#momapy.sbml","title":"momapy.sbml","text":"<p>Partial SBML support: reader only, model skeleton (no layout, no writer).</p> <p>This subpackage exposes a minimal SBML model used as a building block by other formats (for example CellDesigner). It is intentionally incomplete: only an SBML reader is provided, there is no layout representation, and there is no writer. Do not rely on it for round-tripping SBML files.</p> <p>Modules:</p> Name Description <code>elements</code> <p>Abstract base classes for SBML model elements.</p> <code>io</code> <p>Input/output module for SBML maps.</p> <code>map</code> <p>Map class for SBML maps.</p> <code>model</code> <p>Concrete SBML model classes.</p> <code>utils</code> <p>Utility functions for SBML map manipulation.</p> <p>Classes:</p> Name Description <code>BQBiol</code> <p>BioModels.net biology qualifiers.</p> <code>BQModel</code> <p>BioModels.net model qualifiers.</p> <code>BiomodelQualifier</code> <p>BioModels.net qualifiers.</p> <code>Compartment</code> <p>Compartment.</p> <code>ModifierSpeciesReference</code> <p>Modifier species reference.</p> <code>RDFAnnotation</code> <p>RDF annotation.</p> <code>Reaction</code> <p>Reaction.</p> <code>SBMLMap</code> <p>Class for SBML maps.</p> <code>SBMLModel</code> <p>SBML model.</p> <code>SBMLModelElement</code> <p>Abstract base class for SBML model elements.</p> <code>SimpleSpeciesReference</code> <p>Abstract base class for simple species references.</p> <code>Species</code> <p>Species.</p> <code>SpeciesReference</code> <p>Species reference.</p>"},{"location":"api_reference/sbml/sbml/#momapy.sbml.BQBiol","title":"BQBiol","text":"<p>               Bases: <code>BiomodelQualifier</code></p> <p>BioModels.net biology qualifiers.</p>"},{"location":"api_reference/sbml/sbml/#momapy.sbml.BQModel","title":"BQModel","text":"<p>               Bases: <code>BiomodelQualifier</code></p> <p>BioModels.net model qualifiers.</p>"},{"location":"api_reference/sbml/sbml/#momapy.sbml.BiomodelQualifier","title":"BiomodelQualifier","text":"<p>               Bases: <code>Enum</code></p> <p>BioModels.net qualifiers.</p>"},{"location":"api_reference/sbml/sbml/#momapy.sbml.Compartment","title":"Compartment  <code>dataclass</code>","text":"<pre><code>Compartment(*, id_: str = make_uuid4_as_str(), name: str | None = None, sbo_term: str | None = None, metaid: str | None = None, outside: Optional[ForwardRef(Compartment, module=model)] = None)\n</code></pre> <p>               Bases: <code>SBMLModelElement</code></p> <p>Compartment.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'4ce66fcb-7660-4bca-9e98-054038145d85'</code> <code>name</code> <code>str | None</code> <p>Human-readable name of the element.</p> <code>None</code> <code>sbo_term</code> <code>str | None</code> <p>Optional SBO term identifier for semantic annotation.</p> <code>None</code> <code>metaid</code> <code>str | None</code> <p>Optional metadata identifier for RDF annotations.</p> <code>None</code> <code>outside</code> <code>ForwardRef | None</code> <p>Optional outer compartment for hierarchical nesting.</p> <code>None</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbml/sbml/#momapy.sbml.Compartment.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbml/sbml/#momapy.sbml.ModifierSpeciesReference","title":"ModifierSpeciesReference  <code>dataclass</code>","text":"<pre><code>ModifierSpeciesReference(*, id_: str = make_uuid4_as_str(), name: str | None = None, sbo_term: str | None = None, metaid: str | None = None, referred_element: Species)\n</code></pre> <p>               Bases: <code>SimpleSpeciesReference</code></p> <p>Modifier species reference.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'9f8c1157-861e-4089-87ed-ecde7637990c'</code> <code>name</code> <code>str | None</code> <p>Human-readable name of the element.</p> <code>None</code> <code>sbo_term</code> <code>str | None</code> <p>Optional SBO term identifier for semantic annotation.</p> <code>None</code> <code>metaid</code> <code>str | None</code> <p>Optional metadata identifier for RDF annotations.</p> <code>None</code> <code>referred_element</code> <code>Species</code> <p>The species being referenced.</p> required <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbml/sbml/#momapy.sbml.ModifierSpeciesReference.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbml/sbml/#momapy.sbml.RDFAnnotation","title":"RDFAnnotation  <code>dataclass</code>","text":"<pre><code>RDFAnnotation(*, qualifier: BiomodelQualifier, resources: frozenset[str] = frozenset())\n</code></pre> <p>RDF annotation.</p> <p>RDF annotations are metadata attached to model elements, not model entities themselves, and are therefore plain frozen dataclasses rather than <code>ModelElement</code> subclasses.</p> <p>Parameters:</p> Name Type Description Default <code>qualifier</code> <code>BiomodelQualifier</code> <p>The BioModels qualifier describing the relationship.</p> required <code>resources</code> <code>frozenset[str]</code> <p>Build an immutable unordered collection of unique elements.</p> <code>&lt;dynamic&gt;</code>"},{"location":"api_reference/sbml/sbml/#momapy.sbml.Reaction","title":"Reaction  <code>dataclass</code>","text":"<pre><code>Reaction(*, id_: str = make_uuid4_as_str(), name: str | None = None, sbo_term: str | None = None, metaid: str | None = None, reversible: bool = False, compartment: Compartment | None = None, reactants: frozenset[SpeciesReference] = frozenset(), products: frozenset[SpeciesReference] = frozenset(), modifiers: frozenset[ModifierSpeciesReference] = frozenset())\n</code></pre> <p>               Bases: <code>SBMLModelElement</code></p> <p>Reaction.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'77561546-694a-41b2-a200-8ad5f1138aef'</code> <code>name</code> <code>str | None</code> <p>Human-readable name of the element.</p> <code>None</code> <code>sbo_term</code> <code>str | None</code> <p>Optional SBO term identifier for semantic annotation.</p> <code>None</code> <code>metaid</code> <code>str | None</code> <p>Optional metadata identifier for RDF annotations.</p> <code>None</code> <code>reversible</code> <code>bool</code> <p>Whether the reaction can proceed in both directions.</p> <code>False</code> <code>compartment</code> <code>Compartment | None</code> <p>Optional compartment where the reaction occurs.</p> <code>None</code> <code>reactants</code> <code>frozenset[SpeciesReference]</code> <p>Build an immutable unordered collection of unique elements.</p> <code>&lt;dynamic&gt;</code> <code>products</code> <code>frozenset[SpeciesReference]</code> <p>Build an immutable unordered collection of unique elements.</p> <code>&lt;dynamic&gt;</code> <code>modifiers</code> <code>frozenset[ModifierSpeciesReference]</code> <p>Build an immutable unordered collection of unique elements.</p> <code>&lt;dynamic&gt;</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbml/sbml/#momapy.sbml.Reaction.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbml/sbml/#momapy.sbml.SBMLMap","title":"SBMLMap  <code>dataclass</code>","text":"<pre><code>SBMLMap(*, id_: str = make_uuid4_as_str(), model: SBMLModel | None = None, layout: Layout | None = None, layout_model_mapping: LayoutModelMapping | None = None)\n</code></pre> <p>               Bases: <code>Map</code></p> <p>Class for SBML maps.</p> <p>SBML support is model only: an <code>SBMLMap</code> carries an <code>SBMLModel</code> and no layout. The inherited <code>layout</code> and <code>layout_model_mapping</code> fields are always <code>None</code>.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'8b4723a3-06b5-4d79-ba50-ad3c29b7870a'</code> <code>model</code> <code>SBMLModel | None</code> <p>The model of the map</p> <code>None</code> <code>layout</code> <code>Layout | None</code> <p>The layout of the map</p> <code>None</code> <code>layout_model_mapping</code> <code>LayoutModelMapping | None</code> <p>The layout model mapping of the map</p> <code>None</code> <p>Methods:</p> Name Description <code>get_mapping</code> <p>Return the model element or layout elements mapped to <code>map_element</code>.</p> <code>is_submap</code> <p>Return <code>True</code> if the <code>Map</code> is a submap of another given map, <code>False</code> otherwise.</p>"},{"location":"api_reference/sbml/sbml/#momapy.sbml.SBMLMap.get_mapping","title":"get_mapping","text":"<pre><code>get_mapping(map_element: MapElement) -&gt; ModelElement | list[LayoutElement | frozenset[LayoutElement]] | None\n</code></pre> <p>Return the model element or layout elements mapped to <code>map_element</code>.</p> <p>The lookup is bidirectional: a layout key (a singleton or frozenset) resolves to its model element, and a model element resolves to the list of layout keys mapped to it. Forwards to <code>layout_model_mapping.get_mapping</code>.</p> <p>Returns <code>None</code> when the map has no <code>layout_model_mapping</code> (for example a layout-less map such as one read from SBML).</p> Source code in <code>src/momapy/core/map.py</code> <pre><code>def get_mapping(\n    self,\n    map_element: \"MapElement\",\n) -&gt; \"ModelElement | list[LayoutElement | frozenset[LayoutElement]] | None\":\n    \"\"\"Return the model element or layout elements mapped to `map_element`.\n\n    The lookup is bidirectional: a layout key (a singleton or frozenset)\n    resolves to its model element, and a model element resolves to the\n    list of layout keys mapped to it. Forwards to\n    `layout_model_mapping.get_mapping`.\n\n    Returns `None` when the map has no `layout_model_mapping` (for\n    example a layout-less map such as one read from SBML).\n    \"\"\"\n    if self.layout_model_mapping is None:\n        return None\n    return self.layout_model_mapping.get_mapping(map_element)\n</code></pre>"},{"location":"api_reference/sbml/sbml/#momapy.sbml.SBMLMap.is_submap","title":"is_submap","text":"<pre><code>is_submap(other: Map) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the <code>Map</code> is a submap of another given map, <code>False</code> otherwise.</p> <p>A complete map is never a submap of an incomplete one: if either <code>self</code> or <code>other</code> has a <code>None</code> model, layout, or layout-model mapping, this returns <code>False</code>.</p> Source code in <code>src/momapy/core/map.py</code> <pre><code>def is_submap(self, other: \"Map\") -&gt; bool:\n    \"\"\"Return `True` if the `Map` is a submap of another given map, `False` otherwise.\n\n    A complete map is never a submap of an incomplete one: if either `self`\n    or `other` has a `None` model, layout, or layout-model mapping, this\n    returns `False`.\n    \"\"\"\n    if (\n        self.model is None\n        or self.layout is None\n        or self.layout_model_mapping is None\n        or other.model is None\n        or other.layout is None\n        or other.layout_model_mapping is None\n    ):\n        return False\n    return (\n        self.model.is_submodel(other.model)\n        and self.layout.is_sublayout(other.layout)\n        and self.layout_model_mapping.is_submapping(other.layout_model_mapping)\n    )\n</code></pre>"},{"location":"api_reference/sbml/sbml/#momapy.sbml.SBMLModel","title":"SBMLModel  <code>dataclass</code>","text":"<pre><code>SBMLModel(*, id_: str = make_uuid4_as_str(), name: str | None = None, sbo_term: str | None = None, metaid: str | None = None, compartments: frozenset[Compartment] = frozenset(), species: frozenset[Species] = frozenset(), reactions: frozenset[Reaction] = frozenset())\n</code></pre> <p>               Bases: <code>Model</code></p> <p>SBML model.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'8792b1d4-0bc8-4b90-88f1-78ab8df1707b'</code> <code>name</code> <code>str | None</code> <p>Human-readable name of the model.</p> <code>None</code> <code>sbo_term</code> <code>str | None</code> <p>Optional SBO term identifier for semantic annotation.</p> <code>None</code> <code>metaid</code> <code>str | None</code> <p>Optional metadata identifier for RDF annotations.</p> <code>None</code> <code>compartments</code> <code>frozenset[Compartment]</code> <p>Build an immutable unordered collection of unique elements.</p> <code>&lt;dynamic&gt;</code> <code>species</code> <code>frozenset[Species]</code> <p>Build an immutable unordered collection of unique elements.</p> <code>&lt;dynamic&gt;</code> <code>reactions</code> <code>frozenset[Reaction]</code> <p>Build an immutable unordered collection of unique elements.</p> <code>&lt;dynamic&gt;</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from this <code>Model</code>.</p> <code>is_submodel</code> <p>Check if this model is a submodel of another model.</p>"},{"location":"api_reference/sbml/sbml/#momapy.sbml.SBMLModel.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from this <code>Model</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers of the <code>Model</code>, deduplicating by object identity. The <code>Model</code> itself is not a <code>ModelElement</code> and is not included.</p> <p>This mirrors <code>ModelElement.descendants</code> and contrasts with <code>LayoutElement.descendants</code>, which walks the explicit <code>children()</code> tree (visual containment) rather than reference reachability.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order.</p> Source code in <code>src/momapy/core/model.py</code> <pre><code>def descendants(self) -&gt; list[ModelElement]:\n    \"\"\"Return every `ModelElement` reachable from this `Model`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers of the `Model`, deduplicating by\n    object identity. The `Model` itself is not a `ModelElement` and is\n    not included.\n\n    This mirrors\n    [`ModelElement.descendants`][momapy.core.elements.ModelElement.descendants]\n    and contrasts with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which walks the explicit `children()` tree (visual *containment*)\n    rather than reference reachability.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order.\n    \"\"\"\n    seen: set[int] = set()\n    result: list[ModelElement] = []\n    for field in dataclasses.fields(type(self)):\n        _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbml/sbml/#momapy.sbml.SBMLModel.is_submodel","title":"is_submodel","text":"<pre><code>is_submodel(other: SBMLModel) -&gt; bool\n</code></pre> <p>Check if this model is a submodel of another model.</p> <p>Parameters:</p> Name Type Description Default <code>other</code> <code>SBMLModel</code> <p>Another SBML model to compare against.</p> required <p>Returns:</p> Type Description <code>bool</code> <p>True if this model is a submodel of <code>other</code>, False otherwise.</p> Source code in <code>src/momapy/sbml/model.py</code> <pre><code>def is_submodel(self, other: \"SBMLModel\") -&gt; bool:\n    \"\"\"Check if this model is a submodel of another model.\n\n    Args:\n        other: Another SBML model to compare against.\n\n    Returns:\n        True if this model is a submodel of `other`, False otherwise.\n    \"\"\"\n    return (\n        self.compartments.issubset(other.compartments)\n        and self.species.issubset(other.species)\n        and self.reactions.issubset(other.reactions)\n    )\n</code></pre>"},{"location":"api_reference/sbml/sbml/#momapy.sbml.SBMLModelElement","title":"SBMLModelElement  <code>dataclass</code>","text":"<pre><code>SBMLModelElement(*, id_: str = make_uuid4_as_str(), name: str | None = None, sbo_term: str | None = None, metaid: str | None = None)\n</code></pre> <p>               Bases: <code>ModelElement</code></p> <p>Abstract base class for SBML model elements.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'c71925e2-6726-4a57-95de-afd91a1c73bc'</code> <code>name</code> <code>str | None</code> <p>Human-readable name of the element.</p> <code>None</code> <code>sbo_term</code> <code>str | None</code> <p>Optional SBO term identifier for semantic annotation.</p> <code>None</code> <code>metaid</code> <code>str | None</code> <p>Optional metadata identifier for RDF annotations.</p> <code>None</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbml/sbml/#momapy.sbml.SBMLModelElement.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbml/sbml/#momapy.sbml.SimpleSpeciesReference","title":"SimpleSpeciesReference  <code>dataclass</code>","text":"<pre><code>SimpleSpeciesReference(*, id_: str = make_uuid4_as_str(), name: str | None = None, sbo_term: str | None = None, metaid: str | None = None, referred_element: Species)\n</code></pre> <p>               Bases: <code>SBMLModelElement</code></p> <p>Abstract base class for simple species references.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'83df817a-a131-4b6f-81d6-64c81802886e'</code> <code>name</code> <code>str | None</code> <p>Human-readable name of the element.</p> <code>None</code> <code>sbo_term</code> <code>str | None</code> <p>Optional SBO term identifier for semantic annotation.</p> <code>None</code> <code>metaid</code> <code>str | None</code> <p>Optional metadata identifier for RDF annotations.</p> <code>None</code> <code>referred_element</code> <code>Species</code> <p>The species being referenced.</p> required <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbml/sbml/#momapy.sbml.SimpleSpeciesReference.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbml/sbml/#momapy.sbml.Species","title":"Species  <code>dataclass</code>","text":"<pre><code>Species(*, id_: str = make_uuid4_as_str(), name: str | None = None, sbo_term: str | None = None, metaid: str | None = None, compartment: Compartment | None = None)\n</code></pre> <p>               Bases: <code>SBMLModelElement</code></p> <p>Species.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'2e7ce9c4-a1ad-41f7-b74b-7b31617564b1'</code> <code>name</code> <code>str | None</code> <p>Human-readable name of the element.</p> <code>None</code> <code>sbo_term</code> <code>str | None</code> <p>Optional SBO term identifier for semantic annotation.</p> <code>None</code> <code>metaid</code> <code>str | None</code> <p>Optional metadata identifier for RDF annotations.</p> <code>None</code> <code>compartment</code> <code>Compartment | None</code> <p>The compartment containing this species.</p> <code>None</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbml/sbml/#momapy.sbml.Species.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbml/sbml/#momapy.sbml.SpeciesReference","title":"SpeciesReference  <code>dataclass</code>","text":"<pre><code>SpeciesReference(*, id_: str = make_uuid4_as_str(), name: str | None = None, sbo_term: str | None = None, metaid: str | None = None, referred_element: Species, stoichiometry: float | None = None)\n</code></pre> <p>               Bases: <code>SimpleSpeciesReference</code></p> <p>Species reference.</p> <p>Parameters:</p> Name Type Description Default <code>id_</code> <code>str</code> <p>The id of the map element. This id is purely for the user to keep track     of the element, it does not need to be unique and is not part of the     identity of the element, i.e., it is not considered when testing for     equality between two map elements or when hashing the map element</p> <code>'fd993760-eb01-472f-a543-5ae63fbd83ba'</code> <code>name</code> <code>str | None</code> <p>Human-readable name of the element.</p> <code>None</code> <code>sbo_term</code> <code>str | None</code> <p>Optional SBO term identifier for semantic annotation.</p> <code>None</code> <code>metaid</code> <code>str | None</code> <p>Optional metadata identifier for RDF annotations.</p> <code>None</code> <code>referred_element</code> <code>Species</code> <p>The species being referenced.</p> required <code>stoichiometry</code> <code>float | None</code> <p>Optional stoichiometric coefficient.</p> <code>None</code> <p>Methods:</p> Name Description <code>descendants</code> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p>"},{"location":"api_reference/sbml/sbml/#momapy.sbml.SpeciesReference.descendants","title":"descendants","text":"<pre><code>descendants() -&gt; list[ModelElement]\n</code></pre> <p>Return every <code>ModelElement</code> reachable from <code>self</code>, excluding <code>self</code>.</p> <p>This reflectively walks the dataclass field-graph (i.e. reference reachability) across scalar <code>ModelElement</code> fields and <code>frozenset</code>/<code>tuple</code> containers, deduplicating by object identity. A model element referenced by several parents therefore appears once.</p> <p>Note the deliberate contrast with <code>LayoutElement.descendants</code>, which instead walks the explicit <code>children()</code> tree (visual containment): there a layout element contained under two parents is reached through each, with no identity dedup.</p> <p>Returns:</p> Type Description <code>list[ModelElement]</code> <p>The list of reachable <code>ModelElement</code> instances in visit</p> <code>list[ModelElement]</code> <p>order, without <code>self</code>.</p> Source code in <code>src/momapy/core/elements.py</code> <pre><code>def descendants(self) -&gt; list[\"ModelElement\"]:\n    \"\"\"Return every `ModelElement` reachable from `self`, excluding `self`.\n\n    This reflectively walks the dataclass field-graph (i.e. *reference\n    reachability*) across scalar `ModelElement` fields and\n    `frozenset`/`tuple` containers, deduplicating by object identity. A\n    model element referenced by several parents therefore appears once.\n\n    Note the deliberate contrast with\n    [`LayoutElement.descendants`][momapy.core.elements.LayoutElement.descendants],\n    which instead walks the explicit `children()` tree (visual\n    *containment*): there a layout element contained under two parents is\n    reached through each, with no identity dedup.\n\n    Returns:\n        The list of reachable `ModelElement` instances in visit\n        order, without `self`.\n    \"\"\"\n    seen: set[int] = {id(self)}\n    result: list[ModelElement] = []\n    if dataclasses.is_dataclass(self):\n        for field in dataclasses.fields(type(self)):\n            _walk_model_graph(getattr(self, field.name), seen, result)\n    return result\n</code></pre>"},{"location":"api_reference/sbml/io/sbml/","title":"SBML","text":""},{"location":"api_reference/sbml/io/sbml/#momapy.sbml.io.sbml","title":"momapy.sbml.io.sbml","text":"<p>Classes for reading SBML files.</p> <p>Modules:</p> Name Description <code>reader</code> <p>SBML reader.</p> <p>Classes:</p> Name Description <code>SBMLReader</code> <p>Reader for SBML files (model only; no layout).</p>"},{"location":"api_reference/sbml/io/sbml/#momapy.sbml.io.sbml.SBMLReader","title":"SBMLReader","text":"<p>               Bases: <code>Reader</code></p> <p>Reader for SBML files (model only; no layout).</p> <p>Methods:</p> Name Description <code>check_file</code> <p>Return <code>True</code> if the file looks like SBML.</p> <code>read</code> <p>Read an SBML file and return a reader result object.</p>"},{"location":"api_reference/sbml/io/sbml/#momapy.sbml.io.sbml.SBMLReader.check_file","title":"check_file  <code>classmethod</code>","text":"<pre><code>check_file(file_path: str | PathLike) -&gt; bool\n</code></pre> <p>Return <code>True</code> if the file looks like SBML.</p> <p>A CellDesigner file is also a valid SBML file and matches here; the <code>read()</code> dispatcher resolves the ambiguity by trying readers in registration order (CellDesigner before SBML).</p> <p>Parameters:</p> Name Type Description Default <code>file_path</code> <code>str | PathLike</code> <p>Path of the file to check.</p> required <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> if the file contains an <code>&lt;sbml</code> element, <code>False</code></p> <code>bool</code> <p>otherwise.</p> Source code in <code>src/momapy/sbml/io/sbml/reader.py</code> <pre><code>@classmethod\ndef check_file(cls, file_path: str | os.PathLike) -&gt; bool:\n    \"\"\"Return `True` if the file looks like SBML.\n\n    A CellDesigner file is also a valid SBML file and matches here; the\n    `read()` dispatcher resolves the ambiguity by trying readers in\n    registration order (CellDesigner before SBML).\n\n    Args:\n        file_path: Path of the file to check.\n\n    Returns:\n        `True` if the file contains an `&lt;sbml ` element, `False`\n        otherwise.\n    \"\"\"\n    try:\n        with open(file_path, \"rb\") as f:\n            for line in f:\n                if b\"&lt;sbml \" in line:\n                    return True\n        return False\n    except Exception:\n        return False\n</code></pre>"},{"location":"api_reference/sbml/io/sbml/#momapy.sbml.io.sbml.SBMLReader.read","title":"read  <code>classmethod</code>","text":"<pre><code>read(file_path: str | PathLike, return_type: Literal['map', 'model', 'layout'] = 'map', with_model: bool = True, with_layout: bool = True, with_annotations: bool = True, with_notes: bool = True, **options: Any) -&gt; ReaderResult\n</code></pre> <p>Read an SBML file and return a reader result object.</p> <p>Parameters:</p> Name Type Description Default <code>file_path</code> <code>str | PathLike</code> <p>Path of the SBML file to read.</p> required <code>return_type</code> <code>Literal['map', 'model', 'layout']</code> <p>Shape of <code>result.obj</code>: <code>\"map\"</code> (default) returns an <code>SBMLMap</code>, <code>\"model\"</code> returns the bare <code>SBMLModel</code>. SBML has no layout, so <code>\"layout\"</code> raises <code>NotImplementedError</code>.</p> <code>'map'</code> <code>with_model</code> <code>bool</code> <p>Whether to build the model. When <code>False</code> and <code>return_type=\"map\"</code>, the map's <code>model</code> is <code>None</code>. Defaults to <code>True</code>.</p> <code>True</code> <code>with_layout</code> <code>bool</code> <p>Accepted for signature parity with the other readers; SBML has no layout, so it has no effect. Defaults to <code>True</code>.</p> <code>True</code> <code>with_annotations</code> <code>bool</code> <p>Whether to read annotations. Defaults to <code>True</code>.</p> <code>True</code> <code>with_notes</code> <code>bool</code> <p>Whether to read notes. Defaults to <code>True</code>.</p> <code>True</code> <code>options</code> <code>Any</code> <p>Additional reader-specific options (ignored).</p> <code>{}</code> <p>Returns:</p> Type Description <code>ReaderResult</code> <p>A <code>ReaderResult</code> whose <code>obj</code> is an <code>SBMLMap</code> or <code>SBMLModel</code>.</p> <p>Raises:</p> Type Description <code>NotImplementedError</code> <p>If <code>return_type=\"layout\"</code> (SBML has no layout).</p> Source code in <code>src/momapy/sbml/io/sbml/reader.py</code> <pre><code>@classmethod\ndef read(\n    cls,\n    file_path: str | os.PathLike,\n    return_type: typing.Literal[\"map\", \"model\", \"layout\"] = \"map\",\n    with_model: bool = True,\n    with_layout: bool = True,\n    with_annotations: bool = True,\n    with_notes: bool = True,\n    **options: typing.Any,\n) -&gt; ReaderResult:\n    \"\"\"Read an SBML file and return a reader result object.\n\n    Args:\n        file_path: Path of the SBML file to read.\n        return_type: Shape of `result.obj`: `\"map\"` (default) returns an\n            `SBMLMap`, `\"model\"` returns the bare `SBMLModel`. SBML has no\n            layout, so `\"layout\"` raises `NotImplementedError`.\n        with_model: Whether to build the model. When `False` and\n            `return_type=\"map\"`, the map's `model` is `None`. Defaults to\n            `True`.\n        with_layout: Accepted for signature parity with the other readers;\n            SBML has no layout, so it has no effect. Defaults to `True`.\n        with_annotations: Whether to read annotations. Defaults to `True`.\n        with_notes: Whether to read notes. Defaults to `True`.\n        options: Additional reader-specific options (ignored).\n\n    Returns:\n        A `ReaderResult` whose `obj` is an `SBMLMap` or `SBMLModel`.\n\n    Raises:\n        NotImplementedError: If `return_type=\"layout\"` (SBML has no layout).\n    \"\"\"\n    check_file_exists(file_path)\n    sbml_document = lxml.objectify.parse(file_path)\n    sbml = sbml_document.getroot()\n    (\n        obj,\n        annotations,\n        notes,\n        id_to_element,\n        source_id_to_model_element,\n    ) = cls._make_main_obj(\n        sbml_model=sbml.model,\n        return_type=return_type,\n        with_model=with_model,\n        with_layout=with_layout,\n        with_annotations=with_annotations,\n        with_notes=with_notes,\n    )\n    result = ReaderResult(\n        obj=obj,\n        element_to_annotations=annotations,\n        element_to_notes=notes,\n        id_to_element=id_to_element,\n        source_id_to_model_element=source_id_to_model_element,\n        source_id_to_layout_element=None,\n        source_id_to_annotations=None,\n        source_id_to_notes=None,\n        file_path=file_path,\n    )\n    return result\n</code></pre>"},{"location":"user_manual/main/","title":"User manual","text":"<p>The momapy library is a new python library for working with molecular maps such as SBGN maps. Its key feature is its definition of a map, that is now formed of two entities: a model, that describes what concepts are represented, and a layout, that describes how these concepts are represented. This definition is borrowed from SBML and its extensions layout/render, that allowed users to add a layout to an SBML model. MomaPy aims at extending this definition to all types of molecular maps, and in particular to SBGN maps.</p> <p>MomaPy offers the following features:</p> <ul> <li>support for SBGN PD and AF maps (read/write SBGN-ML with annotations, rendering information, and notes)</li> <li>decomposition of a map object into:<ul> <li>a model object;</li> <li>a layout object;</li> <li>a mapping between the model and layout objects' subelements</li> </ul> </li> <li>map, model, layout and mapping objects comparison; fast object in set checking</li> <li>rendering of maps to images (SVG, PDF, JPEG, PNG, WebP) and other surfaces (e.g. GLFW window)</li> <li>support for styling and css like stylesheets (including effects such as shadows)</li> <li>automatic geometry and anchors (for arcs, shape borders)</li> <li>local positioning (e.g. right of shape, fit set of shapes)</li> <li>easy extension with new model and layout subelements</li> </ul> In\u00a0[1]: Copied! <pre>import os\n\nfrom momapy.core import DoubleHeadedArc, LayoutElement, ModelElement, Node, SingleHeadedArc\nfrom momapy.io import read, write\nfrom momapy.rendering import render_map, render_maps\nimport momapy.builder\nimport momapy.coloring\nimport momapy.styling\nimport momapy.utils\n\nimport momapy.sbgn.styling\nimport momapy.sbgn.utils\n</pre> import os  from momapy.core import DoubleHeadedArc, LayoutElement, ModelElement, Node, SingleHeadedArc from momapy.io import read, write from momapy.rendering import render_map, render_maps import momapy.builder import momapy.coloring import momapy.styling import momapy.utils  import momapy.sbgn.styling import momapy.sbgn.utils In\u00a0[2]: Copied! <pre>from utils import display, show_room, macromolecule_toy, production_toy\n</pre> from utils import display, show_room, macromolecule_toy, production_toy In\u00a0[3]: Copied! <pre>os.makedirs(\"build/\", exist_ok=True)\n</pre> os.makedirs(\"build/\", exist_ok=True) In\u00a0[4]: Copied! <pre>r = read(\"data/phospho1.sbgn\")\nm = r.obj\ndisplay(m)\n</pre> r = read(\"data/phospho1.sbgn\") m = r.obj display(m) <p>A <code>Map</code> object always contains a <code>Model</code>, a <code>Layout</code> and a <code>LayoutModelMapping</code> (that maps model elements to layout elements). It may also have additional attributes depending on its nature. For example, an SBGN PD map also has an <code>id</code>.</p> In\u00a0[5]: Copied! <pre>momapy.utils.pretty_print(m)\n</pre> momapy.utils.pretty_print(m) <pre>&lt;class 'momapy.sbgn.pd.map.SBGNPDMap'&gt;: SBGNPDMap(id_='d057d84f-aae1-4...\n\u2502 * id_: &lt;class 'str'&gt; = d057d84f-aae1-4706-9772-d72eae...\n\u2502 * model: &lt;class 'momapy.sbgn.pd.model.SBGNPDModel'&gt; = SBGNPDModel(id_='3571e538-f611...\n\u2502 * layout: &lt;class 'momapy.sbgn.pd.layout.SBGNPDLayout'&gt; = SBGNPDLayout(id_='f3627ca6-026...\n\u2502 * layout_model_mapping: momapy.core.mapping.LayoutModelMapping | None = LayoutModelMapping({StateVaria...\n</pre> <p>A <code>Model</code> may have an arbitrary number of attributes, depending on its nature. For example, an <code>SBGNPDModel</code> has the following attributes: <code>entity_pools</code>, <code>processes</code>, <code>compartments</code>, <code>modulations</code>, <code>logical_operators</code>, <code>equivalence_operators</code>, <code>submaps</code> and <code>tags</code>, but also an <code>id</code>.</p> In\u00a0[6]: Copied! <pre>momapy.utils.pretty_print(m.model)\n</pre> momapy.utils.pretty_print(m.model) <pre>&lt;class 'momapy.sbgn.pd.model.SBGNPDModel'&gt;: SBGNPDModel(id_='3571e538-f611...\n\u2502 * id_: &lt;class 'str'&gt; = 3571e538-f611-46a7-918e-42838d...\n\u2502 * entity_pools: frozenset[momapy.sbgn.pd.model.EntityPool] = frozenset({Macromolecule(id_='...\n\u2502 * processes: frozenset[momapy.sbgn.pd.model.Process] = frozenset({GenericProcess(id_=...\n\u2502 * compartments: frozenset[momapy.sbgn.pd.model.Compartment] = frozenset()\n\u2502 * modulations: frozenset[momapy.sbgn.pd.model.Modulation] = frozenset({Catalysis(id_='arc3...\n\u2502 * logical_operators: frozenset[momapy.sbgn.pd.model.LogicalOperator] = frozenset()\n\u2502 * equivalence_operators: frozenset[momapy.sbgn.pd.model.EquivalenceOperator] = frozenset()\n\u2502 * submaps: frozenset[momapy.sbgn.pd.model.Submap] = frozenset()\n\u2502 * tags: frozenset[momapy.sbgn.pd.model.Tag] = frozenset()\n</pre> <p>These attributes may be <code>ModelElement</code>s or collections of <code>ModelElements</code>. For example, the <code>entity_pools</code> attribute of an <code>SBGNPDModel</code> may contain zero or more <code>EntityPool</code>s. We pick the first element from the collection:</p> In\u00a0[7]: Copied! <pre>for e in m.model.entity_pools:\n    break\nmomapy.utils.pretty_print(e)\n</pre> for e in m.model.entity_pools:     break momapy.utils.pretty_print(e) <pre>&lt;class 'momapy.sbgn.pd.model.Macromolecule'&gt;: Macromolecule(id_='glyph3_mode...\n\u2502 * id_: &lt;class 'str'&gt; = glyph3_model\n\u2502 * compartment: momapy.sbgn.pd.model.Compartment | None = None\n\u2502 * label: str | None = B\n\u2502 * state_variables: frozenset[momapy.sbgn.pd.model.StateVariable] = frozenset()\n\u2502 * units_of_information: frozenset[momapy.sbgn.pd.model.UnitOfInformation] = frozenset()\n</pre> <p>Here, the element is a <code>Macromolecule</code>. The data model for SBGN PD is built on a hierarchy of classes following the corresponding ontology. Hence this element is also an <code>EntityPool</code>, and more generally a <code>ModelElement</code>:</p> In\u00a0[8]: Copied! <pre>assert isinstance(e, momapy.sbgn.pd.EntityPool)\nassert isinstance(e, ModelElement)\n</pre> assert isinstance(e, momapy.sbgn.pd.EntityPool) assert isinstance(e, ModelElement) <p>A <code>Layout</code> is some sort of canvas that may contain other <code>LayoutElement</code>s that correspond to shapes that represent <code>ModelElement</code>s and that may be rendered. In SBGN, <code>LayoutElement</code>s are either <code>Node</code>s, <code>Arc</code>s or <code>TextLayout</code>s. The different <code>LayoutElement</code>s of a <code>Layout</code> are contained in its <code>layout_elements</code> attribute:</p> In\u00a0[9]: Copied! <pre>momapy.utils.pretty_print(m.layout)\n</pre> momapy.utils.pretty_print(m.layout) <pre>&lt;class 'momapy.sbgn.pd.layout.SBGNPDLayout'&gt;: SBGNPDLayout(id_='f3627ca6-026...\n\u2502 * id_: &lt;class 'str'&gt; = f3627ca6-026e-41aa-8a45-c25b20...\n\u2502 * layout_elements: tuple[momapy.core.elements.LayoutElement] = (MacromoleculeLayout(id_='glyp...\n\u2502 * group_fill: momapy.drawing.NoneValueType | momapy.coloring.Color | None = None\n\u2502 * group_fill_rule: momapy.drawing.FillRule | None = None\n\u2502 * group_filter: momapy.drawing.NoneValueType | momapy.drawing.Filter | None = None\n\u2502 * group_font_family: str | None = None\n\u2502 * group_font_size: float | None = None\n\u2502 * group_font_style: momapy.drawing.FontStyle | None = None\n\u2502 * group_font_weight: momapy.drawing.FontWeight | float | None = None\n\u2502 * group_stroke: momapy.drawing.NoneValueType | momapy.coloring.Color | None = None\n\u2502 * group_stroke_dasharray: momapy.drawing.NoneValueType | tuple[float, ...] | None = None\n\u2502 * group_stroke_dashoffset: momapy.drawing.NoneValueType | float | None = None\n\u2502 * group_stroke_width: momapy.drawing.NoneValueType | float | None = None\n\u2502 * group_text_anchor: momapy.drawing.TextAnchor | None = None\n\u2502 * group_transform: momapy.drawing.NoneValueType | tuple[momapy.geometry.Transformation] | None = None\n\u2502 * fill: momapy.drawing.NoneValueType | momapy.coloring.Color | None = Color(red=255, green=255, blue...\n\u2502 * filter: momapy.drawing.NoneValueType | momapy.drawing.Filter | None = None\n\u2502 * height: &lt;class 'float'&gt; = 250.0\n\u2502 * label: momapy.core.layout.TextLayout | None = None\n\u2502 * position: &lt;class 'momapy.geometry.Point'&gt; = Point(x=240.0, y=205.0)\n\u2502 * stroke: momapy.drawing.NoneValueType | momapy.coloring.Color | None = None\n\u2502 * stroke_dasharray: momapy.drawing.NoneValueType | tuple[float, ...] | None = None\n\u2502 * stroke_dashoffset: momapy.drawing.NoneValueType | float | None = None\n\u2502 * stroke_width: momapy.drawing.NoneValueType | float | None = None\n\u2502 * transform: momapy.drawing.NoneValueType | tuple[momapy.geometry.Transformation] | None = None\n\u2502 * width: &lt;class 'float'&gt; = 450.0\n</pre> <p>A <code>LayoutElement</code> always has a <code>drawing_elements</code> method that returns <code>DrawingElement</code>s that may be rendered using a <code>Renderer</code>. The <code>DrawingElement</code>s are built on the fly based on the other attributes of the <code>LayoutElement</code>. <code>DrawingElement</code>s are like SVG elements: they may represent paths, rectangle, ellipses, text..., and generally have the same attributes as their SVG counterpart (or a subset of them). A <code>Layout</code> itself is a <code>Node</code>, whose returned unique <code>DrawingElement</code> ultimately represents a rectangle built from its <code>position</code>, <code>width</code> and <code>height</code> attributes. The style of this rectangle depends on the styling attribute of the <code>Layout</code> (e.g. the <code>stroke</code>, <code>stroke_width</code>, and <code>fill</code> attributes):</p> In\u00a0[10]: Copied! <pre>momapy.utils.pretty_print(m.layout.drawing_elements())\n</pre> momapy.utils.pretty_print(m.layout.drawing_elements()) <pre>&lt;class 'list'&gt;: [Group(class_='SBGNPDLayout', ...\n\u2502 - 0: &lt;class 'momapy.drawing.Group'&gt; = Group(class_='SBGNPDLayout', f...\n</pre> <p>A <code>Layout</code> may contain other <code>LayoutElement</code>s, which themselves may contain other <code>LayoutElement</code>s, recursively forming a hierarchy of <code>LayoutElement</code>s, and thus of <code>DrawingElement</code>s. While a <code>Layout</code> represents a <code>Model</code>, contained <code>LayoutElement</code>s represent <code>ModelElement</code>s contained by the <code>Model</code>.</p> <p>We pick the first <code>LayoutElement</code> from the <code>SBGNPDLayout</code>:</p> In\u00a0[11]: Copied! <pre>for l in m.layout.layout_elements:\n    break\nmomapy.utils.pretty_print(l)\n</pre> for l in m.layout.layout_elements:     break momapy.utils.pretty_print(l) <pre>&lt;class 'momapy.sbgn.pd.layout.MacromoleculeLayout'&gt;: MacromoleculeLayout(id_='glyph...\n\u2502 * id_: &lt;class 'str'&gt; = glyph2\n\u2502 * layout_elements: tuple[momapy.core.elements.LayoutElement] = (StateVariableLayout(id_='glyp...\n\u2502 * group_fill: momapy.drawing.NoneValueType | momapy.coloring.Color | None = None\n\u2502 * group_fill_rule: momapy.drawing.FillRule | None = None\n\u2502 * group_filter: momapy.drawing.NoneValueType | momapy.drawing.Filter | None = None\n\u2502 * group_font_family: str | None = None\n\u2502 * group_font_size: float | None = None\n\u2502 * group_font_style: momapy.drawing.FontStyle | None = None\n\u2502 * group_font_weight: momapy.drawing.FontWeight | float | None = None\n\u2502 * group_stroke: momapy.drawing.NoneValueType | momapy.coloring.Color | None = None\n\u2502 * group_stroke_dasharray: momapy.drawing.NoneValueType | tuple[float, ...] | None = None\n\u2502 * group_stroke_dashoffset: momapy.drawing.NoneValueType | float | None = None\n\u2502 * group_stroke_width: momapy.drawing.NoneValueType | float | None = None\n\u2502 * group_text_anchor: momapy.drawing.TextAnchor | None = None\n\u2502 * group_transform: momapy.drawing.NoneValueType | tuple[momapy.geometry.Transformation] | None = None\n\u2502 * fill: momapy.drawing.NoneValueType | momapy.coloring.Color | None = Color(red=255, green=255, blue...\n\u2502 * filter: momapy.drawing.NoneValueType | momapy.drawing.Filter | None = None\n\u2502 * height: &lt;class 'float'&gt; = 60.0\n\u2502 * label: momapy.core.layout.TextLayout | None = TextLayout(id_='0d634364-e478-...\n\u2502 * position: &lt;class 'momapy.geometry.Point'&gt; = Point(x=85.0, y=270.0)\n\u2502 * stroke: momapy.drawing.NoneValueType | momapy.coloring.Color | None = Color(red=0, green=0, blue=0, ...\n\u2502 * stroke_dasharray: momapy.drawing.NoneValueType | tuple[float, ...] | None = None\n\u2502 * stroke_dashoffset: momapy.drawing.NoneValueType | float | None = None\n\u2502 * stroke_width: float | None = 1.25\n\u2502 * transform: momapy.drawing.NoneValueType | tuple[momapy.geometry.Transformation] | None = None\n\u2502 * width: &lt;class 'float'&gt; = 108.0\n\u2502 * rounded_corners: &lt;class 'float'&gt; = 5.0\n</pre> <p>This element is a <code>MacromoleculeLayout</code>. Based on its <code>position</code>, <code>width</code> and <code>height</code> attributes, as well as on its styling attributes, it will produce a <code>DrawingElement</code> representing a rectangle with rounded corners, containing some text corresponding to its <code>label</code>. Since this element also contains other <code>LayoutElement</code>s in its <code>layout_elements</code> attribute, it will also produce the <code>DrawingElement</code>s of these contained <code>LayoutElement</code>s (here, a <code>StateVariableLayout</code>):</p> In\u00a0[12]: Copied! <pre>display(l)\n</pre> display(l) <p>A <code>LayoutModelMapping</code> is a mapping from <code>LayoutElement</code>s to <code>ModelElement</code>s and vice-versa. It is used to map the <code>LayoutElement</code>s of a <code>Map</code> to the <code>ModelElement</code>s they represent.</p> <p>We pick the <code>ModelElement</code> mapped to the <code>MacromoleculeLayout</code> we had picked:</p> In\u00a0[13]: Copied! <pre>e = m.layout_model_mapping.get_mapping(l)\nmomapy.utils.pretty_print(e)\n</pre> e = m.layout_model_mapping.get_mapping(l) momapy.utils.pretty_print(e) <pre>&lt;class 'momapy.sbgn.pd.model.Macromolecule'&gt;: Macromolecule(id_='glyph2_mode...\n\u2502 * id_: &lt;class 'str'&gt; = glyph2_model\n\u2502 * compartment: momapy.sbgn.pd.model.Compartment | None = None\n\u2502 * label: str | None = A\n\u2502 * state_variables: frozenset[momapy.sbgn.pd.model.StateVariable] = frozenset({StateVariable(id_='...\n\u2502 * units_of_information: frozenset[momapy.sbgn.pd.model.UnitOfInformation] = frozenset()\n</pre> <p><code>Map</code>s, <code>Model</code>s, <code>Layout</code>s, <code>LayoutModelMapping</code>s, <code>ModelElement</code>s and <code>LayoutElement</code>s are encoded so they can be easily compared. Their identity relies on the value of a subset of their attributes (generally all attributes but their <code>id_</code>). This way, two <code>Map</code>s can be easily compared. Is is also possible to check whether a <code>Map</code> is a sub-map of another <code>Map</code> (not to be confused with SBGN PD's submap glyph).</p> <p>Two <code>Map</code>s are equal if and only if:</p> <ul> <li>their <code>Model</code>s are equal;</li> <li>their <code>Layout</code>s are equal;</li> <li>and their <code>LayoutModelMapping</code>s are equal.</li> </ul> In\u00a0[14]: Copied! <pre>r1 = read(\"data/phospho1.sbgn\")\nm1 = r1.obj\ndisplay(m1)\n</pre> r1 = read(\"data/phospho1.sbgn\") m1 = r1.obj display(m1) In\u00a0[15]: Copied! <pre>r2 = read(\"data/phospho2.sbgn\")\nm2 = r2.obj\ndisplay(m2)\n</pre> r2 = read(\"data/phospho2.sbgn\") m2 = r2.obj display(m2) <p>The two maps represent the exact same concepts, and thus have the same model. However, they do not have the same layout. Hence, the two maps are different. This can be checked easily by comparing the <code>Map</code>, <code>Model</code>, <code>Layout</code> and <code>LayoutModelMapping</code> objects representing the two maps:</p> In\u00a0[16]: Copied! <pre>assert m1 != m2\nassert m1.model == m2.model\nassert m1.layout != m2.layout\nassert m1.layout_model_mapping != m2.layout_model_mapping\n</pre> assert m1 != m2 assert m1.model == m2.model assert m1.layout != m2.layout assert m1.layout_model_mapping != m2.layout_model_mapping <p>A <code>Map</code> <code>M</code> is a sub-map of another <code>Map</code> <code>M'</code> if and only if:</p> <ul> <li>the <code>Model</code> of <code>M</code> is a sub-model of the <code>Model</code> of <code>M'</code>;</li> <li>the <code>Layout</code> of <code>M</code> is a sub-layout of the <code>Layout</code> of <code>M'</code>;</li> <li>and the <code>LayoutModelMapping</code> of <code>M</code> is a sub-mapping of the <code>LayoutModelMapping</code> of <code>M'</code>.</li> </ul> In\u00a0[17]: Copied! <pre>r1 = read(\"data/phospho1.sbgn\")\nm1 = r1.obj\ndisplay(m1)\n</pre> r1 = read(\"data/phospho1.sbgn\") m1 = r1.obj display(m1) In\u00a0[18]: Copied! <pre>r3 = read(\"data/phospho3.sbgn\")\nm3 = r3.obj\ndisplay(m3)\n</pre> r3 = read(\"data/phospho3.sbgn\") m3 = r3.obj display(m3) <p>The second map is an excerpt of the first map:</p> In\u00a0[19]: Copied! <pre>assert m3.is_submap(m1)\nassert m3.model.is_submodel(m1.model)\nassert m3.layout.is_sublayout(m1.layout)\nassert m3.layout_model_mapping.is_submapping(m1.layout_model_mapping)\n</pre> assert m3.is_submap(m1) assert m3.model.is_submodel(m1.model) assert m3.layout.is_sublayout(m1.layout) assert m3.layout_model_mapping.is_submapping(m1.layout_model_mapping) In\u00a0[20]: Copied! <pre>r4 = read(\"data/phospho4.sbgn\")\nm4 = r4.obj\ndisplay(m4)\n</pre> r4 = read(\"data/phospho4.sbgn\") m4 = r4.obj display(m4) <p>Because of the compartment, the model of the first map is not an excerpt of the model of the second map. However, the layout of the first map is an excerpt of the layout of the second map:</p> In\u00a0[21]: Copied! <pre>assert not m3.is_submap(m4)\nassert not m3.model.is_submodel(m4.model)\nassert m3.layout.is_sublayout(m4.layout)\nassert not m3.layout_model_mapping.is_submapping(m4.layout_model_mapping)\n</pre> assert not m3.is_submap(m4) assert not m3.model.is_submodel(m4.model) assert m3.layout.is_sublayout(m4.layout) assert not m3.layout_model_mapping.is_submapping(m4.layout_model_mapping) <p><code>Map</code>, <code>Model</code>, <code>Layout</code>, <code>ModelElement</code> and <code>LayoutElement</code> objects cannot be modified; they are frozen:</p> In\u00a0[22]: Copied! <pre>r = read(\"data/phospho1.sbgn\")\nm = r.obj\ndisplay(m)\n</pre> r = read(\"data/phospho1.sbgn\") m = r.obj display(m) In\u00a0[23]: Copied! <pre>for l in m.layout.layout_elements:\n    break\ndisplay(l)\n</pre> for l in m.layout.layout_elements:     break display(l) In\u00a0[24]: Copied! <pre>try:\n    l.stroke_width = 3.0\nexcept Exception as e:\n    print(e)\n</pre> try:     l.stroke_width = 3.0 except Exception as e:     print(e) <pre>cannot assign to field 'stroke_width'\n</pre> <p>This way they can be hashed, which is necessary to check whether a <code>Map</code> object belongs to a <code>set</code> efficiently, for example. However, we want to be able to modify them programmatically (e.g., change the stroke width of the border of a shape). To this end, each class has a corresponding builder class, that allows the production of objects that are not frozen. Such objects may be built directly from the frozen objects:</p> In\u00a0[25]: Copied! <pre>lb = momapy.builder.builder_from_object(l)\nmomapy.utils.pretty_print(lb)\n</pre> lb = momapy.builder.builder_from_object(l) momapy.utils.pretty_print(lb) <pre>&lt;class 'momapy.builder.MacromoleculeLayoutBuilder'&gt;: MacromoleculeLayoutBuilder(id_...\n\u2502 * id_: &lt;class 'str'&gt; = glyph2\n\u2502 * layout_elements: typing.Union[tuple[momapy.core.elements.LayoutElement], list[momapy.builder.LayoutElementBuilder]] = [StateVariableLayoutBuilder(id...\n\u2502 * group_fill: typing.Union[momapy.drawing.NoneValueType, momapy.coloring.Color, NoneType, momapy.builder.ColorBuilder] = None\n\u2502 * group_fill_rule: typing.Optional[momapy.drawing.FillRule] = None\n\u2502 * group_filter: typing.Union[momapy.drawing.NoneValueType, momapy.drawing.Filter, NoneType, momapy.builder.FilterBuilder] = None\n\u2502 * group_font_family: typing.Optional[str] = None\n\u2502 * group_font_size: typing.Optional[float] = None\n\u2502 * group_font_style: typing.Optional[momapy.drawing.FontStyle] = None\n\u2502 * group_font_weight: typing.Union[momapy.drawing.FontWeight, float, NoneType] = None\n\u2502 * group_stroke: typing.Union[momapy.drawing.NoneValueType, momapy.coloring.Color, NoneType, momapy.builder.ColorBuilder] = None\n\u2502 * group_stroke_dasharray: typing.Union[momapy.drawing.NoneValueType, tuple[float, ...], NoneType, list[float, ...]] = None\n\u2502 * group_stroke_dashoffset: typing.Union[momapy.drawing.NoneValueType, float, NoneType] = None\n\u2502 * group_stroke_width: typing.Union[momapy.drawing.NoneValueType, float, NoneType] = None\n\u2502 * group_text_anchor: typing.Optional[momapy.drawing.TextAnchor] = None\n\u2502 * group_transform: typing.Union[momapy.drawing.NoneValueType, tuple[momapy.geometry.Transformation], NoneType, list[momapy.builder.TransformationBuilder]] = None\n\u2502 * fill: typing.Union[momapy.drawing.NoneValueType, momapy.coloring.Color, NoneType, momapy.builder.ColorBuilder] = ColorBuilder(red=255, green=25...\n\u2502 * filter: typing.Union[momapy.drawing.NoneValueType, momapy.drawing.Filter, NoneType, momapy.builder.FilterBuilder] = None\n\u2502 * height: &lt;class 'float'&gt; = 60.0\n\u2502 * label: typing.Union[momapy.core.layout.TextLayout, NoneType, momapy.builder.TextLayoutBuilder] = TextLayoutBuilder(id_='3de4797...\n\u2502 * position: typing.Union[momapy.geometry.Point, momapy.builder.PointBuilder, NoneType] = PointBuilder(x=85.0, y=270.0)\n\u2502 * stroke: typing.Union[momapy.drawing.NoneValueType, momapy.coloring.Color, NoneType, momapy.builder.ColorBuilder] = ColorBuilder(red=0, green=0, b...\n\u2502 * stroke_dasharray: typing.Union[momapy.drawing.NoneValueType, tuple[float, ...], NoneType, list[float, ...]] = None\n\u2502 * stroke_dashoffset: typing.Union[momapy.drawing.NoneValueType, float, NoneType] = None\n\u2502 * stroke_width: typing.Optional[float] = 1.25\n\u2502 * transform: typing.Union[momapy.drawing.NoneValueType, tuple[momapy.geometry.Transformation], NoneType, list[momapy.builder.TransformationBuilder]] = None\n\u2502 * width: &lt;class 'float'&gt; = 108.0\n\u2502 * rounded_corners: &lt;class 'float'&gt; = 5.0\n</pre> In\u00a0[26]: Copied! <pre>lb.stroke_width = 3.0\nl = momapy.builder.object_from_builder(lb)\ndisplay(l)\n</pre> lb.stroke_width = 3.0 l = momapy.builder.object_from_builder(lb) display(l) <p>The frozen object may then be built back from the builder:</p> In\u00a0[27]: Copied! <pre>l = momapy.builder.object_from_builder(lb)\n</pre> l = momapy.builder.object_from_builder(lb) In\u00a0[28]: Copied! <pre>assert l.stroke_width == 3.0\n</pre> assert l.stroke_width == 3.0 <p><code>SBGNMap</code>s may be read from and written to SBGN-ML files using <code>read</code> and <code>write</code> functions:</p> In\u00a0[29]: Copied! <pre>r = read(\"data/phospho1.sbgn\")\nm = r.obj\nwrite(m, \"build/phospho1_output.sbgn\", writer=\"sbgnml\")\n</pre> r = read(\"data/phospho1.sbgn\") m = r.obj write(m, \"build/phospho1_output.sbgn\", writer=\"sbgnml\") In\u00a0[30]: Copied! <pre>r = read(\"data/phospho1.sbgn\")\nm = r.obj\ndisplay(m)\n</pre> r = read(\"data/phospho1.sbgn\") m = r.obj display(m) <p><code>Map</code>s can be rendered in different formats using a simple render function:</p> In\u00a0[31]: Copied! <pre>render_map(m, \"build/phospho1.pdf\")\nrender_map(m, \"build/phospho1.png\")\nrender_map(m, \"build/phospho1.svg\")\nrender_map(m, \"build/phospho1.webp\")\nrender_map(m, \"build/phospho1.jpeg\")\n</pre> render_map(m, \"build/phospho1.pdf\") render_map(m, \"build/phospho1.png\") render_map(m, \"build/phospho1.svg\") render_map(m, \"build/phospho1.webp\") render_map(m, \"build/phospho1.jpeg\") <p><code>Layout</code>s can be moved to the top left using the <code>top_left</code> option:</p> In\u00a0[32]: Copied! <pre>render_map(m, \"build/phospho1.pdf\", to_top_left=True)\n</pre> render_map(m, \"build/phospho1.pdf\", to_top_left=True) In\u00a0[33]: Copied! <pre>r1 = read(\"data/phospho1.sbgn\")\nm1 = r1.obj\nr2 = read(\"data/phospho2.sbgn\")\nm2 = r2.obj\nr3 = read(\"data/phospho3.sbgn\")\nm3 = r3.obj\nr4 = read(\"data/phospho4.sbgn\")\nm4 = r4.obj\n</pre> r1 = read(\"data/phospho1.sbgn\") m1 = r1.obj r2 = read(\"data/phospho2.sbgn\") m2 = r2.obj r3 = read(\"data/phospho3.sbgn\") m3 = r3.obj r4 = read(\"data/phospho4.sbgn\") m4 = r4.obj <p>Multiple <code>Map</code>s can be rendered in one document using a simple function:</p> In\u00a0[34]: Copied! <pre>render_maps(\n    [m1, m2, m3, m4], \"build/phospho_multi.pdf\", multi_pages=True\n)\n</pre> render_maps(     [m1, m2, m3, m4], \"build/phospho_multi.pdf\", multi_pages=True ) <p>Basic styling can be easily applied to <code>LayoutElement</code>s:</p> In\u00a0[35]: Copied! <pre>r = read(\"data/phospho1.sbgn\")\nm = r.obj\ndisplay(m)\n</pre> r = read(\"data/phospho1.sbgn\") m = r.obj display(m) In\u00a0[36]: Copied! <pre>for l in m.layout.layout_elements:\n    break\ndisplay(l)\n</pre> for l in m.layout.layout_elements:     break display(l) In\u00a0[37]: Copied! <pre>lb = momapy.builder.builder_from_object(l)\nlb.fill = momapy.coloring.lightblue\nlb.stroke = momapy.coloring.brown\nlb.stroke_width = 3.0\nlb.stroke_dasharray = (5, 5)\nmomapy.utils.pretty_print(lb.drawing_elements(), max_depth=3)\n</pre> lb = momapy.builder.builder_from_object(l) lb.fill = momapy.coloring.lightblue lb.stroke = momapy.coloring.brown lb.stroke_width = 3.0 lb.stroke_dasharray = (5, 5) momapy.utils.pretty_print(lb.drawing_elements(), max_depth=3) <pre>&lt;class 'list'&gt;: [Group(class_='MacromoleculeLa...\n\u2502 - 0: &lt;class 'momapy.drawing.Group'&gt; = Group(class_='MacromoleculeLay...\n\u2502 \u2502 &lt;class 'momapy.drawing.Group'&gt;: Group(class_='MacromoleculeLay...\n\u2502 \u2502 \u2502 * class_: str | None = MacromoleculeLayoutBuilder\n\u2502 \u2502 \u2502 * fill: momapy.drawing.NoneValueType | momapy.coloring.Color | None = None\n\u2502 \u2502 \u2502 * fill_rule: momapy.drawing.FillRule | None = None\n\u2502 \u2502 \u2502 * filter: momapy.drawing.NoneValueType | momapy.drawing.Filter | None = None\n\u2502 \u2502 \u2502 * font_family: str | None = None\n\u2502 \u2502 \u2502 * font_size: float | None = None\n\u2502 \u2502 \u2502 * font_style: momapy.drawing.FontStyle | None = None\n\u2502 \u2502 \u2502 * font_weight: momapy.drawing.FontWeight | int | None = None\n\u2502 \u2502 \u2502 * id_: str | None = glyph2\n\u2502 \u2502 \u2502 * stroke: momapy.drawing.NoneValueType | momapy.coloring.Color | None = None\n\u2502 \u2502 \u2502 * stroke_dasharray: momapy.drawing.NoneValueType | tuple[float, ...] | None = None\n\u2502 \u2502 \u2502 * stroke_dashoffset: momapy.drawing.NoneValueType | float | None = None\n\u2502 \u2502 \u2502 * stroke_width: momapy.drawing.NoneValueType | float | None = None\n\u2502 \u2502 \u2502 * text_anchor: momapy.drawing.TextAnchor | None = None\n\u2502 \u2502 \u2502 * transform: momapy.drawing.NoneValueType | tuple[momapy.geometry.Transformation] | None = None\n\u2502 \u2502 \u2502 * elements: tuple[momapy.drawing.DrawingElement] = (Group(class_='MacromoleculeLa...\n\u2502 \u2502 \u2502 \u2502 &lt;class 'tuple'&gt;: (Group(class_='MacromoleculeLa...\n\u2502 \u2502 \u2502 \u2502 \u2502 - 0: &lt;class 'momapy.drawing.Group'&gt; = Group(class_='MacromoleculeLay...\n\u2502 \u2502 \u2502 \u2502 \u2502 \u2502 &lt;class 'momapy.drawing.Group'&gt;: Group(class_='MacromoleculeLay...\n\u2502 \u2502 \u2502 \u2502 \u2502 \u2502 \u2502 * class_: str | None = MacromoleculeLayoutBuilder_own\n\u2502 \u2502 \u2502 \u2502 \u2502 \u2502 \u2502 * fill: momapy.drawing.NoneValueType | momapy.coloring.Color | None = Color(red=173, green=216, blue...\n\u2502 \u2502 \u2502 \u2502 \u2502 \u2502 \u2502 * fill_rule: momapy.drawing.FillRule | None = None\n\u2502 \u2502 \u2502 \u2502 \u2502 \u2502 \u2502 * filter: momapy.drawing.NoneValueType | momapy.drawing.Filter | None = None\n\u2502 \u2502 \u2502 \u2502 \u2502 \u2502 \u2502 * font_family: str | None = None\n\u2502 \u2502 \u2502 \u2502 \u2502 \u2502 \u2502 * font_size: float | None = None\n\u2502 \u2502 \u2502 \u2502 \u2502 \u2502 \u2502 * font_style: momapy.drawing.FontStyle | None = None\n\u2502 \u2502 \u2502 \u2502 \u2502 \u2502 \u2502 * font_weight: momapy.drawing.FontWeight | int | None = None\n\u2502 \u2502 \u2502 \u2502 \u2502 \u2502 \u2502 * id_: str | None = glyph2_own\n\u2502 \u2502 \u2502 \u2502 \u2502 \u2502 \u2502 * stroke: momapy.drawing.NoneValueType | momapy.coloring.Color | None = Color(red=165, green=42, blue=...\n\u2502 \u2502 \u2502 \u2502 \u2502 \u2502 \u2502 * stroke_dasharray: momapy.drawing.NoneValueType | tuple[float, ...] | None = (5, 5)\n\u2502 \u2502 \u2502 \u2502 \u2502 \u2502 \u2502 * stroke_dashoffset: momapy.drawing.NoneValueType | float | None = None\n\u2502 \u2502 \u2502 \u2502 \u2502 \u2502 \u2502 * stroke_width: momapy.drawing.NoneValueType | float | None = 3.0\n\u2502 \u2502 \u2502 \u2502 \u2502 \u2502 \u2502 * text_anchor: momapy.drawing.TextAnchor | None = None\n\u2502 \u2502 \u2502 \u2502 \u2502 \u2502 \u2502 * transform: momapy.drawing.NoneValueType | tuple[momapy.geometry.Transformation] | None = None\n\u2502 \u2502 \u2502 \u2502 \u2502 \u2502 \u2502 * elements: tuple[momapy.drawing.DrawingElement] = (Path(class_=None, fill=None, ...\n\u2502 \u2502 \u2502 \u2502 \u2502 - 1: &lt;class 'momapy.drawing.Group'&gt; = Group(class_='TextLayoutBuilde...\n\u2502 \u2502 \u2502 \u2502 \u2502 \u2502 &lt;class 'momapy.drawing.Group'&gt;: Group(class_='TextLayoutBuilde...\n\u2502 \u2502 \u2502 \u2502 \u2502 \u2502 \u2502 * class_: str | None = TextLayoutBuilder\n\u2502 \u2502 \u2502 \u2502 \u2502 \u2502 \u2502 * fill: momapy.drawing.NoneValueType | momapy.coloring.Color | None = ColorBuilder(red=0, green=0, b...\n\u2502 \u2502 \u2502 \u2502 \u2502 \u2502 \u2502 * fill_rule: momapy.drawing.FillRule | None = None\n\u2502 \u2502 \u2502 \u2502 \u2502 \u2502 \u2502 * filter: momapy.drawing.NoneValueType | momapy.drawing.Filter | None = None\n\u2502 \u2502 \u2502 \u2502 \u2502 \u2502 \u2502 * font_family: str | None = DejaVu Sans\n\u2502 \u2502 \u2502 \u2502 \u2502 \u2502 \u2502 * font_size: float | None = 11.0\n\u2502 \u2502 \u2502 \u2502 \u2502 \u2502 \u2502 * font_style: momapy.drawing.FontStyle | None = FontStyle.NORMAL\n\u2502 \u2502 \u2502 \u2502 \u2502 \u2502 \u2502 * font_weight: momapy.drawing.FontWeight | int | None = FontWeight.NORMAL\n\u2502 \u2502 \u2502 \u2502 \u2502 \u2502 \u2502 * id_: str | None = 31a51c95-68dc-4dfb-819f-97f1bb...\n\u2502 \u2502 \u2502 \u2502 \u2502 \u2502 \u2502 * stroke: momapy.drawing.NoneValueType | momapy.coloring.Color | None = &lt;momapy.drawing.NoneValueType ...\n\u2502 \u2502 \u2502 \u2502 \u2502 \u2502 \u2502 * stroke_dasharray: momapy.drawing.NoneValueType | tuple[float, ...] | None = None\n\u2502 \u2502 \u2502 \u2502 \u2502 \u2502 \u2502 * stroke_dashoffset: momapy.drawing.NoneValueType | float | None = None\n\u2502 \u2502 \u2502 \u2502 \u2502 \u2502 \u2502 * stroke_width: momapy.drawing.NoneValueType | float | None = None\n\u2502 \u2502 \u2502 \u2502 \u2502 \u2502 \u2502 * text_anchor: momapy.drawing.TextAnchor | None = None\n\u2502 \u2502 \u2502 \u2502 \u2502 \u2502 \u2502 * transform: momapy.drawing.NoneValueType | tuple[momapy.geometry.Transformation] | None = None\n\u2502 \u2502 \u2502 \u2502 \u2502 \u2502 \u2502 * elements: tuple[momapy.drawing.DrawingElement] = (Text(class_=None, fill=None, ...\n\u2502 \u2502 \u2502 \u2502 \u2502 - 2: &lt;class 'momapy.drawing.Group'&gt; = Group(class_='StateVariableLay...\n\u2502 \u2502 \u2502 \u2502 \u2502 \u2502 &lt;class 'momapy.drawing.Group'&gt;: Group(class_='StateVariableLay...\n\u2502 \u2502 \u2502 \u2502 \u2502 \u2502 \u2502 * class_: str | None = StateVariableLayoutBuilder\n\u2502 \u2502 \u2502 \u2502 \u2502 \u2502 \u2502 * fill: momapy.drawing.NoneValueType | momapy.coloring.Color | None = None\n\u2502 \u2502 \u2502 \u2502 \u2502 \u2502 \u2502 * fill_rule: momapy.drawing.FillRule | None = None\n\u2502 \u2502 \u2502 \u2502 \u2502 \u2502 \u2502 * filter: momapy.drawing.NoneValueType | momapy.drawing.Filter | None = None\n\u2502 \u2502 \u2502 \u2502 \u2502 \u2502 \u2502 * font_family: str | None = None\n\u2502 \u2502 \u2502 \u2502 \u2502 \u2502 \u2502 * font_size: float | None = None\n\u2502 \u2502 \u2502 \u2502 \u2502 \u2502 \u2502 * font_style: momapy.drawing.FontStyle | None = None\n\u2502 \u2502 \u2502 \u2502 \u2502 \u2502 \u2502 * font_weight: momapy.drawing.FontWeight | int | None = None\n\u2502 \u2502 \u2502 \u2502 \u2502 \u2502 \u2502 * id_: str | None = glyph2a\n\u2502 \u2502 \u2502 \u2502 \u2502 \u2502 \u2502 * stroke: momapy.drawing.NoneValueType | momapy.coloring.Color | None = None\n\u2502 \u2502 \u2502 \u2502 \u2502 \u2502 \u2502 * stroke_dasharray: momapy.drawing.NoneValueType | tuple[float, ...] | None = None\n\u2502 \u2502 \u2502 \u2502 \u2502 \u2502 \u2502 * stroke_dashoffset: momapy.drawing.NoneValueType | float | None = None\n\u2502 \u2502 \u2502 \u2502 \u2502 \u2502 \u2502 * stroke_width: momapy.drawing.NoneValueType | float | None = None\n\u2502 \u2502 \u2502 \u2502 \u2502 \u2502 \u2502 * text_anchor: momapy.drawing.TextAnchor | None = None\n\u2502 \u2502 \u2502 \u2502 \u2502 \u2502 \u2502 * transform: momapy.drawing.NoneValueType | tuple[momapy.geometry.Transformation] | None = None\n\u2502 \u2502 \u2502 \u2502 \u2502 \u2502 \u2502 * elements: tuple[momapy.drawing.DrawingElement] = (Group(class_='StateVariableLa...\n</pre> In\u00a0[38]: Copied! <pre>l = momapy.builder.object_from_builder(lb)\ndisplay(l)\n</pre> l = momapy.builder.object_from_builder(lb) display(l) <p>The <code>label</code> of a <code>Node</code> can also be styled:</p> In\u00a0[39]: Copied! <pre>lb.label.font_family = \"Times New Roman\"\nlb.label.font_size = 30.0\nlb.label.fill = momapy.coloring.red\nlb.label.stroke = momapy.coloring.black\nlb.label.stroke_width = 2.0\nl = momapy.builder.object_from_builder(lb)\ndisplay(l)\n</pre> lb.label.font_family = \"Times New Roman\" lb.label.font_size = 30.0 lb.label.fill = momapy.coloring.red lb.label.stroke = momapy.coloring.black lb.label.stroke_width = 2.0 l = momapy.builder.object_from_builder(lb) display(l) <p>Advanced effects such as transformations (translation, rotation, ...) and filter effects can be applied to <code>LayoutElement</code>s:</p> In\u00a0[40]: Copied! <pre>lb.group_transform = (\n    momapy.geometry.Scaling(2, 1),\n    momapy.geometry.Rotation(0.5, lb.position),\n)\nlb.group_filter = momapy.drawing.Filter(\n    effects=(\n        momapy.drawing.DropShadowEffect(\n            dx=3.0,\n            dy=3.0,\n            std_deviation=5.0,\n            flood_opacity=0.5,\n            flood_color=momapy.coloring.blue,\n        ),\n    )\n)\nl = momapy.builder.object_from_builder(lb)\ndisplay(l)\n</pre> lb.group_transform = (     momapy.geometry.Scaling(2, 1),     momapy.geometry.Rotation(0.5, lb.position), ) lb.group_filter = momapy.drawing.Filter(     effects=(         momapy.drawing.DropShadowEffect(             dx=3.0,             dy=3.0,             std_deviation=5.0,             flood_opacity=0.5,             flood_color=momapy.coloring.blue,         ),     ) ) l = momapy.builder.object_from_builder(lb) display(l) <p>Styles may be applied to a <code>Map</code> using a <code>StyleSheet</code>. A <code>StyleSheet</code> can be built from a text document whose syntax is a subset of the CSS syntax.</p> In\u00a0[41]: Copied! <pre>r = read(\"data/phospho1.sbgn\")\nm = r.obj\ndisplay(m)\n</pre> r = read(\"data/phospho1.sbgn\") m = r.obj display(m) <p>There are pre-built <code>StyleSheet</code> objects for SBGN-ED and Newt-like styles, for colors, and for shadows:</p> In\u00a0[42]: Copied! <pre>m = momapy.styling.apply_style_sheet(m, momapy.sbgn.styling.newt)\ndisplay(m)\n</pre> m = momapy.styling.apply_style_sheet(m, momapy.sbgn.styling.newt) display(m) <p>Applying a <code>StyleSheet</code> to a <code>Map</code> may change the size of the nodes. Some simple functions can be used to tidy the <code>Map</code>:</p> In\u00a0[43]: Copied! <pre>m = momapy.sbgn.utils.newt_tidy(m)\ndisplay(m)\n</pre> m = momapy.sbgn.utils.newt_tidy(m) display(m) In\u00a0[44]: Copied! <pre>m = momapy.styling.apply_style_sheet(m, momapy.sbgn.styling.sbgned)\nm = momapy.sbgn.utils.sbgned_tidy(m)\ndisplay(m)\n</pre> m = momapy.styling.apply_style_sheet(m, momapy.sbgn.styling.sbgned) m = momapy.sbgn.utils.sbgned_tidy(m) display(m) In\u00a0[45]: Copied! <pre>m = momapy.styling.apply_style_sheet(m, momapy.sbgn.styling.cs_default)\ndisplay(m)\n</pre> m = momapy.styling.apply_style_sheet(m, momapy.sbgn.styling.cs_default) display(m) In\u00a0[46]: Copied! <pre>m = momapy.styling.apply_style_sheet(m, momapy.sbgn.styling.fs_shadows)\ndisplay(m)\n</pre> m = momapy.styling.apply_style_sheet(m, momapy.sbgn.styling.fs_shadows) display(m) <p>These pre-built <code>StyleSheet</code>s are built from CSS-like text files:</p> In\u00a0[47]: Copied! <pre>with open(\"../../src/momapy/sbgn/styling/sbgned_no_cs.css\") as f:\n    for line in f.readlines()[:35]:\n        print(line[:-1])\n</pre> with open(\"../../src/momapy/sbgn/styling/sbgned_no_cs.css\") as f:     for line in f.readlines()[:35]:         print(line[:-1]) <pre>AndOperatorLayout {\n\tfilter: unset;\n\tstroke-dasharray: unset;\n\tstroke-dashoffset: unset;\n\tstroke-width: 2.0;\n\ttransform: unset;\n\tfilter: unset;\n\tgroup-font-family: unset;\n\tgroup-font-size: unset;\n\tgroup-font-style: unset;\n\tgroup-font-weight: unset;\n\theight: 42.0;\n\tleft-connector-filter: unset;\n\tleft-connector-length: 10.0;\n\tleft-connector-stroke-dasharray: unset;\n\tleft-connector-stroke-dashoffset: unset;\n\tleft-connector-stroke-width: unset;\n\tright-connector-filter: unset;\n\tright-connector-length: 10.0;\n\tright-connector-stroke-dasharray: unset;\n\tright-connector-stroke-dashoffset: unset;\n\tright-connector-stroke-width: unset;\n\tstroke-dasharray: unset;\n\tstroke-dashoffset: unset;\n\tstroke-width: unset;\n\ttransform: unset;\n\twidth: 42.0;\n}\n\n\nAssociationLayout {\n\tfilter: unset;\n\tstroke-dasharray: unset;\n\tstroke-dashoffset: unset;\n\tstroke-width: 2.0;\n</pre> <p>One may build a <code>StyleSheet</code> from a file and apply it to a <code>Map</code> with simple functions:</p> In\u00a0[48]: Copied! <pre>with open(\"build/my_style_sheet.css\", \"w\") as f:\n    f.write(\"\"\"\n        StateVariableLayout {\n        \tfill: royalblue;\n        \tstroke: white;\n            stroke-width: 1.5;\n            group-filter: unset;\n        }\n        \n        StateVariableLayout &gt; TextLayout {\n          font-size: 7.0;\n          fill: white;\n        }\n        \n        MacromoleculeLayout {\n        \tfill: royalblue;\n        \tstroke: white;\n            stroke-width: 2.0;\n            group-filter: unset;\n        }\n        \n        MacromoleculeLayout &gt; TextLayout {\n            font-size: 14.0;\n            fill: white;\n        }\n        \n        \n        SimpleChemicalLayout {\n        \tfill: gold;\n        \tstroke: white;\n            stroke-width: 2.0;\n            group-filter: unset;\n        }\n        \n        SimpleChemicalLayout &gt; TextLayout {\n            font-size: 10.0;\n            fill: black;\n        }\n        \n        GenericProcessLayout {\n            fill: gray;\n        \tstroke: white;\n        \tright-connector-stroke: gray;\n            right-connector-length: 10.0;\n            right-connector-stroke-width: 1.5;\n        \tleft-connector-stroke: gray;\n            left-connector-length: 10.0;\n            left-connector-stroke-width: 1.5;\n            group-filter: unset;\n        }\n        \n        ConsumptionLayout {\n        \tarrowhead-fill: white;\n        \tarrowhead-stroke: black;\n        \tpath-stroke: gray;\n            path-stroke-width: 1.5;\n            end-shorten: 1.0;\n            group-filter: unset;\n        }\n        \n        ProductionLayout {\n        \tarrowhead-fill: gray;\n        \tarrowhead-stroke: gray;\n        \tpath-stroke: gray;\n            path-stroke-width: 1.5;\n            end-shorten: 1.0;\n            group-filter: unset;\n        }\n        \n        CatalysisLayout {\n        \tarrowhead-fill: white;\n        \tarrowhead-stroke: gray;\n            arrowhead-stroke-width: 2.0;\n        \tpath-stroke: gray;\n            path-stroke-width: 1.5;\n            end-shorten: 1.0;\n            group-filter: unset;\n            stroke-width: 1.0;\n        }\n    \"\"\")\n</pre> with open(\"build/my_style_sheet.css\", \"w\") as f:     f.write(\"\"\"         StateVariableLayout {         \tfill: royalblue;         \tstroke: white;             stroke-width: 1.5;             group-filter: unset;         }                  StateVariableLayout &gt; TextLayout {           font-size: 7.0;           fill: white;         }                  MacromoleculeLayout {         \tfill: royalblue;         \tstroke: white;             stroke-width: 2.0;             group-filter: unset;         }                  MacromoleculeLayout &gt; TextLayout {             font-size: 14.0;             fill: white;         }                           SimpleChemicalLayout {         \tfill: gold;         \tstroke: white;             stroke-width: 2.0;             group-filter: unset;         }                  SimpleChemicalLayout &gt; TextLayout {             font-size: 10.0;             fill: black;         }                  GenericProcessLayout {             fill: gray;         \tstroke: white;         \tright-connector-stroke: gray;             right-connector-length: 10.0;             right-connector-stroke-width: 1.5;         \tleft-connector-stroke: gray;             left-connector-length: 10.0;             left-connector-stroke-width: 1.5;             group-filter: unset;         }                  ConsumptionLayout {         \tarrowhead-fill: white;         \tarrowhead-stroke: black;         \tpath-stroke: gray;             path-stroke-width: 1.5;             end-shorten: 1.0;             group-filter: unset;         }                  ProductionLayout {         \tarrowhead-fill: gray;         \tarrowhead-stroke: gray;         \tpath-stroke: gray;             path-stroke-width: 1.5;             end-shorten: 1.0;             group-filter: unset;         }                  CatalysisLayout {         \tarrowhead-fill: white;         \tarrowhead-stroke: gray;             arrowhead-stroke-width: 2.0;         \tpath-stroke: gray;             path-stroke-width: 1.5;             end-shorten: 1.0;             group-filter: unset;             stroke-width: 1.0;         }     \"\"\") <p>The map with no style sheet:</p> In\u00a0[49]: Copied! <pre>m = read(\"data/mek_erk.sbgn\").obj\ndisplay(m, scale=1.5)\n</pre> m = read(\"data/mek_erk.sbgn\").obj display(m, scale=1.5) <p>The map with the style sheet applied, and tided:</p> In\u00a0[50]: Copied! <pre>my_style_sheet = momapy.styling.StyleSheet.from_file(\"build/my_style_sheet.css\")\nm = momapy.styling.apply_style_sheet(m, my_style_sheet)\nm = momapy.sbgn.utils.newt_tidy(m)\ndisplay(m, scale=1.5)\n</pre> my_style_sheet = momapy.styling.StyleSheet.from_file(\"build/my_style_sheet.css\") m = momapy.styling.apply_style_sheet(m, my_style_sheet) m = momapy.sbgn.utils.newt_tidy(m) display(m, scale=1.5) <p><code>LayoutElement</code>s support automatic geometry: their \"shape\" can be automatically computed from the <code>DrawingElement</code>s they return, and be accessed with various methods. These methods depend on the nature of the <code>LayoutElement</code> (<code>Node</code> or <code>Arc</code>).</p> In\u00a0[51]: Copied! <pre>l = macromolecule_toy()\ndisplay(l)\n</pre> l = macromolecule_toy() display(l) <p><code>Node</code>s have anchor points, that are specific <code>Point</code>s on their border:</p> In\u00a0[52]: Copied! <pre>l.north_west()\n</pre> l.north_west() Out[52]: <pre>Point(x=80.0001, y=77.5)</pre> In\u00a0[53]: Copied! <pre>display(l, l.north_west())\n</pre> display(l, l.north_west()) <p>All <code>Node</code>s have at least the following anchor points:</p> <ul> <li><code>north_west</code></li> <li><code>north</code></li> <li><code>north_east</code></li> <li><code>east</code></li> <li><code>south_east</code></li> <li><code>south</code></li> <li><code>south_west</code></li> <li><code>west</code></li> <li><code>center</code></li> <li><code>label_center</code></li> </ul> In\u00a0[54]: Copied! <pre>show_room(momapy.sbgn.pd.MacromoleculeLayout)\n</pre> show_room(momapy.sbgn.pd.MacromoleculeLayout) In\u00a0[55]: Copied! <pre>show_room(momapy.sbgn.pd.GenericProcessLayout)\n</pre> show_room(momapy.sbgn.pd.GenericProcessLayout) In\u00a0[56]: Copied! <pre>show_room(momapy.sbgn.pd.NucleicAcidFeatureMultimerLayout)\n</pre> show_room(momapy.sbgn.pd.NucleicAcidFeatureMultimerLayout) <p><code>Node</code>s also have angle points:</p> In\u00a0[57]: Copied! <pre>l.own_angle(130)\n</pre> l.own_angle(130) Out[57]: <pre>Point(x=162.0675, y=75.0)</pre> In\u00a0[58]: Copied! <pre>display(l, l.own_angle(130))\n</pre> display(l, l.own_angle(130)) In\u00a0[59]: Copied! <pre>show_room(momapy.sbgn.pd.MacromoleculeLayout, \"own_angle\")\n</pre> show_room(momapy.sbgn.pd.MacromoleculeLayout, \"own_angle\") In\u00a0[60]: Copied! <pre>display(l, l.angle(130))\n</pre> display(l, l.angle(130)) In\u00a0[61]: Copied! <pre>show_room(momapy.sbgn.pd.MacromoleculeLayout, \"angle\")\n</pre> show_room(momapy.sbgn.pd.MacromoleculeLayout, \"angle\") In\u00a0[62]: Copied! <pre>l = production_toy()\ndisplay(l)\n</pre> l = production_toy() display(l) <p>Analogously to <code>Node</code>s, <code>Arc</code>s have a few anchor points:</p> In\u00a0[63]: Copied! <pre>l.arrowhead_base()\n</pre> l.arrowhead_base() Out[63]: <pre>Point(x=303.7868, y=71.2132)</pre> In\u00a0[64]: Copied! <pre>display(l, l.arrowhead_base())\n</pre> display(l, l.arrowhead_base()) <p>All <code>Arc</code>s have at least the following anchor points:</p> <ul> <li><code>end_point</code></li> <li><code>start_point</code></li> </ul> <p><code>SingleHeadedArcs</code> have the following additional anchor points:</p> <ul> <li><code>arrowhead_base</code></li> <li><code>arrowhead_tip</code></li> </ul> <p>And <code>DoubleHeadedArcs</code> have the following additional ones:</p> <ul> <li><code>start_arrowhead_base</code></li> <li><code>start_arrowhead_tip</code></li> <li><code>end_arrowhead_base</code></li> <li><code>end_arrowhead_tip</code></li> </ul> In\u00a0[65]: Copied! <pre>show_room(momapy.sbgn.pd.ProductionLayout)\n</pre> show_room(momapy.sbgn.pd.ProductionLayout) <p><code>Arc</code>s also have fraction points:</p> In\u00a0[66]: Copied! <pre>l.fraction(0.50)\n</pre> l.fraction(0.50) Out[66]: <pre>(Point(x=225.0, y=150.0), 5.497787143782138)</pre> In\u00a0[67]: Copied! <pre>pos, angle = l.fraction(0.50)\ndisplay(l, pos)\n</pre> pos, angle = l.fraction(0.50) display(l, pos) In\u00a0[68]: Copied! <pre>show_room(momapy.sbgn.pd.ProductionLayout, \"fraction\")\n</pre> show_room(momapy.sbgn.pd.ProductionLayout, \"fraction\") <p>Automatic geometry enables positioning <code>LayoutElement</code>s relatively to one another:</p> In\u00a0[69]: Copied! <pre>r = read(\"data/phospho1.sbgn\")\nm = r.obj\ndisplay(m)\n</pre> r = read(\"data/phospho1.sbgn\") m = r.obj display(m) In\u00a0[70]: Copied! <pre>mb = momapy.builder.builder_from_object(m)\nfor lb in mb.layout.layout_elements:\n    if (\n        hasattr(lb, \"label\") and lb.label is not None and lb.label.text == \"B\"\n    ):  # we select the layout for B\n        eb = lb\n    elif momapy.builder.isinstance_or_builder(\n        lb, momapy.sbgn.pd.GenericProcessLayout\n    ):  # we select the process layout\n        pb = lb\n</pre> mb = momapy.builder.builder_from_object(m) for lb in mb.layout.layout_elements:     if (         hasattr(lb, \"label\") and lb.label is not None and lb.label.text == \"B\"     ):  # we select the layout for B         eb = lb     elif momapy.builder.isinstance_or_builder(         lb, momapy.sbgn.pd.GenericProcessLayout     ):  # we select the process layout         pb = lb In\u00a0[71]: Copied! <pre>eb.position = momapy.positioning.above_left_of(\n    pb, 200, 50\n)  # eb's position is set 200 units above and 50 units left of pb's position\neb.label.position = eb.position\nmomapy.sbgn.utils.tidy(mb)  # sets the arcs to the borders\nm = momapy.builder.object_from_builder(mb)\ndisplay(m)\n</pre> eb.position = momapy.positioning.above_left_of(     pb, 200, 50 )  # eb's position is set 200 units above and 50 units left of pb's position eb.label.position = eb.position momapy.sbgn.utils.tidy(mb)  # sets the arcs to the borders m = momapy.builder.object_from_builder(mb) display(m) <p>The following functions are available:</p> <ul> <li>above_left_of</li> <li>above_of</li> <li>above_right_of</li> <li>right_of</li> <li>below_right_of</li> <li>below_of</li> <li>below_left_of</li> <li>left_of</li> </ul> <p>For container <code>Node</code>s, the <code>fit</code> function is also available:</p> In\u00a0[72]: Copied! <pre>momapy.positioning.fit(mb.layout.layout_elements, xsep=20, ysep=10)\n</pre> momapy.positioning.fit(mb.layout.layout_elements, xsep=20, ysep=10) Out[72]: <pre>Bbox(position=Point(x=237.5, y=164.0), width=453.0, height=292.0)</pre> <p>Each of the above functions has a corresponding <code>set</code> function, which directly sets the returned value(s) to the correct <code>Node</code>'s attributes:</p> In\u00a0[73]: Copied! <pre>mb.layout.stroke = momapy.coloring.red\nmomapy.positioning.set_fit(mb.layout, mb.layout.layout_elements, xsep=20, ysep=10)\nm = momapy.builder.object_from_builder(mb)\ndisplay(m)\n</pre> mb.layout.stroke = momapy.coloring.red momapy.positioning.set_fit(mb.layout, mb.layout.layout_elements, xsep=20, ysep=10) m = momapy.builder.object_from_builder(mb) display(m) <p>The <code>set</code> functions have an <code>anchor</code> option that sets the relative target anchor of the <code>Node</code> receiving the new position (default is <code>center</code>):</p> In\u00a0[74]: Copied! <pre>momapy.positioning.set_above_left_of(\n    eb, pb, 200, 50, anchor=\"south_east\"\n)  # eb's position is set such that its south_east anchor is 200 units above and 50 units left of pb's position\neb.label.position = eb.position\nmomapy.sbgn.utils.tidy(mb)\nm = momapy.builder.object_from_builder(mb)\ndisplay(m)\n</pre> momapy.positioning.set_above_left_of(     eb, pb, 200, 50, anchor=\"south_east\" )  # eb's position is set such that its south_east anchor is 200 units above and 50 units left of pb's position eb.label.position = eb.position momapy.sbgn.utils.tidy(mb) m = momapy.builder.object_from_builder(mb) display(m) <p>New types of <code>Node</code>s and <code>Arc</code>s can be easily programmed. Since the geometry is automatic, it is only required to program the general shape of the <code>LayoutElement</code> using <code>DrawingElement</code>s.</p> In\u00a0[75]: Copied! <pre>import dataclasses\n</pre> import dataclasses In\u00a0[76]: Copied! <pre>@dataclasses.dataclass(frozen=True)\nclass MyTriangle(Node):\n    height: float = 30.0\n    width: float = 30.0\n    fill: momapy.coloring.Color = momapy.coloring.white\n    stroke: momapy.coloring.Color = momapy.coloring.black\n\n    def _border_drawing_elements(self):\n        actions = [\n            momapy.drawing.MoveTo(self.position - (0, self.height / 2)),  # top\n            momapy.drawing.LineTo(\n                self.position + (self.width / 2, self.height / 2)\n            ),  # bottom right\n            momapy.drawing.LineTo(\n                self.position + (-self.width / 2, self.height / 2)\n            ),  # bottom left\n            momapy.drawing.ClosePath(),\n        ]\n        path = momapy.drawing.Path(actions=actions)\n        return [path]\n</pre> @dataclasses.dataclass(frozen=True) class MyTriangle(Node):     height: float = 30.0     width: float = 30.0     fill: momapy.coloring.Color = momapy.coloring.white     stroke: momapy.coloring.Color = momapy.coloring.black      def _border_drawing_elements(self):         actions = [             momapy.drawing.MoveTo(self.position - (0, self.height / 2)),  # top             momapy.drawing.LineTo(                 self.position + (self.width / 2, self.height / 2)             ),  # bottom right             momapy.drawing.LineTo(                 self.position + (-self.width / 2, self.height / 2)             ),  # bottom left             momapy.drawing.ClosePath(),         ]         path = momapy.drawing.Path(actions=actions)         return [path] In\u00a0[77]: Copied! <pre>show_room(MyTriangle)\n</pre> show_room(MyTriangle) In\u00a0[78]: Copied! <pre>show_room(MyTriangle, \"angle\")\n</pre> show_room(MyTriangle, \"angle\") In\u00a0[79]: Copied! <pre>@dataclasses.dataclass(frozen=True)\nclass MyRectangleArrow(SingleHeadedArc):\n    arrowhead_width: float = 10.0\n    arrowhead_height: float = 10.0\n    arrowhead_fill: momapy.coloring.Color | momapy.drawing.NoneValueType = (\n        momapy.coloring.white\n    )\n    arrowhead_stroke: momapy.coloring.Color = momapy.coloring.black\n    path_fill: momapy.coloring.Color | momapy.drawing.NoneValueType = (\n        momapy.drawing.NoneValue\n    )\n    path_stroke: momapy.coloring.Color = momapy.coloring.black\n\n    def _arrowhead_border_drawing_elements(self):\n        actions = [\n            momapy.drawing.MoveTo(\n                momapy.geometry.Point(0, 0)\n            ),  # we draw the arrowhead as if its base is at (0, 0)\n            momapy.drawing.LineTo(\n                momapy.geometry.Point(0, -self.arrowhead_height / 2)\n            ),  # top left\n            momapy.drawing.LineTo(\n                momapy.geometry.Point(self.arrowhead_width, -self.arrowhead_height / 2)\n            ),  # top right\n            momapy.drawing.LineTo(\n                momapy.geometry.Point(self.arrowhead_width, self.arrowhead_height / 2)\n            ),  # bottom right\n            momapy.drawing.LineTo(\n                momapy.geometry.Point(0, self.arrowhead_height / 2)\n            ),  # bottom left\n            momapy.drawing.ClosePath(),\n        ]\n        path = momapy.drawing.Path(actions=actions)\n        return [path]\n</pre> @dataclasses.dataclass(frozen=True) class MyRectangleArrow(SingleHeadedArc):     arrowhead_width: float = 10.0     arrowhead_height: float = 10.0     arrowhead_fill: momapy.coloring.Color | momapy.drawing.NoneValueType = (         momapy.coloring.white     )     arrowhead_stroke: momapy.coloring.Color = momapy.coloring.black     path_fill: momapy.coloring.Color | momapy.drawing.NoneValueType = (         momapy.drawing.NoneValue     )     path_stroke: momapy.coloring.Color = momapy.coloring.black      def _arrowhead_border_drawing_elements(self):         actions = [             momapy.drawing.MoveTo(                 momapy.geometry.Point(0, 0)             ),  # we draw the arrowhead as if its base is at (0, 0)             momapy.drawing.LineTo(                 momapy.geometry.Point(0, -self.arrowhead_height / 2)             ),  # top left             momapy.drawing.LineTo(                 momapy.geometry.Point(self.arrowhead_width, -self.arrowhead_height / 2)             ),  # top right             momapy.drawing.LineTo(                 momapy.geometry.Point(self.arrowhead_width, self.arrowhead_height / 2)             ),  # bottom right             momapy.drawing.LineTo(                 momapy.geometry.Point(0, self.arrowhead_height / 2)             ),  # bottom left             momapy.drawing.ClosePath(),         ]         path = momapy.drawing.Path(actions=actions)         return [path] In\u00a0[80]: Copied! <pre>show_room(MyRectangleArrow)\n</pre> show_room(MyRectangleArrow) In\u00a0[81]: Copied! <pre>show_room(MyRectangleArrow, \"fraction\")\n</pre> show_room(MyRectangleArrow, \"fraction\") <p>New <code>LayoutElement</code>s can be even more easily programmed using the <code>Shape</code>s already available in the <code>meta</code> module:</p> In\u00a0[82]: Copied! <pre>import momapy.meta.shapes\n</pre> import momapy.meta.shapes In\u00a0[83]: Copied! <pre>@dataclasses.dataclass(frozen=True)\nclass MyDoubleRectangleArrow(DoubleHeadedArc):\n    start_arrowhead_width: float = 10.0\n    start_arrowhead_height: float = 10.0\n    start_arrowhead_fill: momapy.coloring.Color | momapy.drawing.NoneValueType = (\n        momapy.coloring.white\n    )\n    start_arrowhead_stroke: momapy.coloring.Color = momapy.coloring.black\n    end_arrowhead_width: float = 20.0\n    end_arrowhead_height: float = 20.0\n    end_arrowhead_fill: momapy.coloring.Color | momapy.drawing.NoneValueType = (\n        momapy.coloring.white\n    )\n    end_arrowhead_stroke: momapy.coloring.Color = momapy.coloring.black\n    path_fill: momapy.coloring.Color | momapy.drawing.NoneValueType = (\n        momapy.drawing.NoneValue\n    )\n    path_stroke: momapy.coloring.Color = momapy.coloring.black\n\n    def _start_arrowhead_border_drawing_elements(self):\n        return momapy.meta.shapes.Rectangle(\n            position=momapy.geometry.Point(-self.start_arrowhead_width / 2, 0),\n            width=self.start_arrowhead_width,\n            height=self.start_arrowhead_height,\n        ).drawing_elements()\n\n    def _end_arrowhead_border_drawing_elements(self):\n        return momapy.meta.shapes.Rectangle(\n            position=momapy.geometry.Point(self.end_arrowhead_width / 2, 0),\n            width=self.end_arrowhead_width,\n            height=self.end_arrowhead_height,\n        ).drawing_elements()\n</pre> @dataclasses.dataclass(frozen=True) class MyDoubleRectangleArrow(DoubleHeadedArc):     start_arrowhead_width: float = 10.0     start_arrowhead_height: float = 10.0     start_arrowhead_fill: momapy.coloring.Color | momapy.drawing.NoneValueType = (         momapy.coloring.white     )     start_arrowhead_stroke: momapy.coloring.Color = momapy.coloring.black     end_arrowhead_width: float = 20.0     end_arrowhead_height: float = 20.0     end_arrowhead_fill: momapy.coloring.Color | momapy.drawing.NoneValueType = (         momapy.coloring.white     )     end_arrowhead_stroke: momapy.coloring.Color = momapy.coloring.black     path_fill: momapy.coloring.Color | momapy.drawing.NoneValueType = (         momapy.drawing.NoneValue     )     path_stroke: momapy.coloring.Color = momapy.coloring.black      def _start_arrowhead_border_drawing_elements(self):         return momapy.meta.shapes.Rectangle(             position=momapy.geometry.Point(-self.start_arrowhead_width / 2, 0),             width=self.start_arrowhead_width,             height=self.start_arrowhead_height,         ).drawing_elements()      def _end_arrowhead_border_drawing_elements(self):         return momapy.meta.shapes.Rectangle(             position=momapy.geometry.Point(self.end_arrowhead_width / 2, 0),             width=self.end_arrowhead_width,             height=self.end_arrowhead_height,         ).drawing_elements() In\u00a0[84]: Copied! <pre>show_room(MyDoubleRectangleArrow)\n</pre> show_room(MyDoubleRectangleArrow)"},{"location":"user_manual/main/#momapy-a-python-library-for-molecular-maps","title":"momapy: a python library for molecular maps\u00b6","text":""},{"location":"user_manual/main/#maps","title":"Maps\u00b6","text":""},{"location":"user_manual/main/#models","title":"Models\u00b6","text":""},{"location":"user_manual/main/#layouts","title":"Layouts\u00b6","text":""},{"location":"user_manual/main/#layoutmodelmappings","title":"LayoutModelMappings\u00b6","text":""},{"location":"user_manual/main/#equality-and-sub-relation-for-maps-models-layouts-and-layoutmodelmappings","title":"Equality and-sub relation for Maps, Models, Layouts and LayoutModelMappings\u00b6","text":""},{"location":"user_manual/main/#equality","title":"Equality\u00b6","text":""},{"location":"user_manual/main/#definition","title":"Definition\u00b6","text":""},{"location":"user_manual/main/#example","title":"Example\u00b6","text":""},{"location":"user_manual/main/#sub-mapmodellayoutmapping","title":"Sub-map/model/layout/mapping\u00b6","text":""},{"location":"user_manual/main/#definition","title":"Definition\u00b6","text":""},{"location":"user_manual/main/#example-1","title":"Example 1\u00b6","text":""},{"location":"user_manual/main/#example-2","title":"Example 2\u00b6","text":""},{"location":"user_manual/main/#frozen-and-builder-objects","title":"Frozen and builder objects\u00b6","text":""},{"location":"user_manual/main/#reading-and-writing","title":"Reading and writing\u00b6","text":""},{"location":"user_manual/main/#rendering","title":"Rendering\u00b6","text":""},{"location":"user_manual/main/#styling","title":"Styling\u00b6","text":""},{"location":"user_manual/main/#basic-styling","title":"Basic styling\u00b6","text":""},{"location":"user_manual/main/#advanced-styling","title":"Advanced styling\u00b6","text":""},{"location":"user_manual/main/#css-like-style-sheets","title":"CSS-like style sheets\u00b6","text":""},{"location":"user_manual/main/#automatic-geometry","title":"Automatic geometry\u00b6","text":""},{"location":"user_manual/main/#for-nodes","title":"For Nodes\u00b6","text":""},{"location":"user_manual/main/#anchors","title":"Anchors\u00b6","text":""},{"location":"user_manual/main/#angles","title":"Angles\u00b6","text":""},{"location":"user_manual/main/#for-arcs","title":"For Arcs\u00b6","text":""},{"location":"user_manual/main/#anchors","title":"Anchors\u00b6","text":""},{"location":"user_manual/main/#fraction","title":"Fraction\u00b6","text":""},{"location":"user_manual/main/#relative-positioning","title":"Relative positioning\u00b6","text":""},{"location":"user_manual/main/#building-new-types-of-nodes-and-arcs","title":"Building new types of Nodes and Arcs\u00b6","text":""},{"location":"user_manual/main/#new-node","title":"New Node\u00b6","text":""},{"location":"user_manual/main/#new-arc","title":"New Arc\u00b6","text":""},{"location":"user_manual/main/#ongoing-and-future-work","title":"Ongoing and future work\u00b6","text":""},{"location":"user_manual/main/#ongoing-work","title":"Ongoing work\u00b6","text":"<ul> <li>Support for SBML layout/render</li> </ul>"},{"location":"user_manual/main/#future-work","title":"Future work\u00b6","text":"<ul> <li>Improve performance of geometry (currently slow)</li> <li>Support for background images and gradients in layouts</li> <li>Support for SBGN ER maps?</li> <li>Support for BioPAX models?</li> <li>Automatic XML/JSON format</li> <li>Developpment of converters</li> </ul>"}]}